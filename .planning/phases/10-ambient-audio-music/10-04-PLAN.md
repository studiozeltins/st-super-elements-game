---
phase: 10-ambient-audio-music
plan: 04
type: execute
wave: 3
depends_on: ['10-02', '10-03']
files_modified:
  - src/game/audio/createMovementAudio.ts
  - src/game/createGame.ts
autonomous: true
requirements: [AMBI-04]

must_haves:
  truths:
    - "The local player moving over grass emits a soft grass-rustle noise burst layered on footsteps, routed through the sfx bus (AMBI-04, D-06)"
    - "Rustle is procedural (filtered-noise burst), movement-tied, and silent when the player is not moving over grass"
    - "The rustle respects the existing global footstep spam budget (a crowded frame never turns to rain)"
  artifacts:
    - src/game/audio/createMovementAudio.ts
  key_links:
    - "createGame player-step call (line ~1246) passes an onGrass surface flag into movementAudio"
    - "Rustle routes through getSfxBus() (the Wave-1 migration seam), not the raw destination"
---

<objective>
Grass rustle (AMBI-04, D-06): the local player hears a soft procedural grass-rustle burst layered on
footsteps while moving over grass. Stays procedural (movement-tied filtered-noise burst, the existing
footstep idiom) and routes through the sfx bus migrated in Wave 1.

Purpose: A cheap, movement-tied ambience touch that makes traversal feel grounded, reusing the
`createNoiseSource`→lowpass burst recipe already in this module and the existing per-frame player-step
call site — no new subsystem, no GPU readback.
Output: A rustle layer in `createMovementAudio` + the one createGame line that flags grass movement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ambient-audio-music/10-CONTEXT.md
@.planning/phases/10-ambient-audio-music/10-RESEARCH.md
@src/game/audio/createMovementAudio.ts
@src/game/audio/audioCore.ts
</context>

<artifacts>
This plan produces:
- `createMovementAudio.ts`: an optional `surface` argument on the player locomotion path + a `playGrassRustle`-style filtered-noise burst layered on player steps over grass (routed via getSfxBus, under the existing spam budget)
- `createGame.ts`: the player-step call (~line 1246) passes an `onGrass` flag derived from a cheap already-available signal
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Grass-rustle burst on player movement over grass (AMBI-04, D-06)</name>
  <files>src/game/audio/createMovementAudio.ts, src/game/createGame.ts</files>
  <read_first>
    - src/game/audio/createMovementAudio.ts lines 57-102 (the player-step path, `underSpamBudget` guard, `playPlayerStep` noise-burst recipe to mirror; note this module now takes getSfxBus after the Wave-1 migration)
    - src/game/audio/audioCore.ts (createNoiseSource, jitter, panned)
    - src/game/createGame.ts lines 936-1011 (isMoving derivation) and 1243-1248 (grounded player-step call site)
  </read_first>
  <action>
    In `createMovementAudio`, add a grass-rustle burst for the LOCAL player: a short lowpassed
    createNoiseSource burst in a distinct, softer band than the footstep tap (a wider, breathier rustle,
    peak well below the step), pitch-jittered via `jitter`, panned via `panned`, routed to `getSfxBus()`
    (the Wave-1 seam — NOT the raw destination). Layer it onto the player footstep emission but gate it on a
    new optional `surface?: 'grass'` parameter threaded to the player locomotion path (extend `updateUnit`
    with the optional `surface` arg, used only for the player kind; other callers unaffected). Reuse the
    existing `underSpamBudget` guard so rustle shares the global footstep budget and never floods. In
    `createGame`, at the grounded player-step call (~line 1246), pass `surface: 'grass'` (via the extended
    signature) when the player is moving over grass — derive `onGrass` from a cheap already-available signal
    (e.g. the player is grounded on walkable island terrain and `isMoving`); do NOT read any GPU texture or
    add a per-frame allocation (client-perf rules). When not moving over grass, pass nothing so rustle is
    silent.
  </action>
  <verify>
    <automated>npx vitest run 2&gt;&amp;1 | tail -3 &amp;&amp; grep -q "getSfxBus" src/game/audio/createMovementAudio.ts &amp;&amp; grep -Eq "surface|rustle|Rustle" src/game/audio/createMovementAudio.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -c "createMovementAudio\|createGame" | grep -qx 0 &amp;&amp; echo RUSTLE_OK</automated>
  </verify>
  <acceptance_criteria>
    - A grass-rustle noise burst plays on the player footstep path when onGrass, silent otherwise
    - Rustle routes through getSfxBus() and respects underSpamBudget
    - Full vitest suite green; tsc clean; no GPU readback / per-frame alloc added
    - Manual (phase playtest): moving over grass produces an audible soft rustle under the footsteps
  </acceptance_criteria>
  <done>The player hears procedural grass rustle while moving over grass, on the sfx bus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Procedural synthesis only — no input, no persistence, no network, no assets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | (N/A) | grass rustle synth | low | accept | No attack surface: procedural WebAudio burst, gesture-gated, never throws mid-frame (ready-guard). No untrusted input. |

Honest assessment: pure procedural SFX on an existing bus. Nothing to mitigate.
</threat_model>

<verification>
- `npx vitest run` full suite green; tsc clean
- grep confirms rustle routes through getSfxBus and reuses the spam budget
- createMovementAudio.ts stays ≤300 LOC functional
</verification>

<success_criteria>
Moving over grass layers a soft procedural rustle on the player's footsteps through the sfx bus, gated
cheaply on grass movement with no GPU readback or per-frame allocation.
</success_criteria>

<output>
Create `.planning/phases/10-ambient-audio-music/10-04-SUMMARY.md` when done.
</output>
