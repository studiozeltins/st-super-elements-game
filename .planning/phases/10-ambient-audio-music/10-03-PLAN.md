---
phase: 10-ambient-audio-music
plan: 03
type: execute
wave: 2
depends_on: ['10-01', '10-02']
files_modified:
  - src/game/audio/createSampleCache.ts
  - src/game/audio/createAmbience.ts
  - src/game/createGame.ts
  - public/audio/ASSETS-LICENSES.md
autonomous: false
requirements: [AMBI-02, AMBI-03, AMBI-05, AMBI-06, AMBI-07]

must_haves:
  truths:
    - "A continuous procedural wind bed plays on the ambient bus and its inner gain swells with wind.getGustEnvelope() every frame, audible on its own between gusts (AMBI-02, D-05)"
    - "Randomized bird one-shots fire at jittered 5-15s intervals with pitch/pan/volume jitter, never a fixed interval (AMBI-03, D-10)"
    - "Ambience varies by time of day — birds active by day, crickets/owl active at night — gated on phase01, no second time source (AMBI-07, D-11)"
    - "Distant goliath grunts fire on long random intervals with gain scaled by nearest-camp proximity (AMBI-05)"
    - "ONE combat-state signal (lastCombatAt stamp + isInCombat) is derived in the frame loop; birds stop entirely while inCombat (AMBI-06 birds-stop half, D-08)"
    - "Creature samples decode once via createSampleCache and are reused per one-shot — no per-play decode (D-15)"
  artifacts:
    - src/game/audio/createSampleCache.ts
    - src/game/audio/createAmbience.ts
    - public/audio/ASSETS-LICENSES.md
  key_links:
    - "createGame frame: const inCombat = isInCombat(elapsedSeconds, lastCombatAt) — consumed by ambience now and by duck+music in 10-06 (D-08)"
    - "ambience.update(deltaSeconds, wind.getGustEnvelope(), phase01(serverClock.nowMicros()), inCombat) inserted right after daynight.update()"
    - "createAmbience attaches to buses.ambient(); creature buffers come from createSampleCache"
---

<objective>
The ambience bed + creature one-shots + the ONE combat-state signal (AMBI-02/03/05/06-birds/07,
D-05/D-08/D-09/D-10/D-11/D-15). Build the decode-once `createSampleCache` loader, the `createAmbience`
module (a procedural gust-reactive wind bed whose inner gain sidechains `wind.getGustEnvelope()`, plus
ONE reusable `scheduleRandomOneShots` driving day birds / night crickets+owl / distant goliath grunt),
and wire it all into `createGame`: construct on the ambient bus, stamp `lastCombatAt` in the three
existing damage callbacks, and fan the derived `isInCombat` into `ambience.update(...)` each frame so
birds stop entirely during combat.

Purpose: The core "world sounds alive" bed, and the DRY combat signal both this plan (birds-stop) and
10-06 (bed-duck + music) consume. Recordings-first per D-04 (synth is the fallback); the actual creature
`.ogg` files are staged at a human checkpoint.
Output: `createSampleCache.ts` + `createAmbience.ts` + createGame wiring + the ASSETS-LICENSES scaffold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ambient-audio-music/10-CONTEXT.md
@.planning/phases/10-ambient-audio-music/10-RESEARCH.md
@.planning/phases/10-ambient-audio-music/10-PATTERNS.md
@src/game/audio/audioCore.ts
@src/game/systems/createWind.ts
@src/game/world/camps.ts
</context>

<artifacts>
This plan produces:
- `createSampleCache.ts` → `createSampleCache(getContext)` returning `{ preload(url), get(url): AudioBuffer | null, ready(): boolean }` — eager fetch of ArrayBuffer, deferred decodeAudioData until a live running context, cache keyed by url AND context (rebuild on context change)
- `createAmbience.ts` → `createAmbience(getContext, getAmbientBus, sampleCache, getCampDistance)` returning `{ update(dt, gust, phase01, inCombat), dispose() }` — procedural wind bed (loop noise→lowpass→inner swell gain) + `scheduleRandomOneShots` for birds/crickets+owl/grunt
- `createGame.ts`: `let lastCombatAt`, stamp in 3 damage callbacks, `import { phase01 }`, `import { isInCombat }`, construct sampleCache + ambience, per-frame `ambience.update(...)`
- `public/audio/{ambient,music,creatures}/` directories + `public/audio/ASSETS-LICENSES.md` scaffold (D-16)
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: createSampleCache decode-once loader + audio asset dirs + ASSETS-LICENSES scaffold (D-14/D-15/D-16)</name>
  <files>src/game/audio/createSampleCache.ts, public/audio/ASSETS-LICENSES.md</files>
  <read_first>
    - src/game/audio/createCombatAudio.ts lines 61-73 (the `ready()` context gate + `bus()` rebuild-per-context idiom to mirror)
    - .planning/phases/10-ambient-audio-music/10-RESEARCH.md §Pitfall 1 (async decode vs gesture-unlock race — the exact hazard this module handles)
    - .planning/phases/10-ambient-audio-music/10-PATTERNS.md §createSampleCache.ts (no in-repo analog — native fetch/decodeAudioData + rebuild-per-context)
  </read_first>
  <action>
    Create `src/game/audio/createSampleCache.ts` (≤300 LOC). `createSampleCache(getContext)` fetches each
    requested url's ArrayBuffer eagerly (network is context-free) and caches it, then decodes to an AudioBuffer
    lazily the first time a live running context is available (guard with the shared `ready()` idiom:
    context && context.state === 'running'), caching the decoded buffer keyed by url AND context — rebuild the
    decode if the context object changes (mirror `bus()`), because the gesture unlock is async (Pitfall 1).
    Expose `preload(url)`, `get(url): AudioBuffer | null` (null until decoded — callers skip silently, never
    throw mid-frame), and `ready()`. Never decode per play (D-15). Also create the asset directory scaffold:
    `public/audio/ambient/`, `public/audio/music/`, `public/audio/creatures/` (add a `.gitkeep` so empty dirs
    commit), and write `public/audio/ASSETS-LICENSES.md` with a provenance table (columns: filename, layer,
    source URL, license) pre-seeded with the expected filenames (birds, crickets, owl, optional grunt for
    creatures; region + combat for music) and a header stating CC0 / YouTube Audio Library only, never ripped
    copyrighted tracks (D-07/D-16). Leave the source/license cells as TODO placeholders to be filled at asset drop.
  </action>
  <verify>
    <automated>test -f public/audio/ASSETS-LICENSES.md &amp;&amp; test -d public/audio/creatures &amp;&amp; test -d public/audio/music &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -c createSampleCache | grep -qx 0 &amp;&amp; echo CACHE_OK</automated>
  </verify>
  <acceptance_criteria>
    - createSampleCache.ts compiles; decodes at most once per (url, context); get() returns null before decode
    - public/audio/{ambient,music,creatures}/ exist and commit; ASSETS-LICENSES.md has the provenance table + CC0-only header
    - No decodeAudioData call happens at construction (deferred to a running context)
  </acceptance_criteria>
  <done>A decode-once sample cache exists and the CC0-tracked asset pipeline is scaffolded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createAmbience — gust-reactive wind bed + reusable one-shot scheduler for creatures (AMBI-02/03/05/07, D-05/D-10/D-11)</name>
  <files>src/game/audio/createAmbience.ts</files>
  <read_first>
    - src/game/audio/ambienceMath.ts (bedGainTarget, nextOneShotDelay, jitterFactor, gruntProximityGain, isBirdTime, isNightCreatureTime — all timing/gain math lives here; the wrapper only touches WebAudio)
    - src/game/audio/createAudioSystem.ts lines 101-112 (the createNoiseSource→biquad lowpass→gain recipe to reuse for the bed)
    - src/game/audio/audioCore.ts (createNoiseSource, jitter, panned, clampGain)
    - src/game/systems/createWind.ts lines 33-37,64-66 (getGustEnvelope contract — hold the wind object, read fresh each frame; never cache .value)
    - .planning/phases/10-ambient-audio-music/10-RESEARCH.md §Pattern 2 + §Pattern 3 + §Pitfall 5
  </read_first>
  <behavior>
    - The bed's inner swell gain target = bedGainTarget(gust) from ambienceMath, applied via setTargetAtTime (zipper-free); bed audible at gust=0
    - Bird layer fires only when isBirdTime(phase01) AND not inCombat; crickets/owl only when isNightCreatureTime(phase01); grunt only when camp distance yields gruntProximityGain &gt; 0
    - One-shot delays come from nextOneShotDelay(min,max,Math.random()); each shot's playbackRate/pan/gain use jitterFactor — never a fixed interval or pitch
    - A gated-off layer (wrong time of day / in combat) schedules no audio and allocates nothing per frame
  </behavior>
  <action>
    Create `src/game/audio/createAmbience.ts` (≤300 LOC).
    `createAmbience(getContext, getAmbientBus, sampleCache, getCampDistance: () =&gt; number)` returns
    `{ update(deltaSeconds, gust, phase01, inCombat), dispose() }`.
    Wind bed (D-05, procedural): a looping createNoiseSource → lowpass biquad → an INNER swell GainNode →
    the ambient bus. On each `update`, set the inner gain toward `bedGainTarget(gust)` via
    `setTargetAtTime` (~0.15s τ) so it swells with the live gust envelope. Keep this swell on the bed's OWN
    inner node, separate from the ambient BUS gain that 10-06 ducks (Pitfall 5 — two writers on one param
    stomp each other). Build lazily on the first update with a running context (rebuild per context).
    One-shot scheduler (D-10, SRP): implement ONE `scheduleRandomOneShots({ active, gainFor, buffers })`
    helper that self-reschedules off an internal timer (setTimeout, NOT the frame loop) — when it fires, if
    `active()` is true it plays a decoded buffer from sampleCache as a short-lived AudioBufferSourceNode with
    `playbackRate.value = jitterFactor(0.15, Math.random())`, a `panned()` node, and a per-shot gain
    (jittered + scaled by `gainFor()`), routed to the ambient bus; then it schedules the next fire at
    `nextOneShotDelay(min,max,Math.random())`. Instantiate three layers: birds (active = isBirdTime(phase01)
    && !inCombat — birds STOP in combat, AMBI-06), crickets+owl (active = isNightCreatureTime(phase01)),
    goliath grunt (active = gruntProximityGain(getCampDistance()) &gt; 0, gain scaled by that proximity, long
    intervals). Store the latest phase01/inCombat from `update` so the timer-driven `active()` reads current
    state. If a creature buffer is absent (not yet dropped), the layer silently no-ops (recordings are the
    default per D-04; a synth fallback for grunt is allowed per D-06 — planner leaves grunt synth-or-recording
    to executor discretion, recording preferred). Zero per-frame allocation in `update` (only AudioParam
    writes + reading the wind object). `dispose()` clears timers and stops the bed source.
  </action>
  <verify>
    <automated>npx vitest run src/game/audio &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -c createAmbience | grep -qx 0 &amp;&amp; echo AMBIENCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - createAmbience.ts compiles, ≤300 LOC functional, imports its timing/gain math from ambienceMath
    - The bed swell node is distinct from the ambient bus node (grep shows a separate inner GainNode)
    - One-shot scheduling uses setTimeout + nextOneShotDelay (not requestAnimationFrame); layers gate via active()
    - `update()` contains no `new`/allocation (zero-alloc per-frame rule); reads the wind gust value fresh
  </acceptance_criteria>
  <done>A gust-reactive procedural bed + a DRY, gated, jittered creature one-shot scheduler exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: createGame integration — combat-state stamp + per-frame ambience update (D-08/D-09, AMBI-06)</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts lines 346 (serverClock), 418-427 (audio construction), 1356-1367 (frame loop, after daynight.update), 1725-1760 (the 3 damage callbacks: spawnSelfNumber/spawnWorldNumber/spawnPlayerNumber)
    - src/game/world/camps.ts (getCampSites — static {x,z} array for proximity)
    - .planning/phases/10-ambient-audio-music/10-PATTERNS.md §createGame.ts (stamp sites + frame wiring + camp proximity)
  </read_first>
  <action>
    Import `isInCombat` from `./audio/combatState`, `phase01` from `./systems/dayNightMath`, and
    `createSampleCache`/`createAmbience` from their modules, and `getCampSites` if not already in scope.
    Declare `let lastCombatAt = -Infinity;` in createGame scope (near `elapsedSeconds`). Stamp
    `lastCombatAt = elapsedSeconds` in the combat-relevant branch of the three damage callbacks: in
    `spawnSelfNumber` when `kind !== 'heal'` (I took damage), in `spawnWorldNumber` when `isMine === true`
    (my hit landed), and in `spawnPlayerNumber` when `isMine === true` (my PVP hit) — MY combat only, so a
    spectated far fight does not duck my exploration (RESEARCH open-question A4 recommendation; playtest-tunable).
    Construct `const sampleCache = createSampleCache(audioSystem.getContext);` and
    `const ambience = createAmbience(audioSystem.getContext, buses.ambient, sampleCache, () =&gt; minCampDistance);`
    where `minCampDistance` is the cheap min over `getCampSites()` to `playerPosition` (small fixed array, no
    live table scan — AMBI-05). In `frame()` immediately AFTER `daynight.update()` (line 1367) add:
    `const inCombat = isInCombat(elapsedSeconds, lastCombatAt);` then
    `ambience.update(deltaSeconds, wind.getGustEnvelope(), phase01(serverClock.nowMicros()), inCombat);`.
    Add `ambience.dispose()` to the game's existing dispose path. Do NOT add the bed-duck or music here — 10-06
    consumes the same `inCombat`. (This computes the ONE combat signal, D-08.)
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; grep -q "lastCombatAt = elapsedSeconds" src/game/createGame.ts &amp;&amp; grep -q "ambience.update(" src/game/createGame.ts &amp;&amp; grep -q "isInCombat(elapsedSeconds" src/game/createGame.ts &amp;&amp; echo FRAME_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - lastCombatAt is stamped in exactly the MY-combat branch of all 3 damage callbacks
    - The frame computes inCombat once and passes it (plus gust + phase01) to ambience.update, right after daynight.update()
    - Full vitest suite green; tsc clean; ambience.dispose() wired into teardown
    - Manual (phase playtest): bird chirps audible by day, stop during a fight; bed swells with visible gusts
  </acceptance_criteria>
  <done>The ambience bed + creatures are live and gated on the DRY combat signal; birds stop in combat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Stage + license CC0 creature/ambient recordings (D-04/D-07/D-16)</name>
  <action>Human-verify gate — no code to write. The executor pauses here after Tasks 1-3 land and the build is green; the human sources CC0 creature recordings, fills the license table, and verifies the day/night one-shots by ear, then replies with the resume signal. Recordings are the user directive default (D-04); until dropped the layers no-op / use the grunt synth fallback, so this is verify-and-populate, not blocking-the-build.</action>
  <what-built>Creature + ambient recordings pipeline: createSampleCache decode-once loader, the
  scheduler that plays them, and public/audio/{ambient,creatures}/ + ASSETS-LICENSES.md. Recordings are
  the default per the user directive (D-04); until dropped, the scheduler no-ops (or grunt uses its synth
  fallback per D-06).</what-built>
  <how-to-verify>
    1. Drop CC0 / YouTube-Audio-Library `.ogg` recordings into `public/audio/creatures/` (bird chirp(s),
       crickets loop or one-shots, owl hoot) and any ambient bed sample into `public/audio/ambient/`.
    2. Fill each row of `public/audio/ASSETS-LICENSES.md` with filename, source URL, and license (CC0 or
       YT Audio Library ONLY — never ripped copyrighted tracks, D-07).
    3. `npm run build`, open the LAN page, click to unlock audio, wait in daytime: confirm randomized bird
       chirps (varied interval + pitch), then verify at night crickets/owl replace them.
    4. If you choose to skip sourcing now, confirm the game still runs silently on those layers (synth
       fallback / no-op) with no console errors — recordings can be added later without code changes.
  </how-to-verify>
  <resume-signal>Type "approved" (assets dropped + licensed) or "defer-assets" (ship with synth/no-op fallback for now), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/audio/*.ogg → decodeAudioData | First-party CC0 assets committed to the repo and served same-origin; no runtime fetch of third-party URLs at play time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-A | Tampering | .ogg assets | low | accept | Assets are first-party, committed, same-origin; no user-supplied audio. decodeAudioData failure is caught and the layer no-ops (never throws mid-frame). |
| T-10-03-L | (legal, not security) | asset licensing | low | mitigate | D-07/D-16 provenance in ASSETS-LICENSES.md; CC0 / YouTube Audio Library only. Enforced at the blocking human checkpoint. |
| T-10-03-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs this phase (native Web Audio + static .ogg). No registry surface. |

Honest assessment: no auth, no network at play time, no untrusted input. The only real items are
first-party asset provenance (licensing) and defensive decode-failure no-op.
</threat_model>

<verification>
- `npx vitest run` full suite green (ambienceMath/combatState twins still pass)
- tsc clean; createSampleCache.ts + createAmbience.ts each ≤300 LOC functional
- grep confirms the frame computes `isInCombat` once and drives `ambience.update`
- Human checkpoint: creature recordings dropped + licensed OR explicitly deferred to synth/no-op fallback
</verification>

<success_criteria>
A continuous gust-reactive wind bed plus randomized, time-of-day-gated, combat-aware (birds-stop)
creature one-shots play on the ambient bus, all driven by the ONE combat-state signal, with samples
decoded once and provenance tracked.
</success_criteria>

<output>
Create `.planning/phases/10-ambient-audio-music/10-03-SUMMARY.md` when done.
</output>
