---
phase: 10-ambient-audio-music
plan: 06
type: execute
wave: 4
depends_on: ['10-03', '10-04']
files_modified:
  - src/game/audio/createMusic.ts
  - src/game/createGame.ts
  - public/audio/ASSETS-LICENSES.md
autonomous: false
requirements: [AMBI-06, MUSIC-01, MUSIC-02]

must_haves:
  truths:
    - "A region exploration music loop plays seamlessly on the music bus at ambient-friendly volume (MUSIC-01, D-12)"
    - "Combat music crossfades in when combat starts and back out when it ends, equal-power, no hard cut, on the SAME combat signal as the ambience (MUSIC-02, D-08/D-12)"
    - "Entering combat ducks the ambient AND music buses -6..-12dB over ~1s and restores over ~2-3s after combat ends (AMBI-06 bed-duck half, D-12 fade timings)"
    - "Both loops decode once via createSampleCache and start on first unlock; only their gains move (no start/stop churn or re-sync drift)"
  artifacts:
    - src/game/audio/createMusic.ts
  key_links:
    - "createGame frame consumes the SAME inCombat computed in 10-03 to call buses.duck(inCombat) + music.setCombat(inCombat)"
    - "createMusic attaches to buses.music(); region + combat buffers come from createSampleCache"
    - "public/audio/music/region.ogg + combat.ogg staged + licensed (blocking — no synth fallback for music)"
---

<objective>
The combat-aware capstone (MUSIC-01/02, AMBI-06 bed-duck, D-08/D-12). Build `createMusic` — two seamless
looping `AudioBufferSourceNode`s (region + combat) on the music bus with an equal-power crossfade driven
by the combat state — and wire the frame loop to duck the ambient+music buses and cross the music on the
SAME `inCombat` signal the ambience already reads (D-08). Then the phase's audio-by-ear verification: a
blocking checkpoint to source the two CC0 music loops (MUSIC has no synth fallback) and the final
golem-fight FPS + duck/crossfade/day-night A/B playtest.

Purpose: Close the "combat-aware music + ambience" loop and gate the phase on a real headphones playtest
and the milestone FPS harness. This is the last plan; it owns the final human sign-off.
Output: `createMusic.ts` + createGame duck/crossfade wiring + music-asset checkpoint + playtest checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ambient-audio-music/10-CONTEXT.md
@.planning/phases/10-ambient-audio-music/10-RESEARCH.md
@.planning/phases/10-ambient-audio-music/10-PATTERNS.md
@src/game/audio/createCombatAudio.ts
</context>

<artifacts>
This plan produces:
- `createMusic.ts` → `createMusic(getContext, getMusicBus, sampleCache)` returning `{ setCombat(inCombat: boolean), dispose() }` — region + combat AudioBufferSourceNodes (loop=true, loopStart/loopEnd), each → its own GainNode → music bus; equal-power (cos/sin) crossfade moved via setTargetAtTime (down ~1s, up ~2-3s); lazy start on first unlock, rebuild per context
- `createGame.ts`: construct createMusic on buses.music; per-frame `buses.duck(inCombat)` + `music.setCombat(inCombat)` using the inCombat already computed in 10-03; music.dispose() in teardown
- `public/audio/music/region.ogg` + `combat.ogg` staged + licensed in ASSETS-LICENSES.md
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: createMusic — two seamless loops + equal-power crossfade (MUSIC-01/02, D-12)</name>
  <files>src/game/audio/createMusic.ts</files>
  <read_first>
    - src/game/audio/createCombatAudio.ts lines 66-104 (the lazy build-once-per-context `bus()` idiom for lazy start + the ramp idiom; note Phase-10 uses setTargetAtTime at second scale, not linearRamp)
    - .planning/phases/10-ambient-audio-music/10-RESEARCH.md §Pattern 4 + §Pitfall 3 (equal-power cos/sin crossfade + loop-boundary click) + §Pitfall 4
    - .planning/phases/10-ambient-audio-music/10-PATTERNS.md §createMusic.ts (no in-repo loop precedent)
  </read_first>
  <action>
    Create `src/game/audio/createMusic.ts` (≤300 LOC). `createMusic(getContext, getMusicBus, sampleCache)`
    returns `{ setCombat(inCombat: boolean), dispose() }`. On the first `setCombat` call with a live running
    context (defer, like `bus()`; rebuild per context — Pitfall 1), lazily create two persistent
    `AudioBufferSourceNode`s from the decoded region + combat buffers (via sampleCache), each with
    `loop = true` and explicit `loopStart`/`loopEnd` (past any lead-in so the seam is clean — Pitfall 3), each
    feeding its own GainNode into the music bus, and start both simultaneously so they stay phase-locked; only
    their gains ever move (no start/stop churn, no re-sync drift). `setCombat(inCombat)` drives an equal-power
    crossfade: region gain toward `cos(x·½π)` and combat gain toward `cos((1-x)·½π)` where x is the target
    (0 = region, 1 = combat), moved via `setTargetAtTime` — down τ so combat swells in over ~1s, restore τ so
    region returns over ~2-3s (align with the duck, D-12), never a per-frame `.value=` step (Pitfall 4).
    Region plays at an ambient-friendly base level (MUSIC-01). If a buffer is not yet decoded, no-op silently
    (music has NO synth fallback — it simply waits for the staged asset). `dispose()` stops both sources.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -c createMusic | grep -qx 0 &amp;&amp; grep -q "loop" src/game/audio/createMusic.ts &amp;&amp; grep -q "setTargetAtTime" src/game/audio/createMusic.ts &amp;&amp; echo MUSIC_OK</automated>
  </verify>
  <acceptance_criteria>
    - createMusic.ts compiles, ≤300 LOC functional; two loop=true sources with explicit loopStart/loopEnd
    - Crossfade uses equal-power cos/sin targets moved via setTargetAtTime; no per-frame `.value=`
    - Both sources start together; only gains move; missing buffer no-ops (no synth fallback)
  </acceptance_criteria>
  <done>Region + combat music loops crossfade equal-power on the music bus, driven by setCombat.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: createGame wiring — construct music + per-frame duck + crossfade on the shared signal (AMBI-06 bed-duck, MUSIC-02, D-08)</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts lines 418-427 (audio construction — buses/sampleCache/ambience now present) and 1356-1367 + the inserted inCombat line from 10-03
    - .planning/phases/10-ambient-audio-music/10-PATTERNS.md §createGame.ts (per-frame fan-out)
  </read_first>
  <action>
    Import `createMusic` and construct `const music = createMusic(audioSystem.getContext, buses.music,
    sampleCache);` next to the ambience construction (reusing the sampleCache from 10-03). In `frame()`, at
    the block where 10-03 already computes `const inCombat = isInCombat(elapsedSeconds, lastCombatAt);` and
    calls `ambience.update(...)`, add the other two consumers of the SAME signal (D-08): `buses.duck(inCombat);`
    (ducks the ambient + music buses -6..-12dB down ~1s / restore ~2-3s — the AMBI-06 bed-duck half) and
    `music.setCombat(inCombat);` (equal-power crossfade region↔combat, MUSIC-02). Do NOT recompute or
    re-derive combat state — consume the one already computed. Add `music.dispose()` to the game's dispose
    path. Confirm the duck writes the ambient BUS gain while the bed swell (10-03) writes the bed's inner node
    — two different nodes, so they compose and do not stomp (Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run 2&gt;&amp;1 | tail -3 &amp;&amp; grep -q "buses.duck(inCombat)" src/game/createGame.ts &amp;&amp; grep -q "music.setCombat(inCombat)" src/game/createGame.ts &amp;&amp; grep -q "createMusic(" src/game/createGame.ts &amp;&amp; echo MUSIC_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - createGame constructs music on buses.music (shared sampleCache) and disposes it
    - The frame calls buses.duck(inCombat) and music.setCombat(inCombat) using the single inCombat from 10-03 (no re-derivation)
    - Full vitest suite green; tsc clean
  </acceptance_criteria>
  <done>Combat ducks the ambience+music buses and crossfades combat music, all on the one combat signal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Source + license the two CC0 music loops (MUSIC-01/02, no synth fallback)</name>
  <action>Human-verify gate — no code to write. The executor pauses after Tasks 1-2 land and the build is green; the human sources the two seamless CC0 music loops, fills the license rows, and confirms the region loop plays without a seam tick, then replies with the resume signal. MUSIC-01/02 have no synth fallback, so the loops must exist for those requirements to pass.</action>
  <what-built>createMusic (region + combat seamless loops + equal-power crossfade) wired to the combat
  signal + the bed/music duck. MUSIC-01/02 have NO synth fallback — the two loops must be sourced.</what-built>
  <how-to-verify>
    1. Drop CC0 / YouTube-Audio-Library seamless loops `public/audio/music/region.ogg` (calm exploration)
       and `public/audio/music/combat.ogg` (tension), authored/trimmed as TRUE loops (zero-crossing
       boundaries; set loopStart/loopEnd if there's lead-in) — Pitfall 3.
    2. Fill the two music rows of `public/audio/ASSETS-LICENSES.md` with filename, source URL, license
       (CC0 / YT Audio Library ONLY — never ripped copyrighted tracks, D-07).
    3. `npm run build`, open the LAN page, unlock audio: confirm the region loop plays seamlessly (no tick
       at the seam) at an ambient-friendly volume.
  </how-to-verify>
  <resume-signal>Type "approved" once region.ogg + combat.ogg are staged, licensed, and loop cleanly.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final golem-fight FPS gate + duck/crossfade/day-night/persist playtest</name>
  <action>Human-verify gate — no code to write. The executor pauses after the music loops are staged (Task 3) and the build is green; the human runs the FPS harness and the by-ear checks below, then replies with the resume signal. Not auto-approvable — FPS-under-load, duck timing, equal-power crossfade, gust sync, and day/night creature swap can only be verified in a live golem fight with headphones.</action>
  <what-built>The full Phase-10 audio stack: bus/compressor, gust-reactive bed, day/night creatures,
  grass rustle, combat duck + music crossfade, and the MUSIC-03 settings. Final by-ear + FPS gate.</what-built>
  <how-to-verify>
    1. FPS gate: run `python scripts/fps_playtest.py` in a golem-class fight with ALL ambiance enabled;
       confirm no FPS regression vs. the pre-phase baseline (the milestone-wide perf rule — summed cost).
    2. Duck + crossfade: enter combat → within ~1s the bed ducks −6..−12dB, birds STOP, and combat music
       crossfades IN (no hard cut); leave combat → after the ~5s cooldown the bed restores over ~2-3s and
       region music crosses back.
    3. Gust sync: watch the flags/grass gust and confirm the wind bed swells with the visible gusts.
    4. Day/night: across a cycle (or time override) confirm birds by day, crickets/owl at night.
    5. Clip check: trigger a dense fight and confirm no audible clipping (master compressor holds).
    6. Settings: set music/SFX sliders + mutes, reload → persisted; muting music leaves SFX audible.
    7. A/B any relevant `?no*` bisect behavior and confirm no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all pass, or list the failing checks (they route to gap closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/audio/music/*.ogg → decodeAudioData | First-party CC0 music loops, committed, same-origin; no runtime third-party fetch at play time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06-A | Tampering | music .ogg assets | low | accept | First-party, committed, same-origin; decode failure no-ops (never throws mid-frame). No user-supplied audio. |
| T-10-06-L | (legal, not security) | music licensing | low | mitigate | D-07/D-16 provenance in ASSETS-LICENSES.md; CC0 / YouTube Audio Library only. Enforced at the blocking music-asset checkpoint. |
| T-10-06-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs (native Web Audio + static .ogg). No registry surface. |

Honest assessment: no auth, no network at play time, no untrusted input. Real items are first-party
music provenance (licensing) and defensive decode-failure no-op.
</threat_model>

<verification>
- `npx vitest run` full suite green; tsc clean; createMusic.ts ≤300 LOC functional
- grep confirms the frame fans the ONE inCombat into duck + music.setCombat (no re-derivation)
- Blocking checkpoints: music loops staged + licensed; final FPS + duck/crossfade/day-night/persist playtest passed
</verification>

<success_criteria>
Combat ducks the ambience (bed −6..−12dB, birds stopped) and crossfades combat music in/out on one
combat signal with no hard cuts; region + combat CC0 loops play seamlessly on the music bus; the golem
fight holds FPS with all ambiance enabled.
</success_criteria>

<output>
Create `.planning/phases/10-ambient-audio-music/10-06-SUMMARY.md` when done.
</output>
