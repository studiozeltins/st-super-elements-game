---
phase: 10-ambient-audio-music
plan: 05
type: execute
wave: 3
depends_on: ['10-02']
files_modified:
  - src/ui/SettingsScreen.tsx
  - src/App.tsx
  - src/styles/hud/switcher.css
autonomous: true
requirements: [MUSIC-03]

must_haves:
  truths:
    - "The settings panel has a SKAŅA section with a Music volume slider, an SFX volume slider, a Music mute toggle, and an SFX mute toggle (MUSIC-03, UI-SPEC)"
    - "Volume + mute persist to localStorage under settings.musicVolume/sfxVolume/musicMuted/sfxMuted and apply live via the imperative Game setters (D-13)"
    - "Music and SFX adjust/mute independently; a corrupt/non-finite persisted value is clamped to [0,1] and falls back to the default (music 0.7, sfx 1.0) (V5)"
    - "Muting a bus preserves its stored volume and re-applies it on unmute; adjusting a muted slider does not auto-unmute (D-13)"
  artifacts: []
  key_links:
    - "App state → useEffect persist + gameRef.current.setMusicVolume/setSfxVolume/setMusicMuted/setSfxMuted (the 10-02 setters)"
    - "SettingsScreen SKAŅA section placed after ATTĒLOŠANA, before KONTS"
---

<objective>
The only user-facing UI in this phase (MUSIC-03, D-13): a SKAŅA (Audio) section in the existing
settings panel with independent Music/SFX volume sliders + mute toggles, persisted locally and applied
live through the imperative Game setters added in 10-02. Built exactly to `10-UI-SPEC.md` — reuse the
`Modal`/`Toggle`/`.settings__section`/`.settings__field` idioms, `var(--accent)`, native
`<input type="range">`, Latvian copy.

Purpose: Let the player mute/adjust music independently of SFX and have it stick across reloads — the
last requirement of the phase, decoupled from the music loops (the sliders drive bus gains that exist as
soon as the Wave-1 bus module does).
Output: extended `SettingsScreenProps` + SKAŅA controls, App state/persist wiring, slider CSS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ambient-audio-music/10-CONTEXT.md
@.planning/phases/10-ambient-audio-music/10-UI-SPEC.md
@src/ui/SettingsScreen.tsx
</context>

<artifacts>
This plan produces:
- `SettingsScreenProps` extended with `musicVolume`, `sfxVolume`, `musicMuted`, `sfxMuted` + `onMusicVolumeChange`, `onSfxVolumeChange`, `onToggleMusicMuted`, `onToggleSfxMuted`; a SKAŅA section rendering 2 sliders + 2 toggles
- `App.tsx`: 4 new `settings.*` useState keys, 4 persist `useEffect`s calling the gameRef setters, props passed to SettingsScreen
- `switcher.css`: `.settings__slider` (styled native range) + `.settings__value` (% readout) blocks
- New localStorage keys: `settings.musicVolume`, `settings.sfxVolume`, `settings.musicMuted`, `settings.sfxMuted`
</artifacts>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SKAŅA section in SettingsScreen — volume sliders + mute toggles (UI-SPEC)</name>
  <files>src/ui/SettingsScreen.tsx, src/styles/hud/switcher.css</files>
  <read_first>
    - src/ui/SettingsScreen.tsx (SettingsScreenProps shape, the showFps/onToggleFps + Toggle idiom, the ATTĒLOŠANA / KONTS section markers, the settings__field select row)
    - .planning/phases/10-ambient-audio-music/10-UI-SPEC.md (the full token/copy/interaction contract — authoritative)
    - src/styles/hud/switcher.css (.settings__field / .settings__select tokens, :hover/:focus-visible treatments to mirror)
  </read_first>
  <action>
    Extend `SettingsScreenProps` with `musicVolume: number`, `sfxVolume: number`, `musicMuted: boolean`,
    `sfxMuted: boolean` and `onMusicVolumeChange(next: number)`, `onSfxVolumeChange(next: number)`,
    `onToggleMusicMuted(next: boolean)`, `onToggleSfxMuted(next: boolean)` — mirroring the existing
    showFps/onToggleFps pairs. Insert a `<p className="settings__section">SKAŅA</p>` section AFTER the
    ATTĒLOŠANA section and BEFORE KONTS. Order within it: Music volume slider → Music mute toggle → SFX
    volume slider → SFX mute toggle. Each slider is a native `<input type="range" min=0 max=100 step=1>` in a
    `.settings__field` row (label span · `.settings__value` % readout · slider), value mapped percent↔gain
    (`Math.round(volume*100)` display, `onChange` emits `next/100`). Copy (Latvian, from UI-SPEC): labels
    `Mūzikas skaļums` / `Efektu skaļums`, readout `{n}%`, mute toggles `Mūzika` / `Skaņas efekti` (affirmative:
    checked = audible; the parent persists the inverse as the mute flag). Give each slider an `aria-label`.
    Reuse `Toggle` verbatim for the two mutes. A slider whose bus is muted dims to opacity 0.55 but stays
    keyboard-operable (adjusting it does NOT auto-unmute — mute independent of volume, D-13). In
    `switcher.css` add a `.settings__slider` block styling the native range (`::-webkit-slider-thumb`,
    `::-moz-range-thumb`, track + filled portion) and a `.settings__value` block, using ONLY the tokens in
    UI-SPEC (`var(--accent)` filled track + thumb, `--line` remainder over `--panel` groove, focus ring
    `0 0 0 2px color-mix(in srgb, var(--accent) 45%, transparent)`, hover border `var(--accent)`, transitions
    ≤0.14s transform/border/box-shadow only, `border-radius: 8px`). Never hardcode a hex — use the tokens.
  </action>
  <verify>
    <automated>grep -q "SKAŅA" src/ui/SettingsScreen.tsx &amp;&amp; grep -q "musicVolume" src/ui/SettingsScreen.tsx &amp;&amp; grep -q "settings__slider\|settings__value" src/styles/hud/switcher.css &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -c "SettingsScreen" | grep -qx 0 &amp;&amp; echo SETTINGS_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - SettingsScreenProps has the 4 value props + 4 handler props; SKAŅA section renders 2 sliders + 2 toggles between ATTĒLOŠANA and KONTS
    - Sliders are native range inputs styled with var(--accent)/tokens (no hardcoded hex); % readout uses .settings__value
    - Muted slider dims but stays operable; tsc clean
  </acceptance_criteria>
  <done>The SKAŅA controls exist to the UI-SPEC contract with token-only styling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: App state + persistence + live-apply to Game setters (D-13, V5 clamp)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx lines 77-92 (settings.* useState init pattern), 880-892 (persist useEffect + gameRef imperative apply, e.g. setPixelFilter), 1048-1058 (SettingsScreen props wiring)
    - .planning/phases/10-ambient-audio-music/10-RESEARCH.md §"Settings persistence + UI" (the verbatim template + defaults)
  </read_first>
  <action>
    Add four `settings.*` useState hooks mirroring the existing pattern: `musicVolume` init
    `Number(localStorage.getItem('settings.musicVolume') ?? '0.7')`, `sfxVolume` init `?? '1'`, and boolean
    `musicMuted`/`sfxMuted` from the `'1'`/`'0'` idiom (absent = unmuted). On read, if the parsed number is
    non-finite or out of [0,1], fall back to the default (music 0.7, sfx 1.0) — do not let garbage reach the
    setter (V5; the bus setter also clamps, defense in depth). Add four persist `useEffect`s mirroring the
    `pixelFilter` effect: each writes its `localStorage` key AND calls the matching imperative Game setter —
    `gameRef.current?.setMusicVolume(musicVolume)`, `setSfxVolume(sfxVolume)`,
    `setMusicMuted(musicMuted)`, `setSfxMuted(sfxMuted)`. Also apply the persisted values once on new-game
    init alongside the existing `setPixelFilter` seed (~line 800) so a fresh game reads them. Pass the eight
    new props into `<SettingsScreen>`; the mute toggles are affirmative (checked = audible), so the handler
    persists the inverse as the muted flag. Never derive audio state in render — App state → imperative Game
    setter only (CLAUDE.md).
  </action>
  <verify>
    <automated>grep -q "settings.musicVolume" src/App.tsx &amp;&amp; grep -q "setMusicVolume" src/App.tsx &amp;&amp; grep -q "setSfxMuted" src/App.tsx &amp;&amp; npx tsc --noEmit -p tsconfig.json 2&gt;&amp;1 | grep -c "App.tsx" | grep -qx 0 &amp;&amp; echo APP_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - 4 useState keys with clamped defaults; 4 persist effects that write localStorage AND call the Game setters
    - New-game init seeds the persisted values (like setPixelFilter); SettingsScreen receives all 8 props
    - tsc clean; `npx vitest run` stays green
    - Manual (phase playtest): set sliders, reload → values persist; muting music leaves SFX audible
  </acceptance_criteria>
  <done>Music/SFX volume + mute persist independently and apply live through the Game bus setters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → React state → AudioParam | Persisted `settings.*Volume`/`*Muted` values are read on load and flow to bus gains. A corrupt/edited value must not reach a gain node. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-V5 | Tampering (local) | volume read from localStorage | low | mitigate | On read, reject non-finite / out-of-[0,1] and fall back to default (music 0.7, sfx 1.0); the bus setter (10-02) clamps again — defense in depth. No NaN reaches an AudioParam. |
| T-10-05-SC | Tampering | package installs | low | accept | No package installs; native range input + existing Radix Toggle. No registry surface. |

Honest assessment: the one real robustness item is the localStorage volume clamp, mitigated on both
read (here) and write (10-02). No auth, no network, no third-party registry.
</threat_model>

<verification>
- tsc clean; `npx vitest run` green
- grep confirms the 4 keys, the 4 setters, and the slider/value CSS exist
- UI-SPEC checker dimensions satisfied (token-only color, Latvian copy, native range, placement)
</verification>

<success_criteria>
The player can mute/adjust music independently of SFX from the settings panel, persisted locally and
applied live, with corrupt values clamped to a safe default.
</success_criteria>

<output>
Create `.planning/phases/10-ambient-audio-music/10-05-SUMMARY.md` when done.
</output>
