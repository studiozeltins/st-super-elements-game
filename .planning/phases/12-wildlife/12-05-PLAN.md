---
phase: 12-wildlife
plan: 05
type: execute
wave: 2
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - src/game/createGame.ts
autonomous: false
requirements: [WILD-01, WILD-02, WILD-03]
must_haves:
  truths:
    - "All three pools + the wing sfx are constructed in createGame, each skipped entirely when its ?no* flag is set (zero objects, clean FPS bisect)"
    - "Butterflies and fireflies update once per frame fed by wind.timeUniform.value (shared clock) + dayNightPhase + player position; birdFlush updates once per frame"
    - "Sprinting through grass spawns a bird flush + plays the wing one-shot at the CPU surface==='grass' stamp site, debounced by flushReady — never a GPU texture read, never every frame"
    - "All three pools + the wing sfx are disposed in createGame teardown"
    - "?nobugs / ?nobirds / ?nofireflies each remove exactly their system; FPS holds through a golem-class fight with ALL ambiance enabled (SC4)"
  artifacts:
    - src/game/createGame.ts
  key_links:
    - "flush spawn hooks the existing grass stamp site (the else-branch of the dust gate at ~:1037-1040) using elapsedSeconds + a lastFlushSec closure var + flushReady — NOT the groundInfluence GPU texture"
    - "wildlifeSfx wired onto buses.sfx beside the other audio siblings (~:461-471)"
    - "update lines added after dustPuffs?.update; dispose added beside dustPuffs?.dispose()"
---

<objective>
Wire the three creature pools + the wing sfx into `createGame.ts` (WILD-01/02/03): perf-bisect
flags, conditional construction, the grass-stamp flush hook, one update line each per frame, and
teardown — then run the perceptual UAT and the SC4 milestone-wide FPS gate.

Purpose: Turn the tested factories into live systems with the project's mandatory `?no*` bisect
discipline, and prove the summed frame cost of ALL ambiance (wind + daynight + audio + wear +
wildlife) holds through a golem fight. createGame is the ~2160-LOC worst offender — WIRE only,
keep all logic in the factories.
Output: createGame.ts wired; perceptual + FPS sign-off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wildlife/12-RESEARCH.md
@.planning/phases/12-wildlife/12-PATTERNS.md
@src/game/createGame.ts
@src/game/systems/createButterflies.ts
@src/game/systems/createBirdFlush.ts
@src/game/systems/createFireflies.ts
@src/game/audio/createWildlifeSfx.ts
@src/game/systems/wildlifeMath.ts
</context>

<artifacts_this_plan_produces>
NEW behavior in `src/game/createGame.ts` (wiring only — no new exported symbols):
- Perf flags `?nobugs` / `?nobirds` / `?nofireflies` (matching the shipped `?nodust`/`?nosmoke` convention).
- Constructed `butterflies` / `fireflies` / `birdFlush` (each `undefined` when disabled) + `wildlifeSfx`.
- A `lastFlushSec` closure var + the grass-stamp flush spawn/`playWingFlap` hook.
- Three `.update(...)` calls per frame + four `dispose()` calls at teardown.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire flags, construction, flush hook, frame updates, and dispose into createGame</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts flag block (perfFlags :335, dustEnabled :350, the kill-switch comment :332-334)
    - src/game/createGame.ts construction (dustPuffs :426-428) and audio siblings (:461-471, OWN_STEP_GAIN is defined later at :1258 and is NOT in scope at the flush site)
    - src/game/createGame.ts grass stamp site (:1022-1040 — the surfaceAt classify + the dust else-gate), elapsedSeconds (:529, advanced :1452), dayNightPhase (~:1478), wind.timeUniform.value (advanced :1455), the dustPuffs?.update line (~:1511), and the dispose block (~:1679-1680)
    - .planning/phases/12-wildlife/12-PATTERNS.md (createGame.ts MOD 1-5 — the exact wiring deltas)
  </read_first>
  <action>
    Wire the three factories + wing sfx into createGame, mirroring the dustPuffs wiring exactly.
    Keep ALL logic in the factories — this file only wires.
    MOD 1 (flags): beside dustEnabled (~:350) add butterfliesEnabled = !perfFlags.has('nobugs'),
      birdsEnabled = !perfFlags.has('nobirds'), firefliesEnabled = !perfFlags.has('nofireflies').
      Append `?nobugs / ?nobirds / ?nofireflies` to the kill-switch comment at :332-334.
    MOD 2 (construction): after dustPuffs (~:428) construct each pool only when enabled (else undefined):
      butterflies = butterfliesEnabled ? createButterflies(scene, (x,z)=>world.getGroundHeight(x,z)) : undefined; same shape for fireflies and birdFlush. Beside the audio siblings (~:471) add
      wildlifeSfx = createWildlifeSfx(audioSystem.getContext, buses.sfx). Add the necessary imports.
    MOD 3 (flush hook): at the grass stamp site (~:1037-1040), add an else-branch to the existing
      `if (playerSurface !== 'grass')` dust gate: `else if (birdFlush && flushReady(lastFlushSec, elapsedSeconds)) { lastFlushSec = elapsedSeconds; birdFlush.spawn(playerPosition.x, playerPosition.z); wildlifeSfx?.playWingFlap(0.6, 0); }`. Declare `let lastFlushSec = -Infinity;` near the other player-loop state. Use a literal gain (~0.6) — do NOT reference OWN_STEP_GAIN (out of scope here). This is the CPU surface==='grass' gate — do NOT read the groundInfluence texture.
    MOD 4 (frame update): after dustPuffs?.update(deltaSeconds) (~:1511) add:
      butterflies?.update(deltaSeconds, pixelRenderer.camera, playerPosition.x, playerPosition.z, dayNightPhase, wind.timeUniform.value);
      fireflies?.update(deltaSeconds, pixelRenderer.camera, playerPosition.x, playerPosition.z, dayNightPhase, wind.timeUniform.value);
      birdFlush?.update(deltaSeconds, pixelRenderer.camera);
      (If dayNightPhase is not already a frame-scope variable at the update site, read the same phase source the day/night cycle uses — do not derive it from React.)
    MOD 5 (dispose): beside dustPuffs?.dispose() (~:1680) add butterflies?.dispose(); fireflies?.dispose(); birdFlush?.dispose(); wildlifeSfx?.dispose().
    Build must typecheck; keep createGame wire-only (no creature logic inline).
  </action>
  <verify>
    <automated>pnpm exec vitest run && pnpm exec tsc --noEmit && grep -cE "nobugs|nobirds|nofireflies" src/game/createGame.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three `?no*` flags parsed; each gates construction (disabled ⇒ undefined, zero objects).
    - The flush spawn is at the CPU surface==='grass' else-branch, debounced by flushReady with a lastFlushSec closure var; no groundInfluence texture read added (`grep -c 'groundInfluence' near the flush hook shows only the existing walk-trail stamp`).
    - Three update lines added after dustPuffs?.update; four dispose lines added beside dustPuffs?.dispose().
    - `pnpm exec tsc --noEmit` clean; full suite green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Perceptual UAT — butterflies (day), bird flush (grass sprint), fireflies (dusk/night)</name>
  <action>Pause for human perceptual verification of the three wildlife feels and their `?no*` bisect flags before the FPS gate. All work was automated in Task 1; this only confirms the visual/audio result.</action>
  <what-built>All three wildlife systems wired live into the game loop with `?nobugs`/`?nobirds`/`?nofireflies` bisect flags: day butterflies over grass, grass-sprint bird flush + wing one-shot, dusk/night firefly swarm.</what-built>
  <how-to-verify>
    Build and serve (`pnpm run build`, open the laragon LAN URL or dev server).
    1. Butterflies (WILD-01): at day (append `?time=0.3` to the URL), roam over grass — confirm butterflies are SPARSE (spotting one feels like an event, not wallpaper), drift naturally, spawn/despawn near you, and NONE appear at night (`?time=0.82`). Append `?nobugs` — they disappear.
    2. Bird flush (WILD-02): at day, sprint through grass — confirm 2-4 birds burst up on a believable rising arc with ONE wing sound, then vanish; running continuously does NOT machine-gun birds (the ~6s debounce). Append `?nobirds` — no flush.
    3. Fireflies (WILD-03): advance to dusk/night (`?time=0.82`) — confirm small quads GLOW and shimmer with a randomized pulse, none by day, and there is no material-recompile hitch when they appear (no light was added). Append `?nofireflies` — they disappear.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (density/glow/arc/timing to retune in wildlifeMath).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC4 milestone-wide FPS gate — golem fight with ALL ambiance enabled</name>
  <action>Pause for the SC4 performance gate. Run the FPS harness in a golem-class fight with every ambiance system on; use the `?no*` flags to bisect any regression. This is a verification gate, not new implementation.</action>
  <what-built>The SC4 milestone-wide FPS gate: frame rate must hold through a golem-class fight with ALL ambiance systems enabled (wind + day/night + audio + wear + wildlife).</what-built>
  <how-to-verify>
    1. Run `scripts/fps_playtest.py` in a golem-class fight with ALL ambiance enabled (no `?no*` flags) — confirm frame rate holds (no regression vs the pre-wildlife baseline).
    2. If FPS regressed, bisect: re-run adding `?nobugs`, then `?nobirds`, then `?nofireflies` — the flag whose addition recovers FPS names the culprit pool; retune its cap/geometry and re-run.
    3. Confirm each pool is one draw call and hard-capped (no unbounded growth over a long session).
  </how-to-verify>
  <resume-signal>Type "approved" when the golem fight holds frame rate with all wildlife on, or report the regressing system named by the bisect.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → URL query flags | The only new external input is boolean `?nobugs`/`?nobirds`/`?nofireflies` presence (cosmetic kill-switches, matching the shipped `?no*` convention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering | `?nobugs`/`?nobirds`/`?nofireflies` URL flags | low | accept | Boolean presence only; each disables a cosmetic system — no privilege, no state change, no persistence. No mitigation needed beyond the existing pattern. |
| T-12-05-DET | (not security) | client determinism | low | accept | Wildlife is cosmetic + player-local (spawns near the local player); no shared/authoritative state, so per-client variance is invisible and harmless. |

No high-severity threats. No package installs in this phase (zero-new-dependencies is locked), so no supply-chain checkpoint applies.
</threat_model>

<verification>
- `pnpm exec vitest run` green; `pnpm exec tsc --noEmit` clean.
- Perceptual UAT passes for butterflies (day), bird flush (grass sprint), fireflies (dusk/night); each `?no*` flag removes exactly its system.
- SC4: `scripts/fps_playtest.py` golem fight with ALL ambiance on holds frame rate; `?no*` bisect available.
</verification>

<success_criteria>
WILD-01/02/03 live in-game behind bisect flags, wired to the shared wind clock + day/night phase +
the CPU grass stamp site + the sfx bus, with the milestone-wide FPS gate (SC4) passed.
</success_criteria>

<output>
Create `.planning/phases/12-wildlife/12-05-SUMMARY.md` when done.
</output>
