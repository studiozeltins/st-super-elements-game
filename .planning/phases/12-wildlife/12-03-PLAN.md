---
phase: 12-wildlife
plan: 03
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/game/systems/createBirdFlush.ts
  - src/game/audio/createWildlifeSfx.ts
autonomous: true
requirements: [WILD-02]
must_haves:
  truths:
    - "createBirdFlush exposes spawn(x,z) that claims 2-4 slots (one flush) and update() that ages each bird along birdArc and despawns it at t01>=1"
    - "All birds draw from one InstancedMesh (one draw call), hard-capped, slot-recycled, zero per-frame allocation"
    - "createWildlifeSfx.playWingFlap plays a procedural synth-first wing one-shot routed through the sfx bus, gesture-guarded (never throws mid-frame before the audio context unlocks)"
    - "The rising-arc animation delegates to wildlifeMath.birdArc; the debounce lives at the call site (12-05), not in spawn()"
  artifacts:
    - src/game/systems/createBirdFlush.ts
    - src/game/audio/createWildlifeSfx.ts
  key_links:
    - "createBirdFlush delegates arc math to wildlifeMath.birdArc (12-01)"
    - "createWildlifeSfx mirrors createAmbience.birdChirp synth + the createAudioSystem (getContext, getSfxBus) module shape; wired onto buses.sfx in 12-05"
    - "pool added to scene root; dispose() ends in mesh.dispose(); wing synth cleans up on .onended"
---

<objective>
Create `createBirdFlush.ts` (WILD-02): an externally-spawned InstancedMesh pool where a call
bursts 2-4 birds up a scripted rising arc then despawns them; and `createWildlifeSfx.ts`, the
procedural wing one-shot on the sfx bus.

Purpose: The "sprint through grass startles birds" event — a visual burst plus a wing flap —
built on the dust pool template and the Phase-10 sfx-bus synth pattern. Zero AI, zero deps.
Output: `createBirdFlush.ts` + `createWildlifeSfx.ts`. (Both wired into createGame in 12-05.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wildlife/12-RESEARCH.md
@.planning/phases/12-wildlife/12-PATTERNS.md
@src/game/systems/createDustPuffs.ts
@src/game/systems/wildlifeMath.ts
@src/game/audio/createAmbience.ts
@src/game/audio/createAudioSystem.ts
</context>

<artifacts_this_plan_produces>
NEW `src/game/systems/createBirdFlush.ts`:
- `export const BIRD_POOL_SIZE` (~12, ~3 flushes of 2-4 in flight).
- `export interface BirdFlush { spawn(x, z): void; update(deltaSeconds, camera): void; dispose(): void }`.
- `export function createBirdFlush(scene, getGroundHeight): BirdFlush`.
NEW `src/game/audio/createWildlifeSfx.ts`:
- `export interface WildlifeSfx { playWingFlap(gain?, pan?): void; dispose(): void }`.
- `export function createWildlifeSfx(getContext, getSfxBus): WildlifeSfx`.
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the externally-spawned bird-flush InstancedMesh pool</name>
  <files>src/game/systems/createBirdFlush.ts</files>
  <read_first>
    - src/game/systems/createDustPuffs.ts (the externally-spawned variant: spawn() slot-claim :102-126, age/recycle update :130-177, dispose :179-186)
    - src/game/systems/wildlifeMath.ts (birdArc out-param, BIRD tunables)
    - .planning/phases/12-wildlife/12-PATTERNS.md (createBirdFlush.ts section — interface + spawn/update split)
  </read_first>
  <behavior>
    - spawn(x,z) claims a Math.random() count of 2-4 slots, each with a random outward heading, age=0, and groundY=getGroundHeight(x,z); returns early when the pool is full.
    - update(deltaSeconds, camera) ages each active bird: t01 = age / BIRD.life; at t01>=1 the bird deactivates (collapse to zeroMatrix); otherwise birdArc(t01, arcScratch) drives height (groundY + arcScratch.y), lateral scatter along its heading (arcScratch.spread), and scale/fade by arcScratch.visible.
    - One InstancedMesh (one draw call), hard cap, slot recycle, needsUpdate gated, zero per-frame allocation, added to scene root.
    - Debounce is NOT here — spawn() is unconditional once called (the call site gates it in 12-05).
  </behavior>
  <action>
    Copy the createDustPuffs externally-spawned spine verbatim (spawn() slot-claim scan with the
    full-pool early return; update() age/recycle loop with matrixDirty gating; dispose() ending in
    mesh.dispose()). Deltas from dust:
    - Export BIRD_POOL_SIZE (~12) and the BirdFlush interface with spawn(x,z) + update(deltaSeconds, camera).
    - Each pool entry holds x/z/groundY/headingX/headingZ/age/active. spawn(x,z) picks count = 2 + floor(random*3), and for each claims the first inactive slot with a random unit heading (cos/sin of a random angle), age=0, groundY once.
    - update: bird.age += deltaSeconds; t01 = age/BIRD.life; if t01>=1 deactivate + zeroMatrix + matrixDirty; else birdArc(t01, arcScratch) (arcScratch built ONCE at closure scope), position = spawn + heading*arcScratch.spread, y = groundY + arcScratch.y, scale = base * arcScratch.visible; compose with the shared billboard quaternion (camera.quaternion read once per frame).
    - Geometry: PlaneGeometry(1,1) billboarded (silhouette matters for birds). MeshLambertMaterial acceptable.
    Keep under 300 functional LOC. No scene light, no GPU readback, no per-frame allocation.
  </action>
  <verify>
    <automated>pnpm exec vitest run && grep -c 'new THREE.InstancedMesh' src/game/systems/createBirdFlush.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exports createBirdFlush, BIRD_POOL_SIZE, BirdFlush interface with spawn(x,z) + update(deltaSeconds, camera) + dispose().
    - Exactly one InstancedMesh (`grep -c 'new THREE.InstancedMesh' === 1`).
    - Arc animation delegates to wildlifeMath.birdArc (imported); no bespoke easing.
    - No per-frame THREE constructor inside update()/spawn(): `grep -vE '^\s*(//|\*)' src/game/systems/createBirdFlush.ts | grep -cE 'readPixels|PointLight|DirectionalLight'` returns 0.
    - Full suite green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build createWildlifeSfx — procedural wing one-shot on the sfx bus</name>
  <files>src/game/audio/createWildlifeSfx.ts</files>
  <read_first>
    - src/game/audio/createAmbience.ts (:109-112 ready() state guard; :119-138 birdChirp synth recipe + .onended cleanup; the createNoiseSource import precedent)
    - src/game/audio/createAudioSystem.ts (:10-26 module shape + the (gain, pan) convention)
    - .planning/phases/12-wildlife/12-PATTERNS.md (createWildlifeSfx.ts section)
  </read_first>
  <action>
    Create `src/game/audio/createWildlifeSfx.ts` as a sibling of createCombatAudio/createWeaponAudio:
    `createWildlifeSfx(getContext: () => AudioContext | null, getSfxBus: () => AudioNode): WildlifeSfx`.
    Include a private ready() that returns the context only when getContext() is non-null and
    context.state === 'running' (the createAmbience :109-112 guard) — never throw in the frame path
    before the gesture unlock. playWingFlap(gain = 1, pan = 0): if !ready() return; synthesize a
    wing beat as 2-3 short filtered-noise transients (bandpass ~300-800Hz, ~40ms each, staggered by
    a few tens of ms) — a wing-beat is broadband air, not tonal — scheduled off ctx.currentTime,
    routed through getSfxBus(), with a StereoPanner for pan. Disconnect each node on .onended
    (mandatory cleanup). Leave a recording-fallback comment noting an .ogg can drop in later via the
    createAmbience sampleCache pattern with zero code change (synth-first ships now). dispose() is a
    no-op or clears any retained nodes. Keep well under 300 functional LOC.
    (Per RESEARCH Open Q1 recommendation: a dedicated module, not an AudioSystem method — SRP + no-monolith.)
  </action>
  <verify>
    <automated>pnpm exec vitest run && grep -c 'playWingFlap' src/game/audio/createWildlifeSfx.ts && grep -c 'getSfxBus' src/game/audio/createWildlifeSfx.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exports createWildlifeSfx + WildlifeSfx interface with playWingFlap(gain?, pan?) + dispose().
    - playWingFlap is gesture-guarded (returns silently when the context is null/not running).
    - Synth routes through getSfxBus() and cleans up on .onended.
    - Full suite green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side cosmetic render pool + Web Audio one-shot; no network, auth, persistence, or shared state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | bird pool / wing sfx | low | accept | Cosmetic, player-local, event-triggered; no privilege or state change. Audio is gesture-guarded so it cannot fire before user interaction. Minimal by design. |

No high-severity threats.
</threat_model>

<verification>
- `pnpm exec vitest run` stays green.
- One InstancedMesh; arc from wildlifeMath.birdArc; zero per-frame alloc; no scene light; no GPU readback.
- Wing synth is gesture-guarded and routes through the sfx bus.
- Manual (deferred to 12-05 UAT): a flush bursts 2-4 birds on a rising arc + one wing sound, then despawns.
</verification>

<success_criteria>
WILD-02 render + audio primitives complete: an externally-spawned bird-flush pool driven by the
tested arc math, and a gesture-safe procedural wing one-shot on the sfx bus — both ready to wire.
</success_criteria>

<output>
Create `.planning/phases/12-wildlife/12-03-SUMMARY.md` when done.
</output>
