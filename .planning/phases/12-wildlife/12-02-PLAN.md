---
phase: 12-wildlife
plan: 02
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/game/systems/createButterflies.ts
autonomous: true
requirements: [WILD-01]
must_haves:
  truths:
    - "One InstancedMesh (one draw call) draws all butterflies regardless of population"
    - "Butterflies self-manage: update() culls those beyond SPAWN.cull and tops up over grass in the SPAWN ring while isDayTime(phase) is true — none spawn at dusk/night"
    - "Each live butterfly drifts on butterflyWander + butterflyBob read from the shared wind clock, anchored to a grass cell near the player"
    - "The pool is hard-capped (BUTTERFLY_POOL_SIZE) with slot recycle — over-spawn never grows live instances"
    - "update() allocates nothing per frame (closure scratch, out-param wander object built once)"
  artifacts:
    - src/game/systems/createButterflies.ts
  key_links:
    - "delegates ALL motion + day gate + ring math to wildlifeMath (12-01)"
    - "reads grass cells via a surfaceAt-backed check and ground y via getGroundHeight — no GPU readback"
    - "added to scene root (never the frozen world group); dispose() ends in mesh.dispose() to free instance GPU buffers"
---

<objective>
Create `createButterflies.ts` (WILD-01): a sparse, day-gated InstancedMesh pool that spawns
and despawns butterflies over grass near the player, each fluttering on a summed-sine wander
driven by the shared wind clock.

Purpose: The daytime "encounter = event" living-world payoff, at exactly one draw call.
Output: `createButterflies.ts` on the `createDustPuffs` pool template, delegating all math to
`wildlifeMath`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wildlife/12-RESEARCH.md
@.planning/phases/12-wildlife/12-PATTERNS.md
@src/game/systems/createDustPuffs.ts
@src/game/systems/wildlifeMath.ts
@src/game/systems/surfaceAt.ts
</context>

<artifacts_this_plan_produces>
NEW `src/game/systems/createButterflies.ts`:
- `export const BUTTERFLY_POOL_SIZE` (sparse, ~8).
- `export interface Butterflies { update(deltaSeconds, camera, playerX, playerZ, phase, t): void; dispose(): void }`.
- `export function createButterflies(scene, getGroundHeight): Butterflies` — NO public spawn() (self-managing).
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the sparse day-gated butterfly InstancedMesh pool</name>
  <files>src/game/systems/createButterflies.ts</files>
  <read_first>
    - src/game/systems/createDustPuffs.ts (the verbatim pool spine: mesh flags :76-92, closure scratch :94-99, slot-claim :102-110, age/recycle + needsUpdate gating :130-177, dispose :179-186)
    - src/game/systems/wildlifeMath.ts (butterflyWander, butterflyBob, isDayTime, inSpawnRing, beyondCull, SPAWN, BUTTERFLY tunables)
    - src/game/systems/surfaceAt.ts (the CPU grass/dirt/path/town classifier — the spawn gate; no GPU read)
    - .planning/phases/12-wildlife/12-PATTERNS.md (createButterflies.ts section — interface + hard-cap doc + scratch discipline)
  </read_first>
  <behavior>
    - One InstancedMesh with BUTTERFLY_POOL_SIZE capacity, frustumCulled=false, no shadows, added to scene root.
    - update() while isDayTime(phase): cull any butterfly whose (playerX-anchorX, playerZ-anchorZ) is beyondCull; top up below target by picking a candidate point in the SPAWN ring around the player, accepting it only when surfaceAt(x,z)==='grass', on a ~0.5s recheck timer (not a per-frame ring scan).
    - When !isDayTime(phase): no new spawns; existing butterflies still cull out, then the pool sits empty (all slots collapsed to zeroMatrix) — a clean night no-op.
    - Each live butterfly: position = anchor + butterflyWander(t,seed,wanderScratch) + groundY + butterflyBob(t,seed); billboarded via one shared camera quaternion read once per frame.
    - Zero per-frame allocation; matrix/instanceColor needsUpdate gated only when dirty.
  </behavior>
  <action>
    Copy the createDustPuffs pool spine verbatim (mesh setup + flags, closure scratch, slot-claim
    scan with the full-pool early return, age/recycle loop with matrixDirty gating, dispose ending
    in mesh.dispose()). Deltas from dust:
    - Interface has NO public spawn() — butterflies self-manage inside update(deltaSeconds, camera, playerX, playerZ, phase, t). Export BUTTERFLY_POOL_SIZE (~8, sparse: encounter = event, not wallpaper).
    - Each pool entry holds anchorX/anchorZ/seed/groundY/active (no velocity — butterflies flutter, they are not thrown).
    - Gate on isDayTime(phase): only top up when true; always run the cull pass.
    - Spawn top-up (on a ~0.5s recheck timer like createSmokeColumns' CULL_RECHECK_INTERVAL): pick a ring point via inSpawnRing offsets around (playerX,playerZ), require surfaceAt(x,z)==='grass', set anchor = that grass point, seed = Math.random()*TAU (cosmetic RNG is fine), groundY = getGroundHeight(x,z) + a small hover.
    - Per-frame per live butterfly: butterflyWander(t, seed, wanderScratch) (out-param object built ONCE at closure scope), y = groundY + butterflyBob(t, seed); compose the matrix with the shared billboard quaternion (read camera.quaternion once per frame into a closure scratch quat) — no per-instance lookAt.
    - Geometry: PlaneGeometry(1,1) billboarded (a butterfly silhouette). MeshLambertMaterial is acceptable (they should read with the daytime scene light). Keep the file under 300 functional LOC.
    Do NOT read the groundInfluence texture; do NOT add any scene light; do NOT allocate inside update().
  </action>
  <verify>
    <automated>pnpm exec vitest run && grep -c 'InstancedMesh' src/game/systems/createButterflies.ts && grep -vE '^\s*(//|\*|/\*)' src/game/systems/createButterflies.ts | grep -cE 'new (Matrix4|Vector3|Color|Quaternion)\(' </automated>
  </verify>
  <acceptance_criteria>
    - createButterflies.ts exports createButterflies, BUTTERFLY_POOL_SIZE, and the Butterflies interface; no public spawn().
    - Exactly one InstancedMesh is constructed (`grep -c 'new THREE.InstancedMesh' === 1`).
    - No THREE object constructors inside update()/the spawn loop — all scratch is closure-level (the filtered grep for `new (Matrix4|Vector3|Color|Quaternion)` shows only closure-scope constructions, none inside loops).
    - No groundInfluence texture read and no scene light added: `grep -vE '^\s*(//|\*)' src/game/systems/createButterflies.ts | grep -cE 'readPixels|PointLight|DirectionalLight|SpotLight'` returns 0.
    - Full suite green (additive — no existing test breaks).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side cosmetic render pool; player-local, no shared/authoritative state, no network/persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02 | Tampering | butterfly pool | low | accept | Purely cosmetic, player-local spawns; per-client visual variance is invisible and harmless. Minimal by design. |

No high-severity threats.
</threat_model>

<verification>
- `pnpm exec vitest run` stays green.
- One InstancedMesh; zero per-frame allocation; no GPU readback; no scene light.
- Manual (deferred to 12-05 UAT): butterflies read sparse over grass by day, none at night, `?nobugs` removes them.
</verification>

<success_criteria>
WILD-01 render factory complete: sparse, day-gated, spawn/despawn-near-player butterflies at
one draw call, all motion delegated to the tested wildlifeMath twin.
</success_criteria>

<output>
Create `.planning/phases/12-wildlife/12-02-SUMMARY.md` when done.
</output>
