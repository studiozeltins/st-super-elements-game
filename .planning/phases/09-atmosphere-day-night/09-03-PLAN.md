---
phase: 09-atmosphere-day-night
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - src/game/world/assets/createLantern.ts
  - src/game/world/createMondstadtWorld.ts
autonomous: true
requirements: [DAYNITE-04]

must_haves:
  truths:
    - "4–6 warm named PointLights exist in the plaza (within SAFE_ZONE_RADIUS=18), added once at world build via the campfire named-light pattern"
    - "Every lantern light is collected by name into ambience.lanternLights during the existing frozen-world traverse loop"
    - "Lanterns are NOT drawn from the combat createLightPool (that size-4 pool is combat-owned and never grown, D-07)"
  artifacts:
    - src/game/world/assets/createLantern.ts
    - src/game/world/createMondstadtWorld.ts
  key_links:
    - "Each lantern PointLight sets light.layers.enableAll() — skipping it flips the renderer lights-state hash and re-inits every lit material per pass"
    - "Lights are added at BUILD time with intensity that later only fades via .intensity (no runtime add/remove — recompile ban); Plan 04 drives the fade from lanternLevel"
---

<objective>
Add plaza lanterns: a new `createLantern.ts` voxel-post asset carrying a named warm `PointLight` (campfire pattern), placed 4–6 times in the plaza at world build, and collected by name into `ambience.lanternLights` in the existing frozen-world traverse loop. This plan builds the lights and wires them into the handle; Plan 04's day/night cycle drives their intensity fade from `lanternLevel`.

Purpose: DAYNITE-04 infrastructure — build-time lights whose intensity later fades in at dusk / out at dawn, with zero runtime light add/remove (the recompile ban).
Output: New `createLantern.ts`; `createMondstadtWorld.ts` places lanterns + collects them into `ambience.lanternLights`.
</objective>

<artifacts_produced>
NEW symbols created by this plan:
- `createLantern.ts` exports: `createLantern(random: SeededRandom): WorldAsset` and `LANTERN_LIGHT_NAME` (string const, e.g. 'lanternLight').
- Lantern placement calls + a `lanternLights: THREE.PointLight[]` collection in createMondstadtWorld.ts (populating the array declared on AmbienceHandles by Plan 02).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atmosphere-day-night/09-RESEARCH.md
@.planning/phases/09-atmosphere-day-night/09-PATTERNS.md
@src/game/world/assets/createCampfire.ts
@.planning/phases/09-atmosphere-day-night/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create createLantern.ts — voxel post + named warm PointLight</name>
  <read_first>
    - src/game/world/assets/createCampfire.ts:11-12,34-81 (LANTERN template: CAMPFIRE_LIGHT_NAME const, WorldAsset { group } return, and the named-PointLight-at-build block at :72-78 with layers.enableAll())
    - src/game/world/assets/assetHelpers (lambert, randomBetween, randomIntBetween helpers createCampfire imports)
    - src/game/world/assets/types (SeededRandom, WorldAsset types)
    - src/game/systems/createLightPool.ts:5-9,22 (the combat-owned size-4 pool — DO NOT use for lanterns, D-07)
  </read_first>
  <files>src/game/world/assets/createLantern.ts</files>
  <action>
    Create `createLantern.ts` copying the `createCampfire.ts` structure: export `LANTERN_LIGHT_NAME = 'lanternLight'`; export `createLantern(random: SeededRandom): WorldAsset` that builds a simple voxel lantern post from BoxGeometry + `lambert()` (a post/pole plus a warm lamp box matching the art) and adds one warm `THREE.PointLight` with `light.name = LANTERN_LIGHT_NAME`, `light.layers.enableAll()` (mandatory — skipping it triggers per-frame material re-init, RESEARCH Pattern 6), a warm color, and a modest distance/decay. Set the light's base intensity to a sensible lit value (Plan 04 scales it by lanternLevel each frame; at build it can start at the base value). Position the light near the lamp box height. Return `{ group }`. Do NOT use createLightPool.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    createLantern.ts exports createLantern + LANTERN_LIGHT_NAME; returns a WorldAsset whose group contains a voxel post and one named warm PointLight with layers.enableAll(). No createLightPool usage. tsc clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Place 4–6 lanterns in the plaza + collect into ambience.lanternLights</name>
  <read_first>
    - src/game/world/createMondstadtWorld.ts:155-172 (createPlaza — plaza build within SAFE_ZONE_RADIUS)
    - src/game/world/createMondstadtWorld.ts:427-448 (placeAsset / camp placement precedent, e.g. placeAsset(createCampfire(...)) at :440)
    - src/game/world/createMondstadtWorld.ts:462-481 (the frozen-world traverse loop that collects campfireLights/campFlags by name — add lantern collection in the SAME loop)
    - src/game/world/createMondstadtWorld.ts (ambience.lanternLights array declared by Plan 02 — populate it here)
  </read_first>
  <files>src/game/world/createMondstadtWorld.ts</files>
  <action>
    Import `createLantern` + `LANTERN_LIGHT_NAME`. Place 4–6 lanterns inside the plaza (within SAFE_ZONE_RADIUS=18) at world build using the existing `placeAsset` helper — pick fixed/evenly-spaced plaza positions (Claude's discretion on exact count/positions, plaza-only per D-07). In the existing `group.traverse` loop (:471), add `if (node.name === LANTERN_LIGHT_NAME) lanternLights.push(node as THREE.PointLight);` alongside the campfire collection, declaring `const lanternLights: THREE.PointLight[] = []` before the loop and assigning it to the `ambience.lanternLights` handle (replacing the empty placeholder from Plan 02). Note the world is matrix-frozen after this (:456-457) — intensity changes are free, no matrix cost. Do NOT add any per-frame fade here; Plan 04 owns that write via the handle.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    4–6 lanterns are placed in the plaza and collected into ambience.lanternLights via the named-traverse pattern. lanternLights is non-empty on the returned handle. No runtime add/remove. tsc clean; `pnpm build` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Build-time scene construction only — no network input, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Denial of Service | lantern PointLights (light-count churn) | low | mitigate | Lights added ONCE at build with layers.enableAll(); only .intensity drifts (no runtime add/remove) — avoids the lights-state-hash flip + per-material re-init storm (Anti-Pattern 3). Lantern count fixed 4–6, plaza-only |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — three built-ins only |

No high-severity threats. Client-only cosmetic build-time geometry + lights.
</threat_model>

<verification>
- `pnpm exec tsc -b` clean; `pnpm build` succeeds.
- Grep confirms `LANTERN_LIGHT_NAME` is referenced in both createLantern.ts (definition) and createMondstadtWorld.ts (collection).
- No `createLightPool` reference in createLantern.ts.
- lanternLights collected in the same traverse loop as campfireLights (no second traverse).
</verification>

<success_criteria>
4–6 warm plaza lanterns exist as build-time named PointLights collected into `ambience.lanternLights`, ready for the day/night cycle to fade by intensity — with no runtime light add/remove and no use of the combat light pool.
</success_criteria>

<output>
Create `.planning/phases/09-atmosphere-day-night/09-03-SUMMARY.md` when done.
</output>
