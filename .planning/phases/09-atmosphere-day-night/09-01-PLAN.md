---
phase: 09-atmosphere-day-night
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/dayNightMath.ts
  - src/game/systems/__tests__/dayNightMath.test.ts
autonomous: true
requirements: [DAYNITE-01, DAYNITE-02, DAYNITE-03, DAYNITE-04]

must_haves:
  truths:
    - "phase01(nowMicros) returns a value in [0,1) that advances ~1 per 20 real minutes and wraps continuously (no daylight flash at 0.99→0.01)"
    - "Every night-band phase yields sun+hemisphere intensity ≥ ~45% of the day peak — night is a palette, never a dimmer (D-03)"
    - "lanternLevel is 0 across the day band, 1 across night, and ramps monotonically over dusk (up) and dawn (down); fireflyLevel is gated to the dusk/night band"
    - "phase01 computes the bigint modulo BEFORE Number() — precise at ~1.78e15 micros with no float drift (D-08)"
  artifacts:
    - src/game/systems/dayNightMath.ts
    - src/game/systems/__tests__/dayNightMath.test.ts
  key_links:
    - "dayNightMath is THREE-free (zero import) so it unit-tests without a renderer — the palette source of truth createDayNightCycle wraps"
---

<objective>
Deliver the pure, THREE-free day/night math twin `dayNightMath.ts` plus its vitest suite `dayNightMath.test.ts` — the deterministic source of truth for the time-of-day phase (0..1), the keyframed color/intensity palette, the night exposure floor, and the `lanternLevel`/`fireflyLevel` dusk-gate scalars. Mirrors the shipped Phase-8 `windMath.ts` + `windMath.test.ts` precedent exactly.

Purpose: Isolate every keyframe/phase decision behind a zero-dependency, fully unit-tested module so the rendering wrapper (`createDayNightCycle`, Plan 04) only does `Color.lerpColors` — the "extract reducer logic to zero-import pure helpers, test-first" discipline.
Output: Two new files under `src/game/systems/`; full vitest suite green.
</objective>

<artifacts_produced>
NEW symbols created by this plan (exclude from any drift/source-grounding check — they do not exist yet):
- `dayNightMath.ts` exports: `CYCLE_MICROS` (bigint, 1_200_000_000n), `Keyframe` type, `KEYFRAMES` (readonly const array), `smoothstep(edge0, edge1, x)`, `phase01(nowMicros: bigint): number`, `samplePalette(phase: number): DayNightPalette`, `DayNightPalette` type (plain-number/hex fields: skyTop, horizon, sunColor, sunIntensity, hemiSky, hemiGround, hemiIntensity, lanternLevel, fireflyLevel), plus any `NIGHT_FLOOR`/band-boundary constants the executor names.
- `dayNightMath.test.ts` (new vitest suite).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atmosphere-day-night/09-RESEARCH.md
@.planning/phases/09-atmosphere-day-night/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dayNightMath.ts — phase math + keyframe palette + scalars</name>
  <read_first>
    - src/game/systems/windMath.ts (the exact pure-helper-twin template: zero-import header doc, exported `as const` constant structs, deterministic pure functions, no allocations — mirror its structure; SKIP its `*Glsl` half, day/night has no shader-text twin per 09-PATTERNS.md)
    - .planning/phases/09-atmosphere-day-night/09-RESEARCH.md §"Pattern 4: Keyframe palette model" (band layout example, night-floor rule, wraparound rule, bigint-modulo-before-Number)
  </read_first>
  <files>src/game/systems/dayNightMath.ts</files>
  <behavior>
    - phase01(0n) === 0; phase01(CYCLE_MICROS) === 0 (full wrap); phase01(CYCLE_MICROS / 2n) ≈ 0.5.
    - phase01 with a large realistic epoch (~1_780_000_000_000_000n micros) stays exactly in [0,1) with no float precision loss — the modulo MUST be taken on the bigint before Number() (per D-08).
    - phase01 guards negative input defensively: ((n % C) + C) % C.
    - samplePalette blends adjacent keyframes with smoothstep (not linear) — smoothstep(a,b,a)===0, smoothstep(a,b,b)===1, and is monotonic non-decreasing across [a,b].
    - Night-band floor: at EVERY sampled phase inside the night band, sunIntensity ≥ ~0.45 * day-peak sunIntensity AND hemiIntensity ≥ ~0.45 * day-peak hemiIntensity (D-03 — assert this floor).
    - lanternLevel: 0 across the day band; ramps 0→1 over the dusk sub-band; 1 across night; ramps 1→0 over dawn (DAYNITE-04).
    - fireflyLevel: 0 across the day band; non-zero only inside the dusk/night band (exposed for Phase 12, consumed by nothing this phase).
    - Wraparound continuity: samplePalette(0.99) blends toward samplePalette(0.00) with no daylight flash — the last keyframe wraps to the first (assert horizon/sky continuity across the seam).
  </behavior>
  <action>
    Create `src/game/systems/dayNightMath.ts` mirroring `windMath.ts`: open with a doc block stating it is the single source of truth, ZERO imports (not even THREE) so it is unit-testable without a renderer, deterministic, no allocations. Export `CYCLE_MICROS = 1_200_000_000n` (20 min in micros, per D-01) and a `Keyframe`/`DayNightPalette` type plus a `KEYFRAMES` array `as const` of 4–6 entries over phase [0,1) with fields skyTop, horizon (=fog color), sunColor, sunIntensity, hemiSky, hemiGround, hemiIntensity, lanternLevel, fireflyLevel — all plain numbers/hex, NO THREE.Color. Make the cycle ASYMMETRIC DAY-WEIGHTED (long day band, short dawn/dusk, moderate night) per D-01; exact fractions/hex are Claude's discretion (RESEARCH suggests: pre-dawn ~0.00, dawn ~0.06, day ~0.10–0.55, dusk ~0.62, night ~0.70–0.98). Night palette is BLUE MOONLIGHT (cool hemisphere + dimmed warm sun) held to a ~45% day-exposure floor per D-03 — night is a palette, not darkness. Export `smoothstep(edge0, edge1, x)`, `phase01(nowMicros: bigint): number` doing the bigint modulo BEFORE Number() per D-08, and `samplePalette(phase: number): DayNightPalette` that finds the bracketing keyframes and smoothstep-blends every field (wrapping the last key back to the first for seam continuity). Keep lanternLevel/fireflyLevel as fields of the palette (or sibling pure fns) so createDayNightCycle reads them per frame. Do NOT import THREE; return plain hex/number tuples — the wrapper does the Color.lerpColors.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    dayNightMath.ts compiles, imports nothing, exports CYCLE_MICROS/KEYFRAMES/phase01/samplePalette/smoothstep with the fields listed in artifacts_produced. Keyframes are asymmetric day-weighted with a night blue-moonlight floor ≥ ~45% day intensity.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create dayNightMath.test.ts — exhaustive pure-helper suite (Wave 0 gap)</name>
  <read_first>
    - src/game/systems/__tests__/windMath.test.ts (vitest structure: describe/it/expect, import the pure module, pin constants where they are a contract and pin behavior everywhere else — mirror this)
    - src/game/systems/dayNightMath.ts (the module under test, from Task 1)
    - .planning/phases/09-atmosphere-day-night/09-VALIDATION.md §"Wave 0 Requirements" (this file closes the sole Wave 0 gap)
  </read_first>
  <files>src/game/systems/__tests__/dayNightMath.test.ts</files>
  <action>
    Create the vitest suite mirroring `windMath.test.ts`. Cover: (1) phase01 wraparound — phase01(0n)===0, phase01(CYCLE_MICROS)===0, phase01(CYCLE_MICROS/2n)≈0.5; (2) modulo precision at a large realistic micros epoch (~1.78e15) — result strictly in [0,1); (3) negative-input guard returns [0,1); (4) night-band exposure floor — loop the night band asserting sunIntensity and hemiIntensity each stay ≥ ~0.45 of the day-peak value at every sampled phase (DAYNITE-03); (5) lanternLevel === 0 in the day band, === 1 in the night band, monotonic ramp over dusk (up) and dawn (down) (DAYNITE-04); (6) fireflyLevel 0 in day, non-zero only in dusk/night; (7) smoothstep monotonicity + endpoints; (8) wraparound continuity — samplePalette(0.99) blends toward samplePalette(0.00) with no daylight spike on horizon/skyTop. Pin exact keyframe literals only where they are a contract (as windMath.test.ts pins the nine SWAY literals); pin behavior/bounds everywhere else.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Suite is green. Covers DAYNITE-01 (day-weighted phase + smoothstep blend), DAYNITE-02 (bigint-modulo-before-Number precision + wraparound), DAYNITE-03 (night ≥45% floor), DAYNITE-04 (lanternLevel band boundaries). No watch-mode flags. Runs < 10s.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server reducer timestamp → client clock | Read-only bigint consumed downstream (Plan 04/05); this plan only defines the modulo math |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | phase01 bigint→Number conversion | low | mitigate | Modulo taken on the bigint BEFORE Number() (D-08); asserted in dayNightMath.test.ts at ~1.78e15 micros so a large timestamp cannot overflow/precision-drift into a wrong time-of-day |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed this plan — all deps (three, vitest) already present; RESEARCH Package Legitimacy Audit: nothing to add |

No high-severity threats. This is pure, deterministic, THREE-free client math with no network/auth/IO surface.
</threat_model>

<verification>
- `pnpm exec tsc -b` clean.
- `pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts` green, < 10s.
- dayNightMath.ts imports nothing (grep confirms zero import lines).
</verification>

<success_criteria>
The deterministic phase/palette math exists and is fully unit-tested: day-weighted 20-min phase, smoothstep keyframe blend, night ≥45% exposure floor, lanternLevel/fireflyLevel band scalars, bigint-modulo-before-Number precision, and continuous wraparound.
</success_criteria>

<output>
Create `.planning/phases/09-atmosphere-day-night/09-01-SUMMARY.md` when done.
</output>
