---
phase: 09-atmosphere-day-night
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/world/createMondstadtWorld.ts
autonomous: true
requirements: [ATMO-01, ATMO-02, ATMO-03]

must_haves:
  truths:
    - "A vertical gradient sky (top=sky key, bottom=horizon) is visible behind the world; distant terrain dissolves into the horizon/fog color and the world edge is hidden"
    - "The sky-dome bottomColor uniform IS the same THREE.Color instance as scene.fog.color — fog and sky-bottom physically cannot diverge (ATMO-02)"
    - "Fog near stays ≥ ~80 (well past SAFE_ZONE_RADIUS=18 + engage range) so telegraphs/enemies/gems inside the gameplay radius keep full contrast (ATMO-03); far dissolves the world edge (~250–320)"
    - "createMondstadtWorld returns an ambience: AmbienceHandles object (skyLight, sunLight, fog, background, lanternLights, setSkyTop) that day/night writes through — day/night never touches the scene directly"
  artifacts:
    - src/game/world/createMondstadtWorld.ts
  key_links:
    - "scene.fog and scene.background are mutated IN PLACE forever (never reassigned, never type-swapped) — reassign = full-scene USE_FOG recompile storm + breaks the overlay pass save/restore-by-reference"
    - "The sun DIRECTION basis (sunDirection/sunRight/sunUp, SUN_OFFSET, setShadowFocus) is untouched — day/night drifts .color/.intensity only (D-02)"
---

<objective>
Widen `createMondstadtWorld` to expose an `AmbienceHandles` object and add the gradient sky-dome that single-sources its bottom color with `scene.fog.color`, then tune fog near/far so distant terrain dissolves into the sky while combat stays crisp inside the gameplay radius. Fog and background already exist (`Fog(0x8ecae6, 80, 300)`, `Color(0x8ecae6)`) — this plan couples them to a sky and hands day/night the write handles; it does NOT yet drift anything (Plan 04 owns the per-frame drift).

Purpose: Establish the ATMO-02 load-bearing single-source contract (one THREE.Color feeds fog + sky-bottom) and the handle surface the day/night cycle mutates through.
Output: Modified `createMondstadtWorld.ts` — new `AmbienceHandles` type, widened `createLighting` return, sky-dome ShaderMaterial, tuned fog, `ambience` on the return object + `MondstadtWorld` interface.
</objective>

<artifacts_produced>
NEW symbols created by this plan:
- `AmbienceHandles` interface (exported from createMondstadtWorld.ts): `{ skyLight: THREE.HemisphereLight; sunLight: THREE.DirectionalLight; fog: THREE.Fog; background: THREE.Color; lanternLights: THREE.PointLight[]; setSkyTop(c: THREE.Color): void }`.
- `ambience: AmbienceHandles` field on the `MondstadtWorld` interface + return object.
- A sky-dome factory/helper (executor names it, e.g. `createSkyDome`) building an inward BackSide sphere with a 2-uniform gradient `ShaderMaterial` (topColor, bottomColor).
- Widened `createLighting` return shape `{ skyLight, sunLight }`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atmosphere-day-night/09-RESEARCH.md
@.planning/phases/09-atmosphere-day-night/09-PATTERNS.md
@src/game/world/createMondstadtWorld.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen createLighting + define AmbienceHandles + return it</name>
  <read_first>
    - src/game/world/createMondstadtWorld.ts:129-153 (createLighting — skyLight HemisphereLight at :130 is a local const, fn returns only sunLight; widen to return both)
    - src/game/world/createMondstadtWorld.ts:42-66 (MondstadtWorld interface — add ambience field)
    - src/game/world/createMondstadtWorld.ts:369-372, 484-556 (createLighting call site + the return object to extend)
    - .planning/phases/09-atmosphere-day-night/09-RESEARCH.md §"Pattern 6" (AmbienceHandles shape) and §"Pattern 3" (single-source contract)
  </read_first>
  <files>src/game/world/createMondstadtWorld.ts</files>
  <action>
    Change `createLighting` to `return { skyLight, sunLight }` (expose the HemisphereLight const at :130 alongside the DirectionalLight). Update its call site (`:369`) to destructure both. Define and export an `AmbienceHandles` interface with skyLight (HemisphereLight), sunLight (DirectionalLight), fog (THREE.Fog), background (THREE.Color), lanternLights (THREE.PointLight[] — leave as an empty array here; Plan 03 populates it), and setSkyTop(c: THREE.Color): void. Add `ambience: AmbienceHandles` to the `MondstadtWorld` interface (:42-66). On the return object (:484), add the `ambience` object wiring skyLight/sunLight, `fog: scene.fog`, `background: scene.background as THREE.Color`, `lanternLights`, and a `setSkyTop` that copies the passed color into the sky-dome topColor uniform (Task 2 creates that uniform — declare a `let skyTopUniform` scratch this task can reference, or fold setSkyTop into Task 2). Do NOT touch the sun DIRECTION basis (sunDirection/sunRight/sunUp at :124-127, SUN_OFFSET, setShadowFocus at :535) — day/night drifts sunLight.color/.intensity only (D-02). Do NOT drift any color this plan; just expose the handles.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    createLighting returns { skyLight, sunLight }; AmbienceHandles is exported; world.ambience exists on the interface and return object with all six members; sun direction basis untouched. tsc clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add gradient sky-dome (bottomColor === fog.color) + tune fog near/far</name>
  <read_first>
    - src/game/world/createMondstadtWorld.ts:218-223 (scene.background + scene.fog creation — mutate in place, never reassign)
    - src/game/world/createMondstadtWorld.ts:450-457 (scene.add(group); world freeze — add the dome to the scene OUTSIDE the frozen group)
    - .planning/phases/09-atmosphere-day-night/09-RESEARCH.md §"Pattern 3" (classic three 2-uniform gradient-sky shader; fog:false, depthWrite:false, BackSide) and §"Pattern 5" (fog tuning: near ≥ 80 >> SAFE_ZONE_RADIUS=18, far ~250–320; never Fog→FogExp2)
    - .planning/research/PITFALLS.md (Pitfall 4 fog/background reassign recompile; Pitfall 5 unlit-at-night)
  </read_first>
  <files>src/game/world/createMondstadtWorld.ts</files>
  <action>
    Build an inward-facing sky dome: a large BackSide sphere with a 2-uniform gradient `ShaderMaterial` (topColor, bottomColor, plus the classic offset/exponent horizon-falloff uniforms) — the standard three.js gradient-sky shader. Prefer a fixed-origin dome with radius ≫ fog.far (RESEARCH Open Question 1: acceptable if the horizon shift proves invisible; avoids per-frame camera tracking / frame wiring). Set the material `fog: false`, `depthWrite: false`, and render it behind everything (add to `scene` OUTSIDE the frozen `world.group`). Enforce the ATMO-02 / D-04 single-source contract: set `skyMat.uniforms.bottomColor.value = scene.fog.color` so the dome bottom IS the same THREE.Color instance as the fog color (they can never diverge). Wire `setSkyTop(c)` to copy into `skyMat.uniforms.topColor.value` in place. Tune fog in place per D-05 + D-06: keep `scene.fog.near` ≥ ~80 (well past SAFE_ZONE_RADIUS=18 + engage range → ATMO-03 combat contrast) and set `scene.fog.far` into ~250–320 to dissolve the world edge (WORLD_BOUND=130 → ATMO-01); mutate `.near`/`.far`/`.color` on the existing Fog object IN PLACE (D-05 — shared object identity), NEVER `new Fog()`, never a Texture background, never Fog→FogExp2 (Pitfall 4). Keep scene.background a Color mutated in place (the dome occludes it; it is the fallback fill). Leave day/night colors at their current daytime hex — no drift this plan.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    A gradient sky-dome renders behind the world; its bottomColor uniform is the identical THREE.Color object as scene.fog.color (assert-by-construction: same reference, not a copy). Fog near ≥ 80, far in ~250–320, mutated in place. scene.fog and scene.background object identities are never reassigned. setSkyTop writes the topColor uniform. tsc clean; `pnpm build` succeeds (shader compiles).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure client render setup — no network input, no auth, no user data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Denial of Service | scene.fog / scene.background reassignment | low | mitigate | Fog/background mutated in place forever; never reassigned or type-swapped — prevents the full-scene USE_FOG shader recompile hitch (Pitfall 4). Verified by renderer.info.programs.length staying constant across a cycle (Plan 05 playtest) |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — sky-dome uses three built-ins (ShaderMaterial); RESEARCH Package Legitimacy Audit: nothing to add |

No high-severity threats. Client-only cosmetic render; ATMO-03 explicitly preserves combat readability (fog near beyond gameplay radius).
</threat_model>

<verification>
- `pnpm exec tsc -b` clean.
- `pnpm build` succeeds (the sky-dome ShaderMaterial compiles).
- Grep confirms `scene.fog =` / `scene.background =` appear only once each (original creation) — no reassignment added.
- `bottomColor.value` assigned from `scene.fog.color` (same-reference contract) present in source.
</verification>

<success_criteria>
The world has a gradient sky whose bottom color is physically the fog color, fog is tuned to dissolve the world edge while keeping the gameplay radius crisp, and `createMondstadtWorld` exposes the `AmbienceHandles` day/night will drive — with the sun direction and fog/background object identities untouched.
</success_criteria>

<output>
Create `.planning/phases/09-atmosphere-day-night/09-02-SUMMARY.md` when done.
</output>
