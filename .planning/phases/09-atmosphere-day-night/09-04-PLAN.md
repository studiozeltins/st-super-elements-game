---
phase: 09-atmosphere-day-night
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/game/net/createServerClock.ts
  - src/game/systems/createDayNightCycle.ts
autonomous: true
requirements: [DAYNITE-01, DAYNITE-02, DAYNITE-03, DAYNITE-04]

must_haves:
  truths:
    - "createServerClock exposes anchor(serverMicros) + nowMicros(); nowMicros returns a Date.now()-based estimate until the first anchor, then baseServerMicros + performance.now() delta (D-08)"
    - "createDayNightCycle.update() pulls phase01(clock.nowMicros()) from dayNightMath, samples the palette, and writes fog.color / sky-top / sun+hemi color+intensity / lantern intensity through AmbienceHandles — zero per-frame allocation"
    - "The single scratch horizon THREE.Color feeds BOTH fog.color and the sky-bottom (already the same reference) — ATMO-02 single-source drift"
    - "When disabled (?nodaynight), a neutral day keyframe is applied ONCE and update() is a no-op (FPS bisection freeze, D-09)"
  artifacts:
    - src/game/net/createServerClock.ts
    - src/game/systems/createDayNightCycle.ts
  key_links:
    - "All palette math is delegated to dayNightMath (THREE-free); createDayNightCycle only does Color.lerpColors into preallocated scratch Colors, then .copy() into live fog/uniform/light colors — never new Color() per frame"
    - "Sun .color/.intensity drift only; the sun DIRECTION basis is never touched (D-02). Lantern lights get .intensity = base * lanternLevel (no add/remove)"
---

<objective>
Deliver the day/night runtime: `createServerClock.ts` (server-micros anchor + `Date.now()`-fallback estimator, reusing the `createAttackViewClock` pattern) and `createDayNightCycle.ts` (the sibling factory mirroring `createWind.ts` — pulls the phase, samples the `dayNightMath` palette, and writes colors/intensities/lantern-fade through `AmbienceHandles` with zero per-frame allocation). This plan produces the two runtime modules; Plan 05 constructs and wires them into the frame loop and the LAN-sync tap.

Purpose: DAYNITE-01/03 color drift + DAYNITE-04 lantern fade + the DAYNITE-02 clock estimator, all behind the pure-twin + mutate-in-place discipline.
Output: `src/game/net/createServerClock.ts`, `src/game/systems/createDayNightCycle.ts`.
</objective>

<artifacts_produced>
NEW symbols created by this plan:
- `createServerClock.ts` exports: `createServerClock(): ServerClock` and `ServerClock` interface `{ anchor(serverMicros: bigint): void; nowMicros(): bigint }`.
- `createDayNightCycle.ts` exports: `createDayNightCycle(enabled: boolean, clock: ServerClock, ambience: AmbienceHandles): DayNightCycle` and `DayNightCycle` interface `{ update(): void }` (executor may add `applyNeutral()` internally). Module-const keyframe `THREE.Color`s + preallocated scratch Colors.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atmosphere-day-night/09-RESEARCH.md
@.planning/phases/09-atmosphere-day-night/09-PATTERNS.md
@src/game/systems/createWind.ts
@.planning/phases/09-atmosphere-day-night/09-01-SUMMARY.md
@.planning/phases/09-atmosphere-day-night/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create createServerClock.ts — anchor + nowMicros estimator (Date.now fallback)</name>
  <read_first>
    - src/game/systems/createAttackViewClock.ts:65-92 (the proven server-clock estimator: base + BigInt(round((performance.now()-basePerf)*1000)), and the anchor capture on arrival — reuse this shape verbatim)
    - .planning/phases/09-atmosphere-day-night/09-RESEARCH.md §"Pattern 2" (the exact createServerClock target module) and §"Guards"
  </read_first>
  <files>src/game/net/createServerClock.ts</files>
  <action>
    Create the new `src/game/net/` directory and `createServerClock.ts`. Export a `ServerClock` interface `{ anchor(serverMicros: bigint): void; nowMicros(): bigint }` and `createServerClock(): ServerClock`. Internals: `baseServerMicros: bigint | null = null`, `basePerfMs = 0`. `anchor(serverMicros)` sets `baseServerMicros = serverMicros; basePerfMs = performance.now()` (a setter called from a table callback, NEVER a render). `nowMicros()` returns `BigInt(Math.round(Date.now() * 1000))` while `baseServerMicros === null` (the fallback until the first tick anchors), else `baseServerMicros + BigInt(Math.round((performance.now() - basePerfMs) * 1000))`. This is the exact createAttackViewClock estimator. No THREE import.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    createServerClock.ts exists under src/game/net/, exports ServerClock + createServerClock, nowMicros falls back to Date.now()*1000 before first anchor and uses the performance.now() delta after. tsc clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create createDayNightCycle.ts — phase→palette→AmbienceHandles, zero-alloc</name>
  <read_first>
    - src/game/systems/createWind.ts:1-68 (the sibling-factory precedent: interface, createX(enabled) constructor, held-by-reference mutable state, delegates ALL math to the pure twin, one update() called only by createGame.frame() — mirror the doc framing + structure)
    - src/game/systems/dayNightMath.ts (phase01, samplePalette, CYCLE_MICROS, lanternLevel/fireflyLevel — the pure twin from Plan 01)
    - src/game/world/createMondstadtWorld.ts (AmbienceHandles type + setSkyTop from Plan 02; fog.color is already the sky-bottom reference)
    - .planning/phases/09-atmosphere-day-night/09-RESEARCH.md §"Pattern 3" (zero-alloc lerpColors-into-scratch write path) + §"Pattern 4" (?nodaynight neutral freeze)
  </read_first>
  <files>src/game/systems/createDayNightCycle.ts</files>
  <action>
    Create `createDayNightCycle.ts` mirroring `createWind.ts`. Copy the factory doc framing (the ONE day/night source; consumers hold the OBJECTS, .value/.color mutated in place). Export a `DayNightCycle` interface `{ update(): void }` and `createDayNightCycle(enabled: boolean, clock: ServerClock, ambience: AmbienceHandles): DayNightCycle`. Preallocate module-const keyframe `THREE.Color`s (built once from the dayNightMath hex fields) and preallocated scratch Colors — NEVER `new THREE.Color()` per frame. `update()`: pull `phase01(clock.nowMicros())`, `samplePalette(phase)`, then `THREE.Color.prototype.lerpColors` / `.setHex` into scratch Colors and `.copy()` them into `ambience.fog.color` (the horizon scratch — this is the SAME instance as the sky-bottom, so fog + sky-bottom drift together, ATMO-02), `ambience.setSkyTop(scratchSkyTop)`, `ambience.sunLight.color` + `.intensity`, `ambience.skyLight.color`/`.groundColor`/`.intensity`, and each `ambience.lanternLights[i].intensity = LANTERN_BASE * palette.lanternLevel`. Drift the sun/hemi .color and .intensity ONLY — never touch the sun direction (D-02). When `!enabled` (`?nodaynight`, D-09): apply a neutral day keyframe ONCE in the constructor and make `update()` a no-op. On first construction, apply the current phase's palette immediately (snap, no 30s sunrise on load — Pitfall 6). Do NOT re-tint any material (let the lights carry the mood). This factory is the ONLY writer of the ambience handles.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    createDayNightCycle compiles against dayNightMath + AmbienceHandles + ServerClock; update() does zero allocation (only lerpColors/.copy/.setHex/.intensity into preallocated objects); disabled mode applies a neutral key once and no-ops update(); sun direction untouched; lantern intensity = base * lanternLevel. tsc clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server reducer timestamp → serverClock.anchor | Untrusted/optional server bigint drives a COSMETIC clock only; never gates gameplay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | serverClock anchor (malicious/absent server timestamp) | low | accept | A wrong/absent timestamp only shifts the local time-of-day (cosmetic). Date.now() fallback bounds the absent case within NTP skew (≪1s, invisible on a 20-min cycle). The phase is client-computed and NEVER gates spawns/drops/damage — it stays 100% cosmetic (Security Domain) |
| T-09-02 | Information Disclosure | phase used as a gameplay gate | low | mitigate | Design constraint enforced: createDayNightCycle only writes fog/sky/light colors + lantern intensity — no gameplay branch reads the phase. Any future time-gated gameplay must come from a server reducer, not this clock |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — three + existing modules only |

No high-severity threats. The clock is spoofable-by-design and kept strictly cosmetic; `Date.now()` fallback guarantees DAYNITE-02 either way.
</threat_model>

<verification>
- `pnpm exec tsc -b` clean; `pnpm test` full suite green (dayNightMath tests still pass).
- Grep confirms no `new THREE.Color(` inside `createDayNightCycle.update` (zero per-frame alloc) — scratch/keyframe Colors constructed once at module/factory scope only.
- Grep confirms createDayNightCycle never assigns sunLight.position / sun basis (direction frozen).
</verification>

<success_criteria>
The day/night runtime exists: a server-anchored clock with Date.now() fallback and a zero-alloc cycle factory that drifts fog/sky/light colors + lantern intensity through the ambience handles, delegates all math to the tested pure twin, freezes neutrally under `?nodaynight`, and never moves the sun or re-tints a material.
</success_criteria>

<output>
Create `.planning/phases/09-atmosphere-day-night/09-04-SUMMARY.md` when done.
</output>
