---
phase: 13-camera-feel
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/game/systems/createCameraFeel.ts
autonomous: true
requirements: [CAM-03, CAM-04]

must_haves:
  truths:
    - "kickFov(now) fires a two-phase FOV punch, rate-gated by KICK_COOLDOWN_S (CAM-03)"
    - "camera.updateProjectionMatrix() is called ONLY when projectionActive is true (D-07)"
    - "shake(mag) accumulates via Math.max and decays exponentially each apply() (absorbed block)"
    - "setReduceMotion(true) no-ops future kicks/shakes AND snaps in-flight fovOffset+shake to 0 with one final projection rebuild (CAM-04/Pitfall 6)"
    - "setPixelScale(n) pushes the pixel-mode magnitude factor in (renderer has no getter)"
    - "apply(desiredPosition,dt) mutates desiredPosition in place with the shake offset — zero per-frame alloc"
  artifacts:
    - "src/game/systems/createCameraFeel.ts (factory + CameraFeel interface)"
  key_links:
    - "createCameraFeel consumes cameraFeelMath (smooth/startKick/stepFovKick/projectionActive/canKick/CAMERA_FEEL)"
    - "wired into createGame.updateCamera + crit handlers by 13-04"
---

<objective>
Create the new `createCameraFeel.ts` system: the single owner of all discretionary CAMERA
motion — the two-phase FOV kick (CAM-03) and the combat camera shake absorbed wholesale from
createGame's existing block (no-legacy rule). It delegates every spring/gate decision to the
pure `cameraFeelMath` twin from 13-01, holds its state in preallocated fields (zero per-frame
alloc), gates `updateProjectionMatrix()` per D-07, and exposes the reduce-motion + pixel-scale
controls CAM-04 needs.

Purpose: One owner for FOV + shake means the reduce-motion toggle has a single place to zero all
camera motion, and the projection rebuild is gated in one auditable spot.
Output: `src/game/systems/createCameraFeel.ts` (~100 LOC).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-camera-feel/13-RESEARCH.md
@.planning/phases/13-camera-feel/13-PATTERNS.md

# Factory/interface/no-alloc convention to copy:
@src/game/systems/createWind.ts
# The pure twin this delegates to (from 13-01):
@src/game/systems/cameraFeelMath.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createCameraFeel factory — FOV kick + absorbed shake + reduce-motion owner</name>
  <files>src/game/systems/createCameraFeel.ts</files>
  <read_first>
    - src/game/systems/createWind.ts — copy the `createX(flag): X` factory shape, the exported interface with a per-frame method, and the "constructed ONCE, mutate via state, zero per-frame allocs" discipline (preallocate the scratch, never build literals per frame).
    - src/game/systems/cameraFeelMath.ts — the helpers to delegate to: `smooth`, `CAMERA_FEEL`, `FovKickState`, `startKick`, `stepFovKick`, `projectionActive`, `canKick`.
    - .planning/phases/13-camera-feel/13-PATTERNS.md §"createCameraFeel.ts" (the shake constants/block to ABSORB: decay rate 7, floor 0.005, the desiredPosition += (random-0.5)*2*mag block) and §"Pattern 4" in RESEARCH.md (gated projection rebuild, wasActive settle frame).
  </read_first>
  <behavior>
    - kickFov(now): if reduceMotion -> return; if !canKick(now,lastKickAt) -> return (rate gate); else set lastKickAt=now and startKick(fovState).
    - apply(desiredPosition,dt): advance FOV via stepFovKick(fovState,dt); when projectionActive(fovState.offset,wasActive) set camera.fov = BASE_FOV + offset and call updateProjectionMatrix() (the ONLY per-frame call site, D-07); track wasActive for the settle frame. Then add shake: if shakeMagnitude>0, add +/- random*shakeMagnitude to desiredPosition x/y/z, decay `*= exp(-DECAY*dt)`, snap to 0 below floor.
    - shake(mag): if reduceMotion -> no-op; else shakeMagnitude = max(shakeMagnitude, mag).
    - setReduceMotion(enabled): store flag; when true, snap fovState.offset=0, fovState.phase='idle', shakeMagnitude=0, set camera.fov=BASE_FOV and call updateProjectionMatrix() once (restore base — Pitfall 6).
    - setPixelScale(n): store the pixel-mode magnitude factor (consumed by the model in 13-03; kept here too if any camera effect later needs it) — accept and store; forward-compatible.
  </behavior>
  <action>
    Create `src/game/systems/createCameraFeel.ts` exporting a `CameraFeel` interface and a
    `createCameraFeel(opts: { camera: THREE.PerspectiveCamera; reduceMotion: boolean }): CameraFeel`
    factory. Internally hold: one `FovKickState` object, `lastKickAt = -Infinity`, `wasActive =
    false`, `shakeMagnitude = 0`, `reduceMotion` (seeded from opts), `pixelScale`, and the shake
    constants DECAY (7) + FLOOR (0.005) ABSORBED from createGame's block (they are being deleted
    from createGame in 13-04 — this is their new home, no duplication). Expose `apply(desiredPosition:
    THREE.Vector3, dt: number): void`, `kickFov(now: number): void`, `shake(mag: number): void`,
    `setReduceMotion(enabled: boolean): void`, `setPixelScale(n: number): void`. Delegate ALL
    spring/gate math to cameraFeelMath (no new spring idiom). Mutate `desiredPosition` in place and
    reuse no per-frame literals. Gate `updateProjectionMatrix()` strictly via `projectionActive`
    plus the one settle frame (D-07 / RESEARCH Pattern 4). The `Math.random()` shake jitter matches
    the absorbed block exactly. Keep the file under 300 LOC.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>
    createCameraFeel.ts compiles; exposes apply/kickFov/shake/setReduceMotion/setPixelScale; updateProjectionMatrix appears exactly at the gated site(s) (apply settle + setReduceMotion restore); shake constants live here; delegates springs to cameraFeelMath.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side render system; mutates local camera projection only. No network, storage, or user input crosses in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02a | Denial of Service | ungated updateProjectionMatrix / FOV strobe | low | mitigate | D-07 projection gate + KICK_COOLDOWN_S rate gate prevent per-frame rebuild and multi-crit strobe (a self-inflicted perf/UX DoS, not attacker-driven). |
</threat_model>

<verification>
- `npx tsc -b` clean (the new file typechecks against three@0.185.1 and cameraFeelMath).
- `npx vitest run src/game/systems/__tests__/cameraFeelMath.test.ts` still green (delegated logic unchanged).
- grep confirms `updateProjectionMatrix` only appears inside the projection-gated path and the setReduceMotion restore.
</verification>

<success_criteria>
A single camera-motion owner exists: FOV kick is two-phase + rate-gated + projection-gated (CAM-03),
shake is absorbed (no legacy block left for 13-04 to delete), and reduce-motion zeroes both instantly
(CAM-04). All spring math delegates to the 13-01 twin.
</success_criteria>

<output>
Create `.planning/phases/13-camera-feel/13-02-SUMMARY.md` when done.
</output>
