---
phase: 13-camera-feel
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/game/entities/createCharacterModel.ts
autonomous: true
requirements: [CAM-01, CAM-02, CAM-04]

must_haves:
  truths:
    - "The local character MODEL leans forward into run direction via a spring on bodyPivot.rotation.x (CAM-01, D-04 — model NOT camera)"
    - "Idle characters have a subtle breathing sway on bodyPivot.position.y, gated on !isMoving (CAM-02)"
    - "Lean/breathing apply ONLY when a motion config is passed — remote models (3-arg call) are unaffected (D-05 local-only)"
    - "reduceMotion in the motion config zeroes both lean and breathing (via cameraFeelMath) (CAM-04)"
    - "pixelScale from the motion config scales magnitude in pixel mode — breathing tuned most conservatively (D-01/D-03)"
    - "Lean/breathing ride bodyPivot.rotation.x and .position.y, which swings never touch (swings write .rotation.y only) — conflict-free"
  artifacts:
    - "src/game/entities/createCharacterModel.ts (animate signature extended with optional motion arg)"
  key_links:
    - "createCharacterModel consumes cameraFeelMath (smooth/leanTarget/breatheOffset/CAMERA_FEEL)"
    - "createGame passes the reused MOTION_CFG_SCRATCH into the LOCAL animate() call (wired in 13-04)"
---

<objective>
Extend `createCharacterModel.animate` with a spring-damped forward LEAN on the character model
(CAM-01) and a subtle idle BREATHING sway (CAM-02) — both on the model, never the camera (D-04).
Both are gated on an optional motion-config argument so only the local player (which 13-04 passes
the config to) is affected; remote models keep their 3-arg call and stay unchanged (D-05). All the
math delegates to the 13-01 `cameraFeelMath` twin, so reduce-motion zeroing (CAM-04) and pixel-mode
scaling (D-01/D-03) are inherited for free.

Purpose: Lean and breathing are MODEL concerns (CAM-01/CAM-02 are explicit; D-04 overrides the
research camera-roll pattern). Riding the free `bodyPivot.rotation.x` / `.position.y` channels
avoids the world-space Euler trap and the combat-swing conflict.
Output: modified `src/game/entities/createCharacterModel.ts` (~25 LOC added).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-camera-feel/13-RESEARCH.md
@.planning/phases/13-camera-feel/13-PATTERNS.md

# The pure twin this delegates to (from 13-01):
@src/game/systems/cameraFeelMath.ts
# The file being modified — read the structure + swing/bob idioms:
@src/game/entities/createCharacterModel.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model lean spring + idle breathing sway on the free bodyPivot channels</name>
  <files>src/game/entities/createCharacterModel.ts</files>
  <read_first>
    - src/game/entities/createCharacterModel.ts — the `animate` interface declaration (:11) and impl (:235); the `group -> bodyPivot` structure (:138-139); the swing code that writes `bodyPivot.rotation.y` ONLY (:211, :228 — proving .x/.position.y are free); the existing idle head-bob `Math.sin(elapsedSeconds*3)*0.02` (:250) — the empirical pixel-safe amplitude ceiling to stay under.
    - src/game/systems/cameraFeelMath.ts — `smooth`, `leanTarget`, `breatheOffset`, `CAMERA_FEEL.LEAN_K`.
    - .planning/phases/13-camera-feel/13-RESEARCH.md §"Pattern 2" (why bodyPivot.rotation.x pitches in the facing frame, not world space — Pitfall 1) and §"Pattern 3" (breathing must be positional/scale, not rotational, in pixel mode — D-03).
  </read_first>
  <behavior>
    - When a motion config is provided (local player only): leanX spring state closes toward leanTarget(isMoving, motion.reduceMotion, motion.pixelScale) at CAMERA_FEEL.LEAN_K each frame; bodyPivot.rotation.x = leanX. Running -> body pitches forward into the facing direction; stopping -> springs back to upright.
    - bodyPivot.position.y = breatheOffset(elapsedSeconds, isMoving, motion.reduceMotion, motion.pixelScale): a slow subtle rise/fall while standing still; exactly 0 while moving and while reduced.
    - When NO motion config is passed (remote models, current 3-arg callers): NO lean, NO breathing — behavior byte-identical to today.
  </behavior>
  <action>
    Extend the `CharacterModel.animate` signature (interface :11 and impl :235) with a 4th optional
    parameter `motion?` typed as a small `MotionConfig` interface `{ reduceMotion: boolean; pixelScale:
    number }` (declare it in this file and export it so createGame can build the scratch). Add a
    per-model `let leanX = 0` closure state beside the existing swing state (near :201) — per-model,
    NOT shared, so a future remote-breathing extension keeps independent state. Inside `animate`, AFTER
    the existing swing/walk/head-bob code (leave it untouched), add: `if (motion) { leanX = smooth(leanX,
    leanTarget(isMoving, motion.reduceMotion, motion.pixelScale), CAMERA_FEEL.LEAN_K, deltaSeconds);
    bodyPivot.rotation.x = leanX; bodyPivot.position.y = breatheOffset(elapsedSeconds, isMoving,
    motion.reduceMotion, motion.pixelScale); }`. Import the helpers from `../systems/cameraFeelMath`.
    Lean rides `bodyPivot.rotation.x` (child of the yaw-facing group, so .x pitches in the FACING frame
    — never `group.rotation.x`, which would lean in world space, Pitfall 1). Breathing stays positional
    (`.position.y`), never rotational, and inherits the conservative pixelScale for D-03. Do NOT
    texel-snap the lean/breathing values — snapping model rotations is explicitly rejected (D-02);
    pixel-crawl is handled by the conservative pixelScale, not quantization. Do not alter
    the remote/3-arg call path. Keep the file within the 300-LOC functional budget (if it is already
    near, extract only if you touch a cohesive block — do not gratuitously split).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
  </verify>
  <done>
    animate accepts an optional MotionConfig; local-config path springs bodyPivot.rotation.x toward the lean target and sets bodyPivot.position.y to the breathing offset; 3-arg remote calls are unchanged; compiles clean; cameraFeelMath tests still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side model animation; no input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03a | Denial of Service | idle breathing pixel-crawl | low | mitigate | Positional (not rotational) sway + conservative pixelScale (D-03) keep the standing-character pixel-mode case crawl-free; validated by the 13-04 manual playtest gate. |
</threat_model>

<verification>
- `npx tsc -b` clean.
- `npx vitest run src/game/systems/__tests__/cameraFeelMath.test.ts` still green.
- grep confirms lean rides `bodyPivot.rotation.x` (NOT `group.rotation.x`) and breathing rides `bodyPivot.position.y`.
- Perceptual correctness (lean reads right running E/W as well as N/S; standing breathing is calm and crawl-free in pixel mode) is validated in the 13-04 manual playtest.
</verification>

<success_criteria>
The local character leans into run direction and breathes while idle — on the MODEL (CAM-01/CAM-02,
D-04), zeroed by reduce-motion and scaled in pixel mode (CAM-04/D-01/D-03) via the delegated twin.
Remote models unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-camera-feel/13-03-SUMMARY.md` when done.
</output>
