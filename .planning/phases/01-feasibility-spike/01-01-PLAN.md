---
phase: 01-feasibility-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/vendor/threejs-water-pro/"
  - "src/vendor/threejs-sky-pro/"
  - ".gitmodules"
  - ".gitignore"
  - "vite.config.ts"
  - "waterpro-spike.html"
  - "src/spike/waterpro-spike.ts"
  - "src/game/engine/tsl/pixelFilterNode.ts"
autonomous: true
requirements: [STCK-01, STCK-03, SPIKE-01]
user_setup:
  - service: github
    why: "D-01 vendored-bundle git policy: paid Water Pro + Sky Pro bundles live in a PRIVATE repo added as a submodule so the PUBLIC main repo stays license-clean. Task 1 creates it via `gh repo create`; if gh is not authenticated an auth gate will surface at run time."
    dashboard_config:
      - task: "Confirm the studiozeltins org can host a new private repo (or approve the chosen owner/name for the vendor submodule)"
        location: "github.com/studiozeltins"

must_haves:
  truths:
    - "Water Pro v3.2.1 + Sky Pro v2.0.0 prebuilt bundles import from `src/vendor/` via `three/webgpu` + `three/tsl` with zero new runtime deps (STCK-01)"
    - "The paid `src/vendor/**` bundles are NOT committed to the public main repo history — they live in a private submodule (STCK-03, D-01)"
    - "`waterpro-spike.html` boots a WebGPURenderer + WaterSystem + Sky over a getTerrainHeight-sampled beach plane at the game's 45deg tilted camera, and logs the resolved backend after init (SPIKE-01 tracer)"
    - "`pnpm build` emits both the game entry and the spike entry without import errors"
  artifacts:
    - "src/vendor/threejs-water-pro/ (index.js + index.d.ts, private submodule)"
    - "src/vendor/threejs-sky-pro/ (index.js + index.d.ts + data/, private submodule)"
    - "waterpro-spike.html (2nd Vite entry, repo root)"
    - "src/spike/waterpro-spike.ts (async bootstrap + rAF loop)"
    - "src/game/engine/tsl/pixelFilterNode.ts (minimal pixel node — expanded in plan 02)"
    - ".gitmodules"
  key_links:
    - "waterpro-spike.ts imports WaterSystem/SkySystem from src/vendor/ (fails if bundles missing or aliased to their src/)"
    - "water.setSky(sky.createSkyProvider({ envMap: true })) couples sky->water exactly once"
    - "vite.config.ts rollupOptions.input registers the spike entry so `vite build` emits it"
---

<objective>
Vendor the purchased Water Pro + Sky Pro bundles under a private submodule (applying D-01), register the spike as a second Vite entry, and land the leading TRACER: the thinnest end-to-end `waterpro-spike.html` that wires `WebGPURenderer` -> `WaterSystem` -> Sky -> a minimal TSL pixel node over a `getTerrainHeight`-sampled beach plane at the game's tilted camera, confirming the backend resolves.

Purpose: prove the entire WebGPU + vendored-Pro + TSL-post chain works end-to-end on ONE path before any expansion (fidelity, both resolution shapes, perf capture, de-risk). If the chain cannot boot, we learn it here on the first commit, not after four layers.
Output: importable vendored bundles, a booting spike page, and a stub pixel-filter salvage module that plan 02 expands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.4.0-alpha-WEBGPU-WATERPRO-HANDOFF.md
@.planning/phases/01-feasibility-spike/01-CONTEXT.md
@.planning/phases/01-feasibility-spike/01-RESEARCH.md
@.planning/phases/01-feasibility-spike/01-PATTERNS.md
</context>

<artifacts>
## Artifacts this plan produces
- `src/vendor/threejs-water-pro/` — copied prebuilt `build/` (`index.js`, `index.js.map`, `index.d.ts`), private submodule
- `src/vendor/threejs-sky-pro/` — copied prebuilt `build/` incl. `data/` (cloud noise), private submodule
- `.gitmodules` + a private vendor repo (e.g. `studiozeltins/st-super-elements-vendor`)
- `waterpro-spike.html` — 2nd Vite entry at repo root (served, never in game `dist/`)
- `src/spike/waterpro-spike.ts` — async `main()` bootstrap + rAF loop (tracer)
- `src/game/engine/tsl/pixelFilterNode.ts` — minimal nearest-downsample node (salvage stub; plan 02 adds both shapes + rim)
- `vite.config.ts` — gains `build.rollupOptions.input` for the spike entry (react plugin + server block preserved)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Build + vendor the Pro bundles into a private submodule and register the spike Vite entry</name>
  <files>src/vendor/threejs-water-pro/, src/vendor/threejs-sky-pro/, .gitmodules, .gitignore, vite.config.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-RESEARCH.md (Installation/vendoring block; Standard Stack; Security Domain V14)
    - .planning/phases/01-feasibility-spike/01-PATTERNS.md ("waterpro-spike.html (entry) + vite.config.ts (modify)" section — the exact rollupOptions.input shape)
    - pro/Three.js Water Pro v3.2.1/threejs-water-pro/docs/guide/installation.md
    - pro/Three.js Sky Pro v2.0.0/threejs-sky-pro/docs/guide/installation.md
    - vite.config.ts (current 11-line config)
    - .gitignore (line 21 already ignores `pro/`)
  </read_first>
  <action>
    Build each purchased package's ESM bundle using ITS OWN npm (not the repo pnpm): in `pro/Three.js Water Pro v3.2.1/threejs-water-pro` run `npm install && npm run build:lib`, and the same in `pro/Three.js Sky Pro v2.0.0/threejs-sky-pro`. Apply D-01 (STCK-03): create the PRIVATE vendor repo with `gh repo create studiozeltins/st-super-elements-vendor --private` (if gh auth fails an auth gate will surface — retry after), then `git submodule add git@github.com:studiozeltins/st-super-elements-vendor.git src/vendor`. Copy Water Pro's `build/{index.js,index.js.map,index.d.ts}` into `src/vendor/threejs-water-pro/` and Sky Pro's `build/{index.js,index.js.map,index.d.ts,data/}` into `src/vendor/threejs-sky-pro/` — the Sky `data/` dir is MANDATORY (cloud noise loads relative to `index.js`). Commit those files INSIDE the submodule and push, so the paid code lives only in the private repo; the public main repo records a submodule gitlink, never the bundle blobs. Confirm `pro/` stays gitignored. Leave a one-line note in the submodule README that `.31` deploy needs `--recurse-submodules` + read-only private-repo auth (that wiring is Phase 2, not now). Then edit `vite.config.ts` to add `build: { rollupOptions: { input: { main: 'index.html', spike: 'waterpro-spike.html' } } }`, preserving the existing `react()` plugin and the `server: { host: true, allowedHosts: ['elements.kingdom.lv'] }` block verbatim.
  </action>
  <verify>
    <automated>node -e "require('fs').accessSync('src/vendor/threejs-water-pro/index.js'); require('fs').accessSync('src/vendor/threejs-sky-pro/index.js'); require('fs').accessSync('src/vendor/threejs-sky-pro/data'); console.log('vendored ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/vendor/threejs-water-pro/index.js`, `src/vendor/threejs-sky-pro/index.js`, and `src/vendor/threejs-sky-pro/data/` all exist on disk
    - `git ls-files src/vendor` shows a submodule gitlink (a single entry / mode 160000), NOT the bundle files tracked as blobs in the public repo
    - `.gitmodules` references the private vendor repo
    - `pro/` remains ignored (`git check-ignore pro` succeeds)
    - `vite.config.ts` has `rollupOptions.input.spike = 'waterpro-spike.html'` and still contains the `react()` plugin and the `allowedHosts: ['elements.kingdom.lv']` server block
  </acceptance_criteria>
  <reversibility rating="costly">D-01 submodule + public/private split: undoing means rewriting .gitignore, submodule wiring, and .31 deploy auth. Committing paid code to the public repo would be an irreversible license leak in public history — this task must keep bundles inside the private submodule only.</reversibility>
  <done>Both Pro bundles (incl. Sky `data/`) resolve from `src/vendor/` as a private submodule, the public repo records only a gitlink, and the spike is a registered Vite build input.</done>
</task>

<task type="tracer">
  <name>Task 2: TRACER — thinnest end-to-end WebGPU + Water + Sky + pixel-node over a beach plane</name>
  <files>waterpro-spike.html, src/spike/waterpro-spike.ts, src/game/engine/tsl/pixelFilterNode.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-PATTERNS.md ("src/spike/waterpro-spike.ts (bootstrap...)" — the verbatim async bootstrap order + post-chain + frame loop)
    - pro/Three.js Water Pro v3.2.1/threejs-water-pro/docs/guide/sky-pro-integration.md (verbatim recipe)
    - pro/Three.js Water Pro v3.2.1/threejs-water-pro/docs/guide/basic-example.md
    - src/game/engine/createPixelRenderer.ts (camera line 54: PerspectiveCamera(45,1,0.1,500); render loop lines 169-212)
    - src/game/world/terrain.ts (getTerrainHeight, SEA_LEVEL = -0.8, ISLANDS[0] city island beach arc)
    - node_modules/three/examples/jsm/tsl/display/PixelationPassNode.js (reference node structure)
  </read_first>
  <action>
    Wire ONE happy path end to end, no expansion. `waterpro-spike.html` at repo root is standalone (no React `#root`) and loads `<script type="module" src="/src/spike/waterpro-spike.ts">`. In `waterpro-spike.ts` write async `main()` following the sky-pro-integration recipe VERBATIM, adapting only the camera and appending the pixel node last: import from `three/webgpu` (NOT `three`) and `pass` from `three/tsl`; `const renderer = new THREE.WebGPURenderer()`; `renderer.setPixelRatio(Math.min(window.devicePixelRatio, 1))`; `renderer.toneMapping = THREE.ACESFilmicToneMapping`; `await renderer.init()` BEFORE reading `renderer.backend.isWebGPUBackend`; `const camera = new THREE.PerspectiveCamera(45, innerWidth/innerHeight, 0.1, 20000)` positioned/tilted to match the game's top-down view; add a single low-res `PlaneGeometry` beach mesh whose vertex Y comes from `getTerrainHeight(x,z)` sampled over the `ISLANDS[0]` beach arc with a plain `MeshStandardMaterial({ vertexColors:true, flatShading:true })`; `const water = await WaterSystem.create(renderer, scene, camera, 'medium')` then `water.loadPreset(getPresetParams('blackFlag'))`; `const sky = await SkySystem.create({ renderer, camera, scene, quality:'medium' })`, `await sky.applyPreset(PRESETS.partlyCloudy)`, `water.setSky(sky.createSkyProvider({ envMap:true }))` built ONCE; build the post chain `scenePass = pass(water.scene, water.camera)` -> `water.postProcessing.buildNode(...)` -> `sky.applyTo(...)` -> `pixelFilterNode(out, scenePass)` LAST -> `postProcessing.outputNode = out`; `await renderer.compileAsync(scene, camera)`. Frame loop is async: `sky.update(dt)` then `await water.update(dt)` then `postProcessing.render()` (NEVER `renderer.render()`). In `pixelFilterNode.ts` ship a MINIMAL nearest-downsample node (final-pixelate shape only, using `pixelationPass` as the structural reference) — no sun-facing rim yet; that is plan 02. Log the resolved backend and `water.backend` to the console so the tracer self-reports which backend booted. Extend `far` to 20000 per the ocean/sky clip pitfall.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <human-check>Open elements.kingdom.lv/waterpro-spike.html in headed Chrome; console logs backend 'WebGPU' (or 'WebGL2' fallback) and water.backend; the beach plane + sea + sky render pixelated with no import errors.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 and emits a spike bundle for `waterpro-spike.html`
    - `waterpro-spike.ts` imports `three/webgpu` (not bare `three`) and calls `postProcessing.render()` (not `renderer.render()`)
    - `renderer.backend.isWebGPUBackend` is read only AFTER `await renderer.init()`
    - The beach mesh Y values are sourced from `getTerrainHeight` and the sea plane / preset load without throwing
    - Headed Chrome shows the scene through the pixel node and logs the resolved backend
  </acceptance_criteria>
  <done>One end-to-end path (WebGPU -> Water -> Sky -> pixel node -> beach) boots in headed Chrome and builds clean; the architecture is proven before any expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local disk -> public git repo | Paid vendor bundles must never cross into public history |
| purchased ZIP -> vendored bundle | Bundle integrity (built locally, no registry fetch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Information Disclosure | `src/vendor/**` paid bundles vs PUBLIC main repo | high | mitigate | D-01 private submodule; public repo records only a gitlink; `git ls-files src/vendor` asserted to be a 160000 gitlink, never blobs; `pro/` stays gitignored |
| T-01-EP | Elevation of Privilege | `.31` deploy credential for the private submodule | low | accept | Read-only, scoped credential is a Phase 2 concern; flagged in the submodule README, not provisioned this phase |
| T-01-T | Tampering | vendored bundle contents | low | accept | Built locally via `npm run build:lib` from the purchased ZIPs on disk; no third-party registry fetch |
</threat_model>

<verification>
- `pnpm build` succeeds with the spike entry and the vendored submodule present.
- The public repo contains no paid bundle blobs (submodule gitlink only).
- The spike boots in headed Chrome and reports its backend.
</verification>

<success_criteria>
The vendored Pro bundles import cleanly from a private submodule, and the leading tracer renders the full WebGPU + Water + Sky + pixel-node chain end-to-end over a real-terrain beach plane.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike/01-01-SUMMARY.md` when done.
</output>
