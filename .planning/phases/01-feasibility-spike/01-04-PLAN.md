---
phase: 01-feasibility-spike
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - ".planning/phases/01-feasibility-spike/GO-NO-GO.md"
autonomous: false
requirements: [SPIKE-02]
must_haves:
  truths:
    - "A recorded go/no-go artifact set is assembled: spike-vs-master screenshots through the pixel filter (both shapes), WebGPU + WebGL2 FPS numbers, chosen camera far, Sky data/ dist outcome, the 17-shader estimate, and backend confirmation (SPIKE-02)"
    - "The user records a perceptual sign-off (D-02) — 'same pixel-art identity' = GO; unreproducible = STOP and keep the WebGL renderer (sanctioned escape hatch)"
    - "The verdict is recorded as an artifact that gates Phase 2 planning"
  artifacts:
    - ".planning/phases/01-feasibility-spike/GO-NO-GO.md (assembled artifact set + recorded verdict)"
  key_links:
    - "GO verdict unblocks Phase 2 (renderer migration); STOP verdict halts the milestone and keeps WebGL"
    - "The sign-off is perceptual (D-02), with the screenshot-diff as a visual aid only — not a numeric gate"
---

<objective>
Assemble the recorded go/no-go artifact set and capture the user's perceptual sign-off (D-02) with the sanctioned STOP escape hatch. This is the phase's terminal gate: a GO unblocks Phase 2 (renderer migration); a STOP halts the milestone and keeps the WebGL renderer — the pixel-art identity is sacred and not worth losing.

Purpose: make the make-or-break decision an explicit, recorded human judgment on complete evidence (both resolution shapes, both backends' FPS, tone/brightness parity), not an automated diff that would false-STOP on harmless TSL float noise.
Output: `GO-NO-GO.md` with the artifact set and the recorded verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feasibility-spike/01-CONTEXT.md
@.planning/phases/01-feasibility-spike/01-01-SUMMARY.md
@.planning/phases/01-feasibility-spike/01-02-SUMMARY.md
@.planning/phases/01-feasibility-spike/01-03-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `.planning/phases/01-feasibility-spike/GO-NO-GO.md` — the recorded artifact set (spike-vs-master screenshots through the filter for both shapes, WebGPU + WebGL2 FPS at medium tier, chosen camera far, Sky `data/` dist outcome, 17-shader estimate, backend confirmation) and the user's recorded GO/STOP verdict with rationale
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the recorded go/no-go artifact set</name>
  <files>.planning/phases/01-feasibility-spike/GO-NO-GO.md</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-CONTEXT.md (D-02 perceptual sign-off bar; specifics: artifact set = screenshots + FPS numbers)
    - .planning/phases/01-feasibility-spike/01-01-SUMMARY.md
    - .planning/phases/01-feasibility-spike/01-02-SUMMARY.md
    - .planning/phases/01-feasibility-spike/01-03-SUMMARY.md (FPS numbers + 17-shader estimate table)
  </read_first>
  <action>
    Write `GO-NO-GO.md` that consolidates the phase's evidence into one reviewable artifact: (1) side-by-side spike-vs-`master` screenshots through the pixel filter for BOTH `?shape=whole` and `?shape=final` (embed or link the captured images from the headed-Chrome runs); (2) the recorded WebGPU FPS and WebGL2 (`forceWebGL:true`) FPS at the `medium` tier with the on-screen backend proof; (3) the chosen camera `far` value and any tone-mapping (ACES vs neutral) note; (4) the Sky `data/` dist outcome (pass or the flagged failing path); (5) the 17-shader port-surface T-shirt estimate table; (6) a one-line backend confirmation (`renderer.backend.isWebGPUBackend`, `water.backend`). End the doc with an empty VERDICT section (GO / STOP + rationale + which resolution shape Phase 2 should adopt) for the human checkpoint to fill. Do not decide the verdict here — only assemble the evidence.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/01-feasibility-spike/GO-NO-GO.md','utf8'); if(!/VERDICT/.test(s)) process.exit(1); console.log('artifact assembled')"</automated>
  </verify>
  <acceptance_criteria>
    - `GO-NO-GO.md` exists and contains: both-shape screenshots (or links), WebGPU + WebGL2 FPS at medium tier, chosen camera far, Sky `data/` dist outcome, the 17-shader estimate table, and backend confirmation
    - The doc ends with an unfilled VERDICT section (GO/STOP + rationale + chosen shape)
  </acceptance_criteria>
  <done>All phase evidence is consolidated into one reviewable artifact ready for the human sign-off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The complete spike: WebGPURenderer + Water Pro + Sky Pro over a real-terrain beach slice, the TSL pixel filter in both resolution shapes with the sun-facing rim, both-backend FPS at the medium tier, and the lit-water + pooled-wake/spray de-risk — all consolidated in GO-NO-GO.md.</what-built>
  <how-to-verify>
    1. Open `elements.kingdom.lv/waterpro-spike.html` in headed Chrome alongside the `master` game (WebGL) build.
    2. Compare BOTH `?shape=whole` and `?shape=final` against `master` THROUGH the pixel filter — judge whether it is the "same pixel-art identity" (perceptual, per D-02; the screenshot-diff is an aid, NOT the gate).
    3. Review the recorded WebGPU vs WebGL2 FPS at the medium tier in GO-NO-GO.md — is the WebGPU number acceptable, and is the WebGL2 fallback tolerable?
    4. Confirm the lit-water and pooled-wake/spray de-risk read as viable, and glance at the 17-shader port-surface estimate.
    5. Record the verdict in GO-NO-GO.md: GO (which resolution shape Phase 2 adopts) or STOP.
    STOP escape hatch: if the pixel-art look cannot be reproduced acceptably, choosing STOP is sanctioned — the milestone halts and the WebGL renderer is kept. The identity is not worth losing.
  </how-to-verify>
  <resume-signal>Type "GO shape=whole", "GO shape=final", or "STOP" (with a one-line rationale). GO unblocks Phase 2 planning; STOP halts the milestone and keeps WebGL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike evidence -> milestone decision | The recorded verdict gates all downstream (irreversible) game-code migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-R | Repudiation | the go/no-go decision | medium | mitigate | Decision recorded as a committed `GO-NO-GO.md` artifact (verdict + rationale + chosen shape), so Phase 2 planning traces to an explicit sign-off |
| T-04-Q | (process) premature GO on incomplete evidence | medium | mitigate | Task 1 asserts the artifact set is complete (both shapes, both backends, estimate) before the human gate; D-03 slice ensures the outline had real edges to judge |
</threat_model>

<verification>
- `GO-NO-GO.md` assembled with the full evidence set and an unfilled VERDICT.
- Human sign-off recorded (GO + chosen shape, or STOP) with rationale.
</verification>

<success_criteria>
The make-or-break decision is a recorded human perceptual sign-off on complete evidence, with the STOP escape hatch honored — Phase 2 proceeds only on an explicit GO.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike/01-04-SUMMARY.md` when done (record the verdict).
</output>
