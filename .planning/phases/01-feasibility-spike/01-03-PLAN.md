---
phase: 01-feasibility-spike
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - "src/spike/perfHud.ts"
  - "src/spike/derisk.ts"
  - "src/spike/waterpro-spike.ts"
autonomous: true
requirements: [SPIKE-03, SPIKE-04, STCK-02]
must_haves:
  truths:
    - "WebGPU compute is confirmed on the target machine (renderer.backend.isWebGPUBackend === true, water.backend === 'webgpu') AND a forced WebGL2 run measures the fallback FPS at the medium tier (SPIKE-03)"
    - "FPS + backend are drawn on-screen so screenshots self-document which backend produced each number (SPIKE-03)"
    - "Lit water is de-risked: sparkle/SSS/lifted waterColor/bloom + an ADDITIVE transparent overlay glow riding the surface (Water Pro has no emissiveNode) (SPIKE-04)"
    - "Pooled projectile wake (<=16 reused generators, horizontal-only) + water.spray?.addEmitter (optional-chained, null on WebGL2) are proven (SPIKE-04)"
    - "Sky Pro data/ resolves in a built dist/ (vite build + preview), not just dev (STCK-02)"
    - "A realistic T-shirt-size estimate of the 17-shader port surface is produced (SPIKE-04)"
  artifacts:
    - "src/spike/perfHud.ts (on-screen FPS + backend + water.backend + spray-availability readout)"
    - "src/spike/derisk.ts (lit-water overlay + pooled wake/spray de-risk section)"
    - "the 17-shader port-surface estimate table (recorded in the SUMMARY / go/no-go artifact)"
  key_links:
    - "new WebGPURenderer({ forceWebGL: true }) forces the WebGL2 fallback run for its FPS number"
    - "water.wake.addGenerator/updateGenerator reused from a fixed <=16 pool, horizontal motion only"
    - "vite build && vite preview loads Sky data/ from the hashed dist path"
---

<objective>
De-risk the two make-or-break unknowns that are measured, not assumed: on-device perf (WebGPU compute + WebGL2 fallback FPS at the medium tier, headed) and the two no-native-API asks (lit/emissive water via overlay, pooled projectile wake/spray). Also confirm Sky Pro's `data/` survives a real `vite build`, and produce the 17-shader port-surface estimate the sign-off wants.

Purpose: convert the two HIGH-impact UNKNOWNs (A4 FPS, SPIKE-04 techniques) into recorded deliverables so the go/no-go in plan 04 has real numbers and proven techniques in front of it.
Output: `perfHud.ts`, `derisk.ts`, a built `dist/` that renders the sky, and the estimate table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feasibility-spike/01-CONTEXT.md
@.planning/phases/01-feasibility-spike/01-RESEARCH.md
@.planning/phases/01-feasibility-spike/01-PATTERNS.md
@.planning/phases/01-feasibility-spike/01-02-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `src/spike/perfHud.ts` — per-frame FPS + backend + `water.backend` + `water.spray !== null` readout drawn on canvas
- `src/spike/derisk.ts` — lit-water (sparkle/SSS/waterColor/bloom + additive glow overlay) + pooled wake/spray de-risk section
- WebGPU FPS number + WebGL2 (`forceWebGL:true`) FPS number at the `medium` tier (recorded in SUMMARY)
- A built `dist/` verified to load Sky `data/` (STCK-02 smoke)
- The 17-shader port-surface T-shirt estimate table (recorded in SUMMARY)
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: On-screen perf HUD + both-backend FPS capture (SPIKE-03)</name>
  <files>src/spike/perfHud.ts, src/spike/waterpro-spike.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-PATTERNS.md ("src/spike/perfHud.ts (utility) — NO ANALOG" — backend assertion snippet, read AFTER init)
    - .planning/phases/01-feasibility-spike/01-RESEARCH.md ("Backend confirmation for the perf artifact"; Pitfall 2 headless can't run WebGPU compute)
    - src/game/engine/createPixelRenderer.ts (render loop lines 169-212 — frozen-matrix discipline to avoid CPU overhead)
  </read_first>
  <action>
    Create `perfHud.ts` exporting an updatable HUD that draws, on top of the canvas, the live FPS (rolling average), whether `renderer.backend.isWebGPUBackend` is true (read only AFTER `await renderer.init()`), `water.backend` ('webgpu' | 'webgl'), and whether `water.spray !== null`. Render it as an overlay so screenshots self-document which backend produced each FPS number. Wire it into the `waterpro-spike.ts` frame loop with no per-frame allocations (reuse a single text buffer; carry the frozen-matrix discipline from createPixelRenderer if CPU overhead shows). Support a `?forceWebGL=1` query param that constructs `new WebGPURenderer({ forceWebGL: true })` so a second run measures the WebGL2 fallback tier. Do NOT attempt headless capture — SwiftShader cannot run WebGPU compute (Pitfall 2); capture is headed Chrome.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <human-check>Headed Chrome default run shows backend WebGPU + water.backend 'webgpu' + an FPS number at medium tier; a second run with ?forceWebGL=1 shows WebGL2 + its FPS number. Screenshot both.</human-check>
  </verify>
  <acceptance_criteria>
    - `perfHud.ts` reads the backend only after `renderer.init()` and draws FPS + backend + water.backend + spray-availability on-screen
    - `?forceWebGL=1` forces a WebGL2 run whose FPS is captured
    - Both a WebGPU FPS number and a WebGL2 FPS number at the `medium` tier are recorded in the SUMMARY
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Both backends' FPS at the medium tier are measured in headed Chrome with the backend proven on-screen.</done>
</task>

<task type="auto">
  <name>Task 2: De-risk lit water + pooled projectile wake/spray, and estimate the 17-shader port surface (SPIKE-04)</name>
  <files>src/spike/derisk.ts, src/spike/waterpro-spike.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-PATTERNS.md ("SPIKE-04 lit-water + pooled wake/spray" shared pattern)
    - .planning/phases/01-feasibility-spike/01-RESEARCH.md ("Lit water + emissive overlay"; "Pooled projectile wake + spray"; Pitfall 4 vertical-only motion)
    - pro/Three.js Water Pro v3.2.1/threejs-water-pro/docs/guide/wake.md
    - pro/Three.js Water Pro v3.2.1/threejs-water-pro/docs/guide/spray.md
    - pro/Three.js Water Pro v3.2.1/threejs-water-pro/docs/api/{sparkle,sss,color}.md
  </read_first>
  <action>
    Create `derisk.ts` exporting a toggleable section (behind a `?derisk=1` param) added once to the spike. Lit water: set `water.sparkle.intensity`, `water.sss.intensity`, `water.color.waterColor` to lifted values and add `bloom(...)` in the post chain (already present) so the surface is not a flat sheet; add a localized emissive glow as an ADDITIVE transparent overlay mesh riding just above `SEA_LEVEL` (`MeshBasicMaterial({ transparent:true, blending:THREE.AdditiveBlending, depthWrite:false })`) — Water Pro has NO emissiveNode, so this proves the overlay approach (REAC-03/04 precursor). Projectile reactivity: allocate a FIXED pool of <=16 wake generators via `water.wake.addGenerator`, and drive them with `updateGenerator` on a proxy moving HORIZONTALLY low over the sea (vertical-only motion leaves no wake, Pitfall 4); never add/remove per projectile. For vertical impacts call `water.spray?.addEmitter(...)` optional-chained (null on WebGL2 — must not crash the fallback). Separately, produce the 17-shader port-surface estimate: run `grep -rlE "ShaderMaterial|onBeforeCompile" src/game` (17 files) and, while building the beach slice, note per-subsystem which GLSL chunks (terrain sand/swash, rock mottle, grass wind, town cobble, wind props, wildlife) need node equivalents; write a T-shirt-size (S/M/L) estimate table into the plan SUMMARY.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <human-check>With ?derisk=1 in headed Chrome: the sea reads lit (sparkle/SSS/bloom) with a visible additive glow patch; a horizontally-moving proxy leaves a wake; a vertical impact emits spray on WebGPU; the ?forceWebGL=1 run does NOT crash on the null spray.</human-check>
  </verify>
  <acceptance_criteria>
    - `derisk.ts` uses a fixed pool of <=16 wake generators driven by `updateGenerator` (no per-projectile add/remove) and horizontal motion
    - `water.spray?.addEmitter` is optional-chained; the WebGL2 run does not crash
    - The lit-water glow is an additive transparent overlay mesh (not a water-material emissive term)
    - The 17-shader port-surface T-shirt estimate table (all 17 files accounted) is recorded in the SUMMARY
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Both no-native-API asks are proven with a real technique on-screen, and the 17-shader estimate is recorded for the sign-off.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm Sky Pro data/ resolves in a built dist/ (STCK-02)</name>
  <files>src/spike/waterpro-spike.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-RESEARCH.md (Pitfall 3 Sky data/ doesn't resolve in dist/; STCK-02 note — minimal build check now, full Vite plugin is Phase 5)
    - pro/Three.js Sky Pro v2.0.0/threejs-sky-pro/docs/guide/installation.md (data/ load path relative to index.js)
  </read_first>
  <action>
    Run `pnpm build` then `vite preview` (or serve the built `dist/`) and load the spike page from the BUILT output in headed Chrome — not `pnpm run dev`. Confirm the Sky renders (clouds/atmosphere), meaning Sky Pro's `data/` cloud-noise resolved from the hashed dist path. If it 404s or the sky renders black, record the exact failing `data/` path as a flag for the Phase 5 inline Vite copy plugin (STCK-02 hardening) — do NOT block the spike's core go/no-go on it; the minimal outcome here is a recorded pass/flag. This task authors no full plugin; it is the build-output smoke check plus, if needed, a one-line note of the resolution failure.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <human-check>Serve the built dist/ and open the spike in headed Chrome; the sky/clouds render (no black sky, no 404 on a data/ file). Record pass or the exact failing path.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` produces a `dist/` including the spike entry
    - The built (not dev) spike page renders the Sky, OR the exact failing `data/` path is recorded as a Phase-5 flag
    - No new runtime dependency was added
  </acceptance_criteria>
  <done>Whether Sky `data/` survives a real build is a recorded fact (pass, or a precise flag for Phase 5), not an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev query param -> render mode | `?forceWebGL=`/`?derisk=` toggle render paths on a throwaway page |
| built dist/ -> runtime data/ load | Sky data/ must resolve from the hashed path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-IV | Tampering | `?forceWebGL=`/`?derisk=` params | low | accept | Dev-only toggles on a spike page that never ships; not attacker-reachable |
| T-03-D | Denial of Service | unbounded wake generators | medium | mitigate | Fixed pool of <=16 reused generators (REAC-01) asserted; never add/remove per projectile |
| T-03-A | Availability | `water.spray` null on WebGL2 | low | mitigate | Every spray call optional-chained so the fallback backend never crashes |
</threat_model>

<verification>
- WebGPU + WebGL2 FPS numbers at medium tier recorded (headed).
- Lit-water overlay + pooled wake/spray render without crashing either backend.
- Built `dist/` sky-data outcome recorded (pass or precise flag).
- 17-shader T-shirt estimate table recorded.
</verification>

<success_criteria>
On-device perf is measured on both backends, both no-API asks are proven techniques on-screen, Sky `data/` build behavior is a recorded fact, and the 17-shader estimate exists — every input the go/no-go needs is captured.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike/01-03-SUMMARY.md` when done (include the FPS numbers and the 17-shader estimate table).
</output>
