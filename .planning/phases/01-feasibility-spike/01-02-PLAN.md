---
phase: 01-feasibility-spike
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - "src/spike/beachSlice.ts"
  - "src/spike/beachSlice.test.ts"
  - "src/game/engine/tsl/pixelFilterNode.ts"
  - "src/game/engine/tsl/outlineNode.ts"
  - "src/spike/waterpro-spike.ts"
autonomous: true
requirements: [SPIKE-01, SPIKE-02]
must_haves:
  truths:
    - "The spike renders a REPRESENTATIVE beach slice (sand + sea at real getTerrainHeight + rocks/props + a grass patch) so the depth-outline has real edges to bite on (SPIKE-01, D-03)"
    - "The pixel-filter look is reproduced in TSL in BOTH candidate resolution shapes, switchable via `?shape=whole|final` (SPIKE-02)"
    - "The one-sided sun-facing lighter rim from createPixelRenderer.ts is reproduced as a TSL node reading scene-pass depth (not the symmetric built-in edge) (SPIKE-02, RNDR-03 precursor)"
    - "Brightness/tone matches master through the filter without re-encoding sRGB inside the node (PostProcessing handles it)"
  artifacts:
    - "src/spike/beachSlice.ts (getTerrainHeight-sampled mesh + rocks + grass patch)"
    - "src/spike/beachSlice.test.ts (pure height-sampling test crossing SEA_LEVEL)"
    - "src/game/engine/tsl/pixelFilterNode.ts (both shapes: pixelate-whole-chain + final-pixelate)"
    - "src/game/engine/tsl/outlineNode.ts (sun-facing depth-discontinuity rim)"
  key_links:
    - "outlineNode reads scenePass.getViewZNode() (fallback getTextureNode('depth')) — wrong depth source = no rim"
    - "?shape= query param selects Shape A (low-res scene pass) vs Shape B (final pixelate node)"
    - "setEdgeSunDir feeds the sun screen direction into the rim node each frame"
---

<objective>
Expand the tracer's stub pixel node into the real salvage module: a representative beach slice (D-03) plus the full pixel-art identity reproduced in TSL in BOTH resolution shapes with the game's signature one-sided sun-facing rim. This is the hardest 90% of the whole milestone and the direct input to the SPIKE-02 go/no-go.

Purpose: give the perceptual sign-off (D-02) a fair, complete test — real depth discontinuities from props/rocks, both candidate shapes side-by-side, tone/brightness matched to master. The module lives under `src/game/engine/tsl/` (D-04 salvage-structured) so Phase 2 reuses it directly; the game must NOT import it this phase.
Output: `beachSlice.ts`, `pixelFilterNode.ts` (both shapes), `outlineNode.ts` (sun rim).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feasibility-spike/01-CONTEXT.md
@.planning/phases/01-feasibility-spike/01-RESEARCH.md
@.planning/phases/01-feasibility-spike/01-PATTERNS.md
@.planning/phases/01-feasibility-spike/01-01-SUMMARY.md
</context>

<artifacts>
## Artifacts this plan produces
- `src/spike/beachSlice.ts` — representative slice builder (sand+sea shelf, rocks, grass patch) sampling `getTerrainHeight`/`terrainColorAt`
- `src/spike/beachSlice.test.ts` — pure Vitest asserting sampled heights cross `SEA_LEVEL`
- `src/game/engine/tsl/pixelFilterNode.ts` — both resolution shapes (Shape A pixelate-whole-chain, Shape B final-pixelate) behind `?shape=`
- `src/game/engine/tsl/outlineNode.ts` — one-sided sun-facing lighter rim reading scene-pass depth
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the representative beach slice from real terrain sampling</name>
  <files>src/spike/beachSlice.ts, src/spike/beachSlice.test.ts, src/spike/waterpro-spike.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-PATTERNS.md ("src/spike/beachSlice.ts" section — mesh-build pattern, ISLANDS[0] beach arc, pure-import list)
    - src/game/world/terrain.ts (getTerrainHeight, terrainColorAt, beachSandFactor, SEA_LEVEL, ISLANDS[0] beachArc faces -x)
    - src/game/world/createSeaWater.ts (lines 238-243: SEA_LEVEL plane placement reference only)
    - src/game/world/assets/createRockMesh.ts (rock prop analog for depth discontinuities)
  </read_first>
  <behavior>
    - Sampling the slice across the ISLANDS[0] beach arc yields at least one vertex above SEA_LEVEL (sand) and at least one below (sea shelf) — the outline needs the shoreline edge.
    - terrainColorAt is applied as vertex colors so the sand/shore reads without the full custom terrain shader.
  </behavior>
  <action>
    Create `beachSlice.ts` exporting a `buildBeachSlice(): THREE.Group` that imports the pure fns from `src/game/world/terrain.ts` (read-only — nothing in `src/game` imports back, so "zero game code touched" holds). Build a `PlaneGeometry` over the `ISLANDS[0]` beach arc region, `rotateX(-Math.PI/2)`, set each vertex Y to `getTerrainHeight(x,z)` and vertex color to `terrainColorAt(x,z,height)`, material `MeshStandardMaterial({ vertexColors:true, flatShading:true })` (NOT the custom `patchTerrainShader` — that is Phase 3). Add a handful of rocks (reuse the `createRockMesh` silhouette or simple low-poly rocks) and a small grass patch so the depth-outline has genuine discontinuities per D-03. Place the Water Pro sea plane reference at `SEA_LEVEL`. Extract the height-sampling into a pure helper so it is unit-testable. Wire `buildBeachSlice()` into `waterpro-spike.ts` replacing the tracer's single flat plane. Write `beachSlice.test.ts` asserting the sampled slice spans SEA_LEVEL (min height < SEA_LEVEL < max height).
  </action>
  <verify>
    <automated>pnpm test -- beachSlice</automated>
  </verify>
  <acceptance_criteria>
    - `src/spike/beachSlice.ts` exists and exports `buildBeachSlice`
    - `pnpm test -- beachSlice` passes: sampled heights straddle `SEA_LEVEL = -0.8`
    - The slice imports only pure fns from `terrain.ts`; no file under `src/game` imports the spike
    - `pnpm build` still exits 0
  </acceptance_criteria>
  <done>The spike renders sand + shoreline + rocks + grass at real terrain heights, giving the outline pass real edges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reproduce the pixel filter in TSL — both resolution shapes + sun-facing rim</name>
  <files>src/game/engine/tsl/pixelFilterNode.ts, src/game/engine/tsl/outlineNode.ts, src/spike/waterpro-spike.ts</files>
  <read_first>
    - .planning/phases/01-feasibility-spike/01-PATTERNS.md ("pixelFilterNode.ts + outlineNode.ts" — the three load-bearing chunks + both-shapes description)
    - src/game/engine/createPixelRenderer.ts (blit fragment shader lines 98-131: lin() depth, one-sided sun rim lines 117-128, sRGB block lines 112-116; setEdgeSunDir lines 216-218; uniforms uEdge=0xd6dae6, uEdgeStrength=0.95, uThreshold=0.06)
    - node_modules/three/examples/jsm/tsl/display/PixelationPassNode.js (setSize floor-by-pixelSize lines 292-301; depthEdgeIndicator lines 129-139; Nearest filters line 244)
    - .planning/phases/01-feasibility-spike/01-RESEARCH.md (Pattern 2/3; Pitfall 5 sRGB/tone-mapping; A1 getViewZNode)
  </read_first>
  <behavior>
    - Shape A (pixelate-whole-chain): the scene renders into a low-res target floored by pixelSize, then nearest-upscales — mirrors createPixelRenderer Pass 1+2.
    - Shape B (final-pixelate): full-res Water/Sky post, then pixelate as the LAST node.
    - The rim appears on ONE side only (toward the sun screen dir), colored a lighter shade of the base color — never a symmetric white edge.
    - Selecting `?shape=whole` vs `?shape=final` switches shapes at boot; both compile and render.
  </behavior>
  <action>
    In `pixelFilterNode.ts` implement both candidate shapes selectable by the `?shape=whole|final` query param (default `final`), using `pixelationPass` as structural scaffolding but NOT as a drop-in. Shape A renders the scene at reduced internal resolution (floor width/height by `pixelSize`) with Nearest min/mag filters then upscales; Shape B runs the full post chain then pixelates last. In `outlineNode.ts` reproduce the one-sided sun-facing rim from `createPixelRenderer.ts` as a TSL `Fn`: read linear view-space depth via `scenePass.getViewZNode()` (fallback `getTextureNode('depth')` + the proven `lin()` math if unavailable per A1), sample ONLY the neighbour toward the sun screen direction (fed from a `sunDirUniform`, updated each frame like the game's `setEdgeSunDir`), compute the depth jump, and where it exceeds the threshold mix the base color toward a lighter shade (`col*(1+edgeStrength)+0.05`, edgeStrength 0.95, threshold 0.06) — a lighter rim, not white. Do NOT re-encode sRGB inside either node — let `PostProcessing` + renderer color management handle output color so brightness matches master (Pitfall 5); expose a tone-mapping on/off toggle (`?tone=`) to compare ACES vs neutral. Append `outlineNode` inside the pixel node so the rim rides on the pixelated result. Keep each node file under 300 LOC.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <human-check>Load ?shape=whole and ?shape=final in headed Chrome; both render pixelated; the rock/shoreline edges show a one-sided lighter rim on the sun side; brightness reads like master through the filter.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 for both `?shape=whole` and `?shape=final` (no TSL compile error)
    - `outlineNode.ts` reads depth from the scene pass and offsets sampling only toward the `sunDirUniform` direction (one-sided)
    - The rim mixes toward a lighter shade of the base color (not white), matching the createPixelRenderer intent
    - Neither node re-encodes sRGB; a `?tone=` toggle exists
    - Files stay under 300 LOC each; the game does not import `src/game/engine/tsl/`
  </acceptance_criteria>
  <reversibility rating="reversible">D-04 salvage module is new and unwired; if the spike STOPs at go/no-go it is deleted with zero game impact.</reversibility>
  <done>The pixel-art identity (low-res nearest upscale + one-sided sun rim) is reproduced in TSL in both shapes, ready for the perceptual sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev query param -> render mode | `?shape=`/`?tone=` only toggle render paths; spike never ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-IV | Tampering | `?shape=`/`?tone=` query params | low | accept | Dev-only toggles on a throwaway spike page that never ships in game `dist/`; no attacker reach |
| T-02-SC | Information Disclosure | salvage module reuses paid-bundle types | low | accept | `src/game/engine/tsl/` contains only original TSL code; it imports the private-submodule types but ships no paid bundle content (D-01 boundary held in plan 01) |
</threat_model>

<verification>
- `pnpm test -- beachSlice` passes (height crosses SEA_LEVEL).
- `pnpm build` succeeds for both resolution shapes.
- Headed Chrome shows the one-sided sun rim on real prop/shore edges in both shapes.
</verification>

<success_criteria>
The representative beach slice and the full TSL pixel filter (both shapes + sun-facing rim, tone-matched to master) are rendering — the complete instrument the SPIKE-02 go/no-go judges.
</success_criteria>

<output>
Create `.planning/phases/01-feasibility-spike/01-02-SUMMARY.md` when done.
</output>
