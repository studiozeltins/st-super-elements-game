---
phase: 02-server-authoritative-damage-crit-on-enemies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/damage.ts
  - spacetimedb/src/skillGate.ts
  - src/game/data/__tests__/serverSync.test.ts
  - src/game/data/__tests__/skillGate.test.ts
autonomous: true
requirements: [CRIT-06]
must_haves:
  truths:
    - "CHARACTER_COMBAT covers every id in server CHARACTER_STATS, with weaponId/skillDamage/skillCooldownSeconds equal to the client CHARACTERS values (INV-5 parity, per CRIT-06)."
    - "skillGrantActive grants only when isSkill AND nowMicros <= windowEndsAtMicros (boundary-equal still grants); nextSkillReadyAt returns now + cooldownSeconds*1_000_000 as bigint."
  artifacts:
    - spacetimedb/src/damage.ts
    - spacetimedb/src/skillGate.ts
    - src/game/data/__tests__/serverSync.test.ts
    - src/game/data/__tests__/skillGate.test.ts
  key_links:
    - "serverSync.test.ts imports CHARACTER_COMBAT across the package boundary and compares OUTPUTS against CHARACTERS (import-and-compare, not source regex)."
    - "skillGate.test.ts imports the real skillGate helpers across the boundary (same idiom as crit.test.ts)."
  prohibitions:
    - "No import/Math.random/Date.now/ctx literal tokens anywhere in skillGate.ts (source or comments) — the determinism grep-gate over spacetimedb/src matches them even inside comments."
    - "No nested/multi-line braces inside a CHARACTER_COMBAT entry — keep each entry single-line flat."
---

<objective>
Land the two zero-import, test-first foundations the Phase-2 reducer rework wires in, with no runtime behavior change:

1. A per-character combat-data mirror (`CHARACTER_COMBAT`) in `damage.ts` — the server has NO character→weapon map and NO skill-damage/skill-cooldown data today, so `computeBaseDamage` and the skill-cooldown gate cannot run until this exists (RESEARCH §CRITICAL GAP). Parity-locked in `serverSync.test.ts` (INV-5, CRIT-06).
2. A pure `skillGate.ts` helper (`skillGrantActive`, `nextSkillReadyAt`) — the deterministic skill-window math the server cooldown gate will call (D2-03), tested across the package boundary.

Purpose: These are pure data + math with a parity/behaviour gate. No reducer, client, or schema is touched — this de-risks the heavy Plan 02 wiring and keeps `index.ts` from growing.
Output: `CHARACTER_COMBAT` + `CharacterCombat` in `damage.ts`; `skillGate.ts` with two exported functions; parity assertions in `serverSync.test.ts`; a new cross-boundary `skillGate.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-server-authoritative-damage-crit-on-enemies/02-RESEARCH.md
@.planning/phases/02-server-authoritative-damage-crit-on-enemies/02-PATTERNS.md

# Source of truth for the mirror + the analog patterns to copy verbatim:
@src/game/data/characters.ts
@spacetimedb/src/damage.ts
@spacetimedb/src/crit.ts
@src/game/data/__tests__/crit.test.ts
@src/game/data/__tests__/serverSync.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CHARACTER_COMBAT mirror in damage.ts + INV-5 parity assertion</name>
  <files>spacetimedb/src/damage.ts, src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - spacetimedb/src/damage.ts (whole, 111 LOC) — the `WEAPONS` block (21-68) is the exact analog: a `Record` keyed literal with a `// Mirror of …` header comment. `WeaponId` is already exported at line 9.
    - src/game/data/characters.ts — source of truth: `CHARACTERS[id].weapon`, `.skill.damage`, `.skill.cooldownSeconds`. The full id set (17) is in `CHARACTERS` (97-402).
    - spacetimedb/src/index.ts:45-67 — `CHARACTER_STATS`; the mirror MUST cover the SAME 17 keys (aeris…stindzis).
    - src/game/data/__tests__/serverSync.test.ts:280-315 — the `describe('server damage.ts mirror stays in sync')` import-and-compare block; imports at 14-27.
  </read_first>
  <behavior>
    - For every id in CHARACTERS: CHARACTER_COMBAT[id].weaponId === CHARACTERS[id].weapon.
    - For every id: CHARACTER_COMBAT[id].skillDamage === CHARACTERS[id].skill.damage.
    - For every id: CHARACTER_COMBAT[id].skillCooldownSeconds === CHARACTERS[id].skill.cooldownSeconds.
    - Key set of CHARACTER_COMBAT is exactly the key set of CHARACTERS (no missing/extra id).
  </behavior>
  <action>
    In `spacetimedb/src/damage.ts`, append (below the existing `computeBaseDamage`) a `CharacterCombat` interface and a `CHARACTER_COMBAT: Record<string, CharacterCombat>` map. Fields: `weaponId: WeaponId`, `skillDamage: number`, `skillCooldownSeconds: number` (reuse the already-exported `WeaponId`). Copy the `// Mirror of …` header-comment convention from the `WEAPONS` block above it. Populate every id present in `CHARACTER_STATS` (index.ts:45-67) — all 17: aeris, terron, volta, silva, marina, ignis, sarma, nereida, vesper, glacia, zefs, petra, zibo, lapa, rasa, dzirkste, stindzis — sourcing each value verbatim from `CHARACTERS[id].weapon` / `.skill.damage` / `.skill.cooldownSeconds` in `src/game/data/characters.ts`. Keep each entry on ONE line (single-line flat, no nested braces) per RESEARCH Pitfall 3, so it survives any regex scrape. Do NOT add imports — `damage.ts` must stay zero-import (the parity test imports it, not vice versa). File stays well under the 300-LOC cap.

    Then extend `src/game/data/__tests__/serverSync.test.ts`: add `import { CHARACTER_COMBAT } from '../../../../spacetimedb/src/damage';` to the existing server-mirror import block, and inside the `describe('server damage.ts mirror stays in sync')` add an `it.each(Object.keys(CHARACTERS))` case asserting the three field equalities above plus that `CHARACTER_COMBAT` has an entry for the id. Mirror the existing `it.each` style (lines 291/299). This is import-and-compare parity (RESEARCH Pitfall 3) — the robust drift guard for CRIT-06.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <done>serverSync.test.ts passes; every CHARACTERS id has a matching CHARACTER_COMBAT entry (weapon/skillDamage/skillCooldown parity); damage.ts has zero import lines and stays under 300 LOC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: skillGate.ts pure helper + cross-boundary test</name>
  <files>spacetimedb/src/skillGate.ts, src/game/data/__tests__/skillGate.test.ts</files>
  <read_first>
    - spacetimedb/src/crit.ts (whole, 21 LOC) — the canonical zero-import injected-seam helper. Copy its file-header discipline: a comment block that states the determinism rationale WITHOUT ever writing the literal tokens the grep-gate forbids (crit.ts phrases around them successfully — mirror that phrasing).
    - src/game/data/__tests__/crit.test.ts (whole, 25 LOC) — the cross-boundary import idiom (line 5) and the strict-boundary discipline (line 17). skillGate.test.ts is a direct structural clone.
    - RESEARCH §Skill Cooldown Gate — the two exported signatures are already spec'd there.
  </read_first>
  <behavior>
    - skillGrantActive(true, 100n, 200n) === true (in-window).
    - skillGrantActive(true, 200n, 200n) === true (boundary-equal STILL grants — inclusive `<=`).
    - skillGrantActive(true, 201n, 200n) === false (one micro past → no grant).
    - skillGrantActive(false, 100n, 200n) === false (isSkill false → never grants, even in-window).
    - nextSkillReadyAt(1_000_000n, 6) === 7_000_000n (now + cooldownSeconds*1_000_000, bigint).
    - nextSkillReadyAt(0n, 0.45) === 450_000n (rounds the seconds*1e6 product).
  </behavior>
  <action>
    Create `spacetimedb/src/skillGate.ts` as a zero-import pure module exporting exactly two functions: `skillGrantActive(isSkill: boolean, nowMicros: bigint, windowEndsAtMicros: bigint): boolean` returning `isSkill && nowMicros <= windowEndsAtMicros`; and `nextSkillReadyAt(nowMicros: bigint, cooldownSeconds: number): bigint` returning `nowMicros + BigInt(Math.round(cooldownSeconds * 1_000_000))`. Use bigint-micros math (mirrors the existing `AGGRO_DURATION_MICROS`/`droppedAtMicros` convention). CRITICAL: the file (including all comments) must contain NONE of the four literal tokens the determinism grep-gate over `spacetimedb/src` matches even in comments — phrase the header the way `crit.ts` does (describe the injected-time/injected-randomness discipline by concept, not by naming the forbidden APIs).

    Create `src/game/data/__tests__/skillGate.test.ts` cloning `crit.test.ts`'s structure: import the real helpers across the boundary (`import { skillGrantActive, nextSkillReadyAt } from '../../../../spacetimedb/src/skillGate';`) and assert every case in the behavior block. Pin the inclusive boundary explicitly (equal-to-window grants; one past does not), the same way crit.test.ts pins the strict `<` crit boundary.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/data/__tests__/skillGate.test.ts</automated>
  </verify>
  <done>skillGate.test.ts passes all boundary cases; skillGate.ts exports the two functions, has zero import lines, and the determinism grep-gate over spacetimedb/src stays clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client CHARACTERS ↔ server CHARACTER_COMBAT | Two copies of combat data that MUST stay identical; silent drift would let server damage diverge from client feel and undermine the whole server-authoritative model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering (integrity) | CHARACTER_COMBAT mirror | medium | mitigate | INV-5 import-and-compare parity in serverSync.test.ts blocks merge on any weapon/skillDamage/skillCooldown drift (per CRIT-06). |
| T-02-SC | Supply chain | package installs | n/a | accept | This plan installs NO packages (RESEARCH §Package Legitimacy Audit — not applicable); pure first-party source edits only. |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/data/__tests__/serverSync.test.ts src/game/data/__tests__/skillGate.test.ts` — both green.
- `pnpm test` — full suite stays green (no runtime behavior changed).
- Determinism grep-gate: no forbidden randomness/clock tokens introduced in `spacetimedb/src` (skillGate.ts is clean).
</verification>

<success_criteria>
- CHARACTER_COMBAT exists for all 17 ids with parity-locked weapon/skillDamage/skillCooldown values.
- skillGate.ts exports skillGrantActive + nextSkillReadyAt, zero-import, boundary-correct.
- No reducer/client/schema change; full suite green.
</success_criteria>

<output>
Create `.planning/phases/02-server-authoritative-damage-crit-on-enemies/02-01-SUMMARY.md` when done.
</output>
