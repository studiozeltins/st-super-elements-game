---
phase: 03-shards-at-risk
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - spacetimedb/src/deathPenalty.ts
  - src/game/data/__tests__/deathPenalty.test.ts
autonomous: true
requirements: [REQ-shard-risk]
must_haves:
  truths:
    - "applyDeathShardPenalty returns shardsLost=SHARD_DEATH_LOSS and no erosion when transcendShards>0 (has-shards branch)."
    - "With 0 shards but transcendLevel>0 it erodes ONE transcend level (nextTranscendLevel = transcendLevel-1), shardsLost=0."
    - "With 0 shards and 0 transcend but constellation>0 it erodes ONE constellation level (C6->C5), shardsLost=0."
    - "With 0 shards, 0 transcend, 0 constellation everything stays 0, no negative values (u32 safety)."
    - "Branch ORDER guarantees: whenever shards>0 no level is ever eroded."
    - "shardsLost (the value a PVP killer is credited) is exactly 0 on an empty victim (A1 economy safety)."
  artifacts:
    - "spacetimedb/src/deathPenalty.ts exporting DeathShardPenalty + applyDeathShardPenalty (pure, dependency-free)."
    - "src/game/data/__tests__/deathPenalty.test.ts cross-import spec covering all 4 branches + order + empty-victim credit."
  key_links:
    - "deathPenalty.ts imports NOTHING (no ctx/db/random/time) — same purity as transcendInstall.ts so it unit-tests under the client vitest runner via the ../../../../spacetimedb/src path."
    - "The test consumes the client SHARD_DEATH_LOSS mirror from src/game/data/constants.ts (already asserted by serverSync.test.ts:136), never a re-hardcoded literal."
  prohibitions:
    - "NEVER decrement a value outside the branch that already proved it > 0 (u32 underflow safety = branch order)."
    - "NEVER import ctx/db/random/time into deathPenalty.ts — it must stay pure and cross-boundary testable."
    - "NEVER return a hardcoded shardsLost of 1 on the erosion branches — shardsLost is 0 whenever a level was eroded instead."
---

<objective>
Extract the death-penalty decision as a pure, dependency-free helper `applyDeathShardPenalty`
in `spacetimedb/src/deathPenalty.ts` (sibling of `transcendInstall.ts`), and lock its behavior
with a cross-import vitest spec. This is Nyquist Wave 0: the test scaffold and the helper it
guards must exist before any reducer wiring (Plan 02) is written.

Purpose: The helper's branch ORDER is the u32-underflow safety property AND the source of the
`shardsLost` value the PVP killer is credited (0 on an empty victim = the A1 economy invariant).
Isolating it as a pure function makes both properties unit-testable without a DB harness.

Output: `spacetimedb/src/deathPenalty.ts` (helper) + `src/game/data/__tests__/deathPenalty.test.ts`
(all four branches + order + empty-victim credit).

Artifacts this phase produces (full set, delivered across Plans 01-04):
- `spacetimedb/src/deathPenalty.ts` — `applyDeathShardPenalty` (THIS plan)
- `src/game/data/__tests__/deathPenalty.test.ts` (THIS plan)
- `shard_drop` table + `shardDrop` accessor, `spillShards` helper, `collectShard` reducer (Plan 02)
- regenerated `src/module_bindings/` — `tables.shardDrop`, `reducers.collectShard` (Plan 03)
- client `createShardMesh` / `updateShardDrops` / `syncShardDrops`, `sendCollectShard` bridge,
  shard lost/stolen toast strings, `.wallet-chip--drain` CSS (Plan 04)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shards-at-risk/03-RESEARCH.md
@.planning/phases/03-shards-at-risk/03-PATTERNS.md
@.planning/phases/03-shards-at-risk/03-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure applyDeathShardPenalty helper (deathPenalty.ts)</name>
  <files>spacetimedb/src/deathPenalty.ts</files>
  <read_first>
    - spacetimedb/src/transcendInstall.ts (exact analog — copy its purity/header-comment discipline and result-object shape; entire 35-line file)
    - .planning/phases/03-shards-at-risk/03-RESEARCH.md (Pattern 1, lines 139-186 — the recommended primitive signature + branch order)
  </read_first>
  <behavior>
    - applyDeathShardPenalty(transcendShards, transcendLevel, constellation, loss) -> DeathShardPenalty
    - has-shards: (2, 3, 6, 1) -> { shardsLost: 1, erodedTranscend: false, erodedConstellation: false, nextShards: 1, nextTranscendLevel: 3, nextConstellation: 6 }
    - only-transcend: (0, 3, 6, 1) -> { shardsLost: 0, erodedTranscend: true, erodedConstellation: false, nextShards: 0, nextTranscendLevel: 2, nextConstellation: 6 }
    - only-constellation: (0, 0, 6, 1) -> { shardsLost: 0, erodedTranscend: false, erodedConstellation: true, nextShards: 0, nextTranscendLevel: 0, nextConstellation: 5 }
    - nothing: (0, 0, 0, 1) -> all flags false, next* all 0, shardsLost 0 (no negatives)
    - order: (5, 4, 6, 1) -> never erodes (erodedTranscend/erodedConstellation both false)
  </behavior>
  <action>
    Create `spacetimedb/src/deathPenalty.ts` mirroring `transcendInstall.ts`: a header comment
    stating it is dependency-free (no ctx/db/import/random/time) and that branch ORDER is the
    load-bearing u32-underflow safety property (cite the same discipline as resolveTranscendInstall
    / threat T-03-01). Export `interface DeathShardPenalty { shardsLost, erodedTranscend,
    erodedConstellation, nextShards, nextTranscendLevel, nextConstellation }` (all numbers/booleans)
    and `export function applyDeathShardPenalty(transcendShards, transcendLevel, constellation,
    loss)`. Implement exactly three priority gates then a no-op fallthrough, in this order:
    (1) `transcendShards > 0` -> shardsLost = loss, nextShards = Math.max(0, transcendShards - loss),
    other next* unchanged, both eroded flags false; (2) else `transcendLevel > 0` -> erodedTranscend
    true, nextTranscendLevel = transcendLevel - 1, shardsLost 0, nextShards 0; (3) else
    `constellation > 0` -> erodedConstellation true, nextConstellation = constellation - 1,
    shardsLost 0; (4) else all-zero no-op. Only decrement inside the branch that proved `> 0`.
    Return a flat result object (NOT a discriminated union — every branch yields next* values the
    reducer applies). Use `loss` (the reducer passes SHARD_DEATH_LOSS); do not hardcode 1 inside.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p spacetimedb/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - source: `spacetimedb/src/deathPenalty.ts` exports `applyDeathShardPenalty` and `DeathShardPenalty`; file has zero import statements.
    - behavior: each of the six behavior rows above is satisfied by the returned object.
    - test-command: Task 2's spec (`pnpm vitest run src/game/data/__tests__/deathPenalty.test.ts`) passes against this helper.
  </acceptance_criteria>
  <done>The pure helper exists, is import-free, and produces the six documented results with no negative fields.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-import unit test for all penalty branches (deathPenalty.test.ts)</name>
  <files>src/game/data/__tests__/deathPenalty.test.ts</files>
  <read_first>
    - src/game/data/__tests__/transcendInstall.test.ts (exact analog — cross-boundary import idiom at line 5, describe/toEqual structure; entire 72-line file)
    - src/game/data/constants.ts (client SHARD_DEATH_LOSS mirror at line 12 — import it, do not re-hardcode)
  </read_first>
  <action>
    Create `src/game/data/__tests__/deathPenalty.test.ts` mirroring `transcendInstall.test.ts`.
    Import the real helper across the package boundary:
    `applyDeathShardPenalty` from `../../../../spacetimedb/src/deathPenalty`, and `SHARD_DEATH_LOSS`
    from `../constants`. One top-level describe with cases asserting the WHOLE result object via
    `.toEqual({...})` for: has-shards (shardsLost === SHARD_DEATH_LOSS, no erosion), only-transcend
    (erodedTranscend, nextTranscendLevel = input-1, shardsLost 0), only-constellation
    (erodedConstellation, nextConstellation C6->C5, shardsLost 0), and nothing (all false, next*
    all 0, no negatives). Add an ORDER test: with shards AND a transcend level present, assert
    neither eroded flag is set. Add an empty-victim PVP-credit test: assert
    `applyDeathShardPenalty(0, level, constellation, SHARD_DEATH_LOSS).shardsLost === 0` (this is
    the value a PVP killer would be credited — proves A1 / T-03-02). Never re-implement the helper
    locally; import the real one.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/deathPenalty.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: the spec imports `applyDeathShardPenalty` from `../../../../spacetimedb/src/deathPenalty` and `SHARD_DEATH_LOSS` from `../constants` (no local re-implementation, no hardcoded 1).
    - behavior: all four branch cases + the order case + the empty-victim shardsLost===0 case are asserted.
    - test-command: `pnpm vitest run src/game/data/__tests__/deathPenalty.test.ts` is green.
  </acceptance_criteria>
  <done>The spec covers all four branches, the order guarantee, and the empty-victim credit, and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure module → reducer | `deathPenalty.ts` produces the values the reducer applies; a wrong branch order is where economy safety breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `applyDeathShardPenalty` decrement math | high | mitigate | Decrement only inside the branch that proved `> 0`; `Math.max(0, …)` on the shards branch; "nothing" branch returns all-zero. Unit test asserts no negative fields (u32 underflow safety). |
| T-03-02 | Elevation of Privilege / economy | `shardsLost` output feeding the PVP credit | high | mitigate | `shardsLost` is 0 on every erosion branch; the empty-victim test proves a 0-shard kill credits 0 (no minting). |
| T-03-SC | Tampering | npm/pnpm installs | low | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pnpm vitest run src/game/data/__tests__/deathPenalty.test.ts` green (all 4 branches + order + empty-victim credit).
- `pnpm build` (or `tsc --noEmit`) green — helper compiles with zero imports.
</verification>

<success_criteria>
- `spacetimedb/src/deathPenalty.ts` exists, is import-free, and passes its cross-import spec.
- The empty-victim case proves `shardsLost === 0` (A1 pre-encoded before any wiring).
</success_criteria>

<output>
Create `.planning/phases/03-shards-at-risk/03-01-SUMMARY.md` when done.
</output>
