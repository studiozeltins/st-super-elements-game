---
phase: 03-shards-at-risk
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/game/createGame.ts
  - src/App.tsx
  - src/index.css
autonomous: true
requirements: [REQ-shard-risk]
must_haves:
  truths:
    - "shard_drop rows render as a DISTINCT purple octahedron (0x9333ea), 1.4x the gem radius (0.42), brighter emissive (0.6), always sparkling — immediately distinguishable from a gold gem."
    - "The shard pickup reuses the gem magnet/grace/collect loop verbatim (GEM_PICKUP_DELAY 1.2, GEM_MAGNET_RADIUS, GEM_COLLECT_RADIUS) and requests collection via network.sendCollectShard(rawId)."
    - "App.tsx subscribes tables.shardDrop, reads it via useTable, and pushes rows into gameRef.syncShardDrops on change."
    - "The shard counter (.wallet-chip) flashes --pulse on a transcendShards increase and --drain (shrink, same purple, no --danger) on a decrease."
    - "A self-facing toast fires on a real shard MOVEMENT: PVE death drop, stolen-victim, and stole-killer are three textually distinct Latvian strings; a plain ground pickup shows the counter pulse but NO toast."
  artifacts:
    - "createShardMesh / updateShardDrops / syncShardDrops in src/game/createGame.ts."
    - "sendCollectShard bridge in src/App.tsx (and its NetworkBridge type)."
    - "shard lost/stolen/stole toast (diff-driven) + counter flash trigger in src/App.tsx."
    - ".wallet-chip--drain keyframe in src/index.css."
  key_links:
    - "syncShardDrops is driven by the App.tsx useEffect on [shardDropRows], mirroring the gemDrop sync effect (App.tsx:371-373)."
    - "The toast disambiguation reuses the already-subscribed pvpHit table (App.tsx:157) to tell victim-theft from PVE drop; the counter is already reactive via useTable(player)."
    - "sendCollectShard maps to connection.reducers.collectShard({ dropId }) (mirror of sendCollectGem, App.tsx:348)."
  prohibitions:
    - "NEVER color the shard drop gold or reuse GEM_RADIUS — it must read as a rarer purple relative of the gem."
    - "NEVER introduce --danger/red for the loss flash — distinguish loss from gain by MOTION (shrink) not hue."
    - "NEVER show the 'stole/stolen' toast on a plain ground pickup (pickup != theft) — pickup is counter-pulse only."
    - "NEVER add a new color token, font, screen, or broadcast shard_steal table — reuse the shipped token system and event pattern. [D2]"
    - "NEVER let the client write transcendShards — it only renders shard_drop and requests collectShard."
---

<objective>
Render `shard_drop` pickups and surface shard loss/theft on the client, reusing the shipped gem
renderer and event-feedback patterns. Three tiny additive surfaces per the UI contract: a purple
"rare shard" ground drop, a self-facing lost/stolen/stole toast, and a loss/gain flash on the
existing shard counter chip.

Purpose: give the player immediate, in-combat feedback that shards are now at stake, without new
screens, palette, or broadcast tables.

Output: `createShardMesh`/`updateShardDrops`/`syncShardDrops` (createGame.ts), the
`sendCollectShard` bridge + subscribe/useTable/sync + diff-driven toast + counter flash (App.tsx),
and the `.wallet-chip--drain` keyframe (index.css).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shards-at-risk/03-UI-SPEC.md
@.planning/phases/03-shards-at-risk/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render shard_drop pickups (createShardMesh/updateShardDrops/syncShardDrops)</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts:610-661 (createGemMesh + updateGemDrops — mesh + magnet/collect loop), :937-966 (syncGemDrops — add/remove/dispose reconciliation), :270-275 (GEM_PICKUP_DELAY/MAGNET/COLLECT radii, GEM_RADIUS, GEM_EMISSIVE_INTENSITY)
    - .planning/phases/03-shards-at-risk/03-UI-SPEC.md (Component Contract — shard_drop Ground Pickup, locked visual values)
  </read_first>
  <action>
    Clone the gem renderer into a shard renderer with the UI-SPEC visual: `createShardMesh(drop)`
    builds `new THREE.Mesh(new THREE.OctahedronGeometry(0.42), material)` with a
    MeshLambertMaterial `color` + `emissive` = `0x9333ea`, `emissiveIntensity: 0.6`, always sparkling
    (tint the spawn burst `0x9333ea`, ~12 particles); baseY = ground + 0.9 (raise to +1.0 if the
    larger mesh clips). Add `updateShardDrops(deltaSeconds)` mirroring `updateGemDrops` (spin/bob,
    magnet within GEM_MAGNET_RADIUS, request collect within GEM_COLLECT_RADIUS after GEM_PICKUP_DELAY)
    using a private `requestedShardPickups` Set and calling `network.sendCollectShard(shard.rawId)`.
    Add `syncShardDrops(drops)` mirroring `syncGemDrops` (add new, remove+dispose gone, clear the
    request key) against a private `shardDrops` Map. Call `updateShardDrops(deltaSeconds)` in the
    same animation frame that calls `updateGemDrops`. Expose `syncShardDrops` on the game object
    interface returned by createGame (next to `syncGemDrops`). Add `sendCollectShard` to the
    `network`/NetworkBridge type this module consumes (mirror `sendCollectGem`).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - source: `createShardMesh` uses `OctahedronGeometry(0.42)` and `0x9333ea` color/emissive (not GEM_RADIUS/gold); `updateShardDrops` + `syncShardDrops` exist; `syncShardDrops` is exposed on the returned game object; `network.sendCollectShard` is referenced.
    - behavior: shard pickup reuses GEM_PICKUP_DELAY/MAGNET/COLLECT constants; the drop is purple, 1.4x, brighter, always sparkling.
    - test-command: `pnpm build` green.
  </acceptance_criteria>
  <done>Shard drops render as a distinct purple rare crystal and are collectible via the reused magnet/grace/collect loop.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shardDrop subscribe/useTable/sync + sendCollectShard bridge (App.tsx)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:73-90 (subscribe list), :106 (useTable(gemDrop)), :348 (sendCollectGem bridge), :371-373 (syncGemDrops effect)
    - .planning/phases/03-shards-at-risk/03-PATTERNS.md (App.tsx edits, lines 248-289)
  </read_first>
  <action>
    Mirror the gemDrop plumbing for shards: add `tables.shardDrop,` to the subscribe list (near
    `tables.gemDrop` at App.tsx:88); add `const [shardDropRows] = useTable(tables.shardDrop);`
    (near :106); add a sibling `useEffect(() => { gameRef.current?.syncShardDrops(shardDropRows); },
    [shardDropRows]);` (mirror the gemDrop sync effect at :371-373); add
    `sendCollectShard: dropId => connection.reducers.collectShard({ dropId }),` to the network
    bridge object (near `sendCollectGem` at :348) AND to the NetworkBridge type/interface where
    `sendCollectGem` is declared.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - source: `tables.shardDrop` appears in the subscribe list; `shardDropRows` is read via useTable; a useEffect calls `gameRef.current?.syncShardDrops(shardDropRows)`; `sendCollectShard` is on both the bridge object and its type.
    - behavior: shard_drop rows flow from subscription into the renderer identically to gem drops.
    - test-command: `pnpm build` green.
  </acceptance_criteria>
  <done>The client subscribes to shard_drop, feeds rows to syncShardDrops, and can request collectShard.</done>
</task>

<task type="auto">
  <name>Task 3: Shard lost/stolen toast + counter flash (App.tsx + index.css)</name>
  <files>src/App.tsx, src/index.css</files>
  <read_first>
    - src/App.tsx:157-174 (pvpHit/healEvent onInsert self-number pattern — toast mechanism + disambiguation source), :474/:498 (transcendShards prop flow)
    - src/index.css:1118-1134 (.wallet-chip + .wallet-chip--pulse — box metrics + keyframe to mirror)
    - .planning/phases/03-shards-at-risk/03-UI-SPEC.md (Shard Counter + Toast contracts + Copywriting: Latvian strings)
  </read_first>
  <action>
    Counter flash: add a `useRef` holding the previous `transcendShards` + a `useEffect` diffing the
    reactive `myPlayer.transcendShards`. On an UP diff toggle `.wallet-chip--pulse` for one cycle; on
    a DOWN diff toggle a new `.wallet-chip--drain`. In `src/index.css` add `.wallet-chip--drain`
    reusing the `.wallet-chip--pulse` keyframe SHAPE but inverting to a quick shrink (e.g.
    `scale(0.82)` at 35%) and a dimmed text-shadow, staying in `--shard` purple (NO `--danger`).
    Toast: extend the existing self-feedback mechanism (route a short text through the `gameRef`
    self-number pattern, e.g. a `spawnSelfMessage`, or a small self-anchored DOM toast) driven by the
    same diff, disambiguated client-side: (a) DOWN + local player just died with no fatal pvpHit on
    me → PVE drop → `Zvaigžņu šķemba nokrita`; (b) DOWN + a fatal `pvpHit` on me (already-subscribed
    pvpHit table, App.tsx:157) → victim → `Zvaigžņu šķemba nozagta!`; (c) UP coinciding with a PVP
    kill I just landed → killer → `Nozagi zvaigžņu šķembu!`. A plain UP from walking over a shard_drop
    (collectShard) is a PICKUP → counter pulse only, NO toast. Toast auto-dismisses (~1.5s), uses the
    `◈` glyph in `--shard`, and never blocks. If planner strings need tweaking, swap only the strings
    (mechanism/color/glyph unaffected).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - source: `.wallet-chip--drain` exists in index.css (shrink keyframe, `--shard` purple, no `--danger`); App.tsx has a prev-transcendShards useRef + diff useEffect toggling --pulse (up) / --drain (down); three distinct Latvian toast strings present; pickup path shows no 'stole' toast.
    - behavior: gain pulses, loss drains (motion not hue); the three movement toasts are textually distinct; a ground pickup does not fire the steal toast.
    - test-command: `pnpm build` green.
  </acceptance_criteria>
  <done>Counter flashes on gain/loss by motion, and the three shard-movement events show distinct self toasts while a plain pickup shows only a pulse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render/request → server | The client only renders shard_drop and requests collectShard; it never writes transcendShards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | client collect request | medium | mitigate | The client only calls `collectShard({ dropId })`; the server validates grace + ownership. No client-side shard credit. |
| T-03-03 | Tampering | early-grab race on a fresh drop | medium | mitigate | Client honors GEM_PICKUP_DELAY before requesting; the server is the authority if the client is patched. Collect races self-heal via subscription (row vanishes, mesh disposed). |
| T-03-SC | Tampering | npm/pnpm installs | low | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pnpm build` green.
- UI-SPEC checks (verified in Plan 05 playtest): purple/1.4x/brighter/sparkling drop; drain-not-danger loss flash; three distinct toasts; pickup shows no steal toast.
</verification>

<success_criteria>
- Distinct purple shard drop renders and is collectible; counter flashes on gain/loss; the three shard-movement toasts are distinct and a pickup shows none.
</success_criteria>

<output>
Create `.planning/phases/03-shards-at-risk/03-04-SUMMARY.md` when done.
</output>
