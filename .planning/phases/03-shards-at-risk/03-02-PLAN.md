---
phase: 03-shards-at-risk
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - spacetimedb/src/index.ts
autonomous: true
requirements: [REQ-shard-risk]
must_haves:
  truths:
    - "A public shard_drop table (mirror of gem_drop) exists and is registered in the schema, with droppedAtMicros defaulted to 0n (additive migration + grace check)."
    - "spillShards inserts exactly ONE shard_drop row of amount=SHARD_DEATH_LOSS, scattered with GEM_SPILL_SCATTER + clampToWorld, stamped droppedAtMicros = ctx.timestamp.microsSinceUnixEpoch."
    - "collectShard credits ctx.sender's transcendShards by drop.amount only after the reused gemIsCollectible grace passes, then deletes the drop."
    - "On PVE death (takeDamage AND worldTick loop) the existing gem spill is kept AND, when shardsLost>0, a shard_drop is spilled at the death spot (NO killer credit)."
    - "On PVP death (attackPlayer) the existing gem spill/credit is kept AND the killer is credited exactly result.shardsLost (0 on an empty victim), with NO ground drop."
    - "A PVP-killed victim carrying shards but 0 gems still transfers the shard to the killer — the shard credit is NOT gated by the gem spill (`if (stolen > 0)`) guard, so the shard is never destroyed. [A1]"
    - "On any death the victim's next* values are written: player.transcendShards = result.nextShards, and when eroded the owned-character row updates transcendLevel/constellation."
    - "0-shard death erodes transcendLevel first, then one constellation level (C6->C5); the C0-C6 floor is never breached beyond that single C-level."
  artifacts:
    - "shard_drop table + shardDrop accessor in spacetimedb/src/index.ts."
    - "spillShards(ctx, positionX, positionZ, amount, droppedBy) helper."
    - "collectShard reducer."
    - "applyDeathShardPenalty wired into takeDamage, the worldTick player-death loop, and attackPlayer."
  key_links:
    - "collectShard REUSES the generic gemIsCollectible + GEM_PICKUP_DELAY_MICROS (not a fork)."
    - "Each death path resolves the active owned character with findOwnedRow(ctx, <player>, <player>.activeCharacterId) and passes primitives into applyDeathShardPenalty from Plan 01."
    - "The PVP killer credit expression is attacker.transcendShards + result.shardsLost — the same result object that produced the victim's next* values."
  prohibitions:
    - "NEVER credit the PVP killer a hardcoded 1 — always result.shardsLost (0 when the victim eroded a level instead). [A1]"
    - "NEVER nest the PVP killer shard credit inside the gem-spill `if (stolen > 0)` block (index.ts:957) — a 0-gem shard carrier would drop the credit and DESTROY the shard. The shard credit runs whenever result.shardsLost > 0, independent of the gem stolen amount. [A1]"
    - "NEVER decrement transcendShards/transcendLevel/constellation outside the branch that proved it > 0 — rely on the Plan 01 helper's next* values."
    - "NEVER add shard_drop to vacuumGems (index.ts:1907) — a camp must not eat a scarce shard."
    - "NEVER fork gemIsCollectible for shards — reuse the generic one."
    - "NEVER drop the existing gem spill in any death path — the shard logic is ADDITIVE."
    - "NEVER wire fallToDeath (index.ts:1105) — void death costs no shard. [D1]"
    - "NEVER denominate a shard spill — spillShards inserts a single count-1 piece, not a GEM_DENOMINATIONS shower."
    - "NEVER create a shard_steal broadcast event table. [D2]"
threat_mitigations:
  - "T-03-01: victim mutation flows only through the Plan 01 helper's next* (no raw subtraction)."
  - "T-03-02: PVP credit is result.shardsLost, never a fixed 1."
  - "T-03-03: collectShard gates on gemIsCollectible + GEM_PICKUP_DELAY_MICROS server-side."
  - "T-03-04: all shard mutation is server-side; clients never write transcendShards."
  - "T-03-05: shard_drop excluded from vacuumGems."
---

<objective>
Add the server-authoritative shard-drop economy to `spacetimedb/src/index.ts` and wire the
Plan 01 penalty helper into the three death paths, keeping the existing gem spill everywhere.
This is a faithful in-file mirror of the gem-drop machinery (`gem_drop` / `spillDenominations`
/ `collectGem`) plus the highest-risk edit of the phase: the PVP killer credit (A1).

Purpose: turn carried shards into a real stake — PVE death spills a collectible shard, PVP death
transfers it to the killer, and a shard-less death erodes the transcend layer (then one C-level)
in the locked order without ever breaching the C0-C6 floor.

Output: `shard_drop` table + accessor, `spillShards` helper, `collectShard` reducer, and the
three wired death paths — all in `spacetimedb/src/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shards-at-risk/03-RESEARCH.md
@.planning/phases/03-shards-at-risk/03-PATTERNS.md
@spacetimedb/src/deathPenalty.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shard_drop table + spillShards + collectShard (gem-drop mirror)</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts:297-309 (gem_drop table — exact shape to mirror), :565 (schema register), :746-795 (spillDenominations/spillGems insert body), :1505-1521 (collectGem reducer), :83 (SHARD_DEATH_LOSS), :291 (player.transcendShards)
    - .planning/phases/03-shards-at-risk/03-PATTERNS.md (Edits 1-4, lines 92-145 — copyable excerpts + adaptation notes)
  </read_first>
  <action>
    Four in-file edits mirroring the verified gem machinery:
    (1) Define `const shardDrop = table({ name: 'shard_drop', public: true }, { ...identical columns
    to gem_drop... })` next to `gemDrop` (index.ts:297) — id u64 primaryKey autoInc, positionX/Z f32,
    amount u32, droppedBy identity, `droppedAtMicros: t.u64().default(0n)` (the default keeps it an
    additive migration and reads as long-elapsed = collectible for the grace check).
    (2) Register `shardDrop,` in the `schema({ ... })` object (index.ts:565) alongside `gemDrop,`.
    (3) Add `function spillShards(ctx, positionX, positionZ, amount, droppedBy)` modeled on the
    `spillDenominations` insert body (index.ts:769-780) but SINGLE-PIECE: guard `if (amount <= 0)
    return;`, compute one scattered position with `ctx.random()`, `GEM_SPILL_SCATTER`, and
    `clampToWorld`, then insert ONE `ctx.db.shardDrop` row with the passed `amount`, `droppedBy`, and
    `droppedAtMicros: ctx.timestamp.microsSinceUnixEpoch`. Do NOT denominate.
    (4) Add `export const collectShard = spacetimedb.reducer({ dropId: t.u64() }, ...)` mirroring
    `collectGem` (index.ts:1505): `const currentPlayer = requirePlayer(ctx)`, find the drop via
    `ctx.db.shardDrop.id.find(dropId)`, return if absent, gate on the REUSED
    `gemIsCollectible(drop.droppedAtMicros, ctx.timestamp.microsSinceUnixEpoch, GEM_PICKUP_DELAY_MICROS)`,
    then `ctx.db.shardDrop.id.delete(dropId)` and update the player with
    `transcendShards: currentPlayer.transcendShards + drop.amount`. Do NOT touch vacuumGems.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - source: `shard_drop` table defined with `droppedAtMicros: t.u64().default(0n)` and registered in `schema({...})`; `spillShards` and `collectShard` present; `vacuumGems` unchanged (still iterates gemDrop only).
    - behavior: `collectShard` calls the same `gemIsCollectible(...)` used by `collectGem` (no forked grace); `spillShards` inserts exactly one row and early-returns on amount<=0.
    - test-command: `pnpm build` compiles the module (TypeScript) green; `pnpm vitest run src/game/data/__tests__/serverWorldSim.test.ts` (existing grace test) stays green.
  </acceptance_criteria>
  <done>shard_drop table + accessor, spillShards, and collectShard exist and compile; the gem machinery is untouched and vacuumGems still excludes shards.</done>
</task>

<task type="auto">
  <name>Task 2: Wire applyDeathShardPenalty into the 3 death paths (keep gem spill)</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts:1099-1101 (takeDamage PVE spill), :2319-2320 (worldTick PVE spill), :954-963 (attackPlayer PVP kill — highest-risk), :700-704 (findOwnedRow), :725-740 (respawnPlayerAtSpawn), :1592-1600 (activatedConstellationFor read-time clamp)
    - spacetimedb/src/deathPenalty.ts (the helper from Plan 01 — applyDeathShardPenalty signature + result fields)
    - .planning/phases/03-shards-at-risk/03-PATTERNS.md (Edits 5a/5b/5c, lines 147-172 — per-path wiring)
  </read_first>
  <action>
    Wire the pure helper into all three paths, KEEPING the existing `spillGems` call in each:
    Import `applyDeathShardPenalty` from `./deathPenalty`. In each path resolve
    `const activeOwned = findOwnedRow(ctx, <player>, <player>.activeCharacterId)` (helper at :700),
    call `const result = applyDeathShardPenalty(<player>.transcendShards, activeOwned.transcendLevel,
    activeOwned.constellation, SHARD_DEATH_LOSS)`, and write the victim's next* state: set
    `transcendShards: result.nextShards` on the respawned player row, and when
    `result.erodedTranscend || result.erodedConstellation` also
    `ctx.db.ownedCharacter.id.update({ ...activeOwned, transcendLevel: result.nextTranscendLevel,
    constellation: result.nextConstellation })`.
    - PVE takeDamage (index.ts:1099-1101): keep `spillGems(...)`; if `result.shardsLost > 0` call
      `spillShards(ctx, currentPlayer.positionX, currentPlayer.positionZ, result.shardsLost,
      currentPlayer.identity)`; add `transcendShards: result.nextShards` to the respawn spread.
    - PVE worldTick loop (index.ts:2319-2320): identical treatment on `targetPlayer` (droppedBy =
      the dying player's identity).
    - PVP attackPlayer (index.ts:954-963): keep the gem spill/credit; compute `result` on the
      VICTIM (`target`); add `transcendShards: result.nextShards` to the target respawn spread and
      erode the target's owned row when flagged; credit the killer
      `attacker.transcendShards + result.shardsLost` (NOT a hardcoded 1 — 0 on a 0-shard victim).
      CRITICAL — the killer's shard credit MUST be UNCONDITIONAL with respect to the gem spill:
      the existing attacker update at index.ts:957-962 is wrapped in `if (stolen > 0)` where
      `stolen` is the GEM spill amount. Do NOT nest the shard credit inside that gem guard — a
      victim carrying >=1 shard but 0 gems yields `stolen === 0`, skips that block, and the
      shard credit would be DROPPED while the victim's transcendShards is already decremented,
      DESTROYING the shard (violates shard conservation + ROADMAP success criterion #2). Instead
      credit the killer whenever `result.shardsLost > 0` in an attacker update that runs
      independent of the `stolen > 0` branch (either a separate `if (result.shardsLost > 0)`
      attacker update, or a single attacker update whose gate is `stolen > 0 || result.shardsLost
      > 0` folding both credits together). NO ground drop on PVP.
    Do NOT wire fallToDeath (index.ts:1105). For constellation erosion, rely on the existing
    read-time clamp in `activatedConstellationFor` (index.ts:1592) — confirm no un-clamped consumer
    reads `activatedConstellation`; only write the activation row additionally if such a consumer
    is found (document the finding in the SUMMARY). [D3]
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - source: all three paths still call `spillGems`; each calls `applyDeathShardPenalty` and writes `transcendShards: result.nextShards`; the PVP killer credit is `attacker.transcendShards + result.shardsLost` (grep confirms no `+= 1` / `+ 1` shard credit); `fallToDeath` is unchanged.
    - source: the PVP killer shard credit is NOT nested inside the gem-spill `if (stolen > 0)` block (index.ts:957) — it runs whenever `result.shardsLost > 0` regardless of the gem `stolen` amount (its guard is `result.shardsLost > 0` or a combined `stolen > 0 || result.shardsLost > 0`, never `stolen > 0` alone).
    - behavior: PVE paths call `spillShards` only when `result.shardsLost > 0` and never credit a killer; PVP path credits the killer and produces no shard_drop; eroded owned rows update transcendLevel/constellation via result.next*.
    - behavior: a PVP-killed victim carrying >=1 shard but 0 gems (so gem `stolen === 0`) STILL transfers the shard to the killer — the credit is not lost to the gem guard, and world shard total is conserved.
    - test-command: `pnpm build` green; `pnpm vitest run src/game/data/__tests__/deathPenalty.test.ts src/game/data/__tests__/serverSync.test.ts` green.
  </acceptance_criteria>
  <done>All three death paths keep their gem spill and additively apply the shard penalty; PVP credits result.shardsLost (never a fixed 1); fallToDeath untouched; erosion order + floor honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → reducer (collectShard) | Untrusted `dropId` crosses; identity must come from `ctx.sender`, never args. |
| enemy/PVP kill → victim/killer rows | Server decides what is lost/transferred; a client cannot fake a shard gain. |
| shard_drop ground row → any grabber | First armed grab wins; grace + server ownership arbitrate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | victim transcendShards/level decrement | high | mitigate | All victim mutation flows through the Plan 01 helper's `next*` (Math.max-guarded, branch-ordered); no raw subtraction in the reducers. |
| T-03-02 | Elevation of Privilege / economy | attackPlayer killer credit | high | mitigate | Credit `attacker.transcendShards + result.shardsLost` (0 on empty victim); never a hardcoded 1 — the empty-victim mint is impossible. [A1] |
| T-03-03 | Tampering | collectShard early-grab / vacuum race | medium | mitigate | Reuse `gemIsCollectible` + `GEM_PICKUP_DELAY_MICROS`; server refuses grabs inside the 1.2s grace regardless of client. |
| T-03-04 | Spoofing | client forging a shard gain | medium | mitigate | All shard mutation is server-side; `collectShard`/credit act on `requirePlayer(ctx)`; no client-supplied amount or identity trusted. |
| T-03-05 | Denial (of reward) | camp eating a scarce shard | low | mitigate | shard_drop is excluded from `vacuumGems`; only players can grab it after grace. |
| T-03-SC | Tampering | npm/pnpm installs | low | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pnpm build` green (module + client typecheck).
- `pnpm vitest run src/game/data/__tests__/deathPenalty.test.ts src/game/data/__tests__/serverSync.test.ts src/game/data/__tests__/serverWorldSim.test.ts` green.
- Code review: PVP credit uses `result.shardsLost`; all 3 paths keep `spillGems`; vacuumGems untouched; fallToDeath untouched.
</verification>

<success_criteria>
- shard_drop table + spillShards + collectShard exist and compile; grace reused, camps excluded.
- All 3 death paths additively apply the penalty; PVP credits exactly result.shardsLost.
- 0-shard death erodes transcend then one C-level; C0-C6 floor never breached beyond one C.
</success_criteria>

<output>
Create `.planning/phases/03-shards-at-risk/03-02-SUMMARY.md` when done. Record the D3 finding
(whether any un-clamped activatedConstellation consumer required an extra activation-row write).
</output>
