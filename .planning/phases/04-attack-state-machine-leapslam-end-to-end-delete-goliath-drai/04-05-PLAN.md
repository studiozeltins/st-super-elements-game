---
phase: 04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/game/systems/createEntityRenderer.ts
  - src/game/systems/createGoliathRenderer.ts
  - src/game/createGame.ts
autonomous: true
requirements: [ANIM-03]
must_haves:
  truths:
    - "EntityAnimation gains an OPTIONAL animateAttack hook — the enemy renderer compiles unchanged, heroes/camp enemies can implement it later with zero schema change (ANIM-03)"
    - "The goliath visibly crouches during windup (silhouette compress), leaps in a parabolic arc to the LOCKED landing at strike, and eases back to neutral during recovery (D4-16)"
    - "Attack phase + progress driving the animation are derived from unit_attack row micros (same arrival-anchored derivation as the telegraph) — never a client-local timer"
    - "During windup the mesh faces the landing point instead of lerp-chasing its movement target (rooted crouch reads aimed, D4-12)"
  artifacts:
    - "src/game/systems/createEntityRenderer.ts (AttackAnimationView + optional animateAttack + setAttackViews)"
    - "src/game/systems/createGoliathRenderer.ts (goliath animateAttack implementation)"
    - "src/game/createGame.ts (attack-view map built in syncUnitAttacks, fed to the goliath renderer)"
  key_links:
    - "createGame.syncUnitAttacks builds Map<unitId-string, AttackAnimationView> from the same rows the telegraph consumes and pushes it via goliathRenderer.setAttackViews"
    - "createEntityRenderer.update() calls animation.animateAttack?.(view) after animateMovement and skips lookAt during windup"
---

<objective>
Give the slam a body: extend the shared `EntityAnimation` contract with an optional
`animateAttack` hook (ANIM-03 — unit-agnostic, heroes reuse later with zero schema change) and
implement the goliath's crouch → leap arc → slam recovery procedurally on the existing mesh rig
(D4-16), driven by attack views derived from `unit_attack` rows.

Purpose: the telegraph says WHERE, the animation says WHO and WHEN — together they complete the
readability contract that makes the dodge fair.
Output: extended renderer contract + goliath attack animation + view plumbing in createGame.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-CONTEXT.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- **Interface `AttackAnimationView`** (createEntityRenderer.ts): `attackId: string`, `phase: 'windup' | 'strike' | 'recovery'`, `phaseProgress: number` (0..1), `castX/castZ: number`, `landingX/landingZ: number`
- **Optional method `animateAttack?(view: AttackAnimationView): void`** on `EntityAnimation`
- **Renderer method `setAttackViews(views: Map<string, AttackAnimationView>): void`** on the entity renderer
- **Goliath `animateAttack` implementation** in `createGoliathAnimation` (createGoliathRenderer.ts)
- **createGame.ts**: attack-view map derivation inside `syncUnitAttacks` + per-frame phaseProgress refresh

<tasks>

<task type="auto">
  <name>Task 1: createEntityRenderer.ts — AttackAnimationView, optional animateAttack, setAttackViews, windup facing</name>
  <files>src/game/systems/createEntityRenderer.ts</files>
  <read_first>
    - src/game/systems/createEntityRenderer.ts (EntityAnimation interface at ~11-16; update() loop at ~210-234 with the isMoving/lookAt/animateMovement sequence; SNAP_DISTANCE=8 / LERP_RATE=10 constants at ~65-66 — the leap distance constraint context; syncRows key derivation for the view-map key format)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md (section "Pattern 6" — the exact AttackAnimationView shape and plumbing design)
  </read_first>
  <action>
    Three additive edits, generic (unit-agnostic — nothing goliath-specific lands in this file):
    1. Export interface AttackAnimationView with exactly the fields listed in "Artifacts this
       plan's share". Extend EntityAnimation with the OPTIONAL method
       `animateAttack?(view: AttackAnimationView): void` — optional so the enemy renderer's
       existing animations compile untouched (ANIM-03 zero-change reuse).
    2. Add `setAttackViews(views: Map<string, AttackAnimationView>): void` to the renderer's
       returned interface: store the map in a closure variable (replace wholesale each call; keyed
       by the same id-string key syncRows uses for entities).
    3. In update(), after resolving each entity: look up its view; when a view exists call
       `entity.animation.animateAttack?.(view)` AFTER animateMovement. When the view's phase is
       'windup', skip the movement lookAt and instead orient the group toward
       (view.landingX, view.landingZ) — the rooted crouch faces where it will leap (D4-12).
       Strike/recovery phases keep the normal lookAt behavior (the mesh is travelling/settling).
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 (enemy renderer compiles with NO change to its animation objects — proves optionality)
    - `grep -c "animateAttack?" src/game/systems/createEntityRenderer.ts` returns >= 2 (interface declaration + optional-chained call site)
    - `grep -c "setAttackViews" src/game/systems/createEntityRenderer.ts` returns >= 1
    - `grep -c "AttackAnimationView" src/game/systems/createEntityRenderer.ts` returns >= 3 (export, method param, view-map type)
    - git diff shows zero hunks in createEnemyRenderer/enemy animation files (unit-agnostic contract change only)
  </acceptance_criteria>
  <done>The shared animation contract supports per-phase attack animation without touching any non-goliath implementer.</done>
</task>

<task type="auto">
  <name>Task 2: Goliath crouch/leap/slam implementation + attack-view plumbing in createGame</name>
  <files>src/game/systems/createGoliathRenderer.ts, src/game/createGame.ts</files>
  <read_first>
    - src/game/systems/createGoliathRenderer.ts (createGoliathAnimation at ~21-43 — cached part refs, baseBodyY, the animateMovement/animateDeath structure the third method joins)
    - src/game/createGame.ts (syncUnitAttacks added in Plan 04 — the stored rows this task derives views from; the frame loop where goliathRenderer.update runs at ~918-919; goliath row tracking from syncGoliaths)
    - src/game/systems/createEntityRenderer.ts (Task 1 AttackAnimationView + setAttackViews API)
    - src/game/systems/createTelegraphSystem.ts (the arrival-anchored micros derivation from Plan 04 — reuse the same approach for phaseProgress; do not invent a second clock model)
  </read_first>
  <action>
    createGoliathRenderer.ts — implement animateAttack in createGoliathAnimation (D4-16), procedural
    transforms only, THREE.MathUtils.lerp/clamp as the sole interpolation helpers (zero-new-deps
    lock; no AnimationMixer):
    - phase 'windup': crouch — compress the body silhouette: scale body Y toward ~0.6 and lower
      body position toward the ground, eased by phaseProgress (deeper as the strike nears).
    - phase 'strike': leap — parabolic vertical arc: group/body Y offset = sin(phaseProgress * PI) *
      leapHeight (~1.5u); X/Z travel rides the existing position lerp toward the server's landing
      (the server already teleported the row position at strike; the <= 8u band keeps it under
      SNAP_DISTANCE so it lerps as a fast lunge rather than snapping).
    - phase 'recovery': ease every transformed part back to neutral over the phase (restore
      baseBodyY/scale), a heavy "settle" read.
    - Always restore neutral transforms when a view disappears (guard against a mid-attack death
      leaving a squashed mesh).

    createGame.ts — derive views from the rows already stored by syncUnitAttacks (Plan 04):
    - Maintain per-row arrival anchors exactly like the telegraph does (capture performance.now()
      on state transition). Each frame (or in syncUnitAttacks + a light per-frame refresh), build
      Map<string, AttackAnimationView> keyed by the goliath id-string the renderer uses:
      phase from row.state (windup 1 / strike 2 / recovery 3; idle rows produce NO view),
      phaseProgress = clamped elapsed/duration for the current phase from row micros
      (windup: startedAt->strikeAt; strike: strikeAt->grace end approximated by the next state
      change arriving; recovery: until recoveryEndsAtMicros), castX/Z + landingX/Z straight from
      the row. Push via goliathRenderer.setAttackViews(map).
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm test` green
    - `grep -c "animateAttack" src/game/systems/createGoliathRenderer.ts` returns >= 1 (implementation present)
    - `grep -c "setAttackViews" src/game/createGame.ts` returns >= 1 (plumbing present)
    - `grep -c "AnimationMixer" src/game/systems/createGoliathRenderer.ts` returns 0 (procedural rig only, zero-new-deps lock)
    - Source assertion: phaseProgress derivation reads startedAtMicros/strikeAtMicros/recoveryEndsAtMicros from the row — no hard-coded 1.2s windup constant in createGame.ts or the renderer
  </acceptance_criteria>
  <done>The goliath visibly winds up, leaps to the locked landing, and recovers, driven end-to-end by server row state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server rows -> client render | read-only consumption of public unit_attack rows; no new client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Spoofing | animation timing drifting from server truth (mesh strikes before/after the real strike) | low | mitigate | phaseProgress derives from row micros with the same arrival anchoring as the telegraph; acceptance criteria forbid hard-coded durations |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new dependencies (locked); no install tasks in this plan |
</threat_model>

<verification>
- `pnpm build` + `pnpm test` green
- Enemy renderer untouched (optional hook proves ANIM-03's zero-change reuse claim)
- No tween/animation library appears in package.json diff (must be empty)
</verification>

<success_criteria>
- ANIM-03: shared procedural animation contract (idle/move via existing animateMovement; windup/strike/recovery via animateAttack) on EntityAnimation, goliath implements it, enemies compile unchanged
- D4-16: crouch -> leap arc to locked landing -> slam settle, procedural on the existing rig
</success_criteria>

<output>
Create `.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-05-SUMMARY.md` when done
</output>
