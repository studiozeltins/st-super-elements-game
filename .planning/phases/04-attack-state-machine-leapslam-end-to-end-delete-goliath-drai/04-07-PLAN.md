---
phase: 04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai
plan: 07
type: execute
wave: 7
depends_on: ["04-06"]
files_modified: []
autonomous: false
requirements: [FSM-02, FSM-06, ANIM-02, HIT-01]
must_haves:
  truths:
    - "On the MIGRATED local DB, a real engage creates unit_attack rows (SQL-probed) and the full loop reads correctly: telegraph fills, goliath crouches/leaps/slams, juice fires once, dodge works, knockback+stun land (phase SC1-SC4)"
    - "The maincloud DB is backed up, then migrate-published (no wipe) with the cumulative Phase 1-4 schema, and unit_attack rows appear after a real remote engage (FSM-06, SC5)"
    - "Dodge stays fair over real maincloud RTT in a two-client engage — leave the circle before it lands, take no damage (FSM-02/D4-02, SC5)"
    - "Telegraph is legible through the actual pixel filter at target resolution (ANIM-02 human sign-off)"
  artifacts:
    - "Playtest + probe evidence recorded in 04-07-SUMMARY.md (SQL outputs, checkpoint outcomes)"
  key_links:
    - "backup BEFORE maincloud publish (account/account_link are unrestorable after a wipe — the backup is the only safety net)"
    - "localhost + VITE_SPACETIMEDB_HOST=wss://maincloud.spacetimedb.com is the sanctioned way to point dev clients at maincloud for the RTT test (src/config.ts routing rule 1)"
---

<objective>
Prove the spine where it must actually hold: the migrated-DB and maincloud-RTT verification
capstone. Local human playtest of the full loop (SC1-SC4), then maincloud backup + additive
publish + remote two-client playtest over real RTT (SC5) — the latency-fairness validation D4-02
explicitly mandates ("LAN hides the unfairness", STATE blocker #3).

Purpose: FSM-06's "rows exist after a real engage on a MIGRATED (not freshly-seeded) DB" and
D4-02's grace model are only verifiable against live databases and real network latency — this is
the phase's acceptance test, not an afterthought.
Output: verified deployment on local + maincloud, human-verified dodge fairness and telegraph
legibility, evidence in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-CONTEXT.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md
</context>

## Artifacts this phase produces (this plan's share)

- No new code symbols — this plan verifies the Phase-4 artifacts (unit_attack/attack_strike tables, runUnitAttacks pass, telegraph/animation/juice systems, stun pipeline) on migrated live databases and records evidence. Temporary edit: `.env.local` `VITE_SPACETIMEDB_HOST` toggled for the maincloud RTT test and reverted.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Local migrated-DB end-to-end playtest (SC1-SC4)</name>
  <files>none (verification only)</files>
  <action>
    Prepare the checkpoint, then hand off to the human: run the baseline unit_attack SQL probe on
    the local server, confirm `pnpm build` output is current for laragon, present the five
    verification steps below, and WAIT for the human verdict. After the human engages, re-run the
    SQL probe (rows must exist — FSM-06) and capture `spacetime logs` output; record all probe
    evidence in the SUMMARY.
  </action>
  <what-built>The complete leapSlam spine is live on the local migrated DB (published in Plan 03, client built through Plan 06): server FSM + telegraph + crouch/leap/slam animation + one-shot strike juice + knockback/stun + deleted contact drain.</what-built>
  <how-to-verify>
    Before you start, Claude runs the baseline probes and reports them:
    - `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local` (note row count)
    - `pnpm build` freshness so laragon serves the current dist.

    Then in a browser at your LAN/localhost game URL:
    1. Provoke a goliath (hit it) and stand still: an icy-cyan circle should appear at your feet
       area, its inner disc filling outward over ~1.2s, the goliath crouching in place facing you.
    2. DODGE: when the fill is past halfway, sprint out of the circle. Expected: the goliath leaps
       to where the circle was, slam juice fires ONCE (flash + dust + one small camera shake + one
       thump), and you take ZERO damage (SC1).
    3. FACETANK: stay inside the circle. Expected: you take one big hit (405/585/765 pre-resist by
       goliath size), you are knocked back ~3 units away from the circle center, and your movement
       is dead for ~0.3s before control returns (SC4).
    4. CONTACT: walk into and hug the goliath between attacks. Expected: body shove but ZERO
       damage from contact ever — damage only arrives via landed slams (SC2); it keeps attacking
       (~every 6s cycle, no dead zone at point-blank).
    5. Death mid-windup: kill a goliath during its crouch. Expected: telegraph disappears, no
       posthumous slam.
    After the engage, Claude re-runs the unit_attack SQL probe — rows must now EXIST on this
    migrated DB (FSM-06) — and checks `spacetime logs 2d-impact-game-fr9ti --server local -n 100`
    for errors, then records both outputs in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local</automated>
    <human-check>All five playtest steps confirmed by the human (dodge, facetank+knockback+stun, zero contact damage, one-shot juice, no posthumous slam)</human-check>
  </verify>
  <done>Human approved SC1-SC4 on the local migrated DB; post-engage SQL probe shows unit_attack rows exist; logs clean.</done>
  <resume-signal>Type "approved" if all five checks pass, or describe exactly which step failed and what you saw</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Maincloud backup, additive publish, remote probe, RTT client setup</name>
  <files>.env.local</files>
  <read_first>
    - CLAUDE.md (sections "Backup / restore" — backup command/flags, account tables NOT restorable; "Server-authoritative enemies + goliaths (deploy notes)" — migrate-publish rules, no seed needed for lazily-created rows; "How the client chooses which server to hit" — the VITE_SPACETIMEDB_HOST localhost override)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md (Pitfall 7 — maincloud carries the CUMULATIVE Phase 1-4 schema diff: attackEnemies/attackRay signature changes, enemy_hit, pvp_hit columns, skill cooldown columns, plus this phase's tables)
  </read_first>
  <action>
    Maincloud has not been published since before Phase 1 — this publish carries the cumulative
    crit-era + FSM schema. Order matters (RESEARCH Pitfall 7):
    1. BACKUP FIRST: obtain the bearer token (`spacetime login show --token`; if not logged in,
       pause for user authentication) and run
       `pnpm run backup -- --server maincloud --token <bearer>` — confirm a fresh
       backup/restore_*.json exists before anything else.
    2. PUBLISH (additive migrate, never a wipe):
       `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server maincloud --yes`
       — NO --delete-data under any circumstance (maincloud holds real accounts;
       account/account_link have NO restore path). If the migrate is REJECTED, stop and report the
       exact error — options like clearing a blocking table require user sign-off.
    3. PROBE: `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server maincloud`
       exits 0 (table exists, empty until an engage);
       `spacetime logs 2d-impact-game-fr9ti --server maincloud -n 50` shows no errors. No
       seed_world-style activation is needed — unit_attack rows lazily create on first engage.
    4. RTT CLIENT SETUP for Task 3: set `VITE_SPACETIMEDB_HOST=wss://maincloud.spacetimedb.com` in
       .env.local (per src/config.ts, this override applies ONLY to a browser at localhost — the
       vite dev server), start `pnpm dev`, and tell the user to open TWO browser windows at the
       localhost:5173 URL: both connect over real maincloud RTT. Note for cleanup: revert
       .env.local and stop the dev server after Task 3.
  </action>
  <verify>
    <automated>spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server maincloud</automated>
  </verify>
  <acceptance_criteria>
    - A backup/restore_*.json with today's timestamp exists BEFORE the publish command ran
    - Publish output reports a successful migrate/update; the command line contains no delete-data flag
    - `spacetime sql ... --server maincloud` for unit_attack AND attack_strike both exit 0
    - maincloud logs show no panic/error entries post-publish
    - .env.local contains the maincloud host override and `pnpm dev` is serving (for Task 3)
  </acceptance_criteria>
  <done>Maincloud runs the cumulative Phase 1-4 module additively, backed up, probed healthy, with two RTT clients ready to connect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Two-client maincloud RTT playtest — dodge fairness + telegraph legibility (SC5)</name>
  <files>.env.local (revert after approval)</files>
  <action>
    Confirm the Task-2 RTT setup is serving (two localhost windows -> maincloud), present the five
    verification steps below, and WAIT for the human verdict. After the engage, run the maincloud
    unit_attack SQL probe (rows must exist on the migrated cloud DB — FSM-06/SC5) and record the
    output. On approval: revert the .env.local override, stop the dev server, and confirm the
    laragon-served build still targets the local server per config routing.
  </action>
  <what-built>The full Phase-4 loop running against maincloud, reachable from two localhost dev-server windows over real internet RTT — the environment D4-02's grace model must be judged in (LAN hides the unfairness).</what-built>
  <how-to-verify>
    With the two localhost:5173 windows from Task 2 (both connected to maincloud — confirm the
    websocket URL in devtools points at maincloud, not 127.0.0.1):
    1. DODGE FAIRNESS (the phase's core promise, FSM-02/D4-02): in window A, provoke a goliath and
       repeatedly practice leaving the circle just before the fill completes — a dodge that LOOKS
       clean on your screen must deal NO damage, over ~10 attempts. If clean-looking dodges still
       get hit, the grace needs tuning (graceTicks is data in ATTACKS — report it, do not accept).
    2. BYSTANDER TRUTH (D4-03): stand BOTH windows' characters in one telegraph — both get hit;
       then one inside one outside — only the inside one is hit. The circle never lies.
    3. SPECTATOR VIEW: window B watches window A get slammed — B sees A's knockback displacement
       and the same single juice moment.
    4. TELEGRAPH LEGIBILITY (ANIM-02): through the pixel filter at your normal resolution, the
       icy-cyan outline + fill must read instantly against Mondstadt-green terrain — no shimmer,
       no broken/dotted ring, fill fraction readable as "time left".
    5. MIGRATED-DB ROWS (FSM-06/SC5): after the engage, Claude runs
       `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server maincloud` — rows
       exist, created by a real engage on the migrated cloud DB — and records the output.
    Afterward Claude reverts .env.local, stops the dev server, and confirms `pnpm build` output is
    what laragon serves for LAN play (which uses the LOCAL server per config routing).
  </how-to-verify>
  <verify>
    <automated>spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server maincloud</automated>
    <human-check>Dodge fairness over real RTT (~10 clean dodges, zero unfair hits), bystander truth, spectator view, and pixel-filter telegraph legibility all confirmed</human-check>
  </verify>
  <done>Human approved SC5 over maincloud RTT; unit_attack rows exist on the migrated cloud DB after a real engage; .env.local reverted.</done>
  <resume-signal>Type "approved" if dodge feels fair over RTT and all checks pass; otherwise describe the failure (especially any "I dodged that!" moments — those block the phase)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy -> maincloud DB with real accounts | schema migration + operator credentials cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | DoS (data loss) | destructive maincloud publish destroying accounts (no restore path for account/account_link) | critical | mitigate | backup runs FIRST and is verified to exist; publish command has no wipe flag; migrate rejection = stop-and-report, never escalate to a wipe |
| T-04-15 | Information Disclosure | bearer token / backup JSON (contains identities) leaking | medium | mitigate | backup/*.json is gitignored (existing); token used transiently on the command line, never written to a file or committed |
| T-04-16 | Tampering | shipping an unfair grace window that punishes honest dodges (integrity of the fairness promise) | high | mitigate | Task 3 step 1 is a blocking human gate over real RTT; graceTicks is data-only tunable (RESEARCH A2) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new dependencies (locked); no install tasks in this plan |
</threat_model>

<verification>
- unit_attack rows exist post-engage on BOTH migrated DBs (local + maincloud) — SQL evidence in SUMMARY
- Both human checkpoints approved (SC1-SC4 local, SC5 remote)
- .env.local reverted; no credential or backup file staged in git
</verification>

<success_criteria>
- SC1: leave-the-telegraph = no damage; stay = hit (verified on live server, then over RTT)
- SC2: zero contact damage; attack in every band at point-blank
- SC3/SC4: telegraph + animation + one-shot juice + knockback/stun verified by a human through the pixel filter
- SC5/FSM-06: rows created by a real engage on the MIGRATED maincloud DB; dodge fair over real RTT
</success_criteria>

<output>
Create `.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-07-SUMMARY.md` when done
</output>
