---
phase: 04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/attacks.ts
  - spacetimedb/src/unitAttackFsm.ts
  - spacetimedb/src/attackHitbox.ts
  - src/game/data/__tests__/attacks.test.ts
  - src/game/data/__tests__/unitAttackFsm.test.ts
  - src/game/data/__tests__/attackHitbox.test.ts
  - src/game/data/__tests__/serverSync.test.ts
autonomous: true
requirements: [FSM-03, FSM-04, FSM-05, ATK-06]
must_haves:
  truths:
    - "ATTACKS registry exists as data: one leapSlam entry carries every timing/geometry/reaction number locked in discuss (D4-01, D4-04..D4-07, D4-10)"
    - "selectAttack returns leapSlam for EVERY distance 0..maxBand when off cooldown, and null while on cooldown (D4-08 rhythm, ATK-05 dead-zone sweep)"
    - "FSM deadline math resolves a strike whose deadline a jumped/coalesced tick already passed — exactly once, never dropped (FSM-05)"
    - "Circle hit resolution and knockback displacement are pure, tested functions incl. edge-of-circle and zero-length-vector boundaries (ATK-06, HIT-01 math)"
    - "serverSync.test.ts asserts ATTACKS duration/shape parity + the 4.5x per-size arithmetic 405/585/765 (INV-5) and the whole suite stays green"
  artifacts:
    - "spacetimedb/src/attacks.ts"
    - "spacetimedb/src/unitAttackFsm.ts"
    - "spacetimedb/src/attackHitbox.ts"
    - "src/game/data/__tests__/attacks.test.ts"
    - "src/game/data/__tests__/unitAttackFsm.test.ts"
    - "src/game/data/__tests__/attackHitbox.test.ts"
  key_links:
    - "attacks.test.ts / serverSync.test.ts import ATTACKS across the package boundary from spacetimedb/src/attacks (same mechanism as crit.test.ts)"
    - "serverSync.test.ts imports GOLIATH_SIZE_STATS from spacetimedb/src/enemyStats to pin contactDamage x 4.5 = 405/585/765"
---

<objective>
Build the tested, zero-import pure foundation of the unit-agnostic attack FSM: the data-driven
`ATTACKS` registry + `UNIT_ATTACKS` lists + `selectAttack` selection fn (FSM-03, FSM-04, D4-08),
the deterministic FSM deadline/transition math (FSM-05), and the circle hitbox + knockback
displacement resolvers (ATK-06, HIT-01 math) — all test-first, before any reducer or table is
touched. Implements locked numbers D4-01 (windup 8 ticks), D4-02 (graceTicks 1), D4-04 (4.5x →
405/585/765), D4-05 (radii 4.0/4.75/5.5), D4-06 (recovery 8 ticks), D4-07 (cooldown 3.5s),
D4-09/D4-10 (knockback 3u + stun 2 ticks as per-attack DATA fields).

Purpose: everything Plan 02's reducer glue calls is already proven correct here; the pure/glue
split is the established pure-helper testing discipline (crit.ts pattern).
Output: three new server pure-helper modules + three new test files + serverSync.test.ts INV-5
extension, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-CONTEXT.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- **Module `spacetimedb/src/attacks.ts`** exporting: `UNIT_KIND_GOLIATH` (0), `ATTACK_STATE_IDLE` (0), `ATTACK_STATE_WINDUP` (1), `ATTACK_STATE_STRIKE` (2), `ATTACK_STATE_RECOVERY` (3), interface `AttackSpec` (fields: `shape`, `role`, `windupTicks`, `activeTicks`, `graceTicks`, `recoveryTicks`, `cooldownMicros`, `radiusBySize`, `damageMultiplier`, `minBand`, `maxBand`, `knockback`, `stunTicks`, `move`, `poiseThreshold`), const `ATTACKS: Record<string, AttackSpec>` (one entry: `leapSlam`), const `UNIT_ATTACKS: Record<number, Record<string, readonly string[]>>`, function `selectAttack(distance, nowMicros, cooldownUntilMicros, available): string | null`
- **Module `spacetimedb/src/unitAttackFsm.ts`** exporting: interface `WindupEntry`, functions `enterWindup(nowMicros, tickMicros, spec, sizeIndex, castX, castZ, targetX, targetZ): WindupEntry`, `strikeDeadline(startedAtMicros, windupTicks, tickMicros): bigint`, `graceDeadline(strikeAtMicros, graceTicks, tickMicros): bigint`, `recoveryDeadline(strikeAtMicros, graceTicks, recoveryTicks, tickMicros): bigint`, `stunDeadline(nowMicros, stunTicks, tickMicros): bigint`, `deadlinePassed(nowMicros, deadlineMicros): boolean`, `dueAttackTransitions(state, nowMicros, strikeAtMicros, graceAtMicros, recoveryEndsAtMicros): number[]`
- **Module `spacetimedb/src/attackHitbox.ts`** exporting: `resolveCircleHit(px, pz, centerX, centerZ, radius): boolean`, `knockbackDisplacement(victimX, victimZ, centerX, centerZ, distance, fallbackHeadingX, fallbackHeadingZ): { x: number; z: number }`
- **Test files**: `src/game/data/__tests__/attacks.test.ts`, `src/game/data/__tests__/unitAttackFsm.test.ts`, `src/game/data/__tests__/attackHitbox.test.ts`
- **Extended**: `src/game/data/__tests__/serverSync.test.ts` gains an "ATTACKS registry invariants (INV-5)" describe block

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATTACKS registry + UNIT_ATTACKS + selectAttack, test-first, plus serverSync INV-5 extension</name>
  <files>spacetimedb/src/attacks.ts, src/game/data/__tests__/attacks.test.ts, src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - spacetimedb/src/enemyStats.ts (GOLIATH_SIZE_STATS registry shape — the per-size readonly-array pattern radiusBySize follows; contactDamage 90/130/170)
    - spacetimedb/src/goliathAI.ts (chooseGoliathTargetCamp — the selection-fn doc-comment contract style selectAttack copies)
    - spacetimedb/src/crit.ts (header discipline for dependency-free helpers — copy the comment style; it doubles as the determinism grep-gate workaround)
    - src/game/data/__tests__/crit.test.ts (cross-boundary import pattern + boundary-case discipline)
    - src/game/data/__tests__/serverSync.test.ts (existing cross-boundary parity block at lines ~20-28 the new describe block sits beside)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md (section "Code Examples" — full AttackSpec field list and drafted serverSync extension block)
  </read_first>
  <behavior>
    - Test: ATTACKS.leapSlam has windupTicks 8, activeTicks 1, graceTicks 1, recoveryTicks 8, cooldownMicros 3_500_000n, radiusBySize [4.0, 4.75, 5.5], damageMultiplier 4.5, minBand 0, maxBand 8, knockback 3, stunTicks 2, move 'leap', shape 'circle', role 'skill' (D4-01/02/04/05/06/07/10)
    - Test: every ATTACKS entry has integer windupTicks >= 2 and integer recoveryTicks (0.35s / 2-tick windup floor, FSM-05)
    - Test: selectAttack(d, 0n, 0n, UNIT_ATTACKS[0].default) !== null for every d in 0..8 step 0.25 (ATK-05 dead-zone sweep)
    - Test: selectAttack returns null while nowMicros < cooldownUntilMicros (cooldown gates the skill)
    - Test: selectAttack returns null when distance > maxBand of every available attack (chase sentinel)
    - Test (serverSync.test.ts): GOLIATH_SIZE_STATS.map(s => Math.round(s.contactDamage * ATTACKS.leapSlam.damageMultiplier)) equals [405, 585, 765]; ATTACKS.leapSlam.radiusBySize.length === GOLIATH_SIZE_STATS.length
  </behavior>
  <action>
    Write the failing tests first (attacks.test.ts + the new serverSync describe block), then create
    spacetimedb/src/attacks.ts to make them green. The module is dependency-free (no import
    statements at all) so the client vitest runner tests it directly — copy the crit.ts header
    comment style, phrased so the repo determinism grep gate stays empty (name the banned globals
    only by concept in comments, never by their literal call text — crit.ts discipline).

    Exports (per D4 registry decisions; layout is the discretion granted in CONTEXT):
    - Unit-kind + state consts: UNIT_KIND_GOLIATH = 0; ATTACK_STATE_IDLE = 0, ATTACK_STATE_WINDUP = 1,
      ATTACK_STATE_STRIKE = 2, ATTACK_STATE_RECOVERY = 3 (u32-friendly numbers, matching the
      existing aggroKind u32 idiom).
    - interface AttackSpec with exactly these fields: shape ('circle' | 'cone' | 'lane'),
      role ('skill' | 'basic') for the D4-08 rhythm, windupTicks, activeTicks, graceTicks (D4-02),
      recoveryTicks, cooldownMicros (bigint), radiusBySize (readonly number[], positionally aligned
      with goliath sizeIndex like GOLIATH_SIZE_STATS), damageMultiplier, minBand, maxBand,
      knockback, stunTicks (D4-09: reaction is per-attack DATA), move ('none' | 'leap' | 'charge'),
      poiseThreshold (field exists now per FSM-04; consumed Phase 7).
    - ATTACKS: Record<string, AttackSpec> with the single leapSlam entry using the exact values in
      the behavior block above; poiseThreshold: 600.
    - UNIT_ATTACKS: Record<number, Record<string, readonly string[]>> = { 0: { default: ['leapSlam'] } }
      (per-archetype lists — a new unit is one list, FSM-04).
    - selectAttack(distance: number, nowMicros: bigint, cooldownUntilMicros: bigint,
      available: readonly string[]): string | null — doc-comment states the D4-08 rule verbatim:
      skill in band + off cooldown wins (maximize DPS); else basic in band (none exist in Phase 4);
      else null = keep chasing. Band test: minBand <= distance <= maxBand. Cooldown test:
      nowMicros >= cooldownUntilMicros. leapSlam's band 0..8 covers every distance this phase
      (no facetank dead zone, ATK-05); Phases 5/6 SPLIT bands by adding entries, never reshaping.

    Extend serverSync.test.ts with a new describe block "ATTACKS registry invariants (INV-5)"
    importing ATTACKS and UNIT_ATTACKS from '../../../../spacetimedb/src/attacks' and
    GOLIATH_SIZE_STATS from '../../../../spacetimedb/src/enemyStats' — assert the tick-multiple
    durations, the [405, 585, 765] arithmetic, and the radiusBySize/GOLIATH_SIZE_STATS length match
    (drafted in RESEARCH "Code Examples"). Do not modify any existing assertion.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/attacks.test.ts` passes with the behavior-block cases present
    - `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` passes including the new INV-5 block and every pre-existing assertion
    - `grep -c "^import" spacetimedb/src/attacks.ts` returns 0 (zero-import pure module)
    - `grep -rn "Math.random\|Date.now" spacetimedb/src` returns nothing (determinism gate stays empty)
    - `grep -c "poiseThreshold" spacetimedb/src/attacks.ts` returns >= 2 (interface field + leapSlam value — FSM-04 field exists now)
  </acceptance_criteria>
  <done>attacks.ts exports the full registry/selection API with locked D4 values; sweep test proves an attack exists in every band 0..8; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: unitAttackFsm.ts — deterministic deadline math + windup entry + late-tick transition resolution</name>
  <files>spacetimedb/src/unitAttackFsm.ts, src/game/data/__tests__/unitAttackFsm.test.ts</files>
  <read_first>
    - spacetimedb/src/combatMath.ts (bigint-micros arithmetic pattern: damagePerTick / windowBucketFor — caller injects tickMicros)
    - spacetimedb/src/crit.ts (zero-import header discipline)
    - spacetimedb/src/attacks.ts (AttackSpec shape + state consts created in Task 1 — enterWindup consumes an AttackSpec-shaped arg)
    - src/game/data/__tests__/crit.test.ts (boundary-case test discipline)
  </read_first>
  <behavior>
    - Test: enterWindup(now, tick, leapSlamSpec, sizeIndex 2, castX, castZ, targetX, targetZ) returns startedAtMicros === now, strikeAtMicros === now + 8n * tick, landingX/Z === the target sample (ATK-01 landing LOCKED at cast), castX/Z passthrough (D4-12 root point), radius === 5.5 (sizeIndex-indexed), poise === 0 (reset-on-windup-entry), strikeResolved === false, state === ATTACK_STATE_WINDUP
    - Test: deadlinePassed(now, deadline) is true when now === deadline (>= semantics, never equality-only) and false one microsecond before
    - Test: dueAttackTransitions(ATTACK_STATE_WINDUP, startedAt + 10n * tick, strikeAt, graceAt, recoveryEndsAt) — a "jumped two intervals" now — returns [ATTACK_STATE_STRIKE, ATTACK_STATE_RECOVERY] in order: the passed strike deadline is resolved, never dropped (FSM-05)
    - Test: dueAttackTransitions(ATTACK_STATE_WINDUP, strikeAt, ...) returns exactly [ATTACK_STATE_STRIKE] (strike fires at its exact deadline, grace not yet due)
    - Test: dueAttackTransitions(ATTACK_STATE_RECOVERY, recoveryEndsAt, ...) returns [ATTACK_STATE_IDLE]
    - Test: graceDeadline(strikeAt, 1, tick) === strikeAt + tick; recoveryDeadline(strikeAt, 1, 8, tick) === strikeAt + 9n * tick; stunDeadline(now, 2, tick) === now + 2n * tick
  </behavior>
  <action>
    Write the failing tests first, then create spacetimedb/src/unitAttackFsm.ts. Dependency-free
    except for a pure sibling import of the ATTACK_STATE_* consts and AttackSpec type from
    './attacks' (pure-to-pure sibling import is the established goliathAI.ts precedent). All time
    inputs are bigint micros; the caller injects tickMicros (WORLD_TICK_INTERVAL_MICROS = 150_000n
    on the server side) — this module never reads a clock.

    Exports:
    - strikeDeadline(startedAtMicros, windupTicks, tickMicros) = startedAtMicros + BigInt(windupTicks) * tickMicros
    - graceDeadline(strikeAtMicros, graceTicks, tickMicros) — the single damage-resolution moment
      chosen for D4-02 (discretion: zero-storage grace — resolve ONCE at strikeAt + graceTicks * tick
      vs live positions; a player who left within the final ~150ms reads outside and counts OUT)
    - recoveryDeadline(strikeAtMicros, graceTicks, recoveryTicks, tickMicros) = strikeAt + (grace + recovery) * tick
    - stunDeadline(nowMicros, stunTicks, tickMicros) — HIT-01 stun window end (D4-10: 2 ticks)
    - deadlinePassed(nowMicros, deadlineMicros) — the ONLY comparison primitive, >= semantics (FSM-05)
    - enterWindup(...) returning a WindupEntry object with every unit_attack row field the windup
      transition writes: state, startedAtMicros, strikeAtMicros, landingX, landingZ, castX, castZ,
      radius (spec.radiusBySize[sizeIndex] with a last-element clamp for out-of-range index),
      strikeResolved: false, poise: 0
    - dueAttackTransitions(state, nowMicros, strikeAtMicros, graceAtMicros, recoveryEndsAtMicros): number[]
      — returns the ORDERED list of target states due at this now, cascading so a coalesced tick
      that jumped past several deadlines emits every due transition in one call (WINDUP past grace
      returns strike then recovery; the glue in Plan 02 walks this list applying side effects).

    Note for the verifier (RESEARCH assumption A6): damage resolves exactly ONCE, at the grace
    deadline — this is the CONTEXT D4-02 reading of FSM-02's "resolves once at the strike frame";
    D4-02 outranks a literal same-microsecond reading.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts` passes with every behavior-block case present, including the jumped-two-intervals case
    - `grep -cE "^import .* from '\./attacks'" spacetimedb/src/unitAttackFsm.ts` returns 1 (the only import is the pure sibling)
    - `grep -rn "Math.random\|Date.now" spacetimedb/src` returns nothing
    - unitAttackFsm.ts contains no reference to ctx, tables, or the database — source assertion: `grep -c "ctx\." spacetimedb/src/unitAttackFsm.ts` returns 0
  </acceptance_criteria>
  <done>All FSM timing is pure, injected-clock math; the late/coalesced-tick case provably resolves rather than drops; landing lock + poise reset are pinned by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: attackHitbox.ts — circle resolver + knockback displacement with zero-vector fallback</name>
  <files>spacetimedb/src/attackHitbox.ts, src/game/data/__tests__/attackHitbox.test.ts</files>
  <read_first>
    - spacetimedb/src/combatMath.ts (distanceBetween — the circle test IS this helper; do not write new distance code)
    - spacetimedb/src/goliathAI.ts (headingFromStep lines ~44-57 — the exact zero-length-vector fallback branch shape to replicate for knockback direction)
    - src/game/data/__tests__/crit.test.ts (boundary-case discipline)
  </read_first>
  <behavior>
    - Test: resolveCircleHit(px, pz, cx, cz, r) is true strictly inside, true exactly ON the radius edge (inclusive <=), false outside
    - Test: knockbackDisplacement pushes the victim AWAY from the circle center by exactly `distance` units along the normalized (victim - center) vector
    - Test: victim standing exactly on the center (zero-length vector) falls back to the supplied fallback heading direction, still displaced by `distance` (HIT-01 edge case)
    - Test: displacement preserves direction sign on both axes (victim north-east of center moves further north-east)
  </behavior>
  <action>
    Write the failing tests first, then create spacetimedb/src/attackHitbox.ts. Imports only the
    pure sibling distanceBetween from './combatMath' (established precedent: goliathAI.ts imports
    it). Exports:
    - resolveCircleHit(px, pz, centerX, centerZ, radius): boolean — implemented as
      distanceBetween(px, pz, centerX, centerZ) <= radius (ATK-06: reuse existing geometry helpers)
    - knockbackDisplacement(victimX, victimZ, centerX, centerZ, distance, fallbackHeadingX,
      fallbackHeadingZ): { x: number; z: number } — returns the DISPLACED position: normalize
      (victim - center); when the delta length is exactly 0, use the fallback heading (the goliath's
      facing, supplied by the caller) as the direction; scale by distance and add to the victim
      position. No world clamping here — the caller owns bounds policy (D4-11: knockback deaths off
      edges are a feature, no safety rails).

    Cone and lane resolvers are Phase 5 (ATK-02/03) and Phase 6 (ATK-04) deliverables per ROADMAP
    traceability — do NOT create dead stub functions for them (CLAUDE.md: no dead code). ATK-06's
    Phase-4 share is the circle resolver + the geometry-reuse pattern the later shapes copy
    (cone reuses isWithinForwardArc, lane reuses segment projection — noted for those phases).
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attackHitbox.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/attackHitbox.test.ts` passes with every behavior-block case present, including edge-of-circle and zero-vector fallback
    - `pnpm test` (full suite) is green — no existing test broken
    - `grep -c "distanceBetween" spacetimedb/src/attackHitbox.ts` returns >= 2 (import + use — geometry reuse, ATK-06)
    - `grep -rn "Math.random\|Date.now" spacetimedb/src` returns nothing
  </acceptance_criteria>
  <done>Circle resolution and knockback math are pure, tested, and reuse combatMath geometry; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan adds pure, table-free, client-unreachable modules only; no reducer, no table, no client input crosses anything |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | attacks.ts values drifting from locked D4 numbers over time | low | mitigate | serverSync.test.ts INV-5 block pins 405/585/765, tick multiples, radius array length — release-blocking if red |
| T-04-02 | Elevation of Privilege | nondeterminism sneaking into pure helpers (global RNG / wall clock) | medium | mitigate | repo grep gate over spacetimedb/src stays empty (acceptance criterion in every task); all time/randomness injected as args |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new dependencies this phase (locked); RESEARCH Package Legitimacy Audit confirms no installs — nothing to verify |
</threat_model>

<verification>
- `pnpm test` — full suite green (475+ existing tests + 3 new files + serverSync extension)
- `grep -rn "Math.random\|Date.now" spacetimedb/src` — empty
- No file in this plan exceeds 300 functional LOC (attacks.ts ~120, unitAttackFsm.ts ~100, attackHitbox.ts ~60 budgets from RESEARCH)
</verification>

<success_criteria>
- FSM-03: selectAttack exists with the mandated signature and passes the every-band sweep
- FSM-04: ATTACKS + UNIT_ATTACKS are data — adding an attack is one entry, a new unit is one list; poiseThreshold field present
- FSM-05: deadline math is >= based, injected-clock, and the jumped-two-intervals test proves resolve-never-drop
- ATK-06: circle resolver reuses distanceBetween; knockback displacement tested incl. zero-vector fallback
- INV-5: serverSync.test.ts extended and green
</success_criteria>

<output>
Create `.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-01-SUMMARY.md` when done
</output>
