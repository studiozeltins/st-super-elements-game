---
phase: 04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - spacetimedb/src/index.ts
  - spacetimedb/src/unitAttacks.ts
autonomous: true
requirements: [FSM-01, FSM-02, FSM-06, ATK-01, HIT-01]
must_haves:
  truths:
    - "unit_attack (public, by_unit btree on unitKind+unitId) and attack_strike (public event) tables exist in the schema, fully additive (FSM-01, FSM-06)"
    - "player rows gain stunnedUntilMicros with .default(0n) appended LAST, and updatePosition rejects client positions while stunned — stun is enforced, not advisory (HIT-01)"
    - "runUnitAttacks drives the FSM by iterating live goliaths and lazily upserting unit_attack rows via the by_unit index — it NEVER iterates the (migration-empty) attack table as the driver (FSM-06)"
    - "Windup entry locks the landing at the aggro target's position sampled once, roots the goliath at its cast point, and resets poise to 0 (ATK-01, D4-12)"
    - "Strike damage resolves exactly once, at the grace deadline, vs LIVE table positions, for EVERYONE inside the circle, into the shared playerDamage map (FSM-02, D4-02, D4-03)"
    - "Victims are knocked back 3u away from the landing center and stunned 2 ticks, written server-side (D4-10, D4-11: no safety rails)"
  artifacts:
    - "spacetimedb/src/unitAttacks.ts"
    - "spacetimedb/src/index.ts (unit_attack + attack_strike table defs, player.stunnedUntilMicros, updatePosition stun guard)"
  key_links:
    - "unitAttacks.ts calls the Plan-01 pure helpers (selectAttack, enterWindup, dueAttackTransitions, resolveCircleHit, knockbackDisplacement) — zero new decision logic in the glue"
    - "strike damage lands in the SAME playerDamage map the existing apply loop consumes (resist 'contact' -> death -> shard spill -> respawn reused verbatim)"
    - "attack_strike insert is emitted unconditionally at the strike transition, before any victim branching (Phase-2 guarded-emission lesson)"
---

<objective>
Land the server half of the FSM spine: the two additive tables (`unit_attack`, `attack_strike`),
the `player.stunnedUntilMicros` column + `updatePosition` stun guard (HIT-01 server side), and the
`runUnitAttacks` glue module that runs windup → strike → recovery per live goliath inside
`worldTick` — landing locked at cast (ATK-01), rooted windup (D4-12), unconditional strike event,
zero-storage dodge grace (D4-02), AOE hits everyone in the circle (D4-03), knockback + stun
(D4-10/D4-11), recovery then 3.5s cooldown (D4-06/D4-07). NOT yet wired into worldTick — the call
site + drain deletion are Plan 03 so this plan compiles and tests green without changing runtime
behavior.

Purpose: schema + glue reviewed and type-checked as one unit before the single-line wiring flips
behavior in Plan 03.
Output: index.ts gains only table defs, one column, one guard; all logic in the new
spacetimedb/src/unitAttacks.ts sibling (~120 LOC, no-monolith rule).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-CONTEXT.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md
@spacetimedb/src/attacks.ts
@spacetimedb/src/unitAttackFsm.ts
@spacetimedb/src/attackHitbox.ts
</context>

## Artifacts this phase produces (this plan's share)

- **Table `unit_attack`** (public) — columns: `id` (u64 pk autoInc), `unitKind` (u32), `unitId` (u64), `state` (u32), `attackId` (string), `startedAtMicros` (u64), `strikeAtMicros` (u64), `recoveryEndsAtMicros` (u64), `cooldownUntilMicros` (u64), `landingX` (f32), `landingZ` (f32), `radius` (f32), `castX` (f32), `castZ` (f32), `strikeResolved` (bool), `poise` (u32); index `by_unit` btree on `['unitKind', 'unitId']`
- **Table `attack_strike`** (public, event) — columns: `unitKind` (u32), `unitId` (u64), `attackId` (string), `landingX` (f32), `landingZ` (f32), `radius` (f32)
- **Column `player.stunnedUntilMicros`** — `t.u64().default(0n)`, appended last
- **Guard** in `updatePosition` reducer: early return while `ctx.timestamp.microsSinceUnixEpoch < currentPlayer.stunnedUntilMicros`
- **Module `spacetimedb/src/unitAttacks.ts`** exporting: `runUnitAttacks(ctx, now, tick, goliaths, goliathPosition, goliathHeading, playerByHex, playerDamage): void`

<tasks>

<task type="auto">
  <name>Task 1: index.ts additive schema — unit_attack + attack_strike tables, stunnedUntilMicros column, updatePosition stun guard</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts lines 380-480 (goliath table + characterActivation table — the multi-column btree index idiom and the appended-.default() comment style to copy; goliath table shows the additive-append discipline)
    - spacetimedb/src/index.ts lines 330-345 (player table tail — skillReadyAtMicros/skillWindowEndsAtMicros are the appended-.default(0n) precedent stunnedUntilMicros sits after)
    - spacetimedb/src/index.ts lines 565-585 (enemy_hit event table — the exact { public: true, event: true } analog + comment style for attack_strike)
    - spacetimedb/src/index.ts lines 690-710 (the schema({...}) export the new tables must be added to)
    - spacetimedb/src/index.ts lines 1060-1095 (updatePosition reducer — the puppet early-return at ~1069-1071 is the exact shape the stun guard copies, placed immediately after it)
    - spacetimedb/src/index.ts lines 790-830, 2200-2215, 2820-2850 (the THREE ctx.db.player.insert call sites at ~797, ~2209 restorePlayers, ~2830 — every one must supply the new field: the insert-type tax, Phase-2 trap)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md (section "spacetimedb/src/index.ts (modified...)" — verified seams 1-4 with excerpts)
  </read_first>
  <action>
    Four additive edits to index.ts, nothing else (no-monolith rule: index.ts gains only defs +
    guard here; the runUnitAttacks call is Plan 03):

    1. `unit_attack` table (FSM-01/FSM-06): copy the characterActivation multi-column-index idiom —
       `table({ name: 'unit_attack', public: true, indexes: [{ accessor: 'by_unit', algorithm: 'btree', columns: ['unitKind', 'unitId'] }] }, { ...columns... })`
       with exactly the columns listed in "Artifacts this plan's share" above. unitKind uses
       UNIT_KIND_GOLIATH = 0 semantics (1 = camp enemy, 2 = hero later — zero schema change for
       reuse, the FSM-01 unit-agnostic mandate). New table: no .default() needed on its columns.
       Add a header comment explaining the row-optional design: rows are lazily upserted per unit;
       the table starts EMPTY on a migrated DB and is never the iteration driver.
    2. `attack_strike` event table (ANIM-04 producer): copy the enemy_hit shape —
       `{ name: 'attack_strike', public: true, event: true }` with columns unitKind, unitId,
       attackId, landingX, landingZ, radius. Comment: event-only, ONE insert per strike (not per
       victim — victims learn via their own player row).
    3. `player.stunnedUntilMicros: t.u64().default(0n)` appended LAST after
       skillWindowEndsAtMicros, with the additive-migrate rationale comment (copy the
       skillReadyAtMicros comment style). Then pay the insert-type tax: supply
       `stunnedUntilMicros: 0n` at ALL THREE ctx.db.player.insert call sites (~797, ~2209
       restorePlayers, ~2830) — grep for every `player.insert` before finishing (per HIT-01, D4-10).
    4. `updatePosition` stun guard (HIT-01 enforcement): immediately after the puppet early-return,
       add: if ctx.timestamp.microsSinceUnixEpoch < currentPlayer.stunnedUntilMicros, return —
       the server owns the victim's position during a stun; a modified client cannot wiggle out
       (threat T-04-03).

    Register both new tables in the schema({...}) export. Do not reorder any existing column or
    table (additive-only migrate discipline).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p spacetimedb && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit -p spacetimedb` exits 0 (module compiles — insert-type tax fully paid)
    - `grep -c "name: 'unit_attack'" spacetimedb/src/index.ts` returns 1 and the same table object contains `by_unit`
    - `grep -c "name: 'attack_strike'" spacetimedb/src/index.ts` returns 1 and that table options object contains `event: true`
    - `grep -c "stunnedUntilMicros" spacetimedb/src/index.ts` returns >= 5 (column def + 3 insert sites + updatePosition guard)
    - stunnedUntilMicros column definition includes `.default(0n)` and appears AFTER skillWindowEndsAtMicros in the player table source order
    - `pnpm test` green (no behavior change yet)
  </acceptance_criteria>
  <done>Schema is additive-complete; stun is server-enforced at the reducer boundary; module type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: unitAttacks.ts — runUnitAttacks glue: lazy upsert, selection, windup/strike/recovery, grace resolution, knockback + stun</name>
  <files>spacetimedb/src/unitAttacks.ts</files>
  <read_first>
    - spacetimedb/src/index.ts lines 2938-3000 (worldTick top: now/tick sampling, goliaths/players/playerByHex map building — the exact arguments runUnitAttacks receives)
    - spacetimedb/src/index.ts lines 3155-3200 (pass 4/4b: the playerDamage map accumulation pattern + the isInsideSafeZone guard to copy into strike resolution; pass 4b at ~3180-3190 is what Plan 03 deletes — its per-victim guards are the model)
    - spacetimedb/src/index.ts lines 3290-3340 (goliath apply loop — persists goliathPosition/goliathHeading map entries; why root/leap are pure map overrides)
    - spacetimedb/src/index.ts line 963 (isInsideSafeZone) and line 838 (clampToWorld) — helpers strike resolution reuses; both must be exported from index.ts or the logic passed in (prefer: export them from index.ts, import in unitAttacks.ts)
    - spacetimedb/src/attacks.ts, spacetimedb/src/unitAttackFsm.ts, spacetimedb/src/attackHitbox.ts (the Plan-01 pure API this glue composes — no decision logic may be re-implemented here)
    - spacetimedb/src/index.ts lines 419-441 (goliath columns: goliathId, sizeIndex, contactDamage, aggroPlayer, aggroExpiresAtMicros, alive, headingX/Z — everything selection/windup reads)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md (sections "Architecture Patterns" 1-5 — lazy upsert, deadline semantics, zero-storage grace option (b), position-map override, knockback/stun three-part design)
  </read_first>
  <action>
    Create spacetimedb/src/unitAttacks.ts (~120 LOC) exporting a single function
    `runUnitAttacks(ctx, now, tick, goliaths, goliathPosition, goliathHeading, playerByHex, playerDamage)`.
    It imports the pure helpers from './attacks', './unitAttackFsm', './attackHitbox', plus
    isInsideSafeZone/clampToWorld/aggroExpired-equivalent reads (export the needed helpers from
    index.ts if not already exported; keep index.ts's export surface minimal). Per live goliath:

    1. LAZY UPSERT (FSM-06): `[...ctx.db.unitAttack.by_unit.filter([UNIT_KIND_GOLIATH, goliathRow.goliathId])][0]`
       — insert an idle row (id: 0n, zeros) when missing. Iterate GOLIATHS as the driver, never the
       attack table. Dead-row hygiene: on each tick, any unit_attack row whose goliath is dead or
       missing is reset to ATTACK_STATE_IDLE (cheap; prevents posthumous strikes and hung
       telegraphs — RESEARCH Pitfall 6).
    2. IDLE: only when the goliath has a live aggro (aggroPlayer set, aggro not expired) and that
       target is online and NOT inside the safe zone — compute distance from the goliath's CURRENT
       map position to the target's live row position, call selectAttack(distance, now,
       row.cooldownUntilMicros, UNIT_ATTACKS[UNIT_KIND_GOLIATH].default). Non-null result → apply
       enterWindup(now, tick, ATTACKS[attackId], goliathRow.sizeIndex, castX, castZ, targetX,
       targetZ) as a row update (landing LOCKED at this single sample — ATK-01; poise reset to 0 —
       the Phase-7 seam; castX/Z = the goliath's current map position). Null → normal chase
       continues (D4-13: chase AI untouched).
    3. WINDUP (D4-12): override goliathPosition entry back to (castX, castZ) — rooted crouch —
       and goliathHeading toward the landing so the crouch reads aimed. Walk
       dueAttackTransitions(state, now, strikeAt, graceAt, recoveryEndsAt) applying each due
       transition in order (a coalesced tick applies strike AND resolution in one pass — FSM-05).
    4. STRIKE transition (first tick with now >= strikeAtMicros): set goliathPosition entry to
       (landingX, landingZ) — the leap (move: 'leap') — set state = ATTACK_STATE_STRIKE, and insert
       the attack_strike event UNCONDITIONALLY at the transition, BEFORE any victim logic
       (Phase-2 guarded-emission lesson).
    5. GRACE RESOLUTION (first tick with now >= graceDeadline, strikeResolved false) — the D4-02
       zero-storage pick: for EVERY online player (D4-03: bystanders included) not inside the safe
       zone, resolveCircleHit(live positionX/Z, landingX, landingZ, radius) → hit victims get
       (a) damage: playerDamage.set(hex, existing + goliathRow.contactDamage * spec.damageMultiplier)
       — RAW, pre-resist; the existing apply loop applies the 'contact' resistance channel, death,
       shard spill, respawn for free; NEVER route through resolvePlayerHit/MAX_HIT_DAMAGE which
       would clamp 405/585/765 to 400 (RESEARCH Pitfall 8);
       (b) knockback: knockbackDisplacement(victimX, victimZ, landingX, landingZ, spec.knockback,
       goliath heading fallback) → write clampToWorld-ed X/Z onto the victim player row (no
       walkability check — D4-11 edge deaths are a feature);
       (c) stun: stunnedUntilMicros = stunDeadline(now, spec.stunTicks, tick) on the victim row.
       Set strikeResolved = true, state = ATTACK_STATE_RECOVERY. Resolution happens exactly ONCE.
    6. RECOVERY: when now >= recoveryEndsAtMicros → state = ATTACK_STATE_IDLE,
       cooldownUntilMicros = now + spec.cooldownMicros (D4-06 recovery 8 ticks; D4-07 cooldown
       3.5s — full cycle ~6s).

    All timing values written to the row come from the Plan-01 pure helpers; this module contains
    ZERO arithmetic of its own beyond map reads/writes. now/tick arrive as arguments — this module
    never samples the clock (FSM-05: sampled once per tick by worldTick).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p spacetimedb && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --noEmit -p spacetimedb` exits 0
    - `grep -c "export function runUnitAttacks" spacetimedb/src/unitAttacks.ts` returns 1
    - `grep -c "by_unit.filter" spacetimedb/src/unitAttacks.ts` returns >= 1 (lazy upsert via index — FSM-06)
    - `grep -c "unitAttack.iter()" spacetimedb/src/unitAttacks.ts` returns 0 as the driver loop — source assertion: the per-goliath loop iterates the goliaths argument (dead-row hygiene may scan the attack table for orphans, but attack selection/transitions are driven from the unit list)
    - `grep -c "resolvePlayerHit\|MAX_HIT_DAMAGE" spacetimedb/src/unitAttacks.ts` returns 0 (slam damage rides the contact-channel apply loop, not the player-hit path)
    - `grep -c "attackStrike.insert" spacetimedb/src/unitAttacks.ts` returns 1 (one unconditional emission point)
    - Functional LOC of unitAttacks.ts <= 300 (no-monolith rule); `pnpm test` green
  </acceptance_criteria>
  <done>The complete FSM lifecycle exists as reducer-side glue over tested pure helpers, compiled and reviewable, with zero runtime effect until Plan 03 wires the call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> updatePosition | untrusted client positions cross here; this plan NARROWS it (stun rejection) |
| worldTick (module identity) -> player rows | server-authoritative damage/knockback/stun writes — no client input in the path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | modified client ignores stun and keeps moving | high | mitigate | updatePosition early-returns while now < stunnedUntilMicros (Task 1.4) — stun enforced server-side; acceptance criterion greps the guard |
| T-04-04 | Tampering | client teleports out of the circle at the last frame | medium | mitigate | existing MAX_STEP_DISTANCE=12 / 0.1s clamp bounds escape speed; grace resolution reads SERVER row positions only (Task 2.5) |
| T-04-05 | Tampering | client spoofs strike damage or landing | high | mitigate | impossible by construction: FSM runs only inside worldTick from ATTACKS data; zero new client-callable reducers this plan (attack surface unchanged) |
| T-04-06 | DoS | poise perma-stun spoof seeded here (Phase-7 concern) | medium | mitigate | poise column is written only by worldTick (reset on windup entry); no client input path exists to it |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new dependencies (locked); no install tasks in this plan |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit -p spacetimedb` exits 0
- `pnpm test` full suite green
- `grep -rn "Math.random\|Date.now" spacetimedb/src` empty
- No module-scope mutable Map/state in unitAttacks.ts (RESEARCH Pitfall 2 — all cross-tick state on the row)
</verification>

<success_criteria>
- FSM-01: unit-agnostic unit_attack table + full windup/strike/recovery lifecycle in worldTick-callable glue
- FSM-02/D4-02/D4-03: single grace-deadline resolution vs live positions, everyone in the circle
- FSM-06: lazy upsert driven from the goliath table; attack table never the driver
- ATK-01: landing locked at the windup-entry sample, immutable through the lifecycle
- HIT-01 (server half): knockback displacement + enforced stun window on the victim row
</success_criteria>

<output>
Create `.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-02-SUMMARY.md` when done
</output>
