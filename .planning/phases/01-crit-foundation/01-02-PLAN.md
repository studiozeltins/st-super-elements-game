---
phase: 01-crit-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/data/characters.ts
  - spacetimedb/src/index.ts
  - src/game/data/__tests__/characters.test.ts
autonomous: true
requirements: [CRIT-01, CRIT-03]
must_haves:
  truths:
    - "All 17 characters carry critRate and critDmg on CharacterDefinition (client) and CHARACTER_STATS (server) with identical values per id"
    - "D-01: critRate/critDmg are role-seeded and distinct across all 17 characters, role-coherent (dps high, tank low, healer/support mid), coherent with the existing Role system"
    - "D-02: concrete per-character crit numbers are a role-seeded first pass the executor sets, kept legibly different so crit reads differently between characters (user tunes in playtest)"
    - "critDmg is stored as a full multiplier (e.g. 1.9) matching the value it will replace"
    - "Each CHARACTER_STATS entry stays a single-line flat literal (no nested braces) so the serverSync extractor keeps parsing"
  artifacts:
    - src/game/data/characters.ts
    - spacetimedb/src/index.ts
    - src/game/data/__tests__/characters.test.ts
  key_links:
    - "client CHARACTERS[id].critRate/critDmg === server CHARACTER_STATS[id].critRate/critDmg (INV-5; asserted in plan 03)"
---

<objective>
Add distinct, role-coherent `critRate`/`critDmg` to every one of the 17 characters — on the client
`CharacterDefinition` (authoring source of truth) and mirrored EXACTLY into the server
`CHARACTER_STATS` (INV-5) — and extend `characters.test.ts` to lock distinctness + role-coherent
ranges. This is CRIT-01 (the stat exists, distinct per character) and the data half of CRIT-03 (the
server mirror). No client roll is deleted and no reducer is touched — that is Phase 2.

Purpose: these are the per-character stats Phase 2's `ctx.random` roll consumes; landing them now,
with parity-ready flat literals, is the data foundation of the crit trilogy.
Output: modified characters.ts + index.ts CHARACTER_STATS + extended characters.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crit-foundation/01-CONTEXT.md
@.planning/phases/01-crit-foundation/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `critRate: number` + `critDmg: number` fields on `CharacterDefinition` (client) and `CharacterStat`
  (server) interfaces, populated on all 17 entries.
- Extended `characters.test.ts` with per-character crit distinctness + role-range assertions.
Sibling plans: 01-01 (pure helpers), 01-03 (parity tests).
</artifacts_this_phase_produces>

<crit_values>
## Crit Value Table (first-pass, D-01/D-02 — executor uses these EXACT numbers in BOTH files)

`critDmg` is a full multiplier. `critRate` is a fraction in [0,1]. Every `critRate` below is distinct
(17 unique values). The user tunes these in playtest later (D-02) — they only need to be distinct and
role-coherent now. The SAME number must appear in `characters.ts` and `CHARACTER_STATS` for each id.

| id | role | critRate | critDmg |
|----|------|----------|---------|
| aeris | support | 0.20 | 1.60 |
| terron | tank | 0.12 | 1.50 |
| volta | dps | 0.34 | 1.90 |
| silva | dps | 0.33 | 2.00 |
| marina | healer | 0.18 | 1.60 |
| ignis | tank | 0.13 | 1.55 |
| sarma | dps | 0.32 | 1.95 |
| nereida | healer | 0.19 | 1.65 |
| vesper | dps | 0.36 | 2.10 |
| glacia | tank | 0.10 | 1.45 |
| zefs | support | 0.22 | 1.70 |
| petra | tank | 0.14 | 1.50 |
| zibo | dps | 0.30 | 1.85 |
| lapa | healer | 0.17 | 1.55 |
| rasa | healer | 0.16 | 1.50 |
| dzirkste | dps | 0.31 | 1.90 |
| stindzis | dps | 0.35 | 2.05 |

Role-coherent ranges these satisfy (the test in Task 3 asserts these bands):
- dps: critRate in [0.28, 0.40], critDmg in [1.80, 2.30]
- tank: critRate in [0.08, 0.16], critDmg in [1.40, 1.60]
- healer: critRate in [0.15, 0.22], critDmg in [1.45, 1.75]
- support: critRate in [0.15, 0.25], critDmg in [1.45, 1.75]
</crit_values>

<tasks>

<task type="auto">
  <name>Task 1: Add critRate/critDmg to CharacterDefinition + all 17 client entries</name>
  <files>src/game/data/characters.ts</files>
  <read_first>
    - src/game/data/characters.ts:51-69 (CharacterDefinition interface — add fields beside role/healthRegen)
    - src/game/data/characters.ts:93-121 (entry shape: multi-line literals; aeris/terron/volta examples)
    - .planning/phases/01-crit-foundation/01-PATTERNS.md §"src/game/data/characters.ts (MODIFY)"
    - The Crit Value Table in this plan
  </read_first>
  <action>
    In the `CharacterDefinition` interface, add two flat scalar fields with JSDoc mirroring the
    existing style: `/** Base critical-hit chance [0,1] (server-rolled in Phase 2). */ critRate:
    number;` and `/** Critical-hit damage as a FULL multiplier (e.g. 1.9). */ critDmg: number;` —
    place them next to `role`. Then add `critRate` and `critDmg` to all 17 character entries using the
    EXACT values from the Crit Value Table (client entries are multi-line, so put the two fields on
    their own line after `role`). Do not reorder or change any existing field. Do NOT touch the client
    crit roll in createGame.ts (that deletion is Phase 2 — no runtime change this phase). This lands
    CRIT-01 (distinct per-character crit stats) on the authoring source of truth.
  </action>
  <verify>
    <automated>pnpm test -- characters</automated>
  </verify>
  <acceptance_criteria>
    - `CharacterDefinition` has `critRate: number` and `critDmg: number`.
    - All 17 entries compile with the exact table values; existing characters.test.ts still green (roster length 17 unchanged).
    - No change to createGame.ts, weapons.ts, or any reducer.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mirror critRate/critDmg into server CHARACTER_STATS (all 17, single-line flat)</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts:33-41 (CharacterStat interface — add the two number fields)
    - spacetimedb/src/index.ts:43-65 (the 17 single-line CHARACTER_STATS entries, incl. ...NO_HEAL spread and inline healer entries)
    - .planning/phases/01-crit-foundation/01-PATTERNS.md §"spacetimedb/src/index.ts CHARACTER_STATS (MODIFY)"
    - The Crit Value Table in this plan (values MUST equal Task 1's exactly)
  </read_first>
  <action>
    Add `critRate: number;` and `critDmg: number;` to the `CharacterStat` interface. Then add
    `critRate: <n>, critDmg: <n>` to each of the 17 entries on the SAME line (after `role`, before the
    `...NO_HEAL` spread or the inline heal fields), using values IDENTICAL to Task 1's table. CRITICAL:
    keep every entry a single-line flat literal with NO nested braces — the serverSync extractor
    (`(\w+):\s*\{([^}]*)\}`) breaks on a nested object. This is the ONLY change index.ts receives this
    phase (no-monolith: WEAPONS/multipliers live in damage.ts from plan 01, never here). This lands the
    data half of CRIT-03 (server mirror); the parity assertion is plan 03.
  </action>
  <verify>
    <automated>pnpm test -- serverSync</automated>
  </verify>
  <acceptance_criteria>
    - `CharacterStat` has `critRate`/`critDmg`; all 17 entries carry them on one line.
    - Existing serverSync parity tests (stars/maxHealth/role/heal) still pass — the single-line extractor still parses every entry.
    - Every server critRate/critDmg equals the client value for the same id (visual diff against the table).
    - index.ts gained ONLY the crit fields (no WEAPONS, no multiplier functions).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend characters.test.ts with crit distinctness + role-coherent ranges (CRIT-01)</name>
  <files>src/game/data/__tests__/characters.test.ts</files>
  <read_first>
    - src/game/data/__tests__/characters.test.ts:1-42 (existing roster-integrity it.each + the "17 total" assertion — reuse CHARACTER_LIST, roster length is 17)
    - src/game/data/characters.ts (ROLE_META keys / Role type for the range map)
    - The Crit Value Table (role-coherent ranges block)
  </read_first>
  <action>
    Add a `describe('per-character crit stats')` block. Assert: (1) every character's `critRate` is a
    number in (0,1) and `critDmg >= 1`; (2) all 17 `critRate` values are DISTINCT
    (`new Set(critRates).size === 17`, CRIT-01); (3) role-coherent ranges — build a per-role
    `{ critRate: [min,max], critDmg: [min,max] }` map matching the ranges in the Crit Value Table and,
    for each character, assert its critRate/critDmg fall in its role's band. Use `it.each` over
    `CHARACTER_LIST` for the per-character asserts and roster length 17 (NOT 18). Keep the test DRY/SRP
    — one describe, guard-clause style, no re-declaring the roster.
  </action>
  <verify>
    <automated>pnpm test -- characters</automated>
  </verify>
  <acceptance_criteria>
    - New crit describe block passes: distinctness (17 unique critRate) + role-range assertions all green.
    - Uses roster length 17; does not duplicate or re-hardcode the roster.
    - `pnpm test` full suite green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan adds static data fields to two in-code registries and a test; no
reducer, network input, or table is added or exposed. The client crit roll is UNCHANGED (removal is
Phase 2), so there is no runtime behavior change and no new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Silent client/server crit-stat drift (INV-5) | medium | mitigate | critRate/critDmg mirrored from one canonical value table; parity is CI-enforced by plan 03's serverSync crit-parity block. Drift fails the build, not runtime. |
| T-01-01 | Tampering | Client-authored crit (existing roll, UNCHANGED) | high | transfer | The per-character stats added here are what Phase 2's server-side `ctx.random` roll consumes to close the client-crit-spoof. No new exposure in Phase 1. |
| T-01-SC | Tampering | npm/pnpm package installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `pnpm test -- characters` and `pnpm test -- serverSync` green.
- `pnpm test` full suite green.
- Server and client crit values match the canonical table for all 17 ids.
- No nested braces introduced in any CHARACTER_STATS entry.
- createGame.ts / reducers unchanged (no runtime behavior change — ROADMAP SC4).
</verification>

<success_criteria>
- ROADMAP SC1: every character has its own distinct, role-coherent critRate/critDmg on CharacterDefinition.
- ROADMAP SC2 (data half): crit values mirrored into CHARACTER_STATS (parity asserted by plan 03).
- ROADMAP SC4: no runtime behavior change; grep discipline held (no Math.random/Date.now added).
</success_criteria>

<output>
Create `.planning/phases/01-crit-foundation/01-02-SUMMARY.md` when done.
</output>
