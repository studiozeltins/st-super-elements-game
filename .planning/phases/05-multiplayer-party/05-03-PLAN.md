---
phase: 05-multiplayer-party
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/module_bindings/
autonomous: true
requirements: [REQ-multiplayer-party]
must_haves:
  truths:
    - "The party module is published to the LOCAL SpacetimeDB via an additive migrate (no data wipe); real accounts/players survive."
    - "Regenerated client bindings expose tables.party / tables.partyMember / tables.partyInvite and reducers.invitePlayer / requestJoin / acceptInvite / declineInvite / leaveParty."
  artifacts:
    - "regenerated src/module_bindings/ with the three new party tables + five reducers typed"
  key_links:
    - "This is a BLOCKING gate: the client plans (04, 05) cannot type-check until bindings are regenerated here."
    - "Publish MUST NOT use -c / --delete-data (account/account_link are not in the backup set)."
---

<objective>
Deploy the additive party schema to the local SpacetimeDB and regenerate the client bindings so
the subsequent client plans can reference the new tables and reducers. This is the blocking gate
between the server module (Plan 02) and all client work (Plans 04-05).

Purpose: new tables/reducers require a migrate-publish followed by binding regeneration, or the
client hits "no such reducer" / missing table types. maincloud is deferred to Phase 7 per the
project deploy policy.
Output: local DB carries the party schema; `src/module_bindings/` is regenerated and type-clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-multiplayer-party/05-RESEARCH.md
@.planning/transcendence/PROGRESS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive migrate-publish to local + regenerate bindings</name>
  <files>src/module_bindings/</files>
  <read_first>
    - CLAUDE.md "This Project: Environments & Deployment" — module path is ./spacetimedb; the npm spacetime:publish scripts point at the wrong ./server path (ignore them).
    - 05-RESEARCH.md "Pitfall 2" and "Pitfall 4" — never --delete-data; publish → generate → build order.
  </read_first>
  <action>
    Confirm the local SpacetimeDB is running (`spacetime server ping local`; if down, start
    `spacetimedb-standalone`). Publish the module ADDITIVELY (migrate, NO data wipe) to local:
    `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes`.
    Do NOT pass `-c` or `--delete-data` — account/account_link are not in the backup set and a wipe
    destroys all logins (CLAUDE.md warning). Do NOT use the `spacetime:publish` npm script (wrong
    module path). Then regenerate bindings: `pnpm run spacetime:generate` (writes src/module_bindings/).
    If publish returns 403/ownership error, surface it as a blocking auth gate (do not force a wipe).
    maincloud publish is DEFERRED to Phase 7 — do not publish to maincloud here.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false && grep -rl "invitePlayer" src/module_bindings/ && grep -rl "party_invite\|partyInvite" src/module_bindings/</automated>
  </verify>
  <done>Local publish succeeds without a wipe; bindings regenerated exposing party/partyMember/partyInvite tables + invitePlayer/requestJoin/acceptInvite/declineInvite/leaveParty; project type-checks against the new bindings.</done>
  <acceptance_criteria>
    - Publish command used no --delete-data / -c flag.
    - src/module_bindings/ references the three new tables and five reducers.
    - `pnpm exec tsc -b` is clean with the regenerated bindings.
    - maincloud was not touched.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live DB (publish) | A wrong flag (`--delete-data`) at this boundary irreversibly destroys real account logins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | publish command / data integrity | high | mitigate | Additive migrate only; explicitly forbid `-c`/`--delete-data`; verify no wipe flag in the command. account/account_link preserved. |
| T-05-07 | Repudiation | stale bindings | medium | mitigate | Regenerate bindings immediately after publish so client and server schemas cannot silently drift ("no such reducer"). |
</threat_model>

<verification>
- `pnpm exec tsc -b` clean against regenerated bindings.
- Bindings grep confirms new tables + reducers present.
- No `--delete-data` in the executed publish command (self-check).
</verification>

<success_criteria>
- Local party schema live (additive, no wipe).
- Bindings regenerated and type-clean — client plans unblocked.
</success_criteria>

<artifacts_produced>
- Regenerated `src/module_bindings/` with party tables + reducers.
- Local `2d-impact-game-fr9ti` carrying the party schema.
</artifacts_produced>

<output>
Create `.planning/phases/05-multiplayer-party/05-03-SUMMARY.md` when done.
</output>
