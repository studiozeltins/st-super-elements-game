---
phase: 05-multiplayer-party
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - src/App.tsx
  - src/ui/PlayerSheet.tsx
  - src/ui/Hud.tsx
  - src/index.css
autonomous: true
requirements: [REQ-multiplayer-party]
must_haves:
  truths:
    - "The client subscribes to party + party_member, and to party_invite filtered to recipientIdentity == my canonical identity (Information-Disclosure mitigation)."
    - "Tapping an online player (from an online-players list) opens the slide-out .player-sheet with the target's name, active character, role badge, and online dot (D-09)."
    - "The sheet's symmetric actions call invitePlayer / requestJoin, and Pamest pulku calls leaveParty when the viewer is partied (D-02)."
    - "A .hud__party-chip entry point shows PULKS or PULKS {n}/4 and opens the party surface."
  artifacts:
    - src/ui/PlayerSheet.tsx
    - ".player-sheet CSS classes + reduced-motion block in src/index.css"
    - ".hud__party-chip in src/ui/Hud.tsx"
  key_links:
    - "Roster/target identity comparisons use myIdentityHex (canonical, App.tsx:129), NOT the raw device identity."
    - "party_invite subscription is filtered on the canonical Identity via the accountLink .where(...) precedent (App.tsx:89)."
    - "PlayerSheet reuses Modal (Radix Dialog) focus-trap/ESC/ARIA — no hand-rolled focus management."
---

<objective>
Wire the client to the party backend and deliver the "send" half of the flow: subscribe the new
tables, expose the five reducer callers, build the online-players tap surface, and the slide-out
`.player-sheet` with the symmetric invite / ask-to-join / leave actions, plus the HUD entry chip.

Purpose: the online-players list is the guaranteed, conflict-free tap-target (avatar raycast
collides with left-click-to-attack — deferred). This plan makes a player able to open another
player's sheet and send an invite or join-request end-to-end.
Output: `.player-sheet` opens from the list, actions fire the correct reducers, HUD chip present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multiplayer-party/05-CONTEXT.md
@.planning/phases/05-multiplayer-party/05-UI-SPEC.md
@.planning/phases/05-multiplayer-party/05-PATTERNS.md

# Edit targets + reuse analogs:
@src/App.tsx
@src/ui/CharacterSheet.tsx
@src/ui/Modal.tsx
@src/ui/Hud.tsx
@src/game/data/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subscribe party tables + wire reducer callers + canonical identity (App.tsx)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:85-104 (subscriptionBuilder .subscribe([...]) block; :89 accountLink .where filter idiom).
    - src/App.tsx:114 (useTable(tables.player)); :123-129 (accountLink → myIdentityHex canonical resolution).
    - src/App.tsx:250-374 (reducer-call handler idiom, e.g. :257 setParty, attackPlayer with targetIdentity).
    - 05-PATTERNS.md "src/App.tsx" section.
  </read_first>
  <action>
    Extend the existing `.subscribe([...])` array (App.tsx:85-104) with `tables.party`,
    `tables.partyMember`, and `tables.partyInvite.where(r => r.recipientIdentity.eq(<canonical Identity>))`
    — filtered so a player only receives invites addressed to them (Info-Disclosure mitigation),
    mirroring the accountLink `.where(row => row.identity.eq(identity))` idiom at :89. Source the
    canonical Identity object from `myLink.canonicalIdentity` (the same object hexed at :129); gate the
    subscription so the filtered invite subscription is (re)built once the canonical identity resolves.
    Add reactive reads: `const [parties] = useTable(tables.party);`
    `const [partyMembers] = useTable(tables.partyMember);` `const [myInvites] = useTable(tables.partyInvite);`.
    Add reducer-caller handlers alongside the existing ones (~:250-374) that call:
    `connection?.reducers.invitePlayer({ targetIdentity })`,
    `connection?.reducers.requestJoin({ targetIdentity })`,
    `connection?.reducers.acceptInvite({ inviteId })`,
    `connection?.reducers.declineInvite({ inviteId })`,
    `connection?.reducers.leaveParty()`.
    Derive party state for the UI: `myPartyId` = the partyId of the partyMembers row whose identity
    hex === myIdentityHex; `myRoster` = partyMembers filtered to myPartyId; a `n/4` count from
    myRoster.length against RAID_PARTY_SIZE (import from src/game/data/constants). Hold UI state for
    the open PlayerSheet target (an online player row) — lifted here so the sheet + HUD chip can read
    it. All identity comparisons use `.toHexString()` against myIdentityHex (canonical), never the raw
    device identity (App.tsx:34 deviceIdentityHex).
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>App.tsx subscribes party/partyMember + filtered partyInvite; exposes the 5 reducer callers; derives myPartyId/myRoster from canonical identity; type-checks.</done>
  <acceptance_criteria>
    - party_invite subscription is filtered on canonical recipientIdentity (not unfiltered).
    - All five reducer callers are wired via connection.reducers.
    - Identity comparisons use myIdentityHex (canonical), confirmed by grep of the new derivations.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PlayerSheet slide-out component + online-players tap surface</name>
  <files>src/ui/PlayerSheet.tsx, src/App.tsx</files>
  <read_first>
    - src/ui/CharacterSheet.tsx:73-105 (overlay+card+close+role-badge shell); :93-101 (ROLE_META badge render).
    - src/ui/Modal.tsx:16-35 (Radix Dialog focus-trap/ESC/ARIA wrapper to reuse).
    - 05-UI-SPEC.md "Component Contracts §1" (.player-sheet structure, actions visibility, touch/focus) and "Copywriting Contract".
  </read_first>
  <action>
    Create src/ui/PlayerSheet.tsx — a right-docked slide-out `.player-sheet` for a tapped target
    player. Reuse Modal (Radix Dialog) semantics for focus-trap/ESC/ARIA (do NOT hand-roll focus).
    Props: the target player row (name, activeCharacterId, online), whether the viewer shares a party
    with the target, whether the viewer's party is full, and callbacks onInvite/onRequestJoin/onLeave/onClose.
    Header: player name (18px/700), active character name + element color, role badge via
    `ROLE_META[CHARACTERS[activeCharacterId].role]` (reuse the CharacterSheet:93-101 pattern verbatim —
    no new server field), online dot (● accent / ○ muted with aria-label 'Tiešsaistē'/'Nesaistē').
    Action stack (gap 16px), Latvian copy per UI-SPEC: primary `Aicināt manā pulkā` (Button
    variant="primary") → onInvite; secondary `Lūgt pievienoties pulkam` (Button variant="ghost") →
    onRequestJoin; when the viewer and target share a party, show ONLY `Pamest pulku` (Button
    variant="danger") → onLeave (behind a Modal confirm per UI-SPEC "Destructive action"). Disable the
    invite action with helper text `Pulks ir pilns (4/4)` when the viewer's party is full. Each action
    ≥44px tall; first action auto-focused.
    In App.tsx, render an online-players list surface (the primary tap-target — RESEARCH A1): derive
    from `players` (useTable) filtered to `online === true && identity.toHexString() !== myIdentityHex`;
    each entry, when tapped, sets the PlayerSheet target state. Mount <PlayerSheet> when a target is set.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --pretty false</automated>
  </verify>
  <done>PlayerSheet renders target name/character/role/online, exposes the symmetric actions with correct visibility + full-party disable, reuses Modal a11y; the online-players list opens it; type-checks.</done>
  <acceptance_criteria>
    - Sheet reuses Modal (Radix) for focus-trap/ESC — no bespoke focus code.
    - Role badge rendered via ROLE_META (no new server field).
    - Actions follow D-02 visibility (invite+ask when unrelated; only Leave when shared party); invite disabled at 4/4.
    - Online-players list excludes self via canonical hex comparison.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: HUD party chip + .player-sheet CSS (with reduced-motion)</name>
  <files>src/ui/Hud.tsx, src/index.css</files>
  <read_first>
    - src/ui/Hud.tsx:160-161 (.hud__meta-row / .hud__zone box language).
    - 05-UI-SPEC.md "Component Contracts §5" (.hud__party-chip) and "Animation & Motion Contract" (reduced-motion block).
    - src/index.css (search `.shard-toast`, `.modal__panel` for radius/keyframe precedents).
  </read_first>
  <action>
    In src/ui/Hud.tsx, add a `.hud__party-chip` inside `.hud__meta-row` (:160), reusing the
    `.hud__zone` box language (panel bg, 1px line border, 10px/700 uppercase). Label `PULKS {n}/4`
    when partied else `PULKS`; hover/focus border → var(--accent); ≥44px hit area on pointer:coarse.
    Tapping opens the party surface (roster in Plan 05; for now it toggles the same party UI state).
    Must NOT collide with the existing `⚑` "Komanda" character-team quick-button — this is the player
    party ("Pulks"), a separate left-cluster text chip.
    In src/index.css, add the `.player-sheet` classes per UI-SPEC §1: `.player-sheet__scrim`
    (inset:0; z-index:14; background rgba(4,7,5,0.82); click-to-close) and `.player-sheet__panel`
    (right-docked; width min(360px,92vw); border-radius 12px 0 0 12px; background var(--panel);
    border-left 1px solid var(--line)) with a translateX(100%)→0 enter (transform/opacity only,
    cubic-bezier(0.2,0.9,0.2,1) 0.22s, matching .modal__panel). Add the mandatory reduced-motion block
    from UI-SPEC: under `@media (prefers-reduced-motion: reduce)` the sheet fades only
    (animation:none; transition: opacity 0.12s linear; transform:none). Use only existing :root tokens
    — no new color tokens.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>HUD shows a distinct .hud__party-chip (PULKS) that opens the party UI; .player-sheet CSS renders the right-docked panel with transform/opacity motion and a reduced-motion fade fallback; build is green.</done>
  <acceptance_criteria>
    - `.hud__party-chip` present and does not reuse the ⚑ Komanda button.
    - `.player-sheet__panel` + `__scrim` styled with the 12px radius + var(--panel), no new tokens.
    - A `prefers-reduced-motion` block exists for .player-sheet.
    - `pnpm build` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server subscription → client | Only rows the viewer is authorized to see should arrive; party_invite is filtered to the recipient. |
| client UI → reducer args | The sheet passes targetIdentity/inviteId; these are lookups — the server (Plan 02) resolves the actor from ctx.sender. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | party_invite subscription | medium | mitigate | Subscribe party_invite.where(recipientIdentity == my canonical identity) so a player never receives invites addressed to others. |
| T-05-01 | Spoofing | reducer callers | high | mitigate | Client only ever supplies targetIdentity/inviteId as data; the authenticated actor is enforced server-side (Plan 02). No client-trusted actor path added. |
| T-05-SC | Tampering | package installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `pnpm build` green (tsc -b + vite build).
- `.player-sheet` opens from the online-players list; actions call the correct reducers (visual/manual in Plan 06).
- grep confirms party_invite subscription is `.where(...recipientIdentity...)` filtered.
</verification>

<success_criteria>
- Send-side flow wired end-to-end: list → sheet → invite/requestJoin/leave.
- HUD entry chip + sheet CSS shipped; reduced-motion honored.
- Build green.
</success_criteria>

<artifacts_produced>
- `src/ui/PlayerSheet.tsx` (new slide-out sheet).
- App.tsx: party subscriptions, 5 reducer callers, canonical party-state derivations, online-players list surface.
- `.hud__party-chip` (Hud.tsx); `.player-sheet` CSS + reduced-motion (index.css).
</artifacts_produced>

<output>
Create `.planning/phases/05-multiplayer-party/05-04-SUMMARY.md` when done.
</output>
