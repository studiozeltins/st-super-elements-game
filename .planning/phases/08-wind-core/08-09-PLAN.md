---
phase: 08-wind-core
plan: 09
type: execute
wave: 2
depends_on: ["08-08"]
files_modified:
  - src/game/world/assets/createCampFlag.ts
  - src/game/world/assets/__tests__/windMaterialLifecycle.test.ts
  - src/game/world/assets/__tests__/assets.test.ts
autonomous: true
gap_closure: true
requirements: [WIND-01, WIND-03]
must_haves:
  truths:
    - "Flag cloth visibly swings/streams TOWARD the current wind direction — the yaw is computed in-shader from the baked heading in modelMatrix[0].xz against uWindDir, with ZERO new uniforms or attributes (pooled 'campFlag' material still serves every flag) — closes UAT tests 4/5/9"
    - "Gusts increase the downwind swing (alignment scales with the existing traveling gust envelope × uWindStrength), so the flag reacts to the same passing gust the smoke does — WIND-01 shared clock, WIND-03 flag character"
    - "With uWindStrength 0 (?nowind) or in deep lulls the cloth hangs limp down from the pole (pitch about the pole edge with x foreshortening) with only tiny residual micro-sway — never a rigid horizontal quad — closes UAT test 6, D-12"
    - "Cloth reads chunkier / voxel-stepped (faceted bands, not a smooth sheet) matching the voxel art identity — closes UAT test 8 request, D-09 aesthetic"
    - "Frozen-matrix rule intact: no per-frame CPU mesh rotation, no per-frame allocations; cloth.castShadow stays false; customProgramCacheKey stays 'campFlag' (D-13)"
    - "Full test suite green — windMaterialLifecycle CR-01/CR-02 regression suite and assets invariants unchanged in meaning (updated only where geometry/shader constants legitimately changed)"
  artifacts:
    - "src/game/world/assets/createCampFlag.ts (directional swing + drape + voxel-stepped cloth in the begin_vertex patch and cloth geometry)"
  key_links:
    - "windMath flagSwingGlsl/flagDrapeGlsl (plan 08-08) → interpolated into the cloth begin_vertex patch — displacement direction now derives from uWindDir, not only gust phase"
    - "wind.directionUniform → vertex DISPLACEMENT yaw (previously only the gust retarded-time phase) — the exact coupling smoke already has and the flag lacked (debug session evidence)"
---

<objective>
Close the three flag UAT gaps in one shader + geometry rework of `createCampFlag.ts`:

1. MAJOR (tests 4/5/9): the cloth wiggles on a random baked axis regardless of wind —
   `uWindDir` only phases the gust front (line 86), never directs displacement, and no
   downwind lean/stream term exists. Fix: in-shader yaw of the vertex offsets about the
   pole hinge toward `uWindDir`, using the baked heading already available in
   `modelMatrix[0].xz` — zero new uniforms, the pooled-material constraint holds.
2. MINOR (test 6): rest geometry is the strong-wind stretched pose and all motion is
   multiplied by `uWindStrength`, so `?nowind` shows a rigid horizontal quad. Fix: drape
   term weighted by the plan 08-08 `flagDrape` weight — pitch vertices down about the
   pole edge as a function of along-distance with x foreshortening; limp hang at
   strength 0 with tiny residual micro-sway NOT gated on `uWindStrength`.
3. COSMETIC (test 8): smooth cloth reads too plain for the voxel art identity. Bundled
   here because it is cheap inside the same rework: stepped per-band displacement +
   flat-shaded facets.

Purpose: the flag finally answers the SAME wind every other consumer answers (WIND-01)
with its own faster cloth character (WIND-03/D-08), direction follows the D-05 wander,
gust snap honors D-04, and the `?nowind` bisect honors D-12.
Output: reworked cloth shader patch + chunkier cloth geometry, all tests green, visual
truths routed to UAT re-verify.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-wind-core/08-UAT.md
@.planning/debug/flag-wind-direction-gust.md
@src/game/world/assets/createCampFlag.ts
@src/game/systems/windMath.ts
</context>

## Artifacts this phase produces

New/changed symbols (source-grounding exclusion list):

- `src/game/world/assets/createCampFlag.ts` — begin_vertex patch gains: gust-scaled
  downwind yaw about the pole hinge (heading from `modelMatrix[0].xz`, blend from
  `flagSwingGlsl`), drape pitch + x foreshortening from `flagDrapeGlsl`, drape-gated
  micro-sway; `createClothGeometry` gains voxel-band segmentation; cloth material gains
  `flatShading: true`; a local cloth band-step constant beside the POLE_* constants
  (art constant, not wind math); doc comments updated to describe the new pose model
- `src/game/world/assets/__tests__/windMaterialLifecycle.test.ts` /
  `src/game/world/assets/__tests__/assets.test.ts` — updated ONLY if geometry/shader
  constant changes legitimately break an assertion (child ordering pole→cloth must NOT
  change; the CR-01/CR-02 lifetime semantics must NOT change)

<tasks>

<task type="auto">
  <name>Task 1: Directional swing + windless drape in the cloth begin_vertex patch</name>
  <files>src/game/world/assets/createCampFlag.ts</files>
  <read_first>
    - src/game/world/assets/createCampFlag.ts (FULL — the wind-guarded material cache :30-57 and its CR-01 comment contract, the begin_vertex patch :79-94, createClothGeometry :103-115, the baked random heading :137-139)
    - src/game/systems/windMath.ts (post-08-08 state — FLAG pose constants, flagSwingGlsl/flagDrapeGlsl signatures, gustGlsl :161-168)
    - .planning/debug/flag-wind-direction-gust.md (## Evidence — especially the constraint-surface entry: modelMatrix already read at line 85, modelMatrix[0].xz is the baked world heading, everything needed is already bound; and the grass/smoke reference entries)
  </read_first>
  <action>
    Rework the cloth shader's begin_vertex replacement (keep the existing uniform
    declarations and the gust evaluation at retarded time exactly as-is — the traveling
    front is correct, only the displacement is direction-blind):

    1. **Downwind yaw (fixes tests 4/5/9, D-05/WIND-03):** derive the cloth's baked
       world heading by normalizing the xz of the model matrix's first column (the
       local +x basis — the direction the cloth extends from the pole; the group's
       random rotation.y bake is a pure y-rotation so this is exact). Compute the signed
       angle from that heading to uWindDir via the dot product and the 2D cross term,
       recovered with atan(sinA, cosA). Yaw the vertex's offset-from-the-pole-hinge (its
       local xz, hinge is the x=0 pole line) by that angle scaled by the flagSwingGlsl
       blend of uWindStrength and the already-computed gust value — at full gust the
       cloth points essentially downwind, at idle nonzero wind it partially aligns, at
       strength 0 it stays on its baked heading (flagSwing contract). Optionally weight
       the yaw slightly by the existing `along` factor so the free end leads and the
       cloth reads as streaming rather than rigidly pivoting — executor's visual call,
       within the truth "cloth visibly swings/streams toward the wind".
    2. **Drape (fixes test 6, D-12):** compute the drape weight via flagDrapeGlsl from
       uWindStrength and gust. Pitch vertices down about the horizontal pole-edge axis
       as a function of along-distance: lower transformed.y and foreshorten
       transformed.x by the sine/cosine of (drape × the FLAG drape-pitch constant)
       scaled by the vertex's distance from the pole, so full drape approximates cloth
       fallen limp against the pole and zero drape restores today's stretched banner.
       Add the limp micro-sway: a small z-offset sine using the FLAG micro-sway
       constants, scaled by the DRAPE weight and NOT by uWindStrength — this is the one
       term that must survive strength 0 so the limp flag keeps a breath of life
       (mirror of how gustGainFactor keeps grass alive under the same flag, but the
       flag's version is drape-gated instead of returning-1-gated).
    3. Keep the existing ripple flap and taut-pull terms (still × uWindStrength — they
       correctly die at strength 0 now that drape takes over). Apply ordering: ripple →
       drape pitch → downwind yaw, so the yaw rotates the fully-posed offset.

    Constraints that MUST survive (from the debug session + CLAUDE.md): zero new
    uniforms/attributes (pooled single cloth material, wind-guarded cache, cache key
    'campFlag' — all per-flag variation comes from modelMatrix and vertex data); no
    per-frame CPU rotation of the mesh (frozen-matrix rule — the group's build-time
    rotation.y bake stays); cloth.castShadow stays false (the depth pass cannot follow
    the patch). Update the function's doc comment: the "Static build-time orientation —
    the cloth answers the wind in-shader" note is now literally true for direction, not
    just amplitude — say so. File stays under 300 functional LOC.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/world/assets/__tests__/windMaterialLifecycle.test.ts src/game/world/assets/__tests__/assets.test.ts && grep -q "flagSwingGlsl" src/game/world/assets/createCampFlag.ts && grep -q "flagDrapeGlsl" src/game/world/assets/createCampFlag.ts</automated>
  </verify>
  <done>Lifecycle + assets suites green; the cloth patch consumes flagSwingGlsl AND flagDrapeGlsl from windMath (no locally re-derived pose math — single source, WIND-01); uniforms wired remain exactly uTime/uWindDir/uWindStrength.</done>
</task>

<task type="auto">
  <name>Task 2: Voxel-stepped cloth detail (UAT test 8 request, D-09 aesthetic)</name>
  <files>src/game/world/assets/createCampFlag.ts, src/game/world/assets/__tests__/windMaterialLifecycle.test.ts, src/game/world/assets/__tests__/assets.test.ts</files>
  <read_first>
    - src/game/world/assets/createCampFlag.ts (post-Task-1 state — createClothGeometry, the material construction :60-63, the patched begin_vertex)
    - .planning/phases/08-wind-core/08-CONTEXT.md (D-09 — chunky voxel puffs rationale: stepped size/opacity avoids soft banding under the nearest-filtered pixel target; same identity applies to cloth)
  </read_first>
  <action>
    Make the cloth read voxel-chunky instead of a smooth sheet, cheaply, inside the same
    pooled material:

    1. Set flatShading true on the cloth Lambert material so the displaced surface
       renders as faceted bands (fragment-derivative normals follow the vertex patch for
       free — no normal recomputation, no CPU cost, cache key unchanged).
    2. Quantize the cloth deformation into bands: introduce a local band-step constant
       beside POLE_HEIGHT (art constant — it is not wind math, so it does NOT go into
       windMath.ts; document that boundary in its comment) and floor-quantize the
       `along` factor used by the ripple/drape terms to that step count in the shader,
       so the cloth deforms in discrete chunky segments rather than a continuous curve.
       Keep the yaw term on the UN-quantized along (a stepped yaw would read as tearing).
    3. Adjust the plane segmentation in createClothGeometry if the band count needs more
       resolution than the current 8x3 (executor's visual call — stay modest, tens of
       vertices, D-13). Vertex-color banner tint and the x-span [0, width] shift must
       be preserved exactly (the pooled-material-per-flag-color mechanism depends on it).
    4. Run the full suite; where an assets/lifecycle assertion pins a now-changed
       geometry constant, update THAT assertion with a comment citing this plan — the
       child ordering (pole index 0, cloth index 1), material pooling semantics, and
       CR-01/CR-02 lifetime behavior must remain untouched and their tests unmodified.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
  </verify>
  <done>Full suite green (all 716+ tests); cloth is flat-shaded with stepped deformation bands; no new materials, draw calls, uniforms, or per-frame CPU work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Client-side render path only; inputs are engine-owned uniforms and build-time seeded RNG. No user input, network, storage, or identity involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | DoS | cloth vertex shader (atan + rotations per vertex) | low | mitigate | Cloth is tens of vertices per flag across a handful of flags — negligible GPU cost; zero new uniforms/draw calls; D-13 gate: if UAT frame feel regresses, run scripts/fps_playtest.py before/after |
| T-08-04 | DoS | material rebuild churn (flatShading + patch change) | low | mitigate | Wind-guarded cache unchanged — one cloth program per wind lifetime, cache key 'campFlag' distinct from grass/canopy; CR-01/CR-02 regression suite must stay green |
| T-08-SC | Tampering | package installs | low | accept | No package-manager installs in this plan (zero-new-deps milestone ruling) |

No high/critical threats — ASVS level 1, nothing blocks.
</threat_model>

<verification>
Automated:
- `pnpm vitest run` — full suite green, including windMaterialLifecycle (CR-01/CR-02)
  and assets invariants.
- `pnpm build` — production bundle compiles (TS + template-literal shader assembly).

Human (visual truths — routed to UAT, not verifiable in CI):
Re-run `/gsd-verify-work` for phase 8 covering the reopened items:
1. During a gust the flag swings/streams in the SAME direction the fireplace smoke
   kinks, and harder gusts swing it harder (UAT tests 4/5/9).
2. Watch across several minutes: the flag's pointing direction follows the slow wind
   wander (D-05), not one fixed axis.
3. Load with `?nowind`: the cloth hangs limp down the pole with a faint micro-sway —
   not a rigid horizontal quad (UAT test 6).
4. The cloth reads chunky/faceted in keeping with the voxel art identity (UAT test 8).
5. Frame feel unchanged vs pre-fix (D-13); `scripts/fps_playtest.py` if suspicious.
</verification>

<success_criteria>
- All four UAT flag gaps addressed by shader/geometry changes in createCampFlag.ts with
  zero new uniforms and the pooled-material + frozen-matrix constraints intact.
- Pose math consumed exclusively from windMath (plan 08-08) — no duplicated formulas.
- Full test suite green; human UAT re-verify items enumerated for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/08-wind-core/08-09-SUMMARY.md` when done
</output>
