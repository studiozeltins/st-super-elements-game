---
phase: 08-wind-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/game/systems/windMath.ts
  - src/game/systems/__tests__/windMath.test.ts
autonomous: true
requirements: [WIND-01, WIND-02, WIND-03]
must_haves:
  truths:
    - "gustEnvelope(t) stays within [0,1] and rests near 0 most of the time (near-still baseline)"
    - "Gust peaks over a simulated hour land roughly every 30-60s with non-uniform gaps — never metronomic"
    - "The gust front translates rigidly: gustAt evaluated at (pos + dir*speed*dt, t + dt) equals gustAt at (pos, t)"
    - "windAngle(t) is deterministic, alloc-free, and wanders within a bounded rate of a few degrees per minute"
    - "The nine grass sway constants are byte-identical to the literals currently hard-coded in the grass shader"
  artifacts:
    - "src/game/systems/windMath.ts (zero-import pure helper)"
    - "src/game/systems/__tests__/windMath.test.ts"
  key_links:
    - "swayGlsl()/gustGlsl() generate GLSL text from the SAME exported constants the JS mirrors use — shader and CPU math can never drift"
---

<objective>
Create the zero-import pure-math foundation for the whole wind system: exported constants
(grass SWAY verbatim per D-01, GUST envelope per D-02/D-03/D-04, WANDER per D-05/D-06,
per-consumer CANOPY/FLAG character constants per WIND-03), pure functions (`sampleWind`,
`gustEnvelope`, `gustAt`, `windAngle`, `gustGainFactor`), and GLSL snippet generators
(`swayGlsl`, `gustGlsl`) — written TEST-FIRST per the project pure-helper discipline.

Purpose: WIND-02's traveling-front math and D-02's non-metronomic cadence are the only novel
math in the phase — pinning them with behavioral unit tests before any THREE code exists makes
every downstream consumer (grass, canopy, flags, smoke, Phase 10 audio) trivially verifiable.
Output: `src/game/systems/windMath.ts` + green `windMath.test.ts` (this is the Wave 0 gap from
08-VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-wind-core/08-RESEARCH.md
@.planning/phases/08-wind-core/08-PATTERNS.md
@src/game/systems/debrisMath.ts
@src/game/systems/__tests__/debrisMath.test.ts
@src/game/world/createGrassField.ts
</context>

## Artifacts this phase produces

New symbols created by this plan (source-grounding exclusion list):

- `src/game/systems/windMath.ts` — exported constant objects `SWAY`, `GUST`, `WANDER`,
  `CANOPY`, `FLAG`; exported functions `sampleWind(t, x, z)`, `gustEnvelope(t)`,
  `gustAt(t, x, z, dirX, dirZ)`, `windAngle(t)`, `gustGainFactor(strength, gust)`,
  `swayGlsl(timeExpr, xExpr, zExpr)`, `gustGlsl(timeExpr, projExpr)`
- `src/game/systems/__tests__/windMath.test.ts` — vitest suite covering the above

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing behavioral tests for windMath</name>
  <files>src/game/systems/__tests__/windMath.test.ts</files>
  <read_first>
    - src/game/systems/__tests__/debrisMath.test.ts (structure analog: imports, one describe per exported function, behavioral assertions)
    - src/game/systems/debrisMath.ts (pure-helper shape the module under test will follow)
    - src/game/world/createGrassField.ts (lines 113-134 — the nine sway literals the SWAY test pins verbatim)
    - .planning/phases/08-wind-core/08-RESEARCH.md (Pattern 3 — envelope/front/wander reference math; "Code Examples → windMath pure-helper")
  </read_first>
  <behavior>
    - SWAY constants (WIND-01, D-01): exact-value pins for the nine grass literals — f1 === 1.7, x1 === 0.35, z1 === 0.25, f2 === 3.3, z2 === 0.7, amp2 === 0.4, ampX === 0.85, ampZ === 0.55, scale === 0.09. Exact pins are correct HERE (verbatim extraction contract); everywhere else pin behavior, not numbers.
    - sampleWind (WIND-01): sampleWind(t, x, z) equals Math.sin(t*1.7 + x*0.35 + z*0.25) + 0.4*Math.sin(t*3.3 + z*0.7) for a handful of (t,x,z) samples — the JS mirror of the grass GLSL.
    - gustEnvelope bounds + rest state (D-01, D-02): sampling t over [0, 3600] at 0.1s steps, every value is within [0, 1]; at least 60% of samples are below 0.05 (near-still baseline — gusts are EVENTS).
    - Peak cadence (WIND-02, D-02): detect peaks as local maxima with value > 0.5 over the simulated hour; assert every inter-peak gap is within [20, 90] seconds, mean gap within [30, 60] seconds, and (max gap − min gap) > 5 seconds (non-metronomic — uniform gaps fail).
    - Rigid front translation (WIND-02, D-03): for several random (t, x, z, dt) with a normalized direction (dirX, dirZ): gustAt(t + dt, x + dirX*GUST.speed*dt, z + dirZ*GUST.speed*dt, dirX, dirZ) equals gustAt(t, x, z, dirX, dirZ) within 1e-9 — the wave translates rigidly at GUST.speed.
    - Wander (D-05, D-06): windAngle(t) called twice with the same t returns the identical value (deterministic, no RNG); sampling the derivative over a simulated hour at 0.5s steps, max |dTheta/dt| ≤ 0.0035 rad/s (≈12°/min upper bound for "a few degrees per minute") AND total angle range over any 10-minute window ≥ 0.035 rad (≈2° — it actually wanders).
    - Strength kill + peak gain (D-04, D-12): gustGainFactor(0, g) === 1 for any g (uWindStrength=0 restores the exact base formula); gustGainFactor(1, 1) is within [2.0, 3.5] (pronounced lean, ~2-3× base sway, no cartoon flattening).
    - Per-consumer character ordering (WIND-03): FLAG.freq > SWAY.f1 > CANOPY.freq (flags flap faster, canopies slower than grass).
    - GLSL generation (WIND-01 single source of truth): swayGlsl('uTime', 'ox', 'oz') contains the substrings '1.7000', '0.3500', '3.3000' (constants rendered via toFixed(4) — float-literal safety); gustGlsl('uTime', 'proj') contains GUST.sharpness rendered via toFixed(4) and the retarded-time subtraction of the projection expression.
  </behavior>
  <action>
    Create `src/game/systems/__tests__/windMath.test.ts` importing from `../windMath`
    (module does not exist yet — this run MUST fail). Follow the debrisMath.test.ts shape:
    `import { describe, expect, it } from 'vitest'`, one describe block per exported
    function/constant group, assertions exactly as listed in the behavior block above.
    Where GUST/WANDER numeric values are asserted, pin BEHAVIOR (bounds, cadence, ordering,
    determinism) — the exact constants are playtest-tunable per the CONTEXT.md discretion
    grant; only the nine SWAY literals get exact-value pins (D-01 verbatim-extraction
    contract). Commit as `test(08-01): add failing behavioral tests for windMath`.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/systems/__tests__/windMath.test.ts (MUST FAIL — ../windMath does not exist yet; RED gate)</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/systems/__tests__/windMath.test.ts` exits non-zero with a module-resolution or assertion failure (RED confirmed before implementation)
    - Test file contains describe blocks covering: SWAY pins, sampleWind mirror, gustEnvelope bounds/rest, peak cadence, rigid translation, windAngle determinism + rate bound, gustGainFactor kill/gain, FLAG/SWAY/CANOPY frequency ordering, GLSL generator output
    - `grep -c "toBeCloseTo\|toBe\|toBeLessThan\|toBeGreaterThan" src/game/systems/__tests__/windMath.test.ts` ≥ 15 (real assertions, not stubs)
    - No `test.skip` / `it.skip` / `it.todo` anywhere in the file: `grep -v '^\s*//' src/game/systems/__tests__/windMath.test.ts | grep -c "\.skip\|\.todo"` returns 0
  </acceptance_criteria>
  <done>Failing test file committed; every WIND-01/02/03 math behavior has a concrete assertion waiting for the implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement windMath.ts pure helper</name>
  <files>src/game/systems/windMath.ts</files>
  <read_first>
    - src/game/systems/__tests__/windMath.test.ts (the contract just written)
    - src/game/systems/debrisMath.ts (file-header doc comment + exported-const + pure-function shape to copy)
    - .planning/phases/08-wind-core/08-RESEARCH.md (Pattern 3 reference implementation: product-of-3-incommensurate-sines envelope, retarded-time front, summed-sine wander; "Code Examples" section for swayGlsl shape)
  </read_first>
  <action>
    Create `src/game/systems/windMath.ts` with ZERO imports (not even three) — doc comment
    at top stating why it is pure (unit-testable without THREE; single source of truth for
    JS mirrors AND generated GLSL). Export, all `as const` where object literals:

    - `SWAY` — the nine grass literals verbatim (D-01/WIND-01): f1 1.7, x1 0.35, z1 0.25,
      f2 3.3, z2 0.7, amp2 0.4, ampX 0.85, ampZ 0.55, scale 0.09.
    - `GUST` — starting values per RESEARCH Pattern 3: w1 = 2π/37, w2 = 2π/23, w3 = 2π/53
      (incommensurate periods → irregular peak coincidence, D-02), sharpness 3.0, gain 1.6
      (peak ≈ 2.6× base, D-04), speed 12.0 world-units/s and wavelength 40.0 (broad slow
      front ~30-50u crossing in ~3-5s, D-03). Tune sharpness/periods until the cadence test
      passes — the TEST is the spec, the numbers are discretion.
    - `WANDER` — base angle + two summed slow sines (D-06: deterministic from the clock, no
      RNG, no allocs). Choose amplitudes/periods (hundreds-of-seconds range) so the test's
      max-rate bound (≤ 0.0035 rad/s) AND minimum-wander bound both pass (D-05: a few
      degrees per minute).
    - `CANOPY` — freq ≈ 0.4 × SWAY.f1, small idleAmp (~0.05), gustAmp (~0.35), phase
      coefficient (~0.15), swayBaseY 2.0, invSwaySpan 0.2 (trees are 5-7u tall, caps from
      ~2.5u — RESEARCH Pattern 4).
    - `FLAG` — freq ≈ 2.5 × SWAY.f1 (WIND-03: flags flap faster), waveK ~6.0 (wave travels
      down the cloth), idleAmp ~0.06, gustAmp ~0.25, tautPull ~0.35, width 1.1, height 0.65,
      invLength = 1/width.

    Functions: `sampleWind(t, x, z)` (JS mirror of the grass two-octave formula built from
    SWAY); `gustEnvelope(t)` = pow(max(0, sin(t·w1)·sin(t·w2)·sin(t·w3)), sharpness);
    `gustAt(t, x, z, dirX, dirZ)` = gustEnvelope(t − (x·dirX + z·dirZ)/GUST.speed) — the
    retarded-time traveling front (WIND-02 REQUIREMENTS formula); `windAngle(t)` = base +
    summed sines; `gustGainFactor(strength, gust)` = 1 + strength·GUST.gain·gust.

    GLSL generators: a private `f = (n) => n.toFixed(4)` formatter (raw integer
    interpolation is a GLSL int and breaks compile — the :118 grass precedent);
    `swayGlsl(timeExpr, xExpr, zExpr)` returning the two-octave sway expression as a GLSL
    string with all SWAY constants formatted through `f`; `gustGlsl(timeExpr, projExpr)`
    returning the envelope-at-retarded-time expression `pow(max(0.0, sin(...)·sin(...)·
    sin(...)), SHARPNESS)` with the time argument `(timeExpr − projExpr / SPEED)`, all
    constants through `f`. Iterate until every test is green. Commit as
    `feat(08-01): implement windMath pure helper (gust envelope, traveling front, wander)`.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/systems/__tests__/windMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/systems/__tests__/windMath.test.ts` exits 0, all tests green (GREEN gate)
    - `grep -c "^import" src/game/systems/windMath.ts` returns 0 (zero-import purity — project pure-helper discipline)
    - `grep -c "Math.random" src/game/systems/windMath.ts` returns 0 (D-06: deterministic, no RNG)
    - Exports present: `grep -c "export const SWAY\|export const GUST\|export const WANDER\|export const CANOPY\|export const FLAG" src/game/systems/windMath.ts` returns 5
    - Exports present: `grep -c "export function sampleWind\|export function gustEnvelope\|export function gustAt\|export function windAngle\|export function gustGainFactor\|export function swayGlsl\|export function gustGlsl" src/game/systems/windMath.ts` returns 7
    - File is ≤ 300 LOC of functional code (CLAUDE.md rule)
  </acceptance_criteria>
  <done>windMath.ts green against the full behavioral suite; constants exported as the single source of truth for every later consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure math module, no I/O, no user input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | swayGlsl/gustGlsl string generation | low | mitigate | GLSL text is built exclusively from compile-time module constants — no user input, no URL params, no runtime values ever interpolated into shader source |
</threat_model>

<verification>
- `pnpm vitest run src/game/systems/__tests__/windMath.test.ts` green (< 5s)
- `pnpm vitest run` green (no other suite disturbed — this plan adds files only)
</verification>

<success_criteria>
- Wave 0 gap from 08-VALIDATION.md closed: windMath.test.ts exists and is green
- WIND-02 math (traveling front, non-metronomic cadence) proven by unit test before any renderer code exists
- The nine grass constants live in exactly one place, exported, ready for the Plan 02 extraction
</success_criteria>

<output>
Create `.planning/phases/08-wind-core/08-01-SUMMARY.md` when done
</output>
