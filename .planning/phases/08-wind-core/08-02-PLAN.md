---
phase: 08-wind-core
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/game/systems/createWind.ts
  - src/game/world/createGrassField.ts
  - src/game/world/createMondstadtWorld.ts
  - src/game/createGame.ts
autonomous: true
requirements: [WIND-01, WIND-02]
must_haves:
  truths:
    - "Exactly ONE wind clock exists in the client — createWind owns it; the grass field's private accumulator is gone"
    - "Grass sway between gusts is arithmetically identical to the pre-phase build (gust term multiplies by 1.0 when the envelope rests at 0)"
    - "A gust front travels across the grass field along the wander direction (retarded-time envelope in the grass vertex shader)"
    - "?nowind sets the strength uniform to 0 without any shader recompile (uniform-driven, never a define)"
    - "wind.getGustEnvelope() returns the 0..1 global envelope — the Phase 10 audio contract exists now"
  artifacts:
    - "src/game/systems/createWind.ts (WindUniforms + Wind interfaces, createWind factory)"
    - "src/game/world/createGrassField.ts (consumes options.wind, local clock deleted, gust term added)"
    - "src/game/createGame.ts (?nowind flag, wind constructed before world, wind.update in frame())"
  key_links:
    - "createGame frame() → wind.update(deltaSeconds) — the ONLY place the clock advances"
    - "wind.timeUniform/directionUniform/strengthUniform → grass shader uniforms, held BY REFERENCE (groundInfluence contract)"
    - "windMath swayGlsl/gustGlsl → grass GLSL — generated from the same constants sampleWind mirrors"
---

<objective>
Build `createWind.ts` (uniform objects + update + CPU sampling API) and perform the keystone
extraction: grass moves onto the shared wind clock with bit-identical base sway (WIND-01,
D-01), gains the multiplicative traveling-gust term (WIND-02, D-03/D-04), and `?nowind`
lands as a uniform-driven kill switch (D-12). The grass field's private clock is DELETED in
the same change (no-legacy rule, milestone Pitfall 10).

Purpose: five later systems (this phase's canopy/flags/smoke, Phase 10 audio, Phase 12
butterflies) consume this module — one coherent clock before consumer #2 exists.
Output: shared wind module wired into `frame()`, refactored grass, `?nowind` flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-wind-core/08-RESEARCH.md
@.planning/phases/08-wind-core/08-PATTERNS.md
@src/game/systems/windMath.ts
@src/game/systems/createGroundInfluence.ts
@src/game/world/createGrassField.ts
@src/game/world/createMondstadtWorld.ts
@src/game/createGame.ts
</context>

## Artifacts this phase produces

New symbols created by this plan (source-grounding exclusion list):

- `src/game/systems/createWind.ts` — `WindUniforms` interface (`timeUniform`,
  `directionUniform`, `strengthUniform`), `Wind` interface (extends WindUniforms with
  `update(deltaSeconds)`, `sampleWind(x, z)`, `sampleGust(x, z)`, `getGustEnvelope()`),
  `createWind(enabled: boolean)` factory
- `src/game/world/createGrassField.ts` — new `wind: WindUniforms` field on the options
  object; new grass shader uniforms `uWindDir`, `uWindStrength`
- `src/game/createGame.ts` — URL bisect flag `?nowind`; `windEnabled` const; `wind` const
- `src/game/world/createMondstadtWorld.ts` — new `wind` field on `MondstadtWorldOptions`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create createWind.ts — uniform provider + CPU API</name>
  <files>src/game/systems/createWind.ts</files>
  <read_first>
    - src/game/systems/createGroundInfluence.ts (lines 23-34 and 142-155 — THE uniform-object-by-reference contract and closure-factory shape to replicate, including the IMPORTANT doc-comment style warning consumers to hold the object, never cache .value)
    - src/game/systems/windMath.ts (the pure functions and constants this factory wraps)
    - .planning/phases/08-wind-core/08-RESEARCH.md (Pattern 1 — the Wind interface shape and per-consumer character table)
  </read_first>
  <behavior>
    - createWind(true).strengthUniform.value === 1; createWind(false).strengthUniform.value === 0 (D-12 uniform-driven kill)
    - After update(1.5), timeUniform.value === 1.5 (single accumulator)
    - directionUniform.value is the SAME Vector2 object before and after update() (mutated in place via .set — D-06/D-13 no allocs), and its components equal (cos(windAngle(t)), sin(windAngle(t)))
    - sampleWind(x, z) === windMath.sampleWind(timeUniform.value, x, z); sampleGust(x, z) === windMath.gustAt(timeUniform.value, x, z, dirX, dirZ); getGustEnvelope() === windMath.gustEnvelope(timeUniform.value)
  </behavior>
  <action>
    Create `src/game/systems/createWind.ts` (~60-80 LOC). Export a standalone
    `WindUniforms` interface — `timeUniform: { value: number }`, `directionUniform:
    { value: THREE.Vector2 }`, `strengthUniform: { value: number }` — mirroring how
    `GroundInfluenceUniforms` is importable on its own (consumers like createGrassField
    type only the uniforms). Export `Wind extends WindUniforms` adding `update(deltaSeconds:
    number): void`, `sampleWind(x: number, z: number): number`, `sampleGust(x: number,
    z: number): number`, `getGustEnvelope(): number` (Phase 10 audio sidechain contract —
    document it returns the un-retarded global envelope, 0..1). Export `createWind(enabled:
    boolean): Wind` as a closure factory: `update` does `timeUniform.value += deltaSeconds`,
    computes `windAngle(t)` from windMath, and calls `directionUniform.value.set(cosTheta,
    sinTheta)` — never re-creates the Vector2 (D-06, D-13). CPU sampling functions delegate
    to windMath (`sampleWind`, `gustAt`, `gustEnvelope`) reading the live clock and
    direction components. Include the groundInfluence-style IMPORTANT doc comment: consumers
    must hold the uniform OBJECTS; caching `.value` reads a stale frame forever. Add a
    colocated test block? No — cover via a small `describe('createWind')` appended to a NEW
    file `src/game/systems/__tests__/createWind.test.ts` asserting the behavior block above
    (THREE imports are fine in tests; vitest handles them).
  </action>
  <verify>
    <automated>pnpm vitest run src/game/systems/__tests__/createWind.test.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/systems/__tests__/createWind.test.ts` exits 0
    - `pnpm build` exits 0
    - `grep -c "export interface WindUniforms\|export interface Wind\|export function createWind" src/game/systems/createWind.ts` returns 3
    - `grep -v '^\s*//' src/game/systems/createWind.ts | grep -c "new THREE.Vector2"` returns 1 (constructed once at factory time; update mutates in place — D-13 zero per-frame allocs)
    - `grep -c "getGustEnvelope" src/game/systems/createWind.ts` ≥ 2 (interface + implementation — the Phase 10 contract exists)
  </acceptance_criteria>
  <done>Wind factory exists with the exact uniform-by-reference contract; clock, wander, and CPU sampling all delegate to windMath.</done>
</task>

<task type="auto">
  <name>Task 2: Extract grass onto the shared clock + add gust term + wire createGame (?nowind)</name>
  <files>src/game/world/createGrassField.ts, src/game/world/createMondstadtWorld.ts, src/game/createGame.ts</files>
  <read_first>
    - src/game/world/createGrassField.ts (FULL file — uniform hookup at :95-99, sway GLSL at :113-134, template-interpolation precedent at :118, cache key at :137, local clock at :147, update increment at :184-186, GrassField interface at :16-20)
    - src/game/world/createMondstadtWorld.ts (options interface :189-196, grassField construction :354, update() body :442-451 including the grass-field update call at :447)
    - src/game/createGame.ts (perfFlags block :295-317, world construction order :300-312, frame() :1304-1362 including the clamped delta at :1306)
    - src/game/systems/windMath.ts (SWAY constants, swayGlsl, gustGlsl, gustGainFactor — the generated GLSL this task consumes)
    - .planning/phases/08-wind-core/08-PATTERNS.md (createGrassField MOD section — the three surgical changes with exact current code excerpts)
  </read_first>
  <action>
    One atomic change across three files (no-legacy rule: old clock dies in the SAME commit
    that adds the new one — Pitfall 1 clock fragmentation).

    **createGrassField.ts:** Add `wind: WindUniforms` (imported from
    `../systems/createWind`) to the options object; pass `options.wind` into
    `createGrassMaterial` in place of the local clock object. In the material patch, wire
    `shader.uniforms.uTime = wind.timeUniform` (the one-line swap at :95) and add
    `shader.uniforms.uWindDir = wind.directionUniform` and `shader.uniforms.uWindStrength =
    wind.strengthUniform` the same uniform-object-by-reference way; declare `uniform vec2
    uWindDir;` and `uniform float uWindStrength;` in the common-include injection. Replace
    the hard-coded two-octave sway expression (the nine literals at :120-122) with the
    string returned by `swayGlsl('uTime', 'bladeOrigin.x', 'bladeOrigin.z')` — the constants
    are now single-sourced from windMath (WIND-01). Keep the fixed sway axis vec2(0.85,
    0.55) exactly as-is (discretion pick per RESEARCH Pattern 2: base formula needs no
    direction input — zero-regression option, D-01). Layer the gust MULTIPLICATIVELY: the
    displacement line gains a trailing factor `* (1.0 + uWindStrength * GAIN * gust)` where
    GAIN is `GUST.gain` interpolated via toFixed(4) and `gust` comes from
    `gustGlsl('uTime', 'dot(bladeOrigin.xz, uWindDir)')` (retarded-time traveling front —
    WIND-02, D-03; peak lean ≈ 2-3× base per D-04). Because the envelope rests at 0,
    between-gust rendering is arithmetically identical to today (D-01 honored literally).
    DELETE the local clock object at :147 and the per-frame increment inside `update()` at
    :184-186; if `update()` is then empty, delete the method from the GrassField interface
    (:16-20) and the returned object entirely (no-legacy rule).

    **createMondstadtWorld.ts:** Add `wind` to `MondstadtWorldOptions` exactly like the
    existing `scorch` field (typed as the WindUniforms interface — actually pass the full
    Wind object through as WindUniforms-compatible); pass it into `createGrassField`
    alongside the existing options at :354. Delete the grass-field update call at :447
    (blades mover and campfire flicker lines stay untouched).

    **createGame.ts:** In the perfFlags block (:295-317): add `const windEnabled =
    !perfFlags.has('nowind');` and extend the flag-listing comment at :295-296 with
    `?nowind` (per D-12). Construct `const wind = createWind(windEnabled);` BEFORE
    `createMondstadtWorld` (:306) — same position as groundInfluence — and pass `wind` into
    the world options. In `frame()`: add `wind.update(deltaSeconds);` EARLY — before
    `world.update(deltaSeconds)` and before the render — so every consumer reads this
    frame's phase. Net createGame growth ≤ ~8 lines (wiring only; logic lives in the
    sibling module).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 (tsc -b + vite build — the whole wiring compiles)
    - `pnpm vitest run` exits 0 (windMath + createWind + all pre-existing suites green)
    - `grep -c "wind.timeUniform" src/game/world/createGrassField.ts` ≥ 1 (grass reads the shared clock)
    - `grep -c "uWindStrength\|uWindDir" src/game/world/createGrassField.ts` ≥ 2 (gust uniforms wired)
    - `grep -c "swayGlsl\|gustGlsl" src/game/world/createGrassField.ts` ≥ 2 (GLSL generated from windMath — single source of truth)
    - `grep -v '^\s*//' src/game/world/createGrassField.ts | grep -c "deltaSeconds"` returns 0 (the private clock and its increment are gone — Pitfall 1)
    - `grep -v '^\s*//' src/game/world/createMondstadtWorld.ts | grep -c "grassField\.update"` returns 0 (dead call deleted at :447 — no-legacy rule)
    - `grep -c "nowind" src/game/createGame.ts` ≥ 2 (flag read + comment listing)
    - `grep -c "wind.update(deltaSeconds)" src/game/createGame.ts` returns 1 (the ONLY clock advance in the client)
    - `grep -rn "customProgramCacheKey" src/game/world/createGrassField.ts | grep -c "grassField"` returns 1 (cache key unchanged — Pitfall 3)
  </acceptance_criteria>
  <done>Grass renders off the shared wind clock with the traveling gust term; the old clock is deleted; ?nowind zeroes the strength uniform. Visual parity is human-verified in Plan 08-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → client bootstrap | `?nowind` read via `URLSearchParams.has()` — boolean presence only, no value parsed or interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | grass shader GLSL template interpolation | low | mitigate | Interpolated values come exclusively from compile-time windMath constants — never user input, never URL params (existing :118 precedent followed) |
| T-08-02 | Tampering | wind state used for gameplay decisions | low | accept | Wind is 100% cosmetic (D-06: no cross-client sync); any future gameplay-relevant wind requires a server reducer — documented here, no action now |
| T-08-03 | Denial of Service | `?nowind` flag input | low | mitigate | `URLSearchParams.has()` presence check per the :295-317 convention; worst case is the user disabling their own cosmetics |
</threat_model>

<verification>
- `pnpm vitest run` green, `pnpm build` green
- Between gusts (envelope at rest) the grass displacement expression multiplies by exactly (1.0 + uWindStrength * GAIN * 0.0) = 1.0 — arithmetic identity with the pre-phase formula
- Known accepted limitation (state in SUMMARY): shadow depth pass ignores onBeforeCompile surface patches — grass shadows already behaved this way; unchanged
</verification>

<success_criteria>
- ONE wind clock exists; grass consumes it; gusts travel across the field (WIND-01, WIND-02)
- `?nowind` is uniform-driven (renderer program count constant across a toggle — no recompile, Pitfall 4)
- Phase 10's `getGustEnvelope()` contract shipped
</success_criteria>

<output>
Create `.planning/phases/08-wind-core/08-02-SUMMARY.md` when done
</output>
