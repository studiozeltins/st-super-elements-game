---
phase: 08-wind-core
plan: 07
type: execute
wave: 2
depends_on: ["08-06"]
files_modified:
  - src/game/createGame.ts
  - src/game/systems/createSmokeColumns.ts
autonomous: true
gap_closure: true
requirements: [WIND-01, WIND-03]
must_haves:
  truths:
    - "createGame.dispose() tears the smoke system down — on game re-creation (StrictMode remount, reconnect) no smoke InstancedMesh, geometry, material, or instance buffer leaks in the abandoned scene (closes WR-01)"
    - "createSmokeColumns.dispose() frees the instance attribute GPU buffers, not just geometry and material"
    - "createSmokeColumns carries no dead state (project no-dead-code rule; clears review IN-01 while touching the file)"
  artifacts:
    - "src/game/createGame.ts (dispose block invokes the smoke teardown beside the other optional systems)"
    - "src/game/systems/createSmokeColumns.ts (complete dispose; dead per-puff field removed)"
  key_links:
    - "createGame dispose() → smokeColumns?.dispose() → scene.remove + geometry/material/instance-buffer release"
---

<objective>
Close WR-01: `smokeColumns` is created at createGame.ts:358 and its `dispose()` exists
(createSmokeColumns.ts:204-208) but is never invoked — every other optional system in the
dispose block (`debrisSystem?.dispose()`, `lightPool?.dispose()`) is torn down, smoke is
not. Every game re-creation (StrictMode dev remount via src/main.tsx:50, prod reconnect via
the App.tsx game effect) leaks the 48-slot InstancedMesh, its BoxGeometry, Lambert
material, and instance buffers in the abandoned scene graph.

Purpose: restore the "flat frame cost / clean lifecycle" truth for the smoke consumer
(D-13); memory-stable re-creation is a precondition for the WIND-01/WIND-03 coherence
re-verification.
Output: wired teardown, complete buffer release, dead state removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-wind-core/08-VERIFICATION.md
@.planning/phases/08-wind-core/08-REVIEW.md
@src/game/systems/createSmokeColumns.ts
@src/game/createGame.ts
</context>

## Artifacts this phase produces

New/changed symbols (source-grounding exclusion list):

- `src/game/createGame.ts` — one new line in the `dispose()` block: optional-chained
  smoke teardown call beside `debrisSystem?.dispose()`
- `src/game/systems/createSmokeColumns.ts` — `dispose()` additionally calls
  `mesh.dispose()` (releases instanceMatrix/instanceColor GPU buffers via the
  InstancedMesh dispose event); the `SmokePuff` interface, pool initializer, and
  `spawnPuff` drop the per-puff emitter-index field that is written but never read
  (review IN-01 — no-dead-code rule); `spawnPuff`'s parameter renamed to `emitterIndex`

<tasks>

<task type="auto">
  <name>Task 1: Wire smoke teardown into createGame.dispose() and complete the release (WR-01)</name>
  <files>src/game/createGame.ts, src/game/systems/createSmokeColumns.ts</files>
  <read_first>
    - src/game/createGame.ts (:1482-1511 — the dispose block; the pattern to match is the optional-chained teardown of debrisSystem/lightPool at :1501-1502; smoke construction at :356-360)
    - src/game/systems/createSmokeColumns.ts (FULL — the SmokePuff interface :42-51, pool initializer :72-81, spawnPuff :112-134, existing dispose :204-208 which removes the mesh and disposes geometry + material but not the InstancedMesh itself)
    - .planning/phases/08-wind-core/08-REVIEW.md (WR-01 — leak analysis; IN-01 — the dead per-puff field)
  </read_first>
  <action>
    **createGame.ts:** add `smokeColumns?.dispose();` immediately after
    `debrisSystem?.dispose();` in the `dispose()` block (~:1501) — matching the existing
    optional-system teardown pattern exactly. No other changes to this file (IN-03's
    ambiance-module extraction is out of gap-closure scope; do not refactor here).

    **createSmokeColumns.ts:** two small changes while touching the file:
    1. In `dispose()`, also call `mesh.dispose()` after removing the mesh from the scene —
       InstancedMesh.dispose() dispatches the dispose event that releases the
       instanceMatrix/instanceColor GPU attribute buffers, which geometry.dispose() +
       material.dispose() alone do not free.
    2. Remove the dead per-puff field that records which emitter spawned the puff — it is
       assigned in the pool initializer and in `spawnPuff` but never read anywhere
       (08-REVIEW IN-01; CLAUDE.md no-dead-code rule makes removing it part of this
       touch). Delete it from the `SmokePuff` interface, the pool initializer object, and
       the `spawnPuff` assignment. No behavior change: nothing reads it.
    3. Rename `spawnPuff`'s live parameter `fireIndex` → `emitterIndex` (declared
       createSmokeColumns.ts:112, read at :122). It indexes emitters, so the new name is
       accurate — and once the dead per-puff field is gone it is the only remaining
       occurrence of the old name, which the acceptance grep below requires to be zero.
       Update both call sites of `spawnPuff` if they use named/commented args (positional
       calls need no change).
       <!-- planner-discipline-allow: fireIndex -->

    Keep everything else byte-identical — the update loop, pool sizing, drift math, and
    ?nosmoke wiring are verified-green and out of scope.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "smokeColumns?.dispose()" src/game/createGame.ts` returns 1, and it appears inside the dispose() block adjacent to debrisSystem?.dispose() (verify with grep -A1 "debrisSystem?.dispose()")
    - `grep -c "mesh.dispose()" src/game/systems/createSmokeColumns.ts` returns 1
    - `grep -v '^\s*//' src/game/systems/createSmokeColumns.ts | grep -c "fireIndex"` returns 0 (dead state gone from interface, initializer, and spawn)
    - `pnpm vitest run` exits 0 (full suite stays green — 703 tests plus any added by plan 08-06); `pnpm build` exits 0
  </acceptance_criteria>
  <done>Game teardown releases every smoke resource (mesh removed, geometry/material/instance buffers freed); no dead state remains in the smoke module; suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Client-only teardown wiring; no user input, no network, no server, no new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-07-01 | Denial of Service | GPU resource leak across game re-creation | low | mitigate | This plan IS the mitigation: dispose chain wired, InstancedMesh buffers released — bounded memory across StrictMode/reconnect cycles |
</threat_model>

<verification>
- `pnpm vitest run` exits 0 and `pnpm build` exits 0
- Dispose parity: every optional system constructed in createGame (lightPool, debrisSystem, smokeColumns) now has a matching teardown in dispose()
- No behavior change to the smoke update loop (drift/spawn/recycle untouched)
</verification>

<success_criteria>
- Game re-creation leaks zero smoke resources — clean-lifecycle truth for the fourth WIND-01 consumer restored (WR-01 closed)
- Dead state removed (IN-01 cleared under the no-dead-code rule)
- Regression guard holds: full suite + production build green
</success_criteria>

<output>
Create `.planning/phases/08-wind-core/08-07-SUMMARY.md` when done
</output>
