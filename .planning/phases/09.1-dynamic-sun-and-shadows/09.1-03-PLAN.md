---
phase: 09.1-dynamic-sun-and-shadows
plan: 03
type: execute
wave: 3
depends_on: ["09.1-02"]
files_modified: []
autonomous: false
requirements: [SHADOW-03, SHADOW-01]
tags: [playtest, perf, fps-gate, lan-sync, shadows]

must_haves:
  truths:
    - "No FPS regression vs the frozen-sun baseline during a golem-class fight (SHADOW-03, D-14, hard acceptance criterion)"
    - "No shadow shimmer/crawl on static props under the pixel filter at dawn/dusk while standing still (SHADOW-03, RESEARCH Pitfall 1)"
    - "Two LAN clients see the SAME sun angle at the same wall-clock moment — server-clock-driven, verified not assumed (SHADOW-01, D-14)"
    - "?nomovingsun toggled at midday produces NO visible jump in shadow length/angle vs the moving arc (SHADOW-04 byte-exactness confirmed in-game)"
  artifacts:
    - "09.1-03-SUMMARY.md — recorded FPS numbers (baseline vs moving), crawl observation, LAN sun-angle match, ?nomovingsun midday A/B result"
  key_links:
    - "FPS gate uses scripts/fps_playtest.py + debug_grant_loadout/debug_spawn_goliaths (LOCAL ONLY) — combat-fps-playtest memory"
    - "If crawl appears, the D-08 escalation ladder (quantize dir buckets; raise SHADOW_MAP_SIZE 1024->2048 if FPS budget allows) is a CONTINGENCY — do NOT pre-build it; record the observation and route a follow-up"
---

<objective>
Prove the moving sun re-earns Phase 9's three frozen-sun guarantees under motion: combat FPS (no regression in a golem fight), pixel-crawl (no shimmer under the filter), and LAN sync (both clients agree). This is the hard acceptance gate for SHADOW-03 (D-14) and the in-game confirmation of SHADOW-01/SHADOW-04.

Purpose: The capped arc + per-frame texel-snap + 30Hz throttle were designed to be free; this plan measures that they actually are, on real hardware in real combat — the milestone's stated real risk (summed frame cost).
Output: Recorded measurements + a human sign-off, or a crawl/regression observation that routes the D-08 contingency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-CONTEXT.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-RESEARCH.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + full suite green (pre-gate)</name>
  <files>(none — build + test only, no source edits)</files>
  <read_first>
    - 09.1-02-SUMMARY.md (what wired; any decisions/deviations)
    - scripts/fps_playtest.py (the harness invocation + how it reports FPS)
  </read_first>
  <action>
    Produce a fresh build and confirm the automated floor is green before the human gate: run the full vitest suite and a production build. Confirm the local SpacetimeDB is running and the `debug_grant_loadout` / `debug_spawn_goliaths` reducers are present (local-only). Do NOT publish any module (client-only phase). Capture the build output location (laragon serves dist/).
  </action>
  <verify>
    <automated>pnpm exec vitest run 2>&1 | grep -Eq '([1-9][0-9]* passed)' && pnpm run build 2>&1 | grep -Eiv '^#' | { ! grep -Eiq 'error|failed'; }</automated>
  </verify>
  <acceptance_criteria>
    Full vitest suite green (including the Plan 01 sun-arc tests) and a clean production build. Local server + debug reducers confirmed available for the golem-spawn gate.
  </acceptance_criteria>
  <done>Full vitest suite green + clean production build; local server + debug_grant_loadout/debug_spawn_goliaths confirmed available.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Golem-fight FPS gate + crawl / midday-A/B / LAN-sync human verification</name>
  <action>Human-verify checkpoint — run the golem-fight FPS playtest and the crawl/midday/LAN checks below; there is no code to write in this task.</action>
  <what-built>
    A moving sun on a capped dome arc: shadows sweep with the day/night server clock (dawn one way, dusk the other), per-frame texel-snapped on a live basis, riding the existing 30Hz shadow throttle. A ?nomovingsun URL flag and OS prefers-reduced-motion each pin the sun byte-exact to Phase 9's frozen high-noon key; ?nodaynight freezes both sun and colors.
  </what-built>
  <how-to-verify>
    1. FPS gate (SHADOW-03, D-14): open the built page (laragon dist), grant a loadout and spawn a golem-class fight via `debug_grant_loadout` / `debug_spawn_goliaths` (local only). Run `scripts/fps_playtest.py` during the fight. Record FPS. Then reload with `?nomovingsun` (frozen-sun baseline) and repeat. CONFIRM: moving-sun FPS shows NO regression vs the frozen baseline.
    2. Crawl check (SHADOW-03): stand still next to a static prop/rock at dawn AND at dusk (or fast-forward by waiting / observing the arc). CONFIRM: shadow edges do NOT buzz/crawl/shimmer under the pixel filter while stationary.
    3. Midday A/B (SHADOW-04): at midday, toggle `?nomovingsun` on/off across a reload. CONFIRM: NO visible jump in shadow length or angle — the frozen key matches the live midday sun (54.204 deg peak, byte-exact literal).
    4. LAN sync (SHADOW-01, D-14): open two clients on the LAN pointed at the same host. CONFIRM: both show the SAME sun angle / shadow direction at the same moment (server-clock-driven).
    5. Readability (SHADOW-02): during the golem fight, CONFIRM telegraphs, enemies, and gem drops keep full contrast — shadows stay short/high-contrast at dawn/dusk (elevation floor holding).
    If crawl OR an FPS regression appears: record the exact observation (which check, magnitude). The D-08 ladder (quantize sun direction to angular buckets; raise SHADOW_MAP_SIZE 1024->2048 only if FPS budget allows) is the contingency — note it for a follow-up plan; do NOT build it inside this gate.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded FPS numbers + LAN/crawl/midday results, or describe the regression/crawl to route the D-08 contingency.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan. The debug reducers (debug_grant_loadout/debug_spawn_goliaths) are LOCAL-ONLY test tooling, never published to a real server. No new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-03 | (n/a) | local debug reducers | low | accept | debug_grant_loadout/debug_spawn_goliaths are local-only and never deployed; no production surface. Documented absence of surface per security contribution. |
</threat_model>

<verification>
- scripts/fps_playtest.py during a golem fight: moving-sun FPS not below the ?nomovingsun baseline.
- Stationary dawn/dusk: no shadow crawl under the pixel filter.
- Midday ?nomovingsun A/B: no visible shadow jump.
- Two LAN clients: identical sun angle.
</verification>

<success_criteria>
Human sign-off recording: (a) no FPS regression vs frozen baseline in a golem fight, (b) no pixel-filter crawl on static props, (c) matching sun angle across two LAN clients, (d) byte-exact midday freeze on ?nomovingsun. Any regression is documented with the D-08 contingency routed rather than silently accepted.
</success_criteria>

<output>
Create `.planning/phases/09.1-dynamic-sun-and-shadows/09.1-03-SUMMARY.md` when done
</output>
