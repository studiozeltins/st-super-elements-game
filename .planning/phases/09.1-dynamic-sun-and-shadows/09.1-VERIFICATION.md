---
phase: 09.1-dynamic-sun-and-shadows
verified: 2026-07-14T20:19:21Z
status: human_needed
score: 10/13 must-haves verified
behavior_unverified: 0
overrides_applied: 0
behavior_unverified_items:
  - truth: "No shadow shimmer/crawl on static props under the pixel filter at dawn/dusk while standing still (SHADOW-03)"
    test: "Stand still next to a static prop/rock at dawn AND dusk in the built client (laragon dist); observe shadow edges under the nearest-filter upscale"
    expected: "Shadow edges do NOT buzz/crawl/shimmer while stationary"
    why_human: "Temporal pixel-crawl is a perception phenomenon; a static automated frame capture cannot prove the absence of inter-frame shimmer. RESEARCH quantifies per-frame basis rotation at ~0.055 texel (sub-texel), so crawl is not expected — but it must be eyeballed under the pixel filter."
  - truth: "Two LAN clients see the SAME sun angle at the same wall-clock moment (SHADOW-01)"
    test: "Open two LAN clients pointed at the same host; compare sun angle / shadow direction at the same moment"
    expected: "Both clients show identical sun angle / shadow direction"
    why_human: "Requires two physical clients on the LAN. Design-guaranteed (sun direction is a pure function of the shared server-clock phase01, the same seam Phase 9's color cycle uses) but not physically verified by automation."
human_verification:
  - test: "Pixel-crawl / shimmer check — stand still by a static prop at dawn AND dusk in the built client; watch shadow edges under the pixel filter"
    expected: "No buzz/crawl/shimmer on shadow edges while stationary"
    why_human: "Temporal perception check; static automated capture cannot observe inter-frame shimmer. If crawl appears, route the D-08 contingency (quantize sun dir to angular buckets; raise SHADOW_MAP_SIZE 1024->2048 only if FPS budget allows) as a follow-up — do NOT pre-build it."
  - test: "Two-client LAN sun-angle agreement — open two LAN clients on the same host, compare sun angle at the same wall-clock moment"
    expected: "Identical sun angle / shadow direction on both"
    why_human: "Requires two physical LAN clients; design-guaranteed via shared server clock but not physically confirmed."
  - test: "Midday ?nomovingsun A/B (SHADOW-04) — at midday, toggle ?nomovingsun on/off across a reload"
    expected: "No visible jump in shadow length or angle (frozen key matches live midday sun)"
    why_human: "Nice-to-have visual confirmation. Mathematically SETTLED at unit level (buildSunBasis noon-equality + literal SUN_OFFSET seed = IEEE-754 bit-identical light position); the in-game glance is a low-priority visual double-check, not a gap."
---

# Phase 09.1: Dynamic Sun & Shadows Verification Report

**Phase Goal:** Shadows sweep with the time-of-day sun on a readability-capped arc — dynamic without hurting combat readability, shimmering, or FPS. (Reverses Phase 9's frozen-sun D-02; must re-earn combat readability, shadow-map perf, and no-pixel-crawl under a moving sun via capped arc + per-frame texel-snap + reduce-motion fallback.)
**Verified:** 2026-07-14T20:19:21Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | sunDir(phase) is C∞-continuous across the 0.99→0.01 wrap (no midnight flip) | ✓ VERIFIED | `dayNightMath.ts:284-298` raised-cosine/sine, periodic by construction; unit test "sunDir wrap continuity" green (41/41) |
| 2 | Sun elevation NEVER drops below ELEV_FLOOR_DEG at any phase (capped dome, SHADOW-02) | ✓ VERIFIED | `domeT = 0.5+0.5*cos(w) ∈ [0,1]` maps to `[FLOOR,PEAK]` — provable; unit test loops 1000 samples, green |
| 3 | Noon (0.5) returns normalized SUN_OFFSET dir, peak 54.204° NOT 75° (SHADOW-04) | ✓ VERIFIED | `SUN_ARC.ELEV_PEAK_DEG=54.204`, `AZ_NOON_DEG=56.31` exact-pinned; unit tests "pins noon peak/azimuth" green |
| 4 | Dawn/dusk azimuths on OPPOSITE sides of noon (distinct shadow direction, SHADOW-01) | ✓ VERIFIED | Sine swing `sin(0)=0` at noon, opposite sign either side; unit test "dawn/dusk azimuth asymmetry" green |
| 5 | buildSunBasis(noonDir) reproduces shipped frozen basis (renderer-free SHADOW-04) | ✓ VERIFIED | Unit test "buildSunBasis reproduces the shipped frozen basis" green; now the SHARED production helper (WR-01 fix) |
| 6 | Shadow basis rebuilt per-frame from live sun dir in setShadowFocus, ZERO new alloc | ✓ VERIFIED | `createMondstadtWorld.ts:719-721` calls shared `buildSunBasis(liveSunDir,...)` into pre-alloc scratch; basisDirty gate (IN-01 fix); code review confirms zero `new THREE.*` in method |
| 7 | Day/night is SOLE writer of sun direction; setShadowFocus only READS it | ✓ VERIFIED | Only `createDayNightCycle.apply()` (`:83`) calls `setSunDirection`; grep shows no other caller |
| 8 | ?nomovingsun OR reduce-motion pins sun to LITERAL SUN_OFFSET; colors keep drifting; ?nodaynight freezes both | ✓ VERIFIED | `createDayNightCycle.ts:81-84` skips sunDir when !moving but colors drift at `:63-91`; `liveSunOffset` seeded from literal `SUN_OFFSET` (`createMondstadtWorld.ts:176`); byte-exact per unit test |
| 9 | shadowMap.needsUpdate NOT forced per-frame — rides existing 30Hz throttle (SHADOW-03) | ✓ VERIFIED | `createPixelRenderer.ts:48,105-106` throttle UNCHANGED (frameParity every-other-frame); grep confirms no per-frame force |
| 10 | No FPS regression vs frozen-sun baseline in a golem-class fight (SHADOW-03, D-14) | ✓ VERIFIED | Real `scripts/fps_playtest.py` A/B: moving 58.5 avg / 30.0 1%-low vs frozen 59.3 / 31.1 — Δ within run-to-run noise (09.1-03-SUMMARY) |
| 11 | No shadow shimmer/crawl on static props under pixel filter at dawn/dusk (SHADOW-03) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Code + sub-texel math present; temporal perception cannot be automated — routed to human |
| 12 | Two LAN clients see SAME sun angle at same wall-clock moment (SHADOW-01) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Design-guaranteed via shared server clock; needs two physical clients — routed to human |
| 13 | ?nomovingsun midday toggle produces NO visible jump (SHADOW-04 in-game A/B) | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Byte-exactness proven at unit level; in-game visual glance is a low-priority human confirmation |

**Score:** 10/13 truths verified (3 present, behavior-unverified — all routed to human)

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/game/systems/dayNightMath.ts` | SUN_ARC, sunDir (out-param), buildSunBasis (shared) | ✓ VERIFIED | Zero-THREE; sunDir uses out-param (WR-02 fix); buildSunBasis is the shared production+test source (WR-01 fix); stale D-02 comment rewritten |
| `src/game/systems/__tests__/dayNightMath.test.ts` | Floor/noon/wrap/asymmetry/basis invariants | ✓ VERIFIED | 41 tests green (15 new sun-arc + 26 existing) |
| `src/game/world/createMondstadtWorld.ts` | setSunDirection, per-frame basis rebuild, frozen literal, basisDirty | ✓ VERIFIED | Imports buildSunBasis (`:40`); basisDirty cache (`:187,719-722`); liveSunOffset literal seed (`:176`); 3 stale comments rewritten |
| `src/game/systems/createDayNightCycle.ts` | movingSunEnabled param, sun-dir write, frozen default | ✓ VERIFIED | 4-arg signature (`:40-45`); persistent scratchSunDir (WR-02); colors drift when frozen; stale comments rewritten |
| `src/game/createGame.ts` | ?nomovingsun + reduce-motion gate, movingSunEnabled | ✓ VERIFIED | `:328-329` matchMedia + gate; passed at `:343`; frame ordering daynight.update (`:1353`) before setShadowFocus (`:1389`) |
| `src/game/engine/createPixelRenderer.ts` | stale comment rewritten, throttle unchanged | ✓ VERIFIED | `:42-48,105-106` — comment updated, autoUpdate=false + frameParity throttle unchanged |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| createDayNightCycle.apply | createMondstadtWorld.setSunDirection | sunDir(phase) → single write channel | ✓ WIRED | `:82-83` writes; world copies into liveSunDir scratch |
| createDayNightCycle.apply | dayNightMath.sunDir | same phase01 as palette (SHADOW-01) | ✓ WIRED | apply(phase) feeds both samplePalette and sunDir |
| setShadowFocus | dayNightMath.buildSunBasis | shared Gram-Schmidt basis (WR-01) | ✓ WIRED | `createMondstadtWorld.ts:40` import + `:720` call |
| createGame | createDayNightCycle | movingSunEnabled = dayNightEnabled && !nomovingsun && !reduceMotion | ✓ WIRED | `:329,343` |
| daynight.update | world.setShadowFocus | frame ordering (dir written before read) | ✓ WIRED | update `:1353` precedes setShadowFocus `:1389` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Sun-arc math invariants | `vitest run dayNightMath.test.ts` | 41/41 pass | ✓ PASS |
| Full workspace suite | `pnpm exec vitest run` | 772/772 pass (48 files) | ✓ PASS |
| Type safety | `pnpm exec tsc --noEmit` | 0 errors (exit 0) | ✓ PASS |
| Golem-fight FPS A/B | `scripts/fps_playtest.py` (moving vs ?nomovingsun) | 58.5/30.0 vs 59.3/31.1, no regression | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| SHADOW-01 | 01, 02, 03 | Sun drifts with same phase/server clock as color cycle | ✓ SATISFIED (code+auto); LAN cross-client → human | Shared phase01 seam; wrap/asymmetry unit tests; LAN two-client glance routed to human |
| SHADOW-02 | 01, 02 | Capped arc — full contrast at every time of day | ✓ SATISFIED | Elevation floor proven by construction + 1000-sample unit test |
| SHADOW-03 | 02, 03 | Per-frame texel-snapped basis; no shimmer; no FPS regression | ✓ FPS + basis SATISFIED; crawl → human | fps_playtest.py measured no regression; throttle unchanged; pixel-crawl temporal check routed to human |
| SHADOW-04 | 01, 02 | Reduce-motion / ?nomovingsun pins fixed high-noon key | ✓ SATISFIED | Byte-exact literal seed + buildSunBasis noon-equality unit test; in-game visual A/B a low-priority human glance |

All 4 declared requirement IDs (SHADOW-01..04) are accounted for in REQUIREMENTS.md (lines 51-65, all marked [x]) and mapped to verified truths/artifacts. No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (none) | — | No TBD/FIXME/XXX/placeholder markers in any modified file | — | Clean |

Code review (09.1-REVIEW.md) found 0 blockers, 2 warnings (WR-01 test-twin drift, WR-02 per-frame alloc) and 1 info (IN-01 redundant frozen-path rebuild). All three were subsequently FIXED (commits 570fab3/854cf5e/6c3c55c): buildSunBasis is now the shared production+test source of truth, sunDir uses an out-param scratch, and a basisDirty flag skips the rebuild on the frozen path. Verified present in current code.

### Human Verification Required

3 items need a human at the machine (see frontmatter `human_verification`):
1. **Pixel-crawl / shimmer** at dawn/dusk under the pixel filter (temporal perception — un-automatable). Sub-texel math (~0.055 texel/frame) predicts no crawl; must be eyeballed.
2. **Two-client LAN sun-angle agreement** (needs two physical clients). Design-guaranteed by the shared server clock.
3. **Midday ?nomovingsun A/B** visual no-jump — mathematically settled at unit level; low-priority visual double-check only.

### Gaps Summary

No gaps. Every automatable truth is code- and test-verified: the pure sun-arc math (41 unit tests), the zero-alloc per-frame renderer wiring, the single-writer sun-direction seam, the byte-exact ?nomovingsun/reduce-motion freeze, the untouched 30Hz shadow throttle, and — critically — the SHADOW-03 hard FPS gate, which was measured for real via scripts/fps_playtest.py and showed no regression vs the frozen baseline. The three remaining items are genuine human-perception / physical-LAN confirmations that no automated check can substitute for; the phase is code-complete and correct pending those glances. Status is human_needed (not passed) solely because those items exist; there are no failures and no D-08 contingency was triggered.

---

_Verified: 2026-07-14T20:19:21Z_
_Verifier: Claude (gsd-verifier)_
