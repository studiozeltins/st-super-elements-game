---
phase: 09.1-dynamic-sun-and-shadows
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/game/systems/dayNightMath.ts
  - src/game/systems/__tests__/dayNightMath.test.ts
autonomous: true
requirements: [SHADOW-01, SHADOW-02, SHADOW-04]
tags: [rendering, day-night, shadows, pure-math, vitest]

must_haves:
  truths:
    - "sunDir(phase01) returns a unit sun-position direction that is C-infinity-continuous across the 0.99->0.01 phase wrap (no midnight direction flip) — per D-01, D-13"
    - "The sun elevation NEVER drops below ELEV_FLOOR_DEG at any phase in [0,1) — capped-dome invariant, per D-02, SHADOW-02"
    - "At noon (phase 0.5) sunDir returns the normalized SUN_OFFSET direction (0.4867, 0.8111, 0.3244) — peak elevation 54.204 deg, NOT 75 deg — per D-03, SHADOW-04, RESEARCH A1"
    - "Dawn (phase < 0.5) and dusk (phase > 0.5) produce azimuths on OPPOSITE sides of the noon azimuth — distinct dawn/dusk shadow direction, per SHADOW-01"
    - "buildSunBasis(noonDir) reproduces the shipped module-const sunRight/sunUp values — makes the SHADOW-04 frozen-basis byte-exactness unit-testable without a renderer"
  artifacts:
    - "src/game/systems/dayNightMath.ts — new SUN_ARC as-const struct, sunDir(phase) export, buildSunBasis(dir) pure helper"
    - "src/game/systems/__tests__/dayNightMath.test.ts — floor invariant, noon peak, wrap continuity, dawn/dusk asymmetry, exact-pin constant tests (all green)"
  key_links:
    - "sunDir consumes the SAME phase01 the color palette consumes — provable color/sun sync (SHADOW-01)"
    - "SUN_ARC.ELEV_PEAK_DEG / AZ_NOON_DEG are D-03 contract constants (exact-pinned), NOT playtest tunables"
---

<objective>
Land ALL sun-arc geometry in the zero-THREE pure twin `dayNightMath.ts` with vitest coverage BEFORE any renderer wiring (D-13, pure-twin-first). This is the only genuinely new logic in the phase: a `sunDir(phase01)` raised-cosine elevation dome + sine azimuth swing that never grazes the horizon, passes through the exact `SUN_OFFSET` key at noon, and is continuous across the phase wrap.

Purpose: A tested, renderer-free contract for the moving sun. Every downstream wiring decision (basis rebuild, frozen fallback, gating) reads against these tested numbers so CPU math and render can never drift.
Output: `SUN_ARC` constants, `sunDir()`, `buildSunBasis()`, and their vitest suite — all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-CONTEXT.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-RESEARCH.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the sun-arc invariant tests against the not-yet-written sunDir/SUN_ARC</name>
  <files>src/game/systems/__tests__/dayNightMath.test.ts</files>
  <read_first>
    - src/game/systems/__tests__/dayNightMath.test.ts (existing structure: imports :2-9, `sampleRange` helper :23-27, describe/it range-assert style :38-59, exact-pin example :32-34)
    - src/game/systems/dayNightMath.ts (existing pure exports: CYCLE_MICROS :13, NIGHT_FLOOR :20, phase01, samplePalette, smoothstep — the sibling shape sunDir mirrors)
    - 09.1-RESEARCH.md Pattern 2 (the concrete trig for sunDir) + Pattern 1 (SUN_OFFSET geometry: |SUN_OFFSET|=61.644, elev 54.204 deg, az 56.310 deg, normalized (0.486664,0.811107,0.324443))
    - 09.1-PATTERNS.md "dayNightMath.test.ts" section (the exact describe blocks to add)
  </read_first>
  <behavior>
    - Floor invariant (SHADOW-02): loop `sampleRange(0,1,1000)`; assert `Math.asin(sunDir(p).y)*180/Math.PI >= SUN_ARC.ELEV_FLOOR_DEG` at EVERY phase.
    - Noon peak (SHADOW-04, D-03): `sunDir(0.5)` ~= normalized SUN_OFFSET — `expect(dir.x).toBeCloseTo(0.4867,3)`, `.y` 0.8111, `.z` 0.3244 (3 decimals — the arc only needs closeness; the frozen path uses the literal for byte-exactness).
    - Unit length: `sunDir(p)` has magnitude ~= 1 across the loop.
    - Wrap continuity (SHADOW-01, D-13): direction delta between `sunDir(0.999)` and `sunDir(0.001)` is tiny (no midnight flip) — mirror the existing phase01 wrap test.
    - Dawn/dusk asymmetry (SHADOW-01): for a small d, `sunDir(0.5-d)` and `sunDir(0.5+d)` azimuths (atan2(x,z)) sit on OPPOSITE sides of AZ_NOON_DEG.
    - Exact-pin contract constants (D-03): `expect(SUN_ARC.ELEV_PEAK_DEG).toBe(54.204)`, `expect(SUN_ARC.AZ_NOON_DEG).toBe(56.310)` — these are contracts, not tunables (mirrors the CYCLE_MICROS exact pin).
    - Basis equality (SHADOW-04): `buildSunBasis(sunDir(0.5))` returns right/up ~= the shipped module-const `sunRight`/`sunUp` derived from SUN_OFFSET (hardcode expected from createMondstadtWorld.ts:152-153 geometry). Proves the frozen-basis reproduction is testable renderer-free.
  </behavior>
  <action>
    Extend `dayNightMath.test.ts` (do NOT create a new file — add describe blocks beside the existing suite, reusing the shipped `sampleRange(from,to,steps)` helper). Import `sunDir`, `SUN_ARC`, `buildSunBasis` alongside current imports. Encode each behavior above as a `describe`/`it` in the file's existing range-assert style. Use `toBeCloseTo(v, 3)` for arc closeness and `toBe(...)` for the D-03 exact-pin constants. These tests MUST FAIL now (symbols do not exist yet) — that is the RED state. Do not stub the symbols in this task.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts 2>&1 | grep -Eiv '^#' | grep -Eiq 'fail|error|is not (defined|a function)|no such export'</automated>
  </verify>
  <acceptance_criteria>
    New tests exist and FAIL because `sunDir`/`SUN_ARC`/`buildSunBasis` are not yet exported. The existing (shipped) tests in the file still pass. No production symbol was written this task.
  </acceptance_criteria>
  <done>Sun-arc invariant tests added and failing (RED); existing tests still green; no production symbol written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement sunDir + SUN_ARC + buildSunBasis; fix the stale D-02 comment</name>
  <files>src/game/systems/dayNightMath.ts</files>
  <read_first>
    - src/game/systems/dayNightMath.ts (module consts :13-20, Keyframe doc :28-46, smoothstep :151-155, samplePalette normalize prologue :196 `const p = ((phase % 1) + 1) % 1;`, KEYFRAMES `as const`)
    - 09.1-RESEARCH.md Pattern 2 (exact sunDir trig) + Pattern 1 (byte-exact geometry) + Open Question 1 (sign convention)
    - 09.1-PATTERNS.md "dayNightMath.ts" section (SUN_ARC struct + convention atan2(x,z)=azimuth, y=up)
  </read_first>
  <behavior>
    - Same test list as Task 1 — this task turns them all GREEN.
  </behavior>
  <action>
    Add to `dayNightMath.ts` beside the existing pure exports (zero THREE import, per D-01/D-13):

    1. `export const SUN_ARC = { NOON_PHASE: 0.5, ELEV_PEAK_DEG: 54.204, ELEV_FLOOR_DEG: 40, AZ_NOON_DEG: 56.310, AZ_SWING_DEG: 70 } as const;` — ELEV_PEAK_DEG and AZ_NOON_DEG MUST equal the SUN_OFFSET direction (D-03, RESEARCH A1). ELEV_FLOOR_DEG (40) and AZ_SWING_DEG (70) are playtest-tunable starting values; document that inline. Do NOT use the CONTEXT D-02 prose's "~75 deg" peak — it is superseded by D-03/A1 (note this correction in the comment).

    2. `export function sunDir(phase: number): { x: number; y: number; z: number }` — open with the defensive normalize `const p = ((phase % 1) + 1) % 1;` (copy from samplePalette). Raised-cosine elevation dome (`domeT = 0.5 + 0.5*cos(w)`, `w = 2*PI*(p - NOON_PHASE)`) mapped to `[ELEV_FLOOR_DEG, ELEV_PEAK_DEG]` — provably >= FLOOR because cos-term in [0,1]. Sine azimuth swing `AZ_NOON_DEG + AZ_SWING_DEG*sin(w)` — sin(0)=0 at noon => exact AZ_NOON, opposite-sign dawn/dusk. Return unit tuple with convention `atan2(x,z)=azimuth`, `y=up`: `{ x: cosE*sin(az), y: sin(elev), z: cosE*cos(az) }`. This is the SUN-POSITION direction (matches normalized SUN_OFFSET), NOT the light-travel direction — the world negates it downstream (D-05 sign convention, Open Question 1).

    3. `export function buildSunBasis(dir: { x:number;y:number;z:number }): { rightX:number; rightY:number; rightZ:number; upX:number; upY:number; upZ:number }` — pure Gram-Schmidt: negate `dir` to light-travel, `right = normalize(cross(worldUp=(0,1,0), lightDir))`, `up = normalize(cross(lightDir, right))`. Zero THREE — plain number math. This mirrors the module-const basis at createMondstadtWorld.ts:151-153 so the noon basis-equality test proves byte-exact frozen reproduction.

    4. NO-LEGACY comment fix: rewrite the `Keyframe.sunColor` doc at `dayNightMath.ts:35` — it currently claims "Direction is FROZEN (D-02); only color/intensity drift." Direction is now a first-class export; the sunColor doc should describe only the tint and stop asserting the direction is frozen. Also update the file-header note if it references a frozen direction.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts 2>&1 | grep -Eq '([1-9][0-9]* passed)' && ! pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts 2>&1 | grep -Eiq 'fail|✗'</automated>
  </verify>
  <acceptance_criteria>
    All sun-arc tests GREEN. `sunDir`, `SUN_ARC`, `buildSunBasis` exported from dayNightMath.ts with zero THREE import. The stale D-02 "Direction is FROZEN" comment at :35 is rewritten. `SUN_ARC.ELEV_PEAK_DEG === 54.204` and `AZ_NOON_DEG === 56.310`. Floor invariant holds across 1000 phase samples.
  </acceptance_criteria>
  <done>sunDir/SUN_ARC/buildSunBasis implemented (zero THREE); all sun-arc tests green; stale D-02 comment rewritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure client-side math with no I/O, no user input, no network, no persistence. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-01 | (n/a) | dayNightMath.sunDir pure function | low | accept | No attack surface: deterministic pure math over a numeric phase, zero external input. Documented absence of surface per security contribution. |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts` fully green.
- No THREE import added to dayNightMath.ts (grep confirms).
- The D-02 "frozen direction" comment at dayNightMath.ts:35 no longer claims the direction is frozen.
</verification>

<success_criteria>
sunDir(0.5) reproduces normalized SUN_OFFSET to 3 decimals; elevation floor holds at every phase; wrap is continuous; dawn/dusk azimuths are opposite-signed; buildSunBasis(noonDir) equals the shipped module-const basis. All tests green with zero renderer involvement.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (every new symbol, across all 3 plans)

**Plan 01 — `src/game/systems/dayNightMath.ts` (pure, zero-THREE):**
- `SUN_ARC` — `as const` struct: `NOON_PHASE`, `ELEV_PEAK_DEG` (54.204, D-03 contract), `ELEV_FLOOR_DEG` (40, tunable), `AZ_NOON_DEG` (56.310, D-03 contract), `AZ_SWING_DEG` (70, tunable)
- `sunDir(phase: number): { x, y, z }` — unit sun-POSITION direction; raised-cosine elevation dome + sine azimuth swing
- `buildSunBasis(dir): { rightX..upZ }` — pure Gram-Schmidt (sun-position dir in, light-travel basis out) for renderer-free basis-equality testing

**Plan 02 — renderer wiring:**
- `AmbienceHandles.setSunDirection(x, y, z): void` (new member on `src/game/world/createMondstadtWorld.ts`) — copies into `liveSunDir` scratch, zero alloc
- Module scratch in createMondstadtWorld.ts: `WORLD_UP`, `SUN_DISTANCE` (= |SUN_OFFSET| ~61.644), `FROZEN_SUN_DIR` (normalized SUN_OFFSET literal), `liveSunDir`, `liveSunOffset`; `sunRight`/`sunUp` become per-frame-mutated scratch
- `createDayNightCycle(enabled, movingSunEnabled, clock, ambience)` — new `movingSunEnabled: boolean` param (signature grows by one bool)
- `createGame.ts`: `?nomovingsun` URL flag, `reduceMotion` (prefers-reduced-motion matchMedia), `movingSunEnabled` local

**No new files. No server/schema/binding changes. No new npm packages.**
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09.1-dynamic-sun-and-shadows/09.1-01-SUMMARY.md` when done
</output>
