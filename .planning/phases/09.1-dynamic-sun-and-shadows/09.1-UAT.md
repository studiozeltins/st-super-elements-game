---
status: testing
phase: 09.1-dynamic-sun-and-shadows
source: [09.1-VERIFICATION.md]
started: 2026-07-14
updated: 2026-07-14
---

## Current Test

number: 1
name: Pixel-crawl / shimmer check on static props at dawn and dusk
expected: |
  Standing still next to a static prop (rock/boulder/tree) while the sun arc moves,
  the shadow edges do NOT buzz, crawl, or shimmer under the nearest-neighbour pixel
  filter — at BOTH dawn and dusk (the low-elevation extremes). RESEARCH quantified
  per-frame basis rotation at ~0.055 texel (sub-texel), so no crawl is predicted, but
  it is a temporal perception check only a human can make.
awaiting: user response

## Tests

### 1. Pixel-crawl / shimmer on static props (SHADOW-03)
expected: Stand still by a static prop at dawn AND at dusk; shadow edges stay stable
  (no buzz/crawl/shimmer) under the pixel filter. If crawl appears, record which time
  of day + severity — that routes the D-08 contingency (quantize sun direction to
  angular buckets; raise SHADOW_MAP_SIZE 1024→2048 only if FPS budget allows). Do NOT
  pre-build D-08; file a follow-up.
result: [pending]

### 2. Two-client LAN sun-angle agreement (SHADOW-01)
expected: Open two clients on the LAN pointed at the same host. At the same wall-clock
  moment both show the SAME sun angle / shadow direction. (Design-guaranteed: sun
  direction is a pure function of the shared server-clock phase01 — the same seam
  Phase 9's colour cycle uses — but not physically verified with two machines yet.)
result: [pending]

### 3. Midday ?nomovingsun A/B — no visible shadow jump (SHADOW-04)
expected: At midday, toggle `?nomovingsun` on/off across a reload. NO visible jump in
  shadow length or angle — the frozen high-noon key matches the live midday sun.
  (Mathematically settled: the buildSunBasis noon-equality unit test + literal
  SUN_OFFSET seed make the frozen light position IEEE-754 bit-identical. This is a
  low-priority visual confirmation.)
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

None found in code or automated evidence. These three items are human-perception /
physical-LAN confirmations only — there are no failures, no blockers, and no D-08
contingency was triggered by the automated FPS A/B (moving 58.5/30.0 vs frozen
59.3/31.1 — no regression). SHADOW-02 (capped arc) and the SHADOW-03 FPS gate are
already fully satisfied by code + automated evidence.
