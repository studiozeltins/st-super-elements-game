---
phase: 09.1-dynamic-sun-and-shadows
plan: 02
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - src/game/world/createMondstadtWorld.ts
  - src/game/systems/createDayNightCycle.ts
  - src/game/createGame.ts
  - src/game/engine/createPixelRenderer.ts
autonomous: true
requirements: [SHADOW-01, SHADOW-02, SHADOW-03, SHADOW-04]
tags: [rendering, day-night, shadows, three, texel-snap, perf]

must_haves:
  truths:
    - "The shadow basis (sunRight/sunUp) is rebuilt per-frame from the live sun direction inside setShadowFocus, BEFORE the existing texel snap — with ZERO new per-frame allocation (D-04, D-07)"
    - "Day/night is the SOLE writer of the sun direction: createDayNightCycle.apply() writes sunDir(phase) via a single setSunDirection channel; setShadowFocus only READS it (D-05)"
    - "?nomovingsun OR prefers-reduced-motion pins the sun to the LITERAL SUN_OFFSET (byte-exact Phase 9 frozen behavior) while colors keep drifting; ?nodaynight freezes both (D-09, D-10, D-11, SHADOW-04)"
    - "renderer.shadowMap.needsUpdate is NOT forced per-frame — the moving light rides the existing 30Hz throttle, zero new GPU cost (D-07, SHADOW-03)"
  artifacts:
    - "src/game/world/createMondstadtWorld.ts — setSunDirection(x,y,z) on AmbienceHandles; per-frame basis rebuild in setShadowFocus; FROZEN_SUN_DIR/SUN_DISTANCE/liveSunDir scratch; stale D-02 comments rewritten"
    - "src/game/systems/createDayNightCycle.ts — movingSunEnabled param; sun-dir write in apply(); frozen-dir seed on construction; stale D-02 comments rewritten"
    - "src/game/createGame.ts — ?nomovingsun flag + prefers-reduced-motion matchMedia; movingSunEnabled passed into createDayNightCycle"
    - "src/game/engine/createPixelRenderer.ts — stale 'sun is static' comment rewritten (code unchanged)"
  key_links:
    - "daynight.update() (createGame.ts:1348) writes sun dir BEFORE world.setShadowFocus() (:1384) reads it — ordering already correct, no new frame line (D-05)"
    - "Sign convention: setSunDirection receives sun-POSITION dir; setShadowFocus negates to light-travel for Gram-Schmidt + sets sunLight.position = focus + dir*SUN_DISTANCE (D-06)"
    - "movingSunEnabled = dayNightEnabled && !nomovingsun && !reduceMotion — nodaynight superset of nomovingsun (D-11, D-12)"
---

<objective>
Wire the tested `sunDir(phase)` math into the renderer: convert the frozen module-const shadow basis into a per-frame scratch rebuild driven by a single day/night-owned sun-direction channel, add the `?nomovingsun` + reduce-motion freeze that reproduces Phase 9's byte-exact frozen sun, and rewrite every stale "sun is frozen / off-limits" comment the phase invalidates. This is ~90% relocation of existing correct code (basis math, texel snap, clock) plus the one setter and gate.

Purpose: The sun (and cast shadows) now drifts with the same server clock as the color cycle, on the capped arc, without new GPU cost or per-frame allocation, and with a dead-still fallback path.
Output: A moving, texel-snapped, reduce-motion-guarded sun wired end-to-end (verified by the FPS/LAN gate in Plan 03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-CONTEXT.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-RESEARCH.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-PATTERNS.md
@.planning/phases/09.1-dynamic-sun-and-shadows/09.1-01-SUMMARY.md
@src/game/systems/dayNightMath.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: World seam — per-frame basis rebuild + setSunDirection channel + frozen literal</name>
  <files>src/game/world/createMondstadtWorld.ts</files>
  <read_first>
    - src/game/world/createMondstadtWorld.ts:140-154 (SUN_OFFSET, SHADOW_FOCUS_SPAN=45, SHADOW_MAP_SIZE=1024, frozen sunDirection/sunRight/sunUp basis + shadowFocusScratch)
    - src/game/world/createMondstadtWorld.ts:669-684 (setShadowFocus texel-snap block — keep the math, swap in the live basis)
    - src/game/world/createMondstadtWorld.ts:78-93 (AmbienceHandles interface + setSkyTop copy-into-scratch shape to mirror) and :67-74 (the D-02 "sun DIRECTION basis is off-limits" doc to rewrite)
    - src/game/world/createMondstadtWorld.ts:229-244 (createLighting — sunLight.position.copy(SUN_OFFSET) build-time seed at :231, no logic change)
    - src/game/systems/dayNightMath.ts (buildSunBasis convention — sun-position dir in, light-travel basis out; the noon-equality test locks the sign)
    - 09.1-RESEARCH.md Pattern 3 (zero-alloc live basis rebuild) + Pitfall 2 (sign convention) + PATTERNS.md "createMondstadtWorld.ts" section
  </read_first>
  <behavior>
    - buildSunBasis(normalized SUN_OFFSET) equals the world's rebuilt sunRight/sunUp at the frozen dir (unit-tested in Plan 01; this task keeps the world consistent with that convention).
    - At the frozen dir (default liveSunDir), setShadowFocus produces the identical sunLight.position/target as the shipped code (byte-exact SHADOW-04 seed).
  </behavior>
  <action>
    In `createMondstadtWorld.ts`:

    1. Keep `SUN_OFFSET = new THREE.Vector3(30,50,20)` as the literal frozen key. Add module/factory-scope pre-allocated scratch (constructed ONCE, mutated forever — zero per-frame alloc, RESEARCH Pattern 3): `WORLD_UP = new THREE.Vector3(0,1,0)`; `SUN_DISTANCE = SUN_OFFSET.length()` (~61.644, preserves current magnitude per D-06 discretion); `FROZEN_SUN_DIR = SUN_OFFSET.clone().normalize()` (sun-position dir); `liveSunDir = new THREE.Vector3().copy(FROZEN_SUN_DIR)` (written by setSunDirection); `liveSunOffset = new THREE.Vector3()`. The existing `sunRight`/`sunUp` become per-frame-mutated scratch (still allocated once at :152-153) instead of frozen values.

    2. Rewrite setShadowFocus (D-04/D-06/D-07): FIRST rebuild the basis from `liveSunDir` into `sunRight`/`sunUp` using the SAME two `crossVectors(...).normalize()` calls, now per-frame — negate liveSunDir to light-travel (`scratch.copy(liveSunDir).negate()` or equivalent, matching the buildSunBasis convention) then `sunRight.crossVectors(WORLD_UP, lightDir).normalize()`, `sunUp.crossVectors(lightDir, sunRight).normalize()`. Compute `liveSunOffset.copy(liveSunDir).multiplyScalar(SUN_DISTANCE)` (sun sits at +offset from focus). THEN run the existing texel-snap block VERBATIM on the now-live sunRight/sunUp, and set `sunLight.position.copy(shadowFocusScratch).add(liveSunOffset)` (replacing the frozen `.add(SUN_OFFSET)`). Keep both `updateMatrixWorld(true)` calls. ZERO `new THREE.Vector3()` inside this method.

    3. Add `setSunDirection(x: number, y: number, z: number): void` to the `AmbienceHandles` interface (:84-93) and implement it in the returned handles: `liveSunDir.set(x, y, z)` (copy-into-scratch, mutate in place, zero alloc — mirrors setSkyTop). Consumers hold the reference; never reassign liveSunDir.

    4. NO-LEGACY comment rewrites: the "sun never moves relative to the focus" comment at :148-150, the "sun basis is fixed" note at :670-671, and the "sun DIRECTION basis is off-limits (D-02)" doc on the `ambience` field at :67-74 are now false — rewrite them to describe direction as a live per-frame write channel that setShadowFocus reads. createLighting :231 build-time seed stays (setShadowFocus overwrites it every frame).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -Eiv '^#' | { ! grep -Eiq 'createMondstadtWorld'; } && ! grep -RIn 'never moves relative to the focus\|basis is off-limits' src/game/world/createMondstadtWorld.ts</automated>
  </verify>
  <acceptance_criteria>
    setShadowFocus rebuilds sunRight/sunUp from liveSunDir per-frame with no new allocation, snaps on the live basis, and positions the light at focus + liveSunDir*SUN_DISTANCE. AmbienceHandles exposes setSunDirection. With liveSunDir at FROZEN_SUN_DIR the output is identical to the shipped frozen code. All three stale D-02/"sun is fixed" comments are rewritten. TypeScript compiles with no new errors in this file.
  </acceptance_criteria>
  <done>Per-frame zero-alloc basis rebuild + setSunDirection channel + FROZEN_SUN_DIR/SUN_DISTANCE landed; stale comments rewritten; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Day/night writer — write sun dir per-frame; frozen seed; movingSunEnabled param</name>
  <files>src/game/systems/createDayNightCycle.ts</files>
  <read_first>
    - src/game/systems/createDayNightCycle.ts:38-42 (current signature createDayNightCycle(enabled, clock, ambience)), :52-82 (apply() sole-writer body, esp. the sun color/intensity block :64-67), :84-99 (enabled/freeze branch + update no-op), :17-21 (class doc claiming direction is off-limits)
    - src/game/systems/dayNightMath.ts (sunDir export from Plan 01)
    - 09.1-RESEARCH.md "Sole-writer extension" code example (D-05/D-10) + PATTERNS.md "createDayNightCycle.ts" section
  </read_first>
  <action>
    In `createDayNightCycle.ts`:

    1. Grow the signature to `createDayNightCycle(enabled, movingSunEnabled: boolean, clock, ambience)`. Import `sunDir` from dayNightMath.

    2. In `apply(phase)`, beside the existing sun color/intensity writes (:64-67): when `movingSunEnabled`, compute `const d = sunDir(phase)` and call `ambience.setSunDirection(d.x, d.y, d.z)` (pure math -> world scratch, zero alloc). When NOT movingSunEnabled, do NOT call sunDir — leave the world's liveSunDir at its FROZEN_SUN_DIR default (byte-exact SHADOW-04). Colors STILL drift in the !movingSunEnabled case whenever dayNightEnabled (D-10): only the direction write is skipped.

    3. Construction path (:84-92): the initial `apply()` establishes the frozen sun implicitly (world defaults liveSunDir to FROZEN_SUN_DIR), so the `?nodaynight` no-op path already has a correct static sun. No extra call needed unless movingSunEnabled — confirm the frozen default is correct for both `enabled` and `!enabled` construction branches. `nodaynight ⊇ nomovingsun` (D-11) is enforced at the createGame gate (movingSunEnabled already false when !enabled); this factory just honors both flags.

    4. NO-LEGACY comment rewrites: the class doc at :17-21 ("The sun DIRECTION basis is off-limits (D-02) — only .color and .intensity drift") and the inline comment at :64 ("the direction basis is FROZEN (D-02)") are now stale — rewrite them: direction is a first-class per-frame write channel when movingSunEnabled, frozen literal otherwise.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -Eiv '^#' | { ! grep -Eiq 'createDayNightCycle'; } && ! grep -RIn 'basis is off-limits\|direction basis is FROZEN' src/game/systems/createDayNightCycle.ts</automated>
  </verify>
  <acceptance_criteria>
    createDayNightCycle takes movingSunEnabled and, when true, writes sunDir(phase) through ambience.setSunDirection every frame; when false, leaves the frozen default and colors still drift. Stale D-02 comments rewritten. Compiles clean.
  </acceptance_criteria>
  <done>movingSunEnabled param + per-frame sun-dir write + frozen default honored; stale D-02 comments rewritten; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Gate — ?nomovingsun flag + reduce-motion matchMedia; renderer comment fix</name>
  <files>src/game/createGame.ts, src/game/engine/createPixelRenderer.ts</files>
  <read_first>
    - src/game/createGame.ts:306-324 (?no* bisect-flag block + comment listing the flags; dayNightEnabled at :324), :337-338 (serverClock + createDayNightCycle call site), :1344-1348 and :1384 (frame ordering — already correct, DO NOT reorder)
    - src/game/engine/deviceProfile.ts:21 (window.matchMedia one-liner precedent)
    - src/game/engine/createPixelRenderer.ts:42-45 (stale "sun ... are static" comment) and :99-103 (30Hz needsUpdate throttle — leave UNCHANGED)
    - 09.1-RESEARCH.md "Gating in createGame" code example (D-09/D-11/D-12) + PATTERNS.md "createGame.ts" and "createPixelRenderer.ts" sections
  </read_first>
  <action>
    1. In `createGame.ts`: append `?nomovingsun` to the bisect-flag comment list at :306-308. After `dayNightEnabled` (:324), add `const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;` (mirrors deviceProfile.ts:21) and `const movingSunEnabled = dayNightEnabled && !perfFlags.has('nomovingsun') && !reduceMotion;` (nodaynight ⊇ nomovingsun, D-11). Pass `movingSunEnabled` into the `createDayNightCycle(dayNightEnabled, movingSunEnabled, serverClock, world.ambience)` call at :338. Keep ALL logic here to ~4 lines — no math in createGame (1963 LOC, must not grow logic). Do NOT touch the frame ordering: daynight.update() (:1348) already runs before world.setShadowFocus() (:1384).

    2. In `createPixelRenderer.ts`: rewrite ONLY the stale comment at :42-45 ("The sun and all shadow-casting geometry are static...") — the sun DIRECTION now drifts on a slow 20-min arc, but the per-frame angular delta is sub-texel so the existing 30Hz depth-map refresh (frameParity at :99-103) still keeps up with zero new GPU cost; do NOT force needsUpdate every frame (that would double the biggest GPU cost — a SHADOW-03 regression). Leave `shadowMap.autoUpdate = false` and the :99-103 throttle block UNCHANGED.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -Eiv '^#' | { ! grep -Eiq 'createGame|createPixelRenderer'; } && grep -q "nomovingsun" src/game/createGame.ts && grep -q "prefers-reduced-motion" src/game/createGame.ts && ! grep -Rq "shadow-casting geometry are static" src/game/engine/createPixelRenderer.ts</automated>
  </verify>
  <acceptance_criteria>
    ?nomovingsun and prefers-reduced-motion both feed movingSunEnabled, which is passed into createDayNightCycle; nodaynight implies frozen sun. The renderer 30Hz throttle is unchanged and its stale "static sun" comment is rewritten. Full build compiles: `pnpm exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>?nomovingsun + prefers-reduced-motion gate wired into movingSunEnabled and passed to createDayNightCycle; renderer stale comment rewritten (throttle unchanged); tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query -> client render toggle | `?nomovingsun` is read via `URLSearchParams.has()` (presence only, no value parsed). Same class as the existing `?nowind`/`?nodaynight` read-only render flags. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09.1-02 | Tampering | ?nomovingsun URL flag | low | accept | `.has('nomovingsun')` reads presence only, never a value — nothing to inject. Sun direction is a pure function of the SERVER clock phase01, cosmetic, not authoritative; clients cannot meaningfully desync it. Documented absence of surface per security contribution (client-only render). |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` compiles clean across all four files.
- `pnpm exec vitest run` full suite green (Plan 01 math still passes).
- grep confirms: no `new THREE.Vector3()` inside setShadowFocus; ?nomovingsun + prefers-reduced-motion present in createGame; all four stale D-02/"static sun" comments rewritten.
- Frame ordering unchanged (daynight.update before setShadowFocus).
</verification>

<success_criteria>
The sun basis rebuilds per-frame from a day/night-written direction with zero alloc, texel-snaps on the live basis, and freezes byte-exact to the SUN_OFFSET literal under ?nomovingsun/reduce-motion/?nodaynight while colors keep drifting. The 30Hz shadow throttle is untouched. No stale frozen-sun comments remain.
</success_criteria>

<output>
Create `.planning/phases/09.1-dynamic-sun-and-shadows/09.1-02-SUMMARY.md` when done
</output>
