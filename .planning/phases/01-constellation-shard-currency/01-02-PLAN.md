---
phase: 01-constellation-shard-currency
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - spacetimedb/src/index.ts
  - scripts/snapshot.mjs
autonomous: true
requirements: [REQ-shard-currency-mint]

must_haves:
  truths:
    - "The player table has a transcendShards u32 column, seeded to 0, present at every insert/restore site."
    - "grantCharacter routes the owned-character dupe branch through resolveDupeGrant; ALL THREE return sites (new-character, sub-C6, C6-overflow) carry shardMinted so pullBanner's unconditional shardsMinted += result.shardMinted never adds undefined."
    - "pullBanner accumulates shardsMinted and credits transcendShards; the 800-gem DUPLICATE_REFUND path is gone (gems unchanged on a C6-dupe)."
    - "snapshot.mjs keep-list carries transcend_shards so backups do not drop the new column."
  artifacts:
    - "transcendShards column on the player table (spacetimedb/src/index.ts)"
    - "resolveDupeGrant import + call inside grantCharacter"
    - "'transcend_shards' entry in scripts/snapshot.mjs player keep list"
  key_links:
    - "index.ts imports './gachaOverflow' — the same module the wave-1 test imports across the package boundary."
    - "All four mirror sites (table def, seedPlayer insert, RestorePlayerRow, snapshot keep-list) change together or the module fails to insert players."
    - "Final wallet write drops '+ refund' and adds 'transcendShards: currentPlayer.transcendShards + shardsMinted'."
---

<objective>
Wire the wave-1 pure helper into the live economy: add the durable `transcendShards` currency to the `player` table (all four mirror sites), route `grantCharacter`'s dupe branch through `resolveDupeGrant`, replace the `DUPLICATE_REFUND` (800-gem) accumulator in `pullBanner` with a `shardsMinted` accumulator credited to `transcendShards`, and add the column to the backup keep-list. No deploy in this plan — code + `pnpm test` only.

Purpose: Make a C6-overflow dupe mint exactly one scarce shard (gems unchanged) while a sub-C6 dupe still increments constellation — the core of REQ-shard-currency-mint — server-authoritatively and deterministically.
Output: modified `spacetimedb/src/index.ts` (column + seed + restore + grantCharacter + pullBanner + delete DUPLICATE_REFUND) and `scripts/snapshot.mjs` (keep-list), with the full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-constellation-shard-currency/01-RESEARCH.md
@.planning/phases/01-constellation-shard-currency/01-PATTERNS.md

# The file being modified is its own best exemplar (existing `gems` currency flow):
@spacetimedb/src/index.ts
# The helper created in wave 1 (imported here):
@spacetimedb/src/gachaOverflow.ts
# Backup keep-list to extend:
@scripts/snapshot.mjs
</context>

<artifacts_this_phase_produces>
This plan adds: the `transcendShards: t.u32()` column on `player`; the `resolveDupeGrant` call inside `grantCharacter`; the `shardsMinted` accumulator + `transcendShards` credit in `pullBanner`; the `'transcend_shards'` snapshot keep-list entry. It removes the `DUPLICATE_REFUND` constant and the `refund` accumulator.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add transcendShards column at all four mirror sites</name>
  <files>spacetimedb/src/index.ts, scripts/snapshot.mjs</files>
  <read_first>
    - spacetimedb/src/index.ts (player table def ~267-288; seedPlayer insert ~646-661; RestorePlayerRow ~1374-1387 consumed by restorePlayers ~1415-1423 — study the existing `gems`/`gemsFromKills`/`gemsCollected` lines to copy their shape)
    - scripts/snapshot.mjs (player `keep` array ~14-27 — note snake_case column names)
    - .planning/phases/01-constellation-shard-currency/01-PATTERNS.md (section "Additive currency column (four mirror sites, always together)")
  </read_first>
  <action>
    Add the durable currency column mirroring the existing `gems` u32 pattern at ALL four sites (STDB requires every non-defaulted column at insert time — Pitfall 1):
    (1) player table def (~267-288): add `transcendShards: t.u32()` alongside the other u32 currency columns.
    (2) seedPlayer insert (~646-661): add `transcendShards: 0` so new players seed to zero.
    (3) RestorePlayerRow object (~1374-1387): add `transcendShards: t.u32()` — the `restorePlayers` insert (`{ ...row, ... }`) then carries it automatically; verify no separate literal is needed.
    (4) scripts/snapshot.mjs player `keep` array (~14-27): add `'transcend_shards'` (snake_case — the HTTP/SQL layer uses column names) next to `'gems'`.
    Do not touch the constants yet and do not wire grantCharacter yet (that is Task 2).
  </action>
  <verify>
    <automated>grep -c "transcendShards" spacetimedb/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "transcendShards" spacetimedb/src/index.ts` returns at least 3 (table def, seed insert, RestorePlayerRow).
    - `grep -c "transcend_shards" scripts/snapshot.mjs` returns 1.
    - `pnpm test` still green (no behavior change yet; schema-mirror tests unaffected).
  </acceptance_criteria>
  <done>The new u32 currency column exists at table/seed/restore sites and in the snapshot keep-list; suite remains green.</done>
</task>

<!-- planner-discipline-allow: DUPLICATE_REFUND -->
<task type="auto">
  <name>Task 2: Route grantCharacter through resolveDupeGrant and mint shards in pullBanner; remove the 800-gem refund constant</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts (import cluster ~3-20; DUPLICATE_REFUND const ~73 and SHARD_PER_OVERFLOW_DUPE ~81; grantCharacter dupe branch ~1548-1571; pullBanner accumulator init ~1612, call sites ~1634-1637 & ~1650-1653, final wallet write ~1684-1687)
    - spacetimedb/src/gachaOverflow.ts (resolveDupeGrant signature + DupeGrantOutcome)
    - .planning/phases/01-constellation-shard-currency/01-PATTERNS.md (section "spacetimedb/src/index.ts (modify)" — the five change points with same-file `gems` exemplars)
    - .planning/phases/01-constellation-shard-currency/01-RESEARCH.md (Pitfall 5 "Gems drift on a C6-dupe"; Anti-Patterns)
  </read_first>
  <action>
    Wire the mint path so a C6-overflow dupe raises `transcendShards` by `SHARD_PER_OVERFLOW_DUPE` while gems stay unchanged:
    (a) Add `import { resolveDupeGrant } from './gachaOverflow';` to the existing relative-import cluster (~3-20).
    (b) grantCharacter has THREE return sites — ALL must return the SAME `{ isNew, constellation, shardMinted }` shape so the unconditional `shardsMinted += result.shardMinted` in pullBanner (step c) is safe on every path (an omitted `shardMinted` on the very common new-character path would add `undefined` → `NaN` and corrupt the durable u32 `transcendShards`, or fail the server typecheck). Update all three:
      - Brand-new-character branch (~1560): change `return { isNew: true, constellation: 0, maxed: false };` to `return { isNew: true, constellation: 0, shardMinted: 0 };` (drop `maxed`, add `shardMinted: 0`).
      - Owned sub-C6 + C6-overflow branches (~1562-1570): call `resolveDupeGrant(owned.constellation, MAX_CONSTELLATION, SHARD_PER_OVERFLOW_DUPE)`. Update `ownedCharacter` + `setActivation` ONLY when the returned `constellation` differs from `owned.constellation`. Return `{ isNew: false, constellation, shardMinted }` (carry both from the helper result), replacing the old `{ isNew: false, constellation, maxed }` shape. No `grantCharacter` return may omit `shardMinted`.
    (c) pullBanner: rename the `let refund = 0` accumulator (~1612) to `let shardsMinted = 0`. At BOTH call sites (~1634-1637 & ~1650-1653) replace `if (result.maxed) refund += DUPLICATE_REFUND;` with `shardsMinted += result.shardMinted;`.
    (d) Final wallet write (~1684-1687): change `gems: currentPlayer.gems - totalCost + refund` to `gems: currentPlayer.gems - totalCost` (drop `+ refund`) and add `transcendShards: currentPlayer.transcendShards + shardsMinted`.
    (e) Delete the `DUPLICATE_REFUND` constant (~73) and remove EVERY reference to it and to the old `refund` variable. Do NOT leave a comment that names the deleted constant (a leftover comment would defeat the removal check and could echo into a gate). Do NOT redeclare `SHARD_PER_OVERFLOW_DUPE` (~81 already exists).
  </action>
  <verify>
    <automated>test "$(grep -c 'DUPLICATE_REFUND' spacetimedb/src/index.ts)" -eq 0 && pnpm exec tsc --noEmit -p spacetimedb && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DUPLICATE_REFUND' spacetimedb/src/index.ts` returns 0 (constant and all references removed, no leftover comment naming it).
    - `grep -c "resolveDupeGrant" spacetimedb/src/index.ts` returns at least 1 (imported and called).
    - `grep -c "transcendShards: currentPlayer.transcendShards + shardsMinted" spacetimedb/src/index.ts` returns 1 (final wallet write credits the shard).
    - ALL THREE `grantCharacter` return sites carry `shardMinted` and none omits it — verify each `return { isNew` in `grantCharacter` includes `shardMinted` (new-character branch returns `shardMinted: 0`, both owned branches carry the helper's `shardMinted`); no `grantCharacter` return still contains `maxed`.
    - `pnpm exec tsc --noEmit -p spacetimedb` passes (server module typechecks — catches a broken `index.ts` inside this plan, not at the 01-03 publish).
    - `pnpm test` green — the wave-1 gachaOverflow unit + existing gacha/pity/serverSync suites all pass.
  </acceptance_criteria>
  <done>C6-overflow dupe mints one shard and leaves gems unchanged; sub-C6 dupe still increments constellation; all three grantCharacter return sites carry shardMinted (new-character path returns shardMinted: 0); the 800-gem refund path is fully removed; server module typechecks and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → pullBanner reducer (WebSocket) | Untrusted `bannerId`/`count` cross here; the shard credit is computed server-side, never received from the client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering/Elevation | pullBanner shard credit | high | mitigate | `shardsMinted` derives only from `resolveDupeGrant` (server constant + owned-constellation state); the client sends no shard amount. |
| T-1-03 | Elevation (cross-player mutation) | player.update target | high | mitigate | `pullBanner` resolves the caller via `requirePlayer`/`ctx.sender` and updates only `currentPlayer.identity`'s row. |
| T-1-02 | Tampering | reducer determinism | low | mitigate | Mint uses the pure `resolveDupeGrant` (no random/time), preserving determinism (CLAUDE.md rule 2). |
| T-1-SC | Tampering | package installs | low | accept | Zero external packages installed this phase (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pnpm test` green (gachaOverflow unit + gacha/pity + serverSync).
- `grep DUPLICATE_REFUND spacetimedb/src/index.ts` returns nothing.
- Column present at table/seed/restore sites and in snapshot keep-list.
</verification>

<success_criteria>
- A C6-overflow dupe path credits `transcendShards += SHARD_PER_OVERFLOW_DUPE` with gems unchanged (Pitfall 5 avoided); sub-C6 dupe unchanged behavior.
- `DUPLICATE_REFUND` and the `refund` accumulator are gone.
- No publish yet — deploy + binding regen is plan 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-constellation-shard-currency/01-02-SUMMARY.md` when done.
</output>
