---
phase: 01-constellation-shard-currency
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/module_bindings/player_table.ts
autonomous: true
requirements: [REQ-shard-currency-mint]

must_haves:
  truths:
    - "The updated module is published to the LOCAL server via an additive migrate (no --delete-data)."
    - "Client bindings are regenerated so player_table.ts carries transcend_shards."
    - "Existing local player rows survive the migrate and default transcendShards to 0 (assumption A1 confirmed)."
  artifacts:
    - "Regenerated src/module_bindings/player_table.ts containing transcend_shards"
  key_links:
    - "This plan GATES 01-04: client code that reads player.transcendShards cannot compile until these bindings exist."
    - "Publish target is LOCAL only this phase (REQUIREMENTS + planner note) — no maincloud."
    - "Module path is ./spacetimedb, NOT ./server (the spacetime:publish npm script is broken — use the explicit command)."
---

<objective>
Deploy the wave-2 schema change to the LOCAL SpacetimeDB via an additive migrate (no data wipe) and regenerate the client bindings, so the new `transcendShards` field is real on the server and typed on the client. This plan is the gate between server work and client render: without regenerated bindings the wave-4 client code cannot compile against `player.transcendShards`.

Purpose: Make the additive column live on `local` without destroying accounts/players (CLAUDE.md additive-migrate rule) and confirm assumption A1 (existing rows default the new u32 to 0).
Output: published local module + regenerated `src/module_bindings/*` (notably `player_table.ts` gains `transcend_shards`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-constellation-shard-currency/01-RESEARCH.md

# Deploy sequence + pitfalls:
@.planning/phases/01-constellation-shard-currency/01-RESEARCH.md
# Stale generated target to be regenerated (do NOT hand-edit):
@src/module_bindings/player_table.ts
</context>

<artifacts_this_phase_produces>
This plan regenerates `src/module_bindings/*`; the observable new artifact is `transcend_shards` in `src/module_bindings/player_table.ts`. No hand edits to generated files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Additive-migrate publish to local</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - .planning/phases/01-constellation-shard-currency/01-RESEARCH.md (Deploy Sequence; Pitfall 2 "wrong module path"; Runtime State Inventory / assumption A1)
    - ./.claude/CLAUDE.md (publish + additive-migrate rules; ownership gotcha)
  </read_first>
  <action>
    Ensure the local SpacetimeDB standalone is running (start it if needed). Publish the module to LOCAL only, additive migrate, NO `--delete-data` (a wipe would destroy local accounts — forbidden past Phase A). Use the explicit command, NOT the broken `spacetime:publish` npm script (which targets `--module-path server`):
    `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes`
    Do NOT publish to maincloud in this phase (REQUIREMENTS: local only; full multi-env deploy is Phase 7). If publish returns 403 Forbidden (ownership gotcha), STOP and surface it — do not blindly `server clear` (that wipes local progress); escalate for a human decision.
    Confirm assumption A1: after publish, check that existing local `player` rows show `transcend_shards = 0` and the module did not error on connect (`spacetime sql 2d-impact-game-fr9ti --server local "SELECT identity, transcend_shards FROM player LIMIT 5"`).
  </action>
  <verify>
    <automated>spacetime sql 2d-impact-game-fr9ti --server local "SELECT transcend_shards FROM player LIMIT 1"</automated>
  </verify>
  <acceptance_criteria>
    - The publish command completes without error (additive migrate; no `--delete-data`).
    - `SELECT transcend_shards FROM player` succeeds and existing rows read 0 (A1 confirmed) — no insert/validation error in `spacetime logs`.
  </acceptance_criteria>
  <done>Local module carries the transcendShards column; existing players survive with shards = 0.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate client bindings</name>
  <files>src/module_bindings/player_table.ts</files>
  <read_first>
    - src/module_bindings/player_table.ts (generated header "EDITS TO THIS FILE WILL NOT BE SAVED"; current gems/gemsFromKills/gemsCollected u32 lines — transcend_shards is currently absent)
    - .planning/phases/01-constellation-shard-currency/01-RESEARCH.md (Pitfall 3 "client still on old build")
    - package.json (the `spacetime:generate` script)
  </read_first>
  <action>
    Regenerate the client bindings against the freshly published local schema: `pnpm run spacetime:generate`. This rewrites `src/module_bindings/*`; the target is `player_table.ts` gaining a `transcend_shards` u32 field (as `__t.u32().name("transcend_shards")`). Do NOT hand-edit any generated file — if the field is missing, the publish (Task 1) did not take; re-check the server before touching the binding.
  </action>
  <verify>
    <automated>grep -c "transcend_shards" src/module_bindings/player_table.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "transcend_shards" src/module_bindings/player_table.ts` returns at least 1.
    - `pnpm build` compiles (no type errors from the regenerated bindings).
  </acceptance_criteria>
  <done>`player_table.ts` (regenerated) exposes `transcend_shards`; the client can now type `player.transcendShards`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → local SpacetimeDB (publish) | Deploy action; risk is data loss via an accidental `--delete-data`, not an external attacker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Denial of Service (data loss) | local publish | high | mitigate | Publish is additive migrate with NO `--delete-data`; explicit command targets `./spacetimedb`; A1 check confirms existing rows survive. Maincloud is untouched. |
| T-1-05 | Repudiation/ownership | local DB ownership | medium | accept | Publish uses the current CLI identity; a 403 ownership conflict halts the plan for human decision rather than auto-clearing (which would wipe progress). |
| T-1-SC | Tampering | package installs | low | accept | Zero external packages installed this phase (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `SELECT transcend_shards FROM player` succeeds on local; existing rows = 0.
- `player_table.ts` regenerated with `transcend_shards`.
- `pnpm build` green.
</verification>

<success_criteria>
- Additive migrate applied to local (no wipe); bindings regenerated; client can compile against `transcendShards`.
- Maincloud NOT touched this phase.
</success_criteria>

<output>
Create `.planning/phases/01-constellation-shard-currency/01-03-SUMMARY.md` when done.
</output>
