---
phase: 06-shielddash-lane
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified: []
autonomous: false
requirements: [ATK-04]
tags: [gate, playtest, deploy-verify]

must_haves:
  truths:
    - "Full test suite green and full build clean with all Phase-6 code merged (SC3 parity mandate, serverSync ATTACKS parity incl. shieldDash)"
    - "SC1 verified by a human on the migrated local DB: a player standing in the lane when the charge resolves is hit; a player who steps out of the lane during the 0.9s windup is not"
    - "SC2 verified: the goliath's body visibly COMMITS down the charge path — it relocates with a grounded lunge animation, no teleport-snap at ANY size (incl. size 2 at laneLength 7.95)"
    - "SC3 verified: every player within the lane width along the path is resolved (bystander check), and serverSync ATTACKS parity is green"
    - "The D6-07 after-whiff emergence is observed in play (whiffed slam -> flee past 5.5u -> dash fires at cooldown expiry) and all three documented deviations (leapSlam maxBand 5.5, size-2 lane 7.95, D6-13 renderer reads row.radius instead of a laneHalfWidth mirror) are confirmed or retuned/re-honored by the user"
  artifacts:
    - path: ".planning/phases/06-shielddash-lane/06-05-SUMMARY.md"
      provides: "gate results + playtest verdict + any retuned seeds + user rulings on the three documented deviations"
  key_links:
    - from: "dist/ built client"
      to: "local SpacetimeDB module"
      via: "LAN playtest over ws://<host>:3000 against the MIGRATED database"
      pattern: "2d-impact-game-fr9ti"
---

<objective>
The phase capstone: run the exclusive full-suite + full-build gate (this plan owns the whole
working tree — no parallel-wave flake), verify the migrated deployment, and gate the phase on
the human playtest that SC1/SC2/SC3 and the pure-helper-testing-discipline memory mandate —
lane telegraph legibility through the pixel filter, charge-commit feel, and the D6-07
emergence cannot be unit-tested.

Purpose: every Phase-6 number is a playtest-tunable seed (D-02 pattern; Phases 4 and 5 both
retuned reactions after feel testing), AND this phase carries three planner-documented
deviations that the user must explicitly confirm: leapSlam maxBand 8 -> 5.5 (the Pitfall-1
fix that makes shieldDash reachable at all), size-2 laneLength 8.0 -> 7.95 (the
float-snap guard), and D6-13's laneHalfWidth client-mirror field dropped in favor of the
renderer reading row.radius (no-dead-code — a scalar cannot parity-lock the per-size array).

Output: green gate, playtest verdict, deviation rulings, retune list (if any) applied as
ATTACKS data / juice-constant edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-shielddash-lane/06-CONTEXT.md
@.planning/phases/06-shielddash-lane/06-VALIDATION.md
@.planning/ROADMAP.md
</context>

## Artifacts this phase produces

Full Phase-6 symbol inventory (all landed by prior plans; this plan verifies them live):

- `closestPointOnSegment(px, pz, ax, az, bx, bz)` / `resolveLane(px, pz, castX, castZ, landingX, landingZ, halfWidth)` / `computeLaneEnd(castX, castZ, targetX, targetZ, length)` — `spacetimedb/src/attackHitbox.ts` (06-01)
- `AttackSpec.laneLengthBySize?: readonly number[]` + `ATTACKS.shieldDash` (lane/skill/charge; windup 6, recovery 8, cd 6.0s, halfWidth [1.6, 1.8, 2.0], length [6.5, 7.25, 7.95], dmg 2.5x, band 3.5..8, knockback 3.5, stun 3) + leapSlam maxBand 5.5 retune + `UNIT_ATTACKS` default ['leapSlam','swordSwing','shieldDash'] (06-01)
- Generalized relocate gate `spec.move !== 'none'` in `walkAttackTransitions` (06-01)
- `AttackRenderSpec.shape` widened to `'circle' | 'cone' | 'lane'` + `ATTACK_RENDER.shieldDash` (stunSeconds 0.45, cryo juiceColor, NO half-width field) — `src/game/data/attacks.ts` (06-01)
- Dash-selectability regression guards + charge walk tests + shieldDash registry invariants — four test files (06-01)
- `animateDash` grounded-lunge clip + dispatch (± `goliathAttackClips.ts` carve) — `src/game/systems/createGoliathRenderer.ts` (06-02)
- `playDash` WebAudio clang — `src/game/audio/createAudioSystem.ts` (06-02)
- shieldDash juice tier (burst + lane dust wake + flash + light shake) — `src/game/createGame.ts` (06-02)
- Lane-end computation at selection + lane resolveStrike arm (per-victim knockback center) — `spacetimedb/src/unitAttacks.ts` (06-03)
- Lane rectangle telegraph branch + `fillAxis` scale strategy + re-cast rebuild rule (± `telegraphShapes.ts` carve) — `src/game/systems/createTelegraphSystem.ts` (06-04)

<tasks>

<task type="auto">
  <name>Task 1: Exclusive full gate — suite, build, grep-gate, migrated-DB deploy verification (SC3 automated half)</name>
  <files>(none — verification only; dist/ rebuilt)</files>
  <read_first>
    - .planning/phases/06-shielddash-lane/06-VALIDATION.md (sampling contract + manual-only table)
    - CLAUDE.md "Environments & Deployment" (how the LAN client resolves the local socket; laragon serves dist/)
  </read_first>
  <action>
    Run in order, all must pass before the checkpoint: (1) `pnpm test` — the FULL vitest
    suite including the extended serverSync parity (SC3's release-blocking mandate: green,
    not skipped). (2) `pnpm build` — tsc -b plus vite build, clean; this is the FIRST full
    typecheck over the merged 06-02/06-04 client code (the telegraph and clip files are not
    imported by any vitest target) and refreshes the laragon-served dist/. (3) The
    determinism grep-gate over spacetimedb/src (the verify command's grep must produce no
    output). (4) Deploy-state re-verification on the MIGRATED local DB (never a fresh seed):
    if any spacetimedb/src file changed since 06-03's publish, republish additively with the
    exact 06-03 command (local only, never a wipe, never maincloud), then
    `spacetime logs 2d-impact-game-fr9ti -n 100 --server local` must show no panics and no
    worldTick errors/failures — the grep is SCOPED to `panic|worldTick.*(error|fail)`
    because this is a live populated DB: routine client-facing reducer rejections
    (SenderError from auth/account/position reducers) are expected traffic, NOT failures.
    If the scoped grep matches, triage: a panic or any worldTick failure blocks the gate.
    (5) Confirm zero schema drift: `spacetime describe
    2d-impact-game-fr9ti table unit_attack --json --server local` succeeds and lists the
    same columns as before the phase (zero new columns is a locked Phase-6 property). Record
    every command + result in the SUMMARY. If ANY step fails, fix forward within this plan
    (merge-interaction breakage) and re-run the full sequence.
  </action>
  <verify>
    <automated>pnpm test && pnpm build && ! grep -rnE "Math\.random|Date\.now" spacetimedb/src/ && ! (spacetime logs 2d-impact-game-fr9ti -n 100 --server local | grep -iE "panic|worldTick.*(error|fail)")</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (full suite: lane geometry, dash selectability guards, charge walk, serverSync parity incl. shieldDash)
    - `pnpm build` exits 0 (tsc -b clean + vite build; fresh dist/)
    - Determinism grep over spacetimedb/src returns zero matches
    - Local logs free of panics and worldTick errors/failures (scoped grep — routine client SenderErrors on the live DB are expected and excluded); unit_attack schema unchanged (zero new columns)
    - No maincloud command and no data-wipe flag anywhere in the transcript
  </acceptance_criteria>
  <done>Everything automatable is green on the merged tree and the migrated deployment; the only open questions are feel questions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human playtest — SC1/SC2/SC3 + emergence + deviation rulings on the migrated DB (D-02 seed-then-tune)</name>
  <files>(none — verification; retunes, if requested, are one-line ATTACKS/juice data edits)</files>
  <read_first>
    - .planning/phases/06-shielddash-lane/06-CONTEXT.md sections "Kit numbers & selection rhythm" and "Specifics" (what the user locked and what they expect to tune)
  </read_first>
  <action>
    Present the playtest script below to the user and BLOCK until they respond. During the
    playtest, after the user reports engaging a goliath at range, run `spacetime sql
    2d-impact-game-fr9ti "SELECT attack_id, state FROM unit_attack" --server local` and
    confirm a shieldDash row appeared on the POPULATED (migrated) DB — the FSM-06 lazy-upsert
    check. On retune feedback: apply the requested edits as ATTACKS data / juice-constant
    changes (citing the D6-xx seed being retuned; a damage retune must keep the serverSync
    damage-triple and no-one-shot assertions green — update the derived tests only if the
    user changes the underlying seed), re-run the Task-1 gate, republish additively to local
    ONLY if a spacetimedb/src file changed, rebuild, and re-present. On "approved": record
    the verdict, the deviation rulings, and any applied retunes in the SUMMARY.
  </action>
  <what-built>
    The full Phase-6 shieldDash, live on the migrated local DB and freshly built dist/: at
    5.5-8u with the shared skill cooldown clear, the goliath telegraphs a Frost-cyan lane
    rectangle (instant outline, fill sweeping from the goliath toward the lane end over
    0.9s), then CHARGES its body down the lane — a grounded shield-braced lunge that
    relocates it to the lane end (overshooting past where you stood), hitting EVERY player
    still inside the lane for 2.5x contact damage with a perpendicular bulldoze knockback
    (3.5u) + 0.45s stun, then a 1.2s skid/settle punish window at the lane end. Shield-clang
    SFX + dust wake along the path. The kit is now complete: slam (ranged skill, 0-5.5u) /
    swing-chain (melee basic) / dash (gap-closer skill, 5.5-8u dash-only zone).
  </what-built>
  <how-to-verify>
    Playtest on the LAN build against the migrated local DB (laragon dist/ at your host IP,
    local standalone running — a second client ideal for the bystander check):

    1. SC1 (dodge fairness): stand ~6-7u from a goliath and wait for the lane telegraph.
       Stay in the lane -> you are hit, shoved SIDEWAYS out of the path (not carried along
       it), briefly stunned (0.45s). Repeat and SIDESTEP out of the rectangle during the
       0.9s fill -> you must NOT be hit. The sidestep should feel generous but the windup
       aggressive (shorter than the slam's 1.2s — D6-08 intent).
    2. SC2 (body commit): watch the charge at each goliath size if possible. The goliath
       must visibly LUNGE down the lane to its end — grounded (no leap arc), braced crouch
       during windup, heavy skid at the end. If ANY size (especially the BIGGEST) snaps or
       teleports to the lane end instead of travelling, report "size-N teleports" (the
       7.95 float-snap guard failed — one-line laneLengthBySize edit).
    3. SC3 (all victims + parity): with two clients, stand BOTH players inside the lane at
       different points along it -> BOTH are hit (not just the nearest). Parity is the
       Task-1 automated half (already green).
    4. Emergence (D6-07): let a slam whiff, then run away past ~5.5u and keep your distance.
       Within a few seconds of the shared cooldown expiring (~6s), the goliath should answer
       with the dash gap-closer — slam -> chain -> dash -> chain rhythm over ~30s of melee
       pressure. If the dash NEVER fires, report it (selection issue outranks feel).
    5. Deviation rulings (explicit user confirmation requested): (a) leapSlam maxBand was
       retuned 8 -> 5.5 so the dash has an exclusive 5.5-8u zone — without it the dash is
       mathematically unreachable under your locked shared-cooldown + list-order decisions.
       Confirm the slam still feels right at mid-range, or pick a different partition
       (e.g. slam melee-only at 3.5). (b) size-2 laneLength seeded 7.95 instead of the
       locked 8.0 (float-snap hazard at the exact client snap ceiling). Confirm or ask for
       8.0 + accept the snap risk. (c) D6-13's letter said the client mirror gains a
       laneHalfWidth field; the renderer reads row.radius instead because a scalar cannot
       parity-lock the per-size half-width array and would be dead data (CLAUDE.md
       no-dead-code) — confirm this, or request the honoring variant (a mirrored
       per-size array parity-locked to server radiusBySize).
    6. Readability + tuning (A3, D-02): at MAX pixelation the lane rectangle must read as a
       clear lane (rim not smearing away); the fill must sweep from the goliath toward the
       lane end at constant width (not grow as a wedge); the clang/wake should feel smaller
       than the slam's juice. All numbers are seeds: windup 0.9s, recovery 1.2s, cooldown
       6s, damage 2.5x, knockback 3.5, stun 0.45s, half-widths 1.6/1.8/2.0, lengths
       6.5/7.25/7.95 — report any retunes.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe issues/retunes (e.g. "lane too narrow, dash never fires, keep slam at 8") — Claude applies data edits, re-runs the Task-1 gate, republishes additively if server data changed, and re-presents.</resume-signal>
  <verify>
    <automated>MISSING — feel/legibility verification is human-only by design (pixel-filter legibility and two-client charge feel cannot be asserted headlessly, per 06-VALIDATION.md manual-only table and the 04/05 precedent; all automatable behavior is covered by the 06-01 unit gates and Task 1)</automated>
    <human-check>SC1 in-lane hit + step-out dodge, SC2 grounded body-commit at every size (no snap), SC3 bystander resolution, D6-07 emergence rhythm, deviation rulings (slam maxBand 5.5, size-2 lane 7.95, D6-13 row.radius instead of a laneHalfWidth mirror), lane legibility through the pixel filter — per the numbered script above</human-check>
  </verify>
  <done>User replied "approved" (with or without applied retunes); SC1, SC2, SC3, the emergence check, and all three deviation rulings are confirmed on the migrated local DB over LAN, and the shieldDash unit_attack row was observed via SQL after a real engage (FSM-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN clients → local SpacetimeDB | Playtest traffic over ws://host:3000; all combat writes remain worldTick-internal — the playtest specifically confirms a client cannot influence lane outcomes except by moving |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | server-authoritative lane resolution | high | mitigate | Playtest steps 1/3 observe server-owned behavior (step-out dodge honored, all-victims resolution) over the real network; no new client input surface exists to probe |
| T-06-03 | DoS (gameplay) | retunes crossing the one-shot line | medium | mitigate | Any damage retune re-runs the full gate; serverSync damage assertions + the untouched no-one-shot chain invariant block a violating retune |
| T-06-06 | DoS (data loss) | republish during the retune loop | high | mitigate | Retune republishes are additive data-only edits with the 06-03 command; data-wipe flags prohibited; maincloud never targeted |
| T-06-SC | Tampering | package installs | low | accept | Zero new dependencies across the entire phase |
</threat_model>

<verification>
- Task-1 gate green on the merged tree (full suite + build + grep-gate + deploy checks)
- Human approval received for SC1, SC2, SC3, the D6-07 emergence, and all three deviation rulings on the migrated DB
- shieldDash unit_attack row observed via SQL after a real engage (FSM-06 lazy upsert on a populated DB)
- Any retunes applied as data edits with the gate re-run before final approval
</verification>

<success_criteria>
- All three ROADMAP Phase-6 success criteria verified by a human on the migrated local DB over LAN
- serverSync ATTACKS parity green at phase end (SC3 mandate); grep-gate green
- Phase leaves no red tests, no dirty tree, no unpublished server changes, and an explicit user ruling on all three documented deviations (slam maxBand 5.5, size-2 lane 7.95, D6-13 row.radius)
</success_criteria>

<output>
Create `.planning/phases/06-shielddash-lane/06-05-SUMMARY.md` when done
</output>
