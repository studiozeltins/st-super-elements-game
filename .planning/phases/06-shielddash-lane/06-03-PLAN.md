---
phase: 06-shielddash-lane
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - spacetimedb/src/unitAttacks.ts
  - src/module_bindings
autonomous: true
requirements: [ATK-04]
tags: [spacetimedb, glue, deploy, worldtick]

must_haves:
  truths:
    - "When selectAttack returns shieldDash, the glue computes the lane end with overshoot (computeLaneEnd) and clamps it to the world BEFORE enterWindup, so landingX/Z stores the lane end and enterWindup stays byte-identical (D6-02/D6-14)"
    - "resolveStrike resolves EVERY live non-safe-zone player within row.radius of the cast->landing centerline — not just the first (D6-04, SC3 bystanders)"
    - "A lane victim's knockback center is the closest point on the centerline (per-victim, computed inside the loop) so victims shove perpendicular OUT of the path; an on-centerline victim falls into the existing zero-delta heading fallback (D6-05)"
    - "The republished module runs against the POPULATED local DB with no reducer errors in the logs (migrated-DB verification, zero schema change)"
  artifacts:
    - path: "spacetimedb/src/unitAttacks.ts"
      provides: "lane-end computation at the IDLE selection site + lane arm in resolveStrike with per-victim knockback center"
      contains: "resolveLane"
  key_links:
    - from: "spacetimedb/src/unitAttacks.ts"
      to: "spacetimedb/src/attackHitbox.ts"
      via: "glue imports computeLaneEnd, resolveLane, closestPointOnSegment — all decision arithmetic stays in the pure sibling (glue header contract)"
      pattern: "computeLaneEnd"
    - from: "spacetimedb/src/unitAttacks.ts"
      to: "spacetimedb/src/worldRules.ts"
      via: "lane end clamped per-axis via the existing clampToWorld (clamp the goliath, not the victims — D6-14)"
      pattern: "clampToWorld"
    - from: "spacetimedb/src/unitAttackFsm.ts"
      to: "spacetimedb/src/unitAttacks.ts"
      via: "the generalized teleportToLanding flag flows through the EXISTING goliathPosition override before the single apply — zero glue change for the relocate itself (D6-01)"
      pattern: "teleportToLanding"
---

<objective>
Wire the tested 06-01 pure layer into the worldTick glue: the lane-end computation at the
IDLE selection site (D6-02 overshoot + D6-14 world clamp) and the lane arm in `resolveStrike`
(D6-04 all-victims segment test + D6-05 per-victim perpendicular-bulldoze knockback center) —
then republish the module to the local self-hosted server, regenerate bindings, and verify
against the POPULATED DB. After this plan the server fires real shieldDash attacks
end-to-end.

Purpose: this is the moment the milestone's hardest shape goes live server-side. The glue
adds ZERO decision arithmetic (its header contract) — every calculation was already tested
in 06-01, so this plan is argument selection, one branch per seam, and ops.

Deploy notes: zero schema change — this is a module-logic republish only (RESEARCH Pitfall 9:
zero schema change does not mean zero deploy steps). NO maincloud publish (self-host pivot).
NO full `pnpm build` here — the exclusive full-suite/full-build gate belongs to 06-05
(06-04 runs in parallel this wave).

Output: extended `unitAttacks.ts`, republished module, regenerated (expected-identical)
bindings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-shielddash-lane/06-CONTEXT.md
@.planning/phases/06-shielddash-lane/06-RESEARCH.md
@.planning/phases/06-shielddash-lane/06-PATTERNS.md
@.planning/phases/06-shielddash-lane/06-01-SUMMARY.md
@spacetimedb/src/unitAttacks.ts
@spacetimedb/src/worldRules.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 6 (this plan lands the starred ones):

- `closestPointOnSegment` / `resolveLane` / `computeLaneEnd` — `spacetimedb/src/attackHitbox.ts` (plan 06-01)
- `AttackSpec.laneLengthBySize?` + `ATTACKS.shieldDash` + leapSlam maxBand 5.5 retune + UNIT_ATTACKS append — `spacetimedb/src/attacks.ts` (plan 06-01)
- Generalized relocate gate `spec.move !== 'none'` — `spacetimedb/src/unitAttackFsm.ts` (plan 06-01)
- `ATTACK_RENDER.shieldDash` + shape union widened to `'lane'` — `src/game/data/attacks.ts` (plan 06-01)
- `animateDash` clip + dispatch, `playDash` SFX, shieldDash juice tier — client (plan 06-02)
- ★ Lane-end computation at the IDLE selection site (computeLaneEnd + clampToWorld before enterWindup) — `spacetimedb/src/unitAttacks.ts`
- ★ Lane arm in `resolveStrike` (resolveLane hit test + per-victim closestPointOnSegment knockback center) — `spacetimedb/src/unitAttacks.ts`
- ★ Republished local module + regenerated `src/module_bindings/` (expected identical — zero schema change)
- Lane rectangle telegraph branch + `fillAxis` scale strategy — `src/game/systems/createTelegraphSystem.ts` (plan 06-04)

<tasks>

<task type="auto">
  <name>Task 1: Lane-end computation at selection + lane arm in resolveStrike (D6-02/D6-04/D6-05/D6-14)</name>
  <files>spacetimedb/src/unitAttacks.ts</files>
  <read_first>
    - spacetimedb/src/unitAttacks.ts (FULL — header contract lines 1-8 "ZERO decision arithmetic", IDLE selection site at ~166-187 where selectAttack feeds enterWindup, resolveStrike shape branch + victim loop at ~76-121 incl. the pre-loop isCone center and the WR-01 monotonic stun write, the teleport apply at ~219-223, the attack_strike emission at ~229-236)
    - spacetimedb/src/attackHitbox.ts (the 06-01 exports being imported: computeLaneEnd, resolveLane, closestPointOnSegment signatures)
    - spacetimedb/src/worldRules.ts lines ~14-16 (clampToWorld per-axis signature)
    - spacetimedb/src/unitAttackFsm.ts enterWindup (~75-100) — UNCHANGED reference: sizeIndex clamp shape at ~86 and radius write at ~96 (row.radius gets radiusBySize[clampedIndex] = half-width for free)
    - .planning/phases/06-shielddash-lane/06-PATTERNS.md section "spacetimedb/src/unitAttacks.ts" (Analogs 1-4 with the exact insertion points)
  </read_first>
  <action>
    Two surgical edits honoring the glue header contract (branch + select arguments + call
    pure helpers; zero arithmetic invented here). EDIT 1 — IDLE selection site: after
    selectAttack returns and before enterWindup, when the selected spec's shape is 'lane' and
    laneLengthBySize is present, compute the lane end via computeLaneEnd(from.x, from.z,
    target.positionX, target.positionZ, laneLengthBySize[clampedIndex]) — with clampedIndex
    mirroring enterWindup's own sizeIndex clamp shape (min/max against the array length) —
    then clamp EACH axis via the existing clampToWorld (D6-14: clamp the goliath, never the
    victims) and pass the clamped lane end as enterWindup's target args. Non-lane attacks
    pass the raw target coords exactly as today; enterWindup itself is untouched (D6-02 —
    landingX/Z therefore stores the lane end and row.radius the half-width). Comment the
    branch with D6-02 (aim locked ONCE at cast, overshoot past the target) and D6-14.
    EDIT 2 — resolveStrike lane arm: add an isLane constant beside isCone; the hit test for
    lanes is resolveLane(victim.positionX, victim.positionZ, row.castX, row.castZ,
    row.landingX, row.landingZ, row.radius) — the loop already visits EVERY live player and
    already skips safe-zone victims (D4-03/D6-14 inherited). The knockback center for lanes
    moves INSIDE the victim loop: closestPointOnSegment(victim.positionX, victim.positionZ,
    row.castX, row.castZ, row.landingX, row.landingZ), computed only after a hit; circle/cone
    keep the pre-loop constant center exactly as today. The on-centerline victim needs no new
    code — knockbackDisplacement's zero-delta heading fallback covers it (D6-05; note it in
    the comment). The monotonic stun write (WR-01) and the shared playerDamage map accumulate
    (D6-09 'contact' channel) are untouched — the lane hit flows through the existing lines.
    Import computeLaneEnd, resolveLane, closestPointOnSegment from './attackHitbox' beside
    the existing imports. Also refresh the teleport-apply comment (~line 220) so it names
    'leap'/'charge' relocate via the generalized flag while move 'none' stays planted — the
    apply CODE is already generic and stays byte-identical (D6-01; the attack_strike
    emission likewise already carries landing = lane end and radius = half-width, zero
    changes). Keep the file at or under 300 functional LOC — if the additions cross it,
    carve resolveStrike into a sibling module in this same task per the no-monolith rule.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/attackHitbox.test.ts src/game/data/__tests__/unitAttackFsm.test.ts src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "computeLaneEnd" spacetimedb/src/unitAttacks.ts` returns >= 2 (import + call); same for `resolveLane` and `closestPointOnSegment`
    - The lane-end branch applies clampToWorld to BOTH axes of the computed lane end before enterWindup (source assertion)
    - The lane knockback center is computed inside the victim loop, after the hit test, only for lanes; circle/cone keep the pre-loop center (source assertion)
    - enterWindup, dueAttackTransitions, walkAttackTransitions call sites carry no new arguments; the monotonic stun comparison line is byte-identical to before
    - Full pure-layer + parity suite green (verify command exits 0)
  </acceptance_criteria>
  <done>The glue selects lane arguments and delegates every calculation to 06-01's tested pure helpers; the dash resolves all lane victims with perpendicular bulldoze knockback via existing seams.</done>
</task>

<task type="auto">
  <name>Task 2: Grep-gate + local republish + bindings regen + populated-DB verification (RESEARCH Pitfall 9)</name>
  <files>src/module_bindings</files>
  <read_first>
    - CLAUDE.md sections "Environments & Deployment" and "Server-authoritative enemies + goliaths (deploy notes)" (publish command, module path ./spacetimedb, migrate-vs-wipe rules, seed_world idempotence)
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md "Pitfall 9" (zero schema change still requires the module republish; the FSM lazily upserts rows so no seeding is needed)
  </read_first>
  <action>
    Run in order, each must pass: (1) the determinism grep-gate over spacetimedb/src — the
    grep in the verify command must produce no output (all Phase-6 server code takes injected
    values only); (2) `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb
    --server local --yes` — additive module republish, NEVER a data wipe, NEVER maincloud
    (self-host pivot; the maincloud DB no longer exists); if the local standalone is not
    running, start it first per the CLAUDE.md deploy notes; (3) `pnpm run spacetime:generate`
    — bindings are expected byte-identical (zero schema change; regeneration is free
    insurance — record whether any file changed); (4) populated-DB verification (never a
    fresh seed): `spacetime sql 2d-impact-game-fr9ti "SELECT attack_id, state FROM
    unit_attack" --server local` exits 0 (rows may or may not exist yet — goliaths engage
    lazily; the shieldDash-row check happens during the 06-05 playtest engage), and
    `spacetime logs 2d-impact-game-fr9ti -n 100 --server local` shows no reducer errors or
    panics from worldTick (a glue mistake surfaces here as insert/update failures). Do NOT
    run `pnpm build` (06-04 runs in parallel this wave; the exclusive build gate is 06-05
    Task 1). Record every command + result for the SUMMARY.
  </action>
  <verify>
    <automated>sh -c '! grep -rnE "Math\.random|Date\.now" spacetimedb/src/ && spacetime sql 2d-impact-game-fr9ti "SELECT attack_id, state FROM unit_attack" --server local && ! (spacetime logs 2d-impact-game-fr9ti -n 100 --server local | grep -iE "error|panic")'</automated>
  </verify>
  <acceptance_criteria>
    - The determinism grep over spacetimedb/src produces zero matches (grep-gate green)
    - `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes` exits 0 with no data-wipe flag anywhere in the transcript
    - `pnpm run spacetime:generate` exits 0; any bindings diff is recorded (expected: none)
    - The unit_attack SQL query exits 0 against the populated local DB and the last 100 log lines contain no error/panic entries
    - No maincloud command appears anywhere in the transcript (self-host pivot)
  </acceptance_criteria>
  <done>The lane-wired module is live on the populated local self-hosted DB with clean logs; the client playtest surface is 06-05's job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server reducers | UNCHANGED — zero new reducer args, zero new client-writable input; the lane FSM runs entirely inside the scheduled worldTick |
| LAN clients → local SpacetimeDB | Republished module serves existing subscriptions; combat truth (lane hit, knockback, stun) stays server-computed vs server-accepted player rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | lane hit/dodge resolution | high | mitigate | resolveLane resolves vs server-accepted player rows at strike+grace; stun-window updatePosition rejection (T-04-12) and setActiveCharacter stun gate (05-05) inherited unchanged (ASVS V4/V5) |
| T-06-02 | Tampering/Repudiation | module determinism | medium | mitigate | Grep-gate is a blocking step of Task 2; glue passes injected now/tick only |
| T-06-06 | DoS (data loss) | republish against the populated DB | high | mitigate | Additive module-only republish; data-wipe flags prohibited; maincloud never targeted; verification runs against the POPULATED DB per the migrate-gotchas memory |
| T-06-SC | Tampering | package installs | low | accept | Zero new dependencies across the phase |
</threat_model>

<verification>
- Full pure-layer + parity vitest suite green after the glue edits
- Module published to local only; bindings regenerated; populated-DB sql + logs clean
- No edits outside unitAttacks.ts (and a possible same-slice carve sibling) + regenerated bindings
</verification>

<success_criteria>
- shieldDash fires end-to-end server-side: selectable (5.5-8u), lane end computed with overshoot + world clamp, relocate at strike, all lane victims resolved with perpendicular knockback + monotonic stun
- Glue header contract intact — zero decision arithmetic added to unitAttacks.ts
- Local deployment verified against the populated DB with clean logs; no maincloud, no wipe
</success_criteria>

<output>
Create `.planning/phases/06-shielddash-lane/06-03-SUMMARY.md` when done
</output>
