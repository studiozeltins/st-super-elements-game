---
phase: 11-lived-in-props-wear
plan: 05
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/game/world/terrain.ts
  - src/game/world/grassPlacement.ts
  - src/game/world/__tests__/terrain.test.ts
  - src/game/world/__tests__/grassPlacement.test.ts
autonomous: true
requirements: [WEAR-01]
must_haves:
  truths:
    - "Footpaths bake a light trampled tint into the terrain vertex color — LIGHTER/greener than ROAD_DIRT, distinct from packed-dirt roads (D-03)"
    - "Grass is PARTIALLY thinned along footpaths (blades still poke through) — not fully cleared like a road (D-03)"
    - "The footpath tint feeds ONLY the baked terrainColorAt path — never the aRoad fragment shader (no ruts/clods on footpaths)"
    - "[human-verify] Footpaths read as worn/trampled routes along real camp↔plaza↔bridge lines and never fade"
  artifacts:
    - src/game/world/terrain.ts
    - src/game/world/grassPlacement.ts
  key_links:
    - "footpathFactor (Plan 02) is consumed by terrainColorAt AND grassPlacement — one source of truth"
    - "grass rejection uses the seeded random() already threaded through generateGrassBlades — deterministic thinning"
---

<objective>
Bake the worn-footpath look through the two existing static seams: a light trampled tint in `terrainColorAt` and probabilistic grass thinning in `grassPlacement`, both driven by `footpathFactor` (WEAR-01, D-01, D-03, D-07). This is a build-time bake — zero per-frame cost, no shader edit, and it must stay OFF the `aRoad` packed-dirt fragment path.

Purpose: Deliver the "worn but not bare" visual — blades poke through a lighter-than-road tint along the real traffic graph — reusing machinery instead of a second subsystem.
Output: Footpath tint + thinning live in the world bake, pinned by terrain and grass tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/world/terrain.ts
@src/game/world/grassPlacement.ts
@src/game/world/roads.ts
</context>

<artifacts>
## Artifacts this plan produces
- `FOOTPATH_TINT = new THREE.Color(0x7d8a54)` — module constant near `ROAD_DIRT` in `terrain.ts` (desaturated trampled-grass tone, discretion hue — A2).
- Footpath tint lerp inside `terrainColorAt` (BEFORE the road blend so road wins on overlap).
- Probabilistic footpath thinning in `generateGrassBlades` (keeps ~`1 - footpathFactor` of blades).
- Updated `terrain.test.ts` + `grassPlacement.test.ts`.
- Consumes: `footpathFactor` from `roads.ts` (Plan 02).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Footpath tint in terrainColorAt (baked vertex color only)</name>
  <files>src/game/world/terrain.ts, src/game/world/__tests__/terrain.test.ts</files>
  <read_first>
    - src/game/world/terrain.ts (ROAD_DIRT :131-132, terrainColorAt :144-158, the road blend at :155-156, the aRoad/aRoadCross attribute writes at :341-346 — do NOT touch these)
    - src/game/world/roads.ts (footpathFactor from Plan 02)
    - src/game/world/__tests__/terrain.test.ts (existing terrainColorAt assertion style)
    - 11-RESEARCH.md §"Pattern 1" tint example + §"Pitfall 2"; 11-PATTERNS.md §"terrain.ts (MOD)"
  </read_first>
  <action>
    Add `const FOOTPATH_TINT = new THREE.Color(0x7d8a54);` near `ROAD_DIRT` (:131-132) — a desaturated trampled-grass tone, LIGHTER/greener than `ROAD_DIRT 0x9a7a4e` so footpaths read distinct from packed-dirt roads (D-03; hue is discretion, A2). Inside `terrainColorAt`, import and read `footpathFactor(x,z)` and lerp `grassColor` toward `FOOTPATH_TINT` by `foot * 0.5`, placed BEFORE the existing road blend (:155-156) so the road overrides on overlap. Do NOT write `footpathFactor` into the `aRoad`/`aRoadCross` attributes or sum it with `roadFactor` at :341-346 — that would trigger the packed-dirt cart-rut fragment branch (Pitfall 2). Keep `aRoad = roadFactor` only.

    Extend `terrain.test.ts`: assert that `terrainColorAt` at a point on a footpath spine (off any road) returns a color shifted toward FOOTPATH_TINT — i.e. distinct from the pure meadow color at a far-off point AND distinct from the ROAD_DIRT color at a road-centerline point (the footpath tint is lighter/greener than road). Use `footpathFactor`/`roadFactor` to pick real sample points.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/world/__tests__/terrain.test.ts</automated>
  </verify>
  <done>FOOTPATH_TINT added; terrainColorAt lerps a light footpath tint before the road blend; aRoad attributes untouched; terrain.test.ts asserts footpath color is distinct from both meadow and road, and is green.</done>
</task>

<task type="auto">
  <name>Task 2: Partial grass thinning along footpaths + grassPlacement test recheck</name>
  <files>src/game/world/grassPlacement.ts, src/game/world/__tests__/grassPlacement.test.ts</files>
  <read_first>
    - src/game/world/grassPlacement.ts (roadFactor import :11, rejection seam :74-76, seeded random() threading :46/:66-84)
    - src/game/world/roads.ts (footpathFactor from Plan 02)
    - src/game/world/__tests__/grassPlacement.test.ts (current blade/cluster count assertions — last touched by commit d643c24)
    - 11-RESEARCH.md §"Pattern 2" + §"Pitfall 3"; 11-PATTERNS.md §"grassPlacement.ts (MOD)"
  </read_first>
  <action>
    Import `footpathFactor` alongside the existing `roadFactor` import (:11). Between the existing town-reject and the `getTerrainHeight` call (:74-76), add probabilistic footpath thinning: read `footpathFactor(x,z)`; if it is > 0 and the seeded `random()` (already threaded through `generateGrassBlades`) is < that factor, `continue` (skip the blade). Because `footpathFactor` is capped at `FOOTPATH_MAX = 0.6`, at most ~60% of blades are removed on the path spine, tapering to 0 at the edges — trampled, not cleared (D-03). Do NOT hard-reject like roads.

    Then RECHECK `grassPlacement.test.ts` (Pitfall 3): run it, and update any blade/cluster count assertions that changed due to the new deterministic thinning near path splines (mirror the d643c24 stale-assertion fix pattern — update to the new correct deterministic counts, do not weaken the test's intent). Confirm the test still asserts meadow density away from paths is unchanged.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/world/__tests__/grassPlacement.test.ts</automated>
  </verify>
  <done>Grass is probabilistically thinned along footpaths (blades still poke through); grassPlacement.test.ts is updated to the new deterministic counts and green; away-from-path density unchanged.</done>
</task>

</tasks>

<threat_model>
Client-only, cosmetic build-time bake. Terrain color and grass placement are pure deterministic functions; thinning uses the existing seeded RNG (identical bake per client). No network, auth, input, persistence, or reducers.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | (n/a) | low | accept | Pure baked geometry/color; no external input surface. No mitigation required. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed. |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/world/__tests__/terrain.test.ts src/game/world/__tests__/grassPlacement.test.ts` green.
- `pnpm test` (full suite) green.
- [human-verify — phase gate] Walk the camp↔plaza↔bridge routes on the LAN page: footpaths read as worn/trampled (lighter-than-road tint, thinned-not-cleared grass), distinct from packed-dirt roads, and never fade.
</verification>

<success_criteria>
Footpaths bake a distinct worn tint + partial thinning along the real route graph; both pure tests green; no fragment-shader/aRoad involvement.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-05-SUMMARY.md` when done.
</output>
