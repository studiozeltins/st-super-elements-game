---
phase: 11-lived-in-props-wear
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/world/roads.ts
  - src/game/world/__tests__/roads.test.ts
autonomous: true
requirements: [WEAR-01]
must_haves:
  truths:
    - "A worn-path route graph exists along REAL traffic (plaza↔bridge, bridge↔camp, plaza↔camp, city camp↔camp) — data-driven, never magic coordinates (D-02)"
    - "footpathFactor is a narrower, PARTIAL sibling of roadFactor (capped, never a full clear) (D-01)"
    - "No footpath segment ever crosses water — endpoints are same-island only"
  artifacts:
    - src/game/world/roads.ts
    - src/game/world/__tests__/roads.test.ts
  key_links:
    - "getFootpaths()/footpathFactor() reuse the existing private smoothstep + distanceToSegment helpers — no duplication (D-01)"
    - "footpathFactor is the single source consumed downstream by terrainColorAt, grassPlacement, and surfaceAt (Plans 05, 06)"
    - "footpath tier must stay OUT of the aRoad/roadAcross attribute path (packed-dirt cart-rut fragment trap)"
---

<objective>
Add a worn "footpath" tier to `roads.ts` as a narrower, partial sibling of the road system: `getFootpaths()` (a memoized route graph over the real traffic lines) and `footpathFactor(x,z)` (a capped, partial spline mask). This is the foundation the terrain tint, grass thinning, and surface classifier all read (WEAR-01, D-01, D-02).

Purpose: One shared spline machinery serves both roads and footpaths — no second subsystem. Getting the route graph and the partial-factor semantics right here unblocks all three downstream footpath consumers.
Output: `getFootpaths()` + `footpathFactor()` exported from `roads.ts`, pinned by a new `roads.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/world/roads.ts
@src/game/world/camps.ts
@src/game/world/bridges.ts
</context>

<artifacts>
## Artifacts this plan produces
- `export function getFootpaths(): RoadPoint[][]` — memoized (module-level `cachedFootpaths`) route polylines in `roads.ts`.
- `export function footpathFactor(x: number, z: number): number` — returns `[0, FOOTPATH_MAX]`; partial by construction.
- Module constants `FOOTPATH_HALF_WIDTH ≈ 1.1` (narrower than `ROAD_HALF_WIDTH 2.5`) and `FOOTPATH_MAX ≈ 0.6` (discretion values, tune in UAT — A2).
- New `src/game/world/__tests__/roads.test.ts` pinning footpathFactor contract.
- NO `footpathAcross`/`aFootpath` attribute (deliberate — see anti-pattern).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Add getFootpaths() route graph + footpathFactor() to roads.ts</name>
  <files>src/game/world/roads.ts</files>
  <read_first>
    - src/game/world/roads.ts (roadFactor :95-105, getRoads memoize :26/:37-64, smoothstep :66-70, distanceToSegment :72-87, ROAD_HALF_WIDTH :19, RoadPoint type)
    - src/game/world/camps.ts (getCampSites() :29, CampSite {x,z,archetypeId})
    - src/game/world/bridges.ts (getBridges() :31, BridgeSpec {startX,startZ,endX,endZ})
    - 11-RESEARCH.md §"Pattern 1" (footpathFactor example, same-island rationale) and §"Open Questions 1" (same-island pairing decision)
    - 11-PATTERNS.md §"roads.ts (MOD)" (helper reuse + memoize + anti-pattern warning)
  </read_first>
  <action>
    Add `FOOTPATH_HALF_WIDTH = 1.1` and `FOOTPATH_MAX = 0.6` module constants near `ROAD_HALF_WIDTH`. Add a memoized `getFootpaths(): RoadPoint[][]` mirroring `getRoads`'s lazy-cache shape (module-level `cachedFootpaths`), building route polylines from data-driven anchors only (D-02): for each bridge — a plaza-exit→bridge-start segment (city side) and a bridge-end→nearest-outer-camp segment (outer side); plus plaza-origin→each city-island camp and city-camp↔city-camp. CRITICAL: every segment connects two endpoints on the SAME island so `distanceToSegment` never bakes a path across the water gap — the bridge itself carries the crossing (D-02, Open Question 1 resolution: same-island direct footpaths; outer camps reached via plaza→bridge→camp). Add `footpathFactor(x,z)` as a narrower/partial sibling of `roadFactor` — same double loop over `getFootpaths()` reusing the existing private `smoothstep` and `distanceToSegment` (do NOT duplicate them, D-01), using `FOOTPATH_HALF_WIDTH`, and returning `best * FOOTPATH_MAX` so the factor is partial by construction (never a full clear like a road).

    Do NOT add a `footpathAcross`/`aFootpath` function or attribute. The `roadAcross` (:113-133) feeds the packed-dirt cart-rut fragment shader; footpaths must stay off that path entirely (Pitfall 2 / Pattern 1 key insight). Leave `roadFactor`/`roadAcross` and the `aRoad`/`aRoadCross` attributes untouched.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/world/__tests__/bridges.test.ts src/game/world/__tests__/camps.test.ts</automated>
  </verify>
  <done>`getFootpaths()` and `footpathFactor()` are exported; helpers are reused not duplicated; no `aFootpath`/`footpathAcross` added; existing bridges/camps tests still green (the anchors they cover are unchanged).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin footpathFactor contract with roads.test.ts</name>
  <files>src/game/world/__tests__/roads.test.ts</files>
  <read_first>
    - src/game/world/roads.ts (the new getFootpaths/footpathFactor from Task 1, plus roadFactor for the comparison)
    - src/game/world/__tests__/bridges.test.ts and camps.test.ts (existing vitest style + how anchors are asserted)
    - src/game/world/camps.ts, bridges.ts (to reference real endpoint coordinates in assertions)
  </read_first>
  <behavior>
    - A point exactly on a city-island camp position (a route endpoint) has footpathFactor > 0.
    - A point far from every route (deep off-island, e.g. a large-radius open-sea coordinate) has footpathFactor === 0.
    - footpathFactor is capped: for every sampled point it is <= FOOTPATH_MAX (0.6) — partial, never a full clear.
    - The midpoint over the water gap BETWEEN two islands (halfway on a straight line between a city camp and an outer camp, off any bridge) has footpathFactor === 0 — no path baked across water.
    - getFootpaths() returns a non-empty array and is referentially stable across two calls (memoized).
  </behavior>
  <action>
    Create `src/game/world/__tests__/roads.test.ts` (new file — none exists) asserting the footpathFactor contract in the <behavior> block. Import `getFootpaths`, `footpathFactor` from `../roads`, `getCampSites` from `../camps`, `getBridges` from `../bridges`. Derive test points from real data (a camp coordinate, a bridge start, a computed inter-island midpoint) rather than magic numbers so the assertions track the data-driven route graph. Assert the cap by sampling a grid of points and checking the max never exceeds FOOTPATH_MAX. Keep assertions robust to the exact discretionary pairing (test properties that hold for ANY valid same-island route graph, not one specific endpoint pairing).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/world/__tests__/roads.test.ts</automated>
  </verify>
  <done>`roads.test.ts` exists and is green; it proves on-route>0, off-route=0, capped ≤0.6, and no across-water baking.</done>
</task>

</tasks>

<threat_model>
Client-only, cosmetic geometry math. `getFootpaths`/`footpathFactor` are pure, deterministic, and read only fixed in-repo world data (camps/bridges/origin). No network, auth, input, persistence, or reducers touched. Determinism (identical route bake on every client) is guaranteed by fixed data + no RNG here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | (n/a) | low | accept | Pure geometry over fixed data; no external input surface. No mitigation required. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed (locked zero-new-dependencies). |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/world/__tests__/roads.test.ts` green.
- `pnpm test` (full suite) green.
</verification>

<success_criteria>
A partial, same-island-only footpath route graph exists in `roads.ts` and is behaviorally pinned; downstream consumers (Plans 05, 06) can read `footpathFactor`.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-02-SUMMARY.md` when done.
</output>
