---
phase: 11-lived-in-props-wear
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/createDustPuffs.ts
  - src/game/systems/__tests__/createDustPuffs.test.ts
autonomous: true
requirements: [WEAR-05]
must_haves:
  truths:
    - "A ground-hugging pooled dust system exists: spawn() emits a short low puff, update() ages the pool, dispose() frees it (D-11)"
    - "The pool is hard-capped at DUST_POOL_SIZE (24) — over-spawning never grows live instances beyond the cap (unbounded-growth guard)"
    - "Zero per-frame allocation: all scratch (Matrix4/Vector3/etc.) constructed once at closure level (144→20fps cliff class)"
  artifacts:
    - src/game/systems/createDustPuffs.ts
    - src/game/systems/__tests__/createDustPuffs.test.ts
  key_links:
    - "InstancedMesh added to scene ROOT, never the frozen world group"
    - "Opaque Lambert + stepped color fade (never alpha — bands under the nearest-neighbor pixel filter)"
---

<objective>
Add `createDustPuffs` — a ground-hugging pooled `InstancedMesh` dust system built as a copy-with-deltas of `createSmokeColumns` (WEAR-05, D-11). Unlike smoke (self-emitting camp fires), dust is externally spawned by the moving player, so it exposes a `spawn()` method; `update()` only ages/settles live puffs. Wiring into the game loop happens in Plan 08.

Purpose: This is the phase's ONLY new per-frame draw-call source. Building it in isolation with a hard-cap + zero-alloc test protects the documented FPS cliff and keeps the `?nodust` bisect clean.
Output: `createDustPuffs.ts` factory + a pool-cap/expiry/dispose test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/systems/createSmokeColumns.ts
@src/game/systems/__tests__/entityRenderers.test.ts
</context>

<artifacts>
## Artifacts this plan produces
- `export interface DustPuffs { spawn(x: number, z: number, dirX: number, dirZ: number): void; update(deltaSeconds: number): void; dispose(): void; }`
- `export function createDustPuffs(scene: THREE.Scene, getGroundHeight: (x: number, z: number) => number): DustPuffs`
- Module constants: `DUST_POOL_SIZE = 24` (D-11); ground-hug tuning `MAX_RISE ≈ 0.4`, low `RISE_SPEED`, `PUFF_LIFE ≈ 0.4–0.6s`, smaller `SIZE_TIERS` (~`[0.18,0.14,0.10,0.06]`), dusty-tan opaque fade palette (discretion — A4).
- New `createDustPuffs.test.ts` (pool cap + expiry + dispose).
- NOTE for Plan 08: signature is `createDustPuffs(scene, getGroundHeight)`; spawn takes `(x, z, dirX, dirZ)`; update takes `(deltaSeconds)` only (dust ages the already-spawned pool — spawning is external).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createDustPuffs.ts ground-hugging pool + pool-cap/expiry test</name>
  <files>src/game/systems/createDustPuffs.ts, src/game/systems/__tests__/createDustPuffs.test.ts</files>
  <read_first>
    - src/game/systems/createSmokeColumns.ts (interface :13-19, pool size :19, tuning constants :26-40, fade palette :66-69, InstancedMesh setup + flags :83-99, closure scratch :101-107, slot-claim/hard-cap :110-131, age/move/recycle + needsUpdate gating :134-199, dispose :201-208)
    - src/game/systems/__tests__/entityRenderers.test.ts (headless THREE object testing style — constructing a Scene, asserting on InstancedMesh, no WebGL needed)
    - 11-RESEARCH.md §"Pattern 4" (the smoke→dust delta table) and §"Anti-Patterns" (opaque not alpha; no per-frame alloc; scene root not frozen group)
    - 11-PATTERNS.md §"createDustPuffs.ts" (verbatim-copy checklist)
  </read_first>
  <behavior>
    - After calling spawn() 30 times (with the pool cap at 24), the number of live/active puffs never exceeds DUST_POOL_SIZE (24) — the slot-claim scan returns without growing the pool.
    - A spawned puff becomes inactive after PUFF_LIFE seconds of update(dt) (call update enough total dt to exceed PUFF_LIFE, then assert the live count drops).
    - update(dt) performs zero allocations of Matrix4/Vector3/Quaternion/Color (verified structurally: those constructors appear only at closure scope, asserted by reading the pool has fixed-length arrays; behaviorally: repeated update calls do not change pool array length).
    - dispose() removes the InstancedMesh from the scene and can be called without throwing.
    - The InstancedMesh is parented to the passed scene (scene.children includes it), uses an opaque MeshLambertMaterial, has frustumCulled=false and castShadow=false.
  </behavior>
  <action>
    Create `createDustPuffs.ts` by copying the STRUCTURE of `createSmokeColumns.ts` and applying the ground-hug deltas: `DUST_POOL_SIZE = 24`; a `spawn(x, z, dirX, dirZ)` method that claims the first inactive slot (the smoke slot-claim scan, returning early when full — the hard cap) and initializes a puff at ground height (`getGroundHeight(x,z)`) with a low upward kick (~0.15–0.3) plus a small backward/outward offset from the passed movement direction; `update(deltaSeconds)` that ages, rises slightly, settles, and recycles puffs past `PUFF_LIFE`, reusing the smoke `matrixDirty`/`colorDirty` needsUpdate gating and `scratchMatrix.compose(...)`; and `dispose()` copied verbatim (scene.remove, geometry/material dispose, mesh.dispose()). Copy the closure-level scratch block verbatim (constructed ONCE — never `new Matrix4()`/`new Vector3()`/`new Color()` inside spawn/update; the 144→20fps cliff class). Use an OPAQUE `MeshLambertMaterial` with a precomputed stepped tan→fade color palette (never alpha — bands under the nearest-neighbor pixel filter). Add the InstancedMesh to the `scene` ROOT (never the frozen world group). Set `frustumCulled=false`, `castShadow=false`, `receiveShadow=false`, `DynamicDrawUsage` on instanceMatrix + instanceColor — verbatim from smoke.

    Then write `createDustPuffs.test.ts` (headless, no WebGL — mirror entityRenderers.test.ts) asserting the <behavior> block: construct with a `new THREE.Scene()` and a stub `getGroundHeight` returning 0; over-spawn and assert live count ≤ 24; advance update past PUFF_LIFE and assert expiry; assert scene parenting + material opacity + flags; assert dispose() runs clean. Tests-first: pin the cap/expiry behavior, then implement to green.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/createDustPuffs.test.ts</automated>
  </verify>
  <done>createDustPuffs.ts exports the DustPuffs interface + factory; the pool is hard-capped at 24 and puffs expire; scratch is closure-level (no per-frame alloc); dust is opaque and on the scene root; test file green.</done>
</task>

</tasks>

<threat_model>
Client-only, cosmetic render system. `createDustPuffs` takes only a scene + a pure height function; it processes no external input, no network, no auth, no persistence. The `?nodust` URL flag (consumed in Plan 08) is a boolean presence check that disables a cosmetic system — no privilege or state change.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | ?nodust URL flag (wired Plan 08) | low | accept | Boolean presence only; disables a cosmetic pool. Matches the established `?no*` convention. No mitigation required. |
| T-11-02 | Denial of Service | dust pool growth | low | mitigate | Hard cap at DUST_POOL_SIZE=24 enforced by the slot-claim scan; asserted by the pool-cap test (unbounded-growth guard). |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed (existing three only). |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/systems/__tests__/createDustPuffs.test.ts` green.
- `pnpm test` (full suite) green.
</verification>

<success_criteria>
A hard-capped, zero-alloc, opaque ground-hugging dust pool exists and is behaviorally pinned, ready to wire in Plan 08.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-04-SUMMARY.md` when done.
</output>
