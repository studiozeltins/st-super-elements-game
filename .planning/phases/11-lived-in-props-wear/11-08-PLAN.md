---
phase: 11-lived-in-props-wear
plan: 08
type: execute
wave: 3
depends_on: ["11-04", "11-06"]
files_modified:
  - src/game/audio/createMovementAudio.ts
  - src/game/createGame.ts
autonomous: false
requirements: [WEAR-05]
must_haves:
  truths:
    - "Running over dirt/path/town puffs pooled ground-hug dust; running over grass never does (D-11, D-12)"
    - "The footstep-audio surface is the real surfaceAt() value, not hard-coded 'grass' (D-13)"
    - "?nodust disables the dust pool entirely (construction skipped) — a clean FPS bisect (D-14)"
    - "surfaceAt() is called ONCE per frame at the player step and shared by dust + audio"
    - "[human-verify] Dust is subtle + ground-hugging on dirt/path only; FPS holds with all ambiance enabled"
  artifacts:
    - src/game/audio/createMovementAudio.ts
    - src/game/createGame.ts
  key_links:
    - "createGame WIRES only — all dust logic stays in createDustPuffs (createGame is the ~1,963 LOC worst offender)"
    - "dust InstancedMesh lives on scene root; dustPuffs?.dispose() joins teardown beside smoke"
    - "FootstepSurface (audio) and Surface (surfaceAt) share the same four tags"
---

<objective>
Wire the dust pool + surface classifier into the game loop: widen the footstep-audio surface type, add the `?nodust` flag, construct `createDustPuffs`, compute `surfaceAt()` once per frame at the player step, spawn dust on non-grass surfaces, thread the real surface into footstep audio, and add the per-frame `.update()` + teardown (WEAR-05, D-11..D-14). Then a phase-gate human verification.

Purpose: This is the only integration point — everything built in Waves 1-2 comes together here. Keep `createGame` thin (wire only) and preserve the FPS discipline (`?nodust` bisect, one classifier call).
Output: Live dust on dirt/path/town, surface-aware footsteps, a clean bisect flag, and a signed-off perceptual/FPS gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/createGame.ts
@src/game/audio/createMovementAudio.ts
@src/game/systems/createDustPuffs.ts
@src/game/systems/surfaceAt.ts
</context>

<artifacts>
## Artifacts this plan produces
- `FootstepSurface` widened `'grass'` → `'grass' | 'dirt' | 'path' | 'town'` in `createMovementAudio.ts` (aligned with `surfaceAt`'s `Surface`).
- `?nodust` perf-bisect flag + comment in `createGame.ts` (`const dustEnabled = !perfFlags.has('nodust')`).
- `const dustPuffs = dustEnabled ? createDustPuffs(scene, (x,z) => world.getGroundHeight(x,z)) : undefined;`
- One `surfaceAt(px,pz)` call per frame at the grounded player step, shared by dust spawn + footstep audio.
- `dustPuffs?.update(deltaSeconds)` after the smoke update line; `dustPuffs?.dispose()` at teardown.
- Consumes: `createDustPuffs` (Plan 04), `surfaceAt` (Plan 06).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Widen FootstepSurface to the four surface tags</name>
  <files>src/game/audio/createMovementAudio.ts</files>
  <read_first>
    - src/game/audio/createMovementAudio.ts (FootstepSurface :14, updateUnit surface? :25/:238-246, playStep :215-236, grass-rustle gate :232)
    - src/game/systems/surfaceAt.ts (the Surface type from Plan 06 — the tag set to match)
    - 11-PATTERNS.md §"createMovementAudio.ts (MOD)"
  </read_first>
  <action>
    Widen `export type FootstepSurface = 'grass';` (:14) to `'grass' | 'dirt' | 'path' | 'town'`, matching the `Surface` tag set from `surfaceAt.ts` (align the two — either widen to the identical union or re-export/import one from the other to keep a single tag set). No other behavior change: the grass-rustle gate at :232 already fires only on `=== 'grass'`, so non-grass surfaces simply skip the rustle (the intended D-13 bonus — no new audio code).
  </action>
  <verify>
    <automated>pnpm exec vitest run</automated>
  </verify>
  <done>FootstepSurface accepts all four tags aligned with surfaceAt.Surface; the rustle still fires only on grass; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dust pool + surfaceAt + ?nodust flag into createGame</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts (flag comment :330-332, nobend :341, nosmoke :345, smoke construction :414-418, player step/wear stamp block ~:1006, footstep updateUnit hard-coded 'grass' :1305-1313, smoke update line :1484, smoke dispose :1652)
    - src/game/systems/createDustPuffs.ts (signature createDustPuffs(scene, getGroundHeight); spawn(x,z,dirX,dirZ); update(deltaSeconds); dispose — Plan 04 artifacts note)
    - src/game/systems/surfaceAt.ts (surfaceAt(x,z) — Plan 06)
    - 11-RESEARCH.md §"Pattern 4" spawn gating + §"Pitfall 4"; 11-PATTERNS.md §"createGame.ts (MOD 1–5)"
  </read_first>
  <action>
    WIRE ONLY — all dust logic stays in createDustPuffs; createGame is the ~1,963 LOC worst offender.
    (1) Add `const dustEnabled = !perfFlags.has('nodust');` beside `smokeEnabled` (:345) and add `?nodust` to the flag comment at :330-332 (D-14).
    (2) Beside the smoke construction (:414-418), add `const dustPuffs = dustEnabled ? createDustPuffs(scene, (x, z) => world.getGroundHeight(x, z)) : undefined;` (import createDustPuffs). `?nodust` skips construction entirely — zero objects, clean bisect.
    (3) Import `surfaceAt`. At the grounded player-step site, compute `const surface = surfaceAt(playerPosition.x, playerPosition.z);` EXACTLY ONCE per frame and share it (D-12 single classifier call). There is NO sprint state (grep-verified) — gate dust on movement + grounded: when the player is moving and grounded and `surface !== 'grass'`, call `dustPuffs?.spawn(playerPosition.x, playerPosition.z, worldMoveX, worldMoveZ)` (spawn at the cleaner `updateLocalPlayer` site ~:1006 where `isMoving`/`worldMoveX,Z`/`isGrounded()` are already in scope — RESEARCH A5). 
    (4) Replace the hard-coded `'grass'` in the footstep `movementAudio.updateUnit(...)` call (:1313) with the shared `surface` value (D-13). If the spawn site and the audio site are in different functions, thread the single computed `surface` value through rather than calling `surfaceAt` twice.
    (5) Add `dustPuffs?.update(deltaSeconds);` immediately after `smokeColumns?.update(...)` (:1484), and `dustPuffs?.dispose();` beside the smoke dispose (:1652).
    Do NOT allocate per frame (Pitfall 4). The bend/scorch retunes already flow through the existing groundInfluence/scorch update lines — no change there.
  </action>
  <verify>
    <automated>pnpm exec vitest run</automated>
  </verify>
  <done>?nodust flag + comment added; dustPuffs constructed conditionally; surfaceAt computed once per frame and shared; dust spawns on non-grass only; footstep audio uses the real surface; dust update + dispose wired; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human playtest — verify all Phase 11 success criteria + FPS gate</name>
  <files>none (checkpoint — no code changes)</files>
  <action>
    Present the checklist below to the developer, wait for the playtest result, and record the outcome (approved / itemized per-requirement failures) in the plan SUMMARY. Do not proceed past this task without an explicit human response.
  </action>
  <what-built>
    The full Phase 11 lived-in world: worn footpaths (tint + partial thinning) along real routes, lived-in plaza props, ~2s grass-bend trail, scorch healing over minutes, and ground-hug dust on dirt/path/town. This checkpoint is the phase-wide perceptual + performance gate (VALIDATION.md Manual-Only table).
  </what-built>
  <how-to-verify>
    On the LAN build (`pnpm build`, laragon-served page — NOT the dev server):
    1. WEAR-01: Walk the camp↔plaza↔bridge routes — footpaths read worn/trampled (lighter-than-road tint, grass thinned but blades still poke through), distinct from packed-dirt roads, and never fade.
    2. WEAR-02: Inspect the plaza — crates/barrels stacked at the market edge facing the fountain, fences at path/plaza gaps; arrangement reads deliberate ("who put this here"), and you path around props.
    3. WEAR-03/04: Fight to scorch the ground, leave and return after ~1–3 min (scorch still visible on quick return, healed after longer); while running, watch your own grass-bend trail fade in ~2s.
    4. WEAR-05: Run over grass (NO dust) vs dirt/path/town (subtle ground-hug puffs, not a spray). Append `?nodust` to the URL and confirm the puffs disappear (bisect flag works).
    5. Perf gate: run `scripts/fps_playtest.py` in a golem-class fight with wind + day/night + audio + wear all enabled; confirm no FPS regression vs baseline (toggle `?nodust` to isolate dust cost if needed).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per-requirement) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
Client-only, cosmetic integration. The only external input is the `?nodust` URL flag (boolean presence, disables a cosmetic pool — established `?no*` convention). No network, auth, input parsing, persistence, or reducers. `surfaceAt`/dust are zero-alloc, no GPU read (perf discipline preserved).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | ?nodust URL flag | low | accept | Boolean presence only; disables a cosmetic system, no privilege/state change. Matches the `?no*` convention. |
| T-11-02 | Denial of Service | per-frame dust/surface cost | medium | mitigate | Dust hard-capped (Plan 04); surfaceAt is one pure call/frame shared by dust+audio; `?nodust` bisect + `scripts/fps_playtest.py` gate confirm no regression. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed (locked zero-new-dependencies). |
</threat_model>

<verification>
- `pnpm exec vitest run` (full suite) green.
- Human-verify checkpoint approved (WEAR-01/02/03/04/05 perceptual + `?nodust` bisect + FPS non-regression).
</verification>

<success_criteria>
Dust spawns on dirt/path/town only (never grass), footsteps use the real surface, `?nodust` cleanly disables the pool, FPS holds, and the phase-wide perceptual gate is signed off.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-08-SUMMARY.md` when done.
</output>
