---
phase: 11-lived-in-props-wear
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/world/assets/createCrate.ts
  - src/game/world/assets/createBarrel.ts
  - src/game/world/assets/createFence.ts
  - src/game/world/assets/__tests__/assets.test.ts
autonomous: true
requirements: [WEAR-02]
must_haves:
  truths:
    - "createCrate, createBarrel, createFence each return a WorldAsset (merged-box voxel body + collision obstacles) (D-08)"
    - "Props carry NO PointLight — light-pool discipline (D-10)"
    - "Factories are deterministic under a fixed SeededRandom (identical props on every client)"
  artifacts:
    - src/game/world/assets/createCrate.ts
    - src/game/world/assets/createBarrel.ts
    - src/game/world/assets/createFence.ts
    - src/game/world/assets/__tests__/assets.test.ts
  key_links:
    - "Each factory declares WorldAsset.obstacles so placeAsset (Plan 07) pushes footprints into the collision set"
    - "Fence is a whole RUN (posts+rails merged) in one factory, one obstacle per post"
---

<objective>
Add three frozen-matrix voxel prop factories — `createCrate`, `createBarrel`, `createFence` — as box-voxel siblings of `createLantern`/`createCampfire`, each returning a `WorldAsset` with collision obstacles and NO light. These are the lived-in plaza props (WEAR-02, D-08, D-10). Placement into the world happens in Plan 07.

Purpose: Isolate the pure asset construction (own files, ≤300 LOC each) from the world-wiring; keep `createGame`/`createMondstadtWorld` thin.
Output: Three new factory files + determinism/shape assertions in the existing `assets.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/world/assets/createLantern.ts
@src/game/world/assets/createCampfire.ts
@src/game/world/assets/assetHelpers.ts
@src/game/world/assets/types.ts
@src/game/world/assets/__tests__/assets.test.ts
</context>

<artifacts>
## Artifacts this plan produces
- `export function createCrate(random: SeededRandom): WorldAsset`
- `export function createBarrel(random: SeededRandom): WorldAsset`
- `export function createFence(random: SeededRandom): WorldAsset` (a full run: posts + rails merged)
- Extended `assets.test.ts` covering shape + determinism + obstacle presence for all three.
- No new lights, no exported light-name constants (props are lightless — D-10).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: createCrate.ts and createBarrel.ts voxel factories</name>
  <files>src/game/world/assets/createCrate.ts, src/game/world/assets/createBarrel.ts</files>
  <read_first>
    - src/game/world/assets/createLantern.ts (box() :7-11, mergedMesh() :12-16 helpers to copy verbatim; factory signature + seeded yaw)
    - src/game/world/assets/assetHelpers.ts (edgeLit, lambert, randomBetween, randomIntBetween exports)
    - src/game/world/assets/types.ts (SeededRandom :3, AssetObstacle :14, WorldAsset :22 with obstacles? :35)
    - 11-RESEARCH.md §"Pattern 3" and 11-PATTERNS.md §"createCrate.ts / createBarrel.ts / createFence.ts"
  </read_first>
  <action>
    Create `createCrate.ts`: a box-voxel crate built from six pre-translated plank geometries framing a cube via the `box(w,h,d,x,y,z)` helper (copy box/mergedMesh from createLantern verbatim — do not re-import from createLantern; these are tiny local helpers per the existing convention), merged into one `THREE.Mesh` with `edgeLit(CRATE_WOOD)` (flat-shaded, crisp voxel read). Seed scale via `randomBetween(random, 0.7, 0.95)` and a small seeded yaw via `randomBetween(random, -0.15, 0.15)`. Return `{ group, obstacles: [{ x: 0, z: 0, radius: s*0.6, height: s }] }` so players path around it. NO PointLight (D-10).

    Create `createBarrel.ts`: same pattern, a taller/rounder silhouette (a stacked octagonal-ish box body — e.g. a wider mid-band box between two narrower cap boxes, or simple stacked boxes) with a distinct wood/metal-hoop tone. Same seeded scale/yaw and a single circular obstacle. NO light. (Barrel ships per RESEARCH Open Question 2 recommendation — trivial crate variant; keep it if LOC budget allows, which it does.)

    Keep each file well under 300 functional LOC. Import `edgeLit`/`randomBetween` from `./assetHelpers` and `SeededRandom`/`WorldAsset` from `./types`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/world/assets/__tests__/assets.test.ts</automated>
  </verify>
  <done>createCrate.ts and createBarrel.ts exist, export their factories, return a WorldAsset with a non-empty group and ≥1 obstacle, and carry no light; existing assets test still green.</done>
</task>

<task type="auto">
  <name>Task 2: createFence.ts run factory + assets.test.ts coverage</name>
  <files>src/game/world/assets/createFence.ts, src/game/world/assets/__tests__/assets.test.ts</files>
  <read_first>
    - src/game/world/assets/createLantern.ts (:71-104 merged multi-part run pattern — posts + horizontal members in one group/mesh)
    - src/game/world/assets/__tests__/assets.test.ts (existing describe/it style, how it constructs a SeededRandom, how it asserts on returned WorldAssets)
    - src/game/world/assets/createCrate.ts, createBarrel.ts (from Task 1)
  </read_first>
  <action>
    Create `createFence.ts`: a whole fence RUN in one factory — N posts (e.g. 3–5) along a line with 1–2 horizontal rails between them, all boxes merged into one draw call via mergedMesh + edgeLit(FENCE_WOOD). Return `{ group, obstacles: [...] }` with one small obstacle per post (like the lantern's multi-part single group). Seed post count/jitter via the passed `SeededRandom`. NO light (D-10).

    Extend `assets.test.ts` with a describe block covering all three new factories: each returns a WorldAsset whose `group` has children (a merged mesh) and whose `obstacles` array is non-empty; and each is DETERMINISTIC — calling the factory twice with two equal-seed `createSeededRandom` instances produces the same obstacle count and same group child count. Use the seeded RNG helper the same way the existing tests do. Assert no light: the returned group (and its descendants) contains zero `THREE.Light` instances (traverse and check).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/world/assets/__tests__/assets.test.ts</automated>
  </verify>
  <done>createFence.ts exists and returns a merged run with per-post obstacles; assets.test.ts asserts shape + determinism + lightlessness for all three factories and is green.</done>
</task>

</tasks>

<threat_model>
Client-only, cosmetic build-time geometry. Factories are pure functions of a seeded RNG; no network, auth, input, persistence, or reducers. Determinism (identical props per client) is guaranteed by the seeded RNG (verified in Task 2 tests).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | (n/a) | low | accept | Pure geometry; no external input surface. No mitigation required. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed (uses existing three + BufferGeometryUtils addon). |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/world/assets/__tests__/assets.test.ts` green.
- `pnpm test` (full suite) green.
</verification>

<success_criteria>
Three lightless, deterministic voxel prop factories exist with collision footprints, ready for placement in Plan 07.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-03-SUMMARY.md` when done.
</output>
