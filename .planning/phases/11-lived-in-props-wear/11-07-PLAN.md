---
phase: 11-lived-in-props-wear
plan: 07
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - src/game/world/createMondstadtWorld.ts
autonomous: true
requirements: [WEAR-02]
must_haves:
  truths:
    - "Crates/barrels are stacked at the market-tile edge facing the fountain, and short fence runs sit at plaza-boundary / path-entry gaps (D-09)"
    - "All props are placed deterministically (seeded off WORLD_DECOR_SEED) BEFORE the world freeze (D-09)"
    - "Props declare collision footprints so players path around them"
    - "[human-verify] The plaza reads lived-in — arrangement answers 'who put this here', not random scatter"
  artifacts:
    - src/game/world/createMondstadtWorld.ts
  key_links:
    - "placeAsset (before the L660-661 freeze) parents each prop to the frozen group and pushes obstacles"
    - "Market anchors come from TOWN_DISTRICTS (market-e/market-ne) + fountain origin — data-driven, not magic coordinates"
    - "Consumes createCrate/createBarrel/createFence (Plan 03)"
---

<objective>
Place the crate/barrel/fence props into the plaza at build time — stacked at the market-tile edge near the fountain and as short runs at plaza-boundary/path gaps — deterministically seeded and BEFORE the world freeze (WEAR-02, D-09, D-10). Arrangement answers "who put this here".

Purpose: Turn the prop factories (Plan 03) into a lived-in plaza read using data-driven anchors, with zero per-frame cost (frozen-matrix static meshes).
Output: Deterministic prop placement wired into `createMondstadtWorld`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/world/createMondstadtWorld.ts
@src/game/world/town/townPlan.ts
@src/game/world/assets/createLantern.ts
</context>

<artifacts>
## Artifacts this plan produces
- Prop placement block in `createMondstadtWorld.ts` (own seeded RNG `WORLD_DECOR_SEED ^ 0xc4a7e`), placed between the existing decor block (~:600-652) and the freeze (:660-661).
- ~6–10 crates/barrels stacked at the market-tile edge facing the fountain; 2–3 short fence runs at plaza-boundary/path gaps (counts are discretion — D-09).
- Consumes: `createCrate`/`createBarrel`/`createFence` (Plan 03); `TOWN_DISTRICTS` (market-e cx=15 cz=0, market-ne cx=15 cz=-15; DISTRICT_HALF=7.5) from `town/townPlan.ts`.
- No new lights (D-10).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic plaza prop placement before the world freeze</name>
  <files>src/game/world/createMondstadtWorld.ts</files>
  <read_first>
    - src/game/world/createMondstadtWorld.ts (imports :10-33, WORLD_DECOR_SEED :121, createSeededRandom :4/:358, placeAsset :362-384, lantern-ring precedent :579-620, freeze :660-661, addInstancedMatrices :407-417)
    - src/game/world/town/townPlan.ts (TOWN_DISTRICTS :54-63 — market-e/market-ne tiles; DISTRICT_HALF :27; STEP :29)
    - 11-RESEARCH.md §"Pattern 3" placement example + §"Pitfall 5" (place before the freeze); 11-PATTERNS.md §"createMondstadtWorld.ts (MOD)"
  </read_first>
  <action>
    Import `createCrate`, `createBarrel`, `createFence` from `./assets/...` and (if not already) the market district anchors from `town/townPlan.ts`. Create an own-salt seeded RNG `const propRandom = createSeededRandom(WORLD_DECOR_SEED ^ 0xc4a7e);` (own salt so counts/jitter are deterministic and independent of prior decor draws — D-09). Using the `market-e` (cx=STEP, cz=0) and `market-ne` (cx=STEP, cz=-STEP) tile edges (DISTRICT_HALF=7.5) as data-driven anchors, stack ~6–10 crates/barrels along the market-tile edge FACING the fountain at origin (the "who put this here" read — a market with goods stacked toward the square), each via `placeAsset(createCrate(propRandom)|createBarrel(propRandom), x, z, collisionRadius)`. Run 2–3 short fences via `placeAsset(createFence(propRandom), x, z)` at plaza-boundary gaps / path entries. Vary positions with small seeded jitter for a natural stacked look; keep clusters deliberate, not scattered. ALL placement MUST go between the existing decor block (~:600-652) and the freeze at :660-661 (Pitfall 5 — runtime scene adds to the frozen group are forbidden). Do NOT add any PointLight (D-10). Counts/exact arrangement are discretion — tune for read, not quantity.
  </action>
  <verify>
    <automated>pnpm exec vitest run</automated>
  </verify>
  <done>Crates/barrels stacked at the market edge facing the fountain and short fence runs at path/plaza gaps are placed deterministically before the freeze; obstacles are registered; no lights added; full suite still green (imports compile, no regression).</done>
</task>

</tasks>

<threat_model>
Client-only, cosmetic build-time placement. Placement is deterministic (seeded off WORLD_DECOR_SEED) so every client bakes identical props; no network, auth, input, persistence, or reducers.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | (n/a) | low | accept | Pure seeded placement over fixed anchors; no external input surface. No mitigation required. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed. |
</threat_model>

<verification>
- `pnpm exec vitest run` (full suite) green — confirms imports compile and nothing regressed.
- [human-verify — phase gate] Load the plaza: crates/barrels stacked at the market edge, fences at path/plaza gaps; the arrangement reads deliberate ("who put this here"), not random; players path around props.
</verification>

<success_criteria>
The plaza reads lived-in with deterministically-placed, collidable, lightless props positioned at data-driven anchors before the freeze.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-07-SUMMARY.md` when done.
</output>
