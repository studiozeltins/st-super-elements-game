---
phase: 11-lived-in-props-wear
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/groundInfluenceMath.ts
  - src/game/systems/__tests__/groundInfluenceMath.test.ts
autonomous: true
requirements: [WEAR-03, WEAR-04]
must_haves:
  truths:
    - "Player's grass-bend trail fades to <10% within ~2s (D-04, D-05)"
    - "Scorch/wear still visibly reads at ~1min and heals within ~3min (D-06)"
    - "Decay stays frame-rate independent (composition invariant holds)"
  artifacts:
    - src/game/systems/groundInfluenceMath.ts
    - src/game/systems/__tests__/groundInfluenceMath.test.ts
  key_links:
    - "The one WEAR_REGROW_TIME_CONSTANT_SECONDS is read by BOTH createScorchMap (R) and createGroundInfluence (wear A) — one edit heals both"
    - "decayForDelta / wearDecayForDelta feed the existing per-frame ping-pong fade shaders — no wiring change"
---

<objective>
Retune the two shared decay constants in the pure ground-influence math so the grass-bend trail reads ~2s (WEAR-04) and scorch/wear heal over minutes (WEAR-03), and update the stale unit test that pins the OLD behavior — all in one change (no-legacy-code rule).

Purpose: These are the highest-confidence, tests-first edits; they unblock the perceptual timing UAT and carry zero new per-frame cost (pure constants flowing through the existing fade shaders).
Output: Two retuned constants + a green, behavior-pinned `groundInfluenceMath.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/systems/groundInfluenceMath.ts
@src/game/systems/__tests__/groundInfluenceMath.test.ts
</context>

<artifacts>
## Artifacts this plan produces
- `DECAY_PER_FRAME_AT_60` retuned `0.985 → 0.980` (bend trail ~2s) — module-private constant in `groundInfluenceMath.ts`.
- `WEAR_REGROW_TIME_CONSTANT_SECONDS` retuned `25 → 75` (scorch/wear heal ~2–3min) — module-private constant in `groundInfluenceMath.ts`.
- Updated `groundInfluenceMath.test.ts` (stale wear block replaced; bend-2s block added).
- No new exported symbols, no new files.
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retune bend + scorch/wear decay constants and re-pin the tests</name>
  <files>src/game/systems/groundInfluenceMath.ts, src/game/systems/__tests__/groundInfluenceMath.test.ts</files>
  <read_first>
    - src/game/systems/groundInfluenceMath.ts (constant at :14, doc block/constant at :39-48)
    - src/game/systems/__tests__/groundInfluenceMath.test.ts (existing describes at :59-69 decayForDelta, :71-79 wear composition/slower-than-bend, stale block at :81-85)
    - 11-RESEARCH.md §"Code Examples 1" (verified arithmetic table) and §"Example 2" (target assertions)
  </read_first>
  <behavior>
    - decayForDelta(1) > 0.1 (bend still visible at 1s)
    - decayForDelta(2) < 0.10 (bend faded by ~2s — D-05; at 0.980 the value is 0.0885)
    - decayForDelta(3) < 0.03 (essentially gone by ~3s)
    - decayForDelta frame-rate independent: decayForDelta(1/120)^2 ≈ decayForDelta(1/60)
    - wearDecayForDelta(60) > 0.4 (fresh damage still reads on return — at τ=75 it is 0.449)
    - wearDecayForDelta(180) < 0.1 ("over minutes" — ~2.88min to <10%)
    - wearDecayForDelta regrows slower than decayForDelta at the same small dt (existing test at :77-79 stays valid)
  </behavior>
  <action>
    Change `DECAY_PER_FRAME_AT_60` from `0.985` to `0.980` at groundInfluenceMath.ts:14 (WEAR-04 / D-04 / D-05) and update its doc comment from "~4–5s" to "~2s readable fade". Change `WEAR_REGROW_TIME_CONSTANT_SECONDS` from `25` to `75` at :44 (WEAR-03 / D-06) and update the surrounding doc block (the `exp(-t/25)`/`~58s` prose) to describe the new τ=75 curve. Do NOT leave the old values commented — replace in place (no-legacy-code rule).

    Then re-pin `groundInfluenceMath.test.ts`: REPLACE the stale block at :81-85 ("full wear regrows within about a minute") — its `wearDecayForDelta(60) < 0.1` assertion becomes false at τ=75. Write the new wear contract (wear reads >0.4 at 60s, <0.1 at 180s). ADD a bend describe block mirroring the existing decayForDelta describe (:59-69) asserting decayForDelta(1)>0.1, decayForDelta(2)<0.10, decayForDelta(3)<0.03. Keep the existing composition/frame-rate-independence tests and the "regrows slower than bend" test. Tests-first: write the assertions to match the <behavior> block, confirm they fail against the old constants if run before the edit, then make them pass with the retune.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/groundInfluenceMath.test.ts</automated>
  </verify>
  <done>Both constants retuned in place with updated doc comments; the stale wear assertion is gone; the new bend-2s and wear-75s assertions pass; the full targeted test file is green.</done>
</task>

</tasks>

<threat_model>
This plan is client-only and cosmetic: it edits two pure numeric constants and a unit test. No network, auth, input parsing, persistence, reducers, or new data surfaces are touched. Input surface is unchanged; the changed code is render-timing only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | (n/a) | low | accept | No external input, no new surface — pure constant + test edit. No mitigation required. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed this plan/phase (locked zero-new-dependencies). No supply-chain surface. |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/systems/__tests__/groundInfluenceMath.test.ts` green.
- `pnpm test` (full suite) green — confirms no other test relied on the old constants.
</verification>

<success_criteria>
Bend and wear/scorch decay behavior is pinned to the new feel by passing tests; no dead constants left behind.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-01-SUMMARY.md` when done.
</output>
