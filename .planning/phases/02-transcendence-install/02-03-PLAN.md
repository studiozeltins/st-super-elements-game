---
phase: 02-transcendence-install
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/module_bindings/
autonomous: false
requirements: [REQ-transcend-install]

must_haves:
  truths:
    - "The local SpacetimeDB module carries the transcendCharacter reducer and the owned_character.transcendLevel column"
    - "Regenerated src/module_bindings/ exposes conn.reducers.transcendCharacter and a typed transcendLevel field on the owned_character row"
    - "Existing player/owned_character data survives (additive migrate, no -c / --delete-data)"
  artifacts:
    - src/module_bindings/
  key_links:
    - "This BLOCKING deploy gates all client code (plans 04, 05) — client types come from generated bindings, not the live DB, so an un-published module is a false-positive verification risk"
    - "Module path is spacetimedb/ (NOT server/); ignore the broken spacetime:publish npm scripts"
---

<objective>
[BLOCKING] Deploy the plan-02 schema/reducer changes additively to the local SpacetimeDB and regenerate client bindings. Client TypeScript compiles against `src/module_bindings/`, not the live DB — so `pnpm build` would falsely pass without this step, and the reducer call would 404 ("no such reducer") at runtime. This plan closes that gap and must run AFTER all server edits and BEFORE any client wiring.

Purpose: Make `transcendCharacter` and `owned_character.transcendLevel` visible + typed on the client.
Output: A published local module + freshly generated `src/module_bindings/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcendence-install/02-RESEARCH.md
@.planning/transcendence/PROGRESS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm spacetime + pnpm respond and local DB ownership is intact</name>
  <action>Human verifies the deploy toolchain + ownership before the additive publish (see how-to-verify steps).</action>
  <what-built>Nothing yet — this is a pre-deploy toolchain + auth gate. The next task publishes to local SpacetimeDB and regenerates bindings; that requires `spacetime` + `pnpm` on PATH and a CLI identity that OWNS the local `2d-impact-game-fr9ti` database (a prior `spacetime login` can flip identity and cause 403 Forbidden on republish — see CLAUDE.md "Ownership gotcha").</what-built>
  <how-to-verify>
    1. Run `spacetime server ping local` — expect a successful ping (local standalone running).
    2. Run `pnpm --version` and `spacetime --version` — both respond.
    3. Confirm the local DB is owned by the current CLI identity (a prior maincloud `spacetime login` may have switched it). If a later republish returns 403, the recovery is `spacetime server clear -y` → restart standalone → republish (WIPES local progress) — do not do this blindly.
  </how-to-verify>
  <resume-signal>Type "approved" when spacetime + pnpm respond and local ownership is intact, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Additive publish to local + regenerate bindings</name>
  <files>src/module_bindings/</files>
  <read_first>
    - CLAUDE.md "This Project: Environments & Deployment" (publish command, module path spacetimedb, additive migrate keeps data)
    - .planning/transcendence/PROGRESS.md §Workflow per phase (additive schema → drop -c)
    - .planning/phases/02-transcendence-install/02-RESEARCH.md Pitfalls 2 & 3 (regen order; wrong module path)
  </read_first>
  <action>
    Publish the module ADDITIVELY to local (NO `-c`, NO `--delete-data` — this column is additive and a wipe would destroy accounts/players): `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local -y`. Ignore the `spacetime:publish` npm scripts — they point at the wrong `--module-path server`. Then regenerate client bindings: `pnpm run spacetime:generate`. Do NOT push to maincloud in this plan (local only; maincloud is a later prod point). After regeneration, confirm the generated bindings expose the new reducer + column.
  </action>
  <verify>
    <automated>pnpm run spacetime:generate && grep -rq "transcendCharacter" src/module_bindings && grep -rq "transcendLevel" src/module_bindings && echo BINDINGS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `spacetime publish ... --server local -y` completes without `--delete-data`/`-c` and without a 403 ownership error.
    - `src/module_bindings/` contains a reference to `transcendCharacter` (reducer binding) and `transcendLevel` (owned_character field) after `pnpm run spacetime:generate`.
    - Pre-existing player/owned_character rows still present (spot-check via `spacetime sql 2d-impact-game-fr9ti "SELECT COUNT(*) FROM player"` returns the prior non-zero count).
  </acceptance_criteria>
  <done>The local module has the new reducer + column and regenerated bindings type them for the client; existing data preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy operation → live local DB | A publish flag mistake (`-c`/`--delete-data`) irreversibly destroys accounts + player progress; account/account_link tables are NOT in the backup set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Denial of Service / data destruction | publish flags | high | mitigate | Additive migrate ONLY — explicit command without `-c`/`--delete-data`; acceptance criterion checks pre-existing row counts survive. Human gate confirms local ownership before publish (avoids the 403→clear→wipe recovery path). |
| T-02-07 | Tampering | wrong module path / env drift | medium | mitigate | Explicit `--module-path spacetimedb --server local`; broken npm publish scripts (path `server`) are avoided by using the CLAUDE.md command verbatim; binding regen verified to contain the new symbols. |
</threat_model>

<verification>
- Bindings contain `transcendCharacter` + `transcendLevel`.
- Publish used no destructive flag; prior data intact.
</verification>

<success_criteria>
Local module published additively; `src/module_bindings/` regenerated and typed for the new reducer + column; no data loss.
</success_criteria>

<output>
Create `.planning/phases/02-transcendence-install/02-03-SUMMARY.md` when done.
</output>
