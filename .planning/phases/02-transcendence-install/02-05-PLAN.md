---
phase: 02-transcendence-install
plan: 05
type: execute
wave: 5
depends_on: ["02-03", "02-04"]
files_modified:
  - src/ui/CharacterScreen.tsx
autonomous: false
requirements: [REQ-transcend-install]

must_haves:
  truths:
    - "CharacterScreen shows a C6 · T{n} status badge (purple, --shard) only when transcendLevel > 0"
    - "CharacterScreen renders a gated Install/Pārsniegt control below the con-list calling onTranscend(characterId)"
    - "The control has one enabled state and three visually + textually distinct disabled states: below C6, at cap (T10), not enough shards"
    - "Playtest confirms installing a level deducts shards by the cost curve (level n costs n) and raises in-play damage"
  artifacts:
    - src/ui/CharacterScreen.tsx
  key_links:
    - "Install control disabled iff NOT (constellation>=6 && transcendLevel<MAX_TRANSCEND_LEVEL && transcendShards>=TRANSCEND_SHARD_COST(transcendLevel+1))"
    - "Reads transcendById + transcendShards props from App.tsx (plan 04); onClick → onTranscend(characterId) → transcendCharacter reducer"
    - "Targets LIVE CharacterScreen.tsx (App.tsx:474) — CharacterSheet.tsx is DEAD CODE, never touch it"
---

<objective>
Add the transcend Install control and the `C6 · T{n}` status badge to the LIVE `src/ui/CharacterScreen.tsx`, per the approved 02-UI-SPEC. The control is a purple (`--shard`) variant of the existing `.con-item` button with three distinct gate states; the badge is a purple clone of `.cchar__active-tag`. Then a human playtest confirms SC3 (damage/heal rises) and SC4 (gating) end-to-end.

Purpose: Make the spend affordance discoverable and honestly gated (client gate is UX; server plan-02 is the boundary), completing the visible loop: earn shards → install → power up.
Output: A gated Install button + C6·T{n} badge + a passed install/damage playtest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcendence-install/02-UI-SPEC.md
@.planning/phases/02-transcendence-install/02-PATTERNS.md
@src/game/data/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: C6 · T{n} status badge + props interface</name>
  <files>src/ui/CharacterScreen.tsx</files>
  <read_first>
    - src/ui/CharacterScreen.tsx:15-31 (props interface — add transcendById + onTranscend) and :171-173 (existing shard chip / ◈ glyph — reuse the family treatment)
    - src/ui/CharacterScreen.tsx:235-238 (.cchar__id with AKTĪVS VARONIS tag — add the sibling purple T badge here)
    - .planning/phases/02-transcendence-install/02-UI-SPEC.md §Status Badge + §Color (purple --shard, never --danger)
  </read_first>
  <action>
    Extend the CharacterScreen props interface (:15-31) with `transcendById: Record<string, number>` and `onTranscend(characterId: string): void`. Derive the viewed character's `transcendLevel = transcendById[characterId] ?? 0`. In `.cchar__id` (:235-238), next to the existing `AKTĪVS VARONIS` tag, render a sibling status badge showing `C6 · T{n}` ONLY when `transcendLevel > 0`. Style it as a purple clone of `.cchar__active-tag` (padding 5px 10px, `font: 700 10px var(--font-display)`, `letter-spacing: 0.12em`, `border: 1px solid currentColor`) using `--shard` (#9333ea) — NOT `--accent` green and NOT `--danger`. The green active tag stays green so the two colors read as two facts. Do NOT modify the shared ConstellationRing.tsx (the T badge is a sibling, not baked into the SVG). Latvian copy per UI-SPEC.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Props interface declares `transcendById: Record<string, number>` and `onTranscend(characterId: string): void`.
    - The `C6 · T{n}` badge renders only for `transcendLevel > 0` and uses the `--shard` purple token (no `--danger`, no `--accent`).
    - `src/ui/CharacterSheet.tsx` is NOT modified (dead code); `ConstellationRing.tsx` is NOT modified.
    - `pnpm build` green.
  </acceptance_criteria>
  <done>Transcend status is legible as a purple C6·T{n} badge without opening the constellation tab.</done>
</task>

<task type="auto">
  <name>Task 2: Gated Install / Pārsniegt control with three distinct disabled states</name>
  <files>src/ui/CharacterScreen.tsx</files>
  <read_first>
    - src/ui/CharacterScreen.tsx:343-361 (.con-item gated button + reducer onClick — the copy-shape) and where the con-list `<ol>` closes (~365) + the `.ctab__soon` explainer (~366)
    - src/game/data/constants.ts:8,15 (MAX_TRANSCEND_LEVEL, TRANSCEND_SHARD_COST — import; cost curve is nextLevel)
    - .planning/phases/02-transcendence-install/02-UI-SPEC.md §Component Contract + §Interaction & States + §Copywriting (all Latvian; three distinct gates; no confirm dialog)
  </read_first>
  <action>
    Directly below the `con-list` `<ol>` (closes ~365) and above `.ctab__soon`, render a section kicker `PĀRSNIEGŠANA · T{transcendLevel} / {MAX_TRANSCEND_LEVEL}` (mirror `.ctab__section`) and a purple-accented clone of the `.con-item` button (new modifier swapping `--gold`→`--shard`). Compute `nextLevel = transcendLevel + 1` and `cost = TRANSCEND_SHARD_COST(nextLevel)` (= nextLevel; never client-derive beyond this). Enabled iff `constellation >= 6 && transcendLevel < MAX_TRANSCEND_LEVEL && transcendShards >= cost`; `onClick` → `onTranscend(characterId)`. Render THREE DISTINCT disabled states (do not collapse): (a) below C6 — `opacity:0.45`, label `Vajadzīgs C6`, helper `Atbloķē visas 6 zvaigznes, lai sāktu pārsniegšanu.`, badge shows `C{constellation}`; (b) at cap — solid purple filled badge, label `Maksimums · T10`, helper `Sasniegts pārsniegšanas maksimums.` (celebratory, not error-toned); (c) not enough shards — primed CTA `{cost} ◈` + `opacity:0.45`, helper `Vajag vēl {cost − transcendShards} ◈.`. Enabled CTA copy `Pārsniegt · {cost} ◈`, badge `T{n}` (or `C6` at level 0), `◈` glyph in `--shard`. Add `aria-label="Pārsniegt {displayName}: līmenis T{nextLevel}, maksā {cost} zvaigžņu šķembas"`. Single-tap, no confirmation dialog. On success the existing `.wallet-chip--pulse` shard chip animation + badge bump are the feedback (no toast). One control per VIEWED character (characterId), not the active one.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - The control is enabled exactly when `constellation>=6 && transcendLevel<MAX_TRANSCEND_LEVEL && transcendShards>=TRANSCEND_SHARD_COST(transcendLevel+1)`; otherwise `disabled`.
    - Three disabled branches produce distinct labels/helpers (`Vajadzīgs C6` / `Maksimums · T10` / `Vajag vēl {shortfall} ◈`) and distinct affordances (dimmed-locked / filled-complete / primed-dimmed) — none use `--danger`.
    - `onClick` calls `onTranscend(characterId)` for the viewed character; no confirmation dialog.
    - `pnpm build` green and `pnpm test` full suite green.
  </acceptance_criteria>
  <done>A player can see and (when eligible) tap an honestly-gated Install control; ineligibility always explains why.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Playtest install — cost curve, damage/heal rise, three gate states</name>
  <action>Human runs the install/damage playtest (see how-to-verify steps) to validate the manual-only DoD items.</action>
  <what-built>The full install slice is wired: additive column + transcendCharacter reducer (server), regenerated bindings, damage feed (createGame + App.tsx), and the gated Install control + C6·T{n} badge (CharacterScreen). This checkpoint validates the manual-only DoD items from 02-VALIDATION.md that automated tests cannot cover (live game loop + SpacetimeDB).</what-built>
  <how-to-verify>
    1. `pnpm build`, ensure laragon serves the fresh `dist/` (or run the dev server); open the game and log into a player that owns a C6 character with shards (earn/grant shards via C6-overflow dupes if needed).
    2. Open the character screen → ZVAIGZNES (constellation) tab on that C6 character. Confirm the `PĀRSNIEGŠANA` control is ENABLED and shows cost `1 ◈` at T0.
    3. Tap Install once. Confirm: shard counter drops by exactly 1 (cost = nextLevel), the badge shows `C6 · T1`, and the next cost re-renders to `2 ◈`.
    4. Install again; confirm the second level costs 2 shards (cost curve: level n costs n).
    5. Enter combat with that character and confirm hit damage numbers are visibly higher than an un-transcended baseline. For a healer, confirm heal output rises.
    6. Confirm the three gate states: a below-C6 character shows `Vajadzīgs C6` disabled; when shards < cost the control shows `Vajag vēl {n} ◈` disabled; at T10 it shows `Maksimums · T10`.
  </how-to-verify>
  <resume-signal>Type "approved" if install deducts by the cost curve + damage/heal rises + gating is correct, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI gate → transcendCharacter reducer | The Install button's `disabled` logic is UX only; a crafted client could still call the reducer. Authority lives server-side (plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | client-side install gate bypass | medium | mitigate | The client gate prevents obviously-invalid taps for UX only; the plan-02 reducer independently enforces C6/cap/cost/ownership and rejects with SenderError, so a bypassed client gate cannot over-spend or forge power. |
| T-02-11 | Information disclosure / integrity | showing another player's transcend level | low | mitigate | The control reflects the VIEWED character from owner-filtered `transcendById` (plan 04); it never surfaces or acts on a non-owned character's state. |
</threat_model>

<verification>
- `pnpm build` + `pnpm test` green.
- Human-verify checkpoint approved (install cost curve + damage/heal rise + three gate states).
</verification>

<success_criteria>
The gated Install control + C6·T{n} badge render on the live CharacterScreen; a playtest confirms install deducts by the cost curve and raises damage/heal; all four ROADMAP success criteria observable.
</success_criteria>

<output>
Create `.planning/phases/02-transcendence-install/02-05-SUMMARY.md` when done.
</output>
