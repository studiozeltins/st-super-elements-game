---
phase: 05-swordswing-swordswirl-combo
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements: [ATK-02, ATK-03]
tags: [gate, playtest, deploy-verify]

must_haves:
  truths:
    - "Full test suite green and full build clean with all Phase-5 code merged (SC3 parity mandate)"
    - "SC1 verified by a human on the migrated local DB: frontal close-range player is hit by swordSwing; stepping side/back during the windup escapes"
    - "SC2 verified: swordSwirl always follows swordSwing; standing still or strafing dies, committing OUT of the radius survives"
    - "SC3 verified: cone sector + circle telegraphs and both clips read through the pixel filter at max pixelation; sector points at the aim (A1 yaw sign)"
    - "The goliath stays planted at swing strike (Pitfall 1 gate observed live) and the rhythm is slam → chain → gap → chain (D4-08/D5-13)"
  artifacts:
    - path: ".planning/phases/05-swordswing-swordswirl-combo/05-05-SUMMARY.md"
      provides: "gate results + playtest verdict + any retuned seeds"
  key_links:
    - from: "dist/ built client"
      to: "local SpacetimeDB module"
      via: "LAN playtest over ws://<host>:3000 against the MIGRATED database"
      pattern: "2d-impact-game-fr9ti"
---

<objective>
The phase capstone: run the exclusive full-suite + full-build gate (this plan owns the whole
working tree — no parallel-wave flake), verify the migrated deployment end-to-end, and gate
the phase on the human playtest that D5-15/SC1/SC2/SC3 and the pure-helper-testing-discipline
memory mandate — server-authoritative combat FEEL cannot be unit-tested.

Purpose: every seeded number in this phase (D5-06/09/10/11/12/13 + swirl radii) is a
playtest-tunable seed by design; Phase 4 doubled and tripled reaction numbers after feel
testing. This checkpoint is where the user tunes or approves.

Output: green gate, playtest verdict, retune list (if any) applied as ATTACKS data edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-swordswing-swordswirl-combo/05-CONTEXT.md
@.planning/phases/05-swordswing-swordswirl-combo/05-VALIDATION.md
@.planning/ROADMAP.md
</context>

## Artifacts this phase produces

Full Phase-5 symbol inventory (all landed by prior plans; this plan verifies them live):

- `resolveCone(px, pz, apexX, apexZ, aimX, aimZ, range, minDot)` — `spacetimedb/src/attackHitbox.ts` (05-01)
- `AttackSpec.chainsInto?: string`, `AttackSpec.coneMinDot?: number` — `spacetimedb/src/attacks.ts` (05-01)
- `ATTACKS.swordSwing` (cone basic, chains) + `ATTACKS.swordSwirl` (circle finisher) + `leapSlam` 5.5s retune + `UNIT_ATTACKS` default ['leapSlam','swordSwing'] (05-01)
- `selectAttack(distance, nowMicros, skillCooldownUntilMicros, basicCooldownUntilMicros, available)` (05-01)
- `walkAttackTransitions(...)` + `TransitionPlan` — `spacetimedb/src/unitAttackFsm.ts` (05-01)
- `basicCooldownUntilMicros: t.u64().default(0n)` on `unit_attack` + regenerated `src/module_bindings/` (05-03)
- Shape-branched `resolveStrike` + applier-driven glue (± extracted `unitStrike.ts`) — `spacetimedb/src/unitAttacks.ts` (05-03)
- `src/game/data/attacks.ts` client mirror: `AttackRenderSpec`, `ATTACK_RENDER` (05-04)
- Cone SECTOR telegraph + rebuild-on-attackId — `src/game/systems/createTelegraphSystem.ts` (05-04)
- `animateSwing` / `animateSwirl` clips — `src/game/systems/createGoliathRenderer.ts` (05-02)
- `playSwing` / `playSwirl` + per-attack juice branch — `src/game/audio/createAudioSystem.ts`, `src/game/createGame.ts` (05-02)

<tasks>

<task type="auto">
  <name>Task 1: Exclusive full gate — suite, build, migrated-DB deploy verification (SC3 automated half)</name>
  <files>(none — verification only; dist/ rebuilt)</files>
  <read_first>
    - .planning/phases/05-swordswing-swordswirl-combo/05-VALIDATION.md (sampling contract + manual-only table)
    - CLAUDE.md "Environments & Deployment" (how the LAN client resolves the local socket; laragon serves dist/)
  </read_first>
  <action>
    Run in order, all must pass before the checkpoint: (1) `pnpm test` — the FULL vitest suite
    including the extended serverSync parity (SC3's release-blocking mandate: it must be
    green, not skipped). (2) `pnpm build` — tsc -b plus vite build, clean; this is the first
    full typecheck over all merged Phase-5 client code and refreshes the laragon-served dist/.
    (3) Deploy state re-verification on the MIGRATED local DB (never a fresh seed):
    `spacetime describe 2d-impact-game-fr9ti table unit_attack --json --server local` lists
    basicCooldownUntilMicros; `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack"
    --server local` exits 0; `spacetime logs 2d-impact-game-fr9ti -n 100 --server local` has
    no error lines from the worldTick (a missed idleAttackRow field or a chain bug surfaces
    here as insert/update failures, Pitfall 5). (4) If plans 05-02/05-04 changed client code
    after 05-03's publish, NO republish is needed (client-only changes); republish
    additively ONLY if any spacetimedb/src file changed since the last publish, using the
    exact plan-05-03 command — never with a data wipe, never to maincloud. Record every
    command + result in the SUMMARY. If ANY step fails, fix forward within this plan (test or
    build breakage from merge interactions) and re-run the full sequence.
  </action>
  <verify>
    <automated>pnpm test && pnpm build && spacetime logs 2d-impact-game-fr9ti -n 100 --server local</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (full suite, including all Phase-5 extensions and the no-one-shot invariant)
    - `pnpm build` exits 0 (tsc -b clean + vite build; fresh dist/)
    - describe/sql/logs checks all clean against the migrated local DB
    - No maincloud command appears anywhere in the transcript (self-host pivot)
  </acceptance_criteria>
  <done>Everything automatable is green on the merged tree and the migrated deployment; the only open questions are feel questions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human playtest — SC1/SC2/SC3 feel verification on the migrated DB (D5-15, D-02 seed-then-tune)</name>
  <files>(none — verification; retunes, if requested, are one-line ATTACKS/juice data edits)</files>
  <action>
    Present the playtest script below to the user and BLOCK until they respond. On retune
    feedback: apply the requested edits as ATTACKS data / juice-constant changes (citing the
    D5-xx seed being retuned), re-run the Task-1 gate, republish additively to local ONLY if a
    spacetimedb/src file changed, rebuild, and re-present. On "approved": record the verdict
    and any applied retunes in the SUMMARY.
  </action>
  <what-built>
    The full Phase-5 kit, live on the migrated local DB and freshly built dist/: the goliath
    now opens with leapSlam (5.5s cooldown), fills with a 120° frontal cone swordSwing (0.6s
    windup, 1.5x damage, 0.6s stun) that ALWAYS chains into a self-centered circle swordSwirl
    (0.75s windup warning, 2.5x damage, 4.5u knockback, 1.2s recovery punish window), then a
    2.5s basic cooldown — the D4-08 basic-basic-skill rhythm. Cone sector + circle telegraphs
    in Frost cyan, two new procedural clips, per-attack juice and SFX.
  </what-built>
  <how-to-verify>
    Playtest on the LAN build against the migrated local DB (laragon dist/ at your host IP,
    local standalone running — two clients ideal per the server-authoritative UX discipline):

    1. SC1 (cone, ATK-02): stand directly in front of a goliath inside ~3.5u. Expect: a cyan
       SECTOR telegraph appears at the goliath pointing AT YOU, fills from the goliath over
       0.6s, then you are hit (brief 0.6s stun, NO knockback). Repeat and step to the goliath's
       SIDE or BACK during the fill — you must NOT be hit. Also confirm the sector points the
       right way (A1 yaw-sign assumption — if it points away from the aim, report "sector
       mirrored").
    2. SC2 (chain, ATK-03): let a swing resolve (hit OR dodge it). Expect: immediately after,
       a CIRCLE telegraph appears centered on the goliath's feet for 0.75s. Standing still or
       strafing around the goliath → hit and thrown ~4.5u clear. Sprinting straight OUT of the
       circle → escape. The swirl must follow EVERY swing, including whiffed ones (D5-02),
       and no swirl warning may show during the swing itself (D5-15).
    3. SC3 (readability): at MAX pixelation, both telegraph shapes read clearly (the cone
       sector must not smear into a blob — ANIM-02); the cone mesh visibly disappears when the
       circle appears (no lingering cone through the swirl — the rebuild fix); swing shows a
       wind-back → slash lunge, swirl a coil → full 360° spin with a heavy settle; each strike
       has its own (smaller-than-slam) shake/SFX.
    4. Spine integrity: the goliath must stay PLANTED at swing strike — if it snaps/teleports
       toward you at the hit, report "teleport bug" (Pitfall 1 gate failed). Watch the rhythm
       over ~20s at melee: slam → swing+swirl → short gap → swing+swirl → slam again (~5.5s
       slam cadence, ~2.5s chain gap). Beyond ~3.5u with slam cooling, the goliath should
       CHASE, not idle (D4-13/D5-09).
    5. Tuning (D-02 seed-then-tune): all numbers are seeds — swing/swirl damage, stun 0.6s,
       knockback 4.5u, windups 0.6s/0.75s, swirl radii 4.0/4.5/5.0, chain cooldown 2.5s, juice
       magnitudes. Report any retunes; they are one-line ATTACKS data or juice-constant edits
       (a damage retune must keep the serverSync no-one-shot invariant green — the test will
       catch a violation).
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe issues/retunes (e.g. "swirl radius too big, sector mirrored, stun to 0.9s") — Claude applies data edits, re-runs the Task-1 gate, republishes additively if server data changed, and re-presents.</resume-signal>
  <verify>
    <automated>MISSING — feel verification is human-only by design (server-authoritative UX gate per pure-helper-testing-discipline memory; all automatable behavior is covered by the plan 05-01/05-03/05-04 unit gates and Task 1)</automated>
    <human-check>SC1 frontal-hit/side-escape, SC2 chain forces move-OUT, SC3 telegraph/clip legibility through pixel filter, planted-goliath + rhythm integrity — per the numbered script above</human-check>
  </verify>
  <done>User replied "approved" (with or without applied retunes); SC1, SC2, SC3 and spine-integrity checks all confirmed on the migrated local DB over LAN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN clients → local SpacetimeDB | Playtest traffic over ws://host:3000; all combat writes remain worldTick-internal — playtest specifically confirms a client cannot influence chain/cone outcomes except by moving |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | server-authoritative chain/cone | high | mitigate | Playtest step 4 observes server-owned behavior (planted goliath, unconditional chain) over real network; no new client input surface exists to probe |
| T-05-03 | DoS (gameplay) | retunes crossing the one-shot line | medium | mitigate | Any damage retune re-runs the full gate; the serverSync invariant (max chain < min max-HP) blocks a violating retune |
| T-05-06 | DoS (data loss) | republish during retune loop | high | mitigate | Retune republishes are additive data-only edits with the plan-05-03 command; --delete-data prohibited; maincloud never targeted |
| T-05-SC | Tampering | package installs | low | accept | Zero new dependencies across the entire phase |
</threat_model>

<verification>
- Task-1 gate green on the merged tree (full suite + build + deploy checks)
- Human approval received for SC1, SC2, SC3 + spine integrity on the migrated DB
- Any retunes applied as data edits with the gate re-run before final approval
</verification>

<success_criteria>
- All three ROADMAP Phase-5 success criteria verified by a human over the real network on the migrated local DB
- serverSync ATTACKS parity green at phase end (SC3 mandate)
- Phase leaves no red tests, no dirty tree, no unpublished server changes
</success_criteria>

<output>
Create `.planning/phases/05-swordswing-swordswirl-combo/05-05-SUMMARY.md` when done
</output>
