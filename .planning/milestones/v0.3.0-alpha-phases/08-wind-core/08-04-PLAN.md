---
phase: 08-wind-core
plan: 04
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/game/systems/createSmokeColumns.ts
  - src/game/createGame.ts
autonomous: true
requirements: [WIND-01, WIND-03]
must_haves:
  truths:
    - "Campfires within ~50 units of the player emit thin columns of chunky voxel smoke puffs that rise and drift downwind"
    - "Smoke drift direction and gust kink come from the SAME wind module grass reads — a gust that leans the grass visibly kinks the plume"
    - "Puffs shrink/fade in discrete steps (no alpha blending) — no banding under the nearest-filtered pixel target"
    - "?nosmoke removes smoke entirely (no construction, no draw calls); ?nowind zeroes lateral drift but puffs still rise"
    - "Frame cost is flat: one InstancedMesh, fixed pool, zero per-frame allocations"
  artifacts:
    - "src/game/systems/createSmokeColumns.ts (fixed instanced puff pool over campfires)"
    - "src/game/createGame.ts (?nosmoke flag, conditional construction, optional-chained frame update)"
  key_links:
    - "wind.sampleWind/sampleGust + directionUniform → per-puff CPU drift (the JS mirror of the shader math — WIND-01 one phase)"
    - "getCampSites() + world.getGroundHeight → static fire anchors computed once at construction"
    - "createGame frame() → smokeColumns?.update(deltaSeconds, playerX, playerZ)"
---

<objective>
Add the phase's only NEW draw-call source: campfire smoke columns as a fixed-size
InstancedMesh pool of chunky opaque voxel puffs (D-09), thin-wisp scale ~8-12 puffs per
fire (D-10), radius-culled to fires near the player with the pool reused across fires
(D-11), drifting laterally on the shared wind (WIND-01/WIND-03: smoke drifts as it rises)
with a visible kink at gust fronts (D-04). Ships behind `?nosmoke` for clean FPS bisecting
(D-12).

Purpose: smoke is the fourth WIND-01 consumer and the only CPU-side one — it proves the
`sampleWind`/`sampleGust` JS mirror against the GPU consumers.
Output: `createSmokeColumns.ts` + createGame wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-wind-core/08-RESEARCH.md
@.planning/phases/08-wind-core/08-PATTERNS.md
@src/game/systems/createDebrisSystem.ts
@src/game/systems/createWind.ts
@src/game/world/camps.ts
@src/game/world/assets/createCampfire.ts
@src/game/createGame.ts
</context>

## Artifacts this phase produces

New symbols created by this plan (source-grounding exclusion list):

- `src/game/systems/createSmokeColumns.ts` — `SmokeColumns` interface
  (`update(deltaSeconds, playerX, playerZ)`, `dispose()`), `createSmokeColumns(scene,
  wind, getGroundHeight)` factory, pool/tuning constants (`SMOKE_POOL_SIZE`,
  `SMOKE_CULL_RADIUS`, rise/drift/spawn constants)
- `src/game/createGame.ts` — URL bisect flag `?nosmoke`; `smokeEnabled` const;
  `smokeColumns` const

<tasks>

<task type="auto">
  <name>Task 1: Create createSmokeColumns.ts — fixed instanced puff pool</name>
  <files>src/game/systems/createSmokeColumns.ts</files>
  <read_first>
    - src/game/systems/createDebrisSystem.ts (FULL — THE analog: fixed pool + InstancedMesh setup :22-60, closure scratch objects :62-67, per-frame update + needsUpdate discipline :100-121, dispose :122-127)
    - src/game/systems/createWind.ts (the Wind interface consumed: sampleWind, sampleGust, directionUniform, strengthUniform)
    - src/game/world/camps.ts (:29 — getCampSites() returning static {x, z} camp anchors)
    - src/game/world/assets/createCampfire.ts (:65-70 — flame stack height; emitter top ≈ ground + 1.0)
    - .planning/phases/08-wind-core/08-RESEARCH.md (Pattern 6 — pool sizing, cull cadence, stepped fade, Pitfall 5 instancing rules)
  </read_first>
  <action>
    Create `src/game/systems/createSmokeColumns.ts` (~120-150 LOC, hard cap 300 functional
    LOC — split a pure `smokeMath.ts` helper only if the lifecycle math pushes past it).
    Export `SmokeColumns` interface with `update(deltaSeconds: number, playerX: number,
    playerZ: number): void` and `dispose(): void`, and `createSmokeColumns(scene:
    THREE.Scene, wind: Wind, getGroundHeight: (x: number, z: number) => number):
    SmokeColumns`.

    **Anchors (D-11):** at construction, map `getCampSites()` to fire emitters at
    `{ x, y: getGroundHeight(x, z) + 1.0, z }` — static, computed once; never traverse the
    scene graph.

    **Pool (D-10, D-13, Pitfall 5):** `SMOKE_POOL_SIZE = 48` (4 fires × 12 puffs —
    discretion constant). ONE `InstancedMesh(BoxGeometry(1, 1, 1), MeshLambertMaterial,
    SMOKE_POOL_SIZE)` — Lambert, NOT Basic (Phase 9 day/night dims only lit materials);
    the material stays opaque — alpha blending bands under the nearest-filtered pixel
    target (D-09). Set `instanceMatrix.setUsage(THREE.DynamicDrawUsage)` and the same on
    the instance color buffer after the first setColorAt; `frustumCulled = false`;
    `castShadow = false; receiveShadow = false`; hide inactive slots with a zero-scale
    matrix; add to the SCENE root (never inside the frozen world group). Plain-object puff
    pool array (debrisMath-style fields: position, age, fireIndex, active).

    **Per-frame update (D-11, D-13):** an accumulator rechecks fire-membership at ~2Hz
    (every 0.5s) — only fires within `SMOKE_CULL_RADIUS = 50` world units of the player
    get active spawning. Each fire spawns a puff on its own cadence (~0.7s interval,
    staggered by fire index so columns never pulse in sync). Per active puff per frame:
    rise `y += RISE_SPEED * dt` (~0.8 u/s); lateral drift `x/z += windDir * (BASE_DRIFT +
    GUST_KICK * wind.sampleGust(x, z)) * wind.strengthUniform.value * dt` — reading
    directionUniform components live (never caching the Vector2 contents across frames);
    the gust kick makes the plume visibly kink downwind at a front (D-04). Age drives 3-4
    DISCRETE size tiers (stepped shrink) and 3-4 discrete `setColorAt` steps fading the
    smoke gray toward the sky color 0x8ecae6 — stepped dissolution, chunky voxel read
    (D-09). Recycle at max height (~4.5u — thin wisp, D-10). Compose matrices with
    closure-level scratch Matrix4/Vector3/Quaternion/Color objects — zero per-frame
    allocations; set `instanceMatrix.needsUpdate`/instance-color needsUpdate once per
    frame only when something moved (debris discipline). `?nowind` semantics (D-12,
    RESEARCH open question 3 ruling): strengthUniform.value = 0 zeroes lateral drift and
    gust kick but puffs STILL RISE — fire without wind still smokes.

    **dispose():** remove from scene, dispose geometry + material (debris pattern).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm vitest run` exits 0
    - `grep -c "DynamicDrawUsage" src/game/systems/createSmokeColumns.ts` ≥ 1
    - `grep -c "frustumCulled = false" src/game/systems/createSmokeColumns.ts` returns 1
    - `grep -c "MeshLambertMaterial" src/game/systems/createSmokeColumns.ts` ≥ 1 and `grep -v '^\s*//' src/game/systems/createSmokeColumns.ts | grep -c "MeshBasicMaterial"` returns 0 (lit material — Phase 9 night compatibility)
    - `grep -v '^\s*//' src/game/systems/createSmokeColumns.ts | grep -c "transparent"` returns 0 (opaque stepped fade — D-09)
    - `grep -c "getCampSites" src/game/systems/createSmokeColumns.ts` returns 1 (data-driven anchors, no scene traversal)
    - `grep -v '^\s*//' src/game/systems/createSmokeColumns.ts | grep -cE "new THREE\.(Vector3|Matrix4|Quaternion|Color)" ` output all located OUTSIDE the update function body (closure-level scratch — manual source check: no constructor calls inside update)
    - `grep -c "sampleGust\|sampleWind" src/game/systems/createSmokeColumns.ts` ≥ 1 (drift reads the shared wind — WIND-01)
  </acceptance_criteria>
  <done>Smoke system compiles and follows every Pitfall-5 instancing rule; not yet constructed by the game.</done>
</task>

<task type="auto">
  <name>Task 2: Wire smoke into createGame behind ?nosmoke</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts (:295-317 perfFlags block including the flag-listing comment; :344-346 the conditional debris construction precedent; :1304-1362 frame() including the debris optional-chained update and the player position source used there)
    - src/game/systems/createSmokeColumns.ts (the factory signature from Task 1)
  </read_first>
  <action>
    Three wiring sites, ≤ ~6 net lines (D-12): (1) perfFlags block — add `const
    smokeEnabled = !perfFlags.has('nosmoke');` and extend the flag-listing comment with
    `?nosmoke`. (2) After world construction (needs `world.getGroundHeight` — same
    position as the debris system at :344-346): `const smokeColumns = smokeEnabled ?
    createSmokeColumns(scene, wind, (x, z) => world.getGroundHeight(x, z)) : undefined;`
    — skipped construction entirely under the flag (zero objects, zero draw calls;
    smoke is the phase's only new draw-call source so this is the clean FPS bisect).
    (3) In `frame()`, beside the existing debris optional-chained update:
    `smokeColumns?.update(deltaSeconds, <playerX>, <playerZ>)` using the same local player
    position the frame loop already tracks (read the file — reuse the existing vector, do
    not derive a new one per frame). Must run AFTER `wind.update(deltaSeconds)` so puffs
    read this frame's phase.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm vitest run` exits 0
    - `grep -c "nosmoke" src/game/createGame.ts` ≥ 2 (flag read + comment listing)
    - `grep -c "smokeColumns?.update" src/game/createGame.ts` returns 1 (optional-chaining convention — debris precedent)
    - `grep -c "createSmokeColumns" src/game/createGame.ts` returns 2 (import + conditional construction)
    - In createGame.ts source order: the `wind.update(deltaSeconds)` call appears BEFORE the `smokeColumns?.update` call inside frame()
  </acceptance_criteria>
  <done>Smoke renders over nearby campfires in-game; ?nosmoke removes it entirely; ?nowind leaves straight-rising columns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → client bootstrap | `?nosmoke` read via `URLSearchParams.has()` — boolean presence only, no value parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Tampering | wind/smoke state used for gameplay decisions | low | accept | Smoke is 100% cosmetic — no hitbox, no visibility mechanics; documented so Phase 9+ never gates gameplay on it |
| T-08-03 | Denial of Service | fixed pool exhaustion / unbounded growth | low | mitigate | SMOKE_POOL_SIZE is a hard cap with slot recycling — the unbounded-growth-table failure class (CLAUDE.md perf rule 4) is structurally impossible |
</threat_model>

<verification>
- `pnpm vitest run` and `pnpm build` green
- Zero per-frame allocations in the update path (scratch objects at closure level — manual source review)
- `?nosmoke` skips construction (no InstancedMesh created at all)
</verification>

<success_criteria>
- Smoke columns rise from campfires near the player, drift downwind, and kink at gust fronts (WIND-01, WIND-03)
- Chunky stepped voxel puffs, opaque, Lambert — pixel-filter and Phase-9-night safe (D-09)
- Flat frame cost: fixed 48-instance pool, 2Hz cull recheck, no allocs (D-11, D-13)
</success_criteria>

<output>
Create `.planning/phases/08-wind-core/08-04-SUMMARY.md` when done
</output>
