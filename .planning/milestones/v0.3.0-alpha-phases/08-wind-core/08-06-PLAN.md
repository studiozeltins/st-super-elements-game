---
phase: 08-wind-core
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/world/assets/createCampFlag.ts
  - src/game/world/assets/createCanopyTree.ts
  - src/game/world/assets/__tests__/windMaterialLifecycle.test.ts
  - src/game/world/assets/__tests__/assets.test.ts
  - src/game/systems/windMath.ts
autonomous: true
gap_closure: true
requirements: [WIND-01, WIND-02, WIND-03]
must_haves:
  truths:
    - "A second game instance gets flag cloth/pole and canopy cap materials wired to ITS wind uniforms — flags and canopies never freeze after a StrictMode remount or prod reconnect (SC1/WIND-01, closes CR-01 + CR-02)"
    - "initCanopyWind with a DIFFERENT wind invalidates the canopy pool: old materials are disposed, new builds get fresh materials bound to the new uniforms; re-injecting the SAME wind keeps the pool (D-13 pooling preserved)"
    - "Canopy height weight ramps on height above the TREE BASE, not absolute world Y — a tree on a 7u hill sways exactly like one at sea level, cap bottoms move less than tops (WIND-03, closes WR-02)"
    - "createCampFlag runs through the shared asset factory invariants suite (group shape + determinism), and the suite's 'every factory' claim is true again (closes WR-03)"
  artifacts:
    - "src/game/world/assets/__tests__/windMaterialLifecycle.test.ts (NEW — wind re-injection staleness + pooling-preserved + canopy height-attribute tests)"
    - "src/game/world/assets/createCampFlag.ts (wind-keyed material cache replacing module singletons)"
    - "src/game/world/assets/createCanopyTree.ts (pool invalidation in initCanopyWind + aTreeHeight vertex attribute)"
    - "src/game/world/assets/__tests__/assets.test.ts (createCampFlag entry in the FACTORIES table)"
  key_links:
    - "initCanopyWind(newWind) → canopyMaterials pool cleared + orphans disposed → next getCanopyMaterial builds against newWind's uniform objects"
    - "createCampFlag(random, wind) → wind-guarded material cache → cloth/pole rebuilt whenever a different wind arrives"
    - "cap geometry aTreeHeight attribute → shader heightWeight ramp (replaces the world-Y read)"
---

<objective>
Close the SC1/WIND-01 blocker and two 08-03 quality gaps: flag and canopy materials are
currently MODULE-level singletons whose onBeforeCompile closures capture the FIRST game
instance's wind uniforms (CR-01/CR-02) — under StrictMode (src/main.tsx:50) and on every
prod reconnect (game effect App.tsx:757-810 disposes/recreates createGame) flags and
canopies render on a dead clock forever while grass and smoke keep moving. Scope both
material pools to the wind instance (the grass analog: createGrassField builds its material
per world with wind injected — mirror that lifetime), fix the canopy height ramp to measure
from the tree base instead of sea level (WR-02), and put createCampFlag into the shared
asset invariants suite (WR-03).

Purpose: restore "no system drifts out of sync" (ROADMAP SC1) in the standard dev
environment and after reconnects; keep D-13 pooling (4 canopy materials + 1 cloth per wind
lifetime — per-wind pooling is fine, cross-instance module pooling is the bug).
Output: wind-scoped material caches, tree-base-relative sway ramp, regression tests that
fail on any future module-singleton relapse.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-wind-core/08-VERIFICATION.md
@.planning/phases/08-wind-core/08-REVIEW.md
@src/game/world/assets/createCampFlag.ts
@src/game/world/assets/createCanopyTree.ts
@src/game/world/createGrassField.ts
@src/game/systems/createWind.ts
@src/game/systems/windMath.ts
@src/game/world/assets/__tests__/assets.test.ts
</context>

## Artifacts this phase produces

New/changed symbols (source-grounding exclusion list):

- `src/game/world/assets/createCanopyTree.ts` — `initCanopyWind` gains pool invalidation
  (`canopyMaterials.clear()` + orphan disposal on wind change); new module const
  `CAP_SCALE_Y`; caps gain an `aTreeHeight` float vertex attribute; shader heightWeight
  reads `aTreeHeight` instead of the world-space Y
- `src/game/world/assets/createCampFlag.ts` — module-eager `poleMaterial` and lazy
  `clothMaterial` singletons replaced by a wind-guarded cache: new module vars
  `flagWind`/`poleMaterial`/`clothMaterial` (all nullable) + new private function
  `getFlagMaterials(wind)` returning `{ pole, cloth }`
- `src/game/world/assets/__tests__/windMaterialLifecycle.test.ts` — NEW test file:
  re-injection staleness, same-wind pooling, flag cache rebuild, dispose-event assertions,
  canopy `aTreeHeight` attribute shape
- `src/game/world/assets/__tests__/assets.test.ts` — hoisted `testWind` const;
  `createCampFlag` entry added to the `FACTORIES` table
- `src/game/systems/windMath.ts` — CANOPY doc comment corrected (ramp is height above the
  tree base, baked per-vertex) and gustGlsl docstring softened ("same formula, literals
  rounded to 4 decimals" instead of byte-for-byte; IN-02); no constant value changes

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope flag + canopy materials to the wind instance (CR-01, CR-02)</name>
  <files>src/game/world/assets/__tests__/windMaterialLifecycle.test.ts, src/game/world/assets/createCanopyTree.ts, src/game/world/assets/createCampFlag.ts</files>
  <read_first>
    - src/game/world/assets/createCampFlag.ts (FULL — module poleMaterial :22, lazy clothMaterial singleton :23,36-37, onBeforeCompile closure capturing wind :42-73, mesh assembly :95-119)
    - src/game/world/assets/createCanopyTree.ts (FULL — canopyWind injection :19-28, canopyMaterials Map :23, closure capture `const wind = canopyWind` :41, cap loop :110-127)
    - src/game/world/createGrassField.ts (:61-161 — the PROVEN lifetime analog: material built per world inside createGrassField with wind passed in; this is the scoping to mirror, adapted to the module-cache shape these factories need)
    - src/game/systems/createWind.ts (WindUniforms contract — uniform OBJECTS held by reference, mutated in place; createWind(true) per test)
    - src/game/world/assets/__tests__/assets.test.ts (:1-30 — existing test conventions: seeded random import, initCanopyWind-before-build contract)
    - src/game/engine/disposeObject.ts (world.dispose() disposes every material it reaches — why a stale cache handing out disposed singletons is the second failure mode of CR-01)
    - .planning/phases/08-wind-core/08-REVIEW.md (CR-01 + CR-02 — full failure analysis and suggested fix shapes)
  </read_first>
  <behavior>
    NEW file src/game/world/assets/__tests__/windMaterialLifecycle.test.ts (write FIRST,
    watch the staleness tests fail RED against current code, then fix):
    - Canopy staleness: initCanopyWind(windA); build a tree (createCanopyTree(createSeededRandom(1))); capture a cap material (children[1] as THREE.Mesh).material; then initCanopyWind(windB) with a SECOND createWind(true); build another tree; the new cap material is a DIFFERENT object (expect(next).not.toBe(prev)) — FAILS today because the Map is never invalidated
    - Canopy orphan disposal: register addEventListener('dispose', listener) on the first batch's cap material before re-injecting windB; the listener fires during initCanopyWind(windB)
    - Canopy pooling preserved: re-inject the SAME wind object, build another tree — cap material IS the same object as before (pool survives same-wind re-injection; D-13)
    - Flag staleness: createCampFlag(createSeededRandom(1), windA); capture cloth material (children[1]) and pole material (children[0]); createCampFlag(createSeededRandom(1), windB) yields a different cloth material AND a different pole material — FAILS today
    - Flag pooling preserved: two flags built with the same wind share ONE cloth material object and ONE pole material object
  </behavior>
  <action>
    **createCanopyTree.ts (CR-02):** make `initCanopyWind(wind)` invalidate the pool when
    the wind actually changes: when the incoming wind is not the currently stored
    `canopyWind`, dispose every material in `canopyMaterials` and call
    `canopyMaterials.clear()`, then store the new wind. `getCanopyMaterial` stays
    otherwise unchanged — after a clear, the next call lazily rebuilds each color's
    material whose onBeforeCompile closure now captures the NEW wind. This keeps the D-13
    pooling win (4 materials per wind lifetime shared by every cap) while making the
    pool's lifetime match the wind it is wired to. Update the doc comment on the pool/
    injection to state the lifetime contract: one pool per injected wind; re-injection
    with a different wind disposes and rebuilds (the world injects once per game instance,
    so this is exactly one rebuild per game re-creation).

    **createCampFlag.ts (CR-01):** delete the module-eager pole material and the
    unconditioned lazy cloth singleton. Replace with a wind-guarded cache: nullable module
    vars `flagWind`, `poleMaterial`, `clothMaterial`, and a private
    `getFlagMaterials(wind: WindUniforms): { pole: THREE.MeshLambertMaterial; cloth: THREE.MeshLambertMaterial }`
    that (a) when `wind !== flagWind`: disposes both cached materials if present, nulls
    them, stores the new wind; (b) lazily builds the pole material via the existing
    `lambert(POLE_COLOR)` helper and the cloth material with the EXACT existing
    onBeforeCompile patch (uniform wiring by object reference, GLSL body, `campFlag`
    cache key, DoubleSide, vertexColors — do not touch the shader text; this task is
    lifetime-only). `createCampFlag` calls `getFlagMaterials(wind)` once and uses the
    returned pair for pole and cloth meshes. The pole material carries no wind but joins
    the cache anyway: `disposeObject` disposes it on world teardown, so its lifetime must
    also match the world (per 08-REVIEW CR-01 "apply the same treatment"). Per-flag banner
    color stays on the geometry vertex-color attribute (D-08) — pooling within one wind
    lifetime is unchanged.

    **Invariant to state in code comments:** a cached material may be disposed by
    world.dispose() at game teardown; that is safe because the next game constructs a NEW
    wind, which forces the rebuild path — the cache never hands out a disposed material to
    a live world.

    Write the test file first (imports: describe/expect/it from vitest, THREE, createWind,
    createSeededRandom from '../../rng', createCanopyTree/createCampFlag/initCanopyWind
    from '../index'). Run it, confirm the staleness expectations fail against the current
    singletons, then apply both fixes and go GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/world/assets/__tests__/windMaterialLifecycle.test.ts && pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/game/world/assets/__tests__/windMaterialLifecycle.test.ts exists and `pnpm vitest run src/game/world/assets/__tests__/windMaterialLifecycle.test.ts` exits 0
    - RED phase demonstrated: test committed (or run) failing before the fix commit — commit sequence shows test(08-06) before/with fix, and the SUMMARY records the observed RED failure
    - `grep -c "canopyMaterials.clear()" src/game/world/assets/createCanopyTree.ts` returns 1 (pool invalidation lives in initCanopyWind)
    - `grep -c "function getFlagMaterials" src/game/world/assets/createCampFlag.ts` returns 1
    - `grep -v '^\s*//' src/game/world/assets/createCampFlag.ts | grep -c "^const poleMaterial"` returns 0 (no module-eager material construction remains)
    - `grep -c "flagWind" src/game/world/assets/createCampFlag.ts` >= 2 (guard variable declared and compared)
    - `grep -c "customProgramCacheKey" src/game/world/assets/createCampFlag.ts` returns 1 and `grep -c "'campFlag'" src/game/world/assets/createCampFlag.ts` returns 1 (shader patch and cache key untouched)
    - `pnpm vitest run` exits 0 (full 703-test suite stays green) and `pnpm build` exits 0
  </acceptance_criteria>
  <done>A second createWind instance yields fresh flag + canopy materials bound to the new uniforms (proven by the new test); old materials disposed; same-wind pooling intact; suite + build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Canopy height weight relative to tree base, not world Y (WR-02)</name>
  <files>src/game/world/assets/createCanopyTree.ts, src/game/world/assets/__tests__/windMaterialLifecycle.test.ts, src/game/systems/windMath.ts</files>
  <read_first>
    - src/game/world/assets/createCanopyTree.ts (post-Task-1 state — shader heightWeight line, cap build loop with layerHeight/layerRadius progression and cap.scale.y = 0.55)
    - src/game/systems/windMath.ts (:64-76 — CANOPY constants incl. swayBaseY/invSwaySpan and the doc comment that assumes tree base at world y=0)
    - src/game/world/terrain.ts (:1-20 — MAX_HILL_HEIGHT/offset: terrain reaches ~7.5u, which is why the absolute-Y ramp saturates)
    - .planning/phases/08-wind-core/08-REVIEW.md (WR-02 — failure analysis and the sanctioned fix routes: bake height-above-base per vertex; per-mesh uniforms are impossible with pooled materials)
  </read_first>
  <behavior>
    Append a describe block to windMaterialLifecycle.test.ts (write assertions first):
    - Every cap mesh (children indices >= 1) of createCanopyTree(createSeededRandom(1)) has a geometry attribute named aTreeHeight with itemSize 1 and count equal to the position attribute count
    - Within one tree, aTreeHeight values vary (max - min > 1) — the ramp has something to ramp over; all values are finite
    - The highest cap layer's max aTreeHeight exceeds the lowest cap layer's max (tops are higher above the base)
    - Determinism: two trees from createSeededRandom(1) produce identical aTreeHeight arrays
  </behavior>
  <action>
    **createCanopyTree.ts:** the pooled material cannot take a per-mesh uniform, so bake
    each vertex's height above the TREE BASE into geometry (the 08-REVIEW WR-02 sanctioned
    route). Hoist the cap flatten factor 0.55 into a module const `CAP_SCALE_Y` used both
    for `cap.scale.y` and the bake. In the cap loop, after creating the
    IcosahedronGeometry, build a Float32Array of length position.count where entry i =
    `layerHeight + position.getY(i) * CAP_SCALE_Y` (rotation about Y never changes vertex
    height; the group carries no scale) and set it as attribute `aTreeHeight`
    (THREE.BufferAttribute, itemSize 1).

    In the shader patch: declare `attribute float aTreeHeight;` alongside the uniform
    declarations in the common-include replacement, and compute heightWeight from
    `aTreeHeight` — `clamp((aTreeHeight - swayBaseY) * invSwaySpan, 0.0, 1.0)` with the
    same CANOPY.swayBaseY/CANOPY.invSwaySpan literals via toFixed(4). The ramp must no
    longer read the Y component of the world-space canopy position; `canopyWorld` remains
    solely for the wind-direction projection (`proj`) feeding gustGlsl and the idle-lean
    phase. Update the GLSL comment above the ramp: heights are tree-local, baked at build
    time, so a hill tree and a valley tree get identical weights.

    **windMath.ts:** comment-only edits, two spots. (1) Correct the CANOPY doc comment —
    the ramp is measured in height above the tree base (baked per-vertex attribute), caps
    start ~2.5u above the base, 5u span. (2) While in the file, soften the gustGlsl
    docstring's "byte-for-byte" claim to "same formula, literals rounded to 4 decimals"
    (review IN-02 — the toFixed(4) interpolation makes byte-identity an overstatement).
    No constant values change (truth #9's byte-identical SWAY pins and the CANOPY
    constant values are untouched).

    Write the new describe block first, watch it fail (no attribute exists yet), then
    implement.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/world/assets/__tests__/windMaterialLifecycle.test.ts && pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aTreeHeight" src/game/world/assets/createCanopyTree.ts` >= 3 (attribute bake, GLSL declaration, heightWeight use)
    - `grep -v '^\s*//' src/game/world/assets/createCanopyTree.ts | grep -c "canopyWorld.y"` returns 0 (no non-comment world-Y read remains in the sway ramp)
    - `grep -c "CAP_SCALE_Y" src/game/world/assets/createCanopyTree.ts` >= 2 (single source for the flatten factor: mesh scale + bake)
    - `grep -c "swayBaseY" src/game/systems/windMath.ts` >= 1 with unchanged value 2.0 and `grep -c "invSwaySpan: 0.2" src/game/systems/windMath.ts` returns 1 (constants untouched — comment-only edit)
    - New aTreeHeight describe block passes; `pnpm vitest run` exits 0; `pnpm build` exits 0
  </acceptance_criteria>
  <done>Canopy sway weight is a function of height above the tree's own base — hill and valley trees sway identically, cap bottoms move less than cap tops; suite + build green.</done>
</task>

<task type="auto">
  <name>Task 3: createCampFlag joins the shared asset invariants suite (WR-03)</name>
  <files>src/game/world/assets/__tests__/assets.test.ts</files>
  <read_first>
    - src/game/world/assets/__tests__/assets.test.ts (FULL — module-level initCanopyWind(createWind(true)) :25, AssetFactory type :27, FACTORIES table + doc comment :29-43, the describe.each shape/determinism/climbable checks :49-86)
    - src/game/world/assets/index.ts (createCampFlag barrel export — signature takes (random, wind))
  </read_first>
  <action>
    Hoist the wind into a named const so the flag entry can reuse it: replace the inline
    `initCanopyWind(createWind(true))` with `const testWind = createWind(true);` followed
    by `initCanopyWind(testWind);`. Import `createCampFlag` from '../index'. Add a
    `createCampFlag` entry to the FACTORIES table adapting the two-arg signature to the
    AssetFactory shape: `create: random => createCampFlag(random, testWind)`, `climbable:
    false` (per 08-REVIEW WR-03's suggested adapter). The table's doc comment ("Every
    factory exported from assets/index.ts") becomes true again — do not weaken it. No
    other test changes; the flag must pass the existing group-shape, determinism, and
    not-climbable checks as-is (pole position and seeded banner color are deterministic;
    group rotation is not asserted by the suite).
  </action>
  <verify>
    <automated>pnpm vitest run src/game/world/assets/__tests__/assets.test.ts && pnpm vitest run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createCampFlag" src/game/world/assets/__tests__/assets.test.ts` >= 2 (import + FACTORIES entry)
    - `grep -c "const testWind" src/game/world/assets/__tests__/assets.test.ts` returns 1
    - assets.test.ts passes with the flag entry (3 new test cases: group shape, determinism, not climbable); `pnpm vitest run` exits 0; `pnpm build` exits 0
  </acceptance_criteria>
  <done>The flag runs through the same invariants gate as every other asset factory and the suite's coverage claim is accurate; suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Client-only cosmetic material lifetime + test changes; no user input, no network, no server, no new dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Tampering | canopy/flag GLSL template interpolation | low | mitigate | Unchanged invariant from 08-03: interpolated values come exclusively from compile-time windMath constants — never user input, never URL params; Task 2 adds only the aTreeHeight attribute name (a fixed string literal) |
| T-08-06-02 | Denial of Service | material dispose/rebuild cycle | low | mitigate | Rebuild is bounded: exactly one pool invalidation per game re-creation (wind identity guard), 4 canopy + 2 flag materials — no unbounded churn; pooling-preserved tests pin this |
</threat_model>

<verification>
- `pnpm vitest run` green after every task (703 existing tests + new lifecycle/attribute/flag-table tests)
- `pnpm build` exit 0 after every task
- windMaterialLifecycle.test.ts fails against the pre-fix singletons (RED recorded) and passes after — the StrictMode/reconnect freeze can never silently return
- Pooling counts unchanged within one wind lifetime: 4 canopy cap materials, 1 cloth, 1 pole (D-13)
- No update-loop registration added: sway remains 100% vertex-shader displacement (frozen-matrix rule untouched)
</verification>

<success_criteria>
- Flags and canopies sway on the CURRENT game's wind clock after any dispose/recreate cycle (SC1/WIND-01/WIND-02 unblocked for re-verification)
- Canopy tops move most regardless of terrain height under the tree (WIND-03/WR-02 closed)
- createCampFlag covered by the shared invariants suite (WR-03 closed)
- Full suite + build green; the human playtest checklist (08-VERIFICATION §Human Verification Required) remains the final gate via /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/08-wind-core/08-06-SUMMARY.md` when done
</output>
