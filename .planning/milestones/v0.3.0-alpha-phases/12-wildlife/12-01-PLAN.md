---
phase: 12-wildlife
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/game/systems/wildlifeMath.ts
  - src/game/systems/__tests__/wildlifeMath.test.ts
autonomous: true
requirements: [WILD-01, WILD-02, WILD-03]
must_haves:
  truths:
    - "wildlifeMath.ts is a THREE-free pure twin: every helper is out-param or returns a scalar (zero per-call allocation)"
    - "isDayTime is true across day keys and false at dusk/night; fireflyLevelAt returns 0 in the day band and >0 at dusk/night — reusing the shipped dayNightMath fireflyLevel channel"
    - "butterflyWander is bounded (|x|,|z| <= a1+a2), continuous, and non-repeating over a simulated minute"
    - "birdArc rises from y=0, eases out monotonically, and visible fades to 0 by t01=1"
    - "fireflyPulse stays in [floor,1], is periodic, and its phaseOffset decorrelates two instances"
    - "flushReady is false within the cooldown and true at/after FLUSH_COOLDOWN_SEC"
    - "inSpawnRing / beyondCull are correct at inner/outer/cull boundary radii"
  artifacts:
    - src/game/systems/wildlifeMath.ts
    - src/game/systems/__tests__/wildlifeMath.test.ts
  key_links:
    - "wildlifeMath imports samplePalette from ./dayNightMath for the day/dusk gate (the ONE shipped time-of-day channel)"
    - "the three Wave-1 factories delegate ALL creature math to this twin — it carries the phase's correctness risk"
---

<objective>
Create the pure, THREE-free math twin `wildlifeMath.ts` that every Wave-1 creature
factory delegates to, test-first, mirroring `windMath.ts` / `dayNightMath.ts`.

Purpose: Concentrate ALL of the phase's correctness risk (wander, pulse, arc,
day/dusk gate, spawn/cull ring, flush debounce) in one deterministic, allocation-free,
unit-testable module — so the three render factories stay thin and the visual work is
the only thing left for perceptual UAT.
Output: `wildlifeMath.ts` (pure functions + `as const` tunables) and its vitest twin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wildlife/12-RESEARCH.md
@.planning/phases/12-wildlife/12-PATTERNS.md
@src/game/systems/dayNightMath.ts
@src/game/systems/windMath.ts
@src/game/systems/__tests__/windMath.test.ts
</context>

<artifacts_this_plan_produces>
NEW symbols exported from `src/game/systems/wildlifeMath.ts`:
- Tunable bundles (all `as const`): `WANDER` (a1,f1,a2,f2,bobAmp,bobFreq), `PULSE` (rate,floor), `SPAWN` (inner,outer,cull), `BIRD` (rise,spread,life), and `FLUSH_COOLDOWN_SEC`.
- Gate fns: `isDayTime(phase: number): boolean`, `fireflyLevelAt(phase: number): number`.
- Motion fns: `butterflyWander(t, seed, out: {x,z}): void`, `butterflyBob(t, seed): number`, `fireflyPulse(t, phaseOffset): number`, `birdArc(t01, out: {y,spread,visible}): void`.
- Ring/debounce fns: `inSpawnRing(dx, dz): boolean`, `beyondCull(dx, dz): boolean`, `flushReady(lastSec, nowSec): boolean`.
NEW file `src/game/systems/__tests__/wildlifeMath.test.ts`.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Write the failing vitest twin pinning the whole wildlifeMath surface</name>
  <files>src/game/systems/__tests__/wildlifeMath.test.ts</files>
  <read_first>
    - src/game/systems/__tests__/windMath.test.ts (named-import block; pin BEHAVIOR not exact numbers except verbatim literals — the :22-24 comment)
    - src/game/systems/dayNightMath.ts (fireflyLevel keyframe values: 1 at phase 0.0, 0 at 0.12/0.3/0.5, 1 at 0.66/0.82; samplePalette smoothstep-blends)
    - .planning/phases/12-wildlife/12-RESEARCH.md (Code Example 1 — the exact function signatures + tunable values)
    - .planning/phases/12-wildlife/12-PATTERNS.md (wildlifeMath.test.ts section — the assertion list)
  </read_first>
  <action>
    Create the vitest twin FIRST (test-first, per the windMath/dayNightMath discipline).
    Import the full pure surface by name from '../wildlifeMath'. Write describe/it blocks that
    assert behavior, not fragile exact numbers (follow windMath.test.ts: exact-value pins only
    for verbatim-extracted `as const` literals; elsewhere assert bounds / continuity / monotonicity):
    - butterflyWander: fill an out-param over a simulated minute of t values; assert |out.x|,|out.z| never exceed WANDER.a1+WANDER.a2, that successive samples are continuous (no jumps), and that the path does not repeat within the minute (two distant t's differ).
    - butterflyBob: bounded by WANDER.bobAmp; continuous.
    - isDayTime: true at day keys (0.3, 0.5), false at dusk/night (0.66, 0.82, 0.0).
    - fireflyLevelAt: 0 (or < 0.01) in the day band (0.3, 0.5), > 0 at dusk (0.66) and night (0.82).
    - inSpawnRing: false inside SPAWN.inner, true between inner and outer, false beyond outer (test at the exact radii and just inside/outside).
    - beyondCull: false at SPAWN.cull, true just beyond it.
    - birdArc: fill an out-param; assert out.y === 0 at t01=0, out.y strictly increasing (ease-out) up to t01=1, out.spread grows with t01, and out.visible === 1 early then fades to 0 by t01=1.
    - fireflyPulse: range within [PULSE.floor, 1] across many t; periodic (equal at t and t + period); two different phaseOffsets give different values at the same t (decorrelation).
    - flushReady: false when nowSec-lastSec < FLUSH_COOLDOWN_SEC, true when >=, boundary exactly at FLUSH_COOLDOWN_SEC.
    This test file will not compile/pass yet — wildlifeMath.ts does not exist. That is the intended RED state.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/wildlifeMath.test.ts 2>&1 | grep -qiE 'cannot find module|failed|error' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and imports every exported symbol from '../wildlifeMath' by name.
    - Running the twin fails (module missing) — confirming RED before implementation.
    - Assertions cover all three requirements' math: WILD-01 (wander/isDayTime/ring), WILD-02 (birdArc/flushReady), WILD-03 (fireflyPulse/fireflyLevelAt).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement wildlifeMath.ts pure twin until the suite passes</name>
  <files>src/game/systems/wildlifeMath.ts</files>
  <read_first>
    - src/game/systems/windMath.ts (purity header; `as const` tunable bundles; deterministic-by-construction, zero imports beyond siblings)
    - src/game/systems/dayNightMath.ts (samplePalette export + sunDir out-param zero-alloc precedent)
    - .planning/phases/12-wildlife/12-RESEARCH.md (Code Example 1 — closed forms for every helper)
  </read_first>
  <action>
    Implement `src/game/systems/wildlifeMath.ts` as a THREE-free pure module. The ONLY import is
    `samplePalette` from './dayNightMath' (for the gate). Export the `as const` tunable bundles
    WANDER/PULSE/SPAWN/BIRD and the scalar FLUSH_COOLDOWN_SEC with the researcher's recommended
    values (WANDER a1 0.6/f1 0.9/a2 0.25/f2 2.3/bobAmp 0.35/bobFreq 1.1; PULSE rate 1.6/floor 0.15;
    SPAWN inner 8/outer 22/cull 30; BIRD rise 6/spread 3/life 1.4; FLUSH_COOLDOWN_SEC 6).
    Implement each helper as a closed form using the exact signatures from the artifacts section:
    - isDayTime(phase) returns samplePalette(phase).fireflyLevel < 0.01; fireflyLevelAt(phase) returns samplePalette(phase).fireflyLevel.
    - butterflyWander(t, seed, out): two incommensurate summed sines per axis into out.x/out.z (never a fresh object).
    - butterflyBob(t, seed): a single slow vertical sine scaled by WANDER.bobAmp.
    - fireflyPulse(t, phaseOffset): PULSE.floor + (1-PULSE.floor) * (0.5 + 0.5*sin(t*PULSE.rate + phaseOffset)).
    - birdArc(t01, out): out.y = BIRD.rise*(1-(1-t01)^2) ease-out rise; out.spread = BIRD.spread*t01; out.visible fades from 1 to 0 over the last 15% (max(0, ...)).
    - inSpawnRing / beyondCull: squared-distance comparisons against SPAWN radii.
    - flushReady(lastSec, nowSec): nowSec - lastSec >= FLUSH_COOLDOWN_SEC.
    Keep every function pure and allocation-free (out-params, scalars, no RNG). No inline magic
    numbers — all tunables live in the exported `as const` bundles so the tests single-source them.
    File stays well under the 300-LOC functional budget.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/wildlifeMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Task-1 twin passes fully (GREEN).
    - `grep -c "from 'three'" src/game/systems/wildlifeMath.ts` returns 0 (THREE-free twin).
    - The only import is samplePalette from './dayNightMath'.
    - Full suite still green: `pnpm exec vitest run`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side math module; no network, auth, persistence, or user-input parsing crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | wildlifeMath pure fns | low | accept | Deterministic pure functions with no side effects, no state, no I/O; nothing to tamper. Minimal by design (client-only cosmetic phase). |

No high-severity threats. This is a pure-math module in a client-only, cosmetic phase.
</threat_model>

<verification>
- `pnpm exec vitest run src/game/systems/__tests__/wildlifeMath.test.ts` passes.
- `pnpm exec vitest run` (full suite) stays green (additive — no existing test breaks).
- wildlifeMath.ts imports no THREE; only samplePalette from dayNightMath.
</verification>

<success_criteria>
The pure twin covers WILD-01/02/03 math (wander, day gate, ring, arc, debounce, pulse,
firefly gate) test-first, deterministic and allocation-free, ready for the Wave-1 factories.
</success_criteria>

<output>
Create `.planning/phases/12-wildlife/12-01-SUMMARY.md` when done.
</output>
