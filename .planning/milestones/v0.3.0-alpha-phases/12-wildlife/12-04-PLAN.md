---
phase: 12-wildlife
plan: 04
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/game/systems/createFireflies.ts
autonomous: true
requirements: [WILD-03]
must_haves:
  truths:
    - "Fireflies draw from one InstancedMesh (one draw call) of UNLIT MeshBasicMaterial quads — they stay bright while Phase 9 dims every lit material at night"
    - "Each firefly pulses via instanceColor brightness from fireflyPulse(t, phaseOffset) with a decorrelated per-instance phase; instanceColor is seeded at build and needsUpdate-gated"
    - "The swarm is gated on fireflyLevelAt(phase) > 0 (dusk/night); in full day all slots collapse to zeroMatrix and the update body is skipped — a clean day no-op"
    - "NO scene light of any kind is added — the combat lightPool is never touched"
    - "Zero per-frame allocation; hard-capped pool; added to scene root; dispose() frees instance GPU buffers"
  artifacts:
    - src/game/systems/createFireflies.ts
  key_links:
    - "delegates pulse + gate math to wildlifeMath.fireflyPulse / fireflyLevelAt (12-01)"
    - "drift reuses the summed-sine wander (lower amplitude) over grass near the player; ground y via getGroundHeight"
    - "MeshBasicMaterial (unlit) is the critical delta from the dust Lambert template"
---

<objective>
Create `createFireflies.ts` (WILD-03): a dusk/night swarm of small unlit bright quads that pulse
via instanceColor, glowing against the dark night palette WITHOUT any runtime light.

Purpose: The dusk/night living-world payoff — "glow" achieved by an unlit MeshBasicMaterial +
brightness pulse (no bloom pass exists; additive bands under the pixel filter), never a light
(a firefly light would recompile every lit material; the lightPool is size-4 combat-owned).
Output: `createFireflies.ts` on the createDustPuffs pool template with the unlit-material delta.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-wildlife/12-RESEARCH.md
@.planning/phases/12-wildlife/12-PATTERNS.md
@src/game/systems/createDustPuffs.ts
@src/game/systems/wildlifeMath.ts
@src/game/systems/dayNightMath.ts
</context>

<artifacts_this_plan_produces>
NEW `src/game/systems/createFireflies.ts`:
- `export const FIREFLY_POOL_SIZE` (~32).
- `export interface Fireflies { update(deltaSeconds, camera, playerX, playerZ, phase, t): void; dispose(): void }`.
- `export function createFireflies(scene, getGroundHeight): Fireflies` — self-managing (no public spawn()).
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dusk/night firefly InstancedMesh swarm (unlit, no lights)</name>
  <files>src/game/systems/createFireflies.ts</files>
  <read_first>
    - src/game/systems/createDustPuffs.ts (pool spine :76-186; note it uses Lambert BECAUSE it should dim — fireflies are the OPPOSITE intent)
    - src/game/systems/wildlifeMath.ts (fireflyPulse, fireflyLevelAt, butterflyWander for low-amplitude drift, SPAWN)
    - .planning/phases/12-wildlife/12-PATTERNS.md (createFireflies.ts section — the MeshBasicMaterial delta + the instanceColor pulse loop + Pitfall 6 seeding)
  </read_first>
  <behavior>
    - One InstancedMesh of FIREFLY_POOL_SIZE UNLIT MeshBasicMaterial quads, frustumCulled=false, no shadows, added to scene root; instanceColor seeded for every slot at build and set to DynamicDrawUsage.
    - update() gated on fireflyLevelAt(phase): when the level is 0 (day) collapse all slots to zeroMatrix once and skip the body; when > 0 (dusk/night) hover a swarm over grass near the player and pulse each firefly.
    - Per live firefly: brightness = fireflyPulse(t, phaseOffset) with a decorrelated per-instance phaseOffset; scratchColor = BASE_HUE * brightness -> setColorAt; colorDirty gated. Position: low-amplitude summed-sine drift around anchors ~0.5-1.5 above groundY.
    - Optional dusk fade-in: scale the count of LIT fireflies by the level (RESEARCH Open Q3 recommendation).
    - No scene light. Zero per-frame allocation.
  </behavior>
  <action>
    Copy the createDustPuffs pool spine (mesh setup + flags, zeroMatrix seed loop, setColorAt seed,
    instanceColor.setUsage, closure scratch, dispose ending in mesh.dispose()). The ONE critical
    delta: construct the InstancedMesh with a THREE.MeshBasicMaterial (unlit) instead of
    MeshLambertMaterial — fireflies must stay bright while the night scene dims lit materials.
    Deltas:
    - Interface: no public spawn(); self-manage in update(deltaSeconds, camera, playerX, playerZ, phase, t). Export FIREFLY_POOL_SIZE (~32).
    - Each pool entry holds anchorX/anchorZ/groundY/phaseOffset/seed/active.
    - Gate: const level = fireflyLevelAt(phase); if (level <= 0) { collapse all active slots to zeroMatrix once, mark matrixDirty, and return } — the clean day no-op.
    - When level > 0: top up (on a ~0.5s recheck timer) grass anchors in the SPAWN ring around the player (require the grass check as in butterflies), up to a target scaled by level (dusk fade-in); cull beyond SPAWN.cull.
    - Per live firefly per frame: position = anchor + small summed-sine drift (reuse butterflyWander with a lower-amplitude read, or a dedicated small drift) + hover y over groundY; scratchColor.copy(BASE_HUE).multiplyScalar(fireflyPulse(t, phaseOffset)) -> setColorAt -> colorDirty. Fireflies are tiny points — no billboarding needed (a small BoxGeometry voxel or a plane both read fine); pick the simplest that avoids per-instance lookAt.
    - Seed setColorAt(i, BASE_HUE) for EVERY slot at build and set instanceColor.needsUpdate whenever any pulse value changed that frame (Pitfall 6 — un-seeded instanceColor renders white / no pulse).
    Keep under 300 functional LOC. Add NO scene light and do NO GPU readback.
  </action>
  <verify>
    <automated>pnpm exec vitest run && grep -c 'MeshBasicMaterial' src/game/systems/createFireflies.ts</automated>
  </verify>
  <acceptance_criteria>
    - Exports createFireflies, FIREFLY_POOL_SIZE, and the Fireflies interface; no public spawn().
    - Material is unlit: `grep -c 'MeshBasicMaterial' src/game/systems/createFireflies.ts` returns >= 1 and `grep -c 'MeshLambertMaterial' src/game/systems/createFireflies.ts` returns 0.
    - No scene light: `grep -vE '^\s*(//|\*)' src/game/systems/createFireflies.ts | grep -cE 'PointLight|SpotLight|DirectionalLight|createLightPool'` returns 0.
    - Exactly one InstancedMesh (`grep -c 'new THREE.InstancedMesh' === 1`); instanceColor seeded at build; pulse + gate delegate to wildlifeMath.
    - No GPU readback (`grep -c 'readPixels'` returns 0) and no per-frame allocation in update().
    - Full suite green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side cosmetic render pool; player-local, no shared/authoritative state, no network/persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | firefly pool | low | accept | Purely cosmetic, player-local; no privilege or state change. Minimal by design (client-only cosmetic phase). |

No high-severity threats.
</threat_model>

<verification>
- `pnpm exec vitest run` stays green.
- Unlit MeshBasicMaterial (never Lambert); one InstancedMesh; no scene light; no GPU readback; zero per-frame alloc.
- Manual (deferred to 12-05 UAT): fireflies glow + shimmer at dusk/night (`?time=0.82`), absent by day, no material-recompile hitch, `?nofireflies` removes them.
</verification>

<success_criteria>
WILD-03 render factory complete: a dusk/night firefly swarm that glows via unlit quads +
instanceColor pulse at one draw call, gated by the tested firefly channel, never touching the
combat light pool.
</success_criteria>

<output>
Create `.planning/phases/12-wildlife/12-04-SUMMARY.md` when done.
</output>