---
phase: 13-camera-feel
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - src/game/createGame.ts
  - src/App.tsx
  - src/ui/SettingsScreen.tsx
autonomous: false
requirements: [CAM-01, CAM-02, CAM-03, CAM-04]

must_haves:
  truths:
    - "createGame constructs createCameraFeel and updateCamera delegates to it (shake block deleted, not duplicated) (CAM-03/CAM-04)"
    - "All 5 combat-shake sites call cameraFeel.shake(...) instead of assigning the local magnitude"
    - "FOV kick fires only on the local player's OWN crit hits in spawnWorldNumber/spawnPlayerNumber (isMine && kind in {crit,pvpCrit}); spawnSelfNumber is NOT tapped (D-06)"
    - "The LOCAL animate() call passes the reused MOTION_CFG_SCRATCH; remote animate() stays 3-arg (CAM-01/CAM-02, D-05)"
    - "Game.setReduceMotion(enabled) fans to cameraFeel + MOTION_CFG_SCRATCH; the existing :364 prefers-reduced-motion read is unified as the frame-1 seed (CAM-04/D-08/D-09)"
    - "App persists settings.reduceMotion, defaults to OS prefers-reduced-motion when the key is absent, and seeds a fresh game at construct time (CAM-04/D-09)"
    - "SettingsScreen shows a 'Samazināt kustību' toggle under ATTELOSANA (CAM-04)"
    - "localStorage settings.reduceMotion is coerced to boolean via === '1' — absent/garbage never enables via injection"
  artifacts:
    - "src/game/createGame.ts (cameraFeel wired, shake absorbed away, crit taps, scratch, setReduceMotion)"
    - "src/App.tsx (reduceMotion state + persist + construct seed + prop wiring)"
    - "src/ui/SettingsScreen.tsx (reduce-motion Toggle + props)"
  key_links:
    - "App.tsx settings effect -> Game.setReduceMotion -> cameraFeel + model scratch (one persisted signal, D-08)"
    - "crit handlers -> cameraFeel.kickFov(elapsedSeconds) (D-06)"
    - "pixel-filter boolean tracked in createGame -> MOTION_CFG_SCRATCH.pixelScale + cameraFeel.setPixelScale (D-01/A5)"
---

<objective>
Wire the phase together: construct `createCameraFeel` in createGame, shrink `updateCamera` to
delegate to it (deleting the now-absorbed shake state — no-legacy rule), convert the 5 combat-shake
sites to `cameraFeel.shake(...)` impulses, tap `cameraFeel.kickFov()` in the two "my crit landed"
handlers only (D-06), pass the reused MOTION_CFG_SCRATCH into the local model's `animate()`, and add
the persisted "reduce camera motion" toggle end-to-end (Game facade setter + App state/persist/seed +
SettingsScreen toggle) unified with the existing OS `prefers-reduced-motion` read (D-08/D-09). Then a
human playtest confirms the perceptual/pixel-crawl acceptance that cannot be automated.

Purpose: This is the integration that makes CAM-01..04 observable in-game and gives the reduce-motion
toggle ONE owner for all discretionary motion (lean, breathing, FOV kick, and the existing shake).
Output: modified createGame.ts + App.tsx + SettingsScreen.tsx; a green build; a passed playtest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-camera-feel/13-RESEARCH.md
@.planning/phases/13-camera-feel/13-PATTERNS.md

# The systems being wired (from 13-02 / 13-03):
@src/game/systems/createCameraFeel.ts
@src/game/entities/createCharacterModel.ts
@src/game/systems/cameraFeelMath.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire createCameraFeel into createGame — updateCamera, shake impulses, crit taps, scratch, setReduceMotion</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - .planning/phases/13-camera-feel/13-PATTERNS.md §"src/game/createGame.ts (MOD)" — the 5 concrete edit clusters with exact in-file analogs (setPixelFilter shape, the :364 reduceMotion unify, updateCamera shrink, the local animate call, the crit-handler taps).
    - src/game/createGame.ts — grep-locate (line numbers drift; do NOT trust them blindly): `let shakeMagnitude`, `SHAKE_DECAY_RATE`/`SHAKE_FLOOR`, the `updateCamera` body, the 5 sites assigning the shake magnitude (slime slam / sword swing / swirl / dash / default slam), `spawnWorldNumber`, `spawnPlayerNumber`, `spawnSelfNumber`, `setPixelFilter` (interface + impl), the `const reduceMotion = window.matchMedia(...)` at ~:364, `movingSunEnabled`, `playerModel.animate(`, and `remotePlayer.model.animate(`.
    - .planning/phases/13-camera-feel/13-RESEARCH.md §"Pattern 4" (crit trigger sites + rate gate) and §"Pattern 5"/"Pattern 6" (unify the OS read; sun re-pin is OPTIONAL — do NOT wire runtime sun re-pin, it needs a new setter and D-10 is optional).
  </read_first>
  <action>
    Edit `src/game/createGame.ts` in five surgical clusters. (1) Construct the system:
    `const cameraFeel = createCameraFeel({ camera: pixelRenderer.camera, reduceMotion })` where
    `reduceMotion` is the existing `matchMedia('(prefers-reduced-motion: reduce)')` value read at
    ~:364 — change that `const reduceMotion` to a `let` so it can be re-assigned by the setter and
    remains the frame-1 seed (D-09 unify; keep the existing `movingSunEnabled` derivation using the
    same value). (2) Shrink `updateCamera` to: copy player pos + CAMERA_OFFSET into desiredPosition,
    call `cameraFeel.apply(desiredPosition, deltaSeconds)`, then the existing camera `position.lerp`
    + `lookAt`. DELETE the local shake state and the two shake constants and the in-body jitter/decay
    block from createGame entirely — they now live in createCameraFeel (no duplication). (3) Replace
    each of the 5 combat-shake magnitude assignments with `cameraFeel.shake(<the same expression>)`
    (the Math.max folds into shake()). (4) Add a reused `const MOTION_CFG_SCRATCH = { reduceMotion,
    pixelScale }` allocated ONCE near the render-loop state; before the LOCAL `playerModel.animate(...)`
    call, mutate its fields (`MOTION_CFG_SCRATCH.reduceMotion = reduceMotion; MOTION_CFG_SCRATCH.pixelScale
    = pixelated ? CAMERA_FEEL.PIXEL_SCALE : 1`) and pass it as the 4th arg — never a fresh object literal
    per frame (Pitfall 5). Leave the remote `remotePlayer.model.animate(...)` call 3-arg (D-05). Track a
    `let pixelated` boolean alongside setPixelFilter (renderer has no getter, A5); update it in
    setPixelFilter and also call `cameraFeel.setPixelScale(pixelated ? CAMERA_FEEL.PIXEL_SCALE : 1)`
    there. (5) In `spawnWorldNumber` add `if (isMine && kind === 'crit') cameraFeel.kickFov(elapsedSeconds);`
    and in `spawnPlayerNumber` add `if (isMine && (kind === 'crit' || kind === 'pvpCrit'))
    cameraFeel.kickFov(elapsedSeconds);`. Do NOT tap `spawnSelfNumber` (that is damage taken — D-06).
    Finally add `setReduceMotion(enabled: boolean): void` to the `Game` interface beside setPixelFilter,
    implemented to set the module `reduceMotion = enabled`, call `cameraFeel.setReduceMotion(enabled)`,
    and write `MOTION_CFG_SCRATCH.reduceMotion = enabled`. Import createCameraFeel from
    `./systems/createCameraFeel` and CAMERA_FEEL from `./systems/cameraFeelMath`. Pass a monotonic
    seconds value as `now` to kickFov (reuse the loop's elapsedSeconds).
    <!-- planner-discipline-allow: shakeMagnitude -->
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Project compiles. cameraFeel constructed; updateCamera delegates; `grep -c 'shakeMagnitude' src/game/createGame.ts` is 0 (state + all 5 sites + comments removed, no legacy); `cameraFeel.shake(` appears 5 times; `cameraFeel.kickFov(` appears in spawnWorldNumber and spawnPlayerNumber only; local animate() is 4-arg with MOTION_CFG_SCRATCH; remote animate() stays 3-arg; Game.setReduceMotion exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduce-motion toggle UI — App state/persist/seed + SettingsScreen toggle</name>
  <files>src/App.tsx, src/ui/SettingsScreen.tsx</files>
  <read_first>
    - .planning/phases/13-camera-feel/13-PATTERNS.md §"src/App.tsx (MOD)" and §"src/ui/SettingsScreen.tsx (MOD)" — the three App touch points (state init, persist+apply effect, construct-time seed, prop wiring) and the SettingsScreen prop + Toggle, each with the exact `pixelFilter` analog to clone.
    - src/App.tsx — the `pixelFilter` useState (~:93), the persist effect (~:926), the new-game construct seed (~:828), and the SettingsScreen prop wiring (~:1117).
    - src/ui/SettingsScreen.tsx — the `SettingsScreenProps` fields (~:22-23), the destructure (~:91-92), and the ATTELOSANA Toggle block (~:157-160) using Toggle.tsx.
  </read_first>
  <action>
    Clone the `pixelFilter` wiring for a new `reduceMotion` boolean, differing only in the default.
    In App.tsx: add `const [reduceMotion, setReduceMotion] = useState(() => { const saved =
    localStorage.getItem('settings.reduceMotion'); if (saved === null) return
    window.matchMedia('(prefers-reduced-motion: reduce)').matches; return saved === '1'; });` (OS
    default when the key is absent, D-09; coerce with `=== '1'` so any non-'1' value is off). Add a
    persist+apply effect mirroring the pixelFilter one: `localStorage.setItem('settings.reduceMotion',
    reduceMotion ? '1' : '0'); gameRef.current?.setReduceMotion(reduceMotion);` keyed on `[reduceMotion]`.
    Add the construct-time seed beside the pixelFilter seed so a fresh game starts correct on frame 1:
    `game.setReduceMotion((() => { const s = localStorage.getItem('settings.reduceMotion'); return s ===
    null ? window.matchMedia('(prefers-reduced-motion: reduce)').matches : s === '1'; })());` (same
    predicate as the state init). Pass `reduceMotion={reduceMotion} onToggleReduceMotion={setReduceMotion}`
    to SettingsScreen. In SettingsScreen.tsx: add `reduceMotion: boolean;` and `onToggleReduceMotion(next:
    boolean): void;` to SettingsScreenProps, destructure them, and add `<Toggle label="Samazinat kustibu"
    checked={reduceMotion} onChange={onToggleReduceMotion} />` under the ATTELOSANA section (use the
    correct Latvian diacritics matching the sibling labels). No other behavior changes.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    Project compiles. App has a reduceMotion state defaulting to OS pref when the key is absent, a persist+apply effect calling setReduceMotion, and a construct-time seed; SettingsScreen renders a reduce-motion Toggle under ATTELOSANA and forwards the two new props.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playtest gate — feel + pixel-crawl + toggle</name>
  <files>(none — perceptual verification only)</files>
  <action>
    Automate nothing further; this task is the human acceptance gate for the perceptual criteria
    (feel, pixel-crawl, nausea) that no unit test can cover. Ensure `npm run build` is green and the
    game is loadable, then walk the six checks below and record the verdict.
  </action>
  <what-built>
    Character run-lean + idle breathing on the model, a rare-crit FOV kick, and a persisted
    "reduce camera motion" toggle that zeroes all three plus the existing combat shake — all
    delegating to the tested cameraFeelMath twin. The remaining acceptance is perceptual and
    cannot be automated (feel, crawl, nausea).
  </what-built>
  <how-to-verify>
    1. `npm run build`, then load the game (laragon dist or `npm run dev`).
    2. LEAN (CAM-01): run in several directions incl. E/W and N/S — the character body should
       pitch slightly forward INTO the run direction and spring back upright when you stop. The
       WORLD HORIZON must never tilt (lean is on the model, not the camera). Lean direction must
       read correctly running E/W as well as N/S (Pitfall 1 check).
    3. BREATHING (CAM-02): stand still — a subtle calm breathing sway on the model, never on the
       camera, never while moving.
    4. FOV KICK (CAM-03): land your own crit hits — a brief FOV punch, noticeable but RARE. Trigger
       an AoE/swirl that crits several enemies in one frame — it must NOT strobe (rate gate). Taking
       damage must NOT kick.
    5. PIXEL-CRAWL (CAM-04 / D-03): turn the pixel filter ON and stand still — inspect the stationary
       silhouette closely for shimmer/crawl from breathing (the worst offender). If crawl is visible,
       reduce breathing amplitude in cameraFeelMath.CAMERA_FEEL before frequency and rebuild.
    6. TOGGLE (CAM-04 / D-08): open Settings > ATTELOSANA, enable "reduce camera motion" — lean,
       breathing, FOV kick, AND combat shake must ALL stop immediately (even mid-decay). Reload the
       page — the choice persists. Clear localStorage settings.reduceMotion and reload with OS
       reduce-motion enabled — it should default ON.
  </how-to-verify>
  <resume-signal>Type "approved" once all six checks pass, or describe what feels wrong / crawls / strobes so the constants can be retuned.</resume-signal>
  <verify>
    <human-check>All six checks pass: lean reads correctly in every run direction with no horizon tilt; idle breathing is calm; FOV kick is rare and non-strobing and never fires on damage taken; no pixel-crawl on a standing character in pixel mode; the toggle instantly zeroes lean + breathing + FOV kick + shake, persists across reload, and defaults to the OS pref.</human-check>
  </verify>
  <done>Human verdict recorded: "approved" closes the phase; any failure produces a concrete defect list (which effect, which check) for constant retuning in cameraFeelMath.CAMERA_FEEL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> App state | The persisted `settings.reduceMotion` string is read back on load; it is attacker-controllable only via same-origin local tampering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04a | Tampering | localStorage settings.reduceMotion read | low | mitigate | Coerce with `=== '1'` (never eval/JSON-into-logic); absent key -> OS `prefers-reduced-motion`; any other value -> off. No injection path, no deserialization. |
| T-13-04b | Denial of Service | FOV strobe on multi-crit AoE frame | low | mitigate | KICK_COOLDOWN_S rate gate in createCameraFeel (verified in 13-01/13-02); playtest step 4 confirms. |
</threat_model>

<verification>
- `npm run build` clean (tsc -b typecheck + vite build).
- `npm test` (full vitest suite) green — cameraFeelMath twin included, nothing else regressed.
- `grep -c 'shakeMagnitude' src/game/createGame.ts` == 0 (no-legacy: shake state fully absorbed).
- `cameraFeel.shake(` appears exactly 5x; `cameraFeel.kickFov(` only in the two crit handlers.
- Manual playtest (Task 3) approved — this is the honest acceptance gate for feel and pixel-crawl (perceptual, irreducible).
</verification>

<success_criteria>
In-game: the local character leans into run direction (CAM-01) and breathes while idle (CAM-02) on
the model with no horizon tilt; own crit hits fire a rare, non-strobing FOV kick (CAM-03); a persisted
reduce-motion toggle zeroes lean + breathing + FOV kick + combat shake and defaults to the OS pref
(CAM-04); and no camera-feel effect crawls in pixel mode (CAM-04/D-03), confirmed by playtest.
</success_criteria>

<output>
Create `.planning/phases/13-camera-feel/13-04-SUMMARY.md` when done.
</output>
