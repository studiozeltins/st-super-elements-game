---
phase: 11-lived-in-props-wear
plan: 06
type: tdd
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/game/systems/surfaceAt.ts
  - src/game/systems/__tests__/surfaceAt.test.ts
autonomous: true
requirements: [WEAR-05]
must_haves:
  truths:
    - "surfaceAt(x,z) returns the correct ground surface (grass | dirt | path | town) at any world point (D-12)"
    - "Classification order is town > dirt(road) > path(footpath) > grass, with the road threshold matching grassPlacement (>0.5)"
    - "surfaceAt is pure: zero allocation, no GPU read (returns a string literal) (D-12, client-perf rule)"
  artifacts:
    - src/game/systems/surfaceAt.ts
    - src/game/systems/__tests__/surfaceAt.test.ts
  key_links:
    - "surfaceAt composes roadFactor + footpathFactor (Plan 02) + isInTown — single source of truth for 'what surface is here'"
    - "The Surface tag set must align with FootstepSurface (Plan 08 widens it to match)"
    - "The road threshold >0.5 MUST equal grassPlacement.ts:74 (dust never spawns where grass still grows)"
---

<objective>
Add the pure `surfaceAt(x,z)` classifier that composes the three existing pure functions (`isInTown`, `roadFactor`, `footpathFactor`) into one authoritative ground-surface answer (WEAR-05, D-12). It is the single source of truth consumed by both dust spawning and footstep audio (Plan 08).

Purpose: One classifier, tested at the boundaries, prevents drift between "where dust spawns" and "where the grass rustle plays" — and keeps both off the 144→20fps cliff (pure, zero-alloc, no GPU read).
Output: `surfaceAt.ts` + an exhaustive boundary test (the explicit Wave-0 gap).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-lived-in-props-wear/11-CONTEXT.md
@.planning/phases/11-lived-in-props-wear/11-RESEARCH.md
@.planning/phases/11-lived-in-props-wear/11-PATTERNS.md
@src/game/world/roads.ts
@src/game/world/town/townPlan.ts
@src/game/world/grassPlacement.ts
@src/game/systems/__tests__/groundInfluenceMath.test.ts
</context>

<artifacts>
## Artifacts this plan produces
- `export type Surface = 'grass' | 'dirt' | 'path' | 'town'`
- `export function surfaceAt(x: number, z: number): Surface`
- New `src/game/systems/__tests__/surfaceAt.test.ts` (boundary + ordering + exhaustive/mutually-exclusive).
- Consumes: `roadFactor`/`footpathFactor` (Plan 02, `roads.ts`), `isInTown` (`town/townPlan.ts`).
- NOTE for Plan 08: `Surface` is the tag set `createMovementAudio.FootstepSurface` widens to match (align both to the same four tags).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: surfaceAt.ts pure classifier + boundary test</name>
  <files>src/game/systems/surfaceAt.ts, src/game/systems/__tests__/surfaceAt.test.ts</files>
  <read_first>
    - src/game/world/roads.ts (roadFactor :95-105, footpathFactor from Plan 02)
    - src/game/world/town/townPlan.ts (isInTown :42, TOWN_DISTRICTS :54-63, plaza at origin)
    - src/game/world/grassPlacement.ts (:74 the roadFactor>0.5 threshold this MUST match)
    - src/game/systems/__tests__/groundInfluenceMath.test.ts (vitest describe/it/expect style for a pure module)
    - 11-RESEARCH.md §"Pattern 5"; 11-PATTERNS.md §"surfaceAt.ts" + §"surfaceAt.test.ts"
  </read_first>
  <behavior>
    - Origin (0,0) / any point inside a TOWN_DISTRICTS tile → 'town'.
    - A point on a getRoads() centerline (roadFactor > 0.5), off town → 'dirt'.
    - A point on a getFootpaths() spine that is NOT a road and NOT town (footpathFactor > 0.25) → 'path'.
    - Open meadow far from town/roads/footpaths → 'grass'.
    - Ordering / precedence: town wins over road, road wins over footpath, footpath wins over grass (test a contrived point that would satisfy multiple and confirm the higher-precedence tag).
    - Exhaustive + mutually exclusive: for a grid of sampled points, surfaceAt always returns exactly one of the four tags.
  </behavior>
  <action>
    Create `surfaceAt.ts` exporting `type Surface = 'grass' | 'dirt' | 'path' | 'town'` and `surfaceAt(x,z)` composing, in precedence order: `if (isInTown(x,z)) return 'town'; if (roadFactor(x,z) > 0.5) return 'dirt'; if (footpathFactor(x,z) > 0.25) return 'path'; return 'grass'`. The `> 0.5` road threshold MUST match `grassPlacement.ts:74` (single source of truth — dust never spawns where grass still grows). No allocation, no GPU read — the function only returns a string literal (D-12). Zero-THREE module (purity mirrors windMath).

    Write `surfaceAt.test.ts` FIRST (tests-first — this is the explicit Wave-0 test gap) asserting the full <behavior> block. Derive test points from real data: origin for town; a `getRoads()` centerline point for dirt; a `getFootpaths()` spine point (chosen to be off any road and outside town) for path; a far open-sea/meadow coordinate for grass. Include the precedence/ordering assertion and the exhaustive-single-tag grid assertion. Then implement to green.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/systems/__tests__/surfaceAt.test.ts</automated>
  </verify>
  <done>surfaceAt.ts exports Surface + surfaceAt with the town>dirt>path>grass precedence and the >0.5 road threshold matching grassPlacement; surfaceAt.test.ts covers all four tags, precedence, and exhaustiveness, and is green.</done>
</task>

</tasks>

<threat_model>
Client-only, cosmetic pure function. `surfaceAt` reads only fixed world data via pure helpers and returns a string; no network, auth, input, persistence, or reducers. No allocation / no GPU read (perf-safe by construction).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | (n/a) | low | accept | Pure classification over fixed data; no external input surface. No mitigation required. |
| T-11-SC | Tampering | package installs | low | accept | Zero packages installed. |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/systems/__tests__/surfaceAt.test.ts` green.
- `pnpm test` (full suite) green.
</verification>

<success_criteria>
A single authoritative, boundary-tested surface classifier exists, ready to gate dust spawning and footstep audio in Plan 08.
</success_criteria>

<output>
Create `.planning/phases/11-lived-in-props-wear/11-06-SUMMARY.md` when done.
</output>
