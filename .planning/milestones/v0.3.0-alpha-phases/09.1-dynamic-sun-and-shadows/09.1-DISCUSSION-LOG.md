# Phase 09.1: Dynamic Sun and Shadows - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-14
**Phase:** 09.1-dynamic-sun-and-shadows
**Mode:** `--auto` (recommended option auto-selected each area; no interactive prompts)
**Areas discussed:** Sun-arc geometry, Shadow-basis lifecycle, Texel-snap under motion, Reduce-motion fallback, Flag precedence

---

## Sun-arc geometry — where computed + arc shape/cap

| Option | Description | Selected |
|--------|-------------|----------|
| Pure `dayNightMath` twin, capped dome | `sunDir(phase01)` in the zero-THREE tested twin; azimuth sweeps, elevation held above a tested floor | ✓ |
| Compute in `createDayNightCycle` directly (THREE) | Skip the pure twin; less testable | |
| Full free arc (no cap) | Sun grazes horizon at dawn/dusk | (rejected — violates SHADOW-02/ATMO-03) |

**Auto choice:** Pure twin + capped dome. Mirrors D-08 `phase01` clock + the Phase 8/9 `windMath`/`dayNightMath` pure-twin discipline; the elevation floor is the readability contract (SHADOW-02).

---

## Shadow-basis lifecycle — frozen consts → dynamic

| Option | Description | Selected |
|--------|-------------|----------|
| World-held mutable scratch, per-frame recompute in `setShadowFocus` | Relocate the existing Gram-Schmidt basis build into the frame path, zero-alloc | ✓ |
| Recompute basis inside day/night and pass full basis to world | Two writers of shadow mechanics | |
| Keep basis frozen, only rotate the light | Breaks texel-snap (snap assumes basis matches light dir) | (rejected) |

**Auto choice:** World-held mutable scratch. Day/night writes sun direction via one setter; the world owns shadow-camera mechanics (single-writer contract preserved).

---

## Texel-snap correctness under a rotating basis

| Option | Description | Selected |
|--------|-------------|----------|
| Continuous recompute; rely on slow 20-min arc (sub-texel/frame); playtest-verify | Simplest; keeps existing snap math | ✓ |
| Quantize sun direction to coarse angular buckets up front | Pre-build complexity before measuring | (contingency only) |
| Raise shadow map 1024→2048 up front | Spend FPS budget before measuring | (contingency only) |

**Auto choice:** Continuous recompute, verify in golem-fight playtest; escalation ladder (D-08) held as contingency, not pre-built.

---

## Reduce-motion / `?nomovingsun` fallback

| Option | Description | Selected |
|--------|-------------|----------|
| Both `?nomovingsun` URL flag AND `prefers-reduced-motion` media query | Two independent triggers pin sun to high-noon | ✓ |
| URL flag only | Ignores OS accessibility preference | |
| Media query only | No manual FPS-bisect handle | |

**Auto choice:** Both triggers. Frozen position = exact Phase 9 `SUN_OFFSET` key (SHADOW-04 byte-exact). `?nomovingsun` freezes sun position only — colors keep drifting (distinct from `?nodaynight`).

---

## Flag interaction & precedence

| Option | Description | Selected |
|--------|-------------|----------|
| `?nodaynight` ⊇ `?nomovingsun` (nodaynight freezes palette+sun; nomovingsun freezes sun only) | Consistent bisection semantics | ✓ |
| Independent flags, no implication | `?nodaynight` would leave sun moving over a frozen palette (odd) | |

**Auto choice:** nodaynight implies frozen sun; moving-sun gated on existing `dayNightEnabled`.

---

## Claude's Discretion
- Exact elevation floor/peak angles + azimuth sweep range (playtest-tuned from ~40°→75° envelope).
- `world.setSunDirection(dir)` vs a `sunDirection` member on `AmbienceHandles`.
- Direction representation in the pure twin (unit vector vs azimuth/elevation).
- Azimuth full-360 wrap vs E↔W mirror (must be continuous across the phase wrap).
- `SUN_DISTANCE` constant (default preserves current `|SUN_OFFSET|` ≈ 61.6).

## Deferred Ideas
- Moon-as-second-caster / sun+moon billboard sprites / star field — new capability, own phase.
- Cloud/weather shadows — deferred milestone-wide.
- 8 todo matches (combat/UI/transcendence backlog) reviewed, all false positives, none folded.
