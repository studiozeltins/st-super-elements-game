---
phase: 09.1
slug: dynamic-sun-and-shadows
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-14
---

# Phase 09.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 3.2.4 |
| **Config file** | repo root (existing `dayNightMath.test.ts` already runs under it) |
| **Quick run command** | `pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts` |
| **Full suite command** | `pnpm exec vitest run` |
| **Estimated runtime** | ~3 seconds (quick), ~15 seconds (full) |

> pnpm ONLY — `npm i` crashes on the symlink layout (memory: pnpm-package-manager).

---

## Sampling Rate

- **After every task commit:** Run `pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts`
- **After every plan wave:** Run `pnpm exec vitest run` (full suite green)
- **Before `/gsd-verify-work`:** Full suite green + golem-fight FPS playtest (no regression) + two-client LAN sun-angle glance
- **Max feedback latency:** ~15 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 09.1-01-01 | 01 | 1 | SHADOW-01/02/04 | T-09.1-01 / — | N/A (pure math, no surface) | unit (RED) | `pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts` | ✅ extend existing | ⬜ pending |
| 09.1-01-02 | 01 | 1 | SHADOW-01/02/04 | T-09.1-01 / — | N/A | unit (GREEN) | `pnpm exec vitest run src/game/systems/__tests__/dayNightMath.test.ts` | ✅ | ⬜ pending |
| 09.1-02-01 | 02 | 2 | SHADOW-03/04 | T-09.1-02 / — | ?nomovingsun read-only flag | typecheck + basis-equality (Plan 01) | `pnpm exec tsc --noEmit` | ✅ | ⬜ pending |
| 09.1-02-02 | 02 | 2 | SHADOW-01/03 | T-09.1-02 / — | N/A | typecheck | `pnpm exec tsc --noEmit` | ✅ | ⬜ pending |
| 09.1-02-03 | 02 | 2 | SHADOW-03/04 | T-09.1-02 / — | ?nomovingsun presence-only | typecheck + grep | `pnpm exec tsc --noEmit` | ✅ | ⬜ pending |
| 09.1-03-01 | 03 | 3 | SHADOW-03 | T-09.1-03 / — | N/A | build + full suite | `pnpm exec vitest run && pnpm run build` | ✅ | ⬜ pending |
| 09.1-03-02 | 03 | 3 | SHADOW-01/02/03/04 | T-09.1-03 / — | local-only debug reducers | manual (playtest) | `scripts/fps_playtest.py` during `debug_spawn_goliaths` | manual-only | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/game/systems/__tests__/dayNightMath.test.ts` — EXTEND (file exists): add floor invariant (loop all phases, `asin(dir.y) >= FLOOR`), noon peak (`sunDir(0.5)` ~= normalized SUN_OFFSET), wrap continuity (0.999 vs 0.001), dawn/dusk azimuth asymmetry, exact-pin D-03 constants.
- [ ] Basis-equality assertion via the extracted pure `buildSunBasis(dir)` helper — proves the SHADOW-04 frozen-basis reproduction is byte-testable without a renderer (Plan 01 Task 1/2).
- No framework install needed — vitest 3.2.4 already present.

*Wave 0 work is folded into Plan 01 (the TDD pure-twin plan); the RED task (09.1-01-01) IS the Wave 0 scaffold.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| No FPS regression in a golem-class fight | SHADOW-03 | GPU/frame-cost — not unit-testable; the milestone's stated real risk (summed frame cost) | Built page (laragon dist) → `debug_grant_loadout` + `debug_spawn_goliaths` (local) → run `scripts/fps_playtest.py` during the fight; compare to `?nomovingsun` baseline. No regression. |
| No shadow shimmer/crawl on static props under the pixel filter | SHADOW-03 | Perception under the nearest-filter upscale — not measurable in a unit test | Stand still by a static prop at dawn AND dusk; confirm shadow edges do not buzz/crawl. |
| Two LAN clients see the same sun angle | SHADOW-01 | Requires two real clients + server clock — cross-machine | Open two LAN clients on the same host; confirm identical sun angle / shadow direction at the same moment. |
| ?nomovingsun midday A/B: no visible shadow jump | SHADOW-04 | Byte-exact frozen key vs live arc is a visual A/B | At midday, toggle `?nomovingsun` across a reload; confirm no jump in shadow length/angle. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or fold into the Wave 0 / manual-gate structure
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (Plan 03 Task 2 is the sole manual gate, preceded by an automated build/suite task)
- [x] Wave 0 covers all MISSING references (extend dayNightMath.test.ts; buildSunBasis helper)
- [x] No watch-mode flags (all `vitest run`, never `vitest` watch)
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-14
