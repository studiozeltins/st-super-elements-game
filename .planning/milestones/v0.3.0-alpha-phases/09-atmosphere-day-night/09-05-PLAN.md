---
phase: 09-atmosphere-day-night
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - src/game/createGame.ts
  - src/hooks/useGameTableBridge.ts
autonomous: false
requirements: [ATMO-01, ATMO-02, ATMO-03, DAYNITE-01, DAYNITE-02, DAYNITE-03, DAYNITE-04]

must_haves:
  truths:
    - "createGame constructs a serverClock + daynight cycle after createMondstadtWorld (so world.ambience exists) and advances daynight once in frame() right after wind.update() — the only daynight.update call, pulled by the loop, never React (Pitfall 6.1)"
    - "?nodaynight freezes the palette at a neutral day key (mirrors the ?nowind/?nosmoke bisect convention, D-09)"
    - "Game.syncServerClock(serverMicros) re-anchors the clock; useGameTableBridge taps the enemy/goliath onUpdate EventContext and, on ctx.event.tag === 'Reducer', calls it with ctx.event.value.timestamp.microsSinceUnixEpoch"
    - "All LAN clients converge to the same time of day within one ~150ms tick; a mid-cycle joiner snaps to the correct time (no 30s sunrise on load)"
  artifacts:
    - src/game/createGame.ts
    - src/hooks/useGameTableBridge.ts
  key_links:
    - "The bridge tap is a setter called from a live table callback — NOT a render; the cache-seed iter() loop (no ctx) never anchors, and the tag==='Reducer' guard skips the SubscribeApplied snapshot"
    - "daynight.update(serverClock.nowMicros()) runs after wind.update() in the single frame() — one coherent clock, no private accumulator"
---

<objective>
Wire the day/night runtime into the live game: construct `serverClock` + `createDayNightCycle` in `createGame` against `world.ambience`, add the `?nodaynight` bisect flag, advance the cycle once per `frame()` after `wind.update()`, expose `Game.syncServerClock`, and tap the discarded `EventContext` in `useGameTableBridge` to re-anchor the clock off the `worldTick` reducer timestamp (`Date.now()` fallback). Close with a two-client LAN human playtest gate covering night readability, LAN time-of-day sync, frozen sun direction, in-radius contrast, and lantern fade.

Purpose: Turn the built pieces (math, clock, cycle, ambience handles, lanterns) into a running, LAN-synced day/night cycle and verify the visual/perf/sync behaviors that are not unit-testable.
Output: Modified `createGame.ts` + `useGameTableBridge.ts`; human playtest sign-off.
</objective>

<artifacts_produced>
NEW symbols created by this plan:
- `Game.syncServerClock(serverMicros: bigint): void` (added to the Game interface + return object; passthrough to `serverClock.anchor`).
- `?nodaynight` URLSearchParams bisect flag + `dayNightEnabled` const in createGame.ts.
- Local consts `serverClock` (createServerClock) and `daynight` (createDayNightCycle) in createGame.ts.
- EventContext tap in useGameTableBridge.ts (widened `ctx` type, no new exported symbol).
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atmosphere-day-night/09-RESEARCH.md
@.planning/phases/09-atmosphere-day-night/09-PATTERNS.md
@src/game/createGame.ts
@src/hooks/useGameTableBridge.ts
@.planning/phases/09-atmosphere-day-night/09-03-SUMMARY.md
@.planning/phases/09-atmosphere-day-night/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire serverClock + daynight into createGame (flag, construct, frame line, interface)</name>
  <read_first>
    - src/game/createGame.ts:296-321 (perfFlags bisect convention + wind construction + createMondstadtWorld call — add ?nodaynight, and construct clock/cycle AFTER world so world.ambience exists)
    - src/game/createGame.ts:1320-1327 (frame() head — wind.update() is "the ONLY clock advance"; add daynight.update immediately after)
    - src/game/createGame.ts:138-182 (Game interface — add syncServerClock next to syncGoliaths) and :1774-1792 (the return object where syncEnemies/syncGoliaths are implemented — add syncServerClock passthrough)
    - src/game/net/createServerClock.ts + src/game/systems/createDayNightCycle.ts (the modules from Plan 04)
  </read_first>
  <files>src/game/createGame.ts</files>
  <action>
    Add `const dayNightEnabled = !perfFlags.has('nodaynight');` beside the existing `windEnabled`/`smokeEnabled` (:311-312) and extend the bisect-flag comment (:297-299) to list `?nodaynight`. After `createMondstadtWorld` returns (:314-321, so `world.ambience` exists), construct `const serverClock = createServerClock();` and `const daynight = createDayNightCycle(dayNightEnabled, serverClock, world.ambience);`. In `frame()`, immediately after `wind.update(deltaSeconds)` (:1327), add the single line `daynight.update(serverClock.nowMicros());` (pulled by the loop; never derived per React render — Pitfall 6.1). Add `syncServerClock(serverMicros: bigint): void` to the `Game` interface (:138-182) near the other sync* methods with a doc comment, and implement it in the return object (:1774) as `syncServerClock(m) { serverClock.anchor(m); }`. Keep all logic in the sibling factory — this file only wires (createGame is ~1,963 LOC, must not grow logic).
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    ?nodaynight flag present; serverClock + daynight constructed after world; exactly one daynight.update(serverClock.nowMicros()) call, right after wind.update(); Game.syncServerClock declared + implemented as a serverClock.anchor passthrough. tsc clean; `pnpm build` succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tap the EventContext in useGameTableBridge to re-anchor the clock</name>
  <read_first>
    - src/hooks/useGameTableBridge.ts:28-71 (the mirror() callbacks that currently discard `_ctx: unknown` at :50-61 — the tap seam; and the iter() cache-seed at :48 that has NO ctx)
    - .planning/phases/09-atmosphere-day-night/09-RESEARCH.md §"Pattern 2" (verified access path: ctx.event.tag === 'Reducer' → ctx.event.value.timestamp.microsSinceUnixEpoch; guards; the one-line first-tick tag log for Assumption A1)
    - src/module_bindings (EventContext type export)
  </read_first>
  <files>src/hooks/useGameTableBridge.ts</files>
  <action>
    Widen the discarded ctx in the `mirror()` handle callbacks from `unknown` to `EventContext` (import `type { EventContext } from '../module_bindings'`; update the `TableHandle` onInsert/onUpdate/onDelete signatures accordingly). In the `onUpdate` callback path (the ~150ms worldTick cadence that touches enemy/goliath rows), add: on `ctx.event.tag === 'Reducer'`, read `const micros: bigint = ctx.event.value.timestamp.microsSinceUnixEpoch` and call `gameRef.current?.syncServerClock(micros)` — a re-anchor, NOT a render. Guards: do NOT anchor from the `handle.iter()` cache-seed loop (it has no ctx); the `tag === 'Reducer'` guard already skips the `'SubscribeApplied'` snapshot. As the first runtime step, add a one-time console log of `ctx.event.tag` (and reducer name) on the first goliath/enemy update to confirm a scheduled reducer's broadcast to a non-caller arrives tagged `'Reducer'` (Assumption A1; the `Date.now()` fallback covers it either way). Keep the mirror map/markDirty behavior unchanged — only ADD the tap.
  </action>
  <verify>
    <automated>pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    ctx is typed EventContext and tapped; on a Reducer-tagged event the reducer timestamp micros re-anchors the clock via gameRef.current?.syncServerClock; the iter() seed loop does not anchor; the row-mirroring behavior is otherwise unchanged. tsc clean; `pnpm test` full suite green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Two-client LAN playtest — night readability, sync, frozen sun, lanterns</name>
  <action>Human-only visual/LAN verification gate (no code edits). The executor pauses here after Tasks 1–2 land and the build is green, and waits for the human to run the six-point two-client LAN playtest below and reply with the resume signal. Not auto-approvable — night readability, LAN time-of-day sync, frozen sun, edge dissolve, lantern fade, and the real-time perf soak cannot be unit-tested.</action>
  <what-built>
    A LAN-synced ~20-min day-weighted day/night cycle: gradient sky single-sourced with fog, night blue-moonlight palette at a ~45% exposure floor, plaza lanterns fading in at dusk / out at dawn, and a `?nodaynight` freeze — all advanced from the server `worldTick` reducer timestamp with a `Date.now()` fallback.
  </what-built>
  <how-to-verify>
    Run `pnpm build`, serve over LAN (laragon `dist/`), and open the game on TWO machines/browsers pointed at the same host.
    1. Time-of-day sync: with the debug time-scale knob (local-only) sped up, confirm both clients show the SAME sky tint / time of day simultaneously (within ~1s). Reload one client mid-night — it must SNAP into night, not play a 30s sunrise.
    2. Night readability (DAYNITE-03/ATMO-03): fight a golem at midnight — its ground telegraph disc, the enemy silhouettes, and gem drops inside the plaza/gameplay radius must stay crisp and high-contrast. Night is a blue palette, not darkness. Also confirm the UNLIT/EMISSIVE materials that ignore the light-carried palette — the safe-zone ring (`createMondstadtWorld.ts:167` `0x9fe86a`) and campfire-flame `MeshBasicMaterial` — do NOT dominate or wash out the night frame through the pixel filter (RESEARCH Open Q2 / Pitfall 5); if they dominate, note it for a follow-up day/night-factor uniform gate.
    3. Edge dissolve (ATMO-01/02): walk to the world edge at day AND at night — distant terrain dissolves into the sky/horizon color, the world edge is hidden, and the horizon seam shows no fog/sky mismatch.
    4. Frozen sun (DAYNITE-01/D-02): watch a full sped-up cycle — colors drift dawn→day→dusk→night but shadows NEVER rotate (sun direction frozen).
    5. Lanterns (DAYNITE-04): watch dusk→night→dawn — plaza lanterns fade IN at dusk and OUT at dawn by intensity, with no frame hitch (no light add/remove).
    6. Perf/bisect: append `?nodaynight` — palette freezes at a neutral day key. Run one REAL-TIME (not time-scaled) full-cycle soak and confirm no banding through the pixel filter and steady FPS (renderer.info.programs.length constant — no recompile hitch from fog/lantern drift).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (banding, desync, night too dark, sun rotating, lantern hitch, edge visible).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server worldTick reducer timestamp → useGameTableBridge → Game.syncServerClock | Untrusted/optional server bigint re-anchors a COSMETIC clock; read-only, no server write, no gameplay gate |
| URL params (?nodaynight, debug time-scale) → createGame | Cosmetic bisect/debug flags via URLSearchParams; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | reducer-timestamp anchor (wrong/absent) | low | accept | Wrong/absent timestamp only shifts local time-of-day (cosmetic); Date.now() fallback bounds the absent case within NTP skew (≪1s). Never gates gameplay |
| T-09-02 | Information Disclosure | client-computed phase as a gameplay gate | low | mitigate | The phase is consumed ONLY by the cosmetic cycle (fog/sky/lights); no spawn/drop/damage/access branch reads it. Time-gated gameplay would require a server reducer, not this clock |
| T-09-04 | Tampering | debug time-scale knob desyncing LAN play | low | mitigate | Time-scale knob gated behind the existing local-only debug_* convention so it cannot ship to / desync prod LAN play |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — three + spacetimedb@2.6.1 (already present) + existing modules only |

No high-severity threats. Client-only, read-only, cosmetic; block-on-high is satisfied truthfully (nothing to block).
</threat_model>

<verification>
- `pnpm exec tsc -b` clean; `pnpm build` succeeds; `pnpm test` full suite green.
- Exactly one `daynight.update(` call in createGame.ts, positioned after `wind.update(`.
- Grep confirms `syncServerClock` is on the Game interface, implemented in the return object, and called only from the bridge tap (never from a render body).
- Human playtest checkpoint approved (the six checks above).
</verification>

<success_criteria>
The world runs a LAN-synced ~20-min day-weighted day/night cycle single-sourced with fog, night stays a combat-readable blue palette, plaza lanterns fade at dusk/dawn, the sun direction never moves, `?nodaynight` freezes it, and a two-client human playtest confirms sync, readability, edge dissolve, and no perf hitch.
</success_criteria>

<output>
Create `.planning/phases/09-atmosphere-day-night/09-05-SUMMARY.md` when done.
</output>
