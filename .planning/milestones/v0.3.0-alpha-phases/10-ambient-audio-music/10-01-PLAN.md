---
phase: 10-ambient-audio-music
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/game/audio/combatState.ts
  - src/game/audio/ambienceMath.ts
  - src/game/audio/__tests__/combatState.test.ts
  - src/game/audio/__tests__/ambienceMath.test.ts
autonomous: true
requirements: [AMBI-02, AMBI-03, AMBI-05, AMBI-06, AMBI-07, MUSIC-02]

must_haves:
  truths:
    - "isInCombat enters immediately on a fresh combat stamp and exits only after the cooldown (D-09 hysteresis)"
    - "nextOneShotDelay returns a value strictly inside [minS,maxS] and varies with the supplied random (never a fixed interval)"
    - "bed-gain target rises monotonically with the gust envelope and is audible at gust=0 (D-05 continuous bed)"
    - "goliath grunt gain is 1 at/inside the near radius, 0 at/beyond the far radius, and interpolates between (AMBI-05)"
    - "day/night predicates split phase01 into a day window (birds) and a night window (crickets/owl) with no overlap (AMBI-07/D-11)"
  artifacts:
    - src/game/audio/combatState.ts
    - src/game/audio/ambienceMath.ts
    - src/game/audio/__tests__/combatState.test.ts
    - src/game/audio/__tests__/ambienceMath.test.ts
  key_links:
    - "combatState.isInCombat consumed by createGame frame loop (10-03) feeding BOTH ambience duck and music crossfade (D-08)"
    - "ambienceMath constants/fns consumed by createAmbience (10-03) — bed gain, one-shot timing, proximity, day/night gates"
---

<objective>
Establish the two zero-import pure-helper modules this phase's audio systems compute against,
test-first, mirroring the `windMath` / `dayNightMath` / `pickupLadder` discipline. `combatState.ts`
holds the combat hysteresis (D-08/D-09); `ambienceMath.ts` holds the bed-gain map, the one-shot
scheduler timing/jitter, the goliath-grunt proximity map, and the day/night `active()` gates
(D-10/D-11, AMBI-02/03/05/07). No WebAudio, no `three`, no React — just numbers in, numbers out.

Purpose: Extract every playtest-tunable constant and every branch worth a regression test OUT of the
imperative WebAudio wrappers so they can be verified in ~5s of vitest and never drift. This is the
Wave-0 foundation the ambience/music wrappers and the combat-state stamp all import.
Output: Two pure modules + two vitest twins, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ambient-audio-music/10-CONTEXT.md
@.planning/phases/10-ambient-audio-music/10-RESEARCH.md
@.planning/phases/10-ambient-audio-music/10-VALIDATION.md
</context>

<artifacts>
This plan produces:
- `combatState.ts` → `COMBAT_EXIT_COOLDOWN_SECONDS` (const), `isInCombat(nowS, lastCombatAtS): boolean`
- `ambienceMath.ts` → constants `BED_BASE_GAIN`, `BED_SWELL_GAIN`, `GRUNT_NEAR_RADIUS`, `GRUNT_FAR_RADIUS`, bird/grunt interval constants (`BIRD_MIN_S`/`BIRD_MAX_S`/`GRUNT_MIN_S`/`GRUNT_MAX_S`), day/night thresholds; pure fns `nextOneShotDelay(minS, maxS, rand)`, `bedGainTarget(gust)`, `jitterFactor(spread, rand)`, `gruntProximityGain(distance)`, `isBirdTime(phase01)`, `isNightCreatureTime(phase01)`
- Two vitest twins under `src/game/audio/__tests__/`
</artifacts>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: combatState pure hysteresis + vitest twin (D-08/D-09, AMBI-06/MUSIC-02)</name>
  <files>src/game/audio/combatState.ts, src/game/audio/__tests__/combatState.test.ts</files>
  <read_first>
    - src/game/systems/dayNightMath.ts (phase01 — the tiny-pure-fn + twin discipline to mirror)
    - src/game/systems/__tests__/windMath.test.ts (test structure: pin BEHAVIOR/bounds, not magic numbers)
    - .planning/phases/10-ambient-audio-music/10-RESEARCH.md §"Combat-State Signal" (the exact target signature)
  </read_first>
  <behavior>
    - Test: a stamp at nowS equal to lastCombatAtS is IN combat (enter immediately — delta 0 &lt; cooldown)
    - Test: nowS - lastCombatAtS just under COMBAT_EXIT_COOLDOWN_SECONDS is still IN combat
    - Test: nowS - lastCombatAtS at/over the cooldown is OUT of combat (exit after cooldown)
    - Test: initial state with lastCombatAtS = -Infinity is OUT of combat at any finite nowS
    - Test: a brief lull shorter than the cooldown between two stamps never flips to OUT (hysteresis absorbs it)
  </behavior>
  <action>
    Write `src/game/audio/combatState.ts` as a zero-import pure module (no `three`, no WebAudio) with a
    header comment stating the zero-import contract, mirroring windMath.ts. Export
    `COMBAT_EXIT_COOLDOWN_SECONDS = 5` (a playtest-tunable seed per Claude's discretion, D-09) and
    `isInCombat(nowS: number, lastCombatAtS: number): boolean` returning
    `nowS - lastCombatAtS &lt; COMBAT_EXIT_COOLDOWN_SECONDS`. This is enter-immediately (a fresh stamp makes
    the delta ~0) and exit-after-cooldown (lulls shorter than the window never flip it) per D-09. Write the
    vitest twin covering the five behaviors above; assert bounds/branch behavior, not the magic 5 (except
    the one boundary test that documents the cooldown contract). Run `npx vitest run src/game/audio/__tests__/combatState.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/game/audio/__tests__/combatState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - combatState.test.ts passes with the 5 behaviors green
    - combatState.ts imports nothing (grep confirms no `import` line)
    - isInCombat is a single subtraction + comparison (zero allocation)
  </acceptance_criteria>
  <done>The pure combat hysteresis exists and is proven enter-immediately / exit-after-cooldown.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: ambienceMath pure scheduler/bed/proximity/day-night helpers + vitest twin (D-10/D-11, AMBI-02/03/05/07)</name>
  <files>src/game/audio/ambienceMath.ts, src/game/audio/__tests__/ambienceMath.test.ts</files>
  <read_first>
    - src/game/systems/windMath.ts (zero-import pure header contract + the gust-envelope shape to sidechain)
    - src/game/audio/audioCore.ts (jitter/clampGain semantics to mirror purely)
    - src/game/systems/dayNightMath.ts §phase01 (0..1 cycle position the day/night gates read)
    - src/game/systems/__tests__/windMath.test.ts (cadence/non-uniformity test idiom for never-metronomic)
  </read_first>
  <behavior>
    - Test: nextOneShotDelay(min,max,rand) is strictly within [min,max] across rand in {0, 0.5, 0.999}, equals min at rand=0, approaches max at rand→1
    - Test: two different rand inputs to nextOneShotDelay give different delays (never a fixed interval — the metronome anti-pattern)
    - Test: bedGainTarget(gust) is monotonic non-decreasing in gust over [0,1], is &gt; 0 at gust=0 (audible base bed), and BED_BASE_GAIN + BED_SWELL_GAIN at gust=1
    - Test: jitterFactor(spread, rand) is within [1-spread, 1+spread] and equals 1 at rand=0.5
    - Test: gruntProximityGain returns 1 at distance ≤ GRUNT_NEAR_RADIUS, 0 at distance ≥ GRUNT_FAR_RADIUS, and a value in (0,1) between
    - Test: isBirdTime(p) and isNightCreatureTime(p) are mutually exclusive across a sampled sweep of phase01 in [0,1) (no overlap; day OR night)
  </behavior>
  <action>
    Write `src/game/audio/ambienceMath.ts` as a zero-import pure module (header comment stating the
    contract, mirroring windMath.ts). Export playtest-seed constants and pure fns:
    (a) one-shot timing — `BIRD_MIN_S=5`, `BIRD_MAX_S=15` (AMBI-03 window), `GRUNT_MIN_S`/`GRUNT_MAX_S` long
    intervals (seed ~25/60), and `nextOneShotDelay(minS, maxS, rand)` = `minS + (maxS-minS)*rand` (never
    metronomic — caller supplies Math.random(); D-10);
    (b) per-shot jitter — `jitterFactor(spread, rand)` = `1 + (rand*2-1)*spread` (pure twin of audioCore.jitter,
    for ±10-20% pitch / pan / volume);
    (c) bed gain — `BED_BASE_GAIN` (audible on its own, the gust rests near 0 between gusts), `BED_SWELL_GAIN`,
    and `bedGainTarget(gust)` = `BED_BASE_GAIN + BED_SWELL_GAIN * gust` (AMBI-02 sidechain map, D-05);
    (d) proximity — `GRUNT_NEAR_RADIUS`/`GRUNT_FAR_RADIUS` and `gruntProximityGain(distance)` clamped to [0,1],
    1 inside near, 0 beyond far, linear between (AMBI-05);
    (e) day/night gates — dawn/dusk `phase01` thresholds and `isBirdTime(phase01)` / `isNightCreatureTime(phase01)`
    that partition the cycle (birds by day, crickets/owl at night, no overlap; AMBI-07/D-11).
    All constants are playtest seeds. Write the vitest twin covering the six behaviors above. Run
    `npx vitest run src/game/audio/__tests__/ambienceMath.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/game/audio/__tests__/ambienceMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ambienceMath.test.ts passes with the 6 behaviors green
    - ambienceMath.ts imports nothing (grep confirms no `import` line)
    - nextOneShotDelay never returns a constant across differing rand; bedGainTarget(0) &gt; 0
  </acceptance_criteria>
  <done>All Phase-10 ambience math is pure, tested, and free of WebAudio/three imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure numeric helpers — no I/O, no untrusted input, no auth, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | (N/A) | pure math modules | low | accept | No attack surface: zero-import pure functions, no I/O, no state. Robustness (finite inputs) is enforced by the consuming wrappers' clamp in later plans. |

Honest assessment: this plan produces only pure number→number functions. No untrusted input, no
persistence, no network. Nothing to mitigate.
</threat_model>

<verification>
- `npx vitest run src/game/audio` is green (both new twins pass)
- `npx vitest run` full suite stays green (no regressions)
- Neither pure module imports `three`, WebAudio, or React
</verification>

<success_criteria>
combatState.ts and ambienceMath.ts exist as tested zero-import pure modules; the isInCombat hysteresis
and the ambience math (bed gain, one-shot timing, proximity, day/night gates) are proven by vitest.
</success_criteria>

<output>
Create `.planning/phases/10-ambient-audio-music/10-01-SUMMARY.md` when done.
</output>
