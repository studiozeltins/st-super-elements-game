---
phase: 02-transcendence-install
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - spacetimedb/src/index.ts
autonomous: true
requirements: [REQ-transcend-install]

must_haves:
  truths:
    - "owned_character has an additive transcendLevel: t.u32().default(0) column (backfills to 0, no wipe)"
    - "transcendCharacter({characterId}) deducts TRANSCEND_SHARD_COST(nextLevel) from player.transcendShards and increments owned_character.transcendLevel by 1 on success"
    - "transcendCharacter rejects with SenderError for below-C6 / at-cap / short-shards and for a non-owned characterId, mutating nothing"
    - "healParty heal multiplier adds healer.transcendLevel * TRANSCEND_HEAL_STEP for the active healer"
  artifacts:
    - spacetimedb/src/index.ts
  key_links:
    - "transcendCharacter calls resolveTranscendInstall (plan 01) — args ONLY characterId; cost/level/owner derived server-side"
    - "reducer uses requirePlayer(ctx) + findOwnedRow(ctx,player,characterId) for ctx.sender ownership authority"
    - "transcendLevel appended AFTER constellation on owned_character (STDB rejects mid-table insert on a live table)"
---

<objective>
Add the persistent `transcendLevel` column, the `transcendCharacter` reducer, and the healer transcend scaling — all in `spacetimedb/src/index.ts`. This is the server-authoritative core: the currency spend and power grant that make "shards buy real power" (invariant 3) true.

Purpose: The reducer is the security boundary for a scarce currency. Client gating (plan 05) is UX only; every rule (C6 gate, level cap, shard cost, ownership) is enforced here.
Output: A published-ready module. Publish + binding regen happen in plan 03 (BLOCKING); this plan only edits source.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcendence-install/02-CONTEXT.md
@.planning/phases/02-transcendence-install/02-PATTERNS.md
@.planning/phases/02-transcendence-install/02-RESEARCH.md
@.planning/transcendence/PROGRESS.md
@CLAUDE.md

@spacetimedb/src/transcendInstall.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append transcendLevel column + transcendCharacter reducer</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts:287-290 (player.transcendShards additive `.default(0)` column — the exact pattern to mirror)
    - spacetimedb/src/index.ts:376-386 (owned_character table; currently ends `constellation: t.u32()` at 386 — APPEND after it)
    - spacetimedb/src/index.ts around 677 (requirePlayer) and 694 (findOwnedRow) — reuse, do NOT reimplement
    - spacetimedb/src/index.ts around 986-994 (setConstellation: owner lookup + SenderError shape) and 1595-1704 (pullBanner: balance check + player-row spread+override update)
    - spacetimedb/src/index.ts:70 (MAX_CONSTELLATION) + 78-84 (MAX_TRANSCEND_LEVEL, TRANSCEND_SHARD_COST — reference, do NOT redeclare or reformat)
    - spacetimedb/src/transcendInstall.ts (resolveTranscendInstall — the decision to call)
    - .planning/phases/02-transcendence-install/02-PATTERNS.md §transcendCharacter reducer (assembled logic)
  </read_first>
  <action>
    Append `transcendLevel: t.u32().default(0)` to the `owned_character` table immediately AFTER the existing `constellation: t.u32()` line (~386). APPEND at the end of the column list — never insert mid-table (STDB rejects a mid-table insert on a populated live table). `.default(0)` is load-bearing: it backfills existing rows so the additive migrate needs no wipe.

    Add a new reducer `transcendCharacter` accepting ONLY `{ characterId: t.string() }` (never accept a caller-supplied level, cost, or owner — server derives all of these). Body, mirroring setConstellation + pullBanner: (1) `const currentPlayer = requirePlayer(ctx)`; (2) `const owned = findOwnedRow(ctx, currentPlayer, characterId)`; if `!owned` throw `new SenderError('Character not owned')`; (3) call `resolveTranscendInstall(owned.constellation, MAX_CONSTELLATION, owned.transcendLevel, MAX_TRANSCEND_LEVEL, currentPlayer.transcendShards, TRANSCEND_SHARD_COST)`; (4) on `!result.ok` map reason → SenderError: below_c6 → 'Requires C6', at_cap → 'At transcend cap', insufficient_shards → 'Not enough shards' (throw before any mutation); (5) on ok, atomically update both rows — `ctx.db.player.identity.update({ ...currentPlayer, transcendShards: currentPlayer.transcendShards - result.shardCost })` and `ctx.db.ownedCharacter.id.update({ ...owned, transcendLevel: result.nextLevel })`. Import `resolveTranscendInstall` from `./transcendInstall`. `SenderError` is already imported at the top of index.ts — reuse it. Do NOT reformat the constant blocks at 78-84 or any single-line CHARACTER_STATS entry (serverSync.test.ts parses source as a string — Pitfall 5).
  </action>
  <verify>
    <automated>pnpm exec tsc -p spacetimedb --noEmit 2>/dev/null || pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `spacetimedb/src/index.ts` owned_character table declares `transcendLevel: t.u32().default(0)` appended after `constellation: t.u32()`.
    - A `transcendCharacter` reducer exists whose declared args are exactly `{ characterId: t.string() }` (no level/cost/owner arg).
    - Every reject path throws `SenderError` and performs no `.update(` call before the throw; the shard deduction appears only inside the `result.ok` branch.
    - The reducer imports `resolveTranscendInstall` from `./transcendInstall` and passes `TRANSCEND_SHARD_COST` + `MAX_TRANSCEND_LEVEL` + `MAX_CONSTELLATION` (server constants), never args.
    - TypeScript typechecks (no errors) against the server module.
  </acceptance_criteria>
  <done>owned_character has the additive transcendLevel column and transcendCharacter enforces C6/cap/cost/ownership server-side, deducting shards + raising level only on success.</done>
</task>

<task type="auto">
  <name>Task 2: Extend healParty heal multiplier by transcend level</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts:1496-1529 (healParty reducer; healer resolved as the active owned row ~1504, healMultiplier ~1508, applied ~1513)
    - spacetimedb/src/index.ts:80 (TRANSCEND_HEAL_STEP=0.08 — reference, do NOT redeclare)
    - .planning/phases/02-transcendence-install/02-PATTERNS.md §healParty heal scaling
  </read_first>
  <action>
    In `healParty`, extend the existing `healMultiplier` (currently `1 + healActivated * HEAL_CONSTELLATION_STEP`) by adding the healer's transcend term: `+ (healer ? healer.transcendLevel : 0) * TRANSCEND_HEAL_STEP`. `healer` is already the active owned row (resolved ~1504), so `healer.transcendLevel` is valid after the plan-01/task-1 column exists. This is server-only — the client never computes the heal amount. Do not touch the client `sendHealParty` path. `TRANSCEND_HEAL_STEP` is already declared at index.ts:80 — reference it.
  </action>
  <verify>
    <automated>pnpm exec tsc -p spacetimedb --noEmit 2>/dev/null || pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The `healMultiplier` expression in `healParty` includes a `transcendLevel * TRANSCEND_HEAL_STEP` term sourced from the active healer row.
    - No client file is modified for heal scaling (heal amount stays server-computed).
    - Server module typechecks with no errors.
  </acceptance_criteria>
  <done>Healers with transcendLevel>0 produce a proportionally larger server-computed heal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → transcendCharacter reducer | Untrusted `characterId` crosses; the client may lie about ownership, level, cost, or balance. The reducer is the ONLY enforcement point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing / Elevation of Privilege | transcendCharacter ownership | high | mitigate | Ownership resolved via `requirePlayer(ctx)` (canonical identity from `ctx.sender`) + `findOwnedRow` (owner-scoped); a non-owned `characterId` → `SenderError('Character not owned')`. Caller-supplied identity is never trusted (CLAUDE.md rule 5). |
| T-02-02 | Tampering | reducer args | high | mitigate | Args are exactly `{ characterId }`; shard cost + next level + cap derived server-side from `TRANSCEND_SHARD_COST` / `MAX_TRANSCEND_LEVEL` / the stored row only. No client-supplied level/cost/delta accepted. |
| T-02-03 | Tampering | shard deduction | high | mitigate | `resolveTranscendInstall.insufficient_shards` gate precedes any subtraction; deduct of exactly `result.shardCost` occurs only inside the ok branch — prevents `u32` underflow to a huge balance (Pitfall 4). |
| T-02-04 | Tampering | C6 gate / level cap | high | mitigate | `below_c6` and `at_cap` branches reject before mutation; boundaries unit-tested in plan 01 (C5 → below_c6, MAX → at_cap). |
| T-02-05 | Repudiation / consistency | reducer determinism | medium | mitigate | Decision delegated to the pure `resolveTranscendInstall` (no random/time/ctx); reducer stays deterministic + transactional — rejects mutate nothing. |
| T-02-SC | Tampering | package installs | low | accept | Zero `pnpm add` in this phase (RESEARCH Package Legitimacy Audit: not applicable); no supply-chain surface introduced. |
</threat_model>

<verification>
- Server module typechecks; `resolveTranscendInstall` is the sole decision authority in the reducer.
- No reject path mutates a row; deduct only on ok.
- serverSync.test.ts stays green (constant blocks + CHARACTER_STATS untouched): `pnpm exec vitest run src/game/data/__tests__/serverSync.test.ts`.
</verification>

<success_criteria>
The additive column + reducer + heal term exist and typecheck; all currency rules enforced server-side; serverSync.test.ts still green.
</success_criteria>

<output>
Create `.planning/phases/02-transcendence-install/02-02-SUMMARY.md` when done.
</output>
