---
phase: 02-transcendence-install
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/transcendInstall.ts
  - src/game/data/__tests__/transcendInstall.test.ts
  - src/game/combat/transcendScaling.ts
  - src/game/combat/__tests__/transcendScaling.test.ts
autonomous: true
requirements: [REQ-transcend-install]

must_haves:
  truths:
    - "resolveTranscendInstall returns ok with nextLevel + shardCost=cost(nextLevel) when C6, under cap, and shards>=cost"
    - "resolveTranscendInstall rejects with reason below_c6 / at_cap / insufficient_shards in that priority order"
    - "transcendDamageMultiplier(6,0)=1.48, (6,1)=1.53, (6,10)=1.98, (0,0)=1"
  artifacts:
    - spacetimedb/src/transcendInstall.ts
    - src/game/combat/transcendScaling.ts
    - src/game/data/__tests__/transcendInstall.test.ts
    - src/game/combat/__tests__/transcendScaling.test.ts
  key_links:
    - "transcendInstall.test.ts cross-imports resolveTranscendInstall from ../../../../spacetimedb/src/transcendInstall (server module → client vitest boundary)"
    - "transcendScaling.ts imports CONSTELLATION_DAMAGE_STEP + TRANSCEND_DAMAGE_STEP from data modules (no three.js/DB)"
---

<objective>
Create the two dependency-free pure helpers this phase pivots on, test-first (Wave 0 Nyquist gaps from 02-VALIDATION.md): the server install decision `resolveTranscendInstall` and the client damage-math `transcendDamageMultiplier`. Both are structural clones of shipped Phase 1 helpers and unit-test without a DB or game loop.

Purpose: Reducers cannot run under vitest, so the reject/deduct branching decision MUST be extracted to satisfy the DoD "server logic unit"; damage math is extracted so it tests without three.js. Building + testing these first de-risks every downstream wiring plan.
Output: Two pure modules + two green unit specs. No schema change, no publish, no client wiring.

## Artifacts this phase produces (whole-phase manifest)

New symbols delivered across Phase 2 (this plan delivers the two pure helpers + their specs; later plans wire them):
- `resolveTranscendInstall` — pure server install decision (this plan, `spacetimedb/src/transcendInstall.ts`)
- `transcendDamageMultiplier` — pure client damage math (this plan, `src/game/combat/transcendScaling.ts`)
- `owned_character.transcendLevel` — additive `t.u32().default(0)` column (plan 02, `spacetimedb/src/index.ts`)
- `transcendCharacter({ characterId })` — new reducer (plan 02, `spacetimedb/src/index.ts`)
- `healParty` transcend heal term (plan 02, `spacetimedb/src/index.ts`)
- regenerated `src/module_bindings/` (plan 03 — publish + generate)
- `setActiveTranscend` setter on the Game interface (plan 04, `src/game/createGame.ts`)
- `transcendById` map + feed effect + `transcendCharacter` reducer callback + prop threading (plan 04, `src/App.tsx`)
- `C6 · T{n}` status badge + gated Install control (plan 05, `src/ui/CharacterScreen.tsx`)
- new specs: `transcendInstall.test.ts`, `transcendScaling.test.ts` (this plan)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcendence-install/02-CONTEXT.md
@.planning/phases/02-transcendence-install/02-PATTERNS.md
@.planning/transcendence/PROGRESS.md

@spacetimedb/src/gachaOverflow.ts
@src/game/data/__tests__/gachaOverflow.test.ts
@src/game/combat/comboSystem.ts
@src/game/data/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure server install decision resolveTranscendInstall + cross-boundary unit</name>
  <files>spacetimedb/src/transcendInstall.ts, src/game/data/__tests__/transcendInstall.test.ts</files>
  <read_first>
    - spacetimedb/src/gachaOverflow.ts (exact copy-shape: doc header rationalizing determinism/server-authority, exported result type, one pure fn — no ctx/import/random/time)
    - src/game/data/__tests__/gachaOverflow.test.ts (cross-boundary import + it.each pattern; import path `../../../../spacetimedb/src/...`)
    - src/game/data/constants.ts:8-15 (MAX_TRANSCEND_LEVEL, TRANSCEND_SHARD_COST — reference, do NOT redeclare)
    - .planning/phases/02-transcendence-install/02-PATTERNS.md §transcendInstall.ts (locked signature) + §Sampling edge cases
  </read_first>
  <behavior>
    - Test: (constellation=6, currentTranscend=0, shards>=cost) → { ok:true, nextLevel:1, shardCost:1 }
    - Test: cap boundary — currentTranscend=MAX-1 → ok (nextLevel=MAX); currentTranscend=MAX → { ok:false, reason:'at_cap' }
    - Test: exact-shard boundary — shards===cost(nextLevel) → ok; shards===cost(nextLevel)-1 → { ok:false, reason:'insufficient_shards' }
    - Test: C5 rejection — constellation=5 → { ok:false, reason:'below_c6' } regardless of shards (priority: below_c6 evaluated before at_cap before insufficient_shards)
    - Test: cost curve parametrized n in {1,2,5,10} → shardCost===n
  </behavior>
  <action>
    Create `spacetimedb/src/transcendInstall.ts` mirroring `gachaOverflow.ts` verbatim in structure. Export union type `TranscendInstall = { ok:true; nextLevel:number; shardCost:number } | { ok:false; reason:'below_c6'|'at_cap'|'insufficient_shards' }` and pure function `resolveTranscendInstall(constellation:number, maxConstellation:number, currentTranscend:number, maxTranscend:number, shards:number, cost:(n:number)=>number): TranscendInstall`. Branch ORDER is load-bearing and must be exactly: (1) `constellation < maxConstellation` → below_c6; (2) `currentTranscend >= maxTranscend` → at_cap; (3) compute `nextLevel = currentTranscend + 1`, `shardCost = cost(nextLevel)`, then `shards < shardCost` → insufficient_shards; else ok. The shard check is LAST so it gates the deduct (u32-underflow mitigation — see threat_model). No `ctx`, no `import`, no random, no time. Add a doc header naming the determinism + server-authority rationale (mirror gachaOverflow.ts header). Create the spec at `src/game/data/__tests__/transcendInstall.test.ts` cross-importing `resolveTranscendInstall` from `../../../../spacetimedb/src/transcendInstall` and `MAX_TRANSCEND_LEVEL`, `TRANSCEND_SHARD_COST` from `../../data/constants` (mirror the gachaOverflow.test.ts import shape). Pass `MAX_TRANSCEND_LEVEL` as maxTranscend and `TRANSCEND_SHARD_COST` as the cost fn; use constellation cap 6.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/data/__tests__/transcendInstall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/game/data/__tests__/transcendInstall.test.ts` passes with all five case-groups green (ok, cap boundary, exact-shard boundary, C5 below_c6, cost curve n∈{1,2,5,10}).
    - `spacetimedb/src/transcendInstall.ts` contains zero `import` statements and no reference to `ctx`, `Math.random`, `Date`, or `ctx.timestamp`.
    - Reject-priority asserted: constellation=5 with abundant shards returns reason `below_c6` (not insufficient_shards).
  </acceptance_criteria>
  <done>resolveTranscendInstall exists as a pure module and its cross-boundary spec is green covering all four branches + cost curve + both boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure client damage math transcendDamageMultiplier + unit</name>
  <files>src/game/combat/transcendScaling.ts, src/game/combat/__tests__/transcendScaling.test.ts</files>
  <read_first>
    - src/game/combat/comboSystem.ts (copy-shape: small pure exported fns with a JSDoc line, imports only sibling data constants)
    - src/game/combat/__tests__/comboSystem.test.ts (assert exact values at boundaries)
    - src/game/data/constants.ts:9 (TRANSCEND_DAMAGE_STEP=0.05) + src/game/data/constellations.ts:6 (CONSTELLATION_DAMAGE_STEP=0.08) — import, do NOT redeclare
  </read_first>
  <behavior>
    - Test: transcendDamageMultiplier(0,0) === 1
    - Test: transcendDamageMultiplier(6,0) ≈ 1.48 (constellation only, C6)
    - Test: transcendDamageMultiplier(6,1) ≈ 1.53 (one transcend level = +0.05)
    - Test: transcendDamageMultiplier(6,10) ≈ 1.98 (C6 + T10 cap)
    - Use toBeCloseTo for the float sums
  </behavior>
  <action>
    Create `src/game/combat/transcendScaling.ts` mirroring `comboSystem.ts` style: import `CONSTELLATION_DAMAGE_STEP` from `../data/constellations` and `TRANSCEND_DAMAGE_STEP` from `../data/constants`; export `transcendDamageMultiplier(constellation:number, transcend:number): number` returning `1 + constellation*CONSTELLATION_DAMAGE_STEP + transcend*TRANSCEND_DAMAGE_STEP` with a one-line JSDoc. This SUBSUMES the local `constellationDamageMultiplier` in createGame.ts (plan 04 will swap the call site). Create `src/game/combat/__tests__/transcendScaling.test.ts` importing the fn and asserting the four boundary values with `toBeCloseTo`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/combat/__tests__/transcendScaling.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/game/combat/__tests__/transcendScaling.test.ts` passes; asserts (0,0)=1, (6,0)≈1.48, (6,1)≈1.53, (6,10)≈1.98.
    - `transcendScaling.ts` imports only from `../data/*` (no three.js, no DB, no createGame import).
    - The value delta between (6,n) and (6,n+1) equals TRANSCEND_DAMAGE_STEP (0.05) for the tested levels.
  </acceptance_criteria>
  <done>transcendDamageMultiplier exists as a dependency-free module with a green boundary spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure helper ↔ reducer/game-loop | These fns encode the security-critical decision logic later consumed server-side (install) and client-side (damage). Correct branch ORDER here is what prevents the u32 underflow downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | resolveTranscendInstall branch order | high | mitigate | insufficient_shards is the LAST gate and computes shardCost only after the C6/cap gates pass; the ok branch is the only path that reports a deduct — unit-tested at the exact-shard boundary so the reducer (plan 02) can never subtract below cost. |
| T-02-05 | Repudiation / determinism | transcendInstall.ts / transcendScaling.ts | medium | mitigate | Modules are ctx/import/random/time-free (mirrors gachaOverflow.ts header), preserving the calling reducer's determinism (CLAUDE.md rule 2). |
</threat_model>

<verification>
- `pnpm exec vitest run src/game/data/__tests__/transcendInstall.test.ts src/game/combat/__tests__/transcendScaling.test.ts` green.
- Neither new module imports three.js, spacetimedb runtime, or the game loop.
</verification>

<success_criteria>
Both pure helpers exist with the locked signatures and green boundary specs; no schema/publish/client-wiring touched.
</success_criteria>

<output>
Create `.planning/phases/02-transcendence-install/02-01-SUMMARY.md` when done.
</output>
