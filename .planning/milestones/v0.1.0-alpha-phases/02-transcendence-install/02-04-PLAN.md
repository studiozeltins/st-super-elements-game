---
phase: 02-transcendence-install
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-03"]
files_modified:
  - src/game/createGame.ts
  - src/App.tsx
autonomous: true
requirements: [REQ-transcend-install]

must_haves:
  truths:
    - "createGame's damage multiplier is transcendDamageMultiplier(activeConstellation, activeTranscend), replacing the local constellationDamageMultiplier at both consumption sites"
    - "The Game interface exposes setActiveTranscend(transcend:number):void and the loop holds a mutable activeTranscend fed by App.tsx"
    - "App.tsx builds transcendById from subscribed owned_character rows and pushes the active character's level via setActiveTranscend in the existing scaling effect"
    - "App.tsx exposes a transcendCharacter(characterId) reducer callback and threads transcendById + onTranscend into CharacterScreen"
  artifacts:
    - src/game/createGame.ts
    - src/App.tsx
  key_links:
    - "createGame imports transcendDamageMultiplier from ./combat/transcendScaling (plan 01)"
    - "App.tsx calls conn.reducers.transcendCharacter (plan 03 bindings) and reads row.transcendLevel (plan 03 column)"
    - "setActiveTranscend feed lives in the SAME effect that already calls setActiveConstellation (dep array already has ownedCharacterRows)"
---

<objective>
Wire the transcend level into the client damage path and the React→game bridge: extract the damage multiplier to the pure `transcendDamageMultiplier`, add an `activeTranscend` loop state + `setActiveTranscend` setter, and in `App.tsx` build the per-character transcend map, feed the active level into the loop, expose the reducer callback, and thread props to the UI (plan 05). This makes transcended characters deal observably more damage (SC3).

Purpose: Damage is client-authoritative; the loop must know the active character's transcendLevel to scale hits.
Output: Bigger damage numbers for transcended characters + the prop/callback plumbing the Install UI needs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transcendence-install/02-PATTERNS.md
@.planning/phases/02-transcendence-install/02-RESEARCH.md

@src/game/combat/transcendScaling.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract multiplier + activeTranscend setter in createGame.ts</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts:391-393 (local CONSTELLATION_DAMAGE_STEP + activeConstellation + constellationDamageMultiplier — the block being replaced)
    - src/game/createGame.ts:417 and :485 (the two consumption sites — regular attack + skill)
    - src/game/createGame.ts:807-809 (setActiveConstellation setter — mirror for setActiveTranscend) + interface ~98-99
    - src/game/combat/transcendScaling.ts (transcendDamageMultiplier — plan 01)
    - .planning/phases/02-transcendence-install/02-PATTERNS.md §createGame.ts
  </read_first>
  <action>
    Import `transcendDamageMultiplier` from `./combat/transcendScaling`. Replace the local `constellationDamageMultiplier` block (~391-393): keep `let activeConstellation = 0;`, add `let activeTranscend = 0;`, and remove the local `constellationDamageMultiplier` arrow fn (the pure module subsumes it). At both consumption sites (~417 regular attack, ~485 skill) call `transcendDamageMultiplier(activeConstellation, activeTranscend)` in place of `constellationDamageMultiplier()`. Add to the Game interface (near :98-99, mirroring the setActiveConstellation JSDoc+signature) `setActiveTranscend(transcend: number): void;` and implement the setter next to `setActiveConstellation` (~807): `setActiveTranscend(transcend) { activeTranscend = transcend; }`. Do not change the CONSTELLATION_DAMAGE_STEP import source used elsewhere — transcendScaling imports the step constants itself.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/combat/__tests__/transcendScaling.test.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `constellationDamageMultiplier(` no longer appears as a call in createGame.ts; both attack/skill sites call `transcendDamageMultiplier(activeConstellation, activeTranscend)`.
    - The Game interface declares `setActiveTranscend(transcend: number): void` and the returned object implements it, mutating `activeTranscend`.
    - `pnpm build` is green (typecheck + bundle).
  </acceptance_criteria>
  <done>The loop scales damage by constellation + transcend via the pure module and exposes a setter to feed the active level.</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx bridge — transcendById map, feed effect, reducer callback, prop threading</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:93-101 (useTable subscriptions incl. ownedCharacterRows) + :188-202 (constellationById build — mirror for transcendById)
    - src/App.tsx:366-371 (scaling effect calling setActiveConstellation — ADD setActiveTranscend here; dep array already has ownedCharacterRows)
    - src/App.tsx:242-247 (setConstellation useCallback — mirror for transcendCharacter) + :474-491 (CharacterScreen prop threading)
    - .planning/phases/02-transcendence-install/02-PATTERNS.md §App.tsx bridge
  </read_first>
  <action>
    Build `transcendById: Record<string, number>` from the already-subscribed `ownedCharacterRows` exactly like `constellationById` (:188-192), filtering by `row.owner.toHexString() === myIdentityHex` and reading `row.transcendLevel` (available after plan 03 bindings). In the existing scaling effect (:366-371) add `gameRef.current?.setActiveTranscend(transcendById[active] ?? 0);` right after the `setActiveConstellation` call — the dep array already includes `ownedCharacterRows`, no new dependency needed. Add a `transcendCharacter` useCallback mirroring `setConstellation` (:242-247): `(characterId: string) => { connection?.reducers.transcendCharacter({ characterId }); }` with `[connection]` deps. Thread two new props into `<CharacterScreen>` (:474-491): `transcendById={transcendById}` and `onTranscend={transcendCharacter}`. Do not add a new subscription — reuse ownedCharacterRows.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `transcendById` is built from `ownedCharacterRows` (owner-filtered, reads `transcendLevel`).
    - The scaling effect calls `setActiveTranscend(transcendById[active] ?? 0)` alongside `setActiveConstellation`.
    - A `transcendCharacter` callback calls `connection?.reducers.transcendCharacter({ characterId })` and is passed to CharacterScreen as `onTranscend`, with `transcendById` also passed.
    - `pnpm build` green (this confirms bindings from plan 03 are present — a missing reducer/column would fail typecheck).
  </acceptance_criteria>
  <done>App.tsx feeds the active transcend level into the loop and hands CharacterScreen the data + reducer callback it needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client game loop → damage sent to server | Damage is client-authoritative; the loop rolls the number. The transcend multiplier is a display/parity concern, not the currency boundary (that stays in the plan-02 reducer). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | client damage multiplier | low | accept | Client damage authority is a pre-existing game-wide design (attacks send a rolled number); this phase only extends the multiplier consistently. No new trust boundary is opened — the scarce-currency spend is guarded server-side in plan 02. Balance bounds are a Phase 7 concern (out of scope). |
| T-02-09 | Spoofing | transcendById owner filter | low | mitigate | `transcendById` filters rows by `row.owner.toHexString() === myIdentityHex` (mirrors constellationById), so another player's transcendLevel never feeds the local loop. |
</threat_model>

<verification>
- `pnpm build` green (proves plan-03 bindings resolve).
- Damage multiplier call sites use the pure module; no local constellation-only multiplier remains.
</verification>

<success_criteria>
Transcended active characters scale damage in the loop; App.tsx exposes transcendById + onTranscend to the UI; build green.
</success_criteria>

<output>
Create `.planning/phases/02-transcendence-install/02-04-SUMMARY.md` when done.
</output>
