---
phase: 00-lock-transcendence-constants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/index.ts
  - src/game/data/constants.ts
  - src/game/data/__tests__/serverSync.test.ts
  - src/game/data/__tests__/transcendence.test.ts
  - .planning/transcendence/PROGRESS.md
autonomous: true
requirements:
  - REQ-lock-constants
must_haves:
  truths:
    - "All six SHARD_*/TRANSCEND_* constants exist server-side in spacetimedb/src/index.ts with their locked values as plain single-line const declarations."
    - "The same six constants are mirrored in the client (src/game/data/constants.ts) with identical values, each marked // server."
    - "A pure helper TRANSCEND_SHARD_COST(n) === n exists on both server and client (function, not a numeric const)."
    - "serverSync.test.ts asserts every new constant's server literal equals its client mirror value."
    - "A unit test confirms constants are present and in valid ranges and that TRANSCEND_SHARD_COST(n) === n."
    - "pnpm test and pnpm build are green with no schema/reducer change and no publish."
  artifacts:
    - spacetimedb/src/index.ts
    - src/game/data/constants.ts
    - src/game/data/__tests__/serverSync.test.ts
    - src/game/data/__tests__/transcendence.test.ts
  key_links:
    - "extractServerConstant(name) regex in serverSync.test.ts extracts each numeric constant from the server source — server declarations MUST be plain `const NAME = value;` single-line."
    - "Client constants.ts is the import surface the sync test and later phases read from."
---

<objective>
Lock every transcendence tunable as a constant on BOTH server and client, name-locked and mirrored identically, plus one pure cost helper — with NO logic wiring, NO schema change, NO reducer reference, and NO publish. This is the single source of truth phases 1–7 will reference.

Purpose: Freeze the numbers (MAX_TRANSCEND_LEVEL, steps, shard costs/payouts) in one place so every downstream phase references one authority and the client/server mirror stays guarded by serverSync.test.ts (INV-5).
Output: Six new constants + one helper on each side, sync-test coverage, a range/helper unit test, PROGRESS.md updated, one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-lock-transcendence-constants/00-CONTEXT.md
@.planning/transcendence/PROGRESS.md
@spacetimedb/src/index.ts
@src/game/data/constants.ts
@src/game/data/__tests__/serverSync.test.ts
</context>

## Artifacts this phase produces

New symbols created by this phase (constants + helper), all mirrored server + client:

| Symbol | Kind | Value | Location (server / client) |
|--------|------|-------|----------------------------|
| MAX_TRANSCEND_LEVEL | const (number) | 10 | index.ts / constants.ts |
| TRANSCEND_DAMAGE_STEP | const (number) | 0.05 | index.ts / constants.ts |
| TRANSCEND_HEAL_STEP | const (number) | 0.08 | index.ts / constants.ts |
| SHARD_PER_OVERFLOW_DUPE | const (number) | 1 | index.ts / constants.ts |
| SHARD_DEATH_LOSS | const (number) | 1 | index.ts / constants.ts |
| RAID_SHARD_PAYOUT | const (number) | 6 | index.ts / constants.ts |
| TRANSCEND_SHARD_COST | pure fn `(n) => n` | — | index.ts / constants.ts |

New file paths: `src/game/data/__tests__/transcendence.test.ts`.
No new schema columns, no reducers, no bindings regeneration, no publish.

<tasks>

<task type="auto">
  <name>Task 1: Add transcendence constants + cost helper to the server module</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts lines 67–75 — the existing economy/constellation constant block (STARTER_CHARACTER_ID, PARTY_SIZE, MAX_CONSTELLATION, HEAL_CONSTELLATION_STEP, STARTING_GEMS, GACHA_PULL_COST, DUPLICATE_REFUND, KILL_REWARD_GEMS, MAX_KILL_REWARD_TIER). New constants go directly after this contiguous block, before the "---- Wish banners ----" section at line 77.
    - src/game/data/__tests__/serverSync.test.ts lines 59–63 — extractServerConstant regex `const ${name} = (\d+(?:\.\d+)?);`. Each numeric constant MUST be a plain single-line `const NAME = value;` for this regex to match. A trailing `// comment` after the semicolon is fine.
  </read_first>
  <action>
    In spacetimedb/src/index.ts, immediately after the MAX_KILL_REWARD_TIER line (currently line 75) and before the Wish banners comment block, add a contiguous, commented group of six plain single-line constants with their locked values (per REQ-lock-constants): MAX_TRANSCEND_LEVEL = 10, TRANSCEND_DAMAGE_STEP = 0.05, TRANSCEND_HEAL_STEP = 0.08, SHARD_PER_OVERFLOW_DUPE = 1, SHARD_DEATH_LOSS = 1, RAID_SHARD_PAYOUT = 6. Prefix the group with a short header comment such as "Transcendence tunables (locked Phase 0; wired in phases 1-7)". Then add the pure cost helper as an arrow function `const TRANSCEND_SHARD_COST = (n: number): number => n;` (a function, NOT a numeric const — it must NOT match the extractServerConstant numeric regex). Do NOT reference any of these symbols in any reducer, lifecycle hook, table, or elsewhere — they are declared and left unused this phase. Use the exact names verbatim (SHARD_*/TRANSCEND_* prefixes); never "primogem", "fragment", or "crystal". Because they are intentionally unused, if the module's tsconfig or lint flags unused declarations, keep each on a plain single line (the existing constant block is not flagged, so match that exact style).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('spacetimedb/src/index.ts','utf8'); for (const [n,v] of [['MAX_TRANSCEND_LEVEL',10],['TRANSCEND_DAMAGE_STEP',0.05],['TRANSCEND_HEAL_STEP',0.08],['SHARD_PER_OVERFLOW_DUPE',1],['SHARD_DEATH_LOSS',1],['RAID_SHARD_PAYOUT',6]]){const m=new RegExp('const '+n+' = (\\\\d+(?:\\\\.\\\\d+)?);').exec(s); if(!m||Number(m[1])!==v){throw new Error('bad '+n)}} if(!/const TRANSCEND_SHARD_COST = \\(n: number\\): number => n;/.test(s)){throw new Error('missing helper')} console.log('server constants OK')"</automated>
  </verify>
  <done>All six numeric constants are present as plain single-line `const NAME = value;` with locked values and extractable by the sync regex; TRANSCEND_SHARD_COST is a pure function; no reducer references any of them.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the constants + cost helper in the client</name>
  <files>src/game/data/constants.ts</files>
  <read_first>
    - src/game/data/constants.ts lines 1–6 — existing `// server` mirror pattern (SAFE_ZONE_RADIUS, WORLD_BOUND, MAX_HEALTH, GACHA_PULL_COST each `export const NAME = value; // server`). Add the transcendence mirror in the same file using the same pattern.
  </read_first>
  <action>
    In src/game/data/constants.ts, add the client mirror of all six constants using the existing pattern: `export const NAME = value; // server` with the identical locked values from Task 1 (MAX_TRANSCEND_LEVEL = 10, TRANSCEND_DAMAGE_STEP = 0.05, TRANSCEND_HEAL_STEP = 0.08, SHARD_PER_OVERFLOW_DUPE = 1, SHARD_DEATH_LOSS = 1, RAID_SHARD_PAYOUT = 6). Group them under a short header comment ("Transcendence tunables — mirror spacetimedb/src/index.ts"). Then add the pure helper `export const TRANSCEND_SHARD_COST = (n: number): number => n;` (mirrors the server helper; installing level n costs n shards). Keep values numerically identical to the server so serverSync passes. Do NOT create a schema binding, reducer call, or UI reference — constants only. Exercised discretion: constants live inline in constants.ts (matching the existing SAFE_ZONE_RADIUS mirror pattern) rather than a separate transcendence.ts, so the existing sync test's `../constants` import surface needs no new module.
  </action>
  <verify>
    <automated>pnpm exec tsc -b --noEmit 2>/dev/null; node -e "const s=require('fs').readFileSync('src/game/data/constants.ts','utf8'); for (const [n,v] of [['MAX_TRANSCEND_LEVEL','10'],['TRANSCEND_DAMAGE_STEP','0.05'],['TRANSCEND_HEAL_STEP','0.08'],['SHARD_PER_OVERFLOW_DUPE','1'],['SHARD_DEATH_LOSS','1'],['RAID_SHARD_PAYOUT','6']]){if(!new RegExp('export const '+n+' = '+v.replace('.','\\\\.')+';').test(s)){throw new Error('missing '+n)}} if(!/export const TRANSCEND_SHARD_COST = /.test(s)){throw new Error('missing helper')} console.log('client mirror OK')"</automated>
  </verify>
  <done>Six client constants exported with `// server` marker and identical values, plus the exported TRANSCEND_SHARD_COST helper; all importable from src/game/data/constants.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Extend serverSync coverage, add range/helper unit test, validate</name>
  <files>src/game/data/__tests__/serverSync.test.ts, src/game/data/__tests__/transcendence.test.ts</files>
  <read_first>
    - src/game/data/__tests__/serverSync.test.ts lines 14–19 (client-constant imports from ../constants) and lines 119–129 (the "server constants stay in sync" it.each block using extractServerConstant). Add the six new constants to the import list and one `['NAME', NAME]` row each in that it.each block.
  </read_first>
  <action>
    Part A — serverSync.test.ts: extend the import from '../constants' (lines 14–19) to also import MAX_TRANSCEND_LEVEL, TRANSCEND_DAMAGE_STEP, TRANSCEND_HEAL_STEP, SHARD_PER_OVERFLOW_DUPE, SHARD_DEATH_LOSS, RAID_SHARD_PAYOUT. In the existing `describe('server constants stay in sync with client constants')` it.each array (lines 120–125), add one row per new constant: `['MAX_TRANSCEND_LEVEL', MAX_TRANSCEND_LEVEL]`, `['TRANSCEND_DAMAGE_STEP', TRANSCEND_DAMAGE_STEP]`, `['TRANSCEND_HEAL_STEP', TRANSCEND_HEAL_STEP]`, `['SHARD_PER_OVERFLOW_DUPE', SHARD_PER_OVERFLOW_DUPE]`, `['SHARD_DEATH_LOSS', SHARD_DEATH_LOSS]`, `['RAID_SHARD_PAYOUT', RAID_SHARD_PAYOUT]`. The existing `expect(extractServerConstant(name)).toBe(clientValue)` body already handles decimals like 0.05.
    Part B — new file src/game/data/__tests__/transcendence.test.ts: import the six constants + TRANSCEND_SHARD_COST from '../constants'. Add a describe with: (1) an `it` asserting every constant is a defined number in valid range — MAX_TRANSCEND_LEVEL > 0, TRANSCEND_DAMAGE_STEP > 0, TRANSCEND_HEAL_STEP > 0, SHARD_PER_OVERFLOW_DUPE > 0, SHARD_DEATH_LOSS > 0, RAID_SHARD_PAYOUT > 0; (2) an `it.each` over sample n (e.g. 0,1,2,5,10) asserting `TRANSCEND_SHARD_COST(n) === n`. Follow the existing vitest style (describe/it/expect, import from 'vitest').
    Part C — validation: run the full suite and build; there is NO publish (no schema/logic change).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/game/data/__tests__/serverSync.test.ts src/game/data/__tests__/transcendence.test.ts && pnpm test && pnpm build</automated>
  </verify>
  <done>serverSync.test.ts has a passing sync row for each of the six constants; transcendence.test.ts passes range + TRANSCEND_SHARD_COST(n)===n assertions; `pnpm test` and `pnpm build` are green; no publish performed.</done>
</task>

<task type="auto">
  <name>Task 4: Update PROGRESS.md tracker for Phase 0</name>
  <files>.planning/transcendence/PROGRESS.md</files>
  <read_first>
    - .planning/transcendence/PROGRESS.md lines 9–19 (Status table — the Phase 0 row) and lines 34–42 (Checklist — the "0 — lock transcendence constants" line).
  </read_first>
  <action>
    In .planning/transcendence/PROGRESS.md, mark Phase 0 complete: change the Phase 0 status-table row (line 12) from `⬜ TODO` to `✅ DONE` and fill the Commit column with the atomic commit's short hash once known (the executor should update the hash after committing, or leave a placeholder the commit step fills). In the Checklist (line 36), tick `- [ ] **0** — lock transcendence constants` to `- [x]`. Do not touch other phase rows.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/transcendence/PROGRESS.md','utf8'); if(!/- \\[x\\] \\*\\*0\\*\\*/.test(s)){throw new Error('checklist not ticked')} if(!/\\| 0 \\|[^\\n]*✅ DONE/.test(s)){throw new Error('status row not updated')} console.log('PROGRESS updated')"</automated>
  </verify>
  <done>Phase 0 shows ✅ DONE in the status table and [x] in the checklist; no other rows altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase adds NO reducers, NO schema, NO client input handling, and performs NO publish. No untrusted input crosses any boundary in this phase. The only relevant boundary is the client/server value mirror (a correctness/tampering surface, not a runtime attack surface).

| Boundary | Description |
|----------|-------------|
| server module ↔ client mirror | Two hand-maintained copies of the same tunables; drift silently changes economy math once later phases wire them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01 | Tampering | client/server constant mirror (index.ts ↔ constants.ts) | low | mitigate | serverSync.test.ts asserts each server literal equals its client value (INV-5); build fails on drift. |
| T-00-02 | Tampering | premature reducer reference to unwired constants | low | mitigate | Task actions explicitly forbid any reducer/schema reference this phase; verify greps confirm declaration-only. |
| T-00-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase (constants + tests only); no legitimacy gate required. |
</threat_model>

<verification>
- Server: all six numeric constants extractable by `const NAME = (\d+(?:\.\d+)?);` with locked values; TRANSCEND_SHARD_COST present as a function.
- Client: all six mirrored with `// server` marker and identical values; TRANSCEND_SHARD_COST exported.
- `pnpm exec vitest run src/game/data/__tests__/serverSync.test.ts src/game/data/__tests__/transcendence.test.ts` passes.
- `pnpm test` green (full suite, no regressions).
- `pnpm build` green.
- No schema/reducer diff in spacetimedb/src/index.ts beyond the constant block + helper; no publish.
- PROGRESS.md Phase 0 marked done.
</verification>

<success_criteria>
1. All six SHARD_*/TRANSCEND_* constants exist server-side with locked values (MAX_TRANSCEND_LEVEL=10, TRANSCEND_DAMAGE_STEP=0.05, TRANSCEND_HEAL_STEP=0.08, SHARD_PER_OVERFLOW_DUPE=1, SHARD_DEATH_LOSS=1, RAID_SHARD_PAYOUT=6).
2. serverSync.test.ts asserts every new constant matches its client mirror.
3. A unit test confirms constants present + in valid ranges and TRANSCEND_SHARD_COST(n)===n.
4. pnpm test and pnpm build are green; no publish (no schema/logic change).
5. PROGRESS.md status box + checklist updated for Phase 0.
</success_criteria>

<output>
Create `.planning/phases/00-lock-transcendence-constants/00-01-SUMMARY.md` when done.
Commit message: `feat(transcend): lock constants for transcendence system`
</output>
