---
phase: 05-multiplayer-party
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - src/ui/PartyToast.tsx
  - src/ui/PartyRoster.tsx
  - src/ui/SettingsScreen.tsx
  - src/App.tsx
  - src/index.css
autonomous: true
requirements: [REQ-multiplayer-party]
must_haves:
  truths:
    - "An incoming invite/join-request shows a .party-toast (role=status, aria-live=polite) with Pieņemt/Noraidīt that auto-dismisses at ~10s client-side; hover/focus pauses the timer (D-07)."
    - "A missed/dismissed invite remains actionable in a NOKAVĒTIE AICINĀJUMI section under Settings (D-07)."
    - "The .party-roster panel shows one row per member: active character + role badge + leader crown + online dot (D-10)."
  artifacts:
    - src/ui/PartyToast.tsx
    - src/ui/PartyRoster.tsx
    - "NOKAVĒTIE AICINĀJUMI section in src/ui/SettingsScreen.tsx"
    - ".party-toast / .party-roster / .missed-invites CSS in src/index.css"
  key_links:
    - "Toast/roster/missed-invites all consume the myInvites + myRoster state and the accept/decline/leave reducer callers wired in Plan 04."
    - "The 10s dismiss timer is client-only — the invite persists server-side (D-08); an expired toast becomes a missed-invite row, not a server delete."
    - "Roster derives role from statsFor(activeCharacterId).role via ROLE_META — no new server field."
---

<objective>
Deliver the "receive" half and the live roster: the invite/join-request toast (10s client-side
auto-dismiss, Accept/Decline), the missed-invites list under Settings, and the party roster panel
with role badge, leader crown, and online dot. Render wiring lands in App.tsx.

Purpose: completes the two-client loop — a recipient sees an invite, accepts/declines from a
non-intrusive toast or later from Settings, and both members see a live roster. All rendering
reads already-subscribed rows; the toast timer is client-only (the invite lives on server-side per
D-08).
Output: toast + missed-invites + roster surfaces, all wired to the Plan 04 reducer callers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-multiplayer-party/05-CONTEXT.md
@.planning/phases/05-multiplayer-party/05-UI-SPEC.md
@.planning/phases/05-multiplayer-party/05-PATTERNS.md

# Edit targets + reuse analogs:
@src/App.tsx
@src/ui/SettingsScreen.tsx
@src/ui/CharacterSheet.tsx
@src/game/data/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PartyToast component (10s client-side, accept/decline) + App render wiring</name>
  <files>src/ui/PartyToast.tsx, src/App.tsx, src/index.css</files>
  <read_first>
    - src/App.tsx:518-522 (.shard-toast render + role="status" + key remount trick).
    - src/index.css (search `.shard-toast`, `shard-toast-in` keyframe) — in/out keyframe precedent to clone.
    - 05-UI-SPEC.md "Component Contracts §2" (.party-toast) + "Animation & Motion Contract" (reduced-motion) + "Copywriting Contract" (toast copy).
  </read_first>
  <action>
    Create src/ui/PartyToast.tsx — a right-edge transient for a single incoming invite/request row.
    Container `role="status"` + `aria-live="polite"` (announce without stealing focus — Pitfall 5).
    Message copy (14px/400) from `kind`: 'invite' → `{vārds} aicina tevi savā pulkā`, 'request' →
    `{vārds} lūdz pievienoties tavam pulkam` (kind is DISPLAY only). Action row (gap 8px):
    `Pieņemt` (Button variant="primary") → onAccept(inviteId); `Noraidīt` (Button variant="ghost") →
    onDecline(inviteId); each ≥44px. A client-only ~10s setTimeout dismisses the toast; hovering or
    focusing the toast PAUSES/clears the timer (do not time out a keyboard user). On expiry the toast
    is removed from view only — it does NOT call any reducer (the invite persists server-side, D-08),
    so it remains in Settings' missed list. Copy the `key={...}` remount trick to restart the enter
    animation per new toast.
    In App.tsx, render toast(s) from `myInvites` (recipient == me, from Plan 04): show the newest
    not-yet-dismissed invite as a `<PartyToast>` (stack downward with 8px gap if multiple), passing the
    inviter's name (look up players by joinerIdentity for 'request', or by the party leader for
    'invite' — resolve the human name to show), and the acceptInvite/declineInvite callers. Track
    locally which invite ids have been toast-dismissed so an expired toast does not re-appear but stays
    in the missed list.
    In src/index.css add `.party-toast` (background var(--panel); border 1px var(--line); radius 12px;
    padding 12px; top:88px; right:max(16px,env(safe-area-inset-right)); z-index:30) with a
    translateX(120%)→0 enter and translateX→120% exit (transform/opacity only, ease-out 0.3s), plus the
    mandatory `prefers-reduced-motion` fade-only block. pointer-events only on the buttons (rest passive).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>PartyToast announces politely, offers Pieņemt/Noraidīt wired to accept/decline, auto-dismisses at ~10s client-side (paused on hover/focus) without any server call on expiry; App renders it from myInvites; build green.</done>
  <acceptance_criteria>
    - Toast is role=status + aria-live=polite (no focus steal).
    - Expiry removes the toast view only — no reducer call on timeout (invite persists, D-08).
    - Hover/focus pauses the 10s timer.
    - `.party-toast` has a reduced-motion fade-only block; no new color tokens.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PartyRoster panel (role badge, leader crown, online dot)</name>
  <files>src/ui/PartyRoster.tsx, src/App.tsx, src/index.css</files>
  <read_first>
    - src/ui/CharacterSheet.tsx:93-101 (ROLE_META role-badge render to reuse verbatim).
    - 05-UI-SPEC.md "Component Contracts §4" (.party-roster row spec, states, ordering).
    - src/game/data/characters.ts:14-31 (ROLE_META) — role source.
  </read_first>
  <action>
    Create src/ui/PartyRoster.tsx — a compact panel (reuse Modal size="sm" OR an in-HUD panel reusing
    `.panel`/`.modal__panel` styling), title `Pulks · {n}/4` (n = myRoster.length, 4 = RAID_PARTY_SIZE).
    Props: myRoster (member rows), the party (for leaderIdentity), the `players` set, and myIdentityHex.
    One `.party-roster__row` per member (row padding 11px 12px, radius 8px, background var(--panel)):
    online dot (● var(--accent) / ○ var(--muted), aria-label 'Tiešsaistē'/'Nesaistē') from the matched
    player.online; member/player name (14px); active-character name in its element color from
    player.activeCharacterId; role badge reusing `ROLE_META[CHARACTERS[activeCharacterId].role]`
    (color token + Latvian label + aria — no new server field); leader crown `♛` (var(--gold),
    aria-label 'Pulka vadonis') on the row where party.leaderIdentity hex === member.identity hex.
    Order: leader first, then remaining members by joinedAt. Offline member row: opacity 0.5 + hollow
    dot. Empty state (viewer in no party): heading `Tu neesi nevienā pulkā` + body
    `Piesit spēlētājam, lai izveidotu pulku vai tam pievienotos.`.
    In App.tsx, mount <PartyRoster> from the HUD party chip toggle (Plan 04 state), passing myRoster +
    party + players + myIdentityHex. In src/index.css add `.party-roster` + `.party-roster__row`
    classes (reuse `.modal__panel`/`.panel` radius+bg; role badge reuses `.sheet__role`). No new tokens
    (the ♛ gold status glyph exception is documented in UI-SPEC Color).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>PartyRoster lists members with online dot + name + active character + role badge + leader crown, leader-first ordering, offline dimming, and an empty state; opens from the HUD chip; build green.</done>
  <acceptance_criteria>
    - Role badge via ROLE_META (no new server field); online + leader carry aria-labels (non-color-alone).
    - Leader rendered first; offline rows dimmed with hollow dot.
    - Title shows n/4 against RAID_PARTY_SIZE; empty-state copy present.
    - `pnpm build` succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Missed-invites section in SettingsScreen</name>
  <files>src/ui/SettingsScreen.tsx, src/App.tsx</files>
  <read_first>
    - src/ui/SettingsScreen.tsx:33/51/60 (.settings__section heading + content pattern).
    - 05-UI-SPEC.md "Component Contracts §3" (.missed-invites) + "Copywriting Contract" (empty state).
  </read_first>
  <action>
    In src/ui/SettingsScreen.tsx add a new section `<p className="settings__section">NOKAVĒTIE
    AICINĀJUMI</p>` (contract-default mount, D-07) listing the viewer's pending party_invite rows
    (recipient == me — the same myInvites set), one `.missed-invites__item` each: the message (same
    Latvian copy as the toast, by kind) + `Pieņemt` / `Noraidīt` (reuse Button) wired to the SAME
    acceptInvite/declineInvite reducer callers. Empty state copy `Nav nokavētu aicinājumu.` when the
    list is empty. Thread the required props (myInvites, players for name lookup, onAccept, onDecline)
    from App.tsx down to SettingsScreen. This surface is calm/non-intrusive and shows ALL pending
    invites (including ones whose toast was dismissed/expired) since the invite persists server-side.
    Add minimal `.missed-invites` / `.missed-invites__item` styling to src/index.css if needed
    (reuse `.settings__section` + `.panel` language, no new tokens).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>Settings shows a NOKAVĒTIE AICINĀJUMI section listing all pending invites with working Accept/Decline (same reducers) and an empty state; build green.</done>
  <acceptance_criteria>
    - Section lists pending invites for the viewer (recipient == me), including dismissed/expired-toast ones.
    - Accept/Decline call the same acceptInvite/declineInvite reducers.
    - Empty-state copy `Nav nokavētu aicinājumu.` shown when none.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server subscription → toast/roster/missed-list | Only the viewer's own invites (recipient == me) drive these surfaces; roster reads public party rows. |
| client accept/decline UI → reducer | The UI passes inviteId only; server (Plan 02) re-verifies actor == recipient before mutating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | toast + missed-invites | medium | mitigate | Both render only myInvites (already filtered to recipient == me by the Plan 04 subscription); no cross-player invite is ever shown. |
| T-05-06 | Denial of Service | toast flood | low | accept | aria-live=polite non-blocking toast; expiry drops to a calm Settings list; server-side dedupe (Plan 02). Accepted residual at LAN scale. |
| T-05-02 | Elevation of Privilege | accept/decline from UI | high | mitigate | UI cannot bypass authz — acceptInvite/declineInvite re-check actor == recipientIdentity server-side (Plan 02); the client only supplies inviteId. |
| T-05-SC | Tampering | package installs | low | accept | No package installs this phase. |
</threat_model>

<verification>
- `pnpm build` green.
- Toast/missed-list render only myInvites; grep confirms no unfiltered invite iteration for other recipients.
- Roster/toast/missed-list wired to the Plan 04 accept/decline/leave callers.
</verification>

<success_criteria>
- Receive-side + roster complete: toast (10s client-only) → accept/decline; missed list under Settings; live roster with role/crown/online.
- Build green; reduced-motion + a11y contracts honored.
</success_criteria>

<artifacts_produced>
- `src/ui/PartyToast.tsx`, `src/ui/PartyRoster.tsx` (new).
- NOKAVĒTIE AICINĀJUMI section in `src/ui/SettingsScreen.tsx`.
- `.party-toast` / `.party-roster` / `.missed-invites` CSS.
- App.tsx render wiring for toast + roster + missed-invite prop threading.
</artifacts_produced>

<output>
Create `.planning/phases/05-multiplayer-party/05-05-SUMMARY.md` when done.
</output>
