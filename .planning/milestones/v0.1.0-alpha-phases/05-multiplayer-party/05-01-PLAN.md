---
phase: 05-multiplayer-party
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - spacetimedb/src/index.ts
  - src/game/data/constants.ts
  - src/game/data/__tests__/serverSync.test.ts
  - spacetimedb/src/partyRules.ts
  - src/game/data/__tests__/partyRules.test.ts
autonomous: true
requirements: [REQ-multiplayer-party]
must_haves:
  truths:
    - "nextLeader() returns the oldest-joined member's identity and null when the roster is empty (D-05)."
    - "canAccept() rejects a joiner already in a party and rejects a roster at the RAID_PARTY_SIZE cap (D-06)."
    - "RAID_PARTY_SIZE is declared server-side as a distinct constant (not PARTY_SIZE) and mirrored client-side with parity asserted."
  artifacts:
    - spacetimedb/src/partyRules.ts
    - src/game/data/__tests__/partyRules.test.ts
    - "RAID_PARTY_SIZE constant in spacetimedb/src/index.ts and src/game/data/constants.ts"
  key_links:
    - "serverSync.test.ts extractServerConstant regex must match the one-line `const RAID_PARTY_SIZE = 4;` form in index.ts."
    - "partyRules.test.ts imports the real helper across the package boundary (../../../../spacetimedb/src/partyRules), same as deathPenalty.test.ts."
---

<objective>
Lay the Nyquist Wave-0 foundation for the multiplayer party: a distinct `RAID_PARTY_SIZE = 4`
constant (mirrored client/server with a parity guard) and the pure, dependency-free
`partyRules.ts` helpers (`nextLeader`, `canAccept`) that hold the security-critical promotion
and accept-eligibility logic — extracted so they are unit-testable under vitest before any
reducer wiring exists.

Purpose: STDB reducers cannot run in vitest, so all rule correctness (oldest-joined promotion
per D-05, one-party-per-player + cap enforcement per D-06) lives in pure helpers with unit
coverage. This mirrors the proven `deathPenalty.ts` precedent from Phase 3.
Output: `partyRules.ts` + its test, `RAID_PARTY_SIZE` in three sites, all green under `pnpm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multiplayer-party/05-CONTEXT.md
@.planning/phases/05-multiplayer-party/05-PATTERNS.md
@.planning/phases/05-multiplayer-party/05-VALIDATION.md

# Authoritative analogs to copy structure from:
@spacetimedb/src/deathPenalty.ts
@src/game/data/__tests__/deathPenalty.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RAID_PARTY_SIZE constant (server + client mirror + parity test)</name>
  <files>spacetimedb/src/index.ts, src/game/data/constants.ts, src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - spacetimedb/src/index.ts:72 (`const PARTY_SIZE = 4;`) and :86 (`const RAID_SHARD_PAYOUT = 6;`) — the tunables block to insert into.
    - src/game/data/constants.ts:13 (`export const RAID_SHARD_PAYOUT = 6; // server`) — the client mirror style.
    - src/game/data/__tests__/serverSync.test.ts:69-73 (extractServerConstant regex) and :144-158 (the `it.each([...])` `[name, value]` parity list).
  </read_first>
  <action>
    In spacetimedb/src/index.ts, add a new line in the tunables block immediately after
    `const RAID_SHARD_PAYOUT = 6;` (index.ts:86): `const RAID_PARTY_SIZE = 4;` — the player-party
    (raid squad) cap per D-06, DISTINCT from the existing PARTY_SIZE=4 which is the personal
    4-character team. It MUST be on a single line in the exact `const NAME = <number>;` form so
    serverSync's extractServerConstant regex (:69-73) can read it. Do NOT reference PARTY_SIZE in
    any party code (anti-pattern: silently couples two unrelated caps).
    In src/game/data/constants.ts, add after the RAID_SHARD_PAYOUT mirror (:13):
    `export const RAID_PARTY_SIZE = 4; // server — player-party cap, distinct from PARTY_SIZE (character team)`.
    In src/game/data/__tests__/serverSync.test.ts, import RAID_PARTY_SIZE alongside RAID_SHARD_PAYOUT
    (:18) and append `['RAID_PARTY_SIZE', RAID_PARTY_SIZE]` to the `it.each([...])` parity list
    (after the RAID_SHARD_PAYOUT entry at :155). This satisfies INV-5 client/server parity.
  </action>
  <verify>
    <automated>pnpm vitest run serverSync</automated>
  </verify>
  <done>RAID_PARTY_SIZE exists in index.ts (one-line const form), constants.ts, and passes the serverSync parity assertion; PARTY_SIZE is untouched.</done>
  <acceptance_criteria>
    - serverSync parity test passes with RAID_PARTY_SIZE mapped to 4 on both sides.
    - Server declaration is a single line matching `const RAID_PARTY_SIZE = 4;`.
    - No new reference to PARTY_SIZE was introduced.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure partyRules.ts helpers (nextLeader, canAccept) test-first</name>
  <files>spacetimedb/src/partyRules.ts, src/game/data/__tests__/partyRules.test.ts</files>
  <read_first>
    - spacetimedb/src/deathPenalty.ts (whole file) — determinism header comment style + pure-function shape to copy.
    - src/game/data/__tests__/deathPenalty.test.ts:1-19 — cross-package import mechanism (`../../../../spacetimedb/src/...`) and one-`it()`-per-branch discipline.
    - 05-RESEARCH.md "Pattern 3: Leader promotion" and "Pattern 2: Enforce cap + uniqueness".
  </read_first>
  <behavior>
    nextLeader(members) where members: Array<{ identityHex: string; joinedAtMicros: bigint }>:
    - Empty array -> returns null (caller disbands the party). (D-05)
    - Single member -> returns that member's identityHex.
    - Multiple -> returns the identityHex with the smallest joinedAtMicros (oldest-joined). (D-05)
    - Tie on joinedAtMicros -> deterministic: return the lexicographically smallest identityHex
      (so promotion is reproducible; ctx.timestamp ties are rare but must not be order-dependent).
    canAccept(rosterSize, joinerAlreadyPartied, cap) -> { ok: boolean; reason: string | null }:
    - joinerAlreadyPartied === true -> { ok: false, reason: 'already_partied' } (checked FIRST). (D-06)
    - else rosterSize >= cap -> { ok: false, reason: 'full' }. (D-06)
    - else -> { ok: true, reason: null }.
    - Boundary: rosterSize === cap-1 with a free joiner -> ok; rosterSize === cap -> full.
  </behavior>
  <action>
    Create spacetimedb/src/partyRules.ts as a PURE, dependency-free module (no ctx/db import, no
    spacetimedb import, no random, no time) — identical discipline to deathPenalty.ts, with a
    determinism header comment. Export the two functions and their result types exactly as
    described in <behavior>. Model members with primitive fields (identityHex: string,
    joinedAtMicros: bigint) rather than STDB Identity/Timestamp objects, so the helper stays pure
    and the reducer (Plan 02) maps its rows into these primitives before calling. Implement
    nextLeader via a single reduce that keeps the smaller (joinedAtMicros, then identityHex) pair;
    return null on empty. Implement canAccept with the branch ORDER in <behavior> (already-partied
    before cap), returning the stable reason strings.
    Create the test at src/game/data/__tests__/partyRules.test.ts (client test dir — the
    established deathPenalty precedent; vitest's default include glob picks it up and the
    cross-package import works). Import via `../../../../spacetimedb/src/partyRules`. Write the RED
    tests FIRST covering every bullet in <behavior> (empty, single, oldest-of-many, tie-break,
    canAccept full/already-partied/ok/boundary), confirm they fail, then implement partyRules.ts to
    GREEN. Do NOT place the test beside the server file — vitest cwd is project root and the
    proven convention is the client __tests__ dir.
  </action>
  <verify>
    <automated>pnpm vitest run partyRules</automated>
  </verify>
  <done>partyRules.ts exports nextLeader + canAccept; partyRules.test.ts covers all <behavior> branches and is green; the module imports nothing (pure).</done>
  <acceptance_criteria>
    - `pnpm vitest run partyRules` passes with a test per behavior branch.
    - partyRules.ts has zero imports (grep confirms dependency-free).
    - nextLeader tie-break is deterministic (lexicographic identityHex), covered by a test.
    - canAccept checks already-partied before cap, covered by tests for both reasons.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer (Plan 02) → partyRules helper | The reducer supplies already-authorized, server-read rows; the helper decides promotion/eligibility with no side effects. Correctness here is what makes the cap/uniqueness enforceable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | canAccept eligibility logic | high | mitigate | canAccept rejects both cap-overflow (rosterSize >= RAID_PARTY_SIZE) and double-join (joinerAlreadyPartied); unit-tested at the boundary so Plan 02's accept path has a correct decision function. The DB `unique()` constraint (Plan 02) is the atomic backstop. |
| T-05-SC | Tampering | package installs | low | accept | No package installs in this phase (RESEARCH: zero external deps); nothing to verify. |
</threat_model>

<verification>
- `pnpm vitest run partyRules serverSync` green.
- `grep -n "import" spacetimedb/src/partyRules.ts` returns no import lines (pure module).
- `grep -c "PARTY_SIZE = 4" spacetimedb/src/index.ts` shows both PARTY_SIZE and RAID_PARTY_SIZE declared as distinct one-line consts.
</verification>

<success_criteria>
- RAID_PARTY_SIZE mirrored and parity-asserted (INV-5).
- nextLeader + canAccept fully unit-covered and pure.
- `pnpm test` green.
</success_criteria>

<artifacts_produced>
- `RAID_PARTY_SIZE` constant (server `spacetimedb/src/index.ts`, client `src/game/data/constants.ts`).
- `spacetimedb/src/partyRules.ts` — exports `nextLeader(members)`, `canAccept(rosterSize, joinerAlreadyPartied, cap)` + their result types.
- `src/game/data/__tests__/partyRules.test.ts` — unit coverage for the above.
- serverSync `RAID_PARTY_SIZE` parity assertion.
</artifacts_produced>

<output>
Create `.planning/phases/05-multiplayer-party/05-01-SUMMARY.md` when done.
</output>
