---
phase: 01-constellation-shard-currency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/gachaOverflow.ts
  - src/game/data/__tests__/gachaOverflow.test.ts
autonomous: true
requirements: [REQ-shard-currency-mint]

must_haves:
  truths:
    - "A dupe of a character already at MAX_CONSTELLATION resolves to shardMinted === SHARD_PER_OVERFLOW_DUPE with constellation staying at max."
    - "A dupe of a sub-C6 character resolves to constellation+1 with shardMinted === 0."
    - "The overflow decision has a pure-helper unit test (satisfies acceptance criterion 4)."
  artifacts:
    - "spacetimedb/src/gachaOverflow.ts exporting resolveDupeGrant + DupeGrantOutcome"
    - "src/game/data/__tests__/gachaOverflow.test.ts"
  key_links:
    - "gachaOverflow.ts is dependency-free (no ctx/db/import) so it is cross-importable by the client vitest runner."
    - "Test imports the helper via ../../../../spacetimedb/src/gachaOverflow (cross-package boundary) — same pattern as serverWorldSim.test.ts."
---

<objective>
Create the pure, deterministic overflow-decision helper `resolveDupeGrant` and its unit test BEFORE any reducer wiring exists. This is the Nyquist wave-0 deliverable: the test that proves the C6-overflow mint semantics lands together with the implementation, so later waves wire against a proven contract.

Purpose: Extract the dupe-grant decision into a `ctx`/`db`-free module so it is unit-testable via the repo's established cross-import pattern, and so `grantCharacter` (wave 2) can call it while staying deterministic (CLAUDE.md rule 2).
Output: `spacetimedb/src/gachaOverflow.ts` (exported `resolveDupeGrant`) + `src/game/data/__tests__/gachaOverflow.test.ts` (both branches green).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-constellation-shard-currency/01-RESEARCH.md
@.planning/phases/01-constellation-shard-currency/01-PATTERNS.md

# Analog for the pure-helper module shape:
@spacetimedb/src/combatMath.ts
# Analog for the cross-import test mechanism + structure:
@src/game/data/__tests__/serverWorldSim.test.ts
@src/game/data/__tests__/transcendence.test.ts
# Client mirror constants to drive assertions (do NOT hardcode magic numbers):
@src/game/data/constants.ts
</context>

<artifacts_this_phase_produces>
This plan produces:
- `spacetimedb/src/gachaOverflow.ts` — new pure module exporting `resolveDupeGrant(currentConstellation, maxConstellation, shardPerOverflow)` and interface `DupeGrantOutcome { constellation: number; shardMinted: number }`.
- `src/game/data/__tests__/gachaOverflow.test.ts` — new Vitest file covering both overflow branches.
(Later plans add the `transcendShards` player column, regenerated bindings, the React `transcendShards` prop, and the shard chip.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure resolveDupeGrant helper</name>
  <files>spacetimedb/src/gachaOverflow.ts</files>
  <read_first>
    - spacetimedb/src/combatMath.ts (module shape to copy: file-level comment stating it is kept dependency-free for direct vitest import, then plain exported pure functions with no ctx/db/import)
    - .planning/phases/01-constellation-shard-currency/01-PATTERNS.md (section "spacetimedb/src/gachaOverflow.ts" — concrete implementation)
  </read_first>
  <behavior>
    - resolveDupeGrant(3, 6, 1) === { constellation: 4, shardMinted: 0 }  (sub-C6 dupe: constellation increments, no shard)
    - resolveDupeGrant(5, 6, 1) === { constellation: 6, shardMinted: 0 }  (last sub-C6 step still mints nothing)
    - resolveDupeGrant(6, 6, 1) === { constellation: 6, shardMinted: 1 }  (C6-overflow dupe: constellation pinned at max, one shard minted)
  </behavior>
  <action>
    Create `spacetimedb/src/gachaOverflow.ts` as a dependency-free module (no imports, no `ctx`, no `ctx.db`, no `ctx.random`, no time) mirroring the `combatMath.ts` style. Open with a one-line file comment stating the module is kept dependency-free so it can be unit-tested directly under the client's vitest runner. Export an interface `DupeGrantOutcome` with numeric fields `constellation` and `shardMinted`. Export a pure function `resolveDupeGrant(currentConstellation: number, maxConstellation: number, shardPerOverflow: number): DupeGrantOutcome`: when `currentConstellation < maxConstellation` return `{ constellation: currentConstellation + 1, shardMinted: 0 }`; otherwise return `{ constellation: maxConstellation, shardMinted: shardPerOverflow }`. Keep it pure arithmetic only — this preserves reducer determinism (CLAUDE.md rule 2) and keeps the value server-authoritative (mitigates T-1-01: no client-supplied amount).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p spacetimedb</automated>
  </verify>
  <acceptance_criteria>
    - `spacetimedb/src/gachaOverflow.ts` exists and exports `resolveDupeGrant` + `DupeGrantOutcome`.
    - The module contains no `import` statement, no `ctx`, and no `db` reference (grep confirms: `grep -v '^//' spacetimedb/src/gachaOverflow.ts | grep -cE '\b(import|ctx|db)\b'` returns 0).
  </acceptance_criteria>
  <done>Pure helper file created; returns the two documented outcomes; contains no side-effecting references.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test both overflow branches via cross-import</name>
  <files>src/game/data/__tests__/gachaOverflow.test.ts</files>
  <read_first>
    - src/game/data/__tests__/serverWorldSim.test.ts (cross-import mechanism: `import { ... } from '../../../../spacetimedb/src/...'`)
    - src/game/data/__tests__/transcendence.test.ts (describe/it structure + importing client mirror constants)
    - src/game/data/constants.ts (SHARD_PER_OVERFLOW_DUPE — use this instead of hardcoding 1)
    - spacetimedb/src/gachaOverflow.ts (the module under test, created in Task 1)
  </read_first>
  <behavior>
    - Test: sub-C6 dupe → resolveDupeGrant(current, MAX_CONSTELLATION, SHARD_PER_OVERFLOW_DUPE) returns { constellation: current+1, shardMinted: 0 } for current values below max.
    - Test: C6 dupe → resolveDupeGrant(MAX_CONSTELLATION, MAX_CONSTELLATION, SHARD_PER_OVERFLOW_DUPE) returns { constellation: MAX_CONSTELLATION, shardMinted: SHARD_PER_OVERFLOW_DUPE }.
  </behavior>
  <action>
    Create `src/game/data/__tests__/gachaOverflow.test.ts`. Import `describe, expect, it` from `vitest`. Import the helper via the cross-package relative path `import { resolveDupeGrant } from '../../../../spacetimedb/src/gachaOverflow';` (same mechanism as `serverWorldSim.test.ts`). Import `SHARD_PER_OVERFLOW_DUPE` and `MAX_CONSTELLATION` from `../constants` to avoid re-hardcoding magic numbers — if `MAX_CONSTELLATION` is not exported from the client mirror, use a local `const MAX = 6` and add a comment noting it mirrors the server constant. Write one describe block with: (a) an `it.each` over sub-C6 constellations (e.g. 0,1,2,5) asserting `constellation === current+1` and `shardMinted === 0`; (b) an `it` for the C6-overflow case asserting `constellation === MAX` and `shardMinted === SHARD_PER_OVERFLOW_DUPE`. These two branches are exactly the REQ-shard-currency-mint pure-helper acceptance.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/gachaOverflow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/gachaOverflow.test.ts` passes with both branches covered.
    - The test imports the real helper across the package boundary (not a local re-implementation) and uses `SHARD_PER_OVERFLOW_DUPE` from the client mirror.
  </acceptance_criteria>
  <done>Both overflow branches are asserted and green; the test consumes the shared constant rather than a literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → pullBanner reducer (WebSocket) | Untrusted client input crosses here; the shard amount must never originate client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering/Elevation | resolveDupeGrant / shard mint | high | mitigate | Mint amount derives ONLY from `shardPerOverflow` (a server constant) and owned-constellation state passed by the reducer; the helper takes no client-supplied value. |
| T-1-02 | Tampering | reducer determinism | low | mitigate | Helper is pure arithmetic — no `ctx.random`, no time, no I/O — keeping the calling reducer deterministic (CLAUDE.md rule 2). |
| T-1-SC | Tampering | package installs | low | accept | This phase installs ZERO external packages (RESEARCH §Package Legitimacy Audit: not applicable); no supply-chain surface added. |
</threat_model>

<verification>
- `pnpm vitest run src/game/data/__tests__/gachaOverflow.test.ts` green (both branches).
- Helper module is dependency-free (grep confirms no import/ctx/db).
</verification>

<success_criteria>
- `resolveDupeGrant` exists as a pure module and both overflow branches pass their unit test.
- No `transcendShards` column or reducer wiring yet (that is wave 2) — this plan is intentionally scoped to the testable pure decision.
</success_criteria>

<output>
Create `.planning/phases/01-constellation-shard-currency/01-01-SUMMARY.md` when done.
</output>
