---
phase: 01-constellation-shard-currency
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - src/index.css
  - src/App.tsx
  - src/ui/GachaScreen.tsx
  - src/ui/CharacterScreen.tsx
autonomous: false
requirements: [REQ-shard-currency-mint]

must_haves:
  truths:
    - "The player's transcendShards balance is visible in the GachaScreen wallet (left of the swapping readout, on every tab)."
    - "The player's transcendShards balance is visible in the CharacterScreen topbar (right-aligned, before the close button)."
    - "Both chips are visually identical: glyph ◈, --gold #f2c14e, 15px, letter-spacing 0.04em, always shown (renders 0), aria-label 'Zvaigžņu šķembas: {n}'."
    - "The client only renders the subscribed value (transcendShards={myPlayer?.transcendShards ?? 0}); it never computes or mutates it."
  artifacts:
    - "transcendShards: number prop on GachaScreenProps and CharacterScreenProps"
    - "shard chip render in GachaScreen wallet and CharacterScreen topbar"
    - "shared .wallet-chip treatment (or screen-local copy of the .gacha__wallet values) in src/index.css"
  key_links:
    - "App.tsx threads transcendShards={myPlayer?.transcendShards ?? 0} into BOTH screens from useTable(tables.player)."
    - "Depends on regenerated bindings (01-03) so myPlayer.transcendShards is typed."
---

<objective>
Surface the `transcendShards` balance in the two existing screens per the UI-SPEC: thread the value from the subscribed `player` row through `App.tsx` and render an identical gold `◈` shard chip in the `GachaScreen` wallet and the `CharacterScreen` topbar. This closes success criterion 3 (balance visible in Character and Gacha screens) and ends with a human-verified local playtest of the mint behavior.

Purpose: Read-only display of the scarce currency, mirroring the existing gems wallet chip exactly (no new tokens, no client authority over the value).
Output: modified `src/index.css`, `src/App.tsx`, `src/ui/GachaScreen.tsx`, `src/ui/CharacterScreen.tsx`; a passing `pnpm build`; and a human-verified playtest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-constellation-shard-currency/01-RESEARCH.md
@.planning/phases/01-constellation-shard-currency/01-PATTERNS.md
@.planning/phases/01-constellation-shard-currency/01-UI-SPEC.md

# Screens + plumbing to modify (each is its own best exemplar via the existing `gems` prop):
@src/App.tsx
@src/ui/GachaScreen.tsx
@src/ui/CharacterScreen.tsx
@src/index.css
</context>

<artifacts_this_phase_produces>
This plan adds: `transcendShards: number` to `GachaScreenProps` and `CharacterScreenProps`; the `transcendShards={myPlayer?.transcendShards ?? 0}` prop passed to both screens in `App.tsx`; the `◈` shard chip render in both screens; and the shared `.wallet-chip` visual treatment (preferred) or a screen-local copy of the `.gacha__wallet` values in `src/index.css`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wallet-chip CSS + thread transcendShards through App.tsx into GachaScreen</name>
  <files>src/index.css, src/App.tsx, src/ui/GachaScreen.tsx</files>
  <read_first>
    - src/index.css (`.gacha__wallet` ~1090-1095: `font-size: 15px; color: var(--gold); letter-spacing: 0.04em;` and the `.gacha__gem` glyph span)
    - src/App.tsx (`gems={myPlayer?.gems ?? 0}` passed to GachaScreen ~451 and Hud ~426; `myPlayer` derived from `useTable(tables.player)`)
    - src/ui/GachaScreen.tsx (GachaScreenProps ~38-57; destructure ~67-84; `.gacha__wallet` render ~218-229)
    - .planning/phases/01-constellation-shard-currency/01-UI-SPEC.md (Component Contract — Transcend Shard Chip; Placement → GachaScreen)
    - .planning/phases/01-constellation-shard-currency/01-PATTERNS.md (sections "src/App.tsx" and "src/ui/GachaScreen.tsx")
  </read_first>
  <action>
    (a) In `src/index.css`, add a shared `.wallet-chip` class capturing the locked chip treatment: `color: var(--gold)` (#f2c14e), `font-size: 15px`, `letter-spacing: 0.04em` (values lifted verbatim from `.gacha__wallet`; do NOT invent new tokens). This is the preferred DRY approach; a screen-local copy is an acceptable fallback if the shared class causes layout regressions.
    (b) In `src/App.tsx`, pass `transcendShards={myPlayer?.transcendShards ?? 0}` to the `GachaScreen` instance (alongside the existing `gems={myPlayer?.gems ?? 0}`). The `?? 0` guard also covers the pre-subscription/undefined window. The client never computes this value.
    (c) In `src/ui/GachaScreen.tsx`, add `transcendShards: number` to `GachaScreenProps`, destructure it, and render the shard chip INSIDE `.gacha__wallet`, immediately to the LEFT of the existing swapping readout, on EVERY tab, separated by a 12px gap. Chip content: a `.gacha__gem` (or `.wallet-chip`) span with glyph `◈` (U+25C8) then `{transcendShards}`, with `aria-label="Zvaigžņu šķembas: {transcendShards}"`. Result on a non-characters tab reads `◈ {shards}   ✦ {gems}` (shards first). The chip is always shown, including at 0.
  </action>
  <verify>
    <automated>grep -c "transcendShards" src/ui/GachaScreen.tsx && grep -c "transcendShards" src/App.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `GachaScreenProps` declares `transcendShards: number`; `App.tsx` passes `transcendShards={myPlayer?.transcendShards ?? 0}` to GachaScreen.
    - The `◈` glyph and `{transcendShards}` render inside `.gacha__wallet` on every tab with `aria-label="Zvaigžņu šķembas: ..."`.
    - `pnpm build` compiles.
  </acceptance_criteria>
  <done>Gacha wallet shows the gold `◈` shard chip on every tab, read from the subscribed player row; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Shard chip in CharacterScreen topbar</name>
  <files>src/App.tsx, src/ui/CharacterScreen.tsx</files>
  <read_first>
    - src/ui/CharacterScreen.tsx (CharacterScreenProps ~15-30 — no currency prop today; destructure ~54-64; `.cscreen__topbar` header ~128-170; `.cscreen__close` button ~167-169)
    - src/App.tsx (CharacterScreen instantiation ~472-478 — currently no currency prop)
    - .planning/phases/01-constellation-shard-currency/01-UI-SPEC.md (Placement → CharacterScreen; "Consistency requirement": visually identical to the gacha instance)
    - .planning/phases/01-constellation-shard-currency/01-PATTERNS.md (section "src/ui/CharacterScreen.tsx")
  </read_first>
  <action>
    (a) In `src/App.tsx`, pass `transcendShards={myPlayer?.transcendShards ?? 0}` to the `CharacterScreen` instance.
    (b) In `src/ui/CharacterScreen.tsx`, add `transcendShards: number` to `CharacterScreenProps` and destructure it. Render the shard chip in `.cscreen__topbar`, right-aligned, BEFORE the `.cscreen__close` button, pinned outside the roster scroll region so it stays put while the roster scrolls. Use the SAME glyph/color/size/format as the gacha instance (glyph `◈`, `.wallet-chip`/`.gacha__gem` gold 15px treatment, `aria-label="Zvaigžņu šķembas: {transcendShards}"`, always shown including 0). The two chips must be visually identical.
  </action>
  <verify>
    <automated>grep -c "transcendShards" src/ui/CharacterScreen.tsx && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `CharacterScreenProps` declares `transcendShards: number`; `App.tsx` passes it to CharacterScreen.
    - The `◈` shard chip renders in `.cscreen__topbar` before the close button, outside the roster scroll region, matching the gacha chip's glyph/color/size/aria-label.
    - `pnpm build` compiles.
  </acceptance_criteria>
  <done>CharacterScreen topbar shows an identical gold `◈` shard chip pinned before the close button; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify shard chips render + C6-dupe mint playtest</name>
  <action>Pause for human verification. Present the built state and the verification steps below, then wait for the resume signal before completing the plan. Do not auto-approve.</action>
  <what-built>
    The `transcendShards` balance now renders as a gold `◈` chip in both the GachaScreen wallet (every tab, left of the gems/owned readout) and the CharacterScreen topbar (before the close button), read-only from the subscribed player row. The server mints one shard on a C6-overflow dupe (no 800-gem refund) and increments constellation on a sub-C6 dupe. Module is published to local with regenerated bindings.
  </what-built>
  <how-to-verify>
    1. `pnpm build` is green; open the LAN/laragon build (or `pnpm dev`) and log in.
    2. Confirm the `◈` shard chip is visible in the Gacha wallet on ALL tabs (characters / banners / party) and in the Character screen topbar; both are gold, same glyph/size; both show the same number (including `0`).
    3. Take a character to C6, then pull another dupe of that SAME character: the shard counter increases by 1 and the gem balance does NOT jump by 800 (no refund). Verify in-game and, if desired, via `spacetime sql 2d-impact-game-fr9ti --server local "SELECT transcend_shards, gems FROM player WHERE ..."`.
    4. Pull a dupe of a character still BELOW C6: its constellation increments and NO shard is minted.
    5. Confirm the balance updates reactively in both screens without reload (subscription push).
  </how-to-verify>
  <resume-signal>Type "approved" once both chips render correctly and the C6-dupe mints exactly 1 shard with gems unchanged; otherwise describe what you saw.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subscribed player row → React render | Client displays a server-owned value; it must not compute, cache-mutate, or write it back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-06 | Tampering | client-side balance | high | mitigate | The chip renders `myPlayer?.transcendShards ?? 0` straight from the subscribed row; no client computation or mutation (RESEARCH Anti-Pattern: no client-side balance). All authority stays server-side. |
| T-1-03 | Elevation | cross-player display | low | mitigate | `myPlayer` is derived from the caller's own subscribed identity; other players' rows are not rendered as the wallet. |
| T-1-SC | Tampering | package installs | low | accept | Zero external packages installed this phase (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `pnpm build` green with the regenerated bindings.
- Both chips visible and visually identical (UI-SPEC Component Contract).
- Human-verified: C6-dupe mints 1 shard, gems unchanged; sub-C6 dupe increments constellation only.
</verification>

<success_criteria>
- Shard balance visible in Character and Gacha screens (ROADMAP criterion 3).
- Client renders the subscribed value only; no client-side authority over shards.
- Playtest confirms the full mint semantics on local.
</success_criteria>

<output>
Create `.planning/phases/01-constellation-shard-currency/01-04-SUMMARY.md` when done.
</output>
