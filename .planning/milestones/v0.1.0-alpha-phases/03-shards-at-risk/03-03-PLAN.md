---
phase: 03-shards-at-risk
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/module_bindings/
autonomous: true
requirements: [REQ-shard-risk]
must_haves:
  truths:
    - "The module is published to the local server via an ADDITIVE migrate (no -c / --delete-data), so existing player/account/owned_character data survives."
    - "The migration plan shows only the shard_drop table ADD (and the collectShard reducer), never a destructive column/table drop."
    - "Regenerated bindings expose tables.shardDrop and connection.reducers.collectShard to the client."
  artifacts:
    - "Regenerated src/module_bindings/ containing shard_drop table + collectShard reducer bindings."
  key_links:
    - "This publish gates Plan 04 — the client cannot subscribe to tables.shardDrop or call collectShard until bindings are regenerated."
    - "Publish targets --server local only (maincloud is paused; prod push is a separate manual step)."
  prohibitions:
    - "NEVER pass -c or --delete-data — this is an additive migration that must keep local data."
    - "NEVER use the npm spacetime:publish script (its --module-path server is wrong); use the direct command with --module-path spacetimedb."
    - "NEVER run spacetime login between local publishes (ownership 403 gotcha, per CLAUDE.md)."
---

<objective>
Publish the additive schema (new `shard_drop` table + `collectShard` reducer) to the local
SpacetimeDB and regenerate client bindings. This is a BLOCKING gate: Plan 04's client work
depends on `tables.shardDrop` and `reducers.collectShard` existing in `src/module_bindings/`.
Mirror of the Phase 1/2 dedicated publish plans (01-03, 02-03).

Purpose: bring the server changes live and give the client typed bindings without wiping data.
Output: a migrated local database + regenerated `src/module_bindings/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/transcendence/PROGRESS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive migrate-publish to local + regenerate bindings</name>
  <files>src/module_bindings/</files>
  <read_first>
    - CLAUDE.md ("This Project: Environments & Deployment" + "Ownership gotcha" + "Common failure: no such reducer")
    - .planning/transcendence/PROGRESS.md (Workflow per phase — additive publish drops -c)
    - .planning/phases/03-shards-at-risk/03-RESEARCH.md (Pitfall 3 — additive publish backfill, lines 312-319)
  </read_first>
  <action>
    Ensure the local SpacetimeDB is running (`spacetime start` if the ping fails). Publish the
    module ADDITIVELY (no -c / no --delete-data) so existing data is preserved:
    `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes`.
    Confirm the printed Migration Plan shows only the `shard_drop` table being ADDED (and the new
    `collectShard` reducer) — if it shows any destructive drop, STOP and report. Then regenerate
    the client bindings: `pnpm run spacetime:generate` (writes `src/module_bindings/`). Confirm the
    regenerated bindings contain a `shardDrop` table export and a `collectShard` reducer. Do NOT
    run `spacetime login`. Local only — do not push maincloud (it is paused).
    If publish returns 403 Forbidden (ownership gotcha), STOP and surface it (do not clear/wipe) —
    this is a blocking human decision, not an autonomous recovery.
  </action>
  <verify>
    <automated>spacetime sql 2d-impact-game-fr9ti --server local "SELECT * FROM shard_drop LIMIT 1"</automated>
  </verify>
  <acceptance_criteria>
    - source: `src/module_bindings/` contains `shard_drop` table bindings and a `collectShard` reducer after regeneration.
    - behavior: the publish used no -c/--delete-data; the local DB still has its existing player/owned_character rows (spot-check via `spacetime sql ... "SELECT COUNT(*) FROM player"`).
    - test-command: `spacetime sql 2d-impact-game-fr9ti --server local "SELECT * FROM shard_drop LIMIT 1"` succeeds (table exists, empty result is fine).
  </acceptance_criteria>
  <done>Local DB migrated additively (data intact); bindings regenerated with shardDrop + collectShard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI publish → live DB schema | A destructive flag here would wipe accounts/players; additive-only is the control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Denial of Service / data loss | additive publish | high | mitigate | Publish with NO -c/--delete-data; verify the migration plan is an ADD-only; 403 ownership error stops for human review instead of a wipe. |
| T-03-SC | Tampering | npm/pnpm installs | low | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `spacetime sql 2d-impact-game-fr9ti --server local "SELECT * FROM shard_drop LIMIT 1"` succeeds.
- Existing data intact: `spacetime sql ... "SELECT COUNT(*) FROM player"` returns the prior count (non-zero).
- `src/module_bindings/` contains shardDrop + collectShard.
</verification>

<success_criteria>
- Additive migration live on local; no data wiped; client bindings expose the new table + reducer.
</success_criteria>

<output>
Create `.planning/phases/03-shards-at-risk/03-03-SUMMARY.md` when done.
</output>
