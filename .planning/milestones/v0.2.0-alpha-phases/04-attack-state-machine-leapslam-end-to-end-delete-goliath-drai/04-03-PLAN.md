---
phase: 04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - spacetimedb/src/index.ts
  - src/module_bindings/
autonomous: true
requirements: [ATK-05, FSM-01, FSM-06]
must_haves:
  truths:
    - "worldTick calls runUnitAttacks exactly once, between the position-build passes and the playerDamage apply loop (roadmap pass-ordering lock)"
    - "The goliath-to-player per-tick contact drain (pass 4b) is DELETED in this same slice — a goliath's ONLY damage source vs players is a landed strike (ATK-05)"
    - "Camp drain (pass 3) and enemy-vs-goliath drain (pass 4) are byte-for-byte untouched"
    - "The module publishes additively to the populated local DB (no wipe), bindings regenerate, and the client builds green (FSM-06)"
  artifacts:
    - "spacetimedb/src/index.ts (one runUnitAttacks call, hoisted playerDamage, pass 4b removed)"
    - "src/module_bindings/ regenerated with unit_attack + attack_strike types"
  key_links:
    - "playerDamage map declared BEFORE the runUnitAttacks call so strike damage rides the existing resist('contact')/death/spill/respawn apply"
    - "spacetime publish (local, additive) -> pnpm run spacetime:generate -> pnpm build is the mandated deploy chain"
---

<objective>
Flip the switch: wire `runUnitAttacks` into `worldTick` at the mandated slot (after the
position-build passes, before the single `playerDamage` apply), and in the SAME slice delete the
goliath→player contact drain (pass 4b) plus its now-orphaned range constant — the ATK-05
guarantee that dodgeable strikes REPLACE (never coexist with) the undodgeable drain, backed by the
Plan-01 every-band selection sweep. Then run the deploy chain: additive local publish → bindings
generate → client build. Chase AI between attacks stays untouched (D4-13).

Purpose: goliaths now telegraph and slam on the live local DB; the client can be built against the
new bindings by Waves 4-6.
Output: behavior-live server module on local, regenerated src/module_bindings, green build + suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-CONTEXT.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- **Call site** in `worldTick`: `runUnitAttacks(ctx, now, tick, goliaths, goliathPosition, goliathHeading, playerByHex, playerDamage)` — the single new line index.ts gains
- **Hoisted declaration**: `const playerDamage = new Map<string, number>()` moved above the call site (from its current pass-4 position at ~index.ts:3160)
- **Deletion**: worldTick pass 4b block (~index.ts:3180-3190) and the orphaned goliath-to-provoked-player contact-range constant (~index.ts:275)
- **Generated**: `src/module_bindings/` types for `unit_attack` rows and `attack_strike` events (via `pnpm run spacetime:generate`)

<tasks>

<task type="auto">
  <name>Task 1: Wire runUnitAttacks into worldTick and delete the goliath-to-player contact drain</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - spacetimedb/src/index.ts lines 2938-3200 (worldTick: pass 1 goliath movement builds goliathPosition/goliathHeading ~2996-3075; pass 2 enemy movement ~3077-3120; pass 3 camp raid ~3122; pass 4 enemy-vs-goliath ~3156; playerDamage declared ~3160; pass 4b goliath-to-player drain ~3180-3190 — the block to DELETE)
    - spacetimedb/src/index.ts lines 3290-3380 (goliath apply loop persisting the position maps; playerDamage apply loop with resist('contact')/death/spill/respawn — must remain untouched)
    - spacetimedb/src/index.ts line 275 (the goliath-to-provoked-player contact-range constant — dies with pass 4b if unreferenced)
    - spacetimedb/src/unitAttacks.ts (the Plan-02 function being wired — confirm its exact signature)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md (section "index.ts... 5. Contact-drain DELETION" — the verified block excerpt and hoist note)
  </read_first>
  <action>
    Three coordinated edits inside worldTick, per the roadmap pass-ordering lock:

    1. HOIST: move the `const playerDamage = new Map<string, number>()` declaration from its
       current pass-4 position (~3160) up to just after pass 2 completes (next to the other maps),
       so strike damage written by runUnitAttacks accumulates into the same map pass 4 and the
       apply loop already consume. Pass 4's own accumulation keeps working unchanged (it uses
       .set with ?? 0 merge).
    2. CALL: insert the single line
       `runUnitAttacks(ctx, now, tick, goliaths, goliathPosition, goliathHeading, playerByHex, playerDamage);`
       AFTER pass 2 (both position maps fully built) and BEFORE pass 3 — i.e. between the
       position-build passes and every damage consumer, so windup rooting / leap overrides land in
       the maps before the goliath apply loop persists them (D4-12), and strike damage lands before
       the playerDamage apply (FSM-01 host wiring). Import runUnitAttacks from './unitAttacks'.
    3. DELETE (ATK-05): remove the entire pass 4b block (~3180-3190) — the "provoked goliath
       hammers the player" per-tick drain, comment included. Then grep the goliath-to-provoked-
       player contact-range constant defined at ~line 275; it becomes unreferenced with the block
       gone — delete the constant too (no-legacy-code rule). Do NOT touch pass 3 (camp raid) or
       pass 4 (enemy bites goliath-aggroed player? — the enemy-vs-goliath/enemy-vs-player drains):
       ATK-05 scopes deletion to the goliath-to-PLAYER drain only. Chase/aggro AI is untouched
       (D4-13). The no-facetank guarantee replacing the drain is the Plan-01 selection sweep
       (band 0..maxBand, ATK-05).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p spacetimedb && pnpm test && bash -c '[ "$(grep -c GOLIATH_PLAYER_CONTACT_RANGE spacetimedb/src/index.ts)" -eq 0 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "runUnitAttacks(" spacetimedb/src/index.ts` returns 1 (import line adds a second textual match for the bare name; the CALL appears once) and it appears textually before the "Pass 3" comment inside worldTick
    - `grep -c "GOLIATH_PLAYER_CONTACT_RANGE" spacetimedb/src/index.ts` returns 0 (drain block + orphaned constant gone)
    - The pass-3 and pass-4 blocks are unchanged (git diff shows no hunk touching the camp-raid or enemy-bite passes)
    - `const playerDamage` declaration appears textually BEFORE the runUnitAttacks call in worldTick
    - `pnpm exec tsc --noEmit -p spacetimedb` exits 0; `pnpm test` green
  </acceptance_criteria>
  <done>Strikes are the goliath's only player-damage source; the FSM runs every tick in the mandated slot; module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Additive local publish, regenerate bindings, build client, run gates</name>
  <files>src/module_bindings/</files>
  <read_first>
    - CLAUDE.md (section "This Project: Environments & Deployment" — publish command, module path is ./spacetimedb NOT ./server, generate script, pnpm-only)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md (section "Common Pitfalls" 7 — publish-order discipline; "Project Constraints" — never --delete-data)
  </read_first>
  <action>
    Deploy chain, local only (maincloud is Plan 07):
    1. `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes` —
       additive migrate on the POPULATED local DB, no --delete-data flag ever (FSM-06: the new
       tables must be accepted as an additive diff on a migrated DB; Phase-3 verified this path for
       .default() columns and new tables).
    2. `pnpm run spacetime:generate` — regenerate src/module_bindings with the unitAttack and
       attackStrike types.
    3. `pnpm build` — tsc + vite build must pass against the new bindings.
    4. `pnpm test` — full suite stays green.
    5. Probe the migrated DB: `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local`
       must succeed (returns zero rows — the table exists and is empty until a real engage; the
       rows-appear-on-engage check is Plan 07's playtest), and
       `spacetime logs 2d-impact-game-fr9ti --server local -n 50` shows no panic/error lines from
       the new tick pass.
    If the publish is rejected (schema conflict), STOP and report — do not reach for --delete-data
    (the local DB carries real account rows; migrate-only is a locked ops invariant).
  </action>
  <verify>
    <automated>pnpm build && pnpm test && spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local</automated>
  </verify>
  <acceptance_criteria>
    - `spacetime publish ... --server local --yes` output reports a successful update/migrate (no wipe, no delete-data flag used)
    - `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local` exits 0 (table exists on the migrated DB)
    - `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM attack_strike" --server local` exits 0
    - src/module_bindings contains generated types for unit_attack and attack_strike (grep -ril "unit_attack" src/module_bindings returns at least one file)
    - `pnpm build` exits 0; `pnpm test` green; `spacetime logs` shows no error/panic entries after at least 10 seconds of tick activity
  </acceptance_criteria>
  <done>The FSM is live on the local migrated DB, bindings and client build are current, all gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy -> populated local DB | schema migration crosses onto a DB holding real account rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | DoS (data loss) | destructive publish wiping accounts | critical | mitigate | additive migrate only; the plan text forbids the delete-data flag and mandates STOP-and-report on schema rejection |
| T-04-08 | Tampering | drain deletion leaving a zero-damage facetank hole a player exploits to farm goliaths risk-free | medium | mitigate | Plan-01 selection sweep (attack returned for every distance 0..maxBand) is a committed green test before this plan runs (ATK-05) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new dependencies (locked); no install tasks in this plan |
</threat_model>

<verification>
- `pnpm test` and `pnpm build` green
- `grep -rn "Math.random\|Date.now" spacetimedb/src` empty
- Local DB migrated additively; unit_attack/attack_strike queryable via spacetime sql
</verification>

<success_criteria>
- ATK-05: pass 4b drain and its constant are gone; goliath player-damage flows only through strikes; camp + goliath-vs-enemy drains untouched
- FSM-01: runUnitAttacks executes inside worldTick in the mandated pass slot
- FSM-06: additive publish accepted by the populated local DB; new tables queryable; bindings regenerated
</success_criteria>

<output>
Create `.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-03-SUMMARY.md` when done
</output>
