---
phase: 04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - src/game/audio/createAudioSystem.ts
  - src/App.tsx
  - src/game/createGame.ts
autonomous: true
requirements: [ANIM-04, HIT-01]
must_haves:
  truths:
    - "Strike juice fires exactly ONCE per slam: impact flash/burst + small short camera shake + procedural slam SFX, all triggered by the attack_strike onInsert (ANIM-04, D4-15)"
    - "attack_strike is consumed via useTable onInsert ONLY — it must NOT appear in the manual subscribe list (event-table one-subscription rule, Phase-2 double-fire lesson)"
    - "A stunned local player's screen adopts the server knockback: input frozen ~0.3s and position lerped to the server row, so knockback is felt by the victim, not just spectators (HIT-01 client half)"
    - "Slam SFX is a zero-asset, zero-dep WebAudio one-shot with the AudioContext resumed on first user gesture"
  artifacts:
    - "src/game/audio/createAudioSystem.ts"
    - "src/App.tsx (attack_strike useTable onInsert wiring)"
    - "src/game/createGame.ts (handleAttackStrike + camera shake + stun reconcile)"
  key_links:
    - "App.tsx onInsert -> gameRef.current?.handleAttackStrike(strike) -> effect burst + telegraph flashStrike + camera shake + audio.playSlam"
    - "syncMyServerRow detects a raised stunnedUntilMicros -> input freeze + position lerp toward the server row while syncPositionToServer keeps running (server rejects writes while stunned anyway)"
---

<objective>
Land the strike moment and the hit reaction: full one-shot juice on the `attack_strike` event —
impact burst + rim flash + SMALL short camera shake + hand-rolled WebAudio slam SFX (D4-15,
ANIM-04) — and the client half of HIT-01: the victim's screen adopts the server's knockback and
freezes input for the 0.3s micro-stun (D4-10), reconciling to the authoritative player row.

Purpose: the slam must FEEL like weight (juice + knockback) and the reaction must be visible to
the victim, not just bystanders — the last client piece of the end-to-end spine.
Output: new audio module + event wiring + stun reconcile; everything render/feel-side only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-CONTEXT.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md
@.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-PATTERNS.md
@.planning/phases/03-pvp-crit/03-01-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

- **Module `src/game/audio/createAudioSystem.ts`** exporting: interface `AudioSystem` (`playSlam(): void`, `dispose(): void`), factory `createAudioSystem(): AudioSystem`
- **Game handle** `handleAttackStrike(strike: AttackStrike): void` on the createGame interface
- **App.tsx wiring**: `useTable(tables.attackStrike, { onInsert })` — the ONLY subscription for the event table
- **createGame.ts internals**: camera-shake decaying offset in `updateCamera`, stun-reconcile branch around `updateLocalPlayer`

<tasks>

<task type="auto">
  <name>Task 1: createAudioSystem.ts — zero-asset WebAudio slam one-shot</name>
  <files>src/game/audio/createAudioSystem.ts</files>
  <read_first>
    - src/game/systems/createEffectSystem.ts (the createX() -> interface factory convention — the ONLY analog; no audio code exists anywhere in src/, this file is greenfield per PATTERNS "No Analog Found")
  </read_first>
  <action>
    Create src/game/audio/createAudioSystem.ts (~50 LOC budget), zero dependencies, zero asset
    files (D4-15 slam SFX; RESEARCH Open Q2 resolved to the in-scope WebAudio option — SC3 says
    VFX/SFX, no user waiver exists to descope):
    - Lazy-create a single AudioContext on first need. Browsers gate audio on a user gesture:
      register one-time window 'pointerdown' + 'keydown' listeners at factory time that create/
      resume the context, so by the time a slam lands the context is running (the game always has
      input before combat).
    - playSlam(): a procedural thump — a low sine oscillator (~70Hz sweeping down to ~40Hz over
      ~0.25s) through a gain envelope (fast attack, exponential decay to silence ~0.35s) plus a
      short filtered white-noise burst (~0.12s, lowpass ~400Hz) for the impact texture. All nodes
      created per call, connected to destination, stopped and garbage-collectable after the
      envelope ends. Guard: no-op when the context is absent/suspended (never throw during a frame).
    - dispose(): close the context and remove the gesture listeners.
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `grep -c "AudioContext" src/game/audio/createAudioSystem.ts` returns >= 1 and `grep -rc "new Audio(" src/game/audio/createAudioSystem.ts` returns 0 (WebAudio synthesis, no asset-backed Audio elements)
    - No new entry appears in package.json dependencies (zero-new-deps lock — git diff on package.json is empty)
    - `grep -c "playSlam" src/game/audio/createAudioSystem.ts` returns >= 1
  </acceptance_criteria>
  <done>A self-contained, asset-free slam sound exists behind a factory interface, gesture-safe.</done>
</task>

<task type="auto">
  <name>Task 2: attack_strike wiring — onInsert-only subscription, handleAttackStrike juice, camera shake</name>
  <files>src/App.tsx, src/game/createGame.ts</files>
  <read_first>
    - src/App.tsx lines 225-245 (the enemyHit useTable-with-onInsert block INCLUDING its one-subscription warning comment — attack_strike copies this pattern and the comment verbatim-in-spirit; confirm attack_strike is NOT added to the manual subscribe list)
    - src/game/createGame.ts (game handle interface ~120-150 for the handleAttackStrike declaration; updateCamera at ~836-840 — the desiredPosition line the shake offset joins; effectSystem availability + spawnBurst signature via createEffectSystem.ts ~90-121)
    - src/game/systems/createTelegraphSystem.ts (flashStrike(unitKey) API from Plan 04)
    - src/game/audio/createAudioSystem.ts (Task 1 API)
    - src/module_bindings/ (AttackStrike event row type + tables.attackStrike accessor)
  </read_first>
  <action>
    App.tsx: add `useTable(tables.attackStrike, { onInsert: strike => gameRef.current?.handleAttackStrike(strike) })`
    with a copy of the enemyHit one-subscription comment adapted to attack_strike (ANIM-04; the
    event table must NOT be listed in the manual .subscribe([...]) — double subscription = double
    shake/SFX per slam, the Phase-2 lesson).

    createGame.ts:
    1. Instantiate `const audioSystem = createAudioSystem()` beside the other systems; dispose with
       them.
    2. Implement handleAttackStrike(strike) (D4-15 full juice, each fired ONCE per event):
       - effectSystem.spawnBurst at (landingX, groundY, landingZ) with 0x86e2ff (impact flash +
         dust/shard burst reusing the existing burst helper);
       - telegraphSystem.flashStrike(`${strike.unitKind}:${strike.unitId}`) (rim flash);
       - trigger camera shake: set a shakeMagnitude closure var to ~0.2 (world units);
       - audioSystem.playSlam().
    3. Camera shake (SMALL and SHORT per D4-15 — taste-tunable constants at top of the block):
       in updateCamera, when shakeMagnitude > 0 add a random-per-frame offset vector scaled by
       shakeMagnitude to desiredPosition BEFORE the existing lerp, and decay shakeMagnitude
       exponentially to 0 over ~0.25s. Client-side cosmetic randomness here is fine (render only —
       the determinism rule binds spacetimedb/src, not the renderer).
  </action>
  <verify>
    <automated>pnpm build && pnpm test && bash -c '[ "$(grep -c "tables.attackStrike" src/App.tsx)" -eq 1 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tables.attackStrike" src/App.tsx` returns exactly 1 (the useTable hook — proof it is absent from the manual subscribe list)
    - `grep -c "handleAttackStrike" src/game/createGame.ts` returns >= 2 (interface + implementation)
    - `grep -c "playSlam\|flashStrike\|spawnBurst" src/game/createGame.ts` returns >= 3 within/around handleAttackStrike (all four juice components wired; shake is the fourth via the magnitude var)
    - `pnpm build` exits 0; `pnpm test` green
  </acceptance_criteria>
  <done>One slam event produces exactly one flash + burst + shake + thump on every subscribed client.</done>
</task>

<task type="auto">
  <name>Task 3: Stun reconcile — victim adopts server knockback and freezes input for the micro-stun</name>
  <files>src/game/createGame.ts</files>
  <read_first>
    - src/game/createGame.ts (updateLocalPlayer at ~635-680 — the input-driven movement to gate; syncPositionToServer at ~682-695 — keeps running unmodified; syncMyServerRow handle — where the authoritative self row arrives; VOID_KILL_DEPTH/fall-death at ~663-666 — already covers knocked-off-bridge deaths, D4-11 needs no new path)
    - .planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-RESEARCH.md (section "Pattern 5" part 3 + "Client stun reconcile" code sketch — the three-part HIT-01 design; parts 1-2 landed in Plan 02)
    - .planning/phases/03-pvp-crit/03-01-SUMMARY.md (how syncMyServerRow / self-row handling was wired in Phase 3 — extend, do not duplicate)
  </read_first>
  <action>
    Client half of HIT-01 (server displacement + updatePosition rejection landed in Plan 02):
    1. Track the last-seen stunnedUntilMicros from the self row in syncMyServerRow. When a new row
       arrives with stunnedUntilMicros GREATER than the previous value, start a local stun window:
       stunActiveUntilPerfMs = performance.now() + STUN_RENDER_MS (300 — matches the D4-10
       stunTicks * tick duration; a named constant, comment pointing at ATTACKS.leapSlam.stunTicks).
    2. While the window is active: suppress movement input inside updateLocalPlayer (skip
       input-driven displacement; gravity/ground physics keep running) and lerp playerPosition.x/z
       toward the server row's positionX/Z (factor ~min(1, delta * 12)) so the knockback
       displacement the server wrote becomes what the victim sees. Keep playerPosition.y locally
       owned (void-death physics unchanged).
    3. Do NOT touch syncPositionToServer — it keeps firing; the server rejects position writes
       while stunned (Plan 02 guard), and when the stun expires local ≈ server so movement resumes
       seamlessly. No new death handling: a knockback past an edge falls and dies via the existing
       VOID_KILL_DEPTH path (D4-11: edge deaths are a feature).
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0; `pnpm test` green
    - `grep -c "stunnedUntilMicros" src/game/createGame.ts` returns >= 1 (self-row consumption)
    - Source assertion: updateLocalPlayer contains a stun-window guard that bypasses input-driven movement while active
    - Source assertion: syncPositionToServer body is unchanged (git diff shows no hunk inside it)
  </acceptance_criteria>
  <done>A slammed player is visibly thrown and briefly locked on their own screen, in agreement with the server's authoritative position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server event/rows -> client render | read-only; the stun ENFORCEMENT lives server-side (Plan 02), this plan only renders it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Tampering | modified client skips the input freeze / ignores knockback | high | mitigate | freeze is cosmetic by design — the real control is the Plan-02 updatePosition rejection while now < stunnedUntilMicros; a modified client's writes are dropped server-side |
| T-04-13 | DoS | double-subscribed event table doubling shake/SFX per slam | medium | mitigate | acceptance criterion pins tables.attackStrike to exactly ONE occurrence in App.tsx (useTable only) |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero new dependencies (locked); audio is hand-rolled WebAudio, no packages |
</threat_model>

<verification>
- `pnpm build` + `pnpm test` green
- attack_strike single-subscription grep gate passes
- package.json diff empty (zero-new-deps)
</verification>

<success_criteria>
- ANIM-04/D4-15: flash + burst + small short shake + slam SFX fire exactly once per attack_strike insert
- HIT-01 (client half): victim's screen adopts server knockback + 0.3s input freeze; server remains the authority
</success_criteria>

<output>
Create `.planning/phases/04-attack-state-machine-leapslam-end-to-end-delete-goliath-drai/04-06-SUMMARY.md` when done
</output>
