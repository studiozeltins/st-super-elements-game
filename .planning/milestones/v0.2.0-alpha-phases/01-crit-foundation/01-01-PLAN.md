---
phase: 01-crit-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/crit.ts
  - spacetimedb/src/damage.ts
  - src/game/data/__tests__/crit.test.ts
  - src/game/data/__tests__/damage.test.ts
autonomous: true
requirements: [CRIT-01, CRIT-03]
must_haves:
  truths:
    - "rollCrit(critRate, critDmg, rng) returns { isCrit, multiplier } where multiplier === critDmg on a crit and 1 otherwise, with the crit decision being rng() < critRate"
    - "computeBaseDamage reproduces the client base-damage math for both a basic attack (weapon.damage) and a skill (skillDamage), applying the combo ramp and transcend/constellation ramp"
    - "crit.ts and damage.ts contain zero import lines, no ctx, no Math.random, and no Date.now (determinism discipline, D-08)"
  artifacts:
    - spacetimedb/src/crit.ts
    - spacetimedb/src/damage.ts
    - src/game/data/__tests__/crit.test.ts
    - src/game/data/__tests__/damage.test.ts
  key_links:
    - "damage.ts's mirrored WEAPONS + multiplier functions must equal the client originals (parity locked by plan 03)"
    - "rollCrit's rng parameter is the seam ctx.random slots into during Phase 2 wiring"
---

<objective>
Build the two zero-import, vitest-tested pure helpers that are the server-side foundation of the
crit trilogy: `spacetimedb/src/crit.ts` (`rollCrit`) and `spacetimedb/src/damage.ts`
(`computeBaseDamage` + a verbatim mirror of `WEAPONS`, `regularAttackMultiplier`,
`skillAttackMultiplier`, `transcendDamageMultiplier`). Both are wired to NOTHING this phase — they
are built and pinned by tests only. This is the Phase-1 portion of D-05 (mirror the base-damage
math) plus the D-08 injected-rng helper.

Purpose: Phase 2 will call `computeBaseDamage(...)` and `rollCrit(stat.critRate, stat.critDmg,
ctx.random)` from `attackEnemies`/`attackRay`; Phase 1 lands them as tested pure code before any
reducer is touched (no-monolith: this logic never enters the 3286-LOC `index.ts`).
Output: two new zero-import server siblings + two cross-boundary vitest files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crit-foundation/01-RESEARCH.md
@.planning/phases/01-crit-foundation/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces (Phase-1 slice):
- `spacetimedb/src/crit.ts` — `export function rollCrit(critRate, critDmg, rng): { isCrit, multiplier }`
- `spacetimedb/src/damage.ts` — `export const WEAPONS`, `WeaponId`, `WeaponDefinition`,
  `regularAttackMultiplier`, `skillAttackMultiplier`, `transcendDamageMultiplier`,
  `computeBaseDamage`, and the inlined server copies of `CONSTELLATION_DAMAGE_STEP` (0.08) and
  `TRANSCEND_DAMAGE_STEP` (0.05)
- `src/game/data/__tests__/crit.test.ts`, `src/game/data/__tests__/damage.test.ts`
Sibling plans: 01-02 (crit stat data), 01-03 (parity tests).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: crit.ts pure helper + crit.test.ts (test-first)</name>
  <files>spacetimedb/src/crit.ts, src/game/data/__tests__/crit.test.ts</files>
  <read_first>
    - spacetimedb/src/deathPenalty.ts:1-33 (zero-import header discipline + pure-function shape — the exact template)
    - src/game/data/__tests__/deathPenalty.test.ts:1-30 (cross-boundary import + branch-per-case assertion style)
    - .planning/phases/01-crit-foundation/01-PATTERNS.md §"spacetimedb/src/crit.ts" and §"Pattern 3 RNG injection"
    - spacetimedb/src/rng.ts (confirms an rng thunk is `() => number` in [0,1), matching ctx.random's call shape)
  </read_first>
  <behavior>
    - Test (RED first): rng stub `() => 0.1` with critRate 0.34 → { isCrit: true, multiplier: 1.9 } (pass critDmg 1.9)
    - Test: rng stub `() => 0.99` with critRate 0.34 → { isCrit: false, multiplier: 1 }
    - Test boundary: rng stub returning exactly critRate (`() => 0.34`, critRate 0.34) → isCrit false (strict `<`, not `<=`)
    - Test: critDmg is echoed verbatim as multiplier on a crit (e.g. critDmg 2.1 → multiplier 2.1), proving critDmg is stored as a FULL multiplier
  </behavior>
  <action>
    Write crit.test.ts FIRST (RED), importing the real helper across the boundary with
    `import { rollCrit } from '../../../../spacetimedb/src/crit';` (mirror deathPenalty.test.ts's
    import line). Assert the four behaviors above with deterministic rng stubs. Run the test, confirm
    it fails to resolve the module (RED). Then create crit.ts: a zero-import file (NO `import` line, no
    ctx, no Math.random, no Date.now) with a header comment stating WHY it is dependency-free
    (vitest-testable, preserves reducer determinism per CLAUDE.md rule 2) mirroring deathPenalty.ts's
    header. Export `rollCrit(critRate: number, critDmg: number, rng: () => number)` returning
    `{ isCrit: boolean; multiplier: number }` where `isCrit = rng() < critRate` and
    `multiplier = isCrit ? critDmg : 1`. Use a guard/early-return style; no nested conditionals.
    critDmg is the FULL multiplier convention (returns critDmg directly, not 1 + critDmg) — this is
    load-bearing and matches the value it will replace (the client x1.9). This closes CRIT-01's
    server-side roll primitive and honors D-08 (rng injected, never Math.random).
  </action>
  <verify>
    <automated>pnpm test -- crit</automated>
  </verify>
  <acceptance_criteria>
    - crit.test.ts's four cases pass (GREEN).
    - `grep -nE "import |Math\.random|Date\.now|ctx" spacetimedb/src/crit.ts` prints nothing.
    - crit.ts is under 40 LOC.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: damage.ts (WEAPONS + multipliers + computeBaseDamage) + damage.test.ts (test-first)</name>
  <files>spacetimedb/src/damage.ts, src/game/data/__tests__/damage.test.ts</files>
  <read_first>
    - src/game/data/weapons.ts:1-59 (WEAPONS record to mirror VERBATIM — id/displayName/damage/cooldownSeconds/range/isRanged/projectileSpeed for all 5 weapons)
    - src/game/combat/comboSystem.ts:19-32 (regularAttackMultiplier + skillAttackMultiplier to port verbatim, including the REGULAR_RAMP_PER_COMBO 0.02 / REGULAR_RAMP_CAP 0.75 / SKILL_RAMP_PER_COMBO 0.12 constants)
    - src/game/combat/transcendScaling.ts:1-12 (transcendDamageMultiplier — NOTE it imports CONSTELLATION_DAMAGE_STEP and TRANSCEND_DAMAGE_STEP; these MUST be inlined to stay zero-import)
    - src/game/data/constellations.ts:6 (CONSTELLATION_DAMAGE_STEP = 0.08) and src/game/data/constants.ts:9 (TRANSCEND_DAMAGE_STEP = 0.05) — the two values to inline
    - spacetimedb/src/combatMath.ts (zero-import math sibling exemplar for file shape)
    - .planning/phases/01-crit-foundation/01-PATTERNS.md §"spacetimedb/src/damage.ts" (port targets 1-4)
  </read_first>
  <behavior>
    - Test: WEAPONS deep-equals the client weapons record (import client WEAPONS, expect toEqual) — every field including displayName strings
    - Test: regularAttackMultiplier(0) === 1; caps at 1.75 for large combo (e.g. combo 100 → 1.75, not 3.0); server fn === client fn across sampled combos
    - Test: skillAttackMultiplier(0) === 1; skillAttackMultiplier(10) === 1 + 10*0.12; server fn === client fn across sampled combos
    - Test: transcendDamageMultiplier(0,0) === 1; transcendDamageMultiplier(1,0) === 1.08; transcendDamageMultiplier(0,1) === 1.05; matches client fn
    - Test computeBaseDamage basic: `{ weaponId:'sword', skillDamage:null, combo:0, constellation:0, transcend:0 }` === 60 (sword damage)
    - Test computeBaseDamage skill: `{ weaponId:'sword', skillDamage:220, combo:0, constellation:0, transcend:0 }` === 220 (skill.damage overrides weapon, uses skill ramp)
    - Test computeBaseDamage ramps: a non-zero combo applies the SKILL ramp when skillDamage != null and the REGULAR ramp when skillDamage === null; a non-zero constellation/transcend scales both
  </behavior>
  <action>
    Write damage.test.ts FIRST (RED), importing the real exports across the boundary with
    `import { computeBaseDamage, WEAPONS, regularAttackMultiplier, skillAttackMultiplier,
    transcendDamageMultiplier } from '../../../../spacetimedb/src/damage';` and the client originals
    (`../weapons`, `../../combat/comboSystem`, `../../combat/transcendScaling`) to compare against.
    Assert the behaviors above. Run it (RED). Then create damage.ts as a zero-import file with a
    header comment stating WHY dependency-free (same discipline as combatMath.ts). Port, VERBATIM:
    (1) the `WeaponId` union, `WeaponDefinition` interface, and full `WEAPONS` record from weapons.ts
    (values must match exactly — sword 60, greatsword 110, spear 70, bow 55 ranged, book 65 ranged),
    exported `as const`-style typed record; (2) `regularAttackMultiplier` and `skillAttackMultiplier`
    with their private ramp constants from comboSystem.ts; (3) `transcendDamageMultiplier` from
    transcendScaling.ts, INLINING `const CONSTELLATION_DAMAGE_STEP = 0.08` and
    `const TRANSCEND_DAMAGE_STEP = 0.05` as local consts (do NOT import them — that would break the
    zero-import rule; add a one-line comment noting each mirrors its client source so plan 03 can pin
    it). Then add `computeBaseDamage(input: { weaponId: keyof typeof WEAPONS; skillDamage: number |
    null; combo: number; constellation: number; transcend: number }): number` = `base * ramp *
    transcendDamageMultiplier(constellation, transcend)` where `base = skillDamage ??
    WEAPONS[weaponId].damage` and `ramp = skillDamage != null ? skillAttackMultiplier(combo) :
    regularAttackMultiplier(combo)`. All inputs are plain args (no ctx, no table reads) so it stays a
    pure importable sibling. This lands the Phase-1 portion of D-05 and D-07 (skill base damage). Do
    NOT put any of this in index.ts (no-monolith).
  </action>
  <verify>
    <automated>pnpm test -- damage</automated>
  </verify>
  <acceptance_criteria>
    - damage.test.ts passes (GREEN), including the WEAPONS deep-equal and the server-fn-vs-client-fn comparisons.
    - `grep -nE "import |Math\.random|Date\.now|ctx" spacetimedb/src/damage.ts` prints nothing except intra-file references (no `import` statements at all).
    - damage.ts is under 120 LOC functional and lives as a sibling of index.ts (index.ts is NOT modified by this plan).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 1 crosses NO new trust boundary. `crit.ts` and `damage.ts` are pure, un-imported modules; no
reducer, network input, or table is added or touched. The only existing boundary
(client → reducer) is unchanged this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Client-authored damage/crit (existing client roll, UNCHANGED this phase) | high | transfer | No new exposure in Phase 1. This plan builds+tests the server-side primitives (`computeBaseDamage`, `rollCrit`) that Phase 2 wires into `attackEnemies`/`attackRay` to close the spoof. No runtime change here. |
| T-01-02 | Tampering | Non-deterministic module logic in `spacetimedb/src/crit.ts` / `damage.ts` | medium | mitigate | Helpers are zero-import with rng injected as a parameter; no `Math.random`/`Date.now`/`ctx`. Enforced by design and the grep acceptance-check on each task. Preserves SpacetimeDB reducer determinism (CLAUDE.md rule 2). |
| T-01-SC | Tampering | npm/pnpm package installs | low | accept | Phase 1 installs no external packages (vitest already present); no supply-chain surface added. |
</threat_model>

<verification>
- `pnpm test -- crit` and `pnpm test -- damage` both green.
- `pnpm test` full suite green (no regression to existing serverSync/characters/deathPenalty tests).
- `grep -rnE "import |Math\.random|Date\.now" spacetimedb/src/crit.ts spacetimedb/src/damage.ts` returns no `import`/`Math.random`/`Date.now`.
- No file under `spacetimedb/` gained a `*.test.ts` (tests live client-side).
- `spacetimedb/src/index.ts` is unchanged by this plan.
</verification>

<success_criteria>
- ROADMAP SC3 met for the helper half: zero-import vitest helpers `crit.ts` (`rollCrit`) and
  `damage.ts` (`computeBaseDamage`) exist and pass.
- ROADMAP SC4 held: no runtime behavior change; no `Math.random`/`Date.now` introduced into
  `spacetimedb/src`.
- D-05 (Phase-1 portion) + D-07 satisfied: `WEAPONS` + the three multipliers + skill base damage are
  mirrored as tested pure code, ready for plan 03's import-and-compare parity.
</success_criteria>

<output>
Create `.planning/phases/01-crit-foundation/01-01-SUMMARY.md` when done.
</output>
