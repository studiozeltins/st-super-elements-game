---
phase: 01-crit-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/game/data/__tests__/serverSync.test.ts
autonomous: true
requirements: [CRIT-03]
must_haves:
  truths:
    - "serverSync.test.ts asserts per-character critRate/critDmg parity between client CHARACTERS and server CHARACTER_STATS for all 17 ids (INV-5)"
    - "serverSync.test.ts asserts damage.ts's WEAPONS deep-equals the client WEAPONS, and its multiplier functions equal the client functions across sampled inputs"
    - "serverSync.test.ts pins the inlined CONSTELLATION_DAMAGE_STEP (0.08) via a transcendDamageMultiplier output comparison at constellation=1, transcend=0"
  artifacts:
    - src/game/data/__tests__/serverSync.test.ts
  key_links:
    - "import damage.ts across the package boundary and compare outputs (not regex) — the robust parity path (RESEARCH Pitfall 4)"
    - "crit parity uses the existing readField/extractServerStats regex path, which already captures decimals"
---

<objective>
Extend `serverSync.test.ts` to make client/server drift a build failure for everything Phase 1 added:
(1) per-character crit parity (client CHARACTERS ↔ server CHARACTER_STATS), and (2) the mirrored
base-damage math parity (client `WEAPONS`/`comboSystem`/`transcendScaling` ↔ server `damage.ts`). This
is the test half of CRIT-03 (Success Criterion 2: crit parity AND weapon/multiplier parity). It is the
single owner of serverSync.test.ts this phase, so it runs after the data (01-02) and the helpers
(01-01) exist.

Purpose: INV-5 parity is release-blocking (silent stat drift breaks fairness downstream). This locks
both the crit values and the mirrored damage constants so Phase 2 cannot wire a drifted mirror.
Output: extended serverSync.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crit-foundation/01-RESEARCH.md
@.planning/phases/01-crit-foundation/01-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- Crit-parity assertions + weapon/multiplier import-and-compare assertions in serverSync.test.ts.
Depends on: 01-01 (`spacetimedb/src/damage.ts` exports) and 01-02 (crit fields on client + server).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Crit-value parity block (regex path)</name>
  <files>src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - src/game/data/__tests__/serverSync.test.ts:31-68 (ServerStat interface + readField + extractServerStats — the regex path to extend)
    - src/game/data/__tests__/serverSync.test.ts:107-119 (the role-parity it.each block to mirror)
    - .planning/phases/01-crit-foundation/01-PATTERNS.md §"serverSync.test.ts (MODIFY)" (Crit parity)
    - spacetimedb/src/index.ts:43-65 (confirm crit fields are single-line/flat after plan 01-02)
  </read_first>
  <action>
    Extend the `ServerStat` interface with `critRate: number` and `critDmg: number`. In
    `extractServerStats`, populate them via the existing `readField` (its regex
    `${name}:\s*'?([\w.]+)'?` already captures decimals like `0.34`) —
    `critRate: Number(readField(body, 'critRate'))`, `critDmg: Number(readField(body, 'critDmg'))`.
    Add an `it.each` over `Object.values(CHARACTERS)` (mirroring the existing role-match test) asserting
    `serverStats[id].critRate === CHARACTERS[id].critRate` AND `serverStats[id].critDmg ===
    CHARACTERS[id].critDmg` for all 17 ids. This locks CRIT-03's crit parity (INV-5).
  </action>
  <verify>
    <automated>pnpm test -- serverSync</automated>
  </verify>
  <acceptance_criteria>
    - New crit-parity it.each passes for all 17 characters; a deliberate one-character mismatch would fail it (verified by reasoning against the regex path — no need to commit a mismatch).
    - No NaN read for any crit field (proves the entries stayed single-line/flat).
    - Existing serverSync blocks unaffected.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Weapon + multiplier + CONSTELLATION_DAMAGE_STEP parity (import-and-compare path)</name>
  <files>src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - src/game/data/__tests__/serverSync.test.ts:1-26 (import block to extend) and :213-229 (the regularEnemyBaseGems rebuild-and-compare exemplar to mirror)
    - src/game/data/weapons.ts (client WEAPONS) ; src/game/combat/comboSystem.ts (client multipliers) ; src/game/combat/transcendScaling.ts (client transcendDamageMultiplier)
    - spacetimedb/src/damage.ts (the server exports produced by plan 01-01)
    - .planning/phases/01-crit-foundation/01-PATTERNS.md §"Weapon + multiplier parity" ; 01-RESEARCH.md Pitfall 4
  </read_first>
  <action>
    Add imports: client `WEAPONS as CLIENT_WEAPONS` from `../weapons`; `regularAttackMultiplier as
    clientRegular`, `skillAttackMultiplier as clientSkill` from `../../combat/comboSystem`;
    `transcendDamageMultiplier as clientTranscend` from `../../combat/transcendScaling`; and the server
    side `WEAPONS as SERVER_WEAPONS, regularAttackMultiplier as serverRegular, skillAttackMultiplier as
    serverSkill, transcendDamageMultiplier as serverTranscend` from
    `../../../../spacetimedb/src/damage`. Add a `describe('server damage.ts mirror stays in sync')`
    with: (1) `expect(SERVER_WEAPONS).toEqual(CLIENT_WEAPONS)` (deep-equal, all 5 weapons incl.
    displayName); (2) an `it.each` over sampled combos (e.g. 0,1,5,10,50,100) asserting `serverRegular
    === clientRegular` and `serverSkill === clientSkill`; (3) an `it.each` over sampled
    (constellation,transcend) pairs (e.g. [0,0],[1,0],[0,1],[6,4]) asserting `serverTranscend ===
    clientTranscend`; (4) an explicit assertion that `serverTranscend(1,0) === clientTranscend(1,0)`
    and equals `1.08`, which pins the inlined server `CONSTELLATION_DAMAGE_STEP` (0.08) — it has NO
    index.ts regex path (unlike TRANSCEND_DAMAGE_STEP, already at line 152), so this output comparison
    is its only parity guard. This locks CRIT-03's weapon/multiplier parity (D-05).
  </action>
  <verify>
    <automated>pnpm test -- serverSync</automated>
  </verify>
  <acceptance_criteria>
    - WEAPONS deep-equal passes; every sampled multiplier output matches client === server.
    - The transcend(1,0) === 1.08 assertion passes (pins CONSTELLATION_DAMAGE_STEP).
    - `pnpm test` full suite green (all of Phase 1's tests pass together).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan adds test assertions only. Its security value is preventive: it makes
the client↔server mirror (INV-5) a CI gate so a future phase cannot wire a drifted server stat/damage
mirror (which would silently break the server-authoritative damage/crit that Phases 2-3 rely on).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Silent client/server drift of crit stats and mirrored damage math (INV-5) | medium | mitigate | This plan IS the mitigation: crit parity (regex path) + weapon/multiplier/CONSTELLATION_DAMAGE_STEP parity (import-and-compare) become release-blocking assertions. |
| T-01-01 | Tampering | Client-authored damage/crit (existing, UNCHANGED) | high | transfer | Runtime closure lands Phase 2; this plan guards the mirror it depends on. No new exposure in Phase 1. |
| T-01-SC | Tampering | npm/pnpm package installs | low | accept | No packages installed this phase. |
</threat_model>

<verification>
- `pnpm test -- serverSync` green with the new crit + weapon/multiplier parity blocks.
- `pnpm test` full suite green.
- Parity covers all 17 crit stat pairs, all 5 weapons, both multiplier functions, and CONSTELLATION_DAMAGE_STEP.
</verification>

<success_criteria>
- ROADMAP SC2 fully met: crit-value parity AND weapon-damage/multiplier parity are asserted in serverSync.test.ts (INV-5).
- CRIT-03 complete across plans 01-02 (mirror) + 01-03 (parity assertion).
</success_criteria>

<output>
Create `.planning/phases/01-crit-foundation/01-03-SUMMARY.md` when done.
</output>
