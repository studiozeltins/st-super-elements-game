---
phase: 06-shielddash-lane
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/createGoliathRenderer.ts
  - src/game/systems/goliathAttackClips.ts
  - src/game/audio/createAudioSystem.ts
  - src/game/createGame.ts
autonomous: true
requirements: [ATK-04]
tags: [client, three, animation, webaudio, juice]

must_haves:
  truths:
    - "animateDash renders windup as a rooted shield-braced crouch leaning into the aim, strike/early-recovery as a rapid GROUNDED lunge riding travelFraction toward the locked landing, and late recovery as a heavy skid/settle (D6-12)"
    - "animateDash never writes the model group's vertical position — the charge is grounded (the one leap-vs-dash difference, D6-12)"
    - "Every transform animateDash writes is cleared by the existing resetAttackPose/applyCrouch neutral-restore contract (05-02) — movement and death animations restore neutral with zero new restore code"
    - "A shieldDash strike plays a metallic clang SFX and a dust wake along the lane, gated by the existing distance falloff, with NO circular shockwave (a circular wave misreads the lane shape)"
  artifacts:
    - path: "src/game/systems/createGoliathRenderer.ts"
      provides: "animateDash clip + shieldDash dispatch line (possibly delegating to a goliathAttackClips.ts carve)"
      contains: "animateDash"
    - path: "src/game/audio/createAudioSystem.ts"
      provides: "playDash procedural WebAudio shield clang"
      exports: ["playDash"]
    - path: "src/game/createGame.ts"
      provides: "shieldDash juice tier in handleAttackStrike (burst + wake + flash + light shake + playDash)"
      contains: "shieldDash"
  key_links:
    - from: "src/game/systems/createGoliathRenderer.ts"
      to: "src/game/systems/createEntityRenderer.ts"
      via: "travelFraction measures actual mesh lerp toward the landing; lane lengths <= 7.95 stay under the strict snap ceiling so the charge animates instead of teleporting"
      pattern: "travelFraction"
    - from: "src/game/createGame.ts"
      to: "src/game/systems/createAttackViewClock.ts"
      via: "dash juice tier reads the dash row's cast point via getAttackRows() for the wake origin (cast is not on the attack_strike event), with a burst-only fallback"
      pattern: "getAttackRows"
---

<objective>
Build the Phase-6 client feel layer: the `animateDash` procedural clip (D6-12 — shield-braced
crouch, grounded lunge riding `travelFraction`, skid/settle), the `playDash` procedural
WebAudio clang, and the shieldDash strike-juice tier (burst + dust wake along the lane, no
shockwave). All render-only; parallel-safe with 06-01 (zero file overlap).

Purpose: SC2 says the body COMMITS down the charge path — the server relocates at strike
(06-01/06-03), and THIS plan is what makes that relocation read as a charge instead of a
position glitch. Landing it in wave 1 keeps wave 2 thin.

Note: the dash tier's juiceColor arrives via the existing generic ATTACK_RENDER lookup — the
mirror entry lands in 06-01; until the wave merges, the lookup's existing fallback color is
acceptable (code compiles and runs standalone).

Output: extended `createGoliathRenderer.ts` (± `goliathAttackClips.ts` carve),
`createAudioSystem.ts`, `createGame.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-shielddash-lane/06-CONTEXT.md
@.planning/phases/06-shielddash-lane/06-RESEARCH.md
@.planning/phases/06-shielddash-lane/06-PATTERNS.md
@src/game/systems/createGoliathRenderer.ts
@src/game/audio/createAudioSystem.ts
@src/game/createGame.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 6 (this plan lands the starred ones):

- `closestPointOnSegment` / `resolveLane` / `computeLaneEnd` — `spacetimedb/src/attackHitbox.ts` (plan 06-01)
- `AttackSpec.laneLengthBySize?` + `ATTACKS.shieldDash` + leapSlam maxBand 5.5 retune + UNIT_ATTACKS append — `spacetimedb/src/attacks.ts` (plan 06-01)
- Generalized relocate gate `spec.move !== 'none'` — `spacetimedb/src/unitAttackFsm.ts` (plan 06-01)
- `ATTACK_RENDER.shieldDash` + shape union widened to `'lane'` — `src/game/data/attacks.ts` (plan 06-01)
- ★ `animateDash(view)` clip + `shieldDash` dispatch line — `src/game/systems/createGoliathRenderer.ts` (± ★ `goliathAttackClips.ts` carve-out module if the LOC ceiling trips)
- ★ `playDash(gainFactor?)` — `src/game/audio/createAudioSystem.ts` (interface + implementation + return object)
- ★ shieldDash juice tier in `handleAttackStrike` + its handler-local seed constants (e.g. DASH_BURST_PARTICLES, DASH_SHAKE_MAGNITUDE) — `src/game/createGame.ts`
- Lane-end computation + lane resolveStrike arm — `spacetimedb/src/unitAttacks.ts` (plan 06-03)
- Lane rectangle telegraph branch + `fillAxis` scale strategy — `src/game/systems/createTelegraphSystem.ts` (plan 06-04)

<tasks>

<task type="auto">
  <name>Task 1: animateDash grounded-lunge clip + dispatch (D6-12, D4-12 rooted windup)</name>
  <files>src/game/systems/createGoliathRenderer.ts, src/game/systems/goliathAttackClips.ts</files>
  <read_first>
    - src/game/systems/createGoliathRenderer.ts (FULL — animateLeapSlam structure at ~199-222 as the template, travelFraction at ~99-107, applyCrouch/resetAttackPose/setArmFlare helpers at ~109-132, SWING_LEAN lean precedent at ~154, seed-constant block at ~32-59, animateAttack dispatch at ~246-252, the neutral-restore calls in animateMovement/animateDeath at ~229-244)
    - src/game/systems/createEntityRenderer.ts lines 88-105 and ~250-260 (SNAP_DISTANCE 8 with strict-greater comparison + LERP_RATE 10 — read-only context for WHY the clip must ride travel through strike AND early recovery; do NOT modify this file)
    - .planning/phases/06-shielddash-lane/06-PATTERNS.md section "src/game/systems/createGoliathRenderer.ts" (clip-transform contract) and "Conditional Carve-Outs"
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md "Pattern 5" (lerp arithmetic: ~1.8u of an 8u gap remains after the 150ms strike phase — travel spills into recovery by design)
  </read_first>
  <action>
    Add an animateDash clip function mirroring animateLeapSlam's phase-switch structure, with
    these phase behaviors (named seed constants at the top of the file in the existing
    documented style, each citing D6-12): WINDUP — rooted (D4-12): apply a braced crouch that
    eases in with phaseProgress squared (the leapSlam precedent) plus a forward body lean into
    the aim via the body-rotation channel the SWING_LEAN precedent uses, and a raised
    shield-arm pose via the arm rotation channels setArmFlare demonstrates; STRIKE — release
    the crouch quickly as travel begins and hold a deep forward lunge lean scaled by
    travelFraction(view); write NOTHING to the model group's vertical position (grounded —
    the single deliberate difference from the leap clip); RECOVERY — keep the lunge lean
    until travelFraction reaches the existing LANDED_TRAVEL_FRACTION arrival gate (~0.97),
    then ease into a heavy skid/settle using the same eased-settle shape as the slam's
    recovery (crouch squash decaying with eased phaseProgress). Reuse travelFraction as-is —
    do not duplicate it. Every transform written MUST be one that resetAttackPose clears
    (body rotation.x/y, arm rotation.x/z) or applyCrouch owns — if a new channel is needed,
    extend resetAttackPose in this same task (the 05-02 shared neutral-restore contract);
    prefer staying within the existing channels. Add the dispatch line in animateAttack:
    when view.attackId is 'shieldDash', return animateDash(view), placed with the existing
    swordSwing/swordSwirl dispatch lines. LOC rule: count functional LOC after the change;
    if createGoliathRenderer.ts crosses 300 functional lines, carve ALL four animate* clip
    functions plus their seed constants into src/game/systems/goliathAttackClips.ts taking
    the rig handles (extraction verbatim, not a rewrite — PATTERNS "Conditional Carve-Outs"),
    in this same task, and leave createGoliathRenderer.ts importing them. If the ceiling does
    not trip, do NOT create the file.
  </action>
  <verify>
    <automated>grep -c "animateDash" src/game/systems/createGoliathRenderer.ts src/game/systems/goliathAttackClips.ts 2>/dev/null | awk -F: '{s+=$NF} END {exit (s>=2 ? 0 : 1)}' && pnpm vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `animateDash` appears at least twice across the renderer (or carve-out) files: definition + dispatch reference
    - The animateDash function body contains no assignment to the model group's vertical position channel (grounded lunge, D6-12) — source-verifiable by reading the function
    - travelFraction is called by animateDash and defined exactly once across the touched files (no duplication)
    - All transforms written by animateDash use channels resetAttackPose or applyCrouch restore (source assertion against the 05-02 contract), or resetAttackPose was extended in this task
    - If goliathAttackClips.ts exists, createGoliathRenderer.ts imports from it and neither file exceeds 300 functional LOC; if it does not exist, createGoliathRenderer.ts is <= 300 functional LOC
    - `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` exits 0 (no client-mirror drift introduced)
  </acceptance_criteria>
  <done>The dash clip exists with brace -> grounded lunge -> skid phases riding actual mesh travel, dispatched by attackId, neutral-restore safe; visual sign-off is owned by the 06-05 pixel-filter playtest.</done>
</task>

<task type="auto">
  <name>Task 2: playDash shield-clang SFX + shieldDash strike-juice tier (ANIM-04 precedent, D6-13 discretion)</name>
  <files>src/game/audio/createAudioSystem.ts, src/game/createGame.ts</files>
  <read_first>
    - src/game/audio/createAudioSystem.ts (FULL — playSwing at ~93-134: guard clause, clampGain, noise-through-bandpass body, small-speaker thock layer, the embedded gain lesson at ~100-104; AudioSystem interface at ~10-14 and return object at ~193-195)
    - src/game/createGame.ts lines 1280-1375 (handleAttackStrike: distance gate + juiceFalloff at ~1328-1333, unconditional lastStrike capture at ~1315-1321, swordSwing tier at ~1338-1355 — the lightest tier and the no-shockwave precedent, shake join via Math.max, handler-local seed constants)
    - src/game/systems/createAttackViewClock.ts lines ~44 and ~160-170 (getAttackRows exposure — the dash row lookup for the wake's cast point)
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md "Pattern 6" and "Assumptions Log" A1 (cache-ordering assumption -> the mandatory burst-only fallback)
  </read_first>
  <action>
    In createAudioSystem.ts: add playDash(gainFactor = 1) following the playSwing skeleton
    verbatim for the opener (running-context guard, clampGain, currentTime anchor), then a
    metallic shield-clang recipe: a short high-Q bandpass noise hit layered with a brief
    detuned oscillator pair (two oscillators a few Hz apart for the metallic beat), total
    duration ~0.15-0.25s, exponential gain envelopes. Respect the embedded playtest lesson:
    bandpass discards most noise energy, so peak gains must sit well above the slam's
    broadband levels to be audible — author gains as named constants. Register playDash in
    the AudioSystem interface and the return object.
    In createGame.ts handleAttackStrike: add a shieldDash tier as another early-return if
    block alongside the swordSwing/swordSwirl tiers, AFTER the existing distance gate /
    juiceFalloff / lastStrike capture (it inherits all three by position). The tier: (1)
    spawnBurst at the strike landing (the lane END) using the tier's juiceColor local
    (already resolved generically from ATTACK_RENDER with a fallback — no new lookup code);
    (2) dust wake along the lane: look up the dash's unit_attack row via
    attackViewClock.getAttackRows() keyed by the event's unitKind/unitId and read castX/Z,
    then spawn a few small bursts spaced along the cast->landing segment (a simple loop over
    3-5 interpolated points, handler-local WAKE_POINTS constant) — if the row is missing
    (RESEARCH Assumption A1 cache-ordering), fall back to burst-only at the landing, never
    throw; (3) telegraphSystem.flashStrike for the unit key (rim flash at strike, D6-11);
    (4) a light camera shake joined via Math.max with a DASH_SHAKE_MAGNITUDE seed smaller
    than the slam's, scaled by juiceFalloff; (5) audioSystem.playDash(juiceFalloff). Do NOT
    call the circular shockwave helper — the swing tier's comment explains a circular wave
    misreads a non-circular shape; same logic for the lane (copy that comment style). All
    magnitudes are handler-local named seed constants (NOT parity material, the 05-02
    decision). No per-frame identity work — the tier is event-scoped (onInsert once).
  </action>
  <verify>
    <automated>grep -c "playDash" src/game/audio/createAudioSystem.ts | awk '{exit ($1>=3 ? 0 : 1)}' && grep -c "shieldDash" src/game/createGame.ts | awk '{exit ($1>=1 ? 0 : 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "playDash" src/game/audio/createAudioSystem.ts` returns >= 3 (interface declaration + function definition + return-object registration)
    - `grep -c "playDash" src/game/createGame.ts` returns >= 1 (the tier calls it)
    - The shieldDash tier calls spawnBurst and flashStrike but NOT the circular shockwave helper (source assertion — mirror of the swing-tier precedent comment)
    - The wake path reads castX/Z from getAttackRows() with an explicit missing-row fallback that still spawns the landing burst (source assertion)
    - Shake joins via Math.max (never overwrites a stronger shake) and every new magnitude is a named handler-local constant
    - playDash's opener matches the playSwing guard pattern (silent no-op before gesture unlock; never throws mid-frame)
  </acceptance_criteria>
  <done>A dash strike produces clang + wake + flash + light shake exactly once per attack_strike event, degrading gracefully when the row lookup misses; audibility/feel tuning is owned by the 06-05 playtest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server rows/events → client render | Read-only consumption of unit_attack rows and attack_strike events; this plan writes zero server state and adds zero client-callable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | client render vs server truth | high | mitigate | Clip/juice timing derives from server rows/events only (ANIM-01/ANIM-04); nothing here influences hit resolution — inherited server-side strike resolution is the control |
| T-06-05 | DoS (client perf) | per-frame/identity hot loops | medium | mitigate | Juice tier is event-scoped (onInsert once); no per-row-per-render identity serialization added (identity-hex perf rule); wake is a bounded 3-5 burst loop |
| T-06-SC | Tampering | package installs | low | accept | Zero new dependencies — procedural WebAudio, bundled three only |
</threat_model>

<verification>
- Grep assertions above pass; `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` green
- No modification to createEntityRenderer.ts (SNAP_DISTANCE is read-only context, D6-03)
- Full typecheck/build is owned by the wave merge and the 06-05 exclusive gate
</verification>

<success_criteria>
- animateDash + playDash + the dash juice tier exist, follow their in-file analogs, and are dispatch-reachable by attackId 'shieldDash'
- The clip is grounded (no vertical writes) and neutral-restore safe under the 05-02 contract
- No shockwave, no client timers, no new deps, no per-frame identity work
</success_criteria>

<output>
Create `.planning/phases/06-shielddash-lane/06-02-SUMMARY.md` when done
</output>
