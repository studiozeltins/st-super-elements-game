---
phase: 06-shielddash-lane
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/game/systems/createTelegraphSystem.ts
  - src/game/systems/telegraphShapes.ts
autonomous: true
requirements: [ATK-04]
tags: [client, three, telegraph, frost]

must_haves:
  truths:
    - "A shieldDash windup shows an instant full-lane rectangle outline anchored at the CAST end, oriented by the cast->landing yaw, with a fill sweeping from the cast end toward the lane end over the windup and a rim flash at strike — icy-cyan #86e2ff, timing re-derived from the server row (D6-11, ANIM-01)"
    - "The lane fill scales ONLY along the lane axis — width stays constant (a uniform setScalar fill would read as a growing wedge, RESEARCH Pitfall 3)"
    - "Lane width comes from row.radius x 2 and lane length from |landing - cast| — no client mirror field (documented D6-13 deviation: the row supersedes the mirror; RESEARCH Pitfall 6)"
    - "A same-attack re-cast lane REBUILDS its meshes instead of re-anchoring — a world-clamped re-cast can change the baked length (RESEARCH Pitfall 4); a mid-windup goliath death still drops the telegraph via the existing dead-row hygiene"
  artifacts:
    - path: "src/game/systems/createTelegraphSystem.ts"
      provides: "lane rectangle branch: anchor arm, insert construction, fillAxis scale strategy, lane re-cast rebuild rule"
      contains: "lane"
  key_links:
    - from: "src/game/systems/createTelegraphSystem.ts"
      to: "src/game/data/attacks.ts"
      via: "shape lookup ATTACK_RENDER[row.attackId]?.shape === 'lane' routes the new branch (mirror entry landed in 06-01)"
      pattern: "ATTACK_RENDER"
    - from: "src/game/systems/createTelegraphSystem.ts"
      to: "unit_attack row"
      via: "geometry derives entirely from the row: castX/Z anchor, |landing - cast| length, radius half-width, startedAtMicros/strikeAtMicros timing"
      pattern: "row.radius"
---

<objective>
Add the lane rectangle telegraph branch to `createTelegraphSystem` — the D6-11 rectangle
variant of the D4-14 language: instant full-lane outline, cast-to-end fill sweep, strike rim
flash, all in the shared Frost constants, with the two lane-specific deviations research
proved necessary (axis-only fill scaling, rebuild-on-re-cast).

Purpose: SC1 hinges on this — "a player who steps out of the lane during windup is not hit"
only works if the player can SEE the lane. This is the phase's only greenfield-ish render
geometry; everything else reuses the cone branch seams.

Parallel-safe with 06-03 (zero file overlap; wave-2). Visual sign-off through the pixel
filter is owned by the 06-05 playtest (RESEARCH Assumption A3).

Output: extended `createTelegraphSystem.ts` (± `telegraphShapes.ts` carve if the LOC ceiling
trips).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-shielddash-lane/06-CONTEXT.md
@.planning/phases/06-shielddash-lane/06-RESEARCH.md
@.planning/phases/06-shielddash-lane/06-PATTERNS.md
@src/game/systems/createTelegraphSystem.ts
@src/game/data/attacks.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 6 (this plan lands the starred ones):

- `closestPointOnSegment` / `resolveLane` / `computeLaneEnd` — `spacetimedb/src/attackHitbox.ts` (plan 06-01)
- `AttackSpec.laneLengthBySize?` + `ATTACKS.shieldDash` + leapSlam maxBand 5.5 retune + UNIT_ATTACKS append — `spacetimedb/src/attacks.ts` (plan 06-01)
- Generalized relocate gate `spec.move !== 'none'` — `spacetimedb/src/unitAttackFsm.ts` (plan 06-01)
- `ATTACK_RENDER.shieldDash` + shape union widened to `'lane'` — `src/game/data/attacks.ts` (plan 06-01)
- `animateDash` clip + dispatch, `playDash` SFX, shieldDash juice tier — client (plan 06-02)
- Lane-end computation + lane resolveStrike arm — `spacetimedb/src/unitAttacks.ts` (plan 06-03)
- ★ Lane arm in `anchorGroup` (cast-end anchor + cast->landing yaw) — `src/game/systems/createTelegraphSystem.ts`
- ★ Lane rectangle construction in `insert()` (outline strips + axis-scaled fill plane) — `src/game/systems/createTelegraphSystem.ts` (± ★ `telegraphShapes.ts` carve-out with buildCircle/buildCone/buildLane if the LOC ceiling trips)
- ★ `fillAxis` scale-strategy field on `ActiveTelegraph` + lane re-cast rebuild rule in the sync loop — `src/game/systems/createTelegraphSystem.ts`

<tasks>

<task type="auto">
  <name>Task 1: Lane anchor arm + rectangle mesh construction (D6-11, ANIM-02, Pitfall 6 deviation)</name>
  <files>src/game/systems/createTelegraphSystem.ts, src/game/systems/telegraphShapes.ts</files>
  <read_first>
    - src/game/systems/createTelegraphSystem.ts (FULL — Frost constants block at ~10-35: TELEGRAPH_COLOR, RIM_WIDTH 0.3, PROGRESS_RIM_WIDTH 0.22, flatMaterial, GROUND_EPSILON, render orders; ActiveTelegraph shape at ~37-57; anchorGroup cone arm at ~95-111 incl. the playtest-verified yaw formula; insert() cone branch at ~120-154; the shared minus-half-pi X rotation and the near-zero initial progress scale)
    - src/game/data/attacks.ts (ATTACK_RENDER — the shieldDash mirror entry from 06-01 carries shape 'lane'; NOTE it deliberately has no half-width field)
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md "Pattern 4", "Pitfall 3", "Pitfall 6" and the "Lane telegraph geometry sketch" code example
    - .planning/phases/06-shielddash-lane/06-PATTERNS.md section "src/game/systems/createTelegraphSystem.ts" and "Conditional Carve-Outs"
  </read_first>
  <action>
    ANCHOR: add a lane arm to anchorGroup mirroring the cone arm exactly — group position at
    the CAST end (castX/Z + ground height + GROUND_EPSILON) and yaw from the SAME formula the
    cone uses (atan2 of negated landing-minus-cast Z over landing-minus-cast X — the sign
    convention was visually verified through the pixel filter in 05-05; reuse it verbatim,
    do not re-derive). INSERT: add a lane branch to insert() routed by
    ATTACK_RENDER[row.attackId]?.shape === 'lane'. Derive geometry entirely from the row:
    length = hypot(landing - cast), width = row.radius * 2 (row.radius IS the per-size
    half-width written by enterWindup — the documented D6-13 deviation: no client mirror
    field because a scalar cannot parity-lock to the per-size array and would be dead data;
    comment this at the branch). Local axes before the shared flat rotation: +X along the
    lane (anchorGroup yaws it toward the aim), Y across the width. OUTLINE (instant, full
    lane, D6-11): thin strips of RIM_WIDTH 0.3 (at or above the 0.2u pixel-filter floor,
    ANIM-02) — two long rails plus two end caps built from PlaneGeometry, positioned to trace
    the rectangle perimeter, sharing outlineMaterial. FILL: ONE PlaneGeometry of
    (length, width - 2 x PROGRESS_RIM_WIDTH), translated +length/2 on X so the geometry
    origin sits at the cast end and scaling X sweeps cast -> lane end (D6-11 fill direction),
    material via flatMaterial(PROGRESS_OPACITY), initial X scale near-zero (mirror the
    existing epsilon — an exact 0 makes a degenerate matrix). Both meshes get the shared
    flat-on-ground rotation and render orders. Extend ActiveTelegraph with a fillAxis field
    ('uniform' | 'x') set at insert time: 'x' for lanes, 'uniform' for circle/cone — Task 2
    consumes it. LOC rule: if createTelegraphSystem.ts crosses 300 functional LOC with the
    branch, carve the shape construction into src/game/systems/telegraphShapes.ts exposing
    buildCircle/buildCone/buildLane returning outline + progress + fillAxis (extraction of
    the existing branch bodies verbatim, same slice — PATTERNS "Conditional Carve-Outs");
    otherwise do NOT create the file.
  </action>
  <verify>
    <automated>grep -q "fillAxis" src/game/systems/createTelegraphSystem.ts && grep -rq "'lane'" src/game/systems/createTelegraphSystem.ts src/game/systems/telegraphShapes.ts 2>/dev/null && pnpm vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The lane branch is routed by a shape check against 'lane' and appears in BOTH anchorGroup and insert() (source assertion)
    - Lane width derives from row.radius and length from the cast/landing row fields; no half-width constant or mirror field is introduced anywhere in the client (source assertion, D6-13 deviation honored)
    - The fill PlaneGeometry is translated by half its length on X (origin at the cast end) — source assertion per Pitfall 3
    - fillAxis exists on ActiveTelegraph and is assigned for every shape at insert time ('x' only for lanes)
    - Outline strip width uses the existing RIM_WIDTH constant (no new magic width) and all materials come from the shared outlineMaterial/flatMaterial helpers (Frost language reuse, D4-14)
    - `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` exits 0
  </acceptance_criteria>
  <done>A lane row inserts a correctly anchored, correctly sized rectangle outline + cast-origin fill, built entirely from row data in the shared Frost language.</done>
</task>

<task type="auto">
  <name>Task 2: Axis-aware fill scaling + lane re-cast rebuild rule (Pitfalls 3 + 4)</name>
  <files>src/game/systems/createTelegraphSystem.ts</files>
  <read_first>
    - src/game/systems/createTelegraphSystem.ts (the shared update() progress-scaling loop at ~258-264, the sync loop's chain-swap rebuild path at ~223-227 and the same-attack re-cast re-anchor path at ~229-234, progressFraction timing derivation at ~196-203)
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md "Pattern 4" (why re-cast must rebuild for lanes: world clamping can change |landing - cast| between casts, and the baked geometry would be stale) and "Pitfall 3"
  </read_first>
  <action>
    UPDATE LOOP: make the per-frame progress scaling honor fillAxis so the loop stays ONE
    loop with zero per-frame shape lookups — when fillAxis is 'x', write the windup fraction
    to progress.scale.x only (y and z stay 1, width constant); when 'uniform', keep the
    existing setScalar path byte-for-byte. Preserve the existing epsilon floor and the
    fraction source (progressFraction — timing stays row-derived, ANIM-01; never a client
    timer). RE-CAST RULE: in the sync loop, extend the rebuild condition so a telegraph whose
    render shape is 'lane' ALSO routes through the existing remove-and-insert path when
    startedAtMicros changes (a fresh cast), instead of the re-anchor-only path — circle/cone
    keep today's cheap re-anchor because their geometry depends only on row.radius, but a
    lane's baked length can change after world clamping (Pitfall 4; comment the condition
    with this rationale). The existing dead-row hygiene / alive-map re-gate is untouched — a
    mid-windup goliath death already drops the telegraph (D6-14). flashStrike needs no
    changes (the rim flash keys off the outline mesh generically).
  </action>
  <verify>
    <automated>grep -q "fillAxis" src/game/systems/createTelegraphSystem.ts && grep -q "startedAtMicros" src/game/systems/createTelegraphSystem.ts && pnpm vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The update loop contains exactly one progress-scaling site, branched on the stored fillAxis — no ATTACK_RENDER lookup inside the per-frame loop (source assertion)
    - Lane fill scaling writes scale.x only; the uniform path for circle/cone is unchanged (source assertion)
    - The sync loop routes a lane telegraph with a changed startedAtMicros through remove + insert (rebuild), with the Pitfall-4 rationale comment; circle/cone re-cast behavior unchanged
    - No client-local timers introduced; the fill fraction still derives from progressFraction over row timestamps (ANIM-01)
  </acceptance_criteria>
  <done>The lane fill sweeps cast-to-end at constant width and every fresh cast renders with fresh geometry; circle/cone hot paths are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server rows → client render | Read-only unit_attack subscription; telegraph timing/geometry derive from server rows (ANIM-01) — no client input can alter combat truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | telegraph honesty (dodge fairness) | high | mitigate | Geometry + timing are pure row derivations (cast/landing/radius/startedAt); a modified client can only mislead ITSELF — the server lane test is the truth (inherited control) |
| T-06-05 | DoS (client perf) | per-frame telegraph loop | medium | mitigate | fillAxis stored at insert time keeps the per-frame loop lookup-free; rebuilds only on rare re-cast events with proper geometry disposal |
| T-06-SC | Tampering | package installs | low | accept | Zero new dependencies — bundled three only |
</threat_model>

<verification>
- Grep assertions pass; serverSync parity green
- No edits outside createTelegraphSystem.ts (and the conditional telegraphShapes.ts carve)
- Pixel-filter legibility (RESEARCH A3) is explicitly deferred to the 06-05 human playtest
</verification>

<success_criteria>
- Lane rows render the D6-11 rectangle language: instant outline, cast-to-end axis-only fill, strike flash, Frost constants
- Width from row.radius (documented D6-13 deviation), length from the row segment — zero mirror fields, zero dead data
- Re-cast lanes rebuild; circle/cone paths byte-compatible with Phase 5 behavior
</success_criteria>

<output>
Create `.planning/phases/06-shielddash-lane/06-04-SUMMARY.md` when done
</output>
