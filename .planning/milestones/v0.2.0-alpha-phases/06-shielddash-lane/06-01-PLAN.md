---
phase: 06-shielddash-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/attacks.ts
  - spacetimedb/src/attackHitbox.ts
  - spacetimedb/src/unitAttackFsm.ts
  - src/game/data/attacks.ts
  - src/game/data/__tests__/attacks.test.ts
  - src/game/data/__tests__/attackHitbox.test.ts
  - src/game/data/__tests__/unitAttackFsm.test.ts
  - src/game/data/__tests__/serverSync.test.ts
autonomous: true
requirements: [ATK-04]
tags: [spacetimedb, combat, fsm, pure-helpers, tdd]

must_haves:
  truths:
    - "resolveLane returns true for every point within halfWidth of the cast->landing segment (inclusive edge), false beyond it or beside the segment ends; a zero-length segment degrades to the circle test (D6-04)"
    - "computeLaneEnd returns cast + normalize(target - cast) x length (overshoot, D6-02) with a zero-length-aim fallback to the cast point"
    - "selectAttack returns 'shieldDash' at distance 6.5 with both cooldowns clear — the attack is REACHABLE (RESEARCH Pitfall 1 resolved via leapSlam maxBand 8 -> 5.5)"
    - "walkAttackTransitions relocates to landing at STRIKE for move 'charge' exactly as it does for 'leap', and move 'none' still never relocates (D6-01, Pitfall-1 gate preserved)"
    - "serverSync ATTACKS parity is green with the shieldDash entry present on BOTH sides (id-set equality, stunSeconds 0.45, juiceColor palette membership, damage triple 225/325/425)"
  artifacts:
    - path: "spacetimedb/src/attackHitbox.ts"
      provides: "lane geometry pure helpers"
      exports: ["closestPointOnSegment", "resolveLane", "computeLaneEnd"]
    - path: "spacetimedb/src/attacks.ts"
      provides: "shieldDash entry (lane/skill/charge, D6-03/06/07/08/09/10 seeds), laneLengthBySize? on AttackSpec, UNIT_ATTACKS +'shieldDash', leapSlam maxBand 5.5 retune"
      contains: "laneLengthBySize"
    - path: "spacetimedb/src/unitAttackFsm.ts"
      provides: "generalized relocate-at-strike gate (move !== 'none')"
      contains: "spec.move !== 'none'"
    - path: "src/game/data/attacks.ts"
      provides: "ATTACK_RENDER.shieldDash mirror entry + shape union widened to 'lane'"
      contains: "shieldDash"
  key_links:
    - from: "spacetimedb/src/attacks.ts"
      to: "spacetimedb/src/attacks.ts selectAttack"
      via: "band partition — leapSlam 0..5.5, shieldDash 3.5..8 makes 5.5..8 dash-only under the shared skill cooldown (D6-06/D6-07)"
      pattern: "maxBand"
    - from: "spacetimedb/src/unitAttackFsm.ts"
      to: "spacetimedb/src/unitAttacks.ts"
      via: "teleportToLanding flag consumed by the existing goliathPosition override (glue untouched this plan)"
      pattern: "teleportToLanding"
    - from: "src/game/data/attacks.ts"
      to: "src/game/data/__tests__/serverSync.test.ts"
      via: "id-set equality + stunSeconds/juiceColor it.each auto-assert the new entry the moment it exists on either side"
      pattern: "ATTACK_RENDER"
---

<objective>
Build the entire Phase-6 server PURE layer test-first, plus the client registry mirror entry
that keeps serverSync's id-set parity green at the wave boundary: the lane geometry trio
(`closestPointOnSegment`/`resolveLane`/`computeLaneEnd`, D6-02/D6-04/D6-05), the `shieldDash`
`ATTACKS` entry with all locked D6 seeds plus the Pitfall-1 band fix (leapSlam maxBand
8 -> 5.5 — WITHOUT this, D6-06 + D6-07 make shieldDash unreachable dead code), the
generalized relocate-at-strike gate (D6-01), and the `ATTACK_RENDER.shieldDash` mirror.
This also completes ATK-06's lane tail (segment-projection geometry reuse, no dead stubs).

Purpose: everything gameplay-critical in this phase is deterministic pure logic. Landing it
first, fully tested — with the dash-selectability regression guards that prove the attack can
actually fire — makes the Wave-2 glue plan a thin wiring exercise.

DO NOT publish the module between this plan and 06-03: a published registry entry without the
glue's lane-end computation and lane resolveStrike arm would fire a broken pseudo-dash
(circle test at the landing, no overshoot) — the same hazard class as the 05-01/05-03
publish-window lesson.

Output: extended `attacks.ts`, `attackHitbox.ts`, `unitAttackFsm.ts`, client
`src/game/data/attacks.ts` + green extended test suite (attacks, attackHitbox,
unitAttackFsm, serverSync).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-shielddash-lane/06-CONTEXT.md
@.planning/phases/06-shielddash-lane/06-RESEARCH.md
@.planning/phases/06-shielddash-lane/06-PATTERNS.md
@spacetimedb/src/attacks.ts
@spacetimedb/src/attackHitbox.ts
@spacetimedb/src/unitAttackFsm.ts
@src/game/data/attacks.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 6 (this plan lands the starred ones):

- ★ `closestPointOnSegment(px, pz, ax, az, bx, bz): {x, z}` — `spacetimedb/src/attackHitbox.ts`
- ★ `resolveLane(px, pz, castX, castZ, landingX, landingZ, halfWidth): boolean` — `spacetimedb/src/attackHitbox.ts`
- ★ `computeLaneEnd(castX, castZ, targetX, targetZ, length): {x, z}` — `spacetimedb/src/attackHitbox.ts`
- ★ `AttackSpec.laneLengthBySize?: readonly number[]` — `spacetimedb/src/attacks.ts`
- ★ `ATTACKS.shieldDash` entry + `leapSlam.maxBand` 8 -> 5.5 retune + `UNIT_ATTACKS` goliath default `['leapSlam', 'swordSwing', 'shieldDash']` — `spacetimedb/src/attacks.ts`
- ★ Generalized relocate gate `spec.move !== 'none'` in `walkAttackTransitions` — `spacetimedb/src/unitAttackFsm.ts`
- ★ `AttackRenderSpec.shape` widened to `'circle' | 'cone' | 'lane'` + `ATTACK_RENDER.shieldDash` entry — `src/game/data/attacks.ts`
- ★ New test describes: `closestPointOnSegment`/`resolveLane`/`computeLaneEnd` (attackHitbox.test.ts), dash-selectability regression guards (attacks.test.ts), charge relocate + coalesced charge walk (unitAttackFsm.test.ts), shieldDash registry invariants (serverSync.test.ts)
- `animateDash` clip + dispatch — `src/game/systems/createGoliathRenderer.ts` (plan 06-02)
- `playDash` procedural SFX — `src/game/audio/createAudioSystem.ts` (plan 06-02)
- shieldDash juice tier in `handleAttackStrike` — `src/game/createGame.ts` (plan 06-02)
- Lane-end computation at the IDLE selection site + lane arm in `resolveStrike` (per-victim knockback center) — `spacetimedb/src/unitAttacks.ts` (plan 06-03)
- Lane rectangle telegraph branch (anchor arm, insert branch, `fillAxis` scale strategy, re-cast rebuild rule) — `src/game/systems/createTelegraphSystem.ts` (plan 06-04)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lane geometry pure helpers, test-first (D6-02/D6-04/D6-05, ATK-06 lane tail)</name>
  <files>spacetimedb/src/attackHitbox.ts, src/game/data/__tests__/attackHitbox.test.ts</files>
  <read_first>
    - spacetimedb/src/attackHitbox.ts (FULL — resolveCircleHit/resolveCone doc-comment + inclusive-edge style, knockbackDisplacement zero-delta fallback branch, file-header lane reservation to fulfill)
    - spacetimedb/src/bridges.ts lines 78-97 (distanceToSegment — the clamp-to-[0,1] projection MATH to mirror locally; do NOT import bridges.ts, combat stays decoupled from world topology and the lane needs the closest POINT which distanceToSegment does not return)
    - src/game/data/__tests__/attackHitbox.test.ts (cross-boundary import pattern from ../../../../spacetimedb/src/attackHitbox + exact-boundary test naming style)
    - .planning/phases/06-shielddash-lane/06-PATTERNS.md section "spacetimedb/src/attackHitbox.ts"
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md section "Code Examples" (prescriptive drafts of all three helpers)
  </read_first>
  <behavior>
    - closestPointOnSegment: point projecting onto the segment interior returns the perpendicular foot; point projecting past either end clamps to that endpoint; zero-length segment returns the start point
    - resolveLane: point at perpendicular distance halfWidth-0.1 from the centerline mid-segment -> true; at EXACTLY halfWidth -> true (inclusive edge, matches resolveCircleHit/resolveCone semantics); at halfWidth+0.01 -> false
    - resolveLane: point BEYOND the landing end (past the segment, along the axis) more than halfWidth from the endpoint -> false; within halfWidth of the endpoint (capsule cap) -> true; same for the cast end
    - resolveLane: zero-length segment (cast == landing) degrades to the circle test around that point (D6-04 degenerate semantics)
    - computeLaneEnd: target at distance 4 with length 7.25 returns a point exactly 7.25 from cast along the cast->target direction (overshoot PAST the target, D6-02); result direction equals normalize(target - cast)
    - computeLaneEnd: zero-length aim (target == cast) falls back to the cast point (defensive; minBand 3.5 makes it unreachable in play, but a pure helper must not produce NaN)
  </behavior>
  <action>
    RED first: add three describe blocks (closestPointOnSegment, resolveLane, computeLaneEnd)
    to attackHitbox.test.ts covering every behavior above, importing cross-boundary in the
    existing style with named constants (CAST_X/CAST_Z/LANDING_X/LANDING_Z/HALF_WIDTH). Run —
    must fail (symbols missing). Then GREEN: in attackHitbox.ts add three exports.
    closestPointOnSegment(px, pz, ax, az, bx, bz) returns {x, z} via the standard
    clamp-to-[0,1] projection: segment delta, lengthSquared, early-return start point when
    lengthSquared is 0, projection = dot / lengthSquared clamped to [0,1], interpolated point.
    resolveLane(px, pz, castX, castZ, landingX, landingZ, halfWidth) returns
    Math.hypot from the closest point <= halfWidth (INCLUSIVE, per D6-04). computeLaneEnd(
    castX, castZ, targetX, targetZ, length) returns cast + normalized delta x length with the
    zero-length early return, copying knockbackDisplacement's defensive delta/hypot/early-return
    shape. Each function gets a block doc-comment in the resolveCone style: inclusive edge
    semantics, the documented degenerate case, decision IDs (D6-02/D6-04/D6-05), and a note
    that the projection math mirrors the bridges.ts pattern locally (ATK-06 geometry reuse,
    deliberately NOT imported). Update the file-header comment so the lane reservation becomes
    real code (no-dead-code rule). Functions take plain numbers only — no clock, no RNG
    (pure module contract, grep-gate). No changes to resolveCircleHit, resolveCone, or
    knockbackDisplacement.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attackHitbox.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/attackHitbox.test.ts` exits 0 with all new describes present and passing
    - `grep -c "export function closestPointOnSegment" spacetimedb/src/attackHitbox.ts` returns 1; same for `resolveLane` and `computeLaneEnd`
    - resolveLane calls closestPointOnSegment (shared projection — no duplicated math): `grep -c "closestPointOnSegment" spacetimedb/src/attackHitbox.ts` returns >= 3 (definition + resolveLane call + doc reference)
    - attackHitbox.ts imports nothing new beyond existing pure siblings (no bridges.ts import) and contains no clock or RNG usage
    - The inclusive-edge case (exactly halfWidth -> true) and zero-length-segment degradation are each asserted by a named test
  </acceptance_criteria>
  <done>All three lane geometry helpers exist as pure, documented, fully unit-tested exports; the ATK-06 lane-geometry-reuse mandate is honored via a local projection implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shieldDash registry entry both sides + Pitfall-1 band fix + selectability regression guards (D6-03/06/07/08/09/10/13)</name>
  <files>spacetimedb/src/attacks.ts, src/game/data/attacks.ts, src/game/data/__tests__/attacks.test.ts, src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - spacetimedb/src/attacks.ts (FULL — AttackSpec interface incl. the coneMinDot? optional-field precedent, leapSlam entry with its current maxBand, swordSwirl entry comment-density style, UNIT_ATTACKS, selectAttack first-match-wins loop — selectAttack itself is UNCHANGED this phase)
    - src/game/data/attacks.ts (AttackRenderSpec shape union at ~line 11, ATTACK_RENDER entries — the mirror entry pattern)
    - src/game/data/__tests__/attacks.test.ts (FULL — the leapSlam toEqual literal and the goliath default-list assertion BOTH break with this task's edits and must be updated here, not left red; selectAttack boundary-test style)
    - src/game/data/__tests__/serverSync.test.ts lines 340-490 (registry-invariants describe, swirl damage-triple test style, the it.each auto-harness that covers the new entry: tick multiples, shape parity, stunSeconds, juiceColor palette, graceTicks, id-set equality, non-cone-entries-carry-no-cone-angle)
    - spacetimedb/src/enemyStats.ts lines 69-74 (GOLIATH_SIZE_STATS contactDamage 90/130/170 — the damage-triple derivation)
    - src/game/data/elements.ts (ELEMENTS.cryo.color — the palette member the mirror entry uses)
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md sections "Pitfall 1", "Pitfall 2", "Pitfall 5", "Pitfall 6" and "Code Examples"
  </read_first>
  <behavior>
    - selectAttack at distance 6.5 with both cooldowns clear -> 'shieldDash' (the dead-code regression guard — this is the test that fails if the band fix is ever reverted)
    - selectAttack at distance 4.5 with both cooldowns clear -> 'leapSlam' (slam wins the 3.5..5.5 overlap; list order preserved, D6-07)
    - selectAttack at distance 6.5 with the skill cooldown active -> null (the SHARED gate blocks BOTH skills, D6-06; swing band 0..3.5 is out of band)
    - selectAttack at distance EXACTLY 5.5 with cooldowns clear -> 'leapSlam' (inclusive maxBand edge, existing boundary style)
    - shieldDash registry numbers match the locked seeds exactly (toEqual literal)
    - serverSync: shieldDash per-size damage rounds to [225, 325, 425] (2.5x contactDamage, D6-09), derived from GOLIATH_SIZE_STATS x damageMultiplier — never hardcoded on both sides
    - serverSync: shieldDash shape is 'lane', move is 'charge', laneLengthBySize has GOLIATH_SIZE_STATS.length entries each <= 8 (client SNAP_DISTANCE ceiling — not exported, assert the literal 8 with a comment naming it), radiusBySize has 3 entries
  </behavior>
  <action>
    In spacetimedb/src/attacks.ts: (1) add ONE optional field to AttackSpec following the
    coneMinDot precedent: `laneLengthBySize?: readonly number[]` with a comment citing D6-03
    and the 8u client snap ceiling. The shape union already includes 'lane' and the move union
    already includes 'charge' — NO type widening server-side. (2) THE PITFALL-1 FIX: change
    leapSlam.maxBand from 8 to 5.5 with a loud seed comment citing RESEARCH Pitfall 1 — under
    the shared skill cooldown (D6-06) and slam-first list order (D6-07), a slam band of 0..8
    fully shadows the dash band 3.5..8, making shieldDash unreachable; 5.5 creates the
    dash-only 5.5..8 zone the D6-07 after-whiff emergence story requires. Mark it a playtest
    seed (D-02 latitude) and note it is a documented deviation from D6-07's literal numbers,
    surfaced for user confirmation at the 06-05 checkpoint. (3) Add the shieldDash entry with
    the swordSwirl entry's comment density (every number cites its decision ID + "playtest
    seed" where tunable): shape 'lane' (ATK-04), role 'skill' (D6-06 — writes the SHARED
    cooldownUntilMicros at IDLE), windupTicks 6 (0.9s, D6-08 — sidestep escape not a sprint),
    activeTicks 1, graceTicks 1 (D4-02 verbatim), recoveryTicks 8 (1.2s whiffed-charge punish,
    D6-08), cooldownMicros 6_000_000n (D6-08 seed), radiusBySize [1.6, 1.8, 2.0] (lane
    HALF-WIDTH, D6-03 — row.radius carries it to the client, no mirror field needed),
    laneLengthBySize [6.5, 7.25, 7.95] (D6-03 seeds; size 2 backed off from the locked 8.0 to
    7.95 per RESEARCH Pitfall 2 — 8.0 sits exactly ON the strict-greater snap comparison and a
    float epsilon would render the size-2 charge as a teleport; documented deviation, playtest
    seed), damageMultiplier 2.5 (225/325/425, D6-09), minBand 3.5 (D6-07 — point-blank belongs
    to the swing), maxBand 8 (lane reach), knockback 3.5 (perpendicular bulldoze, D6-05/D6-10),
    stunTicks 3 (0.45s, D6-10 — parity: ATTACK_RENDER.stunSeconds 0.45), move 'charge' (D6-01
    relocate-at-strike), poiseThreshold 600. NO chainsInto, NO coneMinDot. (4) Append
    'shieldDash' LAST to the UNIT_ATTACKS goliath default list (D6-07 order).
    In src/game/data/attacks.ts: widen AttackRenderSpec.shape to 'circle' | 'cone' | 'lane'
    and add the mirror entry: shieldDash with shape 'lane', stunSeconds 0.45 (parity-locked to
    stunTicks 3 x 0.15s tick), juiceColor ELEMENTS.cryo.color (icy-white shield read per the
    D6-13 discretion; telegraphs stay Frost cyan regardless). Carry NO half-width field — the
    renderer reads row.radius (per-size correct); a scalar mirror cannot parity-lock to the
    three-value radiusBySize array and would be dead data (RESEARCH Pitfall 6, documented
    deviation from D6-13's letter — comment this on the entry).
    In attacks.test.ts (SAME task — refactor in place, no red intermediate): update the
    leapSlam toEqual literal to maxBand 5.5; update the goliath default-list assertion to
    ['leapSlam', 'swordSwing', 'shieldDash']; add a shieldDash toEqual literal with the exact
    entry above; add the four selectAttack behaviors above as exact-boundary tests in the
    existing naming style. Verify the existing "leapSlam for EVERY distance" sweep derives its
    upper bound from ATTACKS.leapSlam.maxBand (it does per research) — if any test hardcodes
    the old slam band ceiling, update it to derive from the registry.
    In serverSync.test.ts: add the explicit assertions from the behavior list to the
    registry-invariants describe, next to the swirl damage-triple test in its
    derive-both-sides style. Do NOT modify the it.each auto-harness blocks — they cover the
    new entry automatically once both sides exist (which this task guarantees by landing
    server entry + client mirror together — the id-set equality test never sees a one-sided
    state).
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/serverSync.test.ts` exits 0
    - `grep -c "shieldDash" spacetimedb/src/attacks.ts` returns >= 2 (entry key + UNIT_ATTACKS list) and `grep -c "shieldDash" src/game/data/attacks.ts` returns >= 1 (mirror entry)
    - `grep -c "laneLengthBySize" spacetimedb/src/attacks.ts` returns >= 2 (interface field + entry)
    - leapSlam.maxBand is 5.5 in both the registry and the attacks.test.ts literal; the "returns shieldDash at d=6.5 with both cooldowns clear" test exists and passes
    - serverSync asserts the [225, 325, 425] triple as a computed comparison from GOLIATH_SIZE_STATS and ATTACKS.shieldDash.damageMultiplier, and every laneLengthBySize value <= 8
    - selectAttack function body is UNCHANGED (band selection covers the gap-closer, D6-07 — no weighting code)
    - The shieldDash entries carry no chainsInto, no coneMinDot, and no client-side half-width field
  </acceptance_criteria>
  <done>shieldDash exists on both registry sides with all locked D6 seeds, is provably SELECTABLE at 5.5-8u (Pitfall-1 guard green), and the full parity suite is green with zero one-sided states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generalize the relocate-at-strike gate to move !== 'none' + charge walk tests (D6-01, RESEARCH Pitfall 7)</name>
  <files>spacetimedb/src/unitAttackFsm.ts, src/game/data/__tests__/unitAttackFsm.test.ts</files>
  <read_first>
    - spacetimedb/src/unitAttackFsm.ts (FULL — walkAttackTransitions STRIKE step with the current leap-only relocate gate at ~156-163, the TransitionPlan.teleportToLanding doc comment at ~116, enterWindup which stays UNCHANGED per D6-02)
    - src/game/data/__tests__/unitAttackFsm.test.ts (FULL — the existing paired positive/negative relocate gate test, injected NOW/TICK constants, plain-object row builder style)
    - .planning/phases/06-shielddash-lane/06-RESEARCH.md "Pattern 1" and "Pitfall 7"
  </read_first>
  <behavior>
    - Walk of [STRIKE] for shieldDash (move 'charge') -> plan.teleportToLanding true AND the plan's effective position equals the row's landingX/Z (the relocate)
    - Walk of [STRIKE] for swordSwing (move 'none') -> plan.teleportToLanding false (the existing Pitfall-1 gate negative MUST keep passing under the generalized comparison)
    - Coalesced [STRIKE, RECOVERY, IDLE] walk for shieldDash: relocates once, plan.resolveStrike true exactly once (resolve-never-drop, FSM-05), final state IDLE with cooldownUntilMicros == now + 6_000_000n (role 'skill' writes the SHARED gate, D6-06) and basicCooldownUntilMicros UNCHANGED (per-role split, D5-08)
    - strikeSnapshot for the dash walk carries the dash's own attackId/cast/landing/radius (no chain -> snapshot equals the final geometry; pins the 05-03 lesson that the glue teleports from the snapshot)
  </behavior>
  <action>
    RED first: add the charge cases above to the unitAttackFsm.test.ts walkAttackTransitions
    describe using ATTACKS.shieldDash (imported from the registry — Task 2 landed it) and the
    existing injected NOW/TICK constants. Then GREEN: in walkAttackTransitions change the
    relocate gate's comparison from equality-with-'leap' to `spec.move !== 'none'` — a
    one-token generalization (the RESEARCH-recommended discretion pick: no new TransitionPlan
    field, no glue change; a sibling charge flag would duplicate the gate for identical
    behavior). Update the two doc comments that name the old gate: the
    TransitionPlan.teleportToLanding comment (~line 116) and the STRIKE-step comment, so both
    now say 'leap'/'charge' relocate the unit to the locked landing at strike while 'none'
    stays planted (the Pitfall-1 gate). enterWindup, dueAttackTransitions, and every other
    function stay byte-identical. Module stays clock/RNG-free — now and tick remain injected
    bigints.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts && pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/attackHitbox.test.ts src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts` exits 0 with the charge relocate case, the coalesced [STRIKE, RECOVERY, IDLE] charge walk, and the still-passing move-'none' negative all present
    - `grep -c "spec.move !== 'none'" spacetimedb/src/unitAttackFsm.ts` returns 1
    - The coalesced walk asserts the shared skill cooldown write (cooldownUntilMicros == now + 6_000_000n) and untouched basicCooldownUntilMicros
    - Full wave-1 quartet green (second command): attacks, attackHitbox, unitAttackFsm, serverSync
  </acceptance_criteria>
  <done>move:'charge' rides the proven leap relocate seam with zero new plumbing; the coalesced-tick charge contract (relocate once, resolve once, shared cooldown once) is pure and unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server reducers | UNCHANGED — this plan adds zero client-callable surface; all new logic is pure functions consumed by the scheduled worldTick (research verified ZERO new attack surface this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | lane hit/dodge resolution | high | mitigate | resolveLane runs server-side vs server-accepted player rows inside worldTick; stun-window updatePosition rejection (T-04-12) inherited unchanged (ASVS V4) |
| T-06-02 | Tampering/Repudiation | module determinism | medium | mitigate | All new helpers take plain numbers / injected bigints; grep-gate re-verified at 06-03 and 06-05 |
| T-06-03 | DoS (gameplay) | damage seeds crossing the one-shot line | medium | mitigate | Dash max 425 << 900 min HP pool, no chainsInto so the existing no-one-shot chain invariant is untouched; serverSync damage-triple assertion pins the seeds |
| T-06-SC | Tampering | package installs | low | accept | Zero new dependencies is a locked phase constraint — no install tasks exist |
</threat_model>

<verification>
- `pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/attackHitbox.test.ts src/game/data/__tests__/unitAttackFsm.test.ts src/game/data/__tests__/serverSync.test.ts` — all green
- No file in spacetimedb/src gained clock or RNG usage; selectAttack and enterWindup bodies unchanged
- Module NOT published this plan (publish belongs to 06-03, after the glue lands)
</verification>

<success_criteria>
- The lane geometry trio and the generalized charge gate exist as pure, tested exports
- shieldDash is registered on BOTH sides with locked D6 seeds and is provably selectable at 5.5-8u (the Pitfall-1 regression guard)
- The two documented seed deviations (leapSlam maxBand 5.5, size-2 laneLength 7.95) carry loud comments and are queued for user confirmation at the 06-05 checkpoint
- serverSync ATTACKS parity green including the new entry (SC3 mandate holds at the wave boundary)
</success_criteria>

<output>
Create `.planning/phases/06-shielddash-lane/06-01-SUMMARY.md` when done
</output>
