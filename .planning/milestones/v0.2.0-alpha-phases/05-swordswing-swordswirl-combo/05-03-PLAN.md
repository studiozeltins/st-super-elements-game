---
phase: 05-swordswing-swordswirl-combo
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - spacetimedb/src/unitAttacks.ts
  - spacetimedb/src/index.ts
  - src/module_bindings/
autonomous: true
requirements: [ATK-02, ATK-03]
tags: [spacetimedb, glue, schema, migrate, deploy]

must_haves:
  truths:
    - "The worldTick glue delegates the transition walk to walkAttackTransitions — chain swap + break, leap-only teleport gate, and per-role cooldown writes are live server behavior (ATK-03, D5-01/D5-08)"
    - "resolveStrike branches by spec.shape: cone hits resolve via resolveCone from the apex with aim = landing minus cast; knockback centers on the apex for cones (ATK-02, D5-05, Pitfall 4)"
    - "unit_attack gains basicCooldownUntilMicros as an additive .default(0n) column appended LAST, and the populated local DB migrates without a wipe (D5-08, Pitfall 6)"
    - "idleAttackRow includes basicCooldownUntilMicros so lazy upserts never fail at runtime (Pitfall 5)"
    - "Client bindings are regenerated and contain the new column"
  artifacts:
    - path: "spacetimedb/src/unitAttacks.ts"
      provides: "glue wired to walkAttackTransitions + shape-branched resolveStrike + 5-arg selectAttack call + updated idleAttackRow"
      contains: "walkAttackTransitions"
    - path: "spacetimedb/src/index.ts"
      provides: "additive basicCooldownUntilMicros column on unit_attack"
      contains: "basicCooldownUntilMicros"
    - path: "src/module_bindings"
      provides: "regenerated bindings including basicCooldownUntilMicros"
      contains: "basicCooldownUntilMicros"
  key_links:
    - from: "spacetimedb/src/unitAttacks.ts"
      to: "spacetimedb/src/unitAttackFsm.ts"
      via: "transition walk delegated to the pure applier from plan 05-01"
      pattern: "walkAttackTransitions"
    - from: "spacetimedb/src/unitAttacks.ts"
      to: "spacetimedb/src/attackHitbox.ts"
      via: "cone strike resolution calls resolveCone"
      pattern: "resolveCone"
---

<objective>
Wire the tested pure layer into the live server: refactor the `unitAttacks.ts` glue to
delegate its transition walk to `walkAttackTransitions` (landing the chain swap + break, the
leap-only teleport gate that fixes the latent Pitfall-1 bug, and the per-role cooldown split),
branch `resolveStrike` by shape, append the additive `basicCooldownUntilMicros` column, and
migrate-publish to the LOCAL server (self-host pivot — NO maincloud).

Purpose: after this plan the goliath actually swings, chains into the swirl, and runs the
D4-08 basic-basic-skill rhythm on the migrated local DB.

Output: live server module with the full Phase-5 kit + regenerated client bindings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-swordswing-swordswirl-combo/05-CONTEXT.md
@.planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md
@.planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md
@spacetimedb/src/unitAttacks.ts
@spacetimedb/src/unitAttackFsm.ts
@spacetimedb/src/attackHitbox.ts
@spacetimedb/src/index.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 5 (this plan lands the starred ones):

- `resolveCone(...)` pure resolver — `spacetimedb/src/attackHitbox.ts` (plan 05-01)
- `AttackSpec.chainsInto?` / `AttackSpec.coneMinDot?` + `ATTACKS.swordSwing` / `ATTACKS.swordSwirl` + 5-arg `selectAttack` (plan 05-01)
- `walkAttackTransitions(...)` pure transition applier (plan 05-01)
- ★ `basicCooldownUntilMicros: t.u64().default(0n)` column on `unit_attack` — `spacetimedb/src/index.ts` (+ regenerated `src/module_bindings/`)
- ★ Shape-branched `resolveStrike` + applier-driven transition walk + updated `idleAttackRow` — `spacetimedb/src/unitAttacks.ts` (or extracted sibling `spacetimedb/src/unitStrike.ts` if the 300-LOC ceiling forces it)
- `src/game/data/attacks.ts` client mirror: `AttackRenderSpec`, `ATTACK_RENDER` (plan 05-04)
- Cone SECTOR telegraph branch + rebuild-on-attackId (plan 05-04)
- `animateSwing` / `animateSwirl` clips + `playSwing` / `playSwirl` SFX + juice branch (plan 05-02)

<tasks>

<task type="auto">
  <name>Task 1: Glue refactor — applier delegation, move gate, shape branch, cooldown split (D5-01/D5-05/D5-08/D5-13, Pitfalls 1/2/4/5)</name>
  <files>spacetimedb/src/unitAttacks.ts</files>
  <read_first>
    - spacetimedb/src/unitAttacks.ts (FULL — idleAttackRow at 27-46, resolveStrike hit test + knockback at 68-88, selectAttack call site at 133-138, IDLE-branch enterWindup call at 142-152 with the goliathPosition fallback at 132, the transition walk at 178-204 being replaced)
    - spacetimedb/src/unitAttackFsm.ts (the walkAttackTransitions applier and TransitionPlan shape landed by plan 05-01 — read the ACTUAL shipped signature, not assumptions)
    - spacetimedb/src/attackHitbox.ts (resolveCone signature as shipped by plan 05-01)
    - .planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md section "unitAttacks.ts" (all four insertion points with current code)
  </read_first>
  <action>
    Four coordinated edits, one commit: (1) Replace the inline transition walk (lines 178-204)
    with a call to walkAttackTransitions(row, transitions, spec, now, tick,
    goliathRow.sizeIndex, posX, posZ) — position from the goliathPosition map with the
    existing goliathRow fallback — then execute the returned plan's effects: when
    plan.teleportToLanding, goliathPosition.set to the landing (this is now the ONLY teleport
    path — the unconditional teleport at old line ~183 is deleted, fixing latent Pitfall 1 so
    move 'none' attacks stay planted); when plan.emitStrike, insert the attack_strike event
    from the strikeSnapshot fields (unitKind, unitId, attackId, landingX, landingZ, radius) —
    UNCONDITIONAL, before any victim branching (Phase-2 lesson: the event fires even on a
    whiff-into-chain); when plan.resolveStrike, call resolveStrike with the strikeSnapshot
    (NOT the final row — after a chain swap the final row already carries the swirl's
    geometry); finally, when transitions is non-empty, attackTable.id.update(plan.row). The
    per-role cooldown write and the chain swap + break live INSIDE the applier (already tested
    in plan 05-01) — the glue must not duplicate them. (2) Shape-branch resolveStrike (Pitfall
    4): when spec.shape is 'cone', hit = resolveCone(victim.positionX, victim.positionZ,
    row.castX, row.castZ, row.landingX - row.castX, row.landingZ - row.castZ, row.radius,
    spec.coneMinDot ?? 0.5) — apex at cast, aim locked at windup entry per D5-05; otherwise
    the existing resolveCircleHit call unchanged. Knockback center: pass castX/castZ into
    knockbackDisplacement for cones (push away from the goliath — swing seeds knockback 0 so
    inert this phase, but a later cone must not pull victims toward the aim point), landing
    for circles (swirl: landing equals cast so both identical, D5-04). (3) selectAttack call
    site gains row.basicCooldownUntilMicros as the 4th argument (5-arg signature, D5-08). (4)
    idleAttackRow gains basicCooldownUntilMicros: 0n next to cooldownUntilMicros: 0n (Pitfall
    5 — the row literal is any-typed; missing it only surfaces as a runtime insert failure).
    Pre-authorized: if the file exceeds 300 functional LOC after these edits, extract
    resolveStrike and its helpers into a sibling spacetimedb/src/unitStrike.ts in the same
    commit (RESEARCH Open Question 1).
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/attackHitbox.test.ts && grep -c "walkAttackTransitions" spacetimedb/src/unitAttacks.ts</automated>
  </verify>
  <acceptance_criteria>
    - Full quick suite green (serverSync.test.ts is deliberately EXCLUDED here — parallel wave-2 plan 05-04 edits it concurrently, including a transient TDD RED state; serverSync is re-asserted at the 05-05 exclusive full gate)
    - `grep -c "walkAttackTransitions" spacetimedb/src/unitAttacks.ts` >= 2 (import + call)
    - `grep -c "resolveCone" spacetimedb/src/unitAttacks.ts` >= 2 (import + shape branch) — or in unitStrike.ts if extracted
    - The glue contains exactly ONE goliathPosition.set teleport call and it is guarded by the applier's teleport flag (source assertion — the unconditional teleport line is gone)
    - idleAttackRow contains basicCooldownUntilMicros: 0n
    - `grep -rn "Math\.random\|Date\.now" spacetimedb/src --include=*.ts` returns nothing (determinism grep-gate — the module tree is historically clean of these tokens including comments, established Phase-1 invariant)
    - Every touched file <= 300 functional LOC
  </acceptance_criteria>
  <done>The live worldTick path selects, chains, resolves cone hits, and writes per-role cooldowns exclusively through the tested pure layer; no behavior remains inline-duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Additive column + local migrate-publish + bindings regeneration (D5-08, Pitfall 6)</name>
  <files>spacetimedb/src/index.ts, src/module_bindings/</files>
  <read_first>
    - spacetimedb/src/index.ts lines 333-346 (the stunnedUntilMicros additive-column precedent — copy its comment style and 0n bigint default form) and lines 454-480 (the unit_attack table — current LAST fields are strikeResolved and poise)
    - CLAUDE.md sections "Environments & Deployment" and "Server-authoritative enemies + goliaths (deploy notes)" (publish command, migrate rules, never --delete-data)
    - .planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md section "index.ts"
  </read_first>
  <action>
    Append `basicCooldownUntilMicros: t.u64().default(0n)` as the LAST column of the
    unit_attack table definition, with a comment in the stunnedUntilMicros style explaining:
    basic-attack cooldown split from the skill cooldown (D5-08), appended LAST with a bigint
    0n default so the additive migrate backfills the populated table without a wipe. The
    default literal MUST be 0n, not 0 (t.u64 precedent at index.ts:340-346). attack_strike
    already carries attackId — NO change to it or any other table. Then deploy in order:
    (a) `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local
    --yes` — a plain additive migrate, NEVER --delete-data (the local DB holds Phase-4
    playtest state; self-host pivot means maincloud is NOT published); (b) `pnpm run
    spacetime:generate` to regenerate src/module_bindings (the client ignores the new column —
    render-only); (c) verify the migrated DB per the migrate-gotchas memory: `spacetime
    describe 2d-impact-game-fr9ti table unit_attack --json --server local` must list the new
    column, `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local`
    must succeed (rows, if any, survived with backfilled 0), and `spacetime logs
    2d-impact-game-fr9ti -n 50 --server local` must show no insert/migration errors. If the
    local standalone server is not running, start it (spacetime start / spacetimedb-standalone
    per CLAUDE.md) before publishing — do not silently skip the publish. Do NOT run pnpm build
    here (the exclusive Wave-3 gate in plan 05-05 owns the full build to avoid parallel-wave
    flake with plan 05-04).
  </action>
  <verify>
    <automated>spacetime describe 2d-impact-game-fr9ti table unit_attack --json --server local | grep -q "basicCooldownUntilMicros" && grep -rl "basicCooldownUntilMicros" src/module_bindings/ | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes` exits 0 as a MIGRATE (no data wipe; --delete-data never used)
    - `spacetime describe 2d-impact-game-fr9ti table unit_attack --json --server local` output contains basicCooldownUntilMicros
    - `spacetime sql 2d-impact-game-fr9ti "SELECT * FROM unit_attack" --server local` exits 0 (populated rows survived the migrate)
    - `spacetime logs 2d-impact-game-fr9ti -n 50 --server local` contains no migration/insert error lines
    - At least one file under src/module_bindings/ contains basicCooldownUntilMicros
    - spacetimedb/src/index.ts diff touches ONLY the unit_attack table (one appended column + comment)
  </acceptance_criteria>
  <done>The migrated local DB is live with the new column backfilled, bindings regenerated, and the Phase-5 kit running server-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server reducers | UNCHANGED — zero new reducers, zero new reducer args; all Phase-5 combat logic runs inside the scheduled worldTick |
| CLI→local SpacetimeDB | publish/describe/sql against the LOCAL server only (self-host pivot; maincloud is never targeted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | chain trigger / cone aim spoofing | high | mitigate | Chain entry and cone resolution are worldTick-internal (ASVS V4); a modified client cannot call, skip, redirect, or re-aim any attack — no new client-writable input exists |
| T-05-02 | Tampering/Repudiation | determinism break in module | medium | mitigate | Task-1 grep-gate for clock/RNG in spacetimedb/src; all new code takes injected now/tick |
| T-05-06 | DoS (data loss) | destructive migrate on populated DB | high | mitigate | Additive-only column with .default(0n) appended LAST; publish without --delete-data; rows-survive + logs checks are acceptance criteria |
| T-05-SC | Tampering | package installs | low | accept | Zero new dependencies — no install tasks in this plan |
</threat_model>

<verification>
- Quick suite green after Task 1 (serverSync excluded — owned by parallel plan 05-04 and re-asserted at the 05-05 exclusive gate); publish/describe/sql/logs checks green after Task 2
- Determinism grep-gate clean across spacetimedb/src
- No maincloud interaction anywhere in the transcript
</verification>

<success_criteria>
- Server-side: goliath selects swordSwing in the 0..3.5 band, chains into swordSwirl unconditionally, writes the 2.5s basic cooldown at swirl's IDLE, and stays planted at swing strike
- Migrated local DB carries the new column with backfilled rows; bindings expose it
- All logic paths delegate to the plan-05-01 tested pure layer
</success_criteria>

<output>
Create `.planning/phases/05-swordswing-swordswirl-combo/05-03-SUMMARY.md` when done
</output>
