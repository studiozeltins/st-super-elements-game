---
phase: 05-swordswing-swordswirl-combo
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/game/data/attacks.ts
  - src/game/data/__tests__/serverSync.test.ts
  - src/game/systems/createTelegraphSystem.ts
autonomous: true
requirements: [ATK-02, ATK-03]
tags: [client, threejs, telegraph, parity, mirror]

must_haves:
  truths:
    - "A cone attack renders a 120° SECTOR telegraph at the goliath apex, oriented from cast toward the aim point, with the same fill-is-countdown Frost language as the circle (ATK-02, D5-14)"
    - "When the chain swaps attackId on the same telegraph key, the old mesh is removed and rebuilt — the cone never lingers through the swirl windup (ATK-03, Pitfall 3)"
    - "Swirl reuses the EXISTING circle telegraph path untouched, appearing only at its own windup entry (D5-04, D5-15)"
    - "The client mirror is parity-locked to the server ATTACKS registry by serverSync (D5-07, INV-5)"
  artifacts:
    - path: "src/game/data/attacks.ts"
      provides: "AttackRenderSpec interface + ATTACK_RENDER registry (shape, coneHalfAngleDegrees)"
      exports: ["ATTACK_RENDER"]
    - path: "src/game/systems/createTelegraphSystem.ts"
      provides: "cone sector branch + attackId tracked on ActiveTelegraph + rebuild-on-attackId-change"
      contains: "ATTACK_RENDER"
    - path: "src/game/data/__tests__/serverSync.test.ts"
      provides: "mirror-key/shape/half-angle parity block"
      contains: "ATTACK_RENDER"
  key_links:
    - from: "src/game/systems/createTelegraphSystem.ts"
      to: "src/game/data/attacks.ts"
      via: "insert(row) shape lookup by row.attackId"
      pattern: "ATTACK_RENDER"
    - from: "src/game/data/attacks.ts"
      to: "spacetimedb/src/attacks.ts"
      via: "serverSync parity: keys, shape, cos(half-angle) vs coneMinDot"
      pattern: "coneHalfAngleDegrees"
---

<objective>
Give the client honest eyes for the new kit: the `src/game/data/attacks.ts` mirror (D5-07),
the cone SECTOR telegraph branch in `createTelegraphSystem` (D5-14), and the
rebuild-on-attackId-change fix without which the chain renders a lie (RESEARCH Pitfall 3 —
the cone mesh would linger through the entire swirl windup on the same telegraph key).

Purpose: SC2/SC3 are telegraph-readability criteria. The swirl warning IS the chain's
dodge window — it must appear as a fresh circle the instant the row swaps to swordSwirl.

Output: mirror module + parity tests + shape-branched telegraph system.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-swordswing-swordswirl-combo/05-CONTEXT.md
@.planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md
@.planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md
@src/game/systems/createTelegraphSystem.ts
@src/game/data/goliathArchetypes.ts
@src/game/data/__tests__/serverSync.test.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 5 (this plan lands the starred ones):

- `resolveCone(...)` pure resolver — `spacetimedb/src/attackHitbox.ts` (plan 05-01)
- `AttackSpec.chainsInto?` / `AttackSpec.coneMinDot?` + `ATTACKS.swordSwing` / `ATTACKS.swordSwirl` + 5-arg `selectAttack` (plan 05-01)
- `walkAttackTransitions(...)` pure transition applier (plan 05-01)
- `basicCooldownUntilMicros` column on `unit_attack` + regenerated bindings (plan 05-03)
- ★ `src/game/data/attacks.ts` NEW client mirror: `AttackRenderSpec` interface, `ATTACK_RENDER` registry
- ★ Cone SECTOR telegraph branch + `attackId` on `ActiveTelegraph` + rebuild-on-attackId-change — `src/game/systems/createTelegraphSystem.ts`
- `animateSwing` / `animateSwirl` clips + `playSwing` / `playSwirl` SFX + juice branch (plan 05-02)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client mirror module + serverSync parity block (D5-07, INV-5)</name>
  <files>src/game/data/attacks.ts, src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - src/game/data/goliathArchetypes.ts lines 1-28 (the established client data-module shape: doc comment naming the server source of truth + guarding parity test, exported interface, exported const registry)
    - src/game/data/__tests__/serverSync.test.ts lines 329-344 (the CHARACTER_COMBAT mirror-key parity block — the template for the new block) and the ATTACKS block at 350-371 (extended by plan 05-01 — read the shipped state)
    - .planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md section "Client mirror module shape"
  </read_first>
  <behavior>
    - serverSync: Object.keys(ATTACK_RENDER) sorted equals Object.keys(ATTACKS) sorted (no drift in either direction)
    - serverSync: for every attackId, ATTACK_RENDER[id].shape strictly equals ATTACKS[id].shape
    - serverSync: for every cone entry, Math.cos(coneHalfAngleDegrees in radians) is close to spec.coneMinDot (toBeCloseTo — locks 60° to 0.5, D5-06)
    - serverSync: non-cone entries carry NO coneHalfAngleDegrees
  </behavior>
  <action>
    RED first: add the four parity behaviors above to serverSync.test.ts as a new describe
    block in the CHARACTER_COMBAT-mirror style, importing ATTACK_RENDER from the (not yet
    existing) mirror — run, must fail. Then GREEN: create src/game/data/attacks.ts exporting
    interface AttackRenderSpec { shape: 'circle' | 'cone'; coneHalfAngleDegrees?: number } and
    const ATTACK_RENDER: Record<string, AttackRenderSpec> with exactly three entries: leapSlam
    shape 'circle'; swordSwing shape 'cone', coneHalfAngleDegrees 60 (parity-locked to
    coneMinDot 0.5, D5-06); swordSwirl shape 'circle'. Head the module with a doc comment in
    the goliathArchetypes.ts style: static per-attack RENDER data keyed by attackId; the
    server spacetimedb/src/attacks.ts ATTACKS registry stays the source of truth for all
    timing/damage (rows are denormalized from it); this mirror holds only what the renderer
    needs; guarded by serverSync.test.ts (INV-5, D5-07 — runtime import of the server module
    into the client bundle was explicitly rejected). Juice magnitudes do NOT live here (they
    are handler constants in plan 05-02, not parity material).
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` exits 0 with the four new parity assertions present
    - src/game/data/attacks.ts exists, exports ATTACK_RENDER with exactly the keys leapSlam, swordSwing, swordSwirl
    - The mirror imports NOTHING from spacetimedb/ (grep: no spacetimedb import in src/game/data/attacks.ts) — parity is test-enforced, not bundle-coupled
    - Removing any entry or changing a shape would fail serverSync (guaranteed by the key/shape assertions)
  </acceptance_criteria>
  <done>The client owns a minimal, parity-locked shape registry; drift in either direction is a red release-blocking test.</done>
</task>

<task type="auto">
  <name>Task 2: Cone sector telegraph + rebuild-on-attackId-change (D5-14/D5-15, Pitfall 3, assumption A1)</name>
  <files>src/game/systems/createTelegraphSystem.ts</files>
  <read_first>
    - src/game/systems/createTelegraphSystem.ts (FULL — constants 10-34, flatMaterial 67-78, circle insert 88-139, remove 147-151, the re-anchor branch 177-187 that must become rebuild-aware, progress scaling in update 212-217, ActiveTelegraph interface 36-53)
    - src/game/data/attacks.ts (the mirror shipped by Task 1)
    - .planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md "Pattern 4" (sector construction, yaw derivation, A1 caveat)
  </read_first>
  <action>
    Two changes: (1) REBUILD (Pitfall 3 — do this first, it is the chain-honesty fix): add
    attackId to the ActiveTelegraph interface, populate it in insert(row); in syncAttacks,
    when an existing telegraph is found and row.attackId differs from existing.attackId, call
    remove(key, existing) (already disposes geometry) and re-insert, storing the fresh
    telegraph under the key; keep the existing startedAtMicros re-anchor branch for
    same-attack re-casts only. (2) SECTOR BRANCH (D5-14): in insert(row), look up
    ATTACK_RENDER[row.attackId]; unknown or missing entry → the existing circle path
    (back-compat with rows written before the client updated). For shape 'cone': fullAngle =
    (coneHalfAngleDegrees * 2) degrees converted via THREE.MathUtils.degToRad; outline = a
    6-arg THREE.RingGeometry from (range - RIM_WIDTH) to range with the existing
    RING_SEGMENTS, 1 phi segment, thetaStart -fullAngle/2, thetaLength fullAngle (an arc rim
    at the danger edge, instant at windup entry); progress = a FILLED sector RingGeometry from
    0.001 to range with the same theta window and flatMaterial(PROGRESS_OPACITY), built at
    full size then scale.setScalar(0.0001) — the existing update() progress scaling then
    expands it from the apex for free because the group origin IS the apex. Both meshes get
    rotation.x = -PI/2 and the existing circle renderOrder values. The GROUP goes at castX,
    getGroundHeight + GROUND_EPSILON, castZ (apex — NOT landing) and is yawed to the aim:
    group.rotation.y = atan2 of negated (landingZ - castZ) over (landingX - castX), with the
    sector authored centered on geometry angle zero. This yaw sign is DERIVED (RESEARCH
    assumption A1, MEDIUM confidence) — flag it in the SUMMARY for the plan 05-05 pixel-filter
    visual check; a flip is a one-line sign fix. Where range comes from: row.radius (the
    radiusBySize field is reused as cone range, D5-06). Circle attacks (leapSlam AND
    swordSwirl) take the existing path with ZERO changes — the swirl telegraph appears only at
    its own windup entry because that is when its row arrives (D5-15 sequencing, no
    double-telegraph code needed). Rim/edge extras (straight apex edges) are explicitly
    deferred unless the 05-05 visual check says the sector reads ambiguously (RESEARCH Open
    Question 3). If the file approaches 300 functional LOC, extract the mesh-construction
    helpers (circle + sector) into a sibling module in the same commit.
  </action>
  <verify>
    <automated>grep -c "ATTACK_RENDER" src/game/systems/createTelegraphSystem.ts && grep -c "attackId" src/game/systems/createTelegraphSystem.ts && pnpm vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ATTACK_RENDER" src/game/systems/createTelegraphSystem.ts` >= 2 (import + lookup)
    - `grep -c "attackId" src/game/systems/createTelegraphSystem.ts` >= 3 (interface field, insert population, rebuild comparison)
    - A 6-arg RingGeometry construction exists (source assertion: thetaStart/thetaLength arguments present in two sector meshes)
    - The rebuild branch removes THEN re-inserts on attackId mismatch, and the startedAt re-anchor branch still exists for same-attack re-casts
    - The circle insert path is diff-clean except for the shape-lookup dispatch around it
    - serverSync still green; visual sector orientation check deferred to plan 05-05 (A1)
  </acceptance_criteria>
  <done>Cone rows render an aimed 120° Frost sector filling from the apex; the chain swap rebuilds the telegraph so the swirl's circle warning appears the tick its windup starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server rows → client render | Read-only: telegraph geometry/timing derive entirely from server unit_attack rows (ANIM-01); no client input crosses back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Spoofing (self-inflicted) | telegraph shows wrong danger zone | medium | mitigate | Mirror parity is a release-blocking serverSync test (Task 1); timing re-derived from the server row, never a free-running client timer; rebuild-on-attackId kills the lingering-cone lie |
| T-05-05 | DoS (client perf) | geometry churn on chain rebuilds | low | mitigate | Rebuild reuses the existing remove() disposal path; at most one goliath telegraph per unit key — bounded set, no per-frame allocation |
| T-05-SC | Tampering | package installs | low | accept | Zero new dependencies — no install tasks in this plan |
</threat_model>

<verification>
- `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` green (mirror parity live)
- Grep gates above pass; full typecheck/build owned by the exclusive Wave-3 gate (plan 05-05)
</verification>

<success_criteria>
- ATTACK_RENDER exists, minimal, parity-locked to server ATTACKS
- Cone rows produce an aimed sector telegraph in the D4-14 fill language; circle paths untouched
- Chain swap on the same key visibly replaces cone with circle (mechanism in place; feel verified in 05-05)
</success_criteria>

<output>
Create `.planning/phases/05-swordswing-swordswirl-combo/05-04-SUMMARY.md` when done
</output>
