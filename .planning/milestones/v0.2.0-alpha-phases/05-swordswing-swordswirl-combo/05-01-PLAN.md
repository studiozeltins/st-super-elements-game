---
phase: 05-swordswing-swordswirl-combo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/attacks.ts
  - spacetimedb/src/attackHitbox.ts
  - spacetimedb/src/unitAttackFsm.ts
  - src/game/data/__tests__/attacks.test.ts
  - src/game/data/__tests__/attackHitbox.test.ts
  - src/game/data/__tests__/unitAttackFsm.test.ts
  - src/game/data/__tests__/serverSync.test.ts
autonomous: true
requirements: [ATK-02, ATK-03]
tags: [spacetimedb, combat, fsm, pure-helpers, tdd]

must_haves:
  truths:
    - "resolveCone returns true for a target in front of the apex within range, false for side/back or beyond range, with inclusive edges (ATK-02)"
    - "ATTACKS contains swordSwing (cone, chains into swordSwirl) and swordSwirl (circle) with the exact D5-06/09/10/11/12/13 numbers"
    - "selectAttack gates basics on the basic cooldown (5-arg signature) and swordSwirl is never directly selectable"
    - "walkAttackTransitions ends a coalesced [STRIKE,RECOVERY,IDLE] tick in WINDUP(swordSwirl) with the swing strike resolved and zero cooldown written (ATK-03)"
    - "The swing+swirl chain worst case (680 raw) is asserted below the minimum character HP pool (900) as a live test invariant"
  artifacts:
    - path: "spacetimedb/src/attackHitbox.ts"
      provides: "resolveCone pure resolver"
      exports: ["resolveCone"]
    - path: "spacetimedb/src/attacks.ts"
      provides: "swordSwing + swordSwirl entries, chainsInto?/coneMinDot? on AttackSpec, 5-arg selectAttack, leapSlam 5.5s retune"
      contains: "chainsInto"
    - path: "spacetimedb/src/unitAttackFsm.ts"
      provides: "walkAttackTransitions pure transition applier (Wave-0 chain-glue seam)"
      exports: ["walkAttackTransitions"]
    - path: "src/game/data/__tests__/serverSync.test.ts"
      provides: "damage-seed assertions, no-one-shot invariant, chain integrity, swirl-not-selectable"
      contains: "swordSwirl"
  key_links:
    - from: "spacetimedb/src/attackHitbox.ts"
      to: "spacetimedb/src/goliathAI.ts"
      via: "resolveCone reuses isWithinForwardArc (ATK-06 geometry-reuse mandate)"
      pattern: "isWithinForwardArc"
    - from: "spacetimedb/src/unitAttackFsm.ts"
      to: "spacetimedb/src/attacks.ts"
      via: "walkAttackTransitions looks up spec.chainsInto in ATTACKS and enters the chained windup"
      pattern: "chainsInto"
---

<objective>
Build the entire Phase-5 server PURE layer test-first: the `resolveCone` hitbox resolver
(ATK-02, D5-05/D5-06), the two new `ATTACKS` registry entries with the chain field
(ATK-03, D5-01/D5-02/D5-03), the per-role `selectAttack` split (D5-08/D5-09), and the
`walkAttackTransitions` pure transition applier — the Wave-0 chain-glue test seam that makes
the coalesced-tick chain test (this phase's highest-value test) a plain vitest unit test.

Purpose: everything gameplay-critical in this phase is deterministic pure logic. Landing it
first, fully tested, means the Wave-2 glue plan is a thin wiring exercise and the chain/break
contract (RESEARCH Pitfall 2) is locked by tests before any reducer code moves.

Output: extended `attacks.ts`, `attackHitbox.ts`, `unitAttackFsm.ts` + green extended test
suite (attacks, attackHitbox, unitAttackFsm, serverSync).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-swordswing-swordswirl-combo/05-CONTEXT.md
@.planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md
@.planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md
@spacetimedb/src/attacks.ts
@spacetimedb/src/attackHitbox.ts
@spacetimedb/src/unitAttackFsm.ts
@spacetimedb/src/goliathAI.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 5 (this plan lands the starred ones):

- ★ `resolveCone(px, pz, apexX, apexZ, aimX, aimZ, range, minDot): boolean` — `spacetimedb/src/attackHitbox.ts`
- ★ `AttackSpec.chainsInto?: string` and `AttackSpec.coneMinDot?: number` — `spacetimedb/src/attacks.ts`
- ★ `ATTACKS.swordSwing` and `ATTACKS.swordSwirl` entries — `spacetimedb/src/attacks.ts`
- ★ `selectAttack(distance, nowMicros, skillCooldownUntilMicros, basicCooldownUntilMicros, available)` — 5-arg signature
- ★ `walkAttackTransitions(...)` pure transition applier + its `TransitionPlan` result type — `spacetimedb/src/unitAttackFsm.ts`
- `basicCooldownUntilMicros: t.u64().default(0n)` column on `unit_attack` (plan 05-03)
- `src/game/data/attacks.ts` client mirror: `AttackRenderSpec`, `ATTACK_RENDER` (plan 05-04)
- Cone SECTOR telegraph branch + rebuild-on-attackId in `createTelegraphSystem.ts` (plan 05-04)
- `animateSwing` / `animateSwirl` procedural clips in `createGoliathRenderer.ts` (plan 05-02)
- `playSwing` / `playSwirl` WebAudio SFX + per-attack juice branch (plan 05-02)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveCone pure resolver, test-first (ATK-02, D5-05/D5-06)</name>
  <files>spacetimedb/src/attackHitbox.ts, src/game/data/__tests__/attackHitbox.test.ts</files>
  <read_first>
    - spacetimedb/src/attackHitbox.ts (current resolveCircleHit + knockbackDisplacement, doc-comment style, header promise "cone (Phase 5) reuses isWithinForwardArc")
    - spacetimedb/src/goliathAI.ts lines 69-88 (isWithinForwardArc exact signature and degenerate-edge behavior — reuse VERBATIM, do not reimplement)
    - spacetimedb/src/combatMath.ts (distanceBetween)
    - src/game/data/__tests__/attackHitbox.test.ts (cross-boundary import pattern + exact-boundary test style)
    - .planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md section "spacetimedb/src/attackHitbox.ts"
  </read_first>
  <behavior>
    - Target on the aim axis at distance range-0.1 from apex → true (inside)
    - Target on the aim axis at EXACTLY range → true (inclusive range edge, matches resolveCircleHit's <=)
    - Target on the aim axis at range+0.01 → false (beyond range)
    - Target within range but directly BEHIND the apex (opposite aim) → false (SC1 side/back escape)
    - Target within range at EXACTLY the 60-degree half-angle edge (dot == minDot 0.5) → true (inclusive arc edge)
    - Target within range just past the half-angle edge (dot slightly < 0.5) → false
    - Target AT the apex (px==apexX, pz==apexZ) → true (point-blank, directionLength < 1e-6 branch)
    - Zero-length aim vector (aimX==0, aimZ==0) with target within range → true; beyond range → false (degrades to 360° range check, headingLength < 1e-6 branch)
  </behavior>
  <action>
    RED first: add a `resolveCone` describe block to attackHitbox.test.ts covering every case in
    the behavior list, importing `resolveCone` cross-boundary from
    `../../../../spacetimedb/src/attackHitbox` in the existing style. Run — must fail (symbol
    missing). Then GREEN: in attackHitbox.ts add
    `resolveCone(px, pz, apexX, apexZ, aimX, aimZ, range, minDot): boolean` which returns false
    when `distanceBetween(px, pz, apexX, apexZ)` exceeds `range`, otherwise delegates to
    `isWithinForwardArc(aimX, aimZ, apexX, apexZ, px, pz, minDot)` — imported from
    `./goliathAI` in the same pure-sibling style as the existing `distanceBetween` import from
    `./combatMath`. The aim vector is landing minus cast, NOT normalized (the arc test
    normalizes internally) per D5-05. Add a block doc-comment in the resolveCircleHit style
    stating: inclusive range and arc edges, minDot = cos(half-angle) with 0.5 = the 120° full
    angle from D5-06, and the degenerate semantics (zero aim → 360° range check; target on apex
    → hit). Update the file-header comment so the Phase-5 cone promise becomes real code
    (no-dead-code rule). No changes to resolveCircleHit or knockbackDisplacement.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attackHitbox.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/attackHitbox.test.ts` exits 0 with new resolveCone tests present and passing (all 8 behavior cases asserted)
    - `grep -c "export function resolveCone" spacetimedb/src/attackHitbox.ts` returns 1
    - `grep -c "isWithinForwardArc" spacetimedb/src/attackHitbox.ts` returns >= 2 (import + call) — no local re-implementation of the dot-product test
    - attackHitbox.ts contains no clock or RNG usage (pure module contract preserved)
  </acceptance_criteria>
  <done>resolveCone exists, reuses isWithinForwardArc + distanceBetween, and all edge/degenerate cases pass in the extended attackHitbox suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ATTACKS entries + chain data + 5-arg selectAttack + literal updates (D5-01..D5-03, D5-06, D5-08..D5-13)</name>
  <files>spacetimedb/src/attacks.ts, src/game/data/__tests__/attacks.test.ts, src/game/data/__tests__/serverSync.test.ts</files>
  <read_first>
    - spacetimedb/src/attacks.ts (AttackSpec interface lines 23-39, leapSlam entry 41-59, UNIT_ATTACKS 62-64, selectAttack 76-94)
    - src/game/data/__tests__/attacks.test.ts (FULL — the toEqual literal at 20-37, default-list assert at 51, 4-arg selectAttack calls at 72-90 ALL break and must be updated in this task, RESEARCH Pitfall 7)
    - src/game/data/__tests__/serverSync.test.ts lines 320-371 (ATTACKS parity block + damage-arithmetic assertion style at 360-366)
    - src/game/data/characters.ts (CHARACTERS maxHealth pools — minimum is 900 at zefs, used by the invariant test)
    - spacetimedb/src/enemyStats.ts lines 70-74 (GOLIATH_SIZE_STATS contactDamage 90/130/170)
    - .planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md section "Code Examples" (exact authored entry shape)
  </read_first>
  <behavior>
    - selectAttack with a basic in band and nowMicros < basicCooldownUntilMicros → null (basics now gate, D5-08)
    - selectAttack with nowMicros EXACTLY equal to basicCooldownUntilMicros → returns the basic (inclusive >=, boundary style of the existing cooldown tests)
    - selectAttack at distance 5.0 with the skill on cooldown → null (swing band is 0..3.5, chase sentinel D5-09/D4-13)
    - selectAttack at distance 2.0 with both off cooldown → 'leapSlam' (skill preferred over basic in band)
    - serverSync: swing per-size damage rounds to [135, 195, 255]; swirl to [225, 325, 425] (D5-10)
    - serverSync: max over sizes of (swing + swirl rounded damage) < min over CHARACTERS of maxHealth — the live no-one-shot invariant (680 < 900)
    - serverSync: every chainsInto value resolves to an ATTACKS key and following the chain links terminates without revisiting an attack (no cycles)
    - serverSync: UNIT_ATTACKS goliath default list does NOT contain 'swordSwirl' (D5-03) and DOES contain 'swordSwing'
  </behavior>
  <action>
    In attacks.ts: (1) add two OPTIONAL fields to AttackSpec — `chainsInto?: string` (D5-01) and
    `coneMinDot?: number` — both ABSENT on leapSlam so its toEqual literal keeps working with
    only the cooldown edit. (2) Retune `leapSlam.cooldownMicros` from 3_500_000n to 5_500_000n
    (D5-09), keeping the seed-then-tune comment style citing D5-09. (3) Add the `swordSwing`
    entry: shape 'cone', role 'basic', windupTicks 4, activeTicks 1, graceTicks 1,
    recoveryTicks 8 (inert — always chains), cooldownMicros 2_500_000n (inert — never reaches
    IDLE), radiusBySize [3.0, 3.5, 4.0] (reused as CONE RANGE, D5-06), damageMultiplier 1.5
    (D5-10), minBand 0, maxBand 3.5 (basic melee band, D5-09), knockback 0, stunTicks 4
    (stun-only 0.6s, D5-12), move 'none', poiseThreshold 600, chainsInto 'swordSwirl',
    coneMinDot 0.5 (cos 60° → 120° full angle, D5-06). (4) Add the `swordSwirl` entry: shape
    'circle', role 'basic' (finishing attack's role writes the basic cooldown, D5-13),
    windupTicks 5 (0.75s chain warning, D5-11), activeTicks 1, graceTicks 1, recoveryTicks 8
    (the chain's single punish window, D5-11), cooldownMicros 2_500_000n (THE chain cooldown,
    D5-13), radiusBySize [4.0, 4.5, 5.0] (escape-race seed, RESEARCH Pitfall 9 — document the
    ~6.3u escape-budget arithmetic in the comment so tuning is informed), damageMultiplier 2.5
    (D5-10), minBand 0 and maxBand 3.5 (INERT per D5-03 — authored to swing's values for
    parity sanity), knockback 4.5 (throw clear, D5-12), stunTicks 0, move 'none',
    poiseThreshold 600 — NO chainsInto, NO coneMinDot. Every seeded number carries a short
    comment citing its D5-xx decision and marking it a playtest seed. (5) Change UNIT_ATTACKS
    goliath default to ['leapSlam', 'swordSwing'] (D5-03 — swirl reachable ONLY via chain).
    (6) Extend selectAttack to the 5-arg signature (distance, nowMicros,
    skillCooldownUntilMicros, basicCooldownUntilMicros, available): the skill branch is
    unchanged; before recording a basic candidate, skip when nowMicros <
    basicCooldownUntilMicros (D5-08).
    In attacks.test.ts (SAME task, Pitfall 7 — refactor in place, no dead code): update the
    leapSlam toEqual literal to cooldownMicros 5_500_000n; update the default-list assertion to
    ['leapSlam', 'swordSwing']; migrate every selectAttack call to the 5-arg signature; add the
    four selectAttack behaviors above with exact-boundary assertions in the existing style.
    In serverSync.test.ts: add the four parity behaviors above next to the existing leapSlam
    damage-arithmetic test (360-366 style). The existing it.each tick-invariant block
    auto-covers the two new entries — do not modify it.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/serverSync.test.ts` exits 0
    - `grep -c "swordSwing" spacetimedb/src/attacks.ts` >= 2 and `grep -c "swordSwirl" spacetimedb/src/attacks.ts` >= 2 (entry + chain/list references)
    - `grep -c "5_500_000n" spacetimedb/src/attacks.ts` returns 1 (slam retune, D5-09)
    - selectAttack has exactly 5 parameters; no remaining 4-arg call in attacks.test.ts
    - serverSync asserts [135, 195, 255] and [225, 325, 425] damage arrays and the chain-vs-min-HP invariant as a computed comparison (not a hardcoded 680 < 900 tautology — derive both sides from ATTACKS, GOLIATH_SIZE_STATS, CHARACTERS)
    - leapSlam entry gains NO new fields (chainsInto/coneMinDot absent on it)
  </acceptance_criteria>
  <done>Both entries authored with locked D5 numbers, chain is data, basics gate on their own cooldown, all existing literals updated, parity suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: walkAttackTransitions pure applier + coalesced-tick chain tests (D5-01/D5-04, Pitfalls 1+2, Wave-0 seam)</name>
  <files>spacetimedb/src/unitAttackFsm.ts, src/game/data/__tests__/unitAttackFsm.test.ts</files>
  <read_first>
    - spacetimedb/src/unitAttackFsm.ts (FULL — enterWindup signature, dueAttackTransitions, ATTACK_STATE_* constants; this stays the pure home of FSM math)
    - spacetimedb/src/unitAttacks.ts lines 169-205 (the CURRENT transition walk being modeled — STRIKE teleport line ~183, RECOVERY resolve, IDLE cooldown write; the glue in plan 05-03 will delegate to this new helper)
    - src/game/data/__tests__/unitAttackFsm.test.ts (injected TICK=150_000n / NOW constants, enterWindup field-by-field assertion style)
    - .planning/phases/05-swordswing-swordswirl-combo/05-RESEARCH.md "Pattern 1" (chain swap + break analysis) and "Pitfall 2"
  </read_first>
  <behavior>
    - Walk of [STRIKE] for a spec with move 'leap' → plan.teleportToLanding true; with move 'none' → false (Pitfall 1 gate); plan.emitStrike true in both; strikeSnapshot carries the OLD attack's attackId/landing/radius
    - Walk of [STRIKE, RECOVERY, IDLE] for swordSwing (chainsInto set): final row.state == WINDUP, row.attackId == 'swordSwirl', plan.resolveStrike true (swing resolved, FSM-05 resolve-never-drop), row.cooldownUntilMicros AND row.basicCooldownUntilMicros both UNCHANGED from input (the stale IDLE step never ran — the break contract, Pitfall 2)
    - Chain entry row fields equal enterWindup(now, tick, ATTACKS.swordSwirl, sizeIndex, posX, posZ, posX, posZ) spread — castX==landingX==posX and castZ==landingZ==posZ (self-centered, D5-04), strikeResolved false, swirl strikeAtMicros strictly > now (next tick picks it up; a single pass cannot loop)
    - Walk of [STRIKE, RECOVERY, IDLE] for a spec WITHOUT chainsInto and role 'skill' → final state IDLE, cooldownUntilMicros == now + spec.cooldownMicros, basicCooldownUntilMicros unchanged
    - Same walk with role 'basic' (swordSwirl) → basicCooldownUntilMicros == now + 2_500_000n, cooldownUntilMicros unchanged (per-role split, D5-08/D5-13)
    - Walk of [RECOVERY] with row.strikeResolved already true → plan.resolveStrike false (no double resolution)
    - Leap spec WITH a hypothetical chainsInto in one coalesced walk → chain windup cast equals the LANDING coords (teleport applied before chain entry — generic correctness)
  </behavior>
  <action>
    RED first: add a `walkAttackTransitions` describe block to unitAttackFsm.test.ts asserting
    every behavior above with the existing injected NOW/TICK constants and plain-object rows
    (include basicCooldownUntilMicros on the input row). Then GREEN: in unitAttackFsm.ts export
    a pure function `walkAttackTransitions(row, transitions, spec, now, tick, sizeIndex, posX,
    posZ)` returning a TransitionPlan object: { row (final row after the walk), emitStrike
    (STRIKE step occurred), teleportToLanding (STRIKE occurred AND spec.move === 'leap'),
    resolveStrike (RECOVERY step occurred with row.strikeResolved false), strikeSnapshot (the
    row as of the pre-chain-swap point, or null when no STRIKE/RECOVERY step ran — the glue
    uses it for the unconditional attack_strike event fields and for strike resolution),
    chainedInto (spec.chainsInto when the chain entry fired, else null) }. Walk semantics
    mirror the current glue: STRIKE sets state STRIKE; RECOVERY (when chainsInto is authored)
    spreads enterWindup over the row with attackId = spec.chainsInto and STOPS the walk
    immediately (the remaining stale steps belong to the OLD attack's deadlines — Pitfall 2);
    RECOVERY without a chain sets state RECOVERY + strikeResolved true; IDLE writes now +
    spec.cooldownMicros into basicCooldownUntilMicros when spec.role is 'basic', else into
    cooldownUntilMicros (D5-08). Track the effective position locally: when a STRIKE step ran
    with move 'leap', the chain entry (and returned position intent) uses the landing coords
    instead of posX/posZ. Look up the chained spec via ATTACKS imported from './attacks' (pure
    sibling import, same style as attackHitbox importing combatMath). dueAttackTransitions and
    enterWindup themselves stay UNCHANGED (verified sufficient by research). Keep the module
    clock/RNG-free — now and tick are injected bigints.
  </action>
  <verify>
    <automated>pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts && pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/attackHitbox.test.ts src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run src/game/data/__tests__/unitAttackFsm.test.ts` exits 0 with the coalesced-tick chain test present: input transitions [STRIKE, RECOVERY, IDLE] on swordSwing ends state WINDUP + attackId 'swordSwirl' + both cooldown fields untouched
    - `grep -c "export function walkAttackTransitions" spacetimedb/src/unitAttackFsm.ts` returns 1
    - The per-role cooldown split is asserted for BOTH roles (skill → cooldownUntilMicros, basic → basicCooldownUntilMicros)
    - unitAttackFsm.ts stays under 300 functional LOC and imports only pure siblings
    - Full quick trio + serverSync green (second command)
  </acceptance_criteria>
  <done>The chain/break/cooldown contract is a pure, fully unit-tested function ready for the plan 05-03 glue to delegate to; the phase's highest-value test (coalesced-tick chain) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server reducers | UNCHANGED — this plan adds zero client-callable surface; all new logic is pure functions consumed by the scheduled worldTick |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | chain/cone resolution | high | mitigate | All selection, chain sequencing, and hit resolution stay server-side pure functions with injected now/tick; no new reducer args (ASVS V4) |
| T-05-02 | Tampering/Repudiation | determinism (module code) | medium | mitigate | New code takes injected bigints; verify-gate greps spacetimedb/src for clock/RNG usage in plan 05-03/05-05 |
| T-05-03 | DoS (gameplay) | damage retunes crossing one-shot line | medium | mitigate | serverSync live invariant: max chain damage < min CHARACTERS maxHealth (Task 2) |
| T-05-SC | Tampering | package installs | low | accept | Zero new dependencies is a locked phase constraint — no install tasks exist |
</threat_model>

<verification>
- `pnpm vitest run src/game/data/__tests__/attacks.test.ts src/game/data/__tests__/attackHitbox.test.ts src/game/data/__tests__/unitAttackFsm.test.ts src/game/data/__tests__/serverSync.test.ts` — all green
- No file in spacetimedb/src gained clock/RNG usage; all new functions take injected now/tick
- leapSlam entry unchanged except cooldownMicros 5_500_000n
</verification>

<success_criteria>
- resolveCone (ATK-02 geometry) and walkAttackTransitions (ATK-03 chain contract) exist as pure, tested exports
- Both ATTACKS entries carry the exact locked D5-06/09/10/11/12/13 numbers with decision-cited seed comments
- The no-one-shot invariant and chain-integrity checks are live serverSync assertions
- Zero glue/schema/client changes yet — this plan is importable by vitest only
</success_criteria>

<output>
Create `.planning/phases/05-swordswing-swordswirl-combo/05-01-SUMMARY.md` when done
</output>
