---
phase: 05-swordswing-swordswirl-combo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/createGoliathRenderer.ts
  - src/game/createGame.ts
  - src/game/audio/createAudioSystem.ts
autonomous: true
requirements: [ATK-02, ATK-03]
tags: [client, threejs, animation, webaudio, juice]

must_haves:
  truths:
    - "The goliath plays a distinct wind-back → slash-lunge clip during swordSwing and a coil → 360° spin clip during swordSwirl (SC3, D5-16)"
    - "No clip transform survives past the attack — movement/death restore neutral every frame (04-05 contract, Pitfall 10)"
    - "attack_strike juice differs per attackId: swing lightest (no shockwave), swirl medium, leapSlam keeps the existing full package (ANIM-04)"
    - "Swing and swirl each have a procedural WebAudio SFX variant smaller than the slam's"
  artifacts:
    - path: "src/game/systems/createGoliathRenderer.ts"
      provides: "animateAttack dispatcher branching on view.attackId + two new clips + extended neutral restore"
      contains: "swordSwirl"
    - path: "src/game/audio/createAudioSystem.ts"
      provides: "playSwing + playSwirl procedural SFX on the AudioSystem interface"
      exports: ["playSwing", "playSwirl"]
    - path: "src/game/createGame.ts"
      provides: "handleAttackStrike per-attack juice branch keyed by strike.attackId"
      contains: "swordSwing"
  key_links:
    - from: "src/game/systems/createGoliathRenderer.ts"
      to: "src/game/systems/createEntityRenderer.ts"
      via: "AttackAnimationView.attackId (ALREADY exists — zero interface change, ANIM-03)"
      pattern: "attackId"
    - from: "src/game/createGame.ts"
      to: "src/game/audio/createAudioSystem.ts"
      via: "juice branch calls playSwing/playSwirl per strike.attackId"
      pattern: "playSw(ing|irl)"
---

<objective>
Land the render-only animation + juice half of the phase: two distinct procedural goliath
clips on the existing `animateAttack` hook (D5-16, ANIM-03), per-attack strike juice keyed by
`attack_strike.attackId` (ANIM-04), and two procedural WebAudio SFX variants — with the
neutral-restore contract extended so nothing leaks (RESEARCH Pitfall 10).

Purpose: SC3 requires each attack to read as its own move. `AttackAnimationView` already
carries `attackId` and the `attack_strike` event already carries `attackId` (verified — zero
interface/schema change), so this plan is pure client code and runs in Wave 1 parallel to the
server pure layer (zero file overlap).

Output: branch-per-attack clips, juice, and SFX; codebase still compiles against the EXISTING
server (the new attackIds simply never arrive until plan 05-03 ships).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-swordswing-swordswirl-combo/05-CONTEXT.md
@.planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md
@src/game/systems/createGoliathRenderer.ts
@src/game/systems/createEntityRenderer.ts
@src/game/createGame.ts
@src/game/audio/createAudioSystem.ts
</context>

## Artifacts this phase produces

New symbols created across Phase 5 (this plan lands the starred ones):

- `resolveCone(...)` pure resolver — `spacetimedb/src/attackHitbox.ts` (plan 05-01)
- `AttackSpec.chainsInto?` / `AttackSpec.coneMinDot?` + `ATTACKS.swordSwing` / `ATTACKS.swordSwirl` + 5-arg `selectAttack` (plan 05-01)
- `walkAttackTransitions(...)` pure transition applier (plan 05-01)
- `basicCooldownUntilMicros: t.u64().default(0n)` column on `unit_attack` (plan 05-03)
- `src/game/data/attacks.ts` client mirror: `AttackRenderSpec`, `ATTACK_RENDER` (plan 05-04)
- Cone SECTOR telegraph branch + rebuild-on-attackId in `createTelegraphSystem.ts` (plan 05-04)
- ★ `animateSwing` / `animateSwirl` procedural clips + `animateAttack` attackId dispatcher — `src/game/systems/createGoliathRenderer.ts`
- ★ `playSwing` / `playSwirl` on the `AudioSystem` interface — `src/game/audio/createAudioSystem.ts`
- ★ Per-attack juice branch in `handleAttackStrike` — `src/game/createGame.ts`

<tasks>

<task type="auto">
  <name>Task 1: Two procedural clips + attackId dispatcher + neutral-restore extension (D5-16, ANIM-03, Pitfall 10)</name>
  <files>src/game/systems/createGoliathRenderer.ts</files>
  <read_first>
    - src/game/systems/createGoliathRenderer.ts (FULL — rig part lookups at 36-60, neutral-restore contract in animateMovement at 81-93 and animateDeath at 95-99, existing leapSlam clip body at 100-123, applyCrouch)
    - src/game/systems/createEntityRenderer.ts lines 15-45 (AttackAnimationView shape — attackId ALREADY present at :16; hook optional at :40; renderer owns group rotation via lookAt at 267-273)
    - .planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md section "createGoliathRenderer.ts"
  </read_first>
  <action>
    Refactor `animateAttack(view)` into a dispatcher: view.attackId 'swordSwing' →
    `animateSwing(view)`, 'swordSwirl' → `animateSwirl(view)`, anything else → the existing
    leapSlam clip body moved verbatim into `animateLeapSlam(view)` (refactor in place, no
    duplicate). Both new clips branch on `view.phase` and drive everything from
    `view.phaseProgress` (0..1, server-derived) — NEVER `travelFraction` (leap-specific; these
    are move 'none' attacks whose mesh does not travel). animateSwing (D5-16): windup =
    arm/torso wind-back — rotate the striking arm back and up (e.g. rightArm rotation
    eased by phaseProgress squared, mirroring the existing progress² ease-in) plus a light
    applyCrouch; strike = fast forward slash — sweep the arm forward through the strike phase
    with a slight torso lean; recovery = eased settle toward neutral (rarely seen — swing
    chains — but must be safe). animateSwirl (D5-16): windup = torso coil — wind the torso/body
    child node's yaw AGAINST the coming spin direction; strike = one full 360° yaw of that
    torso/body node across phaseProgress; recovery = heavy eased settle (the chain's 1.2s
    punish window reads as exhaustion). The spin MUST rotate a body/torso child node obtained
    via the existing named-part lookup pattern, never the outer group (the entity renderer owns
    group rotation via lookAt — spinning the group fights it). CRITICAL (Pitfall 10): every
    transform the new clips touch that the existing per-frame restore does not already
    re-write (torso/body yaw, any arm rotation axis beyond the rotation.x the leg-swing lines
    already reset) must be explicitly zeroed in BOTH animateMovement and animateDeath, in the
    same style as applyCrouch(0) — a death mid-spin or an ended chain must leave a neutral rig.
    Camp enemies are untouched (the hook stays optional). Keep the file under 300 functional
    LOC — extract the three clip functions above the adapter literal if needed.
  </action>
  <verify>
    <automated>grep -c "animateSwing\|animateSwirl" src/game/systems/createGoliathRenderer.ts && pnpm vitest run src/game/data/__tests__/serverSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "swordSwing" src/game/systems/createGoliathRenderer.ts` >= 1 and `grep -c "swordSwirl" src/game/systems/createGoliathRenderer.ts` >= 1 (dispatcher branches exist)
    - `grep -c "travelFraction" src/game/systems/createGoliathRenderer.ts` is UNCHANGED from before the edit (new clips did not adopt the leap-only helper)
    - animateMovement AND animateDeath both reset every transform the new clips introduce (source assertion: the torso/body yaw reset appears in both functions)
    - The existing leapSlam clip body is moved, not duplicated (exactly one applyCrouch-based windup crouch block for the slam)
    - File remains <= 300 functional LOC
    - Visual confirmation deferred to the plan 05-05 migrated-DB playtest checkpoint (SC3) — noted in SUMMARY
  </acceptance_criteria>
  <done>animateAttack dispatches per attackId; swing and swirl have distinct phase-driven clips; neutral restore covers all new transforms; slam behavior byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Per-attack strike juice + two WebAudio SFX variants (ANIM-04, D5-12 feel support)</name>
  <files>src/game/createGame.ts, src/game/audio/createAudioSystem.ts</files>
  <read_first>
    - src/game/createGame.ts lines 1391-1405 (handleAttackStrike — the existing full-juice body that becomes the leapSlam branch) and lines 1012-1030 (shake mechanics, SHAKE_START_MAGNITUDE)
    - src/game/audio/createAudioSystem.ts (FULL — playSlam recipe at 30-71: running-context guard, oscillator sweep + gain envelope, filtered-noise burst; AudioSystem interface at 6-10)
    - .planning/phases/05-swordswing-swordswirl-combo/05-PATTERNS.md sections "createGame.ts" and "createAudioSystem.ts"
  </read_first>
  <action>
    In createAudioSystem.ts add two methods to the AudioSystem interface and factory, both
    following the playSlam recipe (silent no-op unless context.state is 'running', envelopes
    via exponentialRampToValueAtTime, fire-and-forget): `playSwing` = a short (~0.15-0.25s)
    bandpass-filtered white-noise sweep — a light whoosh, clearly quieter than the slam;
    `playSwirl` = a longer (~0.4s) noise swell plus a low sine thump softer than the slam's
    70→40Hz hit. Exact frequencies/gains are seeds (Claude discretion per CONTEXT) — the
    contract is: swing lightest, swirl medium, slam unchanged and loudest.
    In createGame.ts, branch `handleAttackStrike(strike)` on strike.attackId: 'swordSwing' →
    small burst (about 10 particles, same 0x86e2ff Frost color), telegraphSystem.flashStrike,
    a light shake (seed ~0.15, vs the slam's 0.45), audioSystem.playSwing, and NO shockwave —
    a circular shockwave misreads a cone (RESEARCH Pattern 6); 'swordSwirl' → medium burst
    (about 18 particles), spawnShockwave at strike.radius (correct — the swirl IS a circle),
    flashStrike, medium shake (seed ~0.3), audioSystem.playSwirl; default (leapSlam and any
    unknown attackId) → the EXISTING full package verbatim (burst 26 + shockwave + flash +
    SHAKE_START_MAGNITUDE + playSlam). Keep lastStrike bookkeeping and the once-per-event
    guarantee comment intact for all branches. Juice magnitudes live as named constants in the
    handler, NOT in the plan-05-04 client mirror (RESEARCH Open Question 2 resolution — they
    are not parity material). No App.tsx change (the event hook already passes strike through
    the single subscription).
  </action>
  <verify>
    <automated>grep -c "playSwing\|playSwirl" src/game/audio/createAudioSystem.ts && grep -c "swordSwing\|swordSwirl" src/game/createGame.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "playSwing" src/game/audio/createAudioSystem.ts` >= 2 (interface + implementation) and same for playSwirl
    - `grep -c "swordSwing" src/game/createGame.ts` >= 1 and `grep -c "swordSwirl" src/game/createGame.ts` >= 1 (juice branches exist)
    - The swing branch contains NO spawnShockwave call; the swirl branch DOES call spawnShockwave with strike.radius (source assertion)
    - The default branch is byte-equivalent to the previous full-juice body (slam feel unchanged)
    - src/App.tsx untouched (git diff shows no change to it)
    - Audible/feel confirmation deferred to the plan 05-05 playtest checkpoint
  </acceptance_criteria>
  <done>Three distinct juice tiers keyed by attackId (swing lightest, swirl medium, slam full) with matching procedural SFX; zero schema/subscription changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server rows/events → client render | Read-only render path; client renders whatever the server-authoritative tables/events say — no write surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Spoofing (self-inflicted) | juice/clips desync from server truth | low | mitigate | Clips driven only by server-derived view.phaseProgress (ANIM-01); juice fires once per server attack_strike event (ANIM-04) |
| T-05-05 | DoS (client perf) | per-frame clip work / audio node churn | low | mitigate | Clips are pure transform math on cached node refs; SFX nodes are short-lived fire-and-forget like playSlam; no per-row identity compares added (CLAUDE.md perf rules) |
| T-05-SC | Tampering | package installs | low | accept | Zero new dependencies — no install tasks in this plan |
</threat_model>

<verification>
- Grep gates above pass; `pnpm vitest run src/game/data/__tests__/serverSync.test.ts` stays green (no test files touched)
- Full typecheck/build deferred to the exclusive Wave-3 gate (plan 05-05) to avoid parallel-wave build flake
</verification>

<success_criteria>
- Each of the three goliath attacks renders a distinct clip and a distinct juice/SFX tier
- The neutral-restore contract provably covers every new transform (no twisted rig after death/chain)
- Zero server, schema, subscription, or App.tsx changes
</success_criteria>

<output>
Create `.planning/phases/05-swordswing-swordswirl-combo/05-02-SUMMARY.md` when done
</output>
