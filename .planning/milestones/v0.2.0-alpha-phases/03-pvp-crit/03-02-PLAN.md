---
phase: 03-pvp-crit
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/module_bindings
autonomous: false
requirements: [CRIT-07]
must_haves:
  truths:
    - "The additive migrate-publish of the extended pvp_hit + reworked attackPlayer SUCCEEDS on the live MIGRATED local DB (no --delete-data) — this publish is itself the acceptance test for D3-01's open question (event-table additive columns with .default())."
    - "Post-publish, the pvp_hit table schema shows exactly 4 columns (target, amount, attacker, isCrit) and the attackPlayer reducer takes exactly 3 params (targetIdentity, isSkill, comboCount) — verified structurally via spacetime describe, since event tables never store rows."
    - "Two-client migrated-DB playtest: a PVP crit floats the truthful crit-kind number over the victim on every subscribed client (attacker upgrade, victim pvpCrit, spectator crit) with exactly ONE number per event per viewer (SC2)."
    - "Distinct per-character crit is visible in PVP (high-critRate dps crits far more often than a 0.10 tank) and the shard-theft loop still resolves correctly on a killing blow, which floats the FULL computed amount (SC3, D3-02)."
    - "Maincloud is untouched this phase (STATE ops invariant — deferred to a user-facing prod point)."
  artifacts:
    - src/module_bindings
  key_links:
    - "Deploy order is fixed and atomic: publish local (additive) → pnpm run spacetime:generate → pnpm build → BOTH playtest clients load the fresh dist/ — a stale client against the new module is the reducer-arg-mismatch failure class."
    - "spacetime describe 2d-impact-game-fr9ti table pvp_hit --json is the structural migrate check (rows can never be inspected — event table)."
  prohibitions:
    - "NEVER --delete-data — the local DB is the migrated playtest fixture and a wipe destroys account/account_link with no restore path."
    - "Do not publish maincloud this phase (deferred to prod point; when it happens: same additive publish, never a wipe)."
    - "Do not 'fix' pre-existing playtest observations in this slice: doubled remote skill/projectile VFX (skillCast/rangedAttack are still double-subscribed) and occasional attacker-only local numbers on server-rejected edge hits (range/safe-zone races) are NOT Phase-3 regressions — flag them, capture todos, leave the code alone."
    - "Do not use the spacetime:publish npm scripts (wrong --module-path); publish with explicit --module-path spacetimedb."
---

<objective>
Deploy the Phase-3 slice to the live migrated local DB and verify CRIT-07 end-to-end with the human two-client playtest — the deploy/verify capstone of the crit trilogy. The publish doubles as the migrate-acceptance test for the additively extended `pvp_hit` event table (D3-01 open question); the playtest is the phase's real UAT (Phase-2 learning: server-authoritative UX bugs ship past a fully green suite).

Purpose: proves SC2/SC3 on real infrastructure — truthful un-spoofable PVP crit numbers on every client, distinct per-character crit in PVP, and an intact shard-theft loop on killing blows.
Output: published local module, regenerated bindings, fresh production build served by laragon, and a human-approved playtest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pvp-crit/03-CONTEXT.md
@.planning/phases/03-pvp-crit/03-RESEARCH.md
@.planning/phases/03-pvp-crit/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

(Phase-level artifact list — code symbols were produced by Plan 03-01; this plan produces deployed/derived artifacts only.)

- **Deployed module:** `2d-impact-game-fr9ti` on `local` with the 4-column `pvp_hit` (`target`, `amount`, `attacker`, `isCrit`) and the 3-param `attackPlayer` (`targetIdentity`, `isSkill`, `comboCount`).
- **Regenerated:** `src/module_bindings/` (post-publish re-run of `pnpm run spacetime:generate`; expected no-op vs Plan 03-01's output, committed if the generator reorders anything).
- **Fresh `dist/`** production build served by laragon for the LAN playtest.
- No new source files, tables, reducers, or client symbols beyond Plan 03-01's list.

<tasks>

<task type="auto">
  <name>Task 1: Deploy — additive local publish (the migrate-acceptance test), structural verify, generate, build</name>
  <files>src/module_bindings</files>
  <read_first>
    - .planning/phases/03-pvp-crit/03-RESEARCH.md §"Common Pitfalls" 5–6 and §"Open Questions" 1 — the deploy-order contract and the publish-rejection fallback ladder.
    - .planning/phases/03-pvp-crit/03-01-SUMMARY.md — what Plan 03-01 actually shipped (any deviations affect the describe checks below).
    - CLAUDE.md §"This Project: Environments & Deployment" — module path is ./spacetimedb (the spacetime:publish npm scripts point at server/ and FAIL); LAN client routing.
  </read_first>
  <action>
    1. **Preflight:** `spacetime server ping local` — if the local server is down, start `spacetimedb-standalone` (or `spacetime start`) and re-ping before anything else.
    2. **Publish (additive migrate — the D3-01 acceptance test):** `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes`. NO data-wipe flags of any kind — this DB is the migrated playtest fixture.
    3. **If the publish is REJECTED on the pvp_hit schema** (RESEARCH Open Q1 fallback ladder, in order): (a) retry with the two new columns stripped of their `.default(...)` calls (event tables may skip the default requirement — rows are never stored, nothing to backfill); (b) if still rejected, use the empty-table escape hatch: `spacetime sql 2d-impact-game-fr9ti --server local "DELETE FROM pvp_hit"` is unnecessary (always empty) — instead temporarily remove the pvp_hit table from the schema, publish, re-add it with all 4 columns, publish again (the enemy_carry precedent: STDB drops empty tables fine). Record whichever fallback was needed in the SUMMARY — it answers D3-01's open question for future event-table migrations.
    4. **Structural verify (Pitfall 6 — rows can never be inspected):** `spacetime describe 2d-impact-game-fr9ti table pvp_hit --json` must list the 4 columns target/amount/attacker/isCrit, and `spacetime describe 2d-impact-game-fr9ti reducer attack_player --json` must list exactly targetIdentity/isSkill/comboCount params.
    5. **Regenerate + build:** `pnpm run spacetime:generate` then `pnpm build` — laragon serves the fresh `dist/` over LAN. Commit any binding churn.
    6. **Maincloud: do NOT publish** (STATE ops invariant — deferred to prod point).
  </action>
  <verify>
    <automated>spacetime describe 2d-impact-game-fr9ti --server local table pvp_hit --json && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `spacetime publish 2d-impact-game-fr9ti --module-path spacetimedb --server local --yes` exits 0 with NO wipe flags (CLI output shows an update/migrate of the existing database, not a fresh create).
    - `spacetime describe 2d-impact-game-fr9ti --server local table pvp_hit --json` output contains all four column names: target, amount, attacker, isCrit.
    - `spacetime describe 2d-impact-game-fr9ti --server local reducer attack_player --json` output contains targetIdentity, isSkill, and comboCount and contains no damage param.
    - `pnpm run spacetime:generate` exits 0; `pnpm build` exits 0.
    - Existing durable data survived: `spacetime sql 2d-impact-game-fr9ti --server local "SELECT COUNT(*) FROM player"` returns a non-zero count (migrated DB, not a fresh one).
  </acceptance_criteria>
  <done>Module published additively to local, pvp_hit + attack_player schema structurally confirmed, bindings regenerated, fresh dist/ built — ready for the human playtest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Two-client migrated-DB PVP playtest (SC2 + SC3 — the phase UAT)</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/03-pvp-crit/03-RESEARCH.md §"Common Pitfalls" 1–3 and 7 (what a regression looks like vs what is pre-existing) and §"Open Questions" 2–3 (feel checks + expected pre-existing noise).
  </read_first>
  <action>
    Everything is already deployed and built (Task 1). Hand the two-client playtest to the human with the exact checklist in how-to-verify. Do not modify code during this task; if the human reports an issue, resume into a fix iteration (Phase-2 precedent: playtest fix iterations are expected, not overrun — the checkpoint plan there took 6x an autonomous plan and found 3 real bugs).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Human types "approved" after all how-to-verify checks pass, or every reported issue is fixed and re-verified in a resume iteration.
    - Any pre-existing (non-Phase-3) observations are recorded in the SUMMARY as flagged todos, not fixed in this slice.
  </acceptance_criteria>
  <done>Human confirms: truthful PVP crit numbers on all clients with no double-draw, distinct per-character crit in PVP, shard theft resolves on a killing blow, no visual regression — resume-signal "approved".</done>
  <what-built>
    The full CRIT-07 slice is live on the migrated local DB: `attackPlayer` is intent-only (server computes base damage + rolls crit via ctx.random), the `pvp_hit` event carries {target, amount(full), attacker, isCrit}, and the client renders victim/attacker/spectator numbers from a single subscription with attacker self-suppression + crit upgrade. Laragon serves the fresh dist/ — open the game at `http://<host-LAN-ip>/` on the host + one LAN device (or two browser windows on the host as a weaker fallback), each logged into a DIFFERENT account, outside a safe zone and not in the same party.
  </what-built>
  <how-to-verify>
    Two clients, live MIGRATED local DB (existing accounts — do not reseed):

    1. **Truthful victim number (SC2).** Client A melee-hits Client B repeatedly. B sees a purple number float over themselves per hit; on a crit B sees a BIGGER purple number with a "!" suffix. Exactly ONE number per hit on B's screen (no pair of numbers ~0.16u apart — that would be the double-subscription regression).
    2. **Attacker suppression + crit upgrade (D3-05).** On A's screen: every hit shows ONE instant number over B (the local display). On a crit, A additionally sees the big magenta crit number pop at B's live position. A must NEVER see two numbers for a non-crit hit.
    3. **Spectator visibility (D2-02).** If a third client/window is available: it sees numbers over B for A's hits (magenta big on crits, normal otherwise). Skip if only two clients.
    4. **Distinct per-character crit (SC3 / CRIT-07).** A plays a high-crit dps (vesper, critRate 0.36) and lands ~20 hits on B — then switches to the low-crit tank (glacia, 0.10) for ~20 hits. Crit frequency must be visibly, obviously different (roughly 1-in-3 vs 1-in-10).
    5. **Skill-tier PVP hit.** A casts a real skill and hits B within the cast window — damage is visibly skill-scaled; A's local skill number + any event crit upgrade read sensibly together (feel check, RESEARCH Open Q2).
    6. **No phantom taken number (Pitfall 2).** When B RECEIVES a crit: B sees the purple crit number only — NOT a purple crit followed by a separate red "taken" number a moment later.
    7. **Killing blow: full amount + shard theft (SC3, D3-02).** A kills B. The final number shows the FULL hit amount (not a tiny HP-sliver). B's gems spill, the shard-theft toast/loop resolves correctly (A credited per the shard rules), and B respawns at spawn. B's account/progress is intact after respawn.
    8. **Un-spoofability spot-check (best-effort).** There is no client code path that sends a damage number for PVP anymore — optionally confirm via devtools that the reducer call carries only targetIdentity/isSkill/comboCount. (Full modified-client testing not required; the arg simply no longer exists.)
    9. **Known pre-existing noise — do NOT report as regressions:** doubled remote skill/projectile VFX (skillCast/rangedAttack are still double-subscribed — flag for a cleanup todo), and an occasional attacker-only local number with no HP change when a hit is server-rejected at the range/safe-zone edge (Pitfall 7).
  </how-to-verify>
  <resume-signal>Type "approved" if checks 1–7 all hold, or describe the issue (e.g. double-drawn victim number, no crit upgrade for the attacker, phantom taken number, sliver killing-blow amount, shard theft broken, crit frequency not distinct).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → live local DB (real accounts/progress) | Additive migrate on a populated DB; a mistaken wipe flag would irreversibly destroy account/account_link logins (no restore path). |
| stale client build → new module | A dist/ built against the old attackPlayer signature calling the new module (or vice versa) silently kills PVP (reducer-arg mismatch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Denial of Service (data loss) | local migrate-publish | high | mitigate | Additive publish only, wipe flags prohibited (frontmatter prohibition + explicit command in Task 1); post-publish player-count SQL check proves the DB survived. |
| T-03-08 | Tampering (integrity) | stale client/module contract | medium | mitigate | Fixed deploy order (publish → generate → build) executed in one task; playtest clients load the fresh dist/; describe-based structural check confirms the live reducer shape before the playtest. |
| T-03-04 | Tampering (rate) | attackPlayer call spam | low | accept | Carried from Plan 03-01: accepted residual per locked D3-06; per-hit values are un-spoofable, rate-shaping deferred. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (zero-new-deps milestone lock; RESEARCH audit not applicable). |
</threat_model>

<verification>
- Publish exit 0 on the migrated DB (no wipe); describe shows the 4-column pvp_hit + 3-param attack_player; player table non-empty post-migrate.
- `pnpm run spacetime:generate` + `pnpm build` green.
- Human playtest checks 1–7 approved (SC2: truthful crit on every client, one number per event; SC3: distinct per-character PVP crit, shard theft on killing blow, no visual regression).
</verification>

<success_criteria>
- CRIT-07 verified live: PVP damage + crit are server-computed and un-spoofable; the truthful crit number floats over the victim on every subscribed client.
- Two-client playtest on the MIGRATED DB approved with the shard-theft loop intact on a killing blow.
- Maincloud untouched; local DB data intact; phase ready for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/03-pvp-crit/03-02-SUMMARY.md` when done.
</output>
