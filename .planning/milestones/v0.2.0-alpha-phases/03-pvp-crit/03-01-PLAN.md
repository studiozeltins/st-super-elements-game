---
phase: 03-pvp-crit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/index.ts
  - src/module_bindings
  - src/game/createGame.ts
  - src/game/combat/damageKind.ts
  - src/game/systems/createDamageNumbers.ts
  - src/App.tsx
autonomous: true
requirements: [CRIT-07]
must_haves:
  truths:
    - "attackPlayer accepts intent only ({ targetIdentity, isSkill, comboCount }) — the client-authored damage arg is gone from the reducer, the bindings, and every client caller; the server composes the hit via resolvePlayerHit (base → × crit via ctx.random → clamp), so a modified client cannot inflate PVP damage or fake a PVP crit (CRIT-07, D3-04)."
    - "pvp_hit carries { target, amount, attacker, isCrit } where amount is the FULL computed hit inserted PRE-branch — killing blows float full hit strength, ARPG-style (D3-01, D3-02); HP application still uses dealt = min(amount, currentHealth)."
    - "The victim's client floats 'pvp' (purple) or 'pvpCrit' (purple, 1.7x, '!' suffix) from the event; the attacker suppresses its own non-crit echo (the instant local display number in applyPvpDamage already covered it) and upgrades to the magenta 'crit' number at the victim's live position on isCrit; spectators render event-driven numbers (D3-05, D2-01/D2-02)."
    - "The pvp_hit event table is subscribed exactly ONCE on the client (its useTable only; removed from the manual subscribe list) — one number per event per viewer, no double-draw."
    - "The kill path (gem spill → shard theft → respawn, index.ts ~1124–1162 incl. the [A1] shard-conservation block) is byte-for-byte untouched and still consumes dealt (SC3)."
    - "pnpm build, pnpm test (full suite incl. serverSync INV-5 parity), and the module build (cd spacetimedb && spacetime build) are all green."
  artifacts:
    - spacetimedb/src/index.ts
    - src/App.tsx
    - src/game/createGame.ts
    - src/game/combat/damageKind.ts
    - src/game/systems/createDamageNumbers.ts
    - src/module_bindings
  key_links:
    - "attackPlayer server signature ↔ regenerated src/module_bindings ↔ Network.sendAttackPlayer(target, isSkill, comboCount) ↔ App.tsx reducer binding — ONE atomic slice; a half-migrated pair is the 'no such reducer' failure class."
    - "resolvePlayerHit(ctx, attacker, isSkill, clampedCombo, 'melee') is the ONLY damage composition for PVP — no inline math, no second clamp (MAX_HIT_DAMAGE clamp lives inside at ~1891), no resistance profile (D3-03)."
    - "pvpHit.insert sits PRE the survivor/fatal branch with the full amount; dealt is computed AFTER the insert (Phase-2 e368fda convention — fatal hits must float a number)."
    - "spawnSelfNumber netting covers BOTH 'pvp' and 'pvpCrit' kinds so syncMyServerRow never re-floats a received PVP crit as a phantom 'taken' number."
  prohibitions:
    - "No PLAYER_RESISTANCES profile on the PVP resolvePlayerHit call (D3-03 — PVP resist tuning is a deferred balance rework)."
    - "No server-side hit-rate cooldown and no server-authoritative combo tracking (D3-06 — deferred; only the existing D2-04 clamp applies)."
    - "No miss/evasion RNG anywhere (explicit pending USER decision — out of scope)."
    - "Determinism grep-gate stays green: this plan adds ZERO new RNG or wall-clock call sites in spacetimedb/src (ctx.random via resolvePlayerHit is the sole randomness)."
---

<objective>
Extend the Phase-2 server-authoritative damage/crit path to PVP (CRIT-07): rework `attackPlayer` to the intent signature (D3-04), widen the `pvp_hit` event table additively with `attacker` + `isCrit` (D3-01) carrying the full computed amount (D3-02), call `resolvePlayerHit` with no resistance profile (D3-03), and rework the client so the victim/attacker/spectator all render truthful event-driven numbers with attacker self-suppression and crit upgrade (D3-05). The D2-03 skill window and D2-04 combo clamp carry automatically inside `resolvePlayerHit`. No new enforcement beyond that (D3-06).

Purpose: closes the last client-authored damage value in the game — the live cheat incentive on the PVP shard-theft loop.
Output: reworked `attackPlayer` + extended `pvp_hit` (server), regenerated bindings, intent-shaped client plumbing, and the 3-way PVP number rendering — all in one slice, build + full suite green. Deploy and playtest happen in Plan 03-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pvp-crit/03-CONTEXT.md
@.planning/phases/03-pvp-crit/03-RESEARCH.md
@.planning/phases/03-pvp-crit/03-PATTERNS.md

# Style-map analog for the new victim crit kind:
@src/game/combat/damageKind.ts
</context>

## Artifacts this phase produces

- **Reducer arg change:** `attackPlayer` args become `{ targetIdentity: t.identity(), isSkill: t.bool(), comboCount: t.u32() }` — the `damage: t.u32()` arg is deleted (D3-04).
- **Event-table columns:** `pvp_hit` gains `attacker: t.identity().default(Identity.zero())` and `isCrit: t.bool().default(false)`, appended at the END after `amount` (D3-01, additive-migrate rule).
- **New client kind:** `DamageKind` union member `'pvpCrit'` + `DAMAGE_KIND_STYLES` entry `pvpCrit: { cssColor: '#c07dff', fontScale: 1.7 }` (victim purple crit variant).
- **New game-handle method:** `spawnPlayerNumber(identityHex: string, amount: number, kind: DamageKind): void` on the `Game` interface in `src/game/createGame.ts` — identity-anchored number at a remote player's live position.
- **Client interface change:** `Network.sendAttackPlayer(target: Player, isSkill: boolean, comboCount: number): void` (was `(target, damage)`).
- **Regenerated:** `src/module_bindings/` via `pnpm run spacetime:generate` (never hand-edited).
- **No new files, no new test files** (RESEARCH Wave 0: zero gaps — `resolvePlayerHit` inputs are already pure-tested).

<tasks>

<task type="auto">
  <name>Task 1: Server — extend pvp_hit additively and rework attackPlayer to intent + resolvePlayerHit</name>
  <files>spacetimedb/src/index.ts</files>
  <read_first>
    - .planning/phases/03-pvp-crit/03-PATTERNS.md §"extend pvp_hit" and §"attackPlayer rework" — the exact before/after excerpts with verified line numbers.
    - spacetimedb/src/index.ts:1–10 (imports — `Identity` is already imported from 'spacetimedb' at line 2), :206 (`MAX_HIT_RANGE`), :211-ish (`MAX_COMBO_FOR_GEMS`), :535–590 (the `pvpHit` table at ~541–548 and the `enemyHit` payload analog at ~569–578).
    - spacetimedb/src/index.ts:1085–1170 — the current `attackPlayer` (~1092–1164): guards ~1095–1113, the client-trusted damage lines ~1115–1117, kill path ~1119–1162 incl. the [A1] shard-conservation comment block ~1144–1157.
    - spacetimedb/src/index.ts:1855–1965 — `resolvePlayerHit` (~1862–1893, note the clamp at ~1891 — do NOT re-clamp in the reducer) and `attackEnemies` (~1899–1948, the shipped intent-reducer analog: arg shape ~1900–1906, combo clamp idiom ~1917, pre-branch `enemyHit.insert` ~1926–1935).
  </read_first>
  <action>
    1. **Extend `pvp_hit` (D3-01).** In the `pvpHit` table definition (~541–548), keep `target: t.identity()` and `amount: t.u32()` in place, then APPEND at the END (order matters for automatic migration): `attacker: t.identity().default(Identity.zero())` and `isCrit: t.bool().default(false)`. `Identity` needs no new import (index.ts:2).
    2. **Rework `attackPlayer` args (D3-04).** Replace the current arg object with `{ targetIdentity: t.identity(), isSkill: t.bool(), comboCount: t.u32() }` — the client-authored damage arg is deleted. Destructure accordingly in the callback.
    3. **Guards stay byte-for-byte** (~1095–1113): `requirePlayer(ctx)`, target lookup, self-attack, party friendly-fire, both safe-zone checks, and the `distanceBetweenPlayers(attacker, target) > MAX_HIT_RANGE` check. Keep the existing throw-on-guard behavior (`SenderError`) exactly as-is — RESEARCH Pitfall 7 documents it as pre-existing and intentional.
    4. **Swap the damage computation.** Delete the two client-trusted lines and the dealt-amount insert (~1115–1117). Replace with, in this exact order: (a) `const combo = Math.min(comboCount, MAX_COMBO_FOR_GEMS);` (D2-04 clamp, same idiom as attackEnemies ~1917); (b) `const { amount, isCrit } = resolvePlayerHit(ctx, attacker, isSkill, combo, 'melee');` — NO resistance profile argument (D3-03; the channel is inert without a profile); (c) `ctx.db.pvpHit.insert({ target: targetIdentity, attacker: attacker.identity, amount, isCrit });` — PRE-branch, full computed amount (D3-02, Phase-2 e368fda convention); (d) `const dealt = Math.min(amount, target.currentHealth);`. Do not add a second MAX_HIT_DAMAGE clamp — it already lives inside resolvePlayerHit (~1891).
    5. **Kill path untouched (SC3).** Everything from `const remainingHealth = target.currentHealth - dealt;` (~1119) through `respawnPlayerAtSpawn` (~1158–1162), including the [A1] shard-conservation block, stays byte-for-byte — it consumes `dealt` exactly as before.
    6. **Audit ALL insert sites.** Grep the whole repo for `pvpHit.insert` — Phase-2 surprise: restore/seed/debug paths can hold compile-time-only insert sites. RESEARCH verified exactly ONE site (~1117, the one being reworked) and `pvp_hit` is not in the backup/restore set, but re-verify with a fresh grep and backfill any other site with `attacker` + `isCrit` (`.default()` does NOT relax the TS insert type — Phase-2 learning).
    7. Build the module: `cd spacetimedb && spacetime build` (do NOT pass a project-path flag — this machine's CLI rejects it). Fix until green.
  </action>
  <verify>
    <automated>cd spacetimedb && spacetime build</automated>
  </verify>
  <acceptance_criteria>
    - `cd spacetimedb && spacetime build` exits 0.
    - `grep -c "Identity.zero()" spacetimedb/src/index.ts` prints >= 1 (the new attacker column default).
    - The `pvpHit` table definition lists columns in the order target, amount, attacker, isCrit (new columns appended at the end, each with a `.default(...)`).
    - The `attackPlayer` reducer's arg object contains exactly the keys `targetIdentity`, `isSkill`, `comboCount` (source assertion — no numeric damage input remains).
    - The `attackPlayer` body contains one `resolvePlayerHit(` call whose last argument is `'melee'` (5 args total — no profile), and one `ctx.db.pvpHit.insert(` call that appears textually BEFORE the `remainingHealth` computation and passes `amount` (not `dealt`).
    - `grep -c "pvpHit.insert" spacetimedb/src/index.ts` prints the same count as before the edit (no new insert sites; the existing one reworked in place).
    - The kill-path region (~1119–1162) shows no diff except the variable feeding it (`dealt` still computed from `Math.min(amount, target.currentHealth)`).
    - `grep -rn "Math\.random\|Date\.now" spacetimedb/src` prints no matches (pre-existing determinism gate — this task must not regress it).
  </acceptance_criteria>
  <done>`pvp_hit` extended additively (append-at-end + defaults), `attackPlayer` is intent-shaped and routes through `resolvePlayerHit` with the pre-branch full-amount event insert, kill path untouched, module build green.</done>
</task>

<task type="auto">
  <name>Task 2: Client intent plumbing — regenerate bindings, rework sendAttackPlayer end-to-end</name>
  <files>src/module_bindings, src/game/createGame.ts, src/App.tsx</files>
  <read_first>
    - .planning/phases/03-pvp-crit/03-PATTERNS.md §"createGame.ts — intent send" and §"App.tsx" Analog 4 (reducer binding) — exact excerpts with line numbers.
    - src/game/createGame.ts:68–111 — the `Network` interface: `sendAttackPlayer` at ~74 (currently `(target: Player, damage: number)`), the `sendAttackEnemies` intent-shaped analog + its doc-comment style at ~79–90.
    - src/game/createGame.ts:368–447 — `dealDamage` (~372–390, calls applyPvpDamage at ~384), `applyRangedProjectileHit` (~404, the second caller, bow = basic), `applyPvpDamage` (~415–440: all gates ~421–432 stay; the per-hit body ~433–437 with the send + burst + local display number; `combo` is in closure scope via registerComboHit ~410–413).
    - src/game/createGame.ts:485–583 — where the display-only `damage` values are computed (basic ~501–504, skill multiplier ~572–574) — these SURVIVE as the D3-05 local display number.
    - src/App.tsx:618–642 — the network bindings: `sendAttackPlayer` at ~629–630 (to rework) and the `sendAttackEnemies` binding shape at ~634–635 (the analog).
  </read_first>
  <action>
    1. **Regenerate bindings first:** `pnpm run spacetime:generate` (compiles the module and rewrites `src/module_bindings/` — never hand-edit). This makes `attackPlayer` expect `{ targetIdentity, isSkill, comboCount }` and `PvpHit` rows expose `attacker`/`isCrit`.
    2. **`Network` interface** (createGame.ts ~74): change `sendAttackPlayer(target: Player, damage: number): void` to `sendAttackPlayer(target: Player, isSkill: boolean, comboCount: number): void`, copying the doc-comment idiom of the adjacent `sendAttackEnemies` block (~79–83).
    3. **`applyPvpDamage`** (~415–440): add an `isSkill: boolean` parameter. All existing gates (~421–432: safe zones, party, PVP_HIT_COOLDOWN_SECONDS, PVP_MAX_HIT_RANGE, VERTICAL_HIT_GATE, radius) stay unchanged — RESEARCH's range-envelope verdict: client 45 (3D) is a strict subset of server 45 (2D XZ); leave both constants and both checks untouched. In the per-hit body swap the send to `network.sendAttackPlayer(remotePlayer.row, isSkill, combo)` (`combo` already in closure scope) and KEEP the `effectSystem.spawnBurst` and the `damageNumbers.spawn(...)` local display number line exactly as-is — that instant number is locked by D3-05 (the `damage` param survives as display-only input).
    4. **Both callers pass intent:** `dealDamage` (~384) forwards its own `isSkill` param into the `applyPvpDamage` call; `applyRangedProjectileHit` (~404) passes `false` (bow projectile = basic tier).
    5. **App.tsx binding** (~629–630): rework to `sendAttackPlayer: (target, isSkill, comboCount) => connection.reducers.attackPlayer({ targetIdentity: target.identity, isSkill, comboCount })` — mirroring the `sendAttackEnemies` binding at ~634–635. Delete any now-dead damage-rounding at this call site (no-legacy rule).
    6. Run `pnpm build && pnpm test` and fix types until green. The existing `pvpHit` onInsert callback still compiles unchanged (it reads `row.target`/`row.amount`, both still present) — its rework is Task 3.
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run spacetime:generate` exits 0 and `git status` shows `src/module_bindings/` modified (regenerated, not hand-edited).
    - `grep -c "sendAttackPlayer(target: Player, isSkill: boolean, comboCount: number)" src/game/createGame.ts` prints 1 (interface signature).
    - The `network.sendAttackPlayer(` call inside `applyPvpDamage` passes `(remotePlayer.row, isSkill, combo)` — no numeric damage argument (source assertion).
    - The `damageNumbers.spawn(` local display line inside `applyPvpDamage`'s per-hit body is still present (D3-05 — instant attacker feedback kept).
    - `applyRangedProjectileHit` passes `false` as the isSkill argument to `applyPvpDamage` (source assertion).
    - The App.tsx `attackPlayer` reducer call passes `{ targetIdentity, isSkill, comboCount }` (source assertion).
    - `PVP_MAX_HIT_RANGE` (createGame.ts) and its gate are textually unchanged from before this task (range envelope untouched).
    - `pnpm build` exits 0 and `pnpm test` exits 0 (full suite incl. `serverSync.test.ts` INV-5 parity — no new mirrored constants expected).
  </acceptance_criteria>
  <done>Bindings regenerated; `sendAttackPlayer` is intent-shaped through the interface, both call sites, and the App.tsx binding; the attacker's local display number survives; build + full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Client rendering — pvpCrit kind, spawnPlayerNumber handle, single-subscription 3-way pvp_hit branch, netting guard</name>
  <files>src/game/combat/damageKind.ts, src/game/systems/createDamageNumbers.ts, src/game/createGame.ts, src/App.tsx</files>
  <read_first>
    - .planning/phases/03-pvp-crit/03-PATTERNS.md §"damageKind.ts", §"createDamageNumbers.ts", §"createGame.ts — new handle method + netting", and §"App.tsx" Analogs 1–3 — exact excerpts.
    - src/game/combat/damageKind.ts (whole, small) — the `DamageKind` union (~2–10) and `DAMAGE_KIND_STYLES` with the `taken`/`takenCrit`/`pvp` precedent (~25–27).
    - src/game/systems/createDamageNumbers.ts:40–68 — the `drawNumber` label ternary (~53–58) that appends '!' for crit-family kinds.
    - src/game/createGame.ts:113–132 (the `Game` interface — `spawnSelfNumber`/`spawnWorldNumber`/`flashRemoteHealth` trio at ~126–130) and :1020–1084 (the handle impls: `spawnSelfNumber` + its PVP netting at ~1060–1064, `spawnWorldNumber` ~1065–1067, `flashRemoteHealth` ~1068–1071, and `syncMyServerRow` ~1027–1046 which consumes the netting).
    - src/App.tsx:108–247 — the manual `.subscribe([...])` list (~112–136; the pvp_hit entry at ~122), the `rangedAttack` self-skip idiom (~170–175), the current `pvpHit` useTable callback (~201–215: the `lastPvpHitOnMeAtRef` mark at ~208 and `flashRemoteHealth` at ~212 must BOTH survive), and the `enemyHit` one-subscription comment block (~223–226).
  </read_first>
  <action>
    1. **`damageKind.ts`:** add `'pvpCrit'` to the `DamageKind` union and `pvpCrit: { cssColor: '#c07dff', fontScale: 1.7 }` to `DAMAGE_KIND_STYLES` — the `takenCrit` precedent (same color as its base kind, 1.7 scale). Purple stays the "a player hit you" semantic; attacker/spectator crits reuse the existing magenta `'crit'` style. The `Record<DamageKind, ...>` type makes a forgotten style entry a compile error.
    2. **`createDamageNumbers.ts`:** extend the `drawNumber` label condition (~53–58) so `'pvpCrit'` also gets the `!` suffix, alongside the two kinds already listed there. One condition; no other renderer change.
    3. **`createGame.ts` — new handle:** add `spawnPlayerNumber(identityHex: string, amount: number, kind: DamageKind): void` to the `Game` interface (next to the trio at ~126–130) and implement it next to `flashRemoteHealth` (~1068–1071): look up `remotePlayers.get(identityHex)`; if missing, return silently (victim despawned); else spawn via `damageNumbers.spawn` at the view's model group position raised by +1 on Y (mirror the `spawnWorldNumber` + `flashRemoteHealth` halves, per PATTERNS excerpt).
    4. **`createGame.ts` — netting guard (RESEARCH Pitfall 2, load-bearing):** in `spawnSelfNumber` (~1060–1064), the PVP-netting condition currently matches only the base pvp kind — extend it to also match `'pvpCrit'` so `pvpDamageSinceSync` accrues for both. Missing this makes every received PVP crit double-float a phantom red taken number via `syncMyServerRow`.
    5. **`App.tsx` — one-subscription rule:** delete the pvp_hit entry from the manual `.subscribe([...])` array (~122). Its `useTable` (~201) opens its own subscription; leaving both delivers every event row twice and double-draws every PVP number (the exact Phase-2 enemy_hit bug — see the comment block at ~223–226). Do NOT touch the other double-subscribed event tables (skillCast/rangedAttack/healEvent) — pre-existing, out of scope; flag in the SUMMARY as a cleanup-todo candidate.
    6. **`App.tsx` — 3-way branch (D3-05, D2-02):** rework the `pvpHit` onInsert per the PATTERNS Analog-3 excerpt: (a) victim branch (`row.target` hex equals `myIdentityRef.current`): keep the `lastPvpHitOnMeAtRef.current = performance.now()` mark (shard-theft toast disambiguation), then `spawnSelfNumber(row.amount, row.isCrit ? 'pvpCrit' : 'pvp')`, return; (b) non-victim: keep `flashRemoteHealth(targetHex)` (victim health-bar reveal), then attacker branch (`row.attacker` hex equals mine): suppress non-crit (already drawn locally in applyPvpDamage — return), on `row.isCrit` call `spawnPlayerNumber(targetHex, row.amount, 'crit')` (the upgrade at the victim's live position), return; (c) spectator: `spawnPlayerNumber(targetHex, row.amount, row.isCrit ? 'crit' : 'normal')` (D2-02 full shared visibility). Suppression lives HERE in the App.tsx callback (mirrors the rangedAttack self-skip idiom), not in createDamageNumbers.
    7. Run `pnpm build && pnpm test` green.
  </action>
  <verify>
    <automated>pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pvpCrit" src/game/combat/damageKind.ts` prints 2 (union member + style entry).
    - `grep -c "pvpCrit" src/game/systems/createDamageNumbers.ts` prints 1 (the label condition).
    - `grep -c "pvpCrit" src/game/createGame.ts` prints >= 1 (the netting condition in spawnSelfNumber matches both PVP kinds).
    - `grep -c "spawnPlayerNumber" src/game/createGame.ts` prints 2 (interface declaration + implementation).
    - `grep -c "tables\.pvpHit" src/App.tsx` prints exactly 1 (the useTable only — removed from the manual subscribe list). Do not add code comments naming that table handle.
    - The reworked onInsert keeps both pre-existing behaviors: `lastPvpHitOnMeAtRef` is still assigned in the victim branch, and `flashRemoteHealth` is still called for non-victim viewers (source assertions).
    - The attacker branch contains an early return for the non-crit case BEFORE any spawn call (suppression, D3-05) and calls `spawnPlayerNumber` with kind `'crit'` only when `row.isCrit` is true (source assertion).
    - `pnpm build` exits 0 and `pnpm test` exits 0.
  </acceptance_criteria>
  <done>Victim purple + pvpCrit numbers, attacker suppression + crit upgrade at the victim's live position, spectator numbers, single pvp_hit subscription, and the netting guard all in place; build + full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → attackPlayer reducer | Untrusted intent (targetIdentity, isSkill, comboCount) crosses here; the server validates and computes all values. |
| server pvp_hit event → all clients | Server-owned {amount, isCrit, attacker} truth crosses to every subscribed client; rendering only — HP was already applied server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | attackPlayer damage input | high | mitigate | CLOSED THIS PLAN: the damage arg is deleted; the server composes via resolvePlayerHit (base from mirrored CHARACTER_COMBAT/WEAPONS, crit via ctx.random, MAX_HIT_DAMAGE clamp retained as defense-in-depth). |
| T-03-02 | Tampering | isSkill flag spam for skill-tier PVP damage | medium | mitigate | skillGrantActive window (D2-03) applies automatically inside resolvePlayerHit — out-of-window skill claims downgrade to basic; zero new code. |
| T-03-03 | Tampering | unbounded comboCount inflating the uncapped skill multiplier | medium | mitigate | Math.min(comboCount, MAX_COMBO_FOR_GEMS) clamp (D2-04) applied in attackPlayer before resolvePlayerHit, same idiom as attackEnemies. |
| T-03-04 | Tampering (rate) | attackPlayer call spam (DPS inflation; client cooldown only) | low | accept | ACCEPTED RESIDUAL per locked D3-06 — per-hit amount + crit are now un-spoofable; rate-shaping deferred (recorded in CONTEXT Deferred Ideas). |
| T-03-05 | Spoofing | attacker identity in the pvp_hit event | medium | mitigate | attacker column filled from the ctx.sender-derived player row (requirePlayer), never from a reducer argument. |
| T-03-06 | Information disclosure | pvp_hit public event visibility | low | accept | Public by design — D2-02 full shared visibility is a locked user choice; payload carries no secrets. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs this phase (RESEARCH §Package Legitimacy Audit — not applicable; zero-new-deps milestone lock). |
</threat_model>

<verification>
- `cd spacetimedb && spacetime build` green after Task 1; `pnpm build && pnpm test` green after Tasks 2 and 3 (full suite incl. serverSync INV-5 parity, crit/damage/skillGate unit suites, deathPenalty shard-conservation regression gate).
- Grep gates: no client-authored damage remains in the PVP path; pvp_hit subscribed once; pvpCrit present in kind union, style map, label check, and netting; determinism gate (no wall-clock/RNG in spacetimedb/src) unregressed.
- Kill path region byte-for-byte untouched (SC3 precondition for Plan 03-02's playtest).
</verification>

<success_criteria>
- CRIT-07 code-complete: `attackPlayer` computes base damage server-side, rolls crit via ctx.random, and emits the extended pvp_hit event; the client sends intent only (SC1).
- All three viewer roles render truthful PVP numbers from the single-subscription event with attacker suppression + crit upgrade (SC2, pending live confirmation in Plan 03-02).
- Whole slice atomic: server signature + bindings + every client caller reworked together; no legacy damage plumbing remains.
</success_criteria>

<output>
Create `.planning/phases/03-pvp-crit/03-01-SUMMARY.md` when done.
</output>
